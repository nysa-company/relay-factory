# T-178 — inspect one Relay job safely while offline

State: Blocked-Escalated
Resume-State: Planning
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-178
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/dead-jobs.test.js
Authentication-Seams: none
Builder ownership: app/tools/job-inspect.js only
Test-author ownership: app/tests/job-inspect.test.js only

## Spec'd description

Add `app/tools/job-inspect.js`, a zero-dependency Node 18+ command that reads
one persisted Relay state file while the service is offline and selects one job
by exact ID. It emits only the stored job's `id`, `eventId`, `status`,
`attempts`, `retries`, and `attemptsSinceRetry`. This gives an operator a
deterministic lifecycle lookup without starting Relay, changing state, or
exposing `lastError`, event payloads, approval actions, or outbox contents.

Product-doc sources:

- [`SPEC.md` behavior 2](../../SPEC.md#behavior-spec) defines the background
  job lifecycle, three-attempt dead-letter boundary, and visible job attempts.
- [`SPEC.md` behaviors 6-7](../../SPEC.md#behavior-spec) define persisted
  on-disk state and the job collection this command reads offline.
- [`SPEC.md` conformance cases 3-4 and 7](../../SPEC.md#test-cases-the-conformance-suite)
  require attempts to be recorded, permanently failing jobs to remain visible
  as dead, and persisted work to survive restart.
- The current [`app/server.js` durable-state model](../../app/server.js)
  establishes the exact stored job fields, statuses, retry counters, and
  job-to-event `eventId` link.
- [`AGENTS.md` Boundaries](../../AGENTS.md#boundaries) keeps the app
  zero-dependency, and [`AGENTS.md` Verification](../../AGENTS.md#verification)
  defines the later full-suite and immutability checks.

The command is a read-only projection over the existing state file. It does
not start the service, process or retry a job, emit an event or approval,
write an outbox record, or change the server, persisted schema, worker,
package manifest, dependencies, UI, or Factory controls.

Product issue: https://github.com/nysa-company/relay-factory/issues/144

## Acceptance criteria

1. Given Frozen Fixture A and job ID `job-event-alpha`, each of two invocations
   of `node app/tools/job-inspect.js <fixture-a-path> job-event-alpha`
   completes within 5 seconds, exits `0`, writes exactly
   `{"id":"job-event-alpha","eventId":"event-alpha","status":"dead","attempts":6,"retries":1,"attemptsSinceRetry":3}\n`
   to stdout, and writes zero stderr bytes. Every frozen sensitive token and
   unselected job ID is absent from both streams, and Fixture A's bytes,
   numeric mode, size, nanosecond modification time, and sorted
   parent-directory entries are unchanged after each invocation.
2. Each frozen lookup miss—`job-missing` and case-distinct
   `Job-event-alpha` against Fixture A, and `job-missing` against Frozen
   Fixture B—completes within 5 seconds, exits `1`, writes zero stdout bytes,
   and writes exactly `job-inspect: job not found\n` to stderr. Both existing
   fixtures and their parent directories remain unchanged by the observations
   named in criterion 1.
3. Each frozen arity case—zero, one, and three positional arguments—completes
   within 5 seconds, exits `2`, writes zero stdout bytes, and writes exactly
   `usage: node app/tools/job-inspect.js <state-file> <job-id>\n` to stderr.
   The one-argument case supplies an absent candidate state path, which
   remains absent, and each case leaves its fresh temporary parent empty.
4. For both frozen read failures, an absent path and a directory path, the
   command completes within 5 seconds, exits `1`, writes zero stdout bytes,
   and writes exactly `job-inspect: cannot read state file\n` to stderr. The
   absent path remains absent; the directory's type, numeric mode, size,
   nanosecond modification time, and entries remain identical; neither
   parent gains or loses an entry.
5. Given Frozen Fixture C, the command completes within 5 seconds, exits `1`,
   writes zero stdout bytes, writes exactly
   `job-inspect: state file is not valid JSON\n` to stderr, does not emit
   `MALFORMED-STATE-MUST-NOT-LEAK`, and leaves the fixture and parent
   directory unchanged by the observations named in criterion 1.
6. For every S1-S22 frozen structural-invalid fixture, the command completes
   within 5 seconds, exits `1`, writes zero stdout bytes, writes exactly
   `job-inspect: invalid Relay state\n` to stderr, and leaves that fixture and
   its parent directory unchanged by the observations named in criterion 1.
   Before invocation, the acceptance test proves Fixtures A-C and S1-S22 are
   pairwise byte-distinct.
7. The acceptance test statically proves that the Builder file's complete
   CommonJS module-specifier set is exactly `node:fs`, its only `node:fs`
   operation is `readFileSync`, and its source contains no `server.js`,
   `fetch(`, `WebSocket`, `node:http`, `node:https`, `node:net`,
   `node:child_process`, `setTimeout`, or `setInterval` reference. Together
   with criteria 1-6's process and filesystem assertions, every command path
   is offline, self-terminating, deterministic, and read-only.
8. Git history contains the complete test-author commit before the Builder
   commit. Apart from role-owned ticket-log appends, the test-author commit
   creates only `app/tests/job-inspect.test.js` and the Builder commit creates
   only `app/tools/job-inspect.js`. The targeted command
   `node --test app/tests/job-inspect.test.js`, `npm test --prefix app`, and
   `.github/scripts/test-immutability-check.sh`, `scripts/repo-check`, and
   `scripts/secret-scan` each exit `0` after implementation; existing
   application files, `app/server.js`, `app/package.json`, lockfiles,
   dependencies, persisted schema, `factory/KIT_PIN`, and Factory controls
   are unchanged by both stages.

## Frozen contract — version 1

- **Freeze result:** PASS. Contract version 1 is frozen.

This is the exact interface for the test-author and Builder. If it proves
wrong, return T-178 to Ready and append a higher-numbered frozen contract;
version 1 must not be edited or removed.

### Command-line interface and errors

- Repository-root invocation is exactly
  `node app/tools/job-inspect.js <state-file> <job-id>`.
- Exactly two positional arguments are accepted.
- Zero, one, or three positional arguments produce exit code `2`, empty
  stdout, and exactly this single LF-terminated stderr line:

  ```text
  usage: node app/tools/job-inspect.js <state-file> <job-id>
  ```

- The command reads `<state-file>` directly as UTF-8. Any read failure,
  including an absent path, a directory in place of a file, or an OS read
  denial, produces exit code `1`, empty stdout, and exactly:

  ```text
  job-inspect: cannot read state file
  ```

- Bytes rejected by `JSON.parse` produce exit code `1`, empty stdout, and
  exactly:

  ```text
  job-inspect: state file is not valid JSON
  ```

- A parsed value that fails the frozen structural rules produces exit code
  `1`, empty stdout, and exactly:

  ```text
  job-inspect: invalid Relay state
  ```

- A valid state with no job whose stored `id` is strictly equal to the
  supplied `<job-id>` produces exit code `1`, empty stdout, and exactly:

  ```text
  job-inspect: job not found
  ```

- Validation precedence is argument count, read, JSON parse, complete
  structural validation, then job lookup.
- Errors never contain the state path, job ID, OS error, parser text, input
  bytes, stack trace, event content, `lastError`, approval content, outbox
  content, or a second line. Every displayed error is terminated by exactly
  one LF.
- No path is written, renamed, deleted, chmodded, or created.

### Structurally valid state and successful output

For this command, structurally valid state is a non-null, non-array JSON object
with own `events`, `jobs`, `approvals`, and `outbox` properties whose values
are arrays.

Every `jobs` element must be a non-null, non-array object with these own
properties:

- non-empty string `id`;
- non-empty string `eventId`;
- string `status` equal to exactly `pending`, `done`, or `dead`; and
- non-negative integer `attempts`, `retries`, and `attemptsSinceRetry`.

Job IDs must be pairwise distinct. Contents of `events`, `approvals`, and
`outbox`, additional top-level properties, and additional job properties are
opaque to structural validation. In particular, `lastError` may be present
but is never copied to output.

The lookup uses exact, case-sensitive JavaScript string equality and does not
trim, normalize, decode, or case-fold either ID. Success exits `0`, writes
zero stderr bytes, and writes one compact JSON object plus exactly one LF. Its
only keys, in order, are:

```json
{"id":<stored id>,"eventId":<stored eventId>,"status":<stored status>,"attempts":<stored attempts>,"retries":<stored retries>,"attemptsSinceRetry":<stored attemptsSinceRetry>}
```

Numbers are copied without conversion. No endpoint, HTTP shape, route, UI
selector, or global page-shell text is introduced.

### Frozen Fixture A — mixed job states and sensitive fields

The test writes this exact parsed value to an isolated temporary `state.json`
using `JSON.stringify(value, null, 2) + "\n"`:

```json
{
  "events": [
    {
      "id": "event-alpha",
      "type": "meeting",
      "payload": {
        "private": "EVENT-PAYLOAD-MUST-NOT-LEAK"
      },
      "receivedAt": "2026-08-09T12:00:00.000Z"
    }
  ],
  "jobs": [
    {
      "id": "job-pending",
      "eventId": "event-pending",
      "status": "pending",
      "attempts": 0,
      "lastError": null,
      "retries": 0,
      "attemptsSinceRetry": 0
    },
    {
      "id": "job-event-alpha",
      "eventId": "event-alpha",
      "status": "dead",
      "attempts": 6,
      "lastError": "SELECTED-LAST-ERROR-MUST-NOT-LEAK",
      "retries": 1,
      "attemptsSinceRetry": 3
    },
    {
      "id": "job-done",
      "eventId": "event-done",
      "status": "done",
      "attempts": 2,
      "lastError": "UNSELECTED-LAST-ERROR-MUST-NOT-LEAK",
      "retries": 1,
      "attemptsSinceRetry": 1
    }
  ],
  "approvals": [
    {
      "private": "APPROVAL-CONTENT-MUST-NOT-LEAK"
    }
  ],
  "outbox": [
    {
      "private": "OUTBOX-CONTENT-MUST-NOT-LEAK"
    }
  ],
  "metadata": {
    "private": "METADATA-MUST-NOT-LEAK"
  }
}
```

Expected stdout is exactly this compact JSON line plus one final LF:

```text
{"id":"job-event-alpha","eventId":"event-alpha","status":"dead","attempts":6,"retries":1,"attemptsSinceRetry":3}
```

The frozen sensitive-token set is every Fixture A string ending
`-MUST-NOT-LEAK`. The unselected-job-ID set is `job-pending` and `job-done`.

### Frozen Fixture B — empty valid state

Fixture B's exact UTF-8 bytes are:

```text
{"events":[],"jobs":[],"approvals":[],"outbox":[]}
```

followed by one LF.

### Frozen Fixture C — malformed JSON

Fixture C's exact UTF-8 bytes are:

```text
{"jobs":[],"secret":"MALFORMED-STATE-MUST-NOT-LEAK"
```

followed by one LF. It is malformed because its object is not closed.

### Frozen read-failure paths and arity inputs

Each case uses a fresh temporary parent:

- `<tmp>/missing-state.json` does not exist before the read-failure
  invocation.
- `<tmp>/state-directory` exists as an empty directory, not a regular file.
- The zero-argument arity case supplies no path. The one-argument arity case
  supplies absent `<tmp>/candidate-state.json`. The three-argument case
  supplies absent `<tmp>/candidate-state.json`, `job-event-alpha`, and
  `extra`. Argument validation must leave each parent empty.

### Frozen structural-invalid fixtures

Each table value is written directly as the displayed compact UTF-8 JSON plus
one LF. No invalid fixture is created by casing, normalization, or mutation.

| ID | Exact JSON value | Violated rule |
|---|---|---|
| S1 | `null` | root is null |
| S2 | `[]` | root is an array |
| S3 | `{"events":[],"jobs":[],"approvals":[]}` | missing own `outbox` array |
| S4 | `{"events":{},"jobs":[],"approvals":[],"outbox":[]}` | `events` is not an array |
| S5 | `{"events":[],"jobs":{},"approvals":[],"outbox":[]}` | `jobs` is not an array |
| S6 | `{"events":[],"jobs":[],"approvals":{},"outbox":[]}` | `approvals` is not an array |
| S7 | `{"events":[],"jobs":[],"approvals":[],"outbox":{}}` | `outbox` is not an array |
| S8 | `{"events":[],"jobs":[null],"approvals":[],"outbox":[]}` | job is null |
| S9 | `{"events":[],"jobs":[[]],"approvals":[],"outbox":[]}` | job is an array |
| S10 | `{"events":[],"jobs":[{"id":7,"eventId":"event-a","status":"dead","attempts":3,"retries":0,"attemptsSinceRetry":3}],"approvals":[],"outbox":[]}` | job `id` is not a string |
| S11 | `{"events":[],"jobs":[{"id":"","eventId":"event-a","status":"dead","attempts":3,"retries":0,"attemptsSinceRetry":3}],"approvals":[],"outbox":[]}` | job `id` is empty |
| S12 | `{"events":[],"jobs":[{"id":"job-a","eventId":7,"status":"dead","attempts":3,"retries":0,"attemptsSinceRetry":3}],"approvals":[],"outbox":[]}` | job `eventId` is not a string |
| S13 | `{"events":[],"jobs":[{"id":"job-a","eventId":"","status":"dead","attempts":3,"retries":0,"attemptsSinceRetry":3}],"approvals":[],"outbox":[]}` | job `eventId` is empty |
| S14 | `{"events":[],"jobs":[{"id":"job-a","eventId":"event-a","status":null,"attempts":3,"retries":0,"attemptsSinceRetry":3}],"approvals":[],"outbox":[]}` | job `status` is not a string |
| S15 | `{"events":[],"jobs":[{"id":"job-a","eventId":"event-a","status":"DEAD","attempts":3,"retries":0,"attemptsSinceRetry":3}],"approvals":[],"outbox":[]}` | job `status` is outside the exact enum |
| S16 | `{"events":[],"jobs":[{"id":"job-a","eventId":"event-a","status":"dead","attempts":1.5,"retries":0,"attemptsSinceRetry":3}],"approvals":[],"outbox":[]}` | job `attempts` is not an integer |
| S17 | `{"events":[],"jobs":[{"id":"job-a","eventId":"event-a","status":"dead","attempts":-1,"retries":0,"attemptsSinceRetry":3}],"approvals":[],"outbox":[]}` | job `attempts` is negative |
| S18 | `{"events":[],"jobs":[{"id":"job-a","eventId":"event-a","status":"dead","attempts":3,"retries":1.5,"attemptsSinceRetry":3}],"approvals":[],"outbox":[]}` | job `retries` is not an integer |
| S19 | `{"events":[],"jobs":[{"id":"job-a","eventId":"event-a","status":"dead","attempts":3,"retries":-1,"attemptsSinceRetry":3}],"approvals":[],"outbox":[]}` | job `retries` is negative |
| S20 | `{"events":[],"jobs":[{"id":"job-a","eventId":"event-a","status":"dead","attempts":3,"retries":0,"attemptsSinceRetry":1.5}],"approvals":[],"outbox":[]}` | job `attemptsSinceRetry` is not an integer |
| S21 | `{"events":[],"jobs":[{"id":"job-a","eventId":"event-a","status":"dead","attempts":3,"retries":0,"attemptsSinceRetry":-1}],"approvals":[],"outbox":[]}` | job `attemptsSinceRetry` is negative |
| S22 | `{"events":[],"jobs":[{"id":"job-same","eventId":"event-a","status":"pending","attempts":0,"retries":0,"attemptsSinceRetry":0},{"id":"job-same","eventId":"event-b","status":"done","attempts":1,"retries":0,"attemptsSinceRetry":1}],"approvals":[],"outbox":[]}` | job IDs are duplicated |

Before invoking the command, the acceptance test asserts that the serialized
bytes of Fixtures A-C and S1-S22 are pairwise distinct. The case-distinct
lookup ID is frozen directly and is not produced by a runtime transformation.

### File locations, protected boundaries, lifecycle, and verification

- Builder ownership is creation of `app/tools/job-inspect.js` only. It is a
  standalone CommonJS command whose complete module-specifier set is
  `node:fs`; its only filesystem operation is `readFileSync`.
- Test-author ownership is creation of `app/tests/job-inspect.test.js` only.
  Its allowed module specifiers are `node:test`, `node:assert`,
  `node:child_process`, `node:fs`, `node:path`, and `node:os`; it uses
  isolated temporary directories and child Node processes with a 5-second
  timeout.
- `app/tests/dead-jobs.test.js` is the existing protected fixture seam and is
  reference-only. Its exact-source and module-specifier assertions inspect
  only `app/tools/dead-jobs.js` and `app/tests/dead-jobs.test.js`; they do not
  constrain the new Builder file. No existing protected test imports or
  snapshots `app/tools/job-inspect.js`.
- Every test case creates its own temporary parent, writes only its frozen
  runtime fixture, snapshots exact bytes plus
  `fs.statSync(path, { bigint: true }).mode`, `.size`, and `.mtimeNs`,
  snapshots sorted parent entries, waits for the child command to terminate,
  and removes the parent recursively in `finally`. Tests share no file,
  process, port, database, table, generated identifier, sequence, counter,
  clock, or timestamp. There are no foreign keys or dependent-table cleanup
  obligations.
- Targeted verification is `node --test app/tests/job-inspect.test.js`.
  Required repository verification after implementation is
  `npm test --prefix app`, `.github/scripts/test-immutability-check.sh`,
  `scripts/repo-check`, and `scripts/secret-scan`.
- The test-author stage precedes the Builder stage. Neither stage changes an
  existing test, `app/server.js`, `app/package.json`, a lockfile, dependency,
  persisted schema, `factory/KIT_PIN`, ticket state, ledger, or Factory
  control.
- This local CLI has no endpoint, UI, cross-origin request, authentication,
  cookie, CORS, domain, credential, preview deployment, or external-send
  behavior. `External: no` remains exact.

## Criteria checklist

- **Pass/fail and unambiguous:** AC1-6 freeze exact arguments, fixture bytes,
  outputs, errors, exit codes, 5-second process bounds, and filesystem
  observations. AC7 freezes statically checkable source boundaries. AC8 is
  decided from Git history, per-stage diffs, and five exact command exits.
- **Coverage:** AC1 covers exact selection, every output field, all three
  valid statuses and counter boundary examples in Fixture A, repeat
  determinism, redaction, and read-only behavior. AC2 covers empty valid
  state, case-sensitive and absent lookup misses, and Fixture B. AC3-5 cover
  arity, read, and parse precedence. AC6 covers every structural rule and
  fixture distinctness. AC7 covers offline/read-only source boundaries; AC8
  covers files, stage ordering, and verification.
- **Derived-fixture distinctness:** A-C and S1-S22 are exact literals and are
  pairwise checked before use. No invalid serialized fixture is derived by a
  casing, normalization, or mutation operation. The case-distinct lookup ID
  is itself frozen as exact bytes.
- **Generated-value determinism:** all IDs, counters, and timestamps are
  literals. No generated identifier, sequence, counter initializer, reset, or
  clock participates in an expectation.
- **Fixture lifecycle closure:** every case has isolated setup, awaits its
  child, and performs recursive `finally` teardown. No serialized invocation
  shares a resource; there is no database, parent row, child table, foreign
  key, or cascading cleanup.
- **Protected boundaries:** all tracked protected tests were inspected. The
  associated seam's exact-source assertion is scoped to `dead-jobs.js` and
  admits no requirement for the new file, so
  `Protected-Test-Conflicts: none` is exact.
- **Global protected text:** the pinned readiness checker returned
  `GLOBAL TEXT PASS` separately for the usage line, all four error lines, and
  the exact successful output line. The command adds no UI or accessible
  name.

## Ambiguity log

1. **Exact lookup and error protocol.** The seed did not specify ID
   normalization, error text, exit codes, or validation precedence. No entry
   in [`factory/rulings.md`](../rulings.md) addresses offline inspection.
   Resolved as strict case-sensitive equality, usage exit `2`, operational
   exits `1`, four path-independent error lines, and the frozen precedence,
   matching the shipped offline-command convention.
2. **Structural-validation depth.** The product has no general state-file
   validator. Resolved narrowly from the
   [`app/server.js` durable-state model](../../app/server.js): validate all
   four collections and every job field needed for selection or output;
   enforce unique job IDs from idempotent one-job-per-event intake; leave
   events, approvals, outbox entries, `lastError`, and additive fields opaque.
3. **Counter and serialization semantics.** The seed named fields but did not
   freeze numeric validity, key order, whitespace, or line ending. Resolved
   from the persisted model as non-negative integer counters, copied numbers,
   compact `JSON.stringify` output, the listed key order, and one LF.
4. **Sensitive-data boundary.** “Operational metadata” did not supply
   fixtures proving excluded data stays private. Fixture A gives event
   payload, selected and unselected `lastError`, approval, outbox, and
   additive metadata unique `-MUST-NOT-LEAK` sentinels; exact streams make
   leakage mechanically observable.
5. **Unreadable input portability.** Permission-denial fixtures are
   unreliable under privileged users. All OS read failures share one frozen
   result; an absent path and a directory path are the portable
   representatives.
6. **Cross-ticket file ownership.** A grep of all Markdown tickets under
   `factory/tickets/` found no other declaration of either T-178-owned path.
   Active and remote leased ticket branches were inspected: T-177 owns the
   distinct event inspector, T-170 through T-172 own completed distinct
   offline-command paths, and T-163 through T-165 own `app/server.js`. No
   implementation-surface conflict exists.
7. **Protected source and global text.** All tracked tests were inspected.
   The declared `app/tests/dead-jobs.test.js` seam constrains only its own test
   and `dead-jobs.js`; no allowlist or snapshot constrains the new file. The
   pinned readiness script accepted every fixed shell-visible literal, so no
   protected-test conflict declaration or seam expansion is required.
8. **Topology, lifecycle, generated values, and external effects.** This is
   an offline local file read with isolated temporary fixtures and no
   database, generated expectation, HTTP route, auth, cookie, CORS, origin,
   credential, preview deployment, UI, approval execution, or send. No deploy
   topology or `external` label is required.

## Non-goals

No server or HTTP route, schema migration, event payload, `lastError`,
approval, outbox, package, dependency, lockfile, or Factory change is in
scope.

## Coordination

T-178 owns only `app/tools/job-inspect.js` and
`app/tests/job-inspect.test.js`. T-179 owns the separate event inspector, so
the two tickets have no implementation-surface conflict. The completed
offline commands T-173 through T-176 and stranded remote identities T-170
through T-172 use distinct paths.

## Planning log

- 2026-08-09: Planner froze contract version 1 for the exact offline command,
  job projection, deterministic success and error streams, valid and invalid
  fixtures, privacy checks, lifecycle closure, protected-source boundaries,
  and test-first stage ownership. Product docs, `factory/rulings.md`, the
  current state model, all tracked protected tests, sibling ticket
  declarations, active worktrees, remote leased ticket branches, and every
  fixed shell-visible literal were checked; no operator ruling,
  protected-test conflict, file conflict, deployment topology, or external
  label was required.
- 2026-08-09: spec-lint of frozen contract version 1 against `SPEC.md`
  (behavior spec 2, 6, 7 and conformance cases 3, 4, 7), `AGENTS.md`
  (boundaries, verification, managed repository baseline), `app/server.js`,
  `app/package.json`, every file in `app/tests/`, `.agents/repo-standard.json`,
  `.github/workflows/ci.yml`, `.github/scripts/test-immutability-check.sh`,
  `scripts/repo-check`, `scripts/secret-scan`, `scripts/artifact-check`,
  `factory/rulings.md`, sibling ticket declarations, and the pinned
  `scripts/ticket-readiness.py` at `factory/KIT_PIN`.

  Contract-element coverage:

  | Contract element (material behavior) | Positive | Failure / empty | Duplicate | Out of scope |
  |---|---|---|---|---|
  | Argument arity — exactly two positional | AC1, AC2, AC5, AC6 invoke with two arguments | AC3: zero, one, three → exit `2`, empty stdout, exact usage line, parent left empty | the three arity cases share one frozen usage literal | four-or-more arguments uncovered (finding 6) |
  | Read of `<state-file>` as UTF-8 | AC1, AC2, AC5, AC6 read frozen fixtures | AC4: absent path and directory path → exit `1`, exact `cannot read state file` | both read failures share one frozen literal | OS read-denial grouped as an equivalent read failure (ambiguity log 5) |
  | `JSON.parse` failure | — | AC5: Fixture C unclosed object → exit `1`, exact `state file is not valid JSON` | — | — |
  | Structural rules on the four top-level collections | AC1, AC2 accept Fixtures A and B | AC6: S1, S2 (non-object root), S3 (missing own `outbox`), S4-S7 (each collection non-array) | S4-S7 are one rule applied per collection | missing `events`, `jobs`, `approvals` and scalar roots (finding 7) |
  | Structural rules on job elements, status enum, counters | AC1 accepts `pending`, `dead`, `done` and zero/positive counters in Fixture A | AC6: S8, S9 (non-object element), S10-S13 (non-string and empty `id`, `eventId`), S14, S15 (non-string and out-of-enum `status`), S16-S21 (non-integer and negative counters) | S16-S21 are the same two rules over three counters | additive job properties and `lastError` are opaque by contract |
  | Pairwise-distinct job IDs | AC1: Fixture A holds three distinct IDs | AC6: S22 duplicated `job-same` → invalid | S22 is the duplicate row for this rule | — |
  | Exact case-sensitive lookup, no trim/normalize/case-fold | AC1 selects `job-event-alpha` | AC2: `job-missing` against A, `job-missing` against empty B → exit `1`, exact `job not found` | AC2's case-distinct `Job-event-alpha` proves no case-folding | Unicode normalization forms untested; contract forbids normalization outright |
  | Projection shape — exactly `id`, `eventId`, `status`, `attempts`, `retries`, `attemptsSinceRetry`, in order, numbers uncopied | AC1 byte-exact stdout across two invocations | AC2 emits zero stdout bytes on a miss | Fixture A's `lastError`, event payload, approvals, outbox, and extra `metadata` must not be copied | — |
  | Compact serialization plus exactly one LF, exit `0`, empty stderr | AC1 byte-exact stdout and zero stderr bytes | AC2-AC6 require zero stdout bytes on every failure | — | — |
  | Error-stream discipline — one LF-terminated line, no path, job ID, OS text, parser text, input bytes, stack, or second line | — | AC2-AC6 assert exact stderr bytes for all four error literals | the four error literals plus the usage literal are pairwise distinguished by AC2-AC6 | — |
  | Validation precedence: arity, read, parse, structure, lookup | — | AC3's one-argument case names an absent path, so exit `2` proves arity beats read; AC4 vs AC5 proves read beats parse; Fixture C is also structurally invalid, so AC5's parse literal proves parse beats structure | — | structure-before-lookup not pinned by a fixture whose `<job-id>` is present (finding 6) |
  | Redaction of event payload, selected and unselected `lastError`, approvals, outbox, additive metadata | AC1: no `-MUST-NOT-LEAK` token and no unselected job ID in either stream | AC5: Fixture C's embedded `MALFORMED-STATE-MUST-NOT-LEAK` absent from both streams | every named sensitive field carries a unique sentinel | — |
  | Read-only, offline, self-terminating execution: no path written, renamed, deleted, chmodded, or created | AC1's two invocations pin Fixture A bytes, numeric mode, size, `mtimeNs`, and sorted parent entries | AC3 requires each fresh parent to stay empty; AC4 requires the absent path to stay absent and the directory's type, mode, size, `mtimeNs`, and entries to be identical; AC5, AC6 repeat AC1's observations | AC1's repeat invocation fixes determinism | — |
  | Source boundary — sole `node:fs` specifier, only `readFileSync`, no `server.js`, `fetch(`, `WebSocket`, `node:http`, `node:https`, `node:net`, `node:child_process`, `setTimeout`, `setInterval` | AC7 static inspection of the Builder file | — | — | — |
  | Fixture lifecycle and isolation | fresh temporary parent per case, only its frozen fixture written, child awaited under a 5-second bound, recursive `finally` teardown; AC6 proves A-C and S1-S22 pairwise byte-distinct before use | — | — | no database, dependent table, non-cascading foreign key, or shared serialized resource exists in this product |
  | Stage ownership, commit order, verification commands | AC8: test-author commit creates only `app/tests/job-inspect.test.js`, Builder commit only `app/tools/job-inspect.js` | AC8 requires five named commands to exit `0` and existing files, schema, `KIT_PIN`, and Factory controls to be unchanged | — | `scripts/artifact-check`, which CI also runs, is not named by AC8 (it passes today) |

  Findings:

  1. Criteria quality — AC1-AC7 freeze exact arguments, exit codes, byte-exact
     stdout and stderr, fixture bytes, numeric mode, size, `mtimeNs`, sorted
     parent entries, a 5-second process bound, projection key set and order,
     module specifiers, and forbidden source references; none requires the
     builder to interpret a qualitative term. AC8 is the exception: it requires
     five commands to "each exit `0` after implementation" without naming the
     branch or working tree in which they are run. That omission is what makes
     finding 2 a builder decision rather than a mechanical check.
  2. **Blocking — AC8 requires `scripts/repo-check` to exit `0`, and it cannot
     on this ticket's own frozen branch.** `scripts/repo-check` enforces the
     `.agents/repo-standard.json` `branch_pattern`
     `^(feat|fix|docs|chore|refactor|test|hotfix|spike)/[a-z0-9]+(?:-[a-z0-9]+)*$`
     against `GITHUB_HEAD_REF` or, when that is unset, the checked-out branch.
     Run in this worktree it prints
     `FAIL: branch name violates branch_pattern: ticket/T-178` and exits `1`,
     both plainly and with `GITHUB_HEAD_REF=ticket/T-178`; every other
     repo-check assertion passes, and `scripts/secret-scan`,
     `scripts/artifact-check --base origin/main`,
     `.github/scripts/test-immutability-check.sh`, and `npm test --prefix app`
     (66 passing) all exit `0` today. The ticket header freezes
     `Branch: ticket/T-178`, and `.github/workflows/ci.yml` runs
     `scripts/repo-check` on every `pull_request`. The managed baseline that
     introduced this check landed in `89b4264` after the last ticket merge
     (`T-176: implementation`, #130), so T-178 is the first ticket to meet it
     and no prior ticket's AC8 named `scripts/repo-check`. The only ways to
     make AC8 true are renaming the factory-owned `Branch` field, editing
     `.agents/repo-standard.json` (whose `branch_pattern` sits beside managed
     files that repo-check hashes), or running the check only on `main` after
     merge — each of which is outside both declared ownerships and contradicts
     AC8's own "Factory controls are unchanged" clause. The ticket must state
     which of these is intended, or drop `scripts/repo-check` from AC8, before
     a builder can satisfy it without bending a control.
  3. Contract coverage — every material behavior in the table has a covering
     criterion, and I recomputed the frozen artifacts rather than trusting
     them. `JSON.stringify(FixtureA, null, 2) + "\n"` reproduces the displayed
     Fixture A block byte-for-byte; the frozen projection of `job-event-alpha`
     is byte-identical to the frozen expected stdout line; Fixture A and
     Fixture B classify valid, Fixture C fails `JSON.parse`, and each of
     S1-S22 classifies structurally invalid for exactly the rule its row
     names, with no S fixture accidentally valid. All 25 serialized fixtures
     are pairwise byte-distinct, every invalid fixture is an exact literal
     rather than a transformation of an accepted fixture, and
     `Job-event-alpha` is absent from Fixture A so AC2's case-distinct miss is
     genuine. No generated identifier, sequence, counter, or timestamp
     participates in an expectation, so there is no initializer or reset to
     trace. No blocking finding.
  4. Consistency — the contract's job shape, `pending`/`done`/`dead` enum, and
     `attempts`/`retries`/`attemptsSinceRetry` counters match
     `app/server.js:41-48` and the retry reset at `server.js:122-123`;
     Fixture A's dead job (`attempts` 6, `retries` 1, `attemptsSinceRetry` 3)
     is exactly what the three-attempt dead-letter boundary at
     `server.js:63` produces after one retry, matching `SPEC.md` behavior 2 and
     conformance cases 3-4. Both `SPEC.md` anchors resolve. The single recorded
     entry in `factory/rulings.md` concerns detail-route envelopes and
     concurrent `app/server.js` ownership and is not contradicted by this
     offline command. Protected-boundary inspection of all thirteen tracked
     tests: the only exact-source and module-specifier assertions are
     `dead-jobs.test.js:344-370` (reads `dead-jobs.js` and its own source) and
     `blocked-approvals.test.js:440-453` (reads `blocked-approvals.js`); no
     test enumerates `app/tools/`, snapshots an unrelated source, or asserts
     page-wide text, so `Protected-Test-Conflicts: none` is exact and the
     declared `Fixture-Seams` entry is correctly reference-only. I re-ran the
     pinned `ticket-readiness.py` in this worktree: `--ticket T-178` returned
     `READINESS PASS`, and `--global-literal` returned `GLOBAL TEXT PASS`
     separately for the usage line, all four error lines, and the Fixture A
     expected output line, leaving no unknown or unparsable static check
     outstanding. `npm test --prefix app` runs `node --test tests/*.test.js`,
     so the new test file is collected without editing `app/package.json`, and
     the immutability check exempts `factory/`. A grep of every ticket under
     `factory/tickets/` found no other declaration of either T-178-owned path.
     Findings 8 and 9 record two non-blocking internal inconsistencies.
  5. Edge coverage — leakage of every named sensitive category (AC1 sentinels
     plus unselected job IDs, AC5's malformed-body secret), isolation
     (per-case temporary parent, recursive teardown), data loss (bytes,
     numeric mode, size, `mtimeNs`, parent entries, and absent paths asserted
     across AC1-AC6), external effect (AC7's offline source boundary), and
     irreversibility (the command never writes) are each covered. This local
     CLI has no endpoint, auth, cookie, CORS, origin, credential, preview
     deployment, or UI, so those categories are correctly out of scope. No
     blocking finding.
  6. AC6 does not freeze the `<job-id>` argument used against S1-S22, and no
     criterion invokes a structurally invalid fixture with an ID that fixture
     actually contains, so structure-before-lookup precedence is asserted only
     indirectly. The contract already resolves the precedence, so this is a
     test-authoring gap rather than a builder decision.
     SPEC-WARN: the test-author may fix the AC6 argument as `job-a` and add one case invoking S22 with `job-same`, asserting the same exit `1` and `invalid Relay state` line.
  7. The missing-own-property rule is exercised only for `outbox` (S3), and the
     non-object root rule only for `null` and `[]`; missing `events`, `jobs`,
     `approvals` and scalar roots such as `5` or `"state"` are equivalent
     permutations of rules the contract already fixes.
     SPEC-WARN: the test-author may extend the AC6 loop with the three remaining missing-key cases and one scalar root, at no contract cost.
  8. Fixture A's `job-pending` and `job-done` carry `eventId` values
     (`event-pending`, `event-done`) that appear in no `events` entry, and
     their IDs do not follow the product's `job-<eventId>` derivation at
     `server.js:42`. Structural validation treats `events` as opaque and the
     lookup never cross-references, so no criterion or output changes.
     SPEC-WARN: a later contract version may rename the two unselected jobs to `job-event-pending` and `job-event-done` and add their events so Fixture A is a state Relay could actually persist.
  9. Ambiguity log 6 states that T-177 owns the distinct event inspector and
     that T-170 through T-172 own "completed" paths, while Coordination
     attributes the event inspector to T-179 and calls T-170 through T-172
     "stranded remote identities". T-177 is `State: Canceled`,
     `Superseded-By: T-179`, and T-170 through T-172 are `State: Backlog`.
     Both passages point away from the two T-178-owned paths, which I verified
     independently, so the conflict conclusion is unaffected.
     SPEC-WARN: reconcile the ambiguity log's sibling-ticket sentence with the Coordination section so a single ticket attribution survives.

  SPEC-LINT: FAIL — AC8 demands a `scripts/repo-check` exit `0` that the frozen `Branch: ticket/T-178` makes impossible, leaving the builder to choose which factory-owned control to bend.

- 2026-08-09: Planner FIX stopped before freezing contract version 2 because
  no product document or entry in [`factory/rulings.md`](../rulings.md)
  resolves the contradiction between AC8's required successful
  `scripts/repo-check`, the managed branch pattern
  `^(feat|fix|docs|chore|refactor|test|hotfix|spike)/[a-z0-9]+(?:-[a-z0-9]+)*$`,
  and the operator-owned `Branch: ticket/T-178`. Operator question: should
  T-178 be implemented on a pattern-compliant branch (and, if so, what exact
  branch identity replaces `ticket/T-178`), should `scripts/repo-check` be
  removed from this ticket's pre-merge acceptance checks, or should the
  managed repository controls be changed through a separate authorized flow?

ROLE-ESCALATE: CONTRACT-BLOCKED

Kit-SHA: 863e38a68235dd682c56ca44ee079797f626413a

OPERATOR ANSWER: Keep the Factory-owned ticket/T-178 branch; the original protected acceptance requires targeted and full app tests plus immutability, while repository checks remain protected-CI controls and must not be added to this ticket's frozen contract.
OPERATOR ANSWER RECEIPT: 21bbe38a4bcb0a89440ea111ae0010ca4250d45ef3eb98e0500a39b55ba55f51
