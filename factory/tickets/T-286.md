# T-286 — Count Relay outbox entries by referenced job status offline

Kit-SHA: 12066ff5f92ac79191e1278b50142614f555e4fd
State: Building
Initiative: I-002
Priority: normal
Risk class: low
External: no
Branch: ticket/T-286
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/outbox-approval-count.test.js, app/tests/job-status-count.test.js
Authentication-Seams: none
Builder ownership: app/tools/outbox-job-status-count.js only
Test-author ownership: app/tests/outbox-job-status-count.test.js only

## Description

Add a zero-dependency, read-only CommonJS command that counts persisted Relay
outbox entries whose approval references a job with a supplied exact status
while Relay is offline.

## Acceptance criteria

1. `node app/tools/outbox-job-status-count.js <state-file> <job-status>` emits
   compact JSON containing exactly one `outboxEntriesForJobStatus` integer.
2. Status matching is exact and case-sensitive. Every outbox entry must
   reference exactly one valid approval and every referenced approval exactly
   one valid job; duplicate job or approval IDs, duplicate outbox references,
   and missing references are rejected before counting, and zero matches is
   valid.
3. Invalid arguments are rejected before file access. Unreadable, malformed,
   or structurally invalid state returns fixed redacted errors.
4. The command is offline, zero-dependency, deterministic, read-only, and
   self-terminating. Tests resolve fixtures from the test file, work from any
   current directory, use a five-second child timeout, and do not assume Git
   history depth.
5. Tests precede implementation. Targeted, full app, and immutability checks
   pass without dependency, server, schema, manifest, or Factory changes.

## Coordination

This ticket owns only its two new paths and is independent of T-285 and T-287.

## Ambiguity log

- **Extra top-level state keys** (e.g. `metadata`): the ticket description
  does not say whether unrecognized top-level keys invalidate state.
  `app/tools/job-status-count.js` (whose counted collection is `jobs`)
  requires exactly the four collection keys, while
  `app/tools/outbox-approval-count.js` (whose counted collection is
  `outbox`) permits and ignores extra top-level keys — the sibling ticket
  [T-282](T-282.md#frozen-contract-—-version-1) already resolved this
  divergence as "the tool's *primary/counted* collection decides." This
  ticket's counted collection is `outbox` (it emits
  `outboxEntriesForJobStatus`, one count per outbox entry), so it follows
  `outbox-approval-count.js`'s convention: extra top-level keys are
  permitted and ignored.
- **Composition of "duplicate job or approval IDs, duplicate outbox
  references, and missing references are rejected" (AC2)**: resolved as the
  union of three shipped precedents plus one new referential rule, matching
  how sibling ticket [T-284](T-284.md#frozen-contract-—-version-1) composed
  its own single-hop version of this same sentence:
  - unique `outbox[].approvalId` — verbatim from `outbox-approval-count.js`;
  - unique `approvals[].id`, non-empty `jobId`, `status` drawn from the four
    values `app/server.js:76` and `app/tools/approval-link.js` establish
    (`pending`/`sent`/`rejected`/`blocked_recipient`) — verbatim from
    `approval-link.js`'s and T-284's approval-validation rule, which is also
    T-284's precedent for outbox→approval referential integrity;
  - unique `jobs[].id` plus the complete job shape (`eventId`, `status` ∈
    `pending`/`done`/`dead`, non-negative `attempts`/`retries`/
    `attemptsSinceRetry`, nullable-or-string `lastError`) — verbatim from
    `app/tools/job-status-count.js`, this ticket's declared Fixture-Seam and
    the shipped single-hop validator for the exact field
    (`status`) this ticket queries;
  - a new second referential-integrity rule, `approvals[].jobId` must
    resolve into the validated `jobs` collection, applying
    `approval-link.js`'s validate-then-resolve technique (also visible
    directly at `app/server.js:180`,
    `state.jobs.find(j => j.id === approval.jobId)`) one hop further than
    T-284 needed. There is no reverse requirement that every approval be
    referenced by an outbox entry, or that every job be referenced by an
    approval — T-282/T-283/T-284 use the same one-directional join shape.
- **Directory-path and permission-denied reads**: both fold into the single
  `cannot read state file` envelope via `readFileSync`'s thrown error,
  matching every shipped sibling tool; no separate `fs.statSync` pre-check
  is required in the frozen contract.
- **Cross-ticket file-boundary conflicts**: grepped every local
  `factory/tickets/*.md` ownership declaration and every `origin/ticket/T-*`
  branch reachable under this repository's `MAX_CONCURRENT_TICKETS=3`
  bounded concurrency (`factory/PROJECT.env`) for `outbox-job-status-count`.
  Only this ticket declares either path, on every branch inspected —
  including `origin/ticket/T-283`, whose own planning run hit exactly this
  kind of collision (two other tickets, T-252 and T-259, independently
  claiming `approval-job-status-count.js`) and was escalated and resolved by
  the operator re-scoping T-283 to
  `approval-referenced-job-status-count.js`. T-282 (`State: Building`),
  T-284 (`State: Building`), T-285 (`State: Planning`), and T-287
  (`State: Planning`) each own distinct, non-overlapping file pairs. No
  conflict; no escalation required for this ticket.
- **Protected-test source boundaries**: inspected both declared Fixture-Seam
  tests, `app/tests/outbox-approval-count.test.js` and
  `app/tests/job-status-count.test.js`, plus the two precedent-only tests
  cited above for validation technique, `app/tests/approval-link.test.js`
  and `app/tests/approval-state.test.js`. Each asserts its module-specifier
  allowlist, forbidden-reference list, and forbidden-substring checks only
  against its own `TOOL` constant (its own tool file under test), never a
  directory listing, wildcard, or shared snapshot. None names or restricts
  `app/tools/outbox-job-status-count.js`. `Protected-Test-Conflicts: none`
  remains exact.
- **Global protected text**: `factory/PROJECT.env` declares `app/tools/` and
  `app/tests/` as `NONVISUAL_PATHS` (verified by
  `app/tests/factory-contract.test.js`), so this command's stdout/stderr
  strings carry no global-shell-text obligation by that rule alone; the
  pinned readiness checker was still run for the new tool path, test path,
  JSON output key, and usage line, and
  `python3 "$FACTORY_RELEASE_PATH/scripts/ticket-readiness.py" --ticket T-286 --workdir "$PWD"`
  — all returned `GLOBAL TEXT PASS` / `READINESS PASS`.
- **Deploy/topology completeness**: the command is a local file-to-stdout
  process with no endpoint, UI, authentication, cookie, CORS, cross-origin
  request, credential, domain, preview-deployment, or external-send surface;
  `External: no` and `Authentication-Seams: none` remain exact.
- **Existing operator rulings**: `factory/rulings.md` was checked before
  freeze. Its detail-route ruling and its T-178 verification ruling are
  unrelated to this command's shape or validation composition. No new
  operator decision was required and no new ruling was applied, so
  `factory/rulings.md` is not amended.

## Frozen contract — version 1

- **Freeze result:** PASS. Contract version 1 is frozen.

This is the exact interface for the Test-author and Builder. If it proves
wrong, return T-286 to Ready and append a higher-numbered frozen contract;
version 1 must not be edited or removed.

### Command interface

`node app/tools/outbox-job-status-count.js <state-file> <job-status>`

- Exactly 2 positional arguments are required. 0, 1, 3, or more arguments
  produce the usage error below, exit code `2`, before any filesystem access
  (`fs.readFileSync` is never called).
- Usage error, exit `2`, empty stdout:
  `usage: node app/tools/outbox-job-status-count.js <state-file> <job-status>\n`
- Success: exit `0`, empty stderr, stdout is exactly
  `{"outboxEntriesForJobStatus":<N>}\n` (compact JSON, this exact single key,
  `<N>` a non-negative JSON integer, no trailing/extra whitespace beyond the
  final `\n`).
- Read failure (absent path, directory path, permission denied, or any other
  `readFileSync` error), exit `1`, empty stdout:
  `outbox-job-status-count: cannot read state file\n`
- JSON parse failure, exit `1`, empty stdout:
  `outbox-job-status-count: state file is not valid JSON\n`
- Structurally invalid Relay state (see below), exit `1`, empty stdout:
  `outbox-job-status-count: invalid Relay state\n`
- Failure precedence is argument count, file read, JSON parse, complete
  structural validation (rules 1–7 below, in order), then matching. No error
  includes the state path, the supplied job-status literal, an OS error,
  parser text, a job/approval/outbox ID, an event, a stack trace, or a
  second line.
- Read-only: the command never creates, writes, or modifies any filesystem
  path. Repeated invocations against the same fixture leave its bytes and
  `fs.statSync(..., { bigint: true })` `mode`/`size`/`mtimeNs` unchanged, and
  leave the parent directory's entry list unchanged.
- Zero-dependency/offline/deterministic/self-terminating: the file's only
  `require` specifier is `"node:fs"`, its only `node:fs` operation is
  `readFileSync`, and it contains none of the literal substrings `server.js`,
  `fetch(`, `WebSocket`, `node:http`, `node:https`, `node:net`,
  `node:child_process`, `setTimeout`, `setInterval`.

### State validation (`isValidState`)

`state` is valid iff all of:

1. `state` is a non-null, non-array object.
2. `state.events`, `state.jobs`, `state.approvals`, `state.outbox` are each
   an own property and each an array. Other top-level keys (e.g.
   `metadata`) may be present and are ignored — this command's counted
   collection is `outbox`, so it follows `outbox-approval-count.js`'s
   extra-top-level-key convention, not `job-status-count.js`'s exact-key
   convention.
3. Every element of `state.jobs` is a non-null, non-array object with: `id`
   a non-empty string, `eventId` a non-empty string, `status` a string that
   is one of `"pending"`, `"done"`, `"dead"`, `attempts` a non-negative
   integer, `retries` a non-negative integer, `attemptsSinceRetry` a
   non-negative integer, and `lastError` either `null` or a string.
   Additional own job properties are allowed and ignored. No two elements of
   `state.jobs` share the same `id`.
4. Every element of `state.approvals` is a non-null, non-array object with:
   `id` a non-empty string, `jobId` a non-empty string, `status` a string
   that is one of `"pending"`, `"sent"`, `"rejected"`,
   `"blocked_recipient"`. Additional own approval properties are allowed and
   ignored. No two elements of `state.approvals` share the same `id`.
5. Every `state.approvals[i].jobId` equals the `id` of some element of
   `state.jobs` (rules 3–4 guarantee at most one match). Any approval whose
   `jobId` matches no job makes the whole state invalid.
6. Every element of `state.outbox` is a non-null, non-array object with own
   properties `to`, `subject`, `body` present (any type), `approvalId` a
   non-empty string, and `sentAt` a non-empty string. Additional own outbox
   properties are allowed and ignored. No two elements of `state.outbox`
   share the same `approvalId`.
7. Every `state.outbox[i].approvalId` equals the `id` of some element of
   `state.approvals` (rules 4 and 6 guarantee at most one match). Any outbox
   entry whose `approvalId` matches no approval makes the whole state
   invalid.

`state.events` element contents are not otherwise validated beyond rule 2.
Any violation of 1–7 makes the state invalid.

### Count semantics

Rules 3–7 guarantee that every valid outbox entry resolves, via its
`approvalId` and then that approval's `jobId`, to exactly one job.
`outboxEntriesForJobStatus` is the number of `state.outbox` entries whose
resolved job has `status === <job-status>` (the second positional argument),
compared with exact, case-sensitive `===` string equality — no trimming,
case folding, or normalization of either the stored or supplied value. Zero
matches is a successful result (exit `0`), not an error. There is no reverse
requirement that every approval or job be referenced.

### Ownership

- Builder ownership: `app/tools/outbox-job-status-count.js` only. No changes
  to `app/server.js`, any `package.json`/lockfile, schema, manifest, or
  Factory control file.
- Test-author ownership: `app/tests/outbox-job-status-count.test.js` only,
  written and committed before the Builder's implementation commit. Tests
  spawn the tool with `node:child_process.spawnSync`, a `5_000` ms
  (five-second) timeout, resolve every fixture path relative to `__dirname`,
  run correctly from any current working directory, and do not assume any
  specific Git history depth. Tests may read but must not modify
  `app/tests/outbox-approval-count.test.js` or
  `app/tests/job-status-count.test.js` (Fixture-Seams, protected,
  unchanged by this ticket), nor
  `app/tests/approval-link.test.js` or `app/tests/approval-state.test.js`
  (precedent-only references, also unchanged).

## Planning log

- 2026-08-27: Planner froze contract version 1 composing three shipped
  single-hop validators (`outbox-approval-count.js`, `approval-link.js`'s
  approval shape and job-resolution technique, `job-status-count.js`) into
  one two-hop outbox→approval→job chain, reusing each verbatim rather than
  inventing a new minimized validation subset. `SPEC.md`, `AGENTS.md`,
  `factory/rulings.md`, `factory/PROJECT.env`, `app/server.js`,
  `app/package.json`, and every local and remote sibling ticket in the
  I-002 count-tool family (T-282 through T-287) were inspected. No
  cross-ticket file-boundary, protected-test-source-boundary, or
  global-protected-text conflict was found, and no operator escalation was
  required.

## Spec-lint log

- 2026-08-27: Frozen contract version 1 linted against the ticket, cited
  product seams, `factory/rulings.md`, and the current repository state.

| Contract element | Positive | Failure/empty | Duplicate | Out-of-scope |
| --- | --- | --- | --- | --- |
| Two-argument CommonJS command and exact compact output key | AC1 | AC3 (arity) | N/A | N/A |
| Exact, case-sensitive two-hop job-status count | AC2 | AC2 (zero matches) | N/A | N/A |
| Argument-before-read precedence and fixed redacted error envelopes | AC3 | AC3 | N/A | N/A |
| State root and required collection arrays; ignored extra top-level keys | AC3 | AC3 | N/A | Extra-key rejection explicitly out of scope |
| Complete job shape, allowed statuses/properties, and unique job IDs | AC2 | AC3 | AC2 | N/A |
| Complete approval shape, allowed statuses/properties, and unique approval IDs | AC2 | AC3 | AC2 | N/A |
| Approval-to-job referential integrity | AC2 | AC3 | N/A | Reverse job reference explicitly out of scope |
| Complete outbox shape, allowed properties, and unique approval references | AC2 | AC3 | AC2 | N/A |
| Outbox-to-approval referential integrity | AC2 | AC3 | AC2 | Reverse approval reference explicitly out of scope |
| Read-only, offline, zero-dependency, deterministic, self-terminating operation | AC4 | AC4 (five-second timeout) | N/A | Network, auth, and external effects explicitly out of scope |
| Test fixture location, cwd independence, ownership, and verification constraints | AC4–AC5 | N/A | N/A | Unrelated protected tests and product files explicitly out of scope |

Findings: none.

SPEC-LINT: PASS

## Test-author log

- 2026-08-27: Added `app/tests/outbox-job-status-count.test.js` in test-only
  commit `685bdd5`. The four contract-facing tests cover AC1 compact output,
  AC2 exact two-hop matching and validation, AC3 fixed error precedence, and
  AC4 cwd-independent read-only offline source boundaries. The focused local
  command `node --test app/tests/outbox-job-status-count.test.js` was run once
  and fails as expected before implementation because the command is absent.
  AC5's tests-first ordering and targeted/full/immutability verification are
  workflow obligations rather than executable product behavior; defer those
  checks to the Builder, protected CI, and final certification.

## Builder log

- 2026-08-27: Implemented `app/tools/outbox-job-status-count.js` verbatim
  against frozen contract version 1, composing `outbox-approval-count.js`'s
  outbox-shape/collection checks, `approval-link.js`'s approval-shape and
  validate-then-resolve job-lookup technique, and `job-status-count.js`'s
  job-shape validator, plus one new outbox→approval referential-integrity
  rule for the second hop. `readFileSync` is the only `node:fs` operation
  used (no `statSync` pre-check, per the ticket's directory/permission-denied
  ambiguity-log resolution). Targeted verification run locally: `node --test
  app/tests/outbox-job-status-count.test.js` (4/4 pass),
  `.github/scripts/test-immutability-check.sh` (test immutability holds for
  `app/tests/`), `scripts/repo-check` (PASS), `scripts/secret-scan` (PASS).
  Per Builder role scope, the repository-wide `npm test --prefix app` suite
  was not run from this role; broad verification is deferred to protected CI
  and final certification.
Reviewer round 1 signed detail:

> REQUEST CHANGES
>
> 1. AC2/AC3: structural-validation coverage is incomplete. The tests omit most required job, approval, outbox, root, and collection-shape constraints, so removing validation for fields such as `lastError`, `retries`, `attemptsSinceRetry`, or `to`/`subject`/`body` would still pass. Add table-driven valid/invalid cases covering rules 1–7, including allowed extra record properties and opaque event elements.
>
> 2. AC4: the read-only assertion invokes the command only once, despite the contract requiring repeated invocations; directory-path read failure is also untested. Add both cases.
>
> FIX-OWNER: test-author

reviewer round 1: REQUEST CHANGES
reviewer round 1 FIX-OWNER: test-author
