# T-227 — count Relay outbox entries with one recipient and subject offline

Kit-SHA: 7ae447baf7287e32a9df60c9453da6edec454e7b
State: Ready
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-227
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/outbox-detail.test.js
Authentication-Seams: none
Builder ownership: app/tools/outbox-recipient-subject-count.js only
Test-author ownership: app/tests/outbox-recipient-subject-count.test.js only

## Description

Add a zero-dependency, read-only Node command that counts sandbox outbox entries
whose recipient and subject exactly match two supplied strings while offline.

## Acceptance criteria

1. `node app/tools/outbox-recipient-subject-count.js <state-file> <recipient> <subject>`
   emits compact JSON with exactly one `outboxWithRecipientAndSubject` integer.
2. Both strings match exactly without trimming, normalization, or coercion; no
   match returns `{"outboxWithRecipientAndSubject":0}` plus one newline.
3. Missing, unreadable, malformed, or structurally invalid state returns fixed
   redacted errors; complete outbox validation precedes counting.
4. Every invocation is offline, read-only, deterministic, and self-terminating.
5. Tests precede implementation; targeted, full app, and immutability checks
   pass without dependency, server, schema, manifest, or Factory changes.

## Coordination

This ticket owns only its two new paths and is independent of T-225 and T-226.
