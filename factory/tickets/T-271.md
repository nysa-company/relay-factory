# T-271 — list pending Relay jobs offline

Kit-SHA: bcffd8f0d3d0596752136d7ee2fd95e4f912806c
State: Building
Initiative: I-002
Priority: normal
Risk class: low
External: no
Branch: ticket/T-271
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/dead-jobs.test.js,app/tests/job-attempts.test.js
Authentication-Seams: none
Builder ownership: app/tools/pending-jobs.js only
Test-author ownership: app/tests/pending-jobs.test.js only

## Spec'd description

Add one zero-dependency Node 18+ command, `node app/tools/pending-jobs.js
<state-file>`, that reads a persisted Relay state file without starting the
service, validates every stored job, and emits a compact JSON array of only
the jobs whose `status` is `pending`, sorted by `id` in code-point order, each
projected to exactly `id`, `eventId`, `attempts`, and `retries`. This gives an
operator a durable, offline view of outstanding work while withholding
`status`, `attemptsSinceRetry`, `lastError`, event payloads, approval content,
and outbox content.

Product-doc sources:

- [`SPEC.md` behavior 2](../../SPEC.md#behavior-spec) defines durable job
  `attempts`, `retries`, and the three-attempt dead-letter boundary that this
  command projects verbatim for pending jobs.
- [`SPEC.md` behavior 6](../../SPEC.md#behavior-spec) makes the complete
  persisted state file Relay's durable source of truth; this command reads
  that artifact without participating in service startup or crash recovery.
- [`SPEC.md` test cases 1 and 3](../../SPEC.md#test-cases-the-conformance-suite)
  establish that an accepted event's job is visible in state `pending` before
  it settles, which is exactly the population this command lists.
- [`AGENTS.md` Boundaries](../../AGENTS.md#boundaries) keeps `app/` as the
  zero-dependency conformance product and Factory controls outside application
  work. [`AGENTS.md` Verification](../../AGENTS.md#verification) defines the
  later full-app and immutability checks.

The job shape and status enum come from [`app/server.js`](../../app/server.js)
(`acceptEvent`, `processJob`, `retryJob`): every job is created with
`id = "job-" + eventId`, `status` in `{"pending","done","dead"}`, and
`attempts`/`retries`/`attemptsSinceRetry` as non-negative integers that only
ever increase or reset to `0`. The list-and-project shape, sort order, and
error-message/exit-code envelope come from the two closest existing
diagnostics: [`app/tools/dead-jobs.js`](../../app/tools/dead-jobs.js) (status
filter, four-field projection, code-point sort) and
[`app/tools/job-attempts.js`](../../app/tools/job-attempts.js) /
[`app/tools/job-progress.js`](../../app/tools/job-progress.js) (the
newer four-category `usage` / `unreadable` / `not valid JSON` / `invalid Relay
state` failure envelope with distinct exit codes, and whole-`jobs`-array
validation including `id` uniqueness before any downstream step).

This ticket adds no endpoint, UI, schema, retry rule, server behavior,
package, dependency, lockfile, Factory control, authentication path,
deployment topology, or external-send behavior.

## Acceptance criteria

1. Given Frozen Fixture A, invoking
   `node app/tools/pending-jobs.js <fixture-path>` exits `0`, writes zero
   stderr bytes, and writes exactly the frozen compact stdout array (defined
   below) plus one trailing LF. Each array entry, parsed, has exactly `id`,
   `eventId`, `attempts`, and `retries` in that order — no `status`,
   `attemptsSinceRetry`, or `lastError` key is present on any entry, and no
   non-pending job's `id` or `eventId` appears anywhere in the array. Neither
   stream contains any Frozen Fixture A privacy sentinel. A second invocation
   produces byte-identical streams. After every invocation, the fixture
   retains its exact bytes, numeric mode, size, and nanosecond modification
   time, and its parent retains its sorted entry names.
2. Given Frozen Fixture C (empty collections) and, separately, Frozen
   Fixture D (only `done`/`dead` jobs), each invocation exits `0`, writes
   zero stderr bytes, writes exactly `[]\n` to stdout, and does not emit
   Fixture D's privacy sentinel. Each fixture retains its exact bytes,
   numeric mode, size, and nanosecond modification time, and its parent
   retains its sorted entry names.
3. In separate fresh empty temporary parents, invoking the command with zero
   positional arguments and, separately, with two positional arguments (both
   naming paths that do not exist) exits `2`, writes zero stdout bytes, and
   writes exactly `usage: node app/tools/pending-jobs.js <state-file>\n` to
   stderr. Both nonexistent paths in the two-argument case remain absent
   afterward, proving arity validation precedes any file access. Each parent
   retains its exact numeric mode, size, nanosecond modification time, and
   entry list.
4. Invoking the command against the frozen nonexistent path and, separately,
   the frozen empty-directory path exits `1`, writes zero stdout bytes, and
   writes exactly `pending-jobs: state file is unreadable\n` to stderr. The
   missing path remains absent; the directory retains its type, numeric mode,
   size, nanosecond modification time, and empty entry list; each parent
   retains its sorted entry names.
5. Invoking the command against Frozen Fixture B exits `1`, writes zero
   stdout bytes, writes exactly
   `pending-jobs: state file is not valid JSON\n` to stderr, and does not
   emit Fixture B's privacy sentinel. Fixture B retains its exact bytes,
   numeric mode, size, and nanosecond modification time, and its parent
   retains its sorted entry names.
6. Each Frozen Structural-Invalid Fixture S1-S26 is invoked once. Every
   invocation exits `1`, writes zero stdout bytes, and writes exactly
   `pending-jobs: state file has invalid Relay state\n` to stderr. S26 (one
   valid pending job followed by one structurally invalid sibling) produces
   the same structural error instead of a one-entry success array, proving
   the entire `jobs` array — including per-job `id` and `eventId` uniqueness
   across the whole array, not only among pending jobs — is validated before
   filtering. Before any invocation, Fixtures A, B, C, D, and S1-S26 are
   pairwise byte-distinct. Every regular-file fixture retains its exact
   bytes, numeric mode, size, and nanosecond modification time, and every
   parent retains its sorted entry names.
7. Every invocation in criteria 1-6 terminates unaided within the child
   harness's `10_000` ms timeout, with no signal and no child-process error.
   Static inspection finds exactly `node:fs` among the Builder file's
   CommonJS module specifiers and no `server.js`, `node:http`, `node:https`,
   `node:net`, `node:child_process`, or `fetch` reference. No invocation
   writes, creates, removes, renames, or chmods any path.
8. Git history places the complete acceptance-test commit before the Builder
   commit. The test-author commit changes only
   `app/tests/pending-jobs.test.js`; the Builder commit changes only
   `app/tools/pending-jobs.js`. Existing application files, protected tests,
   `app/server.js`, `app/package.json`, dependency and lock files, the
   persisted schema, retry behavior, `factory/KIT_PIN`, and Factory controls
   remain unchanged. After implementation,
   `node --test app/tests/pending-jobs.test.js`, `npm test --prefix app`, and
   `.github/scripts/test-immutability-check.sh` each exit `0`.

## Frozen contract — version 1

- **Freeze result:** PASS. Contract version 1 is frozen.

This is the exact interface for the test-author and Builder. If it proves
wrong, return T-271 to Ready and append a higher-numbered frozen contract;
version 1 must not be edited or removed.

### Command-line interface

- Repository-root invocation: `node app/tools/pending-jobs.js <state-file>`.
- Exactly one positional argument is accepted; it is used verbatim (not
  trimmed, decoded, or normalized) as a filesystem path.
- Any other arity exits `2`, writes empty stdout, reads no state path, and
  writes exactly this single LF-terminated stderr line:

  ```text
  usage: node app/tools/pending-jobs.js <state-file>
  ```

- After arity validation, the command synchronously establishes that
  `<state-file>` is a readable regular file via `fs.statSync` followed by
  `isFile()` (matching `app/tools/job-attempts.js` and
  `app/tools/job-progress.js`), reads it as UTF-8, parses it with
  `JSON.parse`, and validates the complete frozen structure below before
  computing any output.
- Success exits `0`, writes empty stderr, and writes one compact JSON array
  plus exactly one LF to stdout. The array contains every job whose `status`
  is exactly `pending`, sorted ascending by `id` using JavaScript's default
  `<`/`>` string comparison (UTF-16 code-unit / code-point order, matching
  `app/tools/dead-jobs.js`). Each element is an object with exactly these
  keys in this order:

  ```json
  {"id":<stored id>,"eventId":<stored eventId>,"attempts":<stored attempts>,"retries":<stored retries>}
  ```

  Values are copied without transformation. `status`, `attemptsSinceRetry`,
  `lastError`, and any additional job property are never emitted. An empty
  result is exactly `[]`.
- The command computes its result in memory and exits. It does not import,
  require, spawn, or start `app/server.js`; open a socket or listener; issue
  a request; or leave a timer or child process running.

### Valid state and failure protocol

A valid state is a parsed, non-null, non-array JSON object with exactly four
own properties: `events`, `jobs`, `approvals`, and `outbox`, each an array.
Entries in `events`, `approvals`, and `outbox` are opaque because this
command does not consume them. Every element of `jobs` must be a non-null,
non-array object with:

- a non-empty string `id`;
- a non-empty string `eventId`;
- `status` exactly `pending`, `done`, or `dead`;
- integer `attempts`, `retries`, and `attemptsSinceRetry`, each greater than
  or equal to zero; and
- `lastError` equal to `null` or a string.

Job `id` values must be pairwise distinct across the entire `jobs` array, and
job `eventId` values must independently be pairwise distinct across the
entire `jobs` array (not only among pending jobs). Additional job properties
are allowed and ignored. The validator does not impose a relationship among
the three counters. The whole `jobs` array is validated — every element,
both uniqueness rules — before any status filtering occurs; a valid pending
job does not override an invalid sibling (S26).

After arity validation, failures have this exact precedence:

1. A filesystem failure while establishing a readable regular file or
   reading its bytes, including a nonexistent path, directory path, or OS
   read denial, exits `1`, writes empty stdout, and writes exactly
   `pending-jobs: state file is unreadable\n` to stderr.
2. Readable bytes rejected by `JSON.parse` exit `1`, write empty stdout, and
   write exactly `pending-jobs: state file is not valid JSON\n` to stderr.
3. Parsed JSON that fails any frozen structural rule (including either
   uniqueness rule) exits `1`, writes empty stdout, and writes exactly
   `pending-jobs: state file has invalid Relay state\n` to stderr.
4. Filtering, projection, and sorting occur only after whole-state
   validation succeeds.

No error includes an input path, supplied argument, operating-system error,
parser text, stack trace, state content, or a second line. Every displayed
error line ends with exactly one LF. No path is written, renamed, deleted,
chmodded, or created.

The arity tests use fresh empty temporary parents. The two-argument case
supplies two paths that do not exist, both of which remain absent
afterward, proving arity validation precedes file access.

### Frozen Fixture A — status filter, sort, and privacy projection

`app/tests/pending-jobs.test.js` writes the following parsed value as the
exact UTF-8 result of `JSON.stringify(value, null, 2) + "\n"` in a fresh
temporary directory; there is no repository fixture file:

```json
{
  "events": [
    {
      "id": "Zulu-event",
      "type": "generic",
      "payload": { "private": "EVENT-PAYLOAD-SENTINEL" },
      "receivedAt": "2026-08-26T18:00:00.000Z"
    },
    {
      "id": "alpha-event",
      "type": "email",
      "payload": {},
      "receivedAt": "2026-08-26T18:00:01.000Z"
    },
    {
      "id": "middle-event",
      "type": "meeting",
      "payload": {},
      "receivedAt": "2026-08-26T18:00:02.000Z"
    },
    {
      "id": "omega-event",
      "type": "generic",
      "payload": { "failTimes": 99 },
      "receivedAt": "2026-08-26T18:00:03.000Z"
    },
    {
      "id": "extra-field-event",
      "type": "generic",
      "payload": {},
      "receivedAt": "2026-08-26T18:00:04.000Z"
    }
  ],
  "jobs": [
    {
      "id": "job-Zulu-event",
      "eventId": "Zulu-event",
      "status": "pending",
      "attempts": 4,
      "lastError": "PENDING-LASTERROR-SENTINEL",
      "retries": 1,
      "attemptsSinceRetry": 0
    },
    {
      "id": "job-alpha-event",
      "eventId": "alpha-event",
      "status": "pending",
      "attempts": 0,
      "lastError": null,
      "retries": 0,
      "attemptsSinceRetry": 0
    },
    {
      "id": "job-middle-event",
      "eventId": "middle-event",
      "status": "done",
      "attempts": 1,
      "lastError": null,
      "retries": 0,
      "attemptsSinceRetry": 1
    },
    {
      "id": "job-omega-event",
      "eventId": "omega-event",
      "status": "dead",
      "attempts": 3,
      "lastError": "DEAD-LASTERROR-SENTINEL",
      "retries": 0,
      "attemptsSinceRetry": 3
    },
    {
      "id": "job-extra-field-event",
      "eventId": "extra-field-event",
      "status": "pending",
      "attempts": 1,
      "lastError": null,
      "retries": 0,
      "attemptsSinceRetry": 1,
      "ignoredDiagnostic": "IGNORED-JOB-FIELD-SENTINEL"
    }
  ],
  "approvals": [
    {
      "id": "appr-middle-event",
      "jobId": "job-middle-event",
      "action": {
        "to": "test@example.com",
        "subject": "Middle action",
        "body": "APPROVAL-ACTION-SENTINEL"
      },
      "status": "sent",
      "proposedAt": "2026-08-26T18:00:05.000Z"
    }
  ],
  "outbox": [
    {
      "to": "test@example.com",
      "subject": "Middle action",
      "body": "OUTBOX-CONTENT-SENTINEL",
      "approvalId": "appr-middle-event",
      "sentAt": "2026-08-26T18:00:06.000Z"
    }
  ]
}
```

The three job `id` values follow Relay's `"job-" + eventId` initializer
(`app/server.js:42`); `job-Zulu-event` is a job that failed once, was
retried (`attemptsSinceRetry` reset to `0`, `retries` incremented to `1`,
`lastError` preserved from before the retry per `app/server.js:115-125`),
and is now pending again — a state reachable through one retry.

The exact frozen stdout line (one compact JSON array plus one LF) is:

```text
[{"id":"job-Zulu-event","eventId":"Zulu-event","attempts":4,"retries":1},{"id":"job-alpha-event","eventId":"alpha-event","attempts":0,"retries":0},{"id":"job-extra-field-event","eventId":"extra-field-event","attempts":1,"retries":0}]
```

`job-Zulu-event` sorts first because `Z` (U+005A) precedes `a` (U+0061) in
code-point order; `job-alpha-event` sorts before `job-extra-field-event`
because `a` (U+0061) precedes `e` (U+0065). `job-middle-event` (`done`) and
`job-omega-event` (`dead`) are excluded entirely — not just their
`lastError` — proving status filtering removes the whole record.

The six-element Fixture A privacy-sentinel set is exactly
`EVENT-PAYLOAD-SENTINEL`, `PENDING-LASTERROR-SENTINEL`,
`DEAD-LASTERROR-SENTINEL`, `IGNORED-JOB-FIELD-SENTINEL`,
`APPROVAL-ACTION-SENTINEL`, and `OUTBOX-CONTENT-SENTINEL`. AC1 checks both
streams against every member. All IDs, counters, and timestamps are exact
literals; no generated identifier, sequence, counter, clock, initializer, or
reset participates in an expected value.

### Frozen Fixtures C and D — empty and all-non-pending results

Fixture C is written as the exact UTF-8 bytes, plus one final LF:

```text
{"events":[],"jobs":[],"approvals":[],"outbox":[]}
```

Fixture D is written as the exact UTF-8 bytes, plus one final LF:

```text
{"events":[],"jobs":[{"id":"job-done-only","eventId":"event-done-only","status":"done","attempts":1,"lastError":null,"retries":0,"attemptsSinceRetry":1},{"id":"job-dead-only","eventId":"event-dead-only","status":"dead","attempts":3,"lastError":"ALL-NON-PENDING-LASTERROR-SENTINEL","retries":0,"attemptsSinceRetry":3}],"approvals":[],"outbox":[]}
```

Both fixtures produce the frozen stdout `[]\n`. Fixture D's sentinel,
`ALL-NON-PENDING-LASTERROR-SENTINEL`, is checked against both streams.

### Frozen Fixture B and read-failure paths

Fixture B is a regular file containing exactly these UTF-8 bytes, with no
final LF, closing quote, or closing brace:

```text
{"events":[],"private":"PENDING-JOBS-MALFORMED-SENTINEL
```

Each read-failure case uses a fresh temporary parent:

- `<tmp>/PATH-SENTINEL-missing-state.json` does not exist before invocation.
- `<tmp>/DIRECTORY-PATH-SENTINEL` exists as an empty directory, not a
  regular file.

The failure-side sentinel set adds `PATH-SENTINEL`, `DIRECTORY-PATH-SENTINEL`,
and `PENDING-JOBS-MALFORMED-SENTINEL` to Fixture A's six sentinels and
Fixture D's one sentinel.

### Frozen structural-invalid fixtures

Each table value is written directly as the displayed compact UTF-8 JSON
plus one final LF. S15 freezes the exact uppercase casing mutation
`PENDING`; it is byte-distinct from every valid fixture's lowercase
`"pending"`. No fixture is constructed at runtime by casing, normalization,
or mutation.

| ID | Violated rule | Exact JSON value before final LF |
|---|---|---|
| S1 | root is null | `null` |
| S2 | root is an array | `[]` |
| S3 | `events` is not an array | `{"events":{},"jobs":[],"approvals":[],"outbox":[]}` |
| S4 | `jobs` is not an array | `{"events":[],"jobs":{},"approvals":[],"outbox":[]}` |
| S5 | `approvals` is not an array | `{"events":[],"jobs":[],"approvals":{},"outbox":[]}` |
| S6 | `outbox` is not an array | `{"events":[],"jobs":[],"approvals":[],"outbox":{}}` |
| S7 | extra top-level property | `{"events":[],"jobs":[],"approvals":[],"outbox":[],"metadata":[]}` |
| S8 | missing top-level property | `{"events":[],"jobs":[],"approvals":[]}` |
| S9 | job is null | `{"events":[],"jobs":[null],"approvals":[],"outbox":[]}` |
| S10 | job is an array | `{"events":[],"jobs":[[]],"approvals":[],"outbox":[]}` |
| S11 | empty job `id` | `{"events":[],"jobs":[{"id":"","eventId":"s-event-invalid","status":"pending","attempts":0,"lastError":null,"retries":0,"attemptsSinceRetry":0}],"approvals":[],"outbox":[]}` |
| S12 | non-string job `id` | `{"events":[],"jobs":[{"id":7,"eventId":"s-event-invalid","status":"pending","attempts":0,"lastError":null,"retries":0,"attemptsSinceRetry":0}],"approvals":[],"outbox":[]}` |
| S13 | empty job `eventId` | `{"events":[],"jobs":[{"id":"s-job-invalid-event-empty","eventId":"","status":"pending","attempts":0,"lastError":null,"retries":0,"attemptsSinceRetry":0}],"approvals":[],"outbox":[]}` |
| S14 | non-string job `eventId` | `{"events":[],"jobs":[{"id":"s-job-invalid-event-type","eventId":7,"status":"pending","attempts":0,"lastError":null,"retries":0,"attemptsSinceRetry":0}],"approvals":[],"outbox":[]}` |
| S15 | status outside exact enum | `{"events":[],"jobs":[{"id":"s-job-invalid-status-case","eventId":"s-event-invalid","status":"PENDING","attempts":0,"lastError":null,"retries":0,"attemptsSinceRetry":0}],"approvals":[],"outbox":[]}` |
| S16 | non-string status | `{"events":[],"jobs":[{"id":"s-job-invalid-status-type","eventId":"s-event-invalid","status":null,"attempts":0,"lastError":null,"retries":0,"attemptsSinceRetry":0}],"approvals":[],"outbox":[]}` |
| S17 | negative `attempts` | `{"events":[],"jobs":[{"id":"s-job-invalid-attempts-negative","eventId":"s-event-invalid","status":"dead","attempts":-1,"lastError":null,"retries":0,"attemptsSinceRetry":3}],"approvals":[],"outbox":[]}` |
| S18 | non-integer `attempts` | `{"events":[],"jobs":[{"id":"s-job-invalid-attempts-type","eventId":"s-event-invalid","status":"pending","attempts":0.5,"lastError":null,"retries":0,"attemptsSinceRetry":0}],"approvals":[],"outbox":[]}` |
| S19 | negative `retries` | `{"events":[],"jobs":[{"id":"s-job-invalid-retries-negative","eventId":"s-event-invalid","status":"dead","attempts":3,"lastError":null,"retries":-1,"attemptsSinceRetry":3}],"approvals":[],"outbox":[]}` |
| S20 | non-integer `retries` | `{"events":[],"jobs":[{"id":"s-job-invalid-retries-type","eventId":"s-event-invalid","status":"pending","attempts":0,"lastError":null,"retries":"0","attemptsSinceRetry":0}],"approvals":[],"outbox":[]}` |
| S21 | negative `attemptsSinceRetry` | `{"events":[],"jobs":[{"id":"s-job-invalid-window-negative","eventId":"s-event-invalid","status":"dead","attempts":3,"lastError":null,"retries":0,"attemptsSinceRetry":-1}],"approvals":[],"outbox":[]}` |
| S22 | non-integer `attemptsSinceRetry` | `{"events":[],"jobs":[{"id":"s-job-invalid-window-type","eventId":"s-event-invalid","status":"pending","attempts":0,"lastError":null,"retries":0,"attemptsSinceRetry":null}],"approvals":[],"outbox":[]}` |
| S23 | invalid `lastError` type | `{"events":[],"jobs":[{"id":"s-job-invalid-error","eventId":"s-event-invalid","status":"dead","attempts":3,"lastError":7,"retries":0,"attemptsSinceRetry":3}],"approvals":[],"outbox":[]}` |
| S24 | duplicate job `id` | `{"events":[],"jobs":[{"id":"s-job-duplicate","eventId":"s-event-a","status":"pending","attempts":0,"lastError":null,"retries":0,"attemptsSinceRetry":0},{"id":"s-job-duplicate","eventId":"s-event-b","status":"done","attempts":1,"lastError":null,"retries":0,"attemptsSinceRetry":1}],"approvals":[],"outbox":[]}` |
| S25 | duplicate job `eventId` | `{"events":[],"jobs":[{"id":"s-job-event-dup-a","eventId":"s-event-duplicate","status":"pending","attempts":0,"lastError":null,"retries":0,"attemptsSinceRetry":0},{"id":"s-job-event-dup-b","eventId":"s-event-duplicate","status":"done","attempts":1,"lastError":null,"retries":0,"attemptsSinceRetry":1}],"approvals":[],"outbox":[]}` |
| S26 | valid pending job followed by invalid sibling missing `attempts` | `{"events":[],"jobs":[{"id":"s-job-valid-pending","eventId":"s-event-valid-pending","status":"pending","attempts":0,"lastError":null,"retries":0,"attemptsSinceRetry":0},{"id":"s-job-invalid-missing-attempts","eventId":"s-event-invalid","status":"pending","lastError":null,"retries":0,"attemptsSinceRetry":0}],"approvals":[],"outbox":[]}` |

Before invoking any command, the test compares the exact serialized bytes of
Fixtures A, B, C, D, and S1-S26 pairwise and requires every pair to differ.

### File locations, lifecycle, protected boundaries, and topology

- Builder ownership: create `app/tools/pending-jobs.js` only. It is a
  standalone CommonJS command whose only module specifier is `node:fs`;
  `process` and `JSON` are runtime globals.
- Test-author ownership: create `app/tests/pending-jobs.test.js` only. Its
  module specifiers are exactly `node:test`, `node:assert`,
  `node:child_process`, `node:fs`, `node:path`, and `node:os`.
- Existing reference-only seams (not edited):
  - `app/tests/dead-jobs.test.js` freezes the closest prior list-and-project
    shape (status filter, four-field entry, code-point sort) and its own
    named `dead-jobs.js` source inspection; it contains no import/export
    allowlist, exact-source assertion, or module restriction for
    `pending-jobs.js`.
  - `app/tests/job-attempts.test.js` freezes the four-category failure
    envelope, the `fs.statSync`/`isFile()` read-failure check, and per-job
    `id` uniqueness ahead of `job-attempts.js`'s own lookup step; it
    likewise contains no restriction on `pending-jobs.js` or `node:fs`.
- The tracked exact-source assertions across `app/tests/*.test.js` each bind
  to a per-file `TOOL` constant naming only that file's own tool; none
  enumerates `app/tools/` or `app/tests/` dynamically or restricts a new
  tool's imports. `Protected-Test-Conflicts: none` remains exact.
- Every runtime case owns a fresh temporary directory, creates all required
  inputs before invocation, invokes the child synchronously with
  `timeout: 10_000`, requires `result.error` to be `undefined` and
  `result.signal` to be `null`, and recursively removes the directory in
  `finally` after the child exits. Repeated read-only invocations need no
  reset. Tests share no path, process, port, database, table, generated
  initializer, counter, or clock.
- There is no database, parent row, dependent table, sibling table, foreign
  key, cascade, or ordered cleanup obligation.
- For each existing regular-file fixture, the test fingerprints exact bytes
  plus `fs.statSync(path, { bigint: true }).mode`, `.size`, and `.mtimeNs`;
  all compare equal before and after invocation. Directory cases also retain
  type and sorted entries; absent paths remain absent.
- Targeted test command: `node --test app/tests/pending-jobs.test.js`.
- Required post-implementation commands: `npm test --prefix app` and
  `.github/scripts/test-immutability-check.sh`.
- The test-author stage precedes the Builder stage. Neither stage edits
  existing tests, `app/server.js`, `app/package.json`, lockfiles,
  `factory/KIT_PIN`, ticket state, the ledger, or Factory controls.
- There are no endpoint paths, HTTP shapes, UI routes, UI selectors, global
  page-shell literals, accessible UI names, auth flows, cookies, CORS
  origins, credentials, domains, cross-origin requests, or preview
  deployments. The complete topology is a local file-to-stdout child
  process.
- The command cannot trigger an external send, so `External: no` remains
  exact.

### Protected global-text result

The pinned Factory readiness checker (`factory/KIT_PIN`
`bcffd8f0d3d0596752136d7ee2fd95e4f912806c`) returned `GLOBAL TEXT PASS`
separately for the usage line, all three error lines, and each new output
property name (`id`, `eventId`, `attempts`, `retries`). No protected
global-text collision, fixture-seam expansion beyond the two declared
reference seams, or protected-test conflict declaration is required.

## Criteria checklist

- **Pass/fail and unambiguous:** AC1-7 freeze exact exit codes, stdout and
  stderr bytes, argument counts, key order, sort order, timeout and signal
  outcomes, sentinels, fixture serialization, stat fields, and prohibited
  source references. AC8 is decided from Git history, per-stage path diffs,
  and three exact command exit statuses. No criterion uses an unquantified
  term.
- **Coverage:** AC1 covers filtering, sorting, key order/omission, privacy,
  and repeatability. AC2 covers both empty-result shapes. AC3 covers arity.
  AC4 covers both portable read failures. AC5 covers malformed JSON. AC6
  covers every structural rule (including both new uniqueness rules) via
  S1-S26 and whole-array validation before filtering. AC7 covers
  termination, read-only behavior, and the offline source boundary. AC8
  covers file ownership and verification boundaries. Every frozen-contract
  element (interface, validator, four fixtures, 26 structural fixtures, file
  locations, protected boundaries, topology) appears in at least one
  criterion.
- **Ambiguity log:** see below; every open question was resolved from
  `SPEC.md`, `app/server.js`, an existing sibling tool, or this ticket's own
  frozen wording — none required operator escalation.
- **Cross-ticket file-boundary conflict:** a grep of `factory/tickets/*.md`
  found only T-271 declaring `app/tools/pending-jobs.js` or
  `app/tests/pending-jobs.test.js`. T-270 (`app/tools/event-type-summary.js`,
  `app/tests/event-type-summary.test.js`) and T-272
  (`app/tools/sent-approvals.js`, `app/tests/sent-approvals.test.js`) are
  Backlog and own distinct paths; their branches `origin/ticket/T-270` and
  `origin/ticket/T-272` were also inspected directly and declare the same
  distinct paths, so no leased-but-unmerged conflict exists either.
- **Deploy/topology completeness:** the contract is entirely local and
  offline; it has no HTTP, UI, auth, cookie, CORS, cross-origin,
  preview-deployment, domain, credential, or external-send topology, stated
  explicitly in "File locations, lifecycle, protected boundaries, and
  topology" above.
- **Derived-fixture distinctness:** S15's exact uppercase `PENDING` is a
  frozen literal byte-distinct from every valid fixture's lowercase
  `"pending"`; AC6 requires Fixtures A, B, C, D, and S1-S26 to be pairwise
  byte-distinct before any invocation.
- **Generated-value determinism:** all fixture IDs, counters, and timestamps
  are literals. Fixture A's `job-Zulu-event`, `job-alpha-event`,
  `job-middle-event`, `job-omega-event`, and `job-extra-field-event` are each
  evaluated against Relay's `"job-" + eventId` initializer
  (`app/server.js:42`), and `job-Zulu-event`'s retried-then-pending counters
  (`attempts: 4`, `retries: 1`, `attemptsSinceRetry: 0`, non-null
  `lastError`) are reachable under one accepted retry
  (`app/server.js:115-125`). No generated identifier, sequence, counter, or
  clock participates in an exact expectation.
- **Fixture lifecycle closure:** every case owns one temporary parent, waits
  for its child, and recursively tears down every artifact in `finally`.
  There is no shared mutable resource, database, parent row, sibling
  dependent table, non-cascading foreign key, or serialized setup/teardown
  dependency.
- **Protected source boundaries:** `app/tests/dead-jobs.test.js` and
  `app/tests/job-attempts.test.js` are the two reference-only seams
  informing this contract; both, and every other tracked test, bind their
  source assertions to their own named `TOOL` constant with no restriction
  on `pending-jobs.js` or `node:fs`. `python3
  "$FACTORY_RELEASE_PATH/scripts/ticket-readiness.py" --ticket T-271
  --workdir "$PWD"` returned `READINESS PASS`.
- **Global protected text:** `python3
  "$FACTORY_RELEASE_PATH/scripts/ticket-readiness.py" --global-literal
  '<text>' --workdir "$PWD"` returned `GLOBAL TEXT PASS` for the usage line,
  all three error lines, and the four output property names — run
  separately for each.

## Ambiguity log

1. **Which error-message/exit-code convention is "established"?** The seed
   named four failure categories (usage, unreadable-file, invalid-JSON,
   invalid-state) with "the established exit codes" (plural), which matches
   the newer `fail()`-helper convention in `app/tools/job-attempts.js` and
   `app/tools/job-progress.js` (usage exits `2`; the other three exit `1`),
   not the older two-message, single-exit-code convention in
   `app/tools/dead-jobs.js`. Resolved in favor of the newer, now-dominant
   convention and declared as a Fixture-Seam.
2. **Does "unique ... IDs and event IDs" apply to the whole `jobs` array or
   only pending jobs?** Resolved as the whole array: the sentence is part of
   "Job records require..." stated before "Invalid records fail before
   filtering," and S26 freezes a valid pending job invalidated by a
   non-pending invalid sibling, proving whole-array validation independent
   of the status filter.
3. **Is job-`eventId` uniqueness a new rule with no code precedent?** Yes —
   no shipped tool enforces it (only `id` uniqueness exists, in
   `job-attempts.js`/`job-progress.js`). `Product-Decisions: frozen` and
   AC3's own unambiguous wording make this ticket the authoritative source;
   no operator escalation was needed or performed.
4. **Which fields are emitted or withheld?** Resolved from AC2's exact key
   list (`id`, `eventId`, `attempts`, `retries`) and the ticket description's
   "without exposing stored errors" — `status`, `attemptsSinceRetry`, and
   `lastError` are withheld even though `status` is the filter key.
5. **What does "code-point order" mean operationally?** Resolved as
   JavaScript's default `<`/`>` string comparison, matching
   `app/tools/dead-jobs.js`'s shipped sort implementation.
6. **How is an unreadable file distinguished from a directory or OS denial
   portably?** Resolved with the `fs.statSync` + `isFile()` pattern from
   `job-attempts.js`/`job-progress.js`; the two required portable fixtures
   are an absent path and an empty directory.
7. **Do sibling or concurrently leased tickets overlap ownership?** No — see
   the Criteria checklist entry above; verified against both the ticket
   files and the two other leased branches.
8. **Do protected source or global-text checks block the contract?**
   No — see the Criteria checklist entries above; the pinned readiness and
   global-text checks both passed for every new shell-visible literal.
9. **Do topology, auth, UI, or external effects add obligations?** No. The
   complete topology is one local file-to-stdout process with no endpoint,
   UI, auth, cookie, CORS, cross-origin, preview-deploy, domain, credential,
   or send path. `External: no` is correct.
10. **Does cleanup have dependent resources?** No. Each child reads only an
    isolated temporary file or directory, exits before recursive teardown,
    and shares no server, database, row, foreign key, port, counter, or
    process.

No entry in `factory/rulings.md` addresses offline-command behavior, so
nothing there changed this contract and no new ruling was recorded.

## Coordination

T-271 owns only `app/tools/pending-jobs.js` and
`app/tests/pending-jobs.test.js`. T-270 and T-272 are Backlog and own the
distinct `event-type-summary` and `sent-approvals` path pairs (verified both
in `factory/tickets/` and on their leased branches
`origin/ticket/T-270`/`origin/ticket/T-272`); there is no file-boundary
conflict. `app/tools/dead-jobs.js` and `app/tools/job-attempts.js` /
`app/tools/job-progress.js`, and their protected tests, are reference-only
and remain unchanged.

## Planning log

- 2026-08-26: Planner froze contract version 1 with byte-exact success and
  failure streams, status-filtered and sorted list output, whole-`jobs`-array
  validation including both `id` and `eventId` uniqueness before filtering,
  4 positive/empty fixtures plus 26 literal structural-invalid fixtures,
  pairwise fixture distinctness, deterministic counter fixtures reachable
  through Relay's initializer and retry path, read-only lifecycle checks,
  protected source boundaries, local-only topology, and test-first file
  ownership. Product docs (`SPEC.md`, `AGENTS.md`), current code
  (`app/server.js`, `app/tools/dead-jobs.js`, `app/tools/job-attempts.js`,
  `app/tools/job-progress.js`, `app/tools/pending-approvals.js`),
  `factory/rulings.md`, all sibling ticket declarations, both other Backlog
  ticket declarations and their leased branches, all tracked protected
  tests, and every new shell-visible literal were checked. The pinned
  `ticket-readiness.py` from `factory/KIT_PIN`
  `bcffd8f0d3d0596752136d7ee2fd95e4f912806c` returned `READINESS PASS` for
  T-271 and `GLOBAL TEXT PASS` for each new shell-visible literal (the usage
  line, the three error lines, and the four output property names). No
  operator ruling or escalation was required.

### Spec-linter report — 2026-08-26

| Contract element | Positive | Failure/empty | Duplicate | Out-of-scope disposition |
|---|---|---|---|---|
| One-argument local command and compact success envelope | AC1 | AC3-5 | n/a | No network/service topology (AC7) |
| Pending-only filter, four-key ordered projection, and ASCII fixture ordering | AC1 | AC2 | n/a | n/a |
| Withholding job/state privacy content | AC1-2 | AC4-6 | n/a | n/a |
| Arity-before-access and exact usage failure | AC3 | AC3 | n/a | n/a |
| Readable-regular-file, JSON, and structural-error precedence | AC4-6 | AC4-6 | n/a | n/a |
| Exact root, collection, job-field, and counter validation | AC6 | AC6 | AC6 (S24-S25) | n/a |
| Whole-array validation before filtering | AC6 (S26) | AC6 (S26) | AC6 (S24-S25) | n/a |
| `events`, `approvals`, and `outbox` entries are opaque | **uncovered** | **uncovered** | n/a | n/a |
| Additional job properties accepted and omitted | AC1 | n/a | n/a | n/a |
| Read-only inputs, termination, and offline dependency boundary | AC1-7 | AC3-6 | n/a | External effects expressly out of scope (AC7/topology) |
| File ownership, commit order, and required verification | AC8 | AC8 | n/a | n/a |
| Unicode sort semantics | **contradictory** | n/a | n/a | n/a |
| Fixture A retried-job reachability | **unreachable as frozen** | n/a | n/a | n/a |

1. Frozen Fixture A's `job-Zulu-event` is asserted to be a Relay-reachable retried pending job, but its exact counters cannot result from `app/server.js`: a first dead-letter occurs at `attempts: 3`/`attemptsSinceRetry: 3`; `retryJob` then resets only `attemptsSinceRetry` to `0` and increments `retries` to `1`, yielding `attempts: 3`, not `4`. A further failed processing attempt would yield `attempts: 4` and `attemptsSinceRetry: 1`, not `0`. This violates the required generated-value reachability check and lets the frozen fixture assert an impossible persisted Relay state.
2. The description requires `id` sorting in code-point order, while the frozen contract simultaneously defines JavaScript default `<`/`>` ordering as “UTF-16 code-unit / code-point order.” Those orders diverge for valid Unicode strings (for example, a supplementary-plane character versus a BMP private-use character), and AC1 exercises only ASCII IDs. The Builder therefore cannot satisfy a single determinate Unicode sort rule.
3. The frozen contract says entries in `events`, `approvals`, and `outbox` are opaque, but no acceptance criterion uses a valid state whose entries would be rejected by a schema-validating implementation. Fixtures A, C, and D contain ordinary product-shaped entries or empty arrays. A Builder could validate those collection entries and pass every criterion, contrary to the contract's material opacity guarantee.

SPEC-LINT: FAIL — unreachable fixture values, contradictory Unicode ordering, and an uncovered opacity rule leave material behavior indeterminate.

## Frozen contract — version 2

- **Freeze result:** PASS. Contract version 2 is frozen.

This version repairs the three Spec-lint findings against version 1. Version
1's command-line interface, valid-state rules, failure protocol and
precedence, Fixtures B/C/D, Fixtures S1-S26, and the file-locations/lifecycle/
protected-boundaries/topology section are unchanged and remain in force.
Version 1 itself is not edited or removed; this section supersedes only the
specific elements named below. Nothing here required an operator ruling —
each repair is derived from `app/server.js`'s own logic or from restating one
existing rule unambiguously.

### Repair 1 — Frozen Fixture A, version 2 (reachable counters)

Verifying finding 1 against `app/server.js` (`processJob` lines 54-66,
`retryJob` lines 115-125) surfaced a second unreachable value in the same
fixture beyond the one the linter named: `job-extra-field-event`'s
`lastError: null` cannot coexist with `attempts: 1` while `status` stays
`pending`, because the only code path that increments `attempts` without
changing `status` away from `pending` (the `attempts <= failTimes` branch)
unconditionally sets `lastError` to a non-null string first. Both records are
repaired together as "Frozen Fixture A, version 2." Every other job record in
Fixture A (`job-alpha-event`, `job-middle-event`, `job-omega-event`) was
independently re-verified against the same code and is already reachable
as-is (a fresh unprocessed job, one successful `processJob` call, and three
failing `processJob` calls respectively); those three records are unchanged.

`job-Zulu-event` is replaced with:

```json
{
  "id": "job-Zulu-event",
  "eventId": "Zulu-event",
  "status": "pending",
  "attempts": 3,
  "lastError": "PENDING-LASTERROR-SENTINEL",
  "retries": 1,
  "attemptsSinceRetry": 0
}
```

This is reachable: three failing `processJob` calls raise `attempts` to `3`
and `attemptsSinceRetry` to `3`, dead-lettering the job; `retryJob` then
resets `attemptsSinceRetry` to `0`, increments `retries` to `1`, leaves
`attempts` at `3` (unchanged), and preserves the prior `lastError` — a state
reachable through three failed attempts and one retry.

`job-extra-field-event` is replaced with:

```json
{
  "id": "job-extra-field-event",
  "eventId": "extra-field-event",
  "status": "pending",
  "attempts": 1,
  "lastError": "EXTRA-FIELD-LASTERROR-SENTINEL",
  "retries": 0,
  "attemptsSinceRetry": 1,
  "ignoredDiagnostic": "IGNORED-JOB-FIELD-SENTINEL"
}
```

This is reachable: one failing `processJob` call (`failTimes >= 1`) raises
`attempts` and `attemptsSinceRetry` to `1` each, sets a non-null `lastError`,
and leaves `status` as `pending` since `attemptsSinceRetry (1) < MAX_ATTEMPTS
(3)`. Only `lastError` changed from version 1 (`null` → a sentinel string);
`attempts`, `retries`, and therefore the projected output are unchanged.

The version 2 sentinel set for Fixture A adds `EXTRA-FIELD-LASTERROR-SENTINEL`
to the six sentinels version 1 named, for seven total. AC1, as repaired
below, checks both streams against all seven.

Because only `job-Zulu-event`'s `attempts` value changed the projected
output, the corrected frozen stdout line for Fixture A version 2 (supersedes
version 1's line) is:

```text
[{"id":"job-Zulu-event","eventId":"Zulu-event","attempts":3,"retries":1},{"id":"job-alpha-event","eventId":"alpha-event","attempts":0,"retries":0},{"id":"job-extra-field-event","eventId":"extra-field-event","attempts":1,"retries":0}]
```

**AC1 is repaired as follows:** wherever AC1 (in the Acceptance criteria
section above) refers to "Frozen Fixture A" or "the frozen compact stdout
array (defined below)," it refers to Frozen Fixture A version 2 and the
version 2 stdout line above — not version 1's. Version 1's `job-Zulu-event`
literal (`attempts: 4`), version 1's `job-extra-field-event` literal
(`lastError: null`), and version 1's stdout line are superseded and MUST NOT
be implemented by the Builder or asserted by the test-author. Every other
requirement of AC1 (key order and omission, privacy-sentinel absence across
all seven sentinels, byte-identical repeat invocation, fixture/parent
integrity) is unchanged and applies verbatim to Fixture A version 2.

### Repair 2 — one determinate sort rule, plus Frozen Fixture E

The sort rule is exactly: ascending by `id` using JavaScript's default
`<`/`>` string comparison, i.e. **UTF-16 code-unit order**. This is the only
ordering rule the Builder must implement. The phrase "code-point order" —
used in the Spec'd description above, in version 1's contract parenthetical,
and in ambiguity-log item 5 — is an imprecise restatement of this same
UTF-16-code-unit rule; it is superseded by this sentence and does not impose
a second or different ordering requirement. That historical phrasing is left
in place above for continuity, not because it adds an obligation.

UTF-16 code-unit order and full Unicode code-point order coincide for every
character below U+10000 (the entire Basic Multilingual Plane) and diverge
only when comparing a supplementary-plane character (U+10000-U+10FFFF,
encoded in UTF-16 as a surrogate pair whose lead unit is U+D800-U+DBFF)
against a BMP character in U+E000-U+FFFF: the lead surrogate's code-unit
value is numerically lower than U+E000-U+FFFF, so UTF-16 code-unit order
sorts the supplementary character first, while full code-point order (which
compares the complete scalar value) sorts every supplementary character
after every BMP character, reversing that pair. Every `id` in every version 1
fixture (A, C, D, S1-S26) is ASCII, so none of them exercises this
divergence. A new fixture makes the UTF-16-code-unit-specific behavior
mechanically checkable.

Frozen Fixture E is written as the exact UTF-8 bytes, plus one final LF:

```text
{"events":[],"jobs":[{"id":"job-😀","eventId":"event-emoji-sentinel","status":"pending","attempts":0,"lastError":null,"retries":0,"attemptsSinceRetry":0},{"id":"job-�","eventId":"event-replacement-sentinel","status":"pending","attempts":0,"lastError":null,"retries":0,"attemptsSinceRetry":0}],"approvals":[],"outbox":[]}
```

`job-😀` contains U+1F600 (a supplementary-plane character, one UTF-16
surrogate pair `😀`); `job-�` contains U+FFFD (a BMP character,
the single code unit `�`). Both jobs are freshly created and unprocessed
(`attempts`/`retries`/`attemptsSinceRetry: 0`, `lastError: null`), so both
are trivially reachable. Comparing `"job-😀"` against `"job-�"` under UTF-16
code-unit order: the shared `"job-"` prefix is equal, then `\uD83D`
(55357) < `�` (65533), so `"job-😀"` sorts first. Under full
Unicode code-point order this pair reverses, because U+1F600 (128512) >
U+FFFD (65533).

**AC9 (new).** Given Frozen Fixture E, invoking
`node app/tools/pending-jobs.js <fixture-path>` exits `0`, writes zero
stderr bytes, and writes exactly this stdout (plus one trailing LF):

```text
[{"id":"job-😀","eventId":"event-emoji-sentinel","attempts":0,"retries":0},{"id":"job-�","eventId":"event-replacement-sentinel","attempts":0,"retries":0}]
```

— confirming the Builder sorts by UTF-16 code-unit order (`job-😀` before
`job-�`), the reverse of what full Unicode code-point order would produce.
Fixture E retains its exact bytes, numeric mode, size, and nanosecond
modification time, and its parent retains its sorted entry names, after
invocation.

### Repair 3 — opacity of `events`/`approvals`/`outbox` entries, plus Frozen Fixture F

No version 1 fixture contained an `events`, `approvals`, or `outbox` entry
that a schema-validating implementation would reject, so the contract's
opacity guarantee for those three arrays' entries was asserted but never
exercised. Frozen Fixture F closes this gap.

Frozen Fixture F is written as the exact UTF-8 bytes, plus one final LF:

```text
{"events":[{"malformed":true},"not-an-object",42,null],"jobs":[{"id":"job-opaque-event","eventId":"opaque-event","status":"pending","attempts":0,"lastError":null,"retries":0,"attemptsSinceRetry":0}],"approvals":[{"missing":"fields"},null],"outbox":["ARBITRARY-OPAQUE-OUTBOX-SENTINEL",42]}
```

`events` and `outbox` each contain a malformed object, a bare string, a
number, and (for `events`) `null`; `approvals` contains a malformed object
and `null`. None resembles a real event/approval/outbox record. The single
job is freshly created and unprocessed, hence reachable.

**AC10 (new).** Given Frozen Fixture F, invoking
`node app/tools/pending-jobs.js <fixture-path>` exits `0`, writes zero
stderr bytes, and writes exactly
`[{"id":"job-opaque-event","eventId":"opaque-event","attempts":0,"retries":0}]`
plus one trailing LF — proving `events`, `approvals`, and `outbox` entries
are never structurally validated, only that each top-level property is an
array (already covered by S3/S5/S6). Fixture F's sentinel
`ARBITRARY-OPAQUE-OUTBOX-SENTINEL` does not appear in either stream. Fixture
F retains its exact bytes, numeric mode, size, and nanosecond modification
time, and its parent retains its sorted entry names, after invocation.

### AC6 pairwise-distinctness, extended

AC6's requirement that "Fixtures A, B, C, D, and S1-S26 are pairwise
byte-distinct" is extended in version 2: before any invocation, Fixture A
version 2 (replacing version 1's Fixture A in this set), B, C, D, E, F, and
S1-S26 must be pairwise byte-distinct. Fixture A version 2 differs from
every other fixture by construction (distinct job records); Fixtures E and F
are likewise distinct from every other fixture by construction.

### Version 2 criteria checklist

- **Pass/fail and unambiguous:** the repaired AC1, and new AC9/AC10, freeze
  exact exit codes, stdout/stderr bytes, key order, sentinel absence, and
  fixture/parent integrity, matching the rest of the suite's style. No
  criterion uses an unquantified term.
- **Coverage:** repaired AC1 exercises corrected Fixture A version 2
  (closing the "unreachable fixture values" finding). AC9 exercises the
  UTF-16-code-unit-vs-code-point divergence (closing the "contradictory
  Unicode ordering" finding). AC10 exercises opacity of `events`/`approvals`/
  `outbox` entries (closing the "uncovered opacity rule" finding). The
  extended AC6 clause covers Fixtures E and F in the pairwise-distinctness
  check.
- **Ambiguity log:** see the version 2 additions below; every item was
  resolved from `app/server.js`'s own logic or from restating one existing
  rule unambiguously — none required operator escalation.
- **Cross-ticket file-boundary conflict:** unchanged from version 1 — no new
  file is introduced; `app/tools/pending-jobs.js` and
  `app/tests/pending-jobs.test.js` remain T-271's sole owned paths.
- **Deploy/topology completeness:** unchanged from version 1 — still no
  network, UI, auth, or external-send topology.
- **Derived-fixture distinctness:** see "AC6 pairwise-distinctness, extended"
  above.
- **Generated-value determinism:** every job record in Fixture A version 2,
  Fixture E, and Fixture F was checked against `app/server.js`'s job
  initializer (`app/server.js:41-49`), `processJob` (`app/server.js:54-66`),
  and `retryJob` (`app/server.js:115-125`) and is reachable as documented in
  Repairs 1-3 above. No generated identifier, sequence, counter, or clock
  participates in an exact expectation.
- **Fixture lifecycle closure:** AC9 and AC10 follow the same one-temporary-
  parent, synchronous-child, `finally`-teardown pattern as every other
  criterion; no new shared or dependent resource is introduced.
- **Protected source boundaries:** unchanged from version 1 — no new file,
  no new module specifier beyond `node:fs` for the Builder file.
- **Global protected text:** AC9 and AC10 introduce no new shell-visible
  literal beyond the four output property names (`id`, `eventId`,
  `attempts`, `retries`) already checked `GLOBAL TEXT PASS` for version 1;
  no re-run was required.

## Ambiguity log — version 2 additions

11. **What does "code-point order" mean operationally, precisely enough to
    resolve a real divergence?** Resolved as UTF-16 code-unit order only
    (JavaScript's default `<`/`>`), per Repair 2 above; "code-point order" in
    earlier text is historical phrasing for the same rule, not a second
    requirement. Confirmed mechanically reachable and non-vacuous via Fixture
    E, whose two IDs are chosen so the two orderings actually disagree.
12. **Does opacity of `events`/`approvals`/`outbox` entries need its own
    fixture, or is it implied by never reading those fields?** Needs its own
    fixture — "the validator does not consume field X" is not the same
    claim as "the validator accepts any shape for field X," and only a
    fixture with a shape a naive stricter implementation would reject can
    distinguish the two. Resolved with Fixture F and AC10.
13. **Was Fixture A's unreachable-counter defect isolated to
    `job-Zulu-event`, as the linter's example suggested?** No — re-deriving
    every job record in Fixture A against `app/server.js` surfaced a second
    unreachable combination (`job-extra-field-event`'s `lastError: null`
    with `attempts: 1`, pending). Both are repaired together in Repair 1;
    the other three records were confirmed reachable as-is.

## Planning log — version 2 repair

- 2026-08-26: Planner reran after `SPEC-LINT: FAIL` on contract version 1.
  Preserved version 1's acceptance criteria, frozen contract, criteria
  checklist, ambiguity log, coordination section, planning log, and the
  spec-linter report byte-for-byte; appended frozen contract version 2
  answering all three material findings: (1) corrected Fixture A's
  `job-Zulu-event` and (newly discovered during verification)
  `job-extra-field-event` to counter values reachable through
  `app/server.js`'s `processJob`/`retryJob` path, with a corrected stdout
  line; (2) replaced the contradictory "code-point order" language with one
  determinate UTF-16-code-unit rule and added Frozen Fixture E plus AC9 to
  make the divergence from full code-point order mechanically checkable;
  (3) added Frozen Fixture F plus AC10 exercising the previously-uncovered
  opacity of `events`/`approvals`/`outbox` entries. No operator ruling or
  escalation was required — every repair was derived from `app/server.js`
  or from restating one existing rule unambiguously. `factory/rulings.md`
  was checked and required no new entry, since no operator ruling was
  applied. No new file, module specifier, or shell-visible literal was
  introduced, so no protected-test or global-text re-check was required
  beyond what version 1 already passed.

### Spec-linter report — 2026-08-26 (round 2)

| Contract element | Positive | Failure/empty | Duplicate | Out-of-scope disposition |
|---|---|---|---|---|
| One-argument local command and compact success envelope | AC1 | AC3-5 | n/a | No service/network topology (AC7) |
| Pending-only filtering, ordered four-key projection, and privacy withholding | AC1 | AC2, AC4-6 | n/a | No external send (topology) |
| UTF-16 code-unit `id` ordering | AC1, AC9 | n/a | n/a | n/a |
| Empty jobs and all-non-pending jobs | AC2 | AC2 | n/a | n/a |
| Arity-before-access and exact usage protocol | AC3 | AC3 | n/a | n/a |
| Readable-regular-file, JSON, and structural failure precedence | AC4-6 | AC4-6 | n/a | n/a |
| Exact root/collection/job validation and accepted extra job properties | AC1 | AC6 (S1-S23, S26) | AC6 (S24-S25) | n/a |
| Whole-array validation before filtering | AC6 (S26) | AC6 (S26) | AC6 (S24-S25) | n/a |
| Opacity of `events`, `approvals`, and `outbox` entries | AC10 | AC6 (S3, S5-S6) | n/a | n/a |
| Read-only operation, termination, and offline source boundary | AC1-7, AC9-10 | AC3-6 | n/a | No HTTP, UI, auth, or external-effect path (topology) |
| Fixture distinctness, lifecycle, ownership, commit order, and verification | AC6, AC8-10 | AC8 | n/a | No database/foreign-key cleanup (lifecycle) |
| Protected tests and global shell text | AC7; readiness/global-literal checks | n/a | n/a | `Protected-Test-Conflicts: none` is supported by exact-source inspection |

SPEC-LINT: PASS

## Test-author log

- 2026-08-26: Added `app/tests/pending-jobs.test.js` in test-only commit
  `c6db02f`. The focused suite covers AC1 (pending projection, privacy,
  repeatability, and immutability), AC2 (empty results), AC3 (arity before
  access), AC4 (unreadable paths), AC5 (invalid JSON), AC6 (S1-S26 and
  fixture distinctness), AC7 (offline source boundary and child harness),
  AC9 (UTF-16 ordering), and AC10 (opaque collection entries). It was run
  locally with `node --test app/tests/pending-jobs.test.js` and fails because
  the owned Builder command is absent, as expected. AC8's Git-history order,
  per-commit path ownership, and post-implementation broad verification are
  not expressible as a pre-implementation acceptance test; retain them as
  Narrator/demo-check items.
