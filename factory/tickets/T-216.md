# T-216 — count Relay approvals at one proposal timestamp offline

Kit-SHA: 7ae447baf7287e32a9df60c9453da6edec454e7b
State: Planning
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-216
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/approval-detail.test.js
Authentication-Seams: none
Builder ownership: app/tools/approval-timestamp-count.js only
Test-author ownership: app/tests/approval-timestamp-count.test.js only

## Description

Add a zero-dependency, read-only Node command that counts persisted Relay
approvals whose stored `proposedAt` value exactly matches one
operator-supplied timestamp while the service is offline.

## Acceptance criteria

1. `node app/tools/approval-timestamp-count.js <state-file> <timestamp>` exits
   zero and emits deterministic compact JSON with exactly one
   `approvalsAtTimestamp` nonnegative integer.
2. Matching compares stored and supplied strings exactly, without parsing,
   normalization, coercion, or timezone conversion. No exact match returns
   `{"approvalsAtTimestamp":0}` followed by one newline.
3. Missing arguments and missing, unreadable, malformed, or structurally
   invalid state are rejected with fixed redacted errors that reveal no path,
   timestamp, approval ID, action, reason, job link, or stored content;
   complete approval validation precedes counting.
4. Every invocation is offline, read-only, and self-terminating; input bytes,
   mode, timestamps, and sibling directory entries remain unchanged.
5. Complete acceptance tests are committed before implementation. Targeted,
   full app, and immutability checks pass without dependency, server, schema,
   package-manifest, lockfile, or Factory-control changes.

## Coordination

This ticket owns only its two new `app/` paths and is independent of T-217,
T-218, and the q75 cohort T-213 through T-215.
