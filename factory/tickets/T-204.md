# T-204 — count all Relay jobs offline

Kit-SHA: 80586f468ad78d21c54a80047bc441e10082eb48
State: Done
Operator-Approval: Receipt
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-204
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/state-summary.test.js
Authentication-Seams: none
Builder ownership: app/tools/job-count.js only
Test-author ownership: app/tests/job-count.test.js only

## Spec'd description

Add `app/tools/job-count.js`, a zero-dependency CommonJS command that reads one
persisted Relay state file while Relay is offline, validates the complete
stored job collection, and reports only its total cardinality as
`{"jobs":<count>}`. This gives operators a deterministic count without
starting Relay, changing durable state, or exposing job IDs, event links,
statuses, counters, errors, event payloads, approval details, outbox details,
or unrelated stored content.

Product-doc and repository sources:

- [`SPEC.md` behavior 2](../../SPEC.md#behavior-spec) defines durable jobs,
  attempts, retries, and the dead-letter outcome.
- [`SPEC.md` behavior 6](../../SPEC.md#behavior-spec) makes the atomically
  persisted state file Relay's durable source, which this command reads
  without participating in startup or recovery.
- [`SPEC.md` behavior 7](../../SPEC.md#behavior-spec) makes jobs and attempts
  operator-visible, while
  [`SPEC.md` conformance cases 2-4 and 7](../../SPEC.md#test-cases-the-conformance-suite)
  require one job per distinct event, recorded attempts, visible dead jobs,
  and durable recovery.
- The current [`app/server.js` state model](../../app/server.js) establishes
  the four stored collections and the persisted job fields and status enum.
- The shipped [`state-summary` command](../../app/tools/state-summary.js) and
  its [acceptance suite](../../app/tests/state-summary.test.js) establish the
  one-path, compact-JSON, redacted, read-only offline-summary convention. The
  shipped [`job-attempts` command](../../app/tools/job-attempts.js) and its
  [acceptance suite](../../app/tests/job-attempts.test.js) establish complete
  job-record and unique-ID validation before reading a job projection. The
  shipped [`event-count` command](../../app/tools/event-count.js) establishes
  the exact count-only command envelope.
- [`AGENTS.md` Boundaries](../../AGENTS.md#boundaries) keeps `app/`
  zero-dependency, and [`AGENTS.md` Verification](../../AGENTS.md#verification)
  defines the later full-suite and immutability checks.

No endpoint, HTTP shape, UI selector, global-shell UI text, persisted schema,
server behavior, package, dependency, lockfile, Factory control,
authentication path, deployment topology, or external-send behavior changes.

## Acceptance criteria

1. Given Frozen Fixture A, each of two invocations of
   `node app/tools/job-count.js <fixture-a-absolute-path>` exits `0`, writes
   exactly `{"jobs":3}\n` to stdout, and writes zero stderr bytes. Neither
   stream contains any member of the frozen sensitive-token set. After each
   invocation, Fixture A has identical bytes and BigInt numeric `mode`, `size`,
   and `mtimeNs`, and its parent has identical sorted entry names.
2. Given Frozen Fixture B, one invocation exits `0`, writes exactly
   `{"jobs":0}\n` to stdout, writes zero stderr bytes, emits no frozen
   sensitive token, and leaves the fixture and parent unchanged by every
   observation named in AC1. Before invocation, Fixture B is parsed and
   asserted to contain the exact empty `jobs` array and frozen opaque event,
   approval, and outbox values.
3. In separate fresh empty temporary parents, the zero-argument invocation
   and the two-argument invocation with absent absolute path
   `<parent>/arity-state-MUST-NOT-LEAK.json` plus literal `extra` each exit
   `2`, write zero stdout bytes, and write exactly
   `usage: node app/tools/job-count.js <state-file>\n` to stderr. The candidate
   path remains absent and each parent retains empty sorted entries.
4. Invoking the command against the frozen absent path
   `<parent>/PATH-MUST-NOT-LEAK.json` and, separately, the frozen empty
   directory `<parent>/DIRECTORY-MUST-NOT-LEAK` exits `1`, writes zero stdout
   bytes, and writes exactly `job-count: cannot read state file\n` to stderr.
   The absent path remains absent. The directory remains a directory with
   identical BigInt numeric `mode`, `size`, `mtimeNs`, and sorted entries; both
   parents retain their sorted entries.
5. Invoking the command against Frozen Fixture C and, separately, Frozen
   Fixture D exits `1`, writes zero stdout bytes, and writes exactly
   `job-count: state file is not valid JSON\n` to stderr. Neither stream
   contains `MALFORMED-PAYLOAD-MUST-NOT-LEAK`. Each fixture's exact bytes and
   BigInt numeric `mode`, `size`, and `mtimeNs`, and each parent's sorted
   entries, remain unchanged.
6. Each S1-S25 frozen structural-invalid fixture is invoked once; every
   invocation exits `1`, writes zero stdout bytes, and writes exactly
   `job-count: invalid Relay state\n` to stderr. S25 contains one valid job
   before an invalid sibling, proving complete job-array validation precedes
   counting. Before invocation, Fixtures A-D and S1-S25 are pairwise
   byte-distinct. Every fixture retains its exact bytes and BigInt numeric
   `mode`, `size`, and `mtimeNs`, and every parent retains its sorted entries.
7. Every child invocation in AC1-AC6 uses a `5_000` ms timeout, returns with
   `error === undefined` and `signal === null`, and emits none of the complete
   frozen sensitive-token set. Static inspection asserts that the Builder
   file's complete CommonJS module-specifier set is exactly `node:fs`, its
   complete `node:fs` operation set is exactly `readFileSync`, and its source
   contains none of `server.js`, `fetch(`, `WebSocket`, `node:http`,
   `node:https`, `node:net`, `node:child_process`, `setTimeout`, or
   `setInterval`. Static inspection also asserts that the Test-author file's
   complete module-specifier set is exactly `node:assert`,
   `node:child_process`, `node:fs`, `node:os`, `node:path`, and `node:test`.
8. Git history contains the complete Test-author commit before the Builder
   commit. Apart from role-owned ticket-log appends, the Test-author commit
   creates only `app/tests/job-count.test.js` and the Builder commit creates
   only `app/tools/job-count.js`. Existing application and test files,
   `app/server.js`, `app/package.json`, lockfiles, dependencies, persisted
   schema, `factory/KIT_PIN`, and Factory controls remain unchanged. After
   implementation, `node --test app/tests/job-count.test.js`,
   `npm test --prefix app`, and
   `.github/scripts/test-immutability-check.sh` each exit `0`.

## Frozen contract — version 1

- **Freeze result:** PASS. Contract version 1 is frozen.

This is the exact interface for the Test-author and Builder. If it proves
wrong, return T-204 to Ready and append a higher-numbered frozen contract;
version 1 must not be edited or removed.

### Command-line interface and failure precedence

- Repository-root invocation is exactly
  `node app/tools/job-count.js <state-file>`.
- Exactly one user-supplied positional argument is accepted. Any other arity
  exits `2`, writes zero stdout bytes, writes exactly the following single
  LF-terminated line to stderr, and does not read or create a candidate state
  path:

  ```text
  usage: node app/tools/job-count.js <state-file>
  ```

- After arity validation, the command synchronously reads `<state-file>`
  directly as UTF-8. Any `readFileSync` failure, including an absent path, a
  directory in place of a regular file, or an OS read denial, exits `1`,
  writes zero stdout bytes, and writes exactly:

  ```text
  job-count: cannot read state file
  ```

- Readable bytes rejected by `JSON.parse` exit `1`, write zero stdout bytes,
  and write exactly:

  ```text
  job-count: state file is not valid JSON
  ```

- A parsed value that fails the frozen structurally-valid-state rules exits
  `1`, writes zero stdout bytes, and writes exactly:

  ```text
  job-count: invalid Relay state
  ```

- Failure precedence is argument count, file read, JSON parse, complete
  structural validation, then counting.
- Every error is one fixed line terminated by exactly one LF. No error includes
  the state path, OS error, parser text, input bytes, stack trace, job field,
  event field, approval field, outbox field, or second line.
- Acceptance uses absolute temporary paths. Symlink inputs and portable
  simulation of OS permission denial are outside the required fixture matrix;
  if `readFileSync` throws for either, the frozen read-error result applies.

### Structurally valid state and successful output

A structurally valid state for this command is a parsed non-null, non-array
JSON object with exactly four own properties: `events`, `jobs`, `approvals`,
and `outbox`, each an array.

Every `jobs` element must be a non-null, non-array object with:

- non-empty string own properties `id` and `eventId`;
- an own `status` property exactly equal to `pending`, `done`, or `dead`;
- own `attempts`, `retries`, and `attemptsSinceRetry` properties that are
  integers greater than or equal to zero; and
- an own `lastError` property equal to `null` or a string.

Job IDs must be pairwise distinct across the complete array. Additional job
properties are allowed and ignored. Event, approval, and outbox elements are
wholly opaque; only the fact that their top-level collections are arrays is
validated. Event IDs need not match job `eventId` values. No relationship is
imposed among `status`, the three counters, and `lastError` beyond each field's
individual rule.

The command validates the complete job array and ID uniqueness before
counting. Success exits `0`, writes zero stderr bytes, and writes one compact
JSON object followed by exactly one LF. Its only key is:

```json
{"jobs":<nonnegative integer count>}
```

`jobs` is exactly the validated `jobs` array length. No stored job ID,
`eventId`, status, counter, error, additional job field, event, approval, or
outbox value is emitted.

### Frozen Fixture A — three jobs and sensitive opaque content

`app/tests/job-count.test.js` writes this exact parsed value to an isolated
temporary `state.json` using `JSON.stringify(value, null, 2) + "\n"`:

```json
{
  "events": [
    {
      "id": "OPAQUE-EVENT-ID-MUST-NOT-LEAK",
      "payload": {
        "private": "EVENT-PAYLOAD-MUST-NOT-LEAK"
      }
    }
  ],
  "jobs": [
    {
      "id": "JOB-ID-PENDING-MUST-NOT-LEAK",
      "eventId": "EVENT-ID-PENDING-MUST-NOT-LEAK",
      "status": "pending",
      "attempts": 0,
      "lastError": null,
      "retries": 0,
      "attemptsSinceRetry": 0
    },
    {
      "id": "JOB-ID-DONE-MUST-NOT-LEAK",
      "eventId": "EVENT-ID-DONE-MUST-NOT-LEAK",
      "status": "done",
      "attempts": 4,
      "lastError": null,
      "retries": 1,
      "attemptsSinceRetry": 1,
      "ignoredDiagnostic": "IGNORED-JOB-FIELD-MUST-NOT-LEAK"
    },
    {
      "id": "JOB-ID-DEAD-MUST-NOT-LEAK",
      "eventId": "EVENT-ID-DEAD-MUST-NOT-LEAK",
      "status": "dead",
      "attempts": 6,
      "lastError": "JOB-ERROR-MUST-NOT-LEAK",
      "retries": 1,
      "attemptsSinceRetry": 3
    }
  ],
  "approvals": [
    {
      "action": {
        "body": "APPROVAL-ACTION-MUST-NOT-LEAK"
      }
    }
  ],
  "outbox": [
    {
      "body": "OUTBOX-CONTENT-MUST-NOT-LEAK"
    }
  ]
}
```

The exact success stdout is `{"jobs":3}\n`.

### Frozen Fixture B — empty jobs with opaque sibling collections

Fixture B is the exact UTF-8 byte string:

```json
{"events":[null],"jobs":[],"approvals":[7],"outbox":["OPAQUE-CONTENT-MUST-NOT-LEAK"]}
```

The code fence has one terminal LF, which is part of the fixture. Its exact
success stdout is `{"jobs":0}\n`.

### Frozen parse-invalid and structural-invalid fixtures

- Fixture C exact bytes:
  `{"jobs":[],"payload":"MALFORMED-PAYLOAD-MUST-NOT-LEAK"\n`
- Fixture D is the zero-byte file.
- S1-S25 are the exact UTF-8 strings below; every row has one terminal LF:

| ID | Exact bytes before terminal LF | Structural reason |
|---|---|---|
| S1 | `null` | root is null |
| S2 | `[]` | root is an array |
| S3 | `{"events":{},"jobs":[],"approvals":[],"outbox":[]}` | `events` is not an array |
| S4 | `{"events":[],"jobs":{},"approvals":[],"outbox":[]}` | `jobs` is not an array |
| S5 | `{"events":[],"jobs":[],"approvals":{},"outbox":[]}` | `approvals` is not an array |
| S6 | `{"events":[],"jobs":[],"approvals":[],"outbox":{}}` | `outbox` is not an array |
| S7 | `{"events":[],"jobs":[],"approvals":[],"outbox":[],"metadata":[]}` | extra top-level property |
| S8 | `{"events":[],"jobs":[],"approvals":[]}` | missing own `outbox` property |
| S9 | `{"events":[],"jobs":[null],"approvals":[],"outbox":[]}` | job is null |
| S10 | `{"events":[],"jobs":[[]],"approvals":[],"outbox":[]}` | job is an array |
| S11 | `{"events":[],"jobs":[{"id":"","eventId":"event-invalid","status":"pending","attempts":0,"lastError":null,"retries":0,"attemptsSinceRetry":0}],"approvals":[],"outbox":[]}` | job `id` is empty |
| S12 | `{"events":[],"jobs":[{"id":7,"eventId":"event-invalid","status":"pending","attempts":0,"lastError":null,"retries":0,"attemptsSinceRetry":0}],"approvals":[],"outbox":[]}` | job `id` is not a string |
| S13 | `{"events":[],"jobs":[{"id":"job-invalid-event-empty","eventId":"","status":"pending","attempts":0,"lastError":null,"retries":0,"attemptsSinceRetry":0}],"approvals":[],"outbox":[]}` | job `eventId` is empty |
| S14 | `{"events":[],"jobs":[{"id":"job-invalid-event-type","eventId":7,"status":"pending","attempts":0,"lastError":null,"retries":0,"attemptsSinceRetry":0}],"approvals":[],"outbox":[]}` | job `eventId` is not a string |
| S15 | `{"events":[],"jobs":[{"id":"job-invalid-status-case","eventId":"event-invalid","status":"DEAD","attempts":3,"lastError":null,"retries":0,"attemptsSinceRetry":3}],"approvals":[],"outbox":[]}` | status is outside the exact enum |
| S16 | `{"events":[],"jobs":[{"id":"job-invalid-status-type","eventId":"event-invalid","status":null,"attempts":3,"lastError":null,"retries":0,"attemptsSinceRetry":3}],"approvals":[],"outbox":[]}` | status is not a string |
| S17 | `{"events":[],"jobs":[{"id":"job-invalid-attempts-negative","eventId":"event-invalid","status":"dead","attempts":-1,"lastError":null,"retries":0,"attemptsSinceRetry":3}],"approvals":[],"outbox":[]}` | `attempts` is negative |
| S18 | `{"events":[],"jobs":[{"id":"job-invalid-attempts-type","eventId":"event-invalid","status":"pending","attempts":0.5,"lastError":null,"retries":0,"attemptsSinceRetry":0}],"approvals":[],"outbox":[]}` | `attempts` is not an integer |
| S19 | `{"events":[],"jobs":[{"id":"job-invalid-retries-negative","eventId":"event-invalid","status":"dead","attempts":3,"lastError":null,"retries":-1,"attemptsSinceRetry":3}],"approvals":[],"outbox":[]}` | `retries` is negative |
| S20 | `{"events":[],"jobs":[{"id":"job-invalid-retries-type","eventId":"event-invalid","status":"pending","attempts":0,"lastError":null,"retries":"0","attemptsSinceRetry":0}],"approvals":[],"outbox":[]}` | `retries` is not an integer |
| S21 | `{"events":[],"jobs":[{"id":"job-invalid-window-negative","eventId":"event-invalid","status":"dead","attempts":3,"lastError":null,"retries":0,"attemptsSinceRetry":-1}],"approvals":[],"outbox":[]}` | `attemptsSinceRetry` is negative |
| S22 | `{"events":[],"jobs":[{"id":"job-invalid-window-type","eventId":"event-invalid","status":"pending","attempts":0,"lastError":null,"retries":0,"attemptsSinceRetry":null}],"approvals":[],"outbox":[]}` | `attemptsSinceRetry` is not an integer |
| S23 | `{"events":[],"jobs":[{"id":"job-invalid-error","eventId":"event-invalid","status":"dead","attempts":3,"lastError":7,"retries":0,"attemptsSinceRetry":3}],"approvals":[],"outbox":[]}` | `lastError` is neither null nor a string |
| S24 | `{"events":[],"jobs":[{"id":"job-duplicate","eventId":"event-a","status":"pending","attempts":0,"lastError":null,"retries":0,"attemptsSinceRetry":0},{"id":"job-duplicate","eventId":"event-b","status":"done","attempts":1,"lastError":null,"retries":0,"attemptsSinceRetry":1}],"approvals":[],"outbox":[]}` | duplicate job IDs |
| S25 | `{"events":[],"jobs":[{"id":"job-valid-first","eventId":"event-valid","status":"done","attempts":1,"lastError":null,"retries":0,"attemptsSinceRetry":1},{"id":"job-invalid-missing-attempts","eventId":"event-invalid","status":"pending","lastError":null,"retries":0,"attemptsSinceRetry":0}],"approvals":[],"outbox":[]}` | valid first job followed by an invalid sibling missing own `attempts` |

Fixtures A-D and S1-S25 are pairwise byte-distinct before invocation. Fixture A
and Fixture B satisfy every frozen structural rule. Fixture C and Fixture D
fail `JSON.parse`. Every S fixture parses successfully and then fails a frozen
structural rule. S15 freezes the exact casing-derived value `DEAD`; its UTF-8
bytes differ from the valid Fixture A status `dead` and from every other
fixture. No other invalid fixture is derived by casing, normalization, or
mutation from valid fixture bytes.

### Sensitive-token set and data minimization

The complete sensitive-token set is every string literal in Fixtures A-C or
the frozen paths that contains `MUST-NOT-LEAK`, including the three job IDs,
three job `eventId` values, ignored job field, job error, opaque event ID and
payload, approval and outbox content, opaque Fixture B content, malformed
content, and the three arity/read/directory path names. For the two `.json`
path names, the complete basename and the stem ending in `MUST-NOT-LEAK` are
both members. Every invocation asserts that neither stdout nor stderr contains
any member.

Only the frozen JSON count may be emitted on success. Fixed errors contain no
fixture-derived data.

### Offline, read-only execution and fixture lifecycle

Every invocation is a synchronous
`spawnSync(process.execPath, [TOOL, ...args])` with piped bytes and
`timeout: 5_000`. Every result asserts `error === undefined` and
`signal === null`.

Each case owns a fresh `fs.mkdtempSync` parent. Before invocation, the
Test-author snapshots each regular file's exact bytes plus
`fs.statSync(path, { bigint: true }).mode`, `size`, and `mtimeNs`, waits
synchronously for each child, and recursively removes the parent in `finally`.
The directory-path case snapshots the directory's same three BigInt numeric
stat fields and sorted entries. Repeated success needs no reset because the
command is read-only. Access time is excluded because reading the fixture is
the behavior under test.

Tests share no state path, process, port, server, database, table, generated
identifier, sequence, counter, clock, or timestamp. There is no parent row,
sibling dependent table, foreign key, cascade, or ordered cleanup obligation.
Every asserted ID, count, status, and counter is direct literal fixture data;
no generated value or initializer participates in an expectation.

### File locations, protected boundaries, and topology

- Builder ownership is creation of `app/tools/job-count.js` only. It is a
  standalone CommonJS command whose complete module-specifier set is exactly
  `node:fs` and whose only `node:fs` operation is `readFileSync`.
- Test-author ownership is creation of `app/tests/job-count.test.js` only. Its
  complete module-specifier set is exactly `node:assert`,
  `node:child_process`, `node:fs`, `node:os`, `node:path`, and `node:test`.
- `app/tests/state-summary.test.js` is the protected reference-only fixture
  seam. It is not edited. Its assertions inspect only
  `app/tools/state-summary.js`, not the new Builder path. The protected
  `job-progress`, `job-attempts`, `dead-jobs`, and other tool suites likewise
  inspect only their corresponding named tools. No protected test enumerates
  `app/tools/` or `app/tests/`, restricts the new tool's import/export surface,
  or snapshots `app/tools/job-count.js`. `Protected-Test-Conflicts: none`
  remains exact.
- `app/package.json` already includes the new test through
  `node --test tests/*.test.js`; neither role edits the manifest.
- Targeted acceptance is `node --test app/tests/job-count.test.js`.
  Post-implementation repository verification is `npm test --prefix app` and
  `.github/scripts/test-immutability-check.sh`.
- No endpoint, HTTP shape, route, UI selector, global page-shell text,
  accessible UI name, authentication, cookie, CORS, cross-origin request,
  credential, domain, preview deployment, or external send is introduced.
  Topology is one local file-to-stdout process, and `External: no` remains
  exact.

## Criteria checklist

- **Pass/fail and unambiguous:** AC1-AC6 freeze exact arguments, exit codes,
  stream bytes, fixture bytes, stat fields, parent entries, and privacy
  observations. AC7 freezes timeout, process result, and static source
  boundaries. AC8 is decided from Git history, path diffs, and three exact
  command exit statuses.
- **Coverage:** AC1 covers a nonzero count, all three valid statuses, complete
  job fields, repetition, privacy, and read-only behavior. AC2 covers the
  all-zero shape and opaque unconsumed collections. AC3 proves arity handling
  precedes candidate-path use. AC4 covers two portable read failures. AC5
  covers malformed and zero-byte JSON. AC6 covers every structural rule,
  complete validation before counting, the exact derived casing fixture, and
  fixture distinctness. AC7 covers termination, privacy, and complete source
  boundaries. AC8 covers role ownership, tests-first history, unchanged
  protected surfaces, and all post-implementation commands.
- **No dead contract elements:** every command path, output and error shape,
  fixture, sensitive token, validation class, lifecycle rule, file location,
  protected boundary, topology disposition, and verification command appears
  in AC1-AC8.
- **Derived-fixture distinctness:** S15's exact uppercase result is `DEAD`,
  byte-distinct from Fixture A's `dead`. Fixtures A-D and S1-S25 are frozen as
  direct serializations and pairwise byte-compared before invocation.
- **Generated-value determinism:** every ID, status, counter, count-producing
  record, and timestamp is literal fixture data. No generated identifier,
  sequence, counter, reset, initializer, or clock supplies an expected value.
- **Fixture lifecycle closure:** every case owns an isolated temporary parent,
  waits for its child, and removes all artifacts recursively in `finally`.
  There is no database, parent row, sibling table, non-cascading foreign key,
  cascade, or shared mutable fixture.
- **Cross-ticket file boundaries:** all local ticket declarations and every
  remote `origin/ticket/T-*` record were inspected. T-200 through T-203 and
  every other sibling or remote ticket own paths distinct from both T-204
  paths; no concurrently visible implementation surface overlaps.
- **Protected boundaries:** the associated seam and tracked protected tests
  were inspected. No allowlist, exact-source assertion, source snapshot, path
  digest, or module restriction applies to the new Builder path or its sole
  `node:fs` specifier. The pinned readiness checker returned `READINESS PASS`.
- **Global protected text:** the pinned readiness checker returned
  `GLOBAL TEXT PASS` separately for the usage line, all three error lines, the
  output property name, and both exact success output lines.
- **Deploy/topology completeness:** the command is local file-to-stdout with
  no endpoint, UI, auth, cookie, CORS, origin, credential, domain,
  preview-deploy, or external-send surface.

## Ambiguity log

1. **Does "all jobs" mean array cardinality or a status-derived total?**
   Resolved from [`SPEC.md` behavior 2](../../SPEC.md#behavior-spec), which
   creates one durable job per accepted event, and the current server state
   model: after complete validation the result is exactly `state.jobs.length`;
   no status filtering or deduplication is performed.
2. **What state structure is required before counting?** The product has no
   single general-purpose state validator. Resolved from the four collections
   and job fields in [`app/server.js`](../../app/server.js), the declared
   [`state-summary` fixture seam](../../app/tests/state-summary.test.js), and
   the shipped [`job-attempts` validator](../../app/tools/job-attempts.js):
   require exactly four array collections, validate every job's complete
   persisted shape and unique ID, allow additional job fields, and leave
   sibling collection elements opaque.
3. **Must a job's `eventId` resolve to an event, or must counters agree with
   status?** Neither `SPEC.md` nor the shipped job validators impose those
   cross-record or cross-field constraints on offline reads. Resolved by
   validating each frozen job field independently, treating event entries as
   opaque, and freezing Fixture A with job `eventId` values not represented by
   the opaque event entry.
4. **How are failures classified without leaking input?** Raw OS and parser
   errors can reveal paths or state bytes. Resolved using the shipped
   count-command convention: usage exit `2`, operational exits `1`, fixed
   single-line redacted errors, empty opposite streams, and precedence from
   arity through complete structure.
5. **How are offline, read-only, and self-terminating claims proved?** Resolved
   with synchronous child invocations under a `5_000` ms timeout, exact file
   and directory fingerprints, repeated success, isolated teardown, and a
   production source boundary admitting only `node:fs` and `readFileSync`.
6. **Are derived fixtures or generated expected values involved?** S15 is the
   only casing-derived fixture; its exact result `DEAD` is frozen and
   byte-distinct from valid `dead`. Every serialization is pairwise
   byte-compared, and all expected identifiers, statuses, counters, and counts
   come from literal test setup.
7. **Does fixture cleanup have hidden dependencies?** No service or database
   is started. Each test owns one temporary directory removed after its child
   exits, so there are no parent rows, sibling tables, foreign keys, cascades,
   resets, or teardown ordering.
8. **Do sibling or concurrently leased tickets overlap ownership?** A grep of
   every local `factory/tickets/*.md` ownership declaration and inspection of
   every remote `origin/ticket/T-*` ticket record found no other owner of
   either T-204 path. T-200 through T-203 and all other visible ticket
   branches own distinct paths.
9. **Do protected tests reject the new source boundary?** Direct inspection of
   `app/tests/state-summary.test.js`, the job-tool protected suites,
   `app/tests/factory-contract.test.js`, and tracked source assertions found
   only tool-specific boundaries. The pinned readiness check returned
   `READINESS PASS`, so `Protected-Test-Conflicts: none` is exact.
10. **Does protected global text collide?** The pinned global-literal checker
    accepted every shell-visible literal named in the criteria checklist. No
    accessible UI name is added.
11. **Are deployment, authentication, or external-send details applicable?**
    No. The complete topology is a local process reading one local path and
    writing stdout/stderr; there is no network, UI, credential, preview, or
    send path, so no `external` label is required.
12. **Does an existing operator ruling change the contract?**
    [`factory/rulings.md`](../rulings.md) was checked before freeze. Its
    detail-route ruling is unrelated, and its T-178 verification ruling
    confirms the targeted test, full app suite, and immutability as the
    post-implementation verification set. No new operator decision is
    required.

## Coordination

T-204 owns only `app/tools/job-count.js` and `app/tests/job-count.test.js`.
The protected `app/tests/state-summary.test.js` seam is reference-only. Every
local sibling ticket and remote ticket branch was checked; no implementation
path overlaps.

## Planning log

- 2026-08-24: Planner froze contract version 1 with byte-level success and
  error envelopes, deterministic fixtures, complete job validation, privacy
  sentinels, read-only lifecycle, role ownership, protected-source and
  global-text results, and local-only topology. `SPEC.md`, `AGENTS.md`,
  `app/server.js`, `app/package.json`, `app/tools/state-summary.js`,
  `app/tests/state-summary.test.js`, `app/tools/job-attempts.js`,
  `app/tests/job-attempts.test.js`, `app/tools/event-count.js`, tracked
  protected source assertions, `.github/scripts/test-immutability-check.sh`,
  `factory/rulings.md`, all local sibling ownership declarations, and every
  remote `origin/ticket/T-*` ticket record were inspected. The pinned
  deterministic checker returned `READINESS PASS` for T-204 and
  `GLOBAL TEXT PASS` for every frozen shell-visible literal. No file-boundary
  conflict, protected-test conflict, topology requirement, external-send
  label, or operator decision remains.

- 2026-08-24: Spec-linter round 1 (v11) reviewed frozen contract version 1
  against `SPEC.md`, `app/server.js`, `app/package.json`,
  `app/tools/event-count.js`, `app/tools/job-attempts.js`,
  `app/tests/state-summary.test.js`, `app/tests/event-count.test.js`,
  `app/tests/factory-contract.test.js`, every tracked `app/tests/*.test.js`
  source assertion, `factory/rulings.md`, `factory/PROJECT.env`,
  `factory/KIT_PIN`, and all local and remote ticket ownership declarations.

### Contract-element coverage

| Material behavior | Positive | Failure / empty | Duplicate / repeat | Out of scope |
|---|---|---|---|---|
| Arity gate: exactly one positional argument | AC1, AC2 | AC3 zero-arg and two-arg exit `2` with the exact usage line | AC3 repeats the same candidate path in the two-arg case | none |
| Read `<state-file>` as UTF-8 via `readFileSync` | AC1, AC2 | AC4 absent path and directory-in-place-of-file exit `1` | AC1 reads Fixture A twice | Symlink inputs and portable permission-denial simulation, frozen to the read-error result |
| `JSON.parse` gate | AC1, AC2 | AC5 Fixture C truncated bytes and Fixture D zero-byte file | — | none |
| Root shape: four own array collections | AC1, AC2 | AC6 S1-S6 (null, array, each non-array collection), S8 (missing own `outbox`) | AC6 S7 extra top-level property | Root scalars grouped with S1/S2 as one equivalence class |
| Per-job field rules (`id`, `eventId`, `status`, three counters, `lastError`) | AC1 covers all three statuses, nonzero counters, both `lastError` forms, and an ignored extra field; AC2 covers the empty array | AC6 S9-S23 | AC6 S18/S20/S22 group the non-integer permutations | Cross-field relationships explicitly not imposed (see finding 1) |
| Job-ID pairwise uniqueness | AC1 three distinct IDs | AC6 S24 | AC6 S24 is the duplicate row | none |
| Complete job validation precedes counting | AC1, AC2 | AC6 S25 valid job followed by invalid sibling | — | none |
| Failure precedence: arity, read, parse, structure, count | — | AC3 before AC4 before AC5 before AC6 | — | OS-denial ordering frozen to the read-error result |
| Success envelope `{"jobs":<count>}` + one LF, zero stderr | AC1 (`3`), AC2 (`0`) | Zero stdout asserted in AC3-AC6 | AC1 asserts byte-identical stdout on both invocations | none |
| Fixed redacted errors and sensitive-token set | AC1, AC2 | AC3-AC6 exact single-line stderr; AC5 names the malformed token | AC7 re-asserts the complete token set on every AC1-AC6 invocation | none |
| Read-only durability (bytes, BigInt `mode`/`size`/`mtimeNs`, parent entries) | AC1, AC2 | AC4 directory stat and entries; AC5, AC6 fixture and parent snapshots | AC1 asserts after each of two invocations | Access time, excluded because reading is the behavior under test |
| Offline self-termination (`spawnSync`, `timeout: 5_000`, `error === undefined`, `signal === null`) | AC7 | AC7 spans every AC1-AC6 child | AC7 covers repeated invocations | none |
| Builder/Test-author source boundary (module specifiers, sole `node:fs` operation, forbidden substrings) | AC7 | AC7 forbidden-substring set | — | none |
| Fixture lifecycle and isolation (`mkdtempSync` parent, `finally` recursive removal) | AC1-AC6 | AC3, AC4 fresh empty parents | AC1 repeat needs no reset | No database, parent row, sibling table, foreign key, or cascade exists |
| Role ownership, tests-first history, unchanged protected surfaces, verification commands | AC8 | AC8 no-change assertions | — | none |

### Findings

1. SPEC-WARN: the frozen rule "no relationship is imposed among `status`, the
   three counters, and `lastError`" has no positive fixture. Fixtures A and B
   contain only canonical combinations (`pending` 0/0/0 with `lastError: null`,
   `done` with `lastError: null`, `dead` with a string `lastError`), yet
   `app/server.js` sets `lastError` to a string on a job that is still
   `pending` between attempts, which is the most common real durable state.
   A builder who over-constrained the validator (for example `pending` implies
   `lastError === null`) would pass every frozen criterion and then report
   `job-count: invalid Relay state` for a real state file. The frozen contract
   states the independence rule explicitly, so no product decision is left
   open and this is not blocking. Recommendation the Test-author may consume
   directly without editing the frozen contract: add one additional valid case
   in `app/tests/job-count.test.js` whose `jobs` array holds a `pending` job
   with `attempts` greater than zero, a nonzero `attemptsSinceRetry`, and a
   string `lastError`, asserting the ordinary success envelope.
2. SPEC-WARN: `Fixture-Seams: app/tests/state-summary.test.js` declares an
   editable seam that the contract then describes as reference-only and that
   Test-author ownership (`app/tests/job-count.test.js only`) excludes. The
   declaration is inert and safe — it only widens the readiness collision scan,
   and `Protected-Test-Conflicts: none` verifies as exact — but the two
   statements read as contradictory. Recommendation: state in the contract that
   the seam declaration grants no edit right.
3. SPEC-WARN: the invalid matrix uses one representative per equivalence class
   for root scalars (S1/S2 stand in for a number, string, or boolean root),
   missing collections (S8 stands in for a missing `events`, `jobs`, or
   `approvals`), and missing job fields (S25 stands in for every own-property
   omission other than `attempts`). This grouping is permitted; adding the
   root-scalar case alone would match the shipped `state-summary` suite at
   negligible cost.

Verification performed this pass, all independently reproduced rather than
read from the planner's claims:

- Every frozen fixture was re-serialized and run through the frozen structural
  rules. Fixture A yields count `3` and Fixture B yields count `0`; Fixtures C
  and D fail `JSON.parse`; every one of S1-S25 parses and then fails exactly
  the structural rule its row names. All 29 fixtures are pairwise byte-distinct
  and no derived invalid fixture, including the casing-derived S15 `DEAD`, is
  byte-identical to any accepted valid fixture.
- Every asserted identifier, status, counter, and count is literal fixture
  data. No generated identifier, sequence, counter, reset, initializer, or
  clock supplies an expected value, so no value is unproducible from its
  initializer.
- Fixture lifecycle closure holds: each case owns one `mkdtempSync` parent
  removed recursively in `finally` after a synchronous child. There is no
  database, parent row, sibling dependent table, non-cascading foreign key,
  cascade, or ordered cleanup obligation, and no helper or test outside
  Test-author ownership requires editing.
- Protected source boundaries were inspected directly. Each shipped tool suite
  reads only its own `app/tools/<name>.js`; no tracked test enumerates
  `app/tools/` or `app/tests/`, snapshots or digests `app/tools/job-count.js`,
  or restricts its import/export surface. `app/tests/factory-contract.test.js`
  asserts only `NONVISUAL_PATHS` and `.gitignore` entries.
  `app/package.json` already globs the new test, so neither role edits it.
- The pinned kit checker was re-run at `factory/KIT_PIN`:
  `--ticket T-204` returned `READINESS PASS`, and `--global-literal` returned
  `GLOBAL TEXT PASS` for the usage line, all three error lines, the `jobs`
  output property name, and both exact success output lines.
- Consistency against product docs holds exactly: `SPEC.md` behaviors 2, 6,
  and 7 and conformance cases 2-4 and 7 read as cited; `app/server.js`
  persists exactly the four collections and exactly the seven job fields the
  contract validates; the error envelope matches the `event-count` count-only
  precedent; and the `factory/rulings.md` detail-route ruling is unrelated
  while its T-178 ruling matches the AC8 verification set.
- No local sibling ticket and no remote `origin/ticket/T-*` record claims
  either T-204 path.

SPEC-LINT: PASS

- 2026-08-24: Test-author committed `app/tests/job-count.test.js` as the first
  commit on `ticket/T-204`, before any implementation commit. Eight tests, one
  per acceptance criterion plus one consumed spec-lint recommendation:

  | Test | Criterion |
  |---|---|
  | `AC1: two invocations against Fixture A each print the frozen three-job count and leave the fixture untouched` | AC1 |
  | `AC2: Fixture B prints the frozen zero count while its opaque sibling collections stay unconsumed` | AC2 |
  | `AC3: zero and two positional arguments fail with the exact usage envelope and never touch the candidate path` | AC3 |
  | `AC4: an absent path and a directory path fail with the exact read-failure envelope and change nothing` | AC4 |
  | `AC5: Fixtures C and D fail with the exact parse-failure envelope and never echo the malformed bytes` | AC5 |
  | `AC6: every S1-S25 structural-invalid fixture fails with the exact invalid-state envelope after a distinctness proof` | AC6 |
  | `AC7: the production command self-terminates under the frozen timeout and both files stay inside their frozen source boundaries` | AC7 |
  | `field independence: a pending job with prior attempts and a string lastError is counted as valid` | Spec-lint finding 1 (no frozen positive fixture for counter/`lastError` independence) |

  Fixtures were independently re-derived before authoring: all 29 of
  Fixtures A-D and S1-S25 are pairwise byte-distinct, Fixture A validates to
  count `3` and Fixture B to count `0`, Fixtures C and D fail `JSON.parse`, and
  every S fixture parses and then fails exactly the structural rule its row
  names. The casing-derived S15 value `DEAD` is byte-distinct from the valid
  `dead` status, so no impossible negative assertion is encoded. Every expected
  identifier, status, counter, and count is literal fixture data; no generated
  identifier, sequence, counter, or clock supplies an expectation. Each case
  owns one `fs.mkdtempSync` parent removed recursively in `finally` after its
  synchronous child; there is no database, parent row, sibling dependent table,
  foreign key, or ordered cleanup obligation.
  `app/tests/state-summary.test.js` was read only as reference and not edited;
  `Protected-Test-Conflicts: none` holds.

  `node --test app/tests/job-count.test.js` was run once locally: all eight
  tests fail solely because `app/tools/job-count.js` does not exist
  (`MODULE_NOT_FOUND` from the spawned child), which is the missing feature and
  not a setup error. Broad verification — `npm test --prefix app`,
  `scripts/repo-check`, `scripts/secret-scan`, and
  `.github/scripts/test-immutability-check.sh` — is deferred to protected CI
  and final certification per the Test-author scope.

  Demo-check items for the Narrator's bundle (criteria not expressible as
  tests from this role): AC8's Git-history ordering, per-commit path
  ownership, unchanged protected surfaces, and the three post-implementation
  command exit statuses.
Reviewer round 1 signed detail:

> ## Review: T-204 — count all Relay jobs offline (PR #206, head `0258f7d`)
>
> **Verdict: APPROVE**

reviewer round 1: APPROVE
