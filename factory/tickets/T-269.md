# T-269 — inspect Relay events with persisted numeric types

Kit-SHA: 6b79df558da1e7471950eb63a5e4e7e2b4d63d1f
State: Planning
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-269
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/event-metadata.test.js
Authentication-Seams: none
Builder ownership: app/tools/event-metadata.js only
Test-author ownership: app/tests/event-metadata.test.js only

## Description

Relay persists a truthy numeric event `type` unchanged, but the existing
offline `event-metadata` command rejects that state before exact-ID lookup.
The command already serializes the selected stored type safely as JSON.

Make exact event inspection accept persisted non-zero JSON-number types and
emit the selected number unchanged. Preserve exact lookup, key order, string
types, structural checks, failure behavior, privacy, and read-only operation.

## Acceptance criteria

1. Looking up valid events whose stored `type` values are numeric `7` and
   `-0.5` exits `0` for each, writes exactly one compact JSON line whose keys
   remain `eventId`, `type`, and `receivedAt` in that order and whose `type`
   value is the unchanged JSON number, and writes zero stderr bytes.
2. Existing valid string-type lookups remain byte-for-byte unchanged. Exact,
   case-sensitive event-ID lookup and not-found behavior remain unchanged.
3. Missing or empty types, zero, booleans, objects, arrays, invalid IDs or
   timestamps, and duplicate event IDs retain the existing invalid-state
   failure before lookup.
4. Output never adds payloads or unrelated event, job, approval, or outbox
   fields. Failures retain the existing fixed redacted one-line envelopes and
   leak none of those fields or the requested ID or path.
5. The command remains deterministic, offline, zero-dependency, read-only,
   and self-terminating. It changes no input bytes, metadata, or sibling path.
6. Tests precede implementation. The targeted test, full app suite, and
   immutability check pass without server, schema, package, dependency,
   Factory, route, or configuration changes.

## Frozen contract — version 1

- **Freeze result:** PASS. Contract version 1 is frozen.

### Interface (unchanged)

- Invocation: `node app/tools/event-metadata.js <state-file> <event-id>`
  (exactly two positional arguments).
- Exit codes: `0` success, `1` read/parse/invalid-state/not-found failure,
  `2` usage failure.
- Success stdout: exactly one compact JSON line, keys in order `eventId`,
  `type`, `receivedAt`, terminated by `\n`, and zero stderr bytes.
- Failure stderr (byte-exact, unchanged from the shipped tool):
  - Usage: `usage: node app/tools/event-metadata.js <state-file> <event-id>\n`
  - Read: `event-metadata: cannot read state file\n`
  - Parse: `event-metadata: state file is not valid JSON\n`
  - Invalid state: `event-metadata: invalid Relay state\n`
  - Not found: `event-metadata: event not found\n`
  Every failure path writes zero stdout bytes.

### Structural-validation delta (the change this ticket makes)

Replace the current per-event `type` check
(`isNonEmptyOwnString(event, "type")`, `app/tools/event-metadata.js:34`) with:

```
function isValidEventType(value) {
  return (
    (typeof value === "string" && value.length > 0) ||
    (typeof value === "number" && Number.isFinite(value) && value !== 0)
  );
}
```

`value !== 0` excludes both `0` and `-0` (strict equality treats them equal).
`Number.isFinite` is a no-op guard: `JSON.parse` never produces `NaN` or
`Infinity`, so no invalid state can smuggle a non-finite number past this
check; it exists to make the predicate self-evidently total, not to reject
any reachable input. All other structural checks (`id`, `receivedAt`,
duplicate-ID detection, collection shape) are unchanged.

### Frozen fixtures (exact bytes; each a full `state.json` file)

**New valid — numeric types (Acceptance criterion 1), two-event state:**

```
{"events":[{"id":"event-numeric-positive","type":7,"receivedAt":"2026-08-07T12:00:02.000Z"},{"id":"event-numeric-fraction","type":-0.5,"receivedAt":"2026-08-07T12:00:03.000Z"}],"jobs":[],"approvals":[],"outbox":[]}
```

Expected outputs (exit `0`, zero stderr bytes each):
- lookup `event-numeric-positive` →
  `{"eventId":"event-numeric-positive","type":7,"receivedAt":"2026-08-07T12:00:02.000Z"}\n`
- lookup `event-numeric-fraction` →
  `{"eventId":"event-numeric-fraction","type":-0.5,"receivedAt":"2026-08-07T12:00:03.000Z"}\n`

**Reclassified fixture — was invalid, is now valid (proves the exact
semantic shift; byte-identical to the tool's pre-existing `type:7`
single-event structural-invalid case, now moved out of the invalid set):**

```
{"events":[{"id":"event-alpha","type":7,"receivedAt":"2026-08-07T12:00:00.000Z"}],"jobs":[],"approvals":[],"outbox":[]}
```

Lookup `event-alpha` → exit `0`,
`{"eventId":"event-alpha","type":7,"receivedAt":"2026-08-07T12:00:00.000Z"}\n`,
zero stderr bytes. This fixture must be removed from the tool's structural-
invalid fixture set and asserted valid instead; it must not be deleted
outright, since it is the regression proof that only the `type` predicate
changed.

**New invalid — zero/boolean/object/array types (Acceptance criterion 3
coverage; each a distinct single-event state, id `event-alpha`, `receivedAt`
`2026-08-07T12:00:00.000Z`, `jobs`/`approvals`/`outbox` empty), asserted
byte-distinct from every other fixture in this contract and from every
fixture already in `app/tests/event-metadata.test.js`:**

- Zero: `{"events":[{"id":"event-alpha","type":0,"receivedAt":"2026-08-07T12:00:00.000Z"}],"jobs":[],"approvals":[],"outbox":[]}`
- Boolean: `{"events":[{"id":"event-alpha","type":true,"receivedAt":"2026-08-07T12:00:00.000Z"}],"jobs":[],"approvals":[],"outbox":[]}`
- Object: `{"events":[{"id":"event-alpha","type":{},"receivedAt":"2026-08-07T12:00:00.000Z"}],"jobs":[],"approvals":[],"outbox":[]}`
- Array: `{"events":[{"id":"event-alpha","type":[],"receivedAt":"2026-08-07T12:00:00.000Z"}],"jobs":[],"approvals":[],"outbox":[]}`

Each of the four exits `1`, writes zero stdout bytes, writes exactly
`event-metadata: invalid Relay state\n` to stderr, and leaves the fixture
file and its parent directory listing byte-for-byte unchanged (same pattern
as the existing `S1`-`S19` structural-invalid assertions).

### Coverage map (acceptance criterion → contract element)

1. AC1 → numeric two-event fixture + its two expected stdout lines above.
2. AC2 → existing `FIXTURE_A`/`ALPHA_STDOUT`/`ZULU_STDOUT` and not-found
   cases in `app/tests/event-metadata.test.js`, unchanged.
3. AC3 → reclassified `type:7` fixture (now valid) plus the zero/boolean/
   object/array fixtures and the existing missing-type (`S13`), empty-type
   (`S14`), invalid-ID, invalid-timestamp, and duplicate-ID (`S16`-`S19`)
   fixtures (all unchanged).
4. AC4 → existing `SENSITIVE_TOKENS` leak assertions and the five byte-exact
   stderr envelopes above, unchanged.
5. AC5 → existing `snapshotFile`/`snapshotDirectory`/`entryNames`
   before/after equality assertions, unchanged.
6. AC6 → `node --test app/tests/event-metadata.test.js`, `npm test --prefix
   app`, `.github/scripts/test-immutability-check.sh`, per the 2026-08-10
   T-178 ruling that repository-baseline CI controls stay outside a ticket's
   frozen acceptance-check set.

## Ambiguity log

- **Meaning of "non-zero JSON-number types."** Resolved from the T-190
  precedent (`factory/tickets/T-190.md`, which establishes stored numeric
  event type `7` as legitimate opaque event data) and the identical wording
  already applied to `event-count`/`event-type-count` in `factory/tickets/
  T-264.md`/`T-267.md` and `T-265.md`/`T-268.md`. Frozen as: finite JSON
  number, `!== 0` (covers `-0` via strict-equality). No operator input
  needed; cited above instead of re-escalated per the rulings-check rule.
- **Cross-ticket file-boundary conflict with T-266.** `factory/tickets/
  T-266.md` on branch `ticket/T-266` (state `Planning` on that branch, per
  `git show origin/ticket/T-266:factory/tickets/T-266.md`) declares the
  identical `Builder ownership: app/tools/event-metadata.js only` and
  `Test-author ownership: app/tests/event-metadata.test.js only` as this
  ticket, with byte-identical description and acceptance criteria. This is
  the pre-existing ticket T-269 was copied from (commit `87cab8f`, "Prepare
  q96 Relay qualification cohort"); T-267/T-268 are the equivalent copies of
  T-264/T-265 and are already named in Coordination. No product-behavior
  ambiguity results — both frozen contracts are content-identical — but
  whichever of T-266/T-269 merges second will need a routine rebase against
  `main`, same as any two branches editing the same files. Logged for the
  dispatcher/reconciler; no operator decision required for this contract's
  content.
- **Test-suite coverage gap for boolean/object/array `type` values.** Acceptance
  criterion 3 (and the shipped tool's pre-existing behavior) requires these
  to stay invalid, but `app/tests/event-metadata.test.js` had no fixture
  exercising them before this contract. Resolved by freezing the four new
  invalid fixtures above so Test-author has exact bytes to add.
- **Deploy/auth/cross-origin topology.** Not applicable — `event-metadata` is
  an offline, zero-dependency, read-only CLI tool with no network, cookie,
  or cross-origin surface.
- **Protected-test source-boundary conflicts.** Checked: no test outside
  `app/tests/event-metadata.test.js` references `event-metadata` (`grep -rl
  event-metadata app/tests/` returns only that file); `app/tests/
  factory-contract.test.js` does not inspect `app/tools/event-metadata.js`.
  No conflict; `Protected-Test-Conflicts: none` stands.
- **Global protected literal text.** Not applicable — every stdout/stderr
  string in this contract is byte-identical to what the shipped tool already
  emits; no new global-shell-visible literal is introduced.

## Coordination

This ticket owns only `app/tools/event-metadata.js` and
`app/tests/event-metadata.test.js`. It is independent of T-267 and T-268, and
shares an identical file-ownership footprint with the concurrently-planned
duplicate ticket T-266 (no product-behavior conflict; see Ambiguity log). It
supersedes only the older commands' string-only structural validation rule;
historical frozen ticket contracts remain unchanged.

## Spec-linter log

| Contract element | Positive | Failure/empty | Duplicate | Out-of-scope disposition |
| --- | --- | --- | --- | --- |
| Invocation, arity, and exit/envelope precedence | Existing arity and success cases | Existing read, parse, invalid-state, and not-found cases | N/A | No server or external effect |
| Exact selected-event projection and string compatibility | `FIXTURE_A` / `ALPHA_STDOUT` / `ZULU_STDOUT` | Exact lookup miss cases | N/A | Payload and related collections remain redacted |
| Non-zero finite numeric `type` acceptance and serialization | Two-event `7` / `-0.5` fixture and reclassified `type:7` fixture | Uncovered: JSON `1e400` parses as non-finite `Infinity` | N/A | `-0` is an equivalent zero permutation represented by zero |
| Invalid event shape before lookup | Existing missing/empty ID/type/timestamp cases and new zero/boolean/object/array fixtures | Exact invalid-state envelope | Duplicate IDs (`S19`) | Other collection shapes unchanged |
| Output privacy and read-only/offline behavior | Existing token, snapshot, import, fs-operation, and timeout assertions | Existing failure envelopes and snapshots | N/A | No network, auth, database, or external-send surface |
| Required verification and ownership limits | Frozen commands and owned paths | N/A | N/A | Factory baseline controls remain outside the frozen acceptance-check set per ruling |

1. The finite-number contract is both inconsistent and uncovered. Its note says
   `JSON.parse` never produces `Infinity`, but `JSON.parse("1e400")` produces
   `Infinity` for valid JSON; the frozen predicate rejects it with
   `Number.isFinite`, while AC1--AC6 and every frozen fixture omit that case.
   A Builder can omit the finite guard, accept this persisted value, and still
   pass every specified criterion (with JSON serialization changing it to
   `null`). Add a criterion and exact invalid fixture for this material
   predicate branch, or revise the frozen predicate and its incorrect premise.

SPEC-LINT: FAIL — the reachable non-finite JSON-number branch is untested and contradicts the frozen contract's premise.
