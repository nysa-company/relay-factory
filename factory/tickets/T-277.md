# T-277 — reject duplicate job event IDs in progress reports

Kit-SHA: bcffd8f0d3d0596752136d7ee2fd95e4f912806c
State: Review
Initiative: I-002
Priority: normal
Risk class: low
External: no
Branch: ticket/T-277
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/job-progress.test.js
Authentication-Seams: none
Builder ownership: app/tools/job-progress.js only
Test-author ownership: app/tests/job-progress.test.js only

## Description

Relay creates one job per accepted event: `acceptEvent` in `app/server.js:36-52`
dedupes by event ID before ever pushing a job (`app/server.js:38-39`) and, for
a genuinely new event, pushes exactly one job whose `eventId` equals the
event's `id` (`app/server.js:41-49`). No product doc other than this
invariant governs the change; there is no `docs/` entry for the offline
report tools, so `app/server.js:36-52` is the cited source of truth.

The offline `job-progress` report (`app/tools/job-progress.js`) does not
enforce this invariant on its input: `isValidState`
(`app/tools/job-progress.js:34-52`) already rejects duplicate `job.id`
values (`app/tools/job-progress.js:51`) but has no equivalent check for
`job.eventId`, so a state file with two otherwise-valid jobs sharing one
`eventId` is currently accepted. Reject duplicate non-empty job `eventId`
values before lookup or reporting, mirroring the existing job-ID uniqueness
check.

## Ambiguity log

1. **Where to add the check.** Not stated by the ticket text. Resolved by
   extending `isValidState` in `app/tools/job-progress.js` with a second
   `Set`-based uniqueness check over `job.eventId`, alongside the existing
   `job.id` uniqueness check, so both run inside the same function `main()`
   already calls before job lookup (`app/tools/job-progress.js:87-92`). See
   Frozen contract below. No operator escalation needed.
2. **Empty-`eventId` duplicates.** AC1 says "the same non-empty `eventId`."
   `isValidJob` (`app/tools/job-progress.js:19-32`) already requires every
   job's `eventId` to be a non-empty string, so by the time uniqueness is
   checked no `eventId` can be empty — no separate empty-string case is
   needed.
3. **Repository-baseline checks in the merge gate.** `AGENTS.md`'s managed
   baseline section lists `scripts/repo-check` and `scripts/secret-scan` as
   pre-completion checks. Checked `factory/rulings.md` first: the
   2026-08-10 T-178 ruling holds that for this family of offline job-report
   CLI tools, pre-merge acceptance is exactly the targeted test, the full app
   suite, and the immutability check, with repository-baseline checks
   remaining protected-CI controls kept out of the ticket's frozen contract.
   Applied that precedent by citation (criterion 6 already reflects it); no
   new ruling was made, so no new `factory/rulings.md` entry was added.
4. **Cross-ticket file-boundary conflicts.** Checked sibling tickets
   `factory/tickets/T-276.md` (owns `app/tools/job-attempts.js` /
   `app/tests/job-attempts.test.js`) and `factory/tickets/T-278.md` (owns
   `app/tools/job-status-count.js` / `app/tests/job-status-count.test.js`).
   Both declare file ownership disjoint from this ticket's
   `app/tools/job-progress.js` / `app/tests/job-progress.test.js`. No
   concurrently in-flight unmerged ticket branches exist beyond these two
   Backlog-state siblings, so no further branch inspection applies. No
   conflict found.
5. **Protected-test source boundaries.** `app/tests/job-progress.test.js`
   AC7 pins `app/tools/job-progress.js`'s `require(...)` specifiers to
   exactly `["node:fs"]` and forbids several substrings (`server.js`,
   `node:http`, `node:https`, `node:net`, `node:child_process`, `fetch`).
   The planned fix needs no new import, so it is compatible without a
   `Protected-Test-Conflicts` declaration; `Protected-Test-Conflicts: none`
   stands.
6. **Exact duplicate-`eventId` fixture bytes.** Unspecified by the ticket
   text. Resolved by freezing fixtures DUP-1..DUP-4 in the Frozen contract
   below — this is Planner's own responsibility, not an operator escalation.

## Acceptance criteria

1. A state containing two otherwise valid jobs with the same non-empty
   `eventId` exits `1`, writes exactly the existing invalid-state stderr line,
   and writes zero stdout bytes.
2. Duplicate event IDs are invalid whether the requested job is first,
   second, or absent and whether the jobs have different statuses. Complete
   job validation and uniqueness checking precede lookup.
3. Valid jobs with unique event IDs retain the existing output shape, field
   order, exact lookup, exit statuses, and stderr messages byte-for-byte.
4. Existing collection, job-field, status, counter, last-error, and unique job
   ID validation remain unchanged. Event records are not made unique here.
5. Failures remain redacted and leak no job or event ID, error text, state
   bytes, input path, parser detail, or operating-system detail.
6. The command remains offline, zero-dependency, deterministic, read-only, and
   self-terminating. Tests precede implementation; the targeted test, full app
   suite, and immutability check pass without other product or Factory changes.

## Frozen contract — version 1

### Scope

- Builder ownership: `app/tools/job-progress.js` only.
- Test-author ownership: `app/tests/job-progress.test.js` only.

### Interface freeze — `app/tools/job-progress.js`

- CLI invocation, exit codes (`0`/`1`/`2`), and all five stderr constants
  keep their exact current byte values, unchanged:
  - `USAGE = "usage: node app/tools/job-progress.js <state-file> <job-id>\n"`
  - `UNREADABLE = "job-progress: state file is unreadable\n"`
  - `NOT_JSON = "job-progress: state file is not valid JSON\n"`
  - `INVALID_STATE = "job-progress: state file has invalid Relay state\n"`
  - `NO_SUCH_JOB = "job-progress: no such job\n"`
- Success stdout shape is unchanged: exactly one line,
  `` `${JSON.stringify({jobId: job.id, status: job.status, attempts: job.attempts, retries: job.retries})}\n` ``,
  with that exact key order.
- `isValidJob`, `isNonEmptyString`, `isNonNegativeInteger`, `COLLECTIONS`,
  `STATUSES`, the collection/shape checks, and the existing job-`id`
  uniqueness check (`new Set(state.jobs.map(job => job.id)).size ===
  state.jobs.length`) are unchanged.
- New behavior: `isValidState` additionally requires event-ID uniqueness
  across `state.jobs`:
  `new Set(state.jobs.map(job => job.eventId)).size === state.jobs.length`.
  This is combined with (`&&`) the existing job-`id` uniqueness check (and
  all prior structural/per-job checks) to determine `isValidState`'s return
  value; either uniqueness check failing takes the identical existing
  `INVALID_STATE` failure path already wired in `main()` (exit `1`, stderr
  `INVALID_STATE`, stdout empty, job lookup never reached). Because
  `isValidJob` already requires every job's `eventId` to be a non-empty
  string, no separate empty-string case is needed.
- No new `require(...)`/import may be added. The frozen module-specifier
  set for `app/tools/job-progress.js`, as asserted by
  `app/tests/job-progress.test.js` AC7, remains exactly `["node:fs"]`, and
  the file must keep excluding `server.js`, `node:http`, `node:https`,
  `node:net`, `node:child_process`, and `fetch`.

### Frozen fixtures — `app/tests/job-progress.test.js`

Four new invalid-state fixtures (mutually byte-distinct and byte-distinct
from every existing fixture in the file), each containing only
per-job-valid jobs whose sole defect is a shared `eventId`. For every
fixture and every listed lookup `job-id` argument: exit code is `1`,
stderr is exactly `INVALID_STATE`, stdout is exactly `""`, the fixture
file's bytes/stat and the parent directory's entries are unchanged before
vs. after (same pattern as the existing AC6 test), and neither stdout nor
stderr contains any literal `job.id` or `eventId` value used in the
fixture (extends AC5's redaction check to these fixtures).

- `DUP-1` (requested job is first in the array; jobs have different
  statuses):
  ```
  {"events":[],"jobs":[{"id":"job-dup-first","eventId":"event-dup-shared","status":"pending","attempts":0,"lastError":null,"retries":0,"attemptsSinceRetry":0},{"id":"job-dup-second","eventId":"event-dup-shared","status":"done","attempts":2,"lastError":null,"retries":1,"attemptsSinceRetry":1}],"approvals":[],"outbox":[]}
  ```
  Lookup arg: `job-dup-first`.
- `DUP-2` (requested job is second in the array; jobs have different
  statuses):
  ```
  {"events":[],"jobs":[{"id":"job-dup-first","eventId":"event-dup-shared","status":"pending","attempts":0,"lastError":null,"retries":0,"attemptsSinceRetry":0},{"id":"job-dup-second","eventId":"event-dup-shared","status":"done","attempts":3,"lastError":null,"retries":1,"attemptsSinceRetry":1}],"approvals":[],"outbox":[]}
  ```
  Lookup arg: `job-dup-second`.
- `DUP-3` (requested job is absent from the array; jobs have different
  statuses):
  ```
  {"events":[],"jobs":[{"id":"job-dup-first","eventId":"event-dup-shared","status":"pending","attempts":0,"lastError":null,"retries":0,"attemptsSinceRetry":0},{"id":"job-dup-second","eventId":"event-dup-shared","status":"done","attempts":4,"lastError":null,"retries":1,"attemptsSinceRetry":1}],"approvals":[],"outbox":[]}
  ```
  Lookup arg: `job-dup-missing` (not the `id` of any job in the fixture).
- `DUP-4` (requested job is first; jobs have the *same* status, proving
  status equality does not exempt the duplicate):
  ```
  {"events":[],"jobs":[{"id":"job-dup-same-a","eventId":"event-dup-same","status":"dead","attempts":5,"lastError":null,"retries":1,"attemptsSinceRetry":2},{"id":"job-dup-same-b","eventId":"event-dup-same","status":"dead","attempts":5,"lastError":null,"retries":1,"attemptsSinceRetry":2}],"approvals":[],"outbox":[]}
  ```
  Lookup arg: `job-dup-same-a`.

Each fixture file's exact bytes are the JSON text shown above followed by
a single trailing `"\n"` (matching this file's existing structural-fixture
convention). All existing tests and fixtures in
`app/tests/job-progress.test.js` (AC1-AC7, `FIXTURE_A`, `MALFORMED_FIXTURE`,
`S1`-`S22`) are preserved byte-for-byte in behavior and must continue to
pass unmodified.

### Verification

Exactly: the targeted test (`node --test app/tests/job-progress.test.js`),
the full app suite (`npm test --prefix app`), and
`.github/scripts/test-immutability-check.sh` — no other product or Factory
changes. Per the 2026-08-10 T-178 ruling in `factory/rulings.md`, applied
here by precedent (see Ambiguity log item 3), repository-baseline checks
(`scripts/repo-check`, `scripts/secret-scan`) remain protected-CI controls
and are not part of this ticket's acceptance gate.

- **Freeze result:** PASS. Contract version 1 is frozen.

## Coordination

This ticket owns only `app/tools/job-progress.js` and
`app/tests/job-progress.test.js`. It is independent of T-276 and T-278.

## Spec-lint — 2026-08-26

### Contract-element coverage

| Contract element | Positive | Failure / empty | Duplicate | Out of scope |
| --- | --- | --- | --- | --- |
| Unique `job.eventId` across valid jobs | AC3 preserves unique-event output | AC1 requires `INVALID_STATE`, exit 1, and empty stdout | AC1-AC2 and DUP-1..DUP-4 cover first, second, absent, differing-status, and same-status lookups | Empty `eventId` is already invalid per-job (AC4) |
| Whole-state validation before lookup | Existing valid lookup remains successful (AC3) | DUP-1..DUP-4 require invalid-state failure before any lookup result | DUP-1..DUP-3 exercise requested first, second, and absent IDs | None |
| Existing validator and CLI interface remain unchanged | AC3 freezes success line, key order, and exact lookup | AC3-AC5 freeze existing error paths; AC4 preserves structural validation | Existing duplicate `job.id` validation remains covered by AC4/S17 | Event-record uniqueness is explicitly excluded |
| Failure privacy and read-only behavior | AC3 preserves successful projection without unrelated fields | AC1, AC5, and fixture rules require empty stdout, exact redacted stderr, and unchanged file/directory state | DUP-1..DUP-4 forbid leaking every fixture job or event ID | No external effects; command is offline and read-only |
| Offline source boundary and execution properties | AC6 freezes zero dependencies, determinism, and termination | AC6 forbids new imports and the named network/process literals | N/A | No global-shell text is introduced; pinned readiness reports `GLOBAL TEXT PASS` for both frozen CLI error literals |
| Required verification | Targeted test, full app suite, and immutability check are named exactly | A failed required command fails the gate | N/A | Repository-baseline checks remain protected-CI controls under the cited T-178 ruling |

1. No blocking findings. The description, criteria, frozen contract, fixtures, current source, AC7 static boundary, and the 2026-08-10 T-178 ruling agree. Pinned `ticket-readiness.py` reports `READINESS PASS`; its bounded global-text scan reports `GLOBAL TEXT PASS` for the frozen invalid-state and usage literals.

SPEC-LINT: PASS

- 2026-08-26: Test-author wrote failing acceptance tests in
  `app/tests/job-progress.test.js` (test-only commit `86ecc62`, before any
  Builder commit) against frozen contract version 1.

  | Test | Criterion |
  |---|---|
  | `T-277 AC1: every frozen duplicate non-empty job eventId fixture exits 1 ...` | AC1 |
  | `T-277 AC2: duplicate event IDs are rejected before first, second, or absent exact lookup ...` | AC2 |

  The frozen DUP-1 through DUP-4 literals cover requested first, second,
  absent, different-status, and same-status cases. Each assertion requires
  exit `1`, byte-exact `INVALID_STATE`, empty stdout, no fixture ID leakage,
  and unchanged fixture and parent-directory state. Existing AC1-AC7 tests
  remain unchanged and cover the frozen unchanged-interface, validation, and
  offline-boundary requirements (AC3-AC5 and the testable portions of AC6).

  Scoped verification only: `node --test app/tests/job-progress.test.js`
  produced 7 passing existing tests and 2 failing new tests. Both fail for the
  intended missing behavior: DUP-1 returns the current successful progress
  line rather than the required invalid-state failure; no setup error occurs.
  Full app-suite and immutability verification are deferred to protected CI
  and final certification.

  Demo-check item for the Narrator: AC6's test-first commit ordering,
  Builder-only implementation diff, and post-build full-suite/immutability
  commands cannot be expressed as this focused CLI test.

- 2026-08-26: Builder implemented contract version 1 in
  `app/tools/job-progress.js` only (commit `f17601e`), extending
  `isValidState` with `new Set(state.jobs.map(job =>
  job.eventId)).size === state.jobs.length` combined via `&&` with the
  existing job-`id` uniqueness check, exactly as frozen. No other file
  changed; no new `require`/import added.

  Scoped verification: `node --test app/tests/job-progress.test.js`
  passes 9/9 (7 pre-existing plus the 2 new T-277 tests).
  `.github/scripts/test-immutability-check.sh` passes. `npm test --prefix
  app` was also run for visibility (not required by the T-178-cited gate):
  it reports pre-existing, non-deterministic failures unrelated to this
  ticket, verified by reverting only `app/tools/job-progress.js` (via a
  tagged, later-dropped `git stash`) and re-running the same full suite —
  the T-277 tests fail as expected on that baseline and a similar-sized
  set of unrelated failures (different test names each run) persists with
  or without this change. Full app-suite and repository-baseline
  verification remain protected-CI and final-certification concerns per
  the ticket's frozen Verification section and the cited T-178 ruling.
