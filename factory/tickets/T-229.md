# T-229 — count Relay approvals with one recipient and body offline

Kit-SHA: 7ae447baf7287e32a9df60c9453da6edec454e7b
State: Planning
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-229
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/approval-detail.test.js
Authentication-Seams: none
Builder ownership: app/tools/approval-recipient-body-count.js only
Test-author ownership: app/tests/approval-recipient-body-count.test.js only

## Spec'd description

Add `app/tools/approval-recipient-body-count.js`, a zero-dependency CommonJS
command that reads one persisted Relay state file while Relay is offline,
validates the complete approval collection, and counts approvals for which the
stored `action.to` and `action.body` values both exactly equal two supplied
command-line strings. It emits only
`{"approvalsWithRecipientAndBody":<count>}` so an operator can inspect the
conjunction without starting Relay, changing state, approving or rejecting an
action, creating an outbox receipt, performing a send, or exposing supplied or
stored message data.

Product-doc and repository sources:

- [`SPEC.md` behavior 3](../../SPEC.md#behavior-spec) defines a proposed send
  action with `to`, `subject`, and `body`; behaviors 4 and 5 define the stored
  `sent`, `blocked_recipient`, and `rejected` outcomes.
- [`SPEC.md` behaviors 6 and 7](../../SPEC.md#behavior-spec) make the atomically
  persisted state file Relay's durable source and approvals operator-visible;
  [conformance cases 5-7](../../SPEC.md#test-cases-the-conformance-suite) cover
  the approval gate, allowlist result, and duplicate-free recovery.
- [`SPEC.md`'s modeling limitation](../../SPEC.md#known-modeling-limitation)
  states that Relay creates sandbox outbox records and performs no external
  delivery.
- The current [`app/server.js`](../../app/server.js) establishes the four
  stored collections, approval core shape, action fields, exact statuses,
  optional rejection reason, and atomic persistence.
- The shipped
  [`approval-recipient-count` command](../../app/tools/approval-recipient-count.js)
  and its
  [acceptance suite](../../app/tests/approval-recipient-count.test.js)
  establish complete approval validation, strict command-argument matching,
  count-only output, redacted errors, and the read-only offline convention.
  The shipped
  [`approval-reason-count` command](../../app/tools/approval-reason-count.js)
  and its
  [acceptance suite](../../app/tests/approval-reason-count.test.js) establish
  the optional `reason` domain.
- The protected
  [approval-detail suite](../../app/tests/approval-detail.test.js) establishes
  the persisted approval shape used by the declared fixture seam.
- [`AGENTS.md` Boundaries](../../AGENTS.md#boundaries) keeps `app/`
  zero-dependency, and [`AGENTS.md` Verification](../../AGENTS.md#verification)
  defines later full-suite and immutability checks.

No endpoint, HTTP shape, UI selector, global-shell UI text, persisted schema,
server behavior, manifest, dependency, lockfile, Factory control,
authentication path, deployment topology, or external-send behavior changes.

## Acceptance criteria

1. Given Frozen Fixture A and arguments
   `TARGET-RECIPIENT-MUST-NOT-LEAK@example.test` and
   `TARGET-BODY-MUST-NOT-LEAK`, each of two invocations of
   `node app/tools/approval-recipient-body-count.js <fixture-a-absolute-path>
   TARGET-RECIPIENT-MUST-NOT-LEAK@example.test TARGET-BODY-MUST-NOT-LEAK`
   exits `0`, writes exactly
   `{"approvalsWithRecipientAndBody":2}\n` to stdout, and writes zero stderr
   bytes. Neither stream contains a frozen sensitive token. After each run,
   Fixture A has identical bytes and BigInt numeric `mode`, `size`, and
   `mtimeNs`, and its parent has identical sorted entry names.
2. Against Frozen Fixture A, the four ordered argument pairs
   `(OTHER-RECIPIENT-MUST-NOT-LEAK@example.test,
   TARGET-BODY-MUST-NOT-LEAK)`,
   `(TARGET-RECIPIENT-MUST-NOT-LEAK@example.test,
   OTHER-BODY-MUST-NOT-LEAK)`, the pair whose two values each begin with one
   U+0020, and `(café@example.test, café body)` each exit `0`, write exactly
   `{"approvalsWithRecipientAndBody":1}\n`, write zero stderr bytes, emit no
   frozen sensitive token, and leave the fixture and parent unchanged by every
   AC1 observation. Before invocation, the test asserts each pair is strictly
   equal to exactly one stored approval's two fields. The first two cases
   prove that recipient-only and body-only matches do not contribute.
3. All six frozen zero-match queries succeed: the case-distinct recipient with
   target body; target recipient with the case-distinct body; string `7` with
   string `8`; two exact zero-byte strings; decomposed `café@example.test`
   with decomposed `café body`; and the target pair against Frozen Fixture B.
   Each exits `0`, writes exactly
   `{"approvalsWithRecipientAndBody":0}\n`, writes zero stderr bytes, emits
   no frozen sensitive token, and leaves its fixture and parent unchanged by
   every AC1 observation. Before invocation, the test asserts the numeric
   stored values are numbers `7` and `8`, no stored pair contains two empty
   strings, each case-distinct value differs bytewise from every corresponding
   stored string, and the composed/decomposed recipient and body hex sequences
   equal the values frozen below.
4. In separate empty temporary parents, invocations with zero, one, two, and
   four positional arguments each exit `2`, write zero stdout bytes, and write
   exactly `usage: node app/tools/approval-recipient-body-count.js
   <state-file> <recipient> <body>\n` to stderr. The one-, two-, and
   four-argument cases use absent path
   `<parent>/arity-state-MUST-NOT-LEAK.json`; the four-argument case adds
   target recipient, target body, and literal `extra`. The path stays absent
   and every parent retains empty sorted entries.
5. With the target argument pair, an absent
   `<parent>/PATH-MUST-NOT-LEAK.json` and an existing empty
   `<parent>/DIRECTORY-MUST-NOT-LEAK` each exit `1`, write zero stdout bytes,
   and write exactly
   `approval-recipient-body-count: cannot read state file\n` to stderr. The
   absent path remains absent; the directory remains a directory with
   identical BigInt numeric `mode`, `size`, `mtimeNs`, and sorted entries; both
   parents retain their sorted entries.
6. With the target pair, Frozen Fixtures C and D each exit `1`, write zero
   stdout bytes, and write exactly
   `approval-recipient-body-count: state file is not valid JSON\n` to stderr.
   Neither stream contains `MALFORMED-PAYLOAD-MUST-NOT-LEAK`; each fixture's
   exact bytes and BigInt numeric `mode`, `size`, and `mtimeNs`, and each
   parent's sorted entries, remain unchanged.
7. With the target pair, every S1-S30 frozen structural-invalid fixture exits
   `1`, writes zero stdout bytes, and writes exactly
   `approval-recipient-body-count: invalid Relay state\n` to stderr. S29 has
   duplicate IDs including one otherwise valid matching pair, and S30 has one
   valid matching approval before an invalid sibling, proving identity and
   complete-array validation precede counting. Before invocation, Fixtures
   A-D and S1-S30 are pairwise byte-distinct, and S26's exact `PENDING` bytes
   differ from valid `pending`. Every fixture and parent remains unchanged by
   the AC1 observations.
8. Every AC1-AC7 child uses a `5_000` ms timeout, returns with
   `error === undefined` and `signal === null`, and emits no frozen sensitive
   token. Static inspection asserts the Builder file's complete CommonJS
   module-specifier set is exactly `node:fs`, its complete `node:fs` operation
   set is exactly `readFileSync`, and it contains none of `server.js`,
   `fetch(`, `WebSocket`, `node:http`, `node:https`, `node:net`,
   `node:child_process`, `setTimeout`, or `setInterval`. The Test-author
   file's complete module-specifier set is exactly `node:assert`,
   `node:child_process`, `node:fs`, `node:os`, `node:path`, and `node:test`.
9. Git history contains the complete Test-author commit before the Builder
   commit. Apart from role-owned ticket-log appends, the Test-author commit
   creates only `app/tests/approval-recipient-body-count.test.js` and the
   Builder commit creates only `app/tools/approval-recipient-body-count.js`.
   Existing app and test files, `app/server.js`, `app/package.json`, lockfiles,
   dependencies, schema, `factory/KIT_PIN`, and Factory controls remain
   unchanged. After implementation,
   `node --test app/tests/approval-recipient-body-count.test.js`,
   `npm test --prefix app`, and
   `.github/scripts/test-immutability-check.sh` each exit `0`.

## Frozen contract — version 1

- **Freeze result:** PASS. Contract version 1 is frozen.

This is the exact interface for Test-author and Builder. If it proves wrong,
return T-229 to Ready and append a higher-numbered frozen contract; version 1
must not be edited or removed.

### Command line and failure precedence

- Repository-root invocation is exactly
  `node app/tools/approval-recipient-body-count.js <state-file> <recipient> <body>`.
- Exactly three user-supplied positional arguments are accepted. Any other
  arity exits `2`, writes zero stdout bytes, writes exactly the following
  single LF-terminated line to stderr, and does not read or create a candidate
  path:

  ```text
  usage: node app/tools/approval-recipient-body-count.js <state-file> <recipient> <body>
  ```

- After arity validation, the command synchronously reads `<state-file>` as
  UTF-8. Any `readFileSync` failure exits `1`, writes zero stdout bytes, and
  writes exactly:

  ```text
  approval-recipient-body-count: cannot read state file
  ```

- Bytes rejected by `JSON.parse` exit `1`, write zero stdout bytes, and write:

  ```text
  approval-recipient-body-count: state file is not valid JSON
  ```

- A parsed value failing the structural rules exits `1`, writes zero stdout
  bytes, and writes:

  ```text
  approval-recipient-body-count: invalid Relay state
  ```

- Precedence is arity, read, parse, complete structural validation, then
  counting. Every error is one fixed line with exactly one LF and includes no
  path, supplied argument, OS error, parser text, input byte, stack trace,
  stored field, or second line.
- Acceptance uses absolute temporary paths. Symlinks and portable simulation
  of permission denial are outside the fixture matrix; if `readFileSync`
  throws, the read-error result applies.

### Valid state, conjunction, and success shape

A valid state is a parsed non-null, non-array object with own `events`, `jobs`,
`approvals`, and `outbox` array properties. Extra top-level properties are
allowed and ignored. Every approval is a non-null, non-array object with:

- non-empty string own `id`, `jobId`, and `proposedAt` properties;
- own `status` exactly `pending`, `sent`, `rejected`, or
  `blocked_recipient`;
- own non-null, non-array object `action` with own `to`, `subject`, and `body`
  properties; and
- absent `reason`, or own `reason` equal to `null` or any string, including
  `""` and `" "`.

Approval IDs are pairwise distinct across the complete array. Action values
are opaque; extra action and approval fields are ignored. Event, job, and
outbox elements are opaque beyond their collection arrays. A `jobId` need not
resolve, timestamps need not parse, and no cross-field relationship is
imposed.

After validating the whole approval array and ID uniqueness, an approval
contributes exactly once only when both
`approval.action.to === <recipient>` and
`approval.action.body === <body>` using case-sensitive JavaScript strict
equality. Neither operand is coerced, trimmed, case-folded, decoded, or
Unicode-normalized. CLI values are strings, so non-string stored values never
match. Empty recipient and body strings are valid arguments. Zero matches is
a successful query.

Success exits `0`, writes zero stderr bytes, and writes exactly one compact
JSON object followed by one LF. Its only key is:

```json
{"approvalsWithRecipientAndBody":<nonnegative integer count>}
```

Neither supplied argument nor any stored action, approval, event, job, outbox,
or metadata value is emitted.

### Frozen Fixture A and exact query matrix

The Test-author writes this exact parsed value to isolated `state.json` using
`JSON.stringify(value, null, 2) + "\n"`:

```json
{
  "events": [{"payload": "EVENT-PAYLOAD-MUST-NOT-LEAK"}],
  "jobs": [{"lastError": "JOB-ERROR-MUST-NOT-LEAK"}],
  "approvals": [
    {
      "id": "APPROVAL-ID-TARGET-ONE-MUST-NOT-LEAK",
      "jobId": "JOB-ID-TARGET-ONE-MUST-NOT-LEAK",
      "action": {"to": "TARGET-RECIPIENT-MUST-NOT-LEAK@example.test", "subject": "SUBJECT-ONE-MUST-NOT-LEAK", "body": "TARGET-BODY-MUST-NOT-LEAK"},
      "status": "pending", "proposedAt": "2026-08-24T12:00:00.000Z"
    },
    {
      "id": "APPROVAL-ID-OTHER-RECIPIENT-MUST-NOT-LEAK",
      "jobId": "JOB-ID-OTHER-RECIPIENT-MUST-NOT-LEAK",
      "action": {"to": "OTHER-RECIPIENT-MUST-NOT-LEAK@example.test", "subject": "SUBJECT-TWO-MUST-NOT-LEAK", "body": "TARGET-BODY-MUST-NOT-LEAK"},
      "status": "sent", "proposedAt": "2026-08-24T12:00:01.000Z", "reason": ""
    },
    {
      "id": "APPROVAL-ID-OTHER-BODY-MUST-NOT-LEAK",
      "jobId": "JOB-ID-OTHER-BODY-MUST-NOT-LEAK",
      "action": {"to": "TARGET-RECIPIENT-MUST-NOT-LEAK@example.test", "subject": null, "body": "OTHER-BODY-MUST-NOT-LEAK"},
      "status": "blocked_recipient", "proposedAt": "not-an-iso-instant", "reason": null,
      "ignoredDiagnostic": "IGNORED-APPROVAL-FIELD-MUST-NOT-LEAK"
    },
    {
      "id": "APPROVAL-ID-TARGET-TWO-MUST-NOT-LEAK",
      "jobId": "JOB-ID-TARGET-TWO-MUST-NOT-LEAK",
      "action": {"to": "TARGET-RECIPIENT-MUST-NOT-LEAK@example.test", "subject": "SUBJECT-FOUR-MUST-NOT-LEAK", "body": "TARGET-BODY-MUST-NOT-LEAK", "ignored": "IGNORED-ACTION-FIELD-MUST-NOT-LEAK"},
      "status": "rejected", "proposedAt": "2026-08-24T12:00:03.000Z", "reason": "REJECTION-REASON-MUST-NOT-LEAK"
    },
    {
      "id": "APPROVAL-ID-LEADING-MUST-NOT-LEAK",
      "jobId": "JOB-ID-LEADING-MUST-NOT-LEAK",
      "action": {"to": " TARGET-RECIPIENT-MUST-NOT-LEAK@example.test", "subject": "SUBJECT-FIVE-MUST-NOT-LEAK", "body": " TARGET-BODY-MUST-NOT-LEAK"},
      "status": "sent", "proposedAt": "2026-08-24T12:00:04.000Z"
    },
    {
      "id": "APPROVAL-ID-UNICODE-MUST-NOT-LEAK",
      "jobId": "JOB-ID-UNICODE-MUST-NOT-LEAK",
      "action": {"to": "café@example.test", "subject": "SUBJECT-SIX-MUST-NOT-LEAK", "body": "café body"},
      "status": "pending", "proposedAt": "2026-08-24T12:00:05.000Z", "reason": " "
    },
    {
      "id": "APPROVAL-ID-NUMERIC-MUST-NOT-LEAK",
      "jobId": "JOB-ID-NUMERIC-MUST-NOT-LEAK",
      "action": {"to": 7, "subject": false, "body": 8},
      "status": "pending", "proposedAt": "2026-08-24T12:00:06.000Z"
    }
  ],
  "outbox": [{"body": "OUTBOX-CONTENT-MUST-NOT-LEAK"}],
  "metadata": {"note": "TOP-LEVEL-METADATA-MUST-NOT-LEAK"}
}
```

Exact Fixture A results are:

| Recipient argument | Body argument | Exact stdout |
|---|---|---|
| `TARGET-RECIPIENT-MUST-NOT-LEAK@example.test` | `TARGET-BODY-MUST-NOT-LEAK` | `{"approvalsWithRecipientAndBody":2}\n` |
| `OTHER-RECIPIENT-MUST-NOT-LEAK@example.test` | `TARGET-BODY-MUST-NOT-LEAK` | `{"approvalsWithRecipientAndBody":1}\n` |
| `TARGET-RECIPIENT-MUST-NOT-LEAK@example.test` | `OTHER-BODY-MUST-NOT-LEAK` | `{"approvalsWithRecipientAndBody":1}\n` |
| ` TARGET-RECIPIENT-MUST-NOT-LEAK@example.test` | ` TARGET-BODY-MUST-NOT-LEAK` | `{"approvalsWithRecipientAndBody":1}\n` |
| `café@example.test` | `café body` | `{"approvalsWithRecipientAndBody":1}\n` |
| `Target-RECIPIENT-MUST-NOT-LEAK@example.test` | `TARGET-BODY-MUST-NOT-LEAK` | `{"approvalsWithRecipientAndBody":0}\n` |
| `TARGET-RECIPIENT-MUST-NOT-LEAK@example.test` | `Target-BODY-MUST-NOT-LEAK` | `{"approvalsWithRecipientAndBody":0}\n` |
| string `7` | string `8` | `{"approvalsWithRecipientAndBody":0}\n` |
| zero-byte string `""` | zero-byte string `""` | `{"approvalsWithRecipientAndBody":0}\n` |
| decomposed `café@example.test` | decomposed `café body` | `{"approvalsWithRecipientAndBody":0}\n` |

The composed/decomposed recipient UTF-8 hex is respectively
`636166c3a9406578616d706c652e74657374` and
`63616665cc81406578616d706c652e74657374`; the body hex is respectively
`636166c3a920626f6479` and `63616665cc8120626f6479`. All query variants are
direct literals, not runtime transformations.

### Frozen Fixtures B-D and paths

- Fixture B is exactly
  `{"events":[null],"jobs":[7],"approvals":[],"outbox":["OPAQUE-OUTBOX-MUST-NOT-LEAK"],"metadata":false}\n`.
  It is valid and returns the exact zero envelope for the target pair.
- Fixture C is exactly
  `{"approvals":[],"payload":"MALFORMED-PAYLOAD-MUST-NOT-LEAK"\n` and is
  rejected by `JSON.parse`; Fixture D is a zero-byte regular file and is also
  rejected by `JSON.parse`.
- Frozen absent paths are
  `<parent>/arity-state-MUST-NOT-LEAK.json` and
  `<parent>/PATH-MUST-NOT-LEAK.json`; the directory path is
  `<parent>/DIRECTORY-MUST-NOT-LEAK`.

### Frozen structural-invalid fixtures

Each row is the exact compact UTF-8 JSON before one final LF and is a direct
literal. No row is generated by transforming another fixture.

| ID | Exact JSON before LF | Violation |
|---|---|---|
| S1 | `null` | null root |
| S2 | `[]` | array root |
| S3 | `{"events":[],"jobs":[],"approvals":[]}` | missing own `outbox` |
| S4 | `{"events":{},"jobs":[],"approvals":[],"outbox":[]}` | non-array `events` |
| S5 | `{"events":[],"jobs":{},"approvals":[],"outbox":[]}` | non-array `jobs` |
| S6 | `{"events":[],"jobs":[],"approvals":{},"outbox":[]}` | non-array `approvals` |
| S7 | `{"events":[],"jobs":[],"approvals":[],"outbox":{}}` | non-array `outbox` |
| S8 | `{"events":[],"jobs":[],"approvals":[null],"outbox":[]}` | null approval |
| S9 | `{"events":[],"jobs":[],"approvals":[[]],"outbox":[]}` | array approval |
| S10 | `{"events":[],"jobs":[],"approvals":[{"jobId":"j","action":{"to":"a","subject":"b","body":"c"},"status":"pending","proposedAt":"t"}],"outbox":[]}` | missing `id` |
| S11 | `{"events":[],"jobs":[],"approvals":[{"id":"","jobId":"j","action":{"to":"a","subject":"b","body":"c"},"status":"pending","proposedAt":"t"}],"outbox":[]}` | empty `id` |
| S12 | `{"events":[],"jobs":[],"approvals":[{"id":7,"jobId":"j","action":{"to":"a","subject":"b","body":"c"},"status":"pending","proposedAt":"t"}],"outbox":[]}` | non-string `id` |
| S13 | `{"events":[],"jobs":[],"approvals":[{"id":"a","action":{"to":"a","subject":"b","body":"c"},"status":"pending","proposedAt":"t"}],"outbox":[]}` | missing `jobId` |
| S14 | `{"events":[],"jobs":[],"approvals":[{"id":"a","jobId":"","action":{"to":"a","subject":"b","body":"c"},"status":"pending","proposedAt":"t"}],"outbox":[]}` | empty `jobId` |
| S15 | `{"events":[],"jobs":[],"approvals":[{"id":"a","jobId":7,"action":{"to":"a","subject":"b","body":"c"},"status":"pending","proposedAt":"t"}],"outbox":[]}` | non-string `jobId` |
| S16 | `{"events":[],"jobs":[],"approvals":[{"id":"a","jobId":"j","status":"pending","proposedAt":"t"}],"outbox":[]}` | missing `action` |
| S17 | `{"events":[],"jobs":[],"approvals":[{"id":"a","jobId":"j","action":null,"status":"pending","proposedAt":"t"}],"outbox":[]}` | null `action` |
| S18 | `{"events":[],"jobs":[],"approvals":[{"id":"a","jobId":"j","action":[],"status":"pending","proposedAt":"t"}],"outbox":[]}` | array `action` |
| S19 | `{"events":[],"jobs":[],"approvals":[{"id":"a","jobId":"j","action":{"subject":"b","body":"c"},"status":"pending","proposedAt":"t"}],"outbox":[]}` | missing `to` |
| S20 | `{"events":[],"jobs":[],"approvals":[{"id":"a","jobId":"j","action":{"to":"a","body":"c"},"status":"pending","proposedAt":"t"}],"outbox":[]}` | missing `subject` |
| S21 | `{"events":[],"jobs":[],"approvals":[{"id":"a","jobId":"j","action":{"to":"a","subject":"b"},"status":"pending","proposedAt":"t"}],"outbox":[]}` | missing `body` |
| S22 | `{"events":[],"jobs":[],"approvals":[{"id":"a","jobId":"j","action":{"to":"a","subject":"b","body":"c"},"status":"pending"}],"outbox":[]}` | missing `proposedAt` |
| S23 | `{"events":[],"jobs":[],"approvals":[{"id":"a","jobId":"j","action":{"to":"a","subject":"b","body":"c"},"status":"pending","proposedAt":""}],"outbox":[]}` | empty `proposedAt` |
| S24 | `{"events":[],"jobs":[],"approvals":[{"id":"a","jobId":"j","action":{"to":"a","subject":"b","body":"c"},"status":"pending","proposedAt":7}],"outbox":[]}` | non-string `proposedAt` |
| S25 | `{"events":[],"jobs":[],"approvals":[{"id":"a","jobId":"j","action":{"to":"a","subject":"b","body":"c"},"proposedAt":"t"}],"outbox":[]}` | missing `status` |
| S26 | `{"events":[],"jobs":[],"approvals":[{"id":"a","jobId":"j","action":{"to":"a","subject":"b","body":"c"},"status":"PENDING","proposedAt":"t"}],"outbox":[]}` | status outside enum |
| S27 | `{"events":[],"jobs":[],"approvals":[{"id":"a","jobId":"j","action":{"to":"a","subject":"b","body":"c"},"status":null,"proposedAt":"t"}],"outbox":[]}` | non-string status |
| S28 | `{"events":[],"jobs":[],"approvals":[{"id":"a","jobId":"j","action":{"to":"a","subject":"b","body":"c"},"status":"rejected","proposedAt":"t","reason":7}],"outbox":[]}` | invalid `reason` |
| S29 | `{"events":[],"jobs":[],"approvals":[{"id":"duplicate","jobId":"j1","action":{"to":"TARGET-RECIPIENT-MUST-NOT-LEAK@example.test","subject":"b","body":"TARGET-BODY-MUST-NOT-LEAK"},"status":"pending","proposedAt":"t1"},{"id":"duplicate","jobId":"j2","action":{"to":"d","subject":"e","body":"f"},"status":"sent","proposedAt":"t2"}],"outbox":[]}` | duplicate IDs after a match |
| S30 | `{"events":[],"jobs":[],"approvals":[{"id":"valid","jobId":"j1","action":{"to":"TARGET-RECIPIENT-MUST-NOT-LEAK@example.test","subject":"b","body":"TARGET-BODY-MUST-NOT-LEAK"},"status":"pending","proposedAt":"t1"},{"id":"invalid","jobId":"j2","action":{"to":"d","subject":"e","body":"f"},"status":"rejected","proposedAt":"t2","reason":false}],"outbox":[]}` | valid match before invalid sibling |

Before invocation the test compares the exact serialized bytes of A-D and
S1-S30 pairwise. A and B pass all structural rules; C and D fail parsing; each
S fixture parses and fails its named rule. S26 is direct literal `PENDING`,
byte-distinct from valid `pending`.

### Sensitive data, lifecycle, boundaries, and topology

The frozen sensitive-token set is every string in Fixtures A-C or frozen path
basename containing `MUST-NOT-LEAK`; both complete `.json` basenames and bare
stems for the two paths whose stems end in `MUST-NOT-LEAK`; the composed and
decomposed Unicode recipient/body values; and all seven Fixture A
`proposedAt` literals. The Test-author derives this set from the fixtures and
also asserts equality with a direct sorted expected list. It checks the set
against both streams after every child.

Each case owns a fresh temporary parent, creates only named inputs, snapshots
sorted entries and existing file bytes plus BigInt numeric `mode`, `size`, and
`mtimeNs`, waits synchronously for children, then recursively removes the
parent in `finally`. Directory cases snapshot the same stat triple and entries.
No case shares a path, process, port, server, database, table, generated ID,
sequence, counter, clock, or timestamp. There is no parent row, sibling table,
foreign key, cascade, reset, or ordered dependent cleanup. Access time is
excluded because reading is the behavior under test.

- Builder ownership is creation of
  `app/tools/approval-recipient-body-count.js` only, with complete module set
  `node:fs` and sole filesystem operation `readFileSync`.
- Test-author ownership is creation of
  `app/tests/approval-recipient-body-count.test.js` only, with the six exact
  module specifiers in AC8.
- `app/tests/approval-detail.test.js` is reference-only: it is not edited and
  grants no edit right. It exercises only `app/server.js`. Existing protected
  tool suites bind only their corresponding tool paths; no protected test
  enumerates the tool/test directories, restricts the new path, or snapshots
  its source. `Protected-Test-Conflicts: none` is exact.
- `app/package.json` already collects the test through
  `node --test tests/*.test.js`. Verification commands are exactly those in
  AC9.
- Topology is one local file-to-stdout process. There is no endpoint, HTTP,
  UI, accessible UI name, auth, cookie, CORS, cross-origin request, credential,
  domain, preview deployment, or external send; `External: no` is exact.

## Criteria checklist

- **Pass/fail and unambiguous:** AC1-AC7 freeze argument vectors, order, exit
  codes, stream bytes, fixture bytes, stat fields, parent entries, and failure
  classes. AC8 freezes timeout and static source sets. AC9 is decided from Git
  history, path diffs, and three exact command statuses.
- **Coverage/no dead elements:** AC1 covers duplicate conjunction matches and
  deterministic repetition; AC2 proves each one-field-only match is excluded
  and covers whitespace and composed Unicode; AC3 covers case, coercion,
  empty strings, normalization, and empty approvals. AC4-AC7 cover precedence,
  read, parse, every structural rule, uniqueness, and complete validation.
  AC8-AC9 cover privacy, termination, source and role boundaries, and every
  verification command. Every contract path, shape, fixture, literal class,
  lifecycle rule, and topology disposition appears in AC1-AC9.
- **Derived fixtures:** every invalid fixture and query variant is a direct
  literal. All 34 serializations are pairwise byte-compared; S26's exact bytes
  and both composed/decomposed byte pairs are asserted before invocation.
- **Generated values:** all IDs, fields, timestamps, statuses, query values,
  and count-producing rows are literal. No initializer, reset, transform,
  sequence, counter, clock, or generated value supplies an expectation.
- **Lifecycle closure:** each case owns and removes one temporary parent after
  synchronous child exit. No database, parent row, sibling dependent table,
  non-cascading foreign key, or cleanup seam exists.
- **Cross-ticket boundaries:** every local ownership declaration and the
  visible leased records for T-228 and T-230 were inspected. They own distinct
  event and outbox command/test pairs; no ticket overlaps either T-229 path.
- **Protected boundaries:** the declared seam and tracked protected tests were
  inspected. No allowlist, exact-source assertion, source snapshot, digest,
  or module restriction reaches the new Builder path or `node:fs`. The pinned
  readiness checker returned `READINESS PASS` after the contract was written.
- **Global protected text:** the pinned checker returned `GLOBAL TEXT PASS`
  separately for the usage line, three error lines, output key, three success
  envelopes, and every non-empty query literal. Empty arguments contribute no
  text bytes; no global page-shell assertion applies.
- **Deploy/topology:** the exact local file-to-stdout topology has no endpoint,
  UI, auth, cookie, CORS, credential, domain, preview, or send surface.

## Ambiguity log

1. **Does matching mean AND or two independent counts?** Resolved from the
   ticket phrase "recipient and body exactly match": one approval contributes
   only when both strict equalities hold. Fixture A deliberately contains a
   recipient-only and a body-only match; AC1-AC2 distinguish conjunction from
   either independent count.
2. **What does exact mean for two CLI strings?** Neither `SPEC.md` nor
   [`factory/rulings.md`](../rulings.md) defines normalization. Resolved from
   the ticket and shipped recipient counter as case-sensitive strict equality
   without coercion, trimming, case folding, decoding, or Unicode
   normalization; direct whitespace, case, numeric, and Unicode literals make
   each choice observable.
3. **Are empty arguments or zero matches errors?** Resolved from the exact
   three-argument interface and original zero-match requirement: empty strings
   are supplied arguments and a valid state with no matching pair exits `0`
   with the frozen zero envelope.
4. **What is structurally validated?** Resolved from `app/server.js`, the
   approval recipient/reason counters, and approval-detail seam: validate four
   arrays, every approval core/action key, status, proposal timestamp, optional
   reason, and unique ID before counting; sibling elements remain opaque.
5. **Are action fields typed, timestamps parsed, or job links resolved?**
   Existing offline validators treat action values as opaque. Frozen numeric
   action fields, non-ISO timestamp, and unresolved links make the absence of
   extra validation observable.
6. **How are errors redacted and ordered?** Resolved from shipped count-command
   conventions: usage exit `2`, other failures exit `1`, fixed single-line
   messages, empty opposite streams, and arity/read/parse/structure/count
   precedence.
7. **How are offline, read-only, and termination claims proved?** Resolved by
   isolated synchronous children under `5_000` ms, byte/stat/entry snapshots,
   repeated success, and the `node:fs`/`readFileSync` source boundary.
8. **Are fixture transformations, generated expectations, or dependent
   cleanup involved?** No. Exact direct serializations and byte distinctions
   are frozen; all expected values are literal; each temporary parent is
   self-contained and removed after its child exits.
9. **Do sibling leases or protected tests conflict?** Local ticket ownership,
   remote T-228/T-230 records, the declared seam, protected source assertions,
   and all tracked test references were inspected. Ownership is disjoint and
   no protected assertion reaches the new path, so no conflict declaration or
   Fixture-Seams expansion is required.
10. **Does global protected text collide?** The pinned checker accepted every
    non-empty shell-visible literal and accessible output name frozen here.
11. **Is deploy/auth/external topology relevant?** No: the entire topology is
    a local process reading one file and writing two process streams. No
    `external` label is required.
12. **Does an operator ruling change the contract?** `factory/rulings.md` was
    checked. Its detail-route ruling is compatible with the approval shape;
    its T-178 ruling confirms the targeted, full-app, and immutability
    post-implementation set. No new operator decision is required.

## Coordination

T-229 owns only `app/tools/approval-recipient-body-count.js` and
`app/tests/approval-recipient-body-count.test.js`. The protected
`app/tests/approval-detail.test.js` seam is reference-only. T-228 and T-230
own disjoint paths.

## Planning log

- 2026-08-24: Planner froze contract version 1 after inspecting `SPEC.md`,
  `AGENTS.md`, `app/server.js`, `app/package.json`, the approval recipient and
  reason counters and tests, the approval-detail seam, protected source
  assertions, `factory/rulings.md`, all local ownership declarations, and all
  visible remote ticket branches. The conjunction, output/error bytes,
  complete validation, direct distinct fixtures, lifecycle, ownership,
  protected-source boundary, global literals, and local-only topology are
  frozen. The pinned deterministic checks returned `READINESS PASS` and
  `GLOBAL TEXT PASS`; no operator decision remains.
