# T-117 — read one Relay sandbox outbox receipt

State: Ready
Initiative: I-002
Priority: normal
Risk class: low
External: no
Branch: ticket/T-117
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/server.js, app/tests/conformance.test.js
Authentication-Seams: none
Builder ownership: app/server.js only
Test-author ownership: app/tests/outbox-detail.test.js only

## Description

Add `GET /api/outbox/:approvalId` to the zero-dependency Relay API.

## Acceptance criteria

1. An existing outbox item returns HTTP 200 as
   `{"sandbox":true,"receipt":<stored outbox item>}`.
2. An unknown approval ID returns HTTP 404 with the deterministic JSON error
   `{"error":"no such outbox receipt"}`.
3. The route is read-only, explicitly identifies the outbox as sandboxed, and
   never performs an external send.
4. Tests are committed before implementation; no dependencies or Factory
   controls change.
