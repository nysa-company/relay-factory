# T-231 — count Relay events with one boolean payload value offline

Kit-SHA: 673d6c9539dff1dc3815e48fdc938387888e856f
State: Planning
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-231
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/event-metadata.test.js
Authentication-Seams: none
Builder ownership: app/tools/event-payload-boolean-count.js only
Test-author ownership: app/tests/event-payload-boolean-count.test.js only

## Spec'd description

Add `app/tools/event-payload-boolean-count.js`, a zero-dependency CommonJS
command that reads one persisted Relay state file while Relay is offline,
validates the complete stored event collection, and counts events having at
least one direct own payload property whose value is the JavaScript boolean
`true` or `false`. It reports only
`{"eventsWithPayloadBoolean":<count>}` so an operator can inspect
boolean-payload cardinality without starting Relay, changing durable state,
enqueueing work, exposing event or sibling-collection content, or treating
nested, inherited, array-contained, or non-boolean values as matches. One
event contributes at most one regardless of how many qualifying properties it
contains.

Product-doc and repository sources:

- [`SPEC.md` behavior 1](../../SPEC.md#behavior-spec) defines accepted events,
  their `{id, type, payload}` intake, and exact-ID idempotency.
- [`SPEC.md` behavior 6](../../SPEC.md#behavior-spec) makes the atomically
  persisted state file Relay's durable source, which this command reads
  without participating in startup or recovery.
- [`SPEC.md` behavior 7](../../SPEC.md#behavior-spec) makes events
  operator-visible, while
  [`SPEC.md` conformance cases 1, 2, and 7](../../SPEC.md#test-cases-the-conformance-suite)
  require accepted events to remain visible, duplicate IDs not to duplicate
  work, and persisted work to survive restart.
- The current [`app/server.js` state model](../../app/server.js) establishes
  the four stored collections, event fields `id`, `type`, `payload`, and
  `receivedAt`, and atomic persistence.
- The shipped [`event-count` command](../../app/tools/event-count.js) and its
  [acceptance suite](../../app/tests/event-count.test.js) establish complete
  event validation, unique event IDs, compact count-only output, fixed
  redacted failures, and the one-path read-only offline convention. The
  protected
  [`event-metadata` suite](../../app/tests/event-metadata.test.js) establishes
  the persisted event core shape used by the declared fixture seam.
- [`AGENTS.md` Boundaries](../../AGENTS.md#boundaries) keeps `app/`
  zero-dependency, and [`AGENTS.md` Verification](../../AGENTS.md#verification)
  defines the later full-suite and immutability checks.

No endpoint, HTTP shape, UI selector, global page-shell text, persisted schema,
server behavior, package, dependency, lockfile, Factory control,
authentication path, deployment topology, or external-send behavior changes.

## Acceptance criteria

1. Given Frozen Fixture A, each of two invocations of
   `node app/tools/event-payload-boolean-count.js <fixture-a-absolute-path>`
   exits `0`, writes exactly `{"eventsWithPayloadBoolean":3}\n` to stdout,
   and writes zero stderr bytes. The result counts each of the first three
   events once, including the event with both direct boolean values, and does
   not count the final four events. Neither stream contains any member of the
   complete frozen sensitive-token set. After each invocation, Fixture A has
   identical bytes and BigInt numeric `mode`, `size`, and `mtimeNs`, and its
   parent has identical sorted entry names.
2. Given Frozen Fixture P and its exact temporary preload, one isolated child
   invocation exits `0`, writes exactly
   `{"eventsWithPayloadBoolean":1}\n` to stdout, and writes zero stderr
   bytes. Before invocation, the Test-author asserts the exact preload source
   and exact serialized Fixture P bytes. The preload gives all ordinary
   payload objects one enumerable inherited boolean; only Fixture P's direct
   own `false` qualifies, while its inherited-only and nested-only booleans do
   not. Neither stream contains a frozen sensitive token. Fixture P and the
   preload retain identical bytes and BigInt numeric `mode`, `size`, and
   `mtimeNs`, and their parent retains identical sorted entry names.
3. Given Frozen Fixture B, one invocation exits `0`, writes exactly
   `{"eventsWithPayloadBoolean":0}\n` to stdout, writes zero stderr bytes,
   emits no frozen sensitive token, and leaves the fixture and parent
   unchanged by every observation named in AC1. Before invocation, Fixture B
   is parsed and asserted to contain the exact empty `events` array and frozen
   opaque job, approval, outbox, and metadata values.
4. In separate fresh empty temporary parents, the zero-argument invocation
   and the two-argument invocation with absent absolute path
   `<parent>/arity-state-MUST-NOT-LEAK.json` plus literal `extra` each exit
   `2`, write zero stdout bytes, and write exactly
   `usage: node app/tools/event-payload-boolean-count.js <state-file>\n` to
   stderr. The candidate path remains absent and each parent retains empty
   sorted entries.
5. Invoking the command against frozen absent path
   `<parent>/PATH-MUST-NOT-LEAK.json` and, separately, frozen empty directory
   `<parent>/DIRECTORY-MUST-NOT-LEAK` exits `1`, writes zero stdout bytes, and
   writes exactly
   `event-payload-boolean-count: cannot read state file\n` to stderr. The
   absent path remains absent. The directory remains a directory with
   identical BigInt numeric `mode`, `size`, `mtimeNs`, and sorted entries; both
   parents retain their sorted entries.
6. Invoking the command against Frozen Fixture C and, separately, Frozen
   Fixture D exits `1`, writes zero stdout bytes, and writes exactly
   `event-payload-boolean-count: state file is not valid JSON\n` to stderr.
   Neither stream contains `MALFORMED-PAYLOAD-MUST-NOT-LEAK`. Each fixture's
   exact bytes and BigInt numeric `mode`, `size`, and `mtimeNs`, and each
   parent's sorted entries, remain unchanged.
7. Each S1-S19 frozen structural-invalid fixture is invoked once; every
   invocation exits `1`, writes zero stdout bytes, and writes exactly
   `event-payload-boolean-count: invalid Relay state\n` to stderr. S19
   contains a qualifying event before its duplicate-ID sibling, proving
   complete event-array validation precedes counting. Before invocation,
   Fixtures A-D, Fixture P, and S1-S19 are pairwise byte-distinct; the preload
   source is byte-distinct from every state fixture. Every fixture retains its
   exact bytes and BigInt numeric `mode`, `size`, and `mtimeNs`, and every
   parent retains its sorted entries.
8. Every product child invocation in AC1-AC7 uses a `5_000` ms timeout,
   returns with `error === undefined` and `signal === null`, and emits none of
   the complete frozen sensitive-token set. Static inspection asserts that
   the Builder file's complete CommonJS module-specifier set is exactly
   `node:fs`, its complete `node:fs` operation set is exactly `readFileSync`,
   and its source contains none of `server.js`, `fetch(`, `WebSocket`,
   `node:http`, `node:https`, `node:net`, `node:child_process`, `setTimeout`,
   or `setInterval`. Static inspection also asserts that the Test-author
   file's complete module-specifier set is exactly `node:assert`,
   `node:child_process`, `node:fs`, `node:os`, `node:path`, and `node:test`.
9. Git history contains the complete Test-author commit before the Builder
   commit. Apart from role-owned ticket-log appends, the Test-author commit
   creates only `app/tests/event-payload-boolean-count.test.js` and the
   Builder commit creates only `app/tools/event-payload-boolean-count.js`.
   Existing application and test files, `app/server.js`, `app/package.json`,
   lockfiles, dependencies, persisted schema, `factory/KIT_PIN`, and Factory
   controls remain unchanged. After implementation,
   `node --test app/tests/event-payload-boolean-count.test.js`,
   `npm test --prefix app`, and
   `.github/scripts/test-immutability-check.sh` each exit `0`.

## Ambiguity log

- **Meaning of “own payload value”:** resolved from the ticket wording and
  JavaScript's persisted JSON object model as an own enumerable string-keyed
  property value of a non-null, non-array payload object. The predicate does
  not traverse nested objects or arrays. Fixture A separates direct, nested,
  array-contained, primitive-payload, and absent-payload cases.
- **Boolean predicate:** resolved as `typeof value === "boolean"`; both
  `true` and `false` qualify, while strings, numbers, null, objects, and arrays
  are not coerced. Fixture A freezes positive cases for both boolean values.
- **Inherited case:** JSON cannot serialize prototype inheritance. Resolved
  with the frozen temporary, child-only preload in AC2. It changes only the
  child process's in-memory prototype, is removed with that case's temporary
  parent, and creates no production dependency or repository file.
- **Event and payload validation:** resolved from the shipped
  [`event-count` command](../../app/tools/event-count.js), its
  [suite](../../app/tests/event-count.test.js), and the protected
  [`event-metadata` suite](../../app/tests/event-metadata.test.js). The root
  and event core are completely validated and event IDs are unique; payload
  content remains opaque for structural validity, so absent, primitive,
  array, and object payloads are valid and only the last kind can match.
- **Error precedence and output privacy:** resolved from the shipped
  [`event-count` command](../../app/tools/event-count.js): arity, read, parse,
  complete structural validation, then counting, with tool-prefixed fixed
  errors and one count-only compact JSON success line.
- **Cross-ticket ownership:** a repository-wide scan of sibling ticket files
  found no other declaration of either owned T-231 path. Under the bounded
  three-ticket cohort, `origin/ticket/T-232` and `origin/ticket/T-233` were
  also inspected with `git show`; they own distinct approval and outbox
  tool/test pairs. The in-flight numeric analogue T-228 likewise owns distinct
  number-count paths. No file-boundary conflict exists within the visibility
  ceiling of the check.
- **Protected source boundary:** the declared fixture seam
  `app/tests/event-metadata.test.js` reads and statically constrains only
  `app/tools/event-metadata.js`; it does not enumerate the tools directory,
  assert an import/export allowlist for the new path, snapshot it, or name it.
  The pinned deterministic readiness scan admits the planned Builder path and
  `node:fs`/`readFileSync` boundary. No protected-test conflict exists and the
  seam remains reference-only.
- **Fixture derivation, generated values, and lifecycle:** every invalid state
  fixture is a direct literal, not a casing, normalization, or mutation of a
  valid fixture, and AC7 proves pairwise byte distinctness. Expected IDs,
  timestamps, boolean values, and counts are frozen literals; there is no
  generated identifier, sequence, counter, reset, or clock. Each case owns a
  fresh temporary parent, synchronously waits for its child, and recursively
  removes that parent in `finally`; there is no database, parent row, sibling
  dependent table, foreign key, cascade, or ordered cleanup obligation.
- **Topology and global protected text:** the change is a local
  file-to-stdout process with no endpoint, UI, auth, cookie, CORS, credential,
  domain, preview deploy, or external send. The pinned global-literal checks
  cover the command name, success envelopes, usage line, and three fixed error
  lines; no protected global-shell text collision exists.
- **Prior rulings:** [`factory/rulings.md`](../rulings.md) contains no product
  or security ruling for boolean payload inspection. Its T-178 verification
  ruling is applied by keeping the exact targeted, full-app, and immutability
  commands in AC9 and leaving repository-baseline checks to protected CI.

## Frozen contract — version 1

- **Freeze result:** PASS. Contract version 1 is frozen.

This is the exact interface for the Test-author and Builder. If it proves
wrong, return T-231 to Ready and append a higher-numbered frozen contract;
version 1 must not be edited or removed.

### Command-line interface and failure precedence

- Repository-root invocation is exactly
  `node app/tools/event-payload-boolean-count.js <state-file>`.
- Exactly one user-supplied positional argument is accepted. Any other arity
  exits `2`, writes zero stdout bytes, writes exactly the following single
  LF-terminated line to stderr, and does not read or create a candidate state
  path:

  ```text
  usage: node app/tools/event-payload-boolean-count.js <state-file>
  ```

- After arity validation, the command synchronously reads `<state-file>`
  directly as UTF-8. Any `readFileSync` failure, including an absent path, a
  directory in place of a regular file, or an OS read denial, exits `1`,
  writes zero stdout bytes, and writes exactly:

  ```text
  event-payload-boolean-count: cannot read state file
  ```

- Readable bytes rejected by `JSON.parse` exit `1`, write zero stdout bytes,
  and write exactly:

  ```text
  event-payload-boolean-count: state file is not valid JSON
  ```

- A parsed value that fails the frozen structurally-valid-state rules exits
  `1`, writes zero stdout bytes, and writes exactly:

  ```text
  event-payload-boolean-count: invalid Relay state
  ```

- Failure precedence is argument count, file read, JSON parse, complete
  structural validation, then counting.
- Every error is one fixed line terminated by exactly one LF. No error includes
  the state path, OS error, parser text, input bytes, stack trace, event field,
  payload value, job field, approval field, outbox field, or second line.
- Acceptance uses absolute temporary paths. Symlink inputs and portable
  simulation of OS permission denial are outside the required fixture matrix;
  if `readFileSync` throws for either, the frozen read-error result applies.

### Structurally valid state, boolean predicate, and success

A structurally valid state is a parsed non-null, non-array object with own
`events`, `jobs`, `approvals`, and `outbox` properties whose values are arrays.
Additional top-level properties are allowed and ignored.

Every `events` element must be a non-null, non-array object with non-empty
string own properties `id`, `type`, and `receivedAt`. Event IDs must be
pairwise distinct across the complete array. Event `payload` is optional and
opaque for structural validity, as are all additional event fields. Job,
approval, and outbox elements are wholly opaque; only the fact that their
top-level collections are arrays is validated.

The command validates the complete event array and ID uniqueness before
counting. An event contributes exactly one when its `payload` is a non-null,
non-array object and at least one direct own enumerable string-keyed payload
property has a value for which `typeof value === "boolean"`. It contributes
zero otherwise. Both `true` and `false` qualify. The command does not traverse
a direct value that is an object or array, inspect inherited properties,
coerce values, parse boolean-looking strings, or count an event more than
once.

Success exits `0`, writes zero stderr bytes, and writes one compact JSON object
followed by exactly one LF. Its only key is:

```json
{"eventsWithPayloadBoolean":<nonnegative integer count>}
```

No stored event ID, type, payload key or value, `receivedAt`, additional event
field, job, approval, outbox, or metadata value is emitted.

### Frozen Fixture A — direct, nested, and non-boolean payload values

`app/tests/event-payload-boolean-count.test.js` writes this exact parsed value
to an isolated temporary `state.json` using
`JSON.stringify(value, null, 2) + "\n"`:

```json
{
  "events": [
    {
      "id": "EVENT-ID-MULTI-MUST-NOT-LEAK",
      "type": "EVENT-TYPE-MULTI-MUST-NOT-LEAK",
      "payload": {
        "enabled": true,
        "disabled": false,
        "nested": { "flag": true },
        "array": [false],
        "text": "BOOLEAN-STRING-MUST-NOT-LEAK"
      },
      "receivedAt": "2026-08-25T12:00:00.000Z"
    },
    {
      "id": "EVENT-ID-FALSE-MUST-NOT-LEAK",
      "type": "EVENT-TYPE-FALSE-MUST-NOT-LEAK",
      "payload": { "flag": false },
      "receivedAt": "2026-08-25T12:00:01.000Z"
    },
    {
      "id": "EVENT-ID-OWN-NAME-MUST-NOT-LEAK",
      "type": "EVENT-TYPE-OWN-NAME-MUST-NOT-LEAK",
      "payload": { "constructor": true },
      "receivedAt": "2026-08-25T12:00:02.000Z"
    },
    {
      "id": "EVENT-ID-NESTED-MUST-NOT-LEAK",
      "type": "EVENT-TYPE-NESTED-MUST-NOT-LEAK",
      "payload": {
        "nested": { "flag": false },
        "array": [true],
        "text": "true",
        "number": 1,
        "nil": null
      },
      "receivedAt": "2026-08-25T12:00:03.000Z"
    },
    {
      "id": "EVENT-ID-ARRAY-PAYLOAD-MUST-NOT-LEAK",
      "type": "EVENT-TYPE-ARRAY-PAYLOAD-MUST-NOT-LEAK",
      "payload": [true],
      "receivedAt": "2026-08-25T12:00:04.000Z"
    },
    {
      "id": "EVENT-ID-PRIMITIVE-PAYLOAD-MUST-NOT-LEAK",
      "type": "EVENT-TYPE-PRIMITIVE-PAYLOAD-MUST-NOT-LEAK",
      "payload": false,
      "receivedAt": "2026-08-25T12:00:05.000Z"
    },
    {
      "id": "EVENT-ID-ABSENT-PAYLOAD-MUST-NOT-LEAK",
      "type": "EVENT-TYPE-ABSENT-PAYLOAD-MUST-NOT-LEAK",
      "receivedAt": "2026-08-25T12:00:06.000Z"
    }
  ],
  "jobs": [{ "lastError": "JOB-ERROR-MUST-NOT-LEAK" }],
  "approvals": [{ "reason": "APPROVAL-REASON-MUST-NOT-LEAK" }],
  "outbox": [{ "body": "OUTBOX-BODY-MUST-NOT-LEAK" }],
  "metadata": { "note": "TOP-LEVEL-METADATA-MUST-NOT-LEAK" }
}
```

The exact success stdout is `{"eventsWithPayloadBoolean":3}\n`. The first
event proves one-count-per-event because both direct boolean values qualify;
its nested and array-contained booleans do not add matches. The next two prove
that direct `false` and an own property named `constructor` qualify. The final
four prove nested-only values, an array payload, a primitive boolean payload,
and an absent payload do not qualify.

### Frozen Fixture P — inherited in-memory boolean

Fixture P has these exact UTF-8 bytes, including the terminal LF:

```json
{"events":[{"id":"EVENT-ID-INHERITED-MUST-NOT-LEAK","type":"event","payload":{},"receivedAt":"2026-08-25T13:00:00.000Z"},{"id":"EVENT-ID-DIRECT-MUST-NOT-LEAK","type":"event","payload":{"flag":false},"receivedAt":"2026-08-25T13:00:01.000Z"},{"id":"EVENT-ID-NESTED-PRELOAD-MUST-NOT-LEAK","type":"event","payload":{"nested":{"flag":true}},"receivedAt":"2026-08-25T13:00:02.000Z"}],"jobs":[],"approvals":[],"outbox":[],"preloadMarker":"BOOLEAN-PRELOAD-MUST-NOT-LEAK"}
```

The Test-author writes a sibling `preload.js` with exactly these bytes:

```js
"use strict";
Object.defineProperty(Object.prototype, "INHERITED-BOOLEAN-MUST-NOT-LEAK", {
  value: true,
  enumerable: true,
  configurable: true,
});
```

The code fence has one terminal LF, which is part of `preload.js`. The product
child is exactly
`spawnSync(process.execPath, ["--require", preloadPath, TOOL, fixturePath],
{ stdio: "pipe", timeout: 5_000 })`; no other product case uses a preload.
Immediately before tool evaluation, every ordinary payload object inherits
enumerable boolean `true`. Exactly the direct own `false` on event 1 qualifies;
the inherited-only event and nested-only event do not, so stdout is
`{"eventsWithPayloadBoolean":1}\n`.

### Frozen Fixtures B-D and filesystem paths

- Fixture B contains exactly the UTF-8 bytes
  `{"events":[],"jobs":[null],"approvals":[7],"outbox":["OPAQUE-OUTBOX-MUST-NOT-LEAK"],"metadata":"OPAQUE-METADATA-MUST-NOT-LEAK"}\n`.
  It is structurally valid and writes exactly
  `{"eventsWithPayloadBoolean":0}\n`.
- Fixture C contains exactly the UTF-8 bytes
  `{"events":[],"payload":"MALFORMED-PAYLOAD-MUST-NOT-LEAK"\n`.
  It is rejected by `JSON.parse` because the object is not closed.
- Fixture D is a zero-byte regular file and is also rejected by `JSON.parse`.
- The arity path is absent absolute path
  `<temporary-parent>/arity-state-MUST-NOT-LEAK.json`.
- The read-failure paths are absent absolute path
  `<temporary-parent>/PATH-MUST-NOT-LEAK.json` and existing empty directory
  `<temporary-parent>/DIRECTORY-MUST-NOT-LEAK`.

### Frozen structural-invalid fixtures

Each value below is written directly as the displayed compact UTF-8 JSON plus
one LF. No invalid fixture is derived from another fixture.

| ID | Exact JSON before final LF | Violated rule |
|---|---|---|
| S1 | `null` | root is null |
| S2 | `[]` | root is an array |
| S3 | `{"events":[],"jobs":[],"approvals":[]}` | missing own `outbox` array |
| S4 | `{"events":{},"jobs":[],"approvals":[],"outbox":[]}` | `events` is not an array |
| S5 | `{"events":[],"jobs":{},"approvals":[],"outbox":[]}` | `jobs` is not an array |
| S6 | `{"events":[],"jobs":[],"approvals":{},"outbox":[]}` | `approvals` is not an array |
| S7 | `{"events":[],"jobs":[],"approvals":[],"outbox":{}}` | `outbox` is not an array |
| S8 | `{"events":[null],"jobs":[],"approvals":[],"outbox":[]}` | event is null |
| S9 | `{"events":[[]],"jobs":[],"approvals":[],"outbox":[]}` | event is an array |
| S10 | `{"events":[{"type":"meeting","receivedAt":"2026-08-25T12:00:00.000Z"}],"jobs":[],"approvals":[],"outbox":[]}` | event lacks own `id` |
| S11 | `{"events":[{"id":"","type":"meeting","receivedAt":"2026-08-25T12:00:00.000Z"}],"jobs":[],"approvals":[],"outbox":[]}` | event `id` is empty |
| S12 | `{"events":[{"id":7,"type":"meeting","receivedAt":"2026-08-25T12:00:00.000Z"}],"jobs":[],"approvals":[],"outbox":[]}` | event `id` is not a string |
| S13 | `{"events":[{"id":"event-alpha","receivedAt":"2026-08-25T12:00:00.000Z"}],"jobs":[],"approvals":[],"outbox":[]}` | event lacks own `type` |
| S14 | `{"events":[{"id":"event-alpha","type":"","receivedAt":"2026-08-25T12:00:00.000Z"}],"jobs":[],"approvals":[],"outbox":[]}` | event `type` is empty |
| S15 | `{"events":[{"id":"event-alpha","type":7,"receivedAt":"2026-08-25T12:00:00.000Z"}],"jobs":[],"approvals":[],"outbox":[]}` | event `type` is not a string |
| S16 | `{"events":[{"id":"event-alpha","type":"meeting"}],"jobs":[],"approvals":[],"outbox":[]}` | event lacks own `receivedAt` |
| S17 | `{"events":[{"id":"event-alpha","type":"meeting","receivedAt":""}],"jobs":[],"approvals":[],"outbox":[]}` | event `receivedAt` is empty |
| S18 | `{"events":[{"id":"event-alpha","type":"meeting","receivedAt":null}],"jobs":[],"approvals":[],"outbox":[]}` | event `receivedAt` is not a string |
| S19 | `{"events":[{"id":"event-alpha","type":"meeting","payload":{"flag":true},"receivedAt":"2026-08-25T12:00:00.000Z"},{"id":"event-alpha","type":"email","receivedAt":"2026-08-25T12:00:01.000Z"}],"jobs":[],"approvals":[],"outbox":[]}` | duplicate event IDs after a qualifying event; complete validation precedes counting |

Fixtures A-D, Fixture P, and S1-S19 are pairwise byte-distinct before
invocation. Fixture A, Fixture B, and Fixture P satisfy every frozen structural
rule. Fixtures C and D fail `JSON.parse`. Every S fixture parses successfully
and then fails its named structural rule. No invalid fixture is created by a
casing, normalization, or mutation transform.

### Sensitive-token set, offline execution, and lifecycle

The complete sensitive-token set is every literal in Fixtures A-D, Fixture P,
the preload, and the filesystem path fixtures containing `MUST-NOT-LEAK`, plus
the full basenames `arity-state-MUST-NOT-LEAK.json` and
`PATH-MUST-NOT-LEAK.json`. Every product invocation asserts that neither
stdout nor stderr contains any member.

Except for AC2's explicitly frozen `--require` prefix, every invocation is
`spawnSync(process.execPath, [TOOL, ...args], { stdio: "pipe", timeout: 5_000
})`. Every result asserts `error === undefined` and `signal === null`.

Each case owns a fresh `fs.mkdtempSync` parent. Before invocation, the
Test-author snapshots each regular file's exact bytes plus
`fs.statSync(path, { bigint: true }).mode`, `size`, and `mtimeNs`, waits
synchronously for each child, and recursively removes the parent in `finally`.
The directory-path case snapshots the directory's same three BigInt numeric
stat fields and sorted entries. Repeated success needs no reset because the
command is read-only. Access time is excluded because reading the fixture is
the behavior under test.

Tests share no state path, process, port, server, database, table, generated
identifier, sequence, counter, clock, or timestamp. There is no parent row,
sibling dependent table, foreign key, cascade, or ordered cleanup obligation.

### File locations, protected boundaries, and topology

- Builder ownership is creation of
  `app/tools/event-payload-boolean-count.js` only. It is a standalone CommonJS
  command whose complete module-specifier set is exactly `node:fs` and whose
  only `node:fs` operation is `readFileSync`.
- Test-author ownership is creation of
  `app/tests/event-payload-boolean-count.test.js` only. Its complete
  module-specifier set is exactly `node:assert`, `node:child_process`,
  `node:fs`, `node:os`, `node:path`, and `node:test`. The temporary preload is
  literal fixture content written and removed by this owned test; it is not a
  repository file.
- `app/tests/event-metadata.test.js` is the protected reference-only fixture
  seam and is not edited. Its static source checks inspect only
  `app/tools/event-metadata.js`, not the new Builder path. No protected test
  enumerates `app/tools/` or `app/tests/`, restricts the new command's module
  surface, or snapshots the new file. `Protected-Test-Conflicts: none` remains
  exact.
- `app/package.json` already includes the new test through
  `node --test tests/*.test.js`; neither role edits the manifest.
- Targeted acceptance is
  `node --test app/tests/event-payload-boolean-count.test.js`.
  Post-implementation verification is `npm test --prefix app` and
  `.github/scripts/test-immutability-check.sh`.
- No endpoint, HTTP shape, route, UI selector, global page-shell text,
  accessible UI name, authentication, cookie, CORS, cross-origin request,
  credential, domain, preview deployment, or external send is introduced.
  Topology is one local file-to-stdout process, and `External: no` remains
  exact.

## Criteria checklist

- **Pass/fail and unambiguous:** AC1-AC7 freeze exact fixtures, argument
  vectors, exit codes, stream bytes, stat fields, parent entries, and failure
  precedence. AC8 freezes timeouts and static source boundaries. AC9 is
  decided from Git history, exact path diffs, and three command exit statuses.
- **Coverage:** AC1 covers every directly serializable predicate branch and
  one-count-per-event. AC2 distinguishes inherited from direct own values.
  AC3 covers zero output and opaque sibling collections. AC4-AC7 cover arity,
  read, parse, every structural rule, complete validation, distinctness, and
  redaction. AC8-AC9 cover termination, offline/source limits, ownership,
  stage order, and verification. Every contract fixture, path, output, and
  source boundary appears in at least one criterion.
- **Cross-ticket and protected-boundary checks:** sibling ticket declarations
  and both other leased cohort branches have no overlapping path. The
  protected seam names only its existing tool, and the pinned readiness scan
  reports no protected source assertion or hash collision for T-231.
- **Deploy/topology completeness:** this is an offline local command. It has
  no cross-origin, auth, cookie, CORS, domain, credential, preview, UI, or send
  surface, so there is no deploy topology to infer.
- **Derived fixtures and generated values:** all invalid fixtures are direct
  byte literals and AC7 proves distinctness. The AC2 preload has exact source
  and an exact in-memory effect; it is a positive semantic harness, not an
  invalid serialized fixture. All expected values are literal, with no
  initializer, reset, sequence, counter, generated ID, or generated time.
- **Lifecycle closure:** fresh temporary parents, synchronous children,
  snapshots, and `finally` cleanup close every fixture lifecycle. No database
  or dependent table exists; the one temporary preload is a sibling file
  covered by the same snapshots and cleanup.
- **Global protected text:** the command name, three JSON success envelopes,
  usage line, and three fixed error lines each returned `GLOBAL TEXT PASS`
  from the pinned collision checker. No global UI literal is added.

## Coordination

T-231 owns only `app/tools/event-payload-boolean-count.js` and
`app/tests/event-payload-boolean-count.test.js`. The protected
`app/tests/event-metadata.test.js` seam is reference-only. T-232 and T-233,
including their remote leased branches, own distinct tool and test paths.

## Planning log

- 2026-08-25: Planner froze contract version 1 with byte-exact success and
  failure envelopes, direct/inherited/nested boolean predicate fixtures,
  complete event validation, privacy sentinels, read-only lifecycle, exact
  role ownership, protected-source boundaries, and local-only topology.
  `SPEC.md`, `AGENTS.md`, `app/server.js`, `app/package.json`, the shipped
  event count and protected event-metadata command/suites,
  `factory/rulings.md`, all local sibling ownership declarations, and the
  remote leased cohort branches were inspected. The pinned deterministic
  checker returned `READINESS PASS` for T-231 and `GLOBAL TEXT PASS` for all
  eight frozen shell-visible literals. No file-boundary conflict,
  protected-test conflict, topology requirement, external-send label, or
  operator decision remains.
