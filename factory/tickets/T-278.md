# T-278 — reject duplicate job event IDs in status counts

Kit-SHA: bcffd8f0d3d0596752136d7ee2fd95e4f912806c
State: Building
Initiative: I-002
Priority: normal
Risk class: low
External: no
Branch: ticket/T-278
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/job-status-count.test.js
Authentication-Seams: none
Builder ownership: app/tools/job-status-count.js only
Test-author ownership: app/tests/job-status-count.test.js only

## Spec'd description

Relay creates one job per accepted event, but the offline `job-status-count`
report ([`app/tools/job-status-count.js`](../../app/tools/job-status-count.js),
shipped by [T-210](T-210.md)) accepts two jobs that reference the same event
ID and can count both. Extend the command's existing complete-state
validation to also reject a `jobs` array containing two elements with the
same non-empty `eventId`, using the identical fixed, redacted
`job-status-count: invalid Relay state` failure already used for every other
structural-validity violation. No new error line, output field, argument, or
dependency is introduced.

Product-doc and repository sources:

- [`SPEC.md` behavior 1](../../SPEC.md#behavior-spec) and
  [conformance case 2](../../SPEC.md#test-cases-the-conformance-suite)
  establish that a repeated event `id` is idempotent at intake and "never
  creates a second job" — exactly one job per event ID is the invariant this
  offline report must not silently violate by double-counting a state that
  already broke it.
- [`app/server.js`](../../app/server.js) establishes the persisted job shape,
  including `eventId`, and that Relay's own intake path is what is meant to
  keep jobs one-per-event; this command is read-only and does not enforce
  that invariant at write time, only refuses to report over a state that
  already violates it.
- [T-210's frozen contract version 1](T-210.md#frozen-contract--version-1)
  establishes the complete current interface this ticket extends unchanged:
  the two-argument CLI, failure precedence (arity, read, parse, structural
  validation, then counting), the four-collection/job-field/status/counter/
  `lastError` structural rules, pairwise-distinct job `id` validation, the
  exact `{"jobsWithStatus":<count>}` success envelope, the three fixed
  redacted error lines, the complete sensitive-token privacy sweep, the
  read-only/offline/self-terminating lifecycle proof, and the `node:fs`
  /`readFileSync`-only source boundary. This ticket's Builder ownership is
  restricted to widening the existing structural-validity check by one rule;
  every other frozen behavior in T-210's contract is preserved byte-for-byte.
- [`AGENTS.md` Boundaries](../../AGENTS.md#boundaries) keeps `app/`
  zero-dependency, and [`AGENTS.md` Verification](../../AGENTS.md#verification)
  defines the later full-suite and immutability checks.

No endpoint, HTTP shape, UI selector, global-shell UI text, persisted schema,
server behavior, package, dependency, lockfile, Factory control,
authentication path, deployment topology, or external-send behavior changes.
Event-collection uniqueness is explicitly out of scope (AC4); only the `jobs`
array's `eventId` field gains a uniqueness rule.

## Acceptance criteria

1. A state containing two otherwise valid jobs with the same non-empty
   `eventId` exits `1`, writes exactly the existing invalid-state stderr line,
   and writes zero stdout bytes.
2. Duplicate event IDs are invalid whether both jobs match the requested
   status or only one does. Complete job validation and uniqueness checking
   precede filtering and counting.
3. Valid jobs with unique event IDs retain the existing output shape, field
   order, literal status matching, exit statuses, and stderr messages
   byte-for-byte. Empty state and zero matches remain valid.
4. Existing collection, job-field, status, counter, last-error, and unique job
   ID validation remain unchanged. Event records are not made unique here.
5. Failures remain redacted and leak no requested status, job or event ID,
   error text, state bytes, input path, parser detail, or operating-system
   detail.
6. The command remains offline, zero-dependency, deterministic, read-only, and
   self-terminating. Tests precede implementation; the targeted test, full app
   suite, and immutability check pass without other product or Factory changes.

## Frozen contract — version 1

- **Freeze result:** PASS. Contract version 1 is frozen.

This is the exact interface for the Test-author and Builder. If it proves
wrong, return T-278 to Ready and append a higher-numbered frozen contract;
version 1 must not be edited or removed.

### Unchanged interface (inherited from T-210's shipped contract)

Everything in [T-210's frozen contract version 1](T-210.md#frozen-contract--version-1)
remains exactly as shipped in the current `app/tools/job-status-count.js` and
is reproduced here as the regression surface this ticket must not disturb:

- CLI: `node app/tools/job-status-count.js <state-file> <status>`; any arity
  other than two exits `2`, writes zero stdout bytes, and writes exactly
  `usage: node app/tools/job-status-count.js <state-file> <status>\n` to
  stderr without touching the candidate path.
- Failure precedence: argument count, then `readFileSync`, then `JSON.parse`,
  then complete structural validation, then counting.
- `readFileSync` failure (absent path, directory, OS denial) exits `1`,
  writes zero stdout bytes, writes exactly
  `job-status-count: cannot read state file\n`.
- `JSON.parse` failure exits `1`, writes zero stdout bytes, writes exactly
  `job-status-count: state file is not valid JSON\n`.
- Structural-validity failure exits `1`, writes zero stdout bytes, writes
  exactly `job-status-count: invalid Relay state\n`. **This ticket adds one
  new rule to this class; the line itself does not change.**
- Structurally valid state: a parsed non-null, non-array object with exactly
  four own array properties `events`, `jobs`, `approvals`, `outbox`. Every
  `jobs` element must be a non-null, non-array object with non-empty string
  own `id` and `eventId`, own `status` exactly one of `pending`/`done`/`dead`,
  own `attempts`/`retries`/`attemptsSinceRetry` as integers `>= 0`, and own
  `lastError` equal to `null` or a string. Job `id` values must be pairwise
  distinct across the array. Additional job properties are allowed and
  ignored; `events`/`approvals`/`outbox` elements stay wholly opaque.
- Success: exit `0`, zero stderr bytes, exactly one line
  `{"jobsWithStatus":<count>}\n`, counting jobs whose stored `status` is
  strictly (case-sensitive, untrimmed) equal to the supplied `<status>`; a
  supplied status outside the stored enum is a valid zero-count query.
- Builder module-specifier set is exactly `node:fs`; its only `node:fs`
  operation is `readFileSync`; its source contains none of `server.js`,
  `fetch(`, `WebSocket`, `node:http`, `node:https`, `node:net`,
  `node:child_process`, `setTimeout`, `setInterval`.
- Test file module-specifier set is exactly `node:assert`,
  `node:child_process`, `node:fs`, `node:os`, `node:path`, `node:test`.
- Frozen Fixtures A, B, C, D and structural-invalid Fixtures S1-S25, defined
  verbatim in [T-210's frozen contract](T-210.md#frozen-fixture-a--all-statuses-repeated-dead-status-and-opaque-content),
  are reused byte-for-byte with unchanged expected outcomes: Fixture A yields
  `2`/`1`/`1`/`0`/`0`/`0` for `dead`/`pending`/`done`/`Dead`/` dead`/`""`;
  Fixture B yields `0` for `dead`; Fixtures C and D fail `JSON.parse`; S1-S25
  each fail structural validation exactly as documented there (S24's
  duplicate-job-`id` case remains a *distinct* violation from this ticket's
  new duplicate-`eventId` case — the two S24 jobs use different `eventId`
  values `event-a`/`event-b`, so S24 does not exercise the new rule).

### New rule: duplicate non-empty job `eventId` is a structural-validity violation

Within the existing single pass that already tracks job `id` uniqueness, the
Builder additionally tracks the set of `eventId` values seen across
`state.jobs` in array order. If a job's `eventId` (already guaranteed a
non-empty string by the unchanged per-job shape rule) has already been seen
earlier in the array, the state is structurally invalid — same class, same
exact fixed stderr line, same exit `1`, same zero stdout bytes as every other
structural violation. This check is independent of and additional to the
existing job-`id` uniqueness check; either violation alone is sufficient to
invalidate the state. No relationship is required between which job in a
duplicate `eventId` pair appears first and whether it matches the queried
`<status>`.

Reference implementation shape (Builder is not required to match this
verbatim, only its externally observable behavior):

```js
const jobIds = new Set();
const eventIds = new Set();
for (const job of state.jobs) {
  if (!isValidJob(job) || jobIds.has(job.id) || eventIds.has(job.eventId)) {
    return false;
  }
  jobIds.add(job.id);
  eventIds.add(job.eventId);
}
```

### New frozen structural-invalid fixtures S26 and S27

Each value below is written directly as the displayed compact UTF-8 JSON plus
one LF, exactly like S1-S25.

| ID | Exact JSON before final LF | Violated rule |
|---|---|---|
| S26 | `{"events":[],"jobs":[{"id":"JOB-ID-EVENTDUP-ALPHA-MUST-NOT-LEAK","eventId":"EVENT-ID-DUP-BOTH-MUST-NOT-LEAK","status":"dead","attempts":1,"lastError":null,"retries":0,"attemptsSinceRetry":1},{"id":"JOB-ID-EVENTDUP-BRAVO-MUST-NOT-LEAK","eventId":"EVENT-ID-DUP-BOTH-MUST-NOT-LEAK","status":"dead","attempts":2,"lastError":null,"retries":0,"attemptsSinceRetry":2}],"approvals":[],"outbox":[]}` | duplicate non-empty `eventId`; **both** jobs match queried status `dead` |
| S27 | `{"events":[],"jobs":[{"id":"JOB-ID-EVENTDUP-CHARLIE-MUST-NOT-LEAK","eventId":"EVENT-ID-DUP-ONE-MUST-NOT-LEAK","status":"dead","attempts":1,"lastError":null,"retries":0,"attemptsSinceRetry":1},{"id":"JOB-ID-EVENTDUP-DELTA-MUST-NOT-LEAK","eventId":"EVENT-ID-DUP-ONE-MUST-NOT-LEAK","status":"pending","attempts":0,"lastError":null,"retries":0,"attemptsSinceRetry":0}],"approvals":[],"outbox":[]}` | duplicate non-empty `eventId`; **only one** job (the first, `dead`) matches queried status `dead` |

Both S26 and S27 are individually valid under every rule shipped in T-210
(each job independently satisfies the complete per-job shape rule, and job
`id` values are pairwise distinct within each fixture) and are accepted by
the current shipped `app/tools/job-status-count.js` — this was verified
directly against the shipped validator before freezing. They are rejected
*only* by this ticket's new `eventId`-uniqueness rule, proving the rule is
both necessary and correctly scoped. Both are queried with status `dead`,
both exit `1`, write zero stdout bytes, and write exactly
`job-status-count: invalid Relay state\n` to stderr — identical to every
S1-S25 outcome. S26 and S27 are pairwise byte-distinct from each other and
from Fixtures A-D and S1-S25 (fresh token vocabulary: `EVENTDUP`, `DUP-BOTH`,
`DUP-ONE`, not used anywhere else in the fixture set). Neither is derived by
casing, trimming, or mutation of an existing valid fixture; both are direct
literal fixture data.

### Sensitive-token set addition

The four new literals `JOB-ID-EVENTDUP-ALPHA-MUST-NOT-LEAK`,
`JOB-ID-EVENTDUP-BRAVO-MUST-NOT-LEAK`, `JOB-ID-EVENTDUP-CHARLIE-MUST-NOT-LEAK`,
`JOB-ID-EVENTDUP-DELTA-MUST-NOT-LEAK`, and the two new literals
`EVENT-ID-DUP-BOTH-MUST-NOT-LEAK`, `EVENT-ID-DUP-ONE-MUST-NOT-LEAK` join the
complete frozen sensitive-token set carried over from T-210. Every S26/S27
child invocation is checked against the complete token set (old and new) on
both streams, exactly like every AC1-AC7-equivalent case.

### File locations, protected boundaries, and topology

- Builder ownership is editing `app/tools/job-status-count.js` only, widening
  its existing structural-validity pass by the one rule above. Its module-
  specifier and `node:fs`-operation sets, and its forbidden-substring
  boundary, are unchanged from T-210 (see above).
- Test-author ownership is editing `app/tests/job-status-count.test.js` only.
  Its module-specifier set is unchanged from T-210 (see above); no new
  dependency is needed to express the new fixtures or assertions.
- `Fixture-Seams: app/tests/job-status-count.test.js` is this ticket's own
  Test-author-owned file (standalone-ticket convention shared with T-276 and
  T-277); it names no external protected reference file.
- No protected test enumerates `app/tools/` or `app/tests/`, restricts this
  file's import/export surface, or snapshots its source. Grep of every
  tracked `app/tests/*.test.js` file found no reference to
  `job-status-count` outside `app/tests/job-status-count.test.js` itself.
  `python3 "$FACTORY_RELEASE_PATH/scripts/ticket-readiness.py" --ticket T-278`
  returned `READINESS PASS`. `Protected-Test-Conflicts: none` remains exact.
- `python3 "$FACTORY_RELEASE_PATH/scripts/ticket-readiness.py" --global-literal "job-status-count: invalid Relay state" --workdir "$PWD"`
  returned `GLOBAL TEXT PASS`; no new global-shell-visible literal is
  introduced by this ticket (the reused error line was already validated by
  T-210's freeze).
- Targeted acceptance is
  `node --test app/tests/job-status-count.test.js`. Post-implementation
  repository verification is `npm test --prefix app` and
  `.github/scripts/test-immutability-check.sh`, per this ticket's AC6 and the
  2026-08-10 T-178 ruling that repository baseline checks stay protected-CI
  controls outside a ticket's frozen contract.
- No endpoint, HTTP shape, route, UI selector, global page-shell text,
  accessible UI name, authentication, cookie, CORS, cross-origin request,
  credential, domain, preview deployment, or external send is introduced.
  Topology is unchanged: one local file-to-stdout process, and `External: no`
  remains exact.

## Criteria checklist

- **Pass/fail and unambiguous:** AC1/AC2 freeze the new rule against S26
  (both-match) and S27 (one-match) with exact exit code, exact stderr bytes,
  and zero stdout bytes. AC3/AC4 freeze that every T-210 fixture, envelope,
  and validation rule is byte-for-byte unchanged. AC5 names the exact
  redaction surface. AC6 freezes the verification command set.
- **Coverage:** the new `eventId`-uniqueness rule is exercised by S26 (both
  jobs match the query) and S27 (only one matches) per AC1/AC2. The unchanged
  success envelope, all three unchanged error lines, and all pre-existing
  structural rules (including S24's distinct duplicate-job-`id` case) are
  exercised via the reused T-210 fixture set per AC3/AC4. The sensitive-token
  sweep, offline/read-only/self-terminating lifecycle, and Builder/Test-author
  source boundaries are exercised per AC5/AC6. No frozen contract element
  (new rule, new fixtures, reused fixtures, error lines, success envelope,
  source boundaries, verification commands) is untouched by a criterion.
- **Ambiguity log:** see below; a non-trivial ticket with a genuine cross-
  ticket file-boundary finding logged, not silently dropped.
- **Cross-ticket file-boundary conflict:** checked every local ticket
  ownership declaration and every `origin/ticket/T-*` branch (`git branch -a`,
  `git ls-remote --heads origin`). No leased/pushed branch declares
  `app/tools/job-status-count.js` or `app/tests/job-status-count.test.js`.
  Dormant Backlog ticket T-200 declares the identical two paths with an
  older, incompatible `{"jobStatus":...,"jobs":...}` envelope — the same
  finding T-210's frozen contract already recorded and shipped past (see
  [T-210's coordination note](T-210.md#coordination)); T-200 remains
  `State: Backlog`, unleased, with no `origin/ticket/T-200` branch, so per
  this role's stated ceiling it is not concurrently in-flight and creates no
  active conflict. T-234, T-241, T-258, and T-237 declare
  `app/tests/job-status-count.test.js` only as a reference `Fixture-Seams`
  entry, not as ownership; `git diff origin/main...origin/ticket/<T>` for
  each against both this ticket's paths is empty, confirming no leased branch
  touches either file.
- **Deploy/topology completeness:** unchanged local file-to-stdout topology;
  no cross-origin, auth, cookie, or preview-deploy surface applies.
- **Derived-fixture distinctness:** S26 and S27 use a fresh token vocabulary
  not present anywhere else in the fixture set (verified: `grep -c EVENTDUP
  app/tests/job-status-count.test.js` returned `0` before this freeze), so
  they are trivially byte-distinct from Fixtures A-D and S1-S25 and from each
  other. Neither is a casing/trim/normalization mutation of a valid fixture.
- **Generated-value determinism:** no generated identifier, sequence,
  counter, clock, or timestamp participates in any expectation; every ID,
  `eventId`, status, and count in S26/S27 is direct literal fixture data.
- **Fixture lifecycle closure:** S26/S27 follow the identical isolated-
  temporary-parent-per-case, snapshot-before/after, `finally`-removal
  lifecycle already used for S1-S25; no database, parent row, sibling table,
  foreign key, cascade, or shared mutable fixture is introduced.
- **Protected source boundaries:** confirmed via
  `ticket-readiness.py --ticket T-278` → `READINESS PASS`. No protected test
  restricts the Builder file's `node:fs`-only import surface or snapshots its
  source.
- **Global protected text:** confirmed via `ticket-readiness.py
  --global-literal` for the reused invalid-state line → `GLOBAL TEXT PASS`.
  No new global-shell-visible literal is introduced.

## Ambiguity log

1. **Does duplicate-`eventId` detection normalize case or trim whitespace?**
   Neither `SPEC.md` nor `factory/rulings.md` addresses query or field
   normalization for this command. Resolved consistently with T-210's ruling
   on exact-match semantics (case-sensitive JavaScript strict equality, no
   trimming or folding) and the existing job-`id`-uniqueness convention
   (plain `Set.has`/`Set.add` on the raw string): `eventId` duplicate
   detection uses the same unnormalized `Set` membership check.
2. **Must a duplicate-`eventId` pair both match the queried status to be
   rejected, or is detection independent of the query?** AC2 states detection
   must fire "whether both jobs match the requested status or only one
   does," and requires validation to precede filtering. Resolved: the check
   runs during the existing single validation pass over the complete `jobs`
   array, before any status filtering, so it is entirely independent of the
   supplied `<status>` argument. S26 (both match) and S27 (only one matches)
   both prove rejection. A third fixture where *neither* job matches the
   query was considered and rejected as redundant: the validation pass never
   inspects the query argument at all, so "neither matches" is an equivalent
   permutation of the same code path already proven by S26/S27, not a
   distinct behavior.
3. **What exact stderr text does the new rejection use?** AC1 requires "the
   existing invalid-state stderr line." Resolved: reuse
   `job-status-count: invalid Relay state\n` verbatim from T-210's frozen
   contract; no new error line, and no new global-literal check is required
   beyond re-confirming that exact line still passes (`GLOBAL TEXT PASS`).
4. **Does this ticket also enforce unique `events[].id`?** AC4 states "Event
   records are not made unique here." Resolved: `events` elements remain
   wholly opaque per T-210's unchanged rule; only `jobs[].eventId` gains a
   uniqueness constraint. No fixture asserts anything about duplicate event
   records.
5. **Does another ticket own these two paths?** Dormant Backlog ticket
   [T-200](T-200.md) declares the identical `app/tools/job-status-count.js`
   and `app/tests/job-status-count.test.js` paths with an older, incompatible
   output envelope. This is not a new finding: T-210's frozen contract and
   Reviewer round already recorded it and the operator advanced T-210 (and
   now T-278) past Backlog while T-200 stayed dormant. T-200 remains
   `State: Backlog` with no lease and no pushed `origin/ticket/T-200` branch,
   so it is not concurrently active and does not block this freeze; it still
   cannot advance without operator retirement or re-scoping. No new operator
   decision is required to freeze T-278.
6. **Does an existing operator ruling change this contract?**
   [`factory/rulings.md`](../rulings.md) was checked before freeze. Its
   detail-route ruling is unrelated; its 2026-08-10 T-178 ruling confirms the
   targeted test, full app suite, and immutability check as the correct
   post-implementation verification set, applied here in AC6. No new ruling
   entry is needed since no new operator decision was made.

## Coordination

This ticket owns only `app/tools/job-status-count.js` and
`app/tests/job-status-count.test.js`. It is independent of T-276 and T-277
(distinct owned paths, confirmed via empty `git diff` against both branches).
Dormant Backlog ticket T-200 duplicates both paths with an incompatible,
older envelope and remains blocked from advancing until the operator retires
or re-scopes it (recorded first by T-210, reconfirmed unchanged here); it is
not concurrently active. `app/tests/job-detail.test.js` and other job-tool
suites were inspected and touch neither owned path.

## Planning log

- 2026-08-26: Planner froze contract version 1, extending T-210's shipped
  interface with one new structural-validity rule (duplicate non-empty job
  `eventId`) reusing the existing invalid-state error line unchanged. Read
  current `app/tools/job-status-count.js` and
  `app/tests/job-status-count.test.js`, `SPEC.md`, `AGENTS.md`,
  `factory/rulings.md`, T-210's full frozen contract and Reviewer round, and
  every sibling ticket declaring an overlapping `Fixture-Seams` or ownership
  path (T-200, T-234, T-237, T-241, T-258, T-259, T-252, T-276, T-277).
  Verified via `git branch -a` and `git ls-remote --heads origin` that no
  leased branch other than this one's own owns either path, and via
  `git diff origin/main...origin/ticket/<T> --stat` for T-234/T-241/T-258/
  T-237 that none touch either file. Verified directly in Node that new
  fixtures S26 and S27 are accepted by the currently shipped validator and
  rejected only once the new `eventId`-uniqueness rule is added, and that S26
  and S27 are byte-distinct from each other and from every fixture already in
  the test file (`EVENTDUP` token absent from the current file). Ran
  `python3 "$FACTORY_RELEASE_PATH/scripts/ticket-readiness.py" --ticket T-278`
  (`READINESS PASS`) and `--global-literal` for the reused invalid-state line
  (`GLOBAL TEXT PASS`) against `/private/tmp/software-factory-q99-source`,
  which matches the pinned `factory/KIT_PIN`. T-200's dormant duplicate
  ownership was reconfirmed non-blocking per T-210's precedent; no new
  concurrent file-boundary conflict, protected-test conflict, topology
  requirement, external-send label, or operator decision remains.

## Spec lint — round 1

### Contract-element coverage

| Contract element | Positive | Failure / empty | Duplicate / repeat | Out of scope |
|---|---|---|---|---|
| CLI arity and precedence | Two-argument fixture cases | Zero/one/three arguments use exact usage failure before path access | Surplus arities grouped | Other surplus counts equivalent |
| Read and parse gates | A/B parse successfully | Missing/directory read failure; C/D parse failure | — | Symlink and portable permission fixtures |
| Exact state shape and unchanged job rules | A/B valid states | S1-S25 cover root, collections, job fields, counters, `lastError`, and duplicate `id` | Equivalent field type/range cases grouped | Opaque sibling contents and event-to-job linkage |
| New raw `eventId` uniqueness rule | Unique IDs in A remain valid | S26 exact duplicate with both `dead`; S27 exact duplicate with only one `dead` | Two query-relation cases prove validation precedes filtering | Event-record uniqueness; case/trim normalization |
| Counting and success envelope | A yields 2/1/1; B and zero-match queries yield 0 | Invalid state emits no count | Repeated `dead` invocation remains stable | Statuses outside stored enum are valid zero queries |
| Exact redacted failure envelopes | — | Arity, read, parse, and invalid-state lines are exact with empty stdout | Shared invalid-state line for S1-S27 | OS/parser details intentionally hidden |
| Privacy and read-only lifecycle | Success streams and fixture snapshots | Failure streams and snapshot checks | Fresh temporary parent per case; repeated read | Access time; no database/foreign-key lifecycle |
| Offline/self-terminating source boundaries | Timed local child invocation | Forbidden network/process/timer substrings and import/`readFileSync` allowlists | Test-file boundary checked once | HTTP/UI/auth/CORS/credentials/external send |
| Ownership and serialized verification | Tests-before-implementation; targeted test | Builder restricted to tool; Test-author restricted to its suite | `npm test --prefix app` and immutability run after targeted test | Baseline protected-CI controls per ruling |

### Findings

1. SPEC-WARN: Keep dormant T-200 from advancing without retirement or re-scoping: it declares these same two paths with an incompatible envelope, though its Backlog state and lack of a ticket branch make it non-concurrent now.
2. SPEC-WARN: An invalid-UTF-8 state-file case is not in the fixture matrix; the frozen UTF-8-read then `JSON.parse` sequence determines it, so the Test-author may add a representative case as defensive coverage.

Independent checks: S26 and S27 parse, isolate only duplicate non-empty
`eventId` values, preserve distinct job IDs, and are byte-distinct from one
another and all current fixtures. The pinned readiness checker returned
`READINESS PASS` for T-278 and `GLOBAL TEXT PASS` for the reused invalid-state
line. The current targeted inherited suite passed. No ruling conflicts, fixture
lifecycle dependencies, generated expectations, protected source snapshots,
allowlists, exact-source assertions, or global-shell text collision blocks the
planned Builder edit.

SPEC-LINT: PASS

## Test-author log

- 2026-08-26: Added failing acceptance tests in
  `app/tests/job-status-count.test.js`: **AC1** covers S26, where both
  otherwise-valid jobs match `dead` but share a non-empty `eventId`; **AC2**
  covers S27, where only the first job matches `dead` and the duplicate must
  still be rejected before filtering. Both assert exit `1`, zero stdout, the
  exact existing invalid-state stderr line, redaction, and read-only fixture
  lifecycle. The complete inherited suite continues to cover AC3–AC6; no
  criterion was unexpressible as a test. Ran the scoped
  `node --test app/tests/job-status-count.test.js`: the two new tests failed
  for the intended missing-feature reason (the current tool counted S26 as 2
  and S27 as 1); all inherited tests passed. Broad verification is deferred
  to protected CI and final certification.
