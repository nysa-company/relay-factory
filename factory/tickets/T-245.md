# T-245 — count Relay events by ID and timestamp offline

Kit-SHA: e55e7aa877aad59a9b95e20df5e85117e334f204
State: Planning
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-245
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/event-count.test.js, app/tests/event-timestamp.test.js
Authentication-Seams: none
Builder ownership: app/tools/event-id-timestamp-count.js only
Test-author ownership: app/tests/event-id-timestamp-count.test.js only

## Spec'd description

Add `app/tools/event-id-timestamp-count.js`, a zero-dependency CommonJS
command that reads one persisted Relay state file while Relay is offline,
validates the complete stored event collection, and counts events for which
both the stored `id` and stored `receivedAt` strictly equal two non-empty
operator-supplied filters. It emits only
`{"eventsWithIdAndTimestamp":<count>}`, allowing an operator to make a narrow
conjunction query without starting Relay, changing durable state, parsing or
normalizing timestamps, or exposing filters, payloads, sibling collections,
or other stored content.

Product-doc and repository sources:

- [`SPEC.md` behavior 1](../../SPEC.md#behavior-spec) defines accepted events
  as `{id, type, payload}` and idempotency by event ID.
- [`SPEC.md` behaviors 6 and 7](../../SPEC.md#behavior-spec) make the atomically
  persisted file Relay's durable state and make events operator-visible.
- [`SPEC.md` conformance cases 1, 2, and 7](../../SPEC.md#test-cases-the-conformance-suite)
  require accepted events to remain visible, duplicate IDs not to duplicate
  work, and persisted work to survive restart.
- The current [`app/server.js` state model](../../app/server.js) establishes
  the four collections and persists events with `id`, `type`, `payload`, and
  an ISO-formatted `receivedAt` string.
- The shipped [`event-count` command](../../app/tools/event-count.js) and its
  [acceptance suite](../../app/tests/event-count.test.js) establish complete
  event validation, unique IDs, compact count-only output, redacted failures,
  and the read-only offline convention.
- The shipped [`event-timestamp` command](../../app/tools/event-timestamp.js)
  and its [acceptance suite](../../app/tests/event-timestamp.test.js) establish
  that `receivedAt` strings are handled verbatim rather than parsed,
  normalized, reformatted, or timezone-converted.
- [`AGENTS.md` Boundaries](../../AGENTS.md#boundaries) keeps `app/`
  zero-dependency; [`AGENTS.md` Verification](../../AGENTS.md#verification)
  and the managed baseline define later implementation checks.

No endpoint, HTTP shape, UI selector, global-shell UI text, persisted schema,
server behavior, package, dependency, lockfile, manifest, Factory control,
authentication path, deployment topology, or external-send behavior changes.

## Acceptance criteria

1. Given Frozen Fixture A and filters `event-alpha` and
   `2026-08-25T12:00:00.000Z`, each of two invocations of
   `node app/tools/event-id-timestamp-count.js <fixture-a-absolute-path> event-alpha 2026-08-25T12:00:00.000Z`
   exits `0`, writes exactly `{"eventsWithIdAndTimestamp":1}\n` to stdout,
   and writes zero stderr bytes. Neither stream contains any frozen sensitive
   token. After each invocation, Fixture A has identical bytes and BigInt
   numeric `mode`, `size`, and `mtimeNs`, and its parent has identical sorted
   entry names.
2. Given Frozen Fixture A, the six filter pairs `event-bravo` /
   `2026-08-25T12:00:01.000Z`, `Event-alpha` /
   `2026-08-25T07:00:00-05:00`, ` event-alpha` /
   ` 2026-08-25T12:00:00.000Z`, `007` / `7`, `event-café` /
   `instant-café`, and `event-verbatim` /
   `VERBATIM-TIMESTAMP-MUST-NOT-BE-NORMALIZED` are queried in that order.
   Every invocation exits `0`, writes exactly
   `{"eventsWithIdAndTimestamp":1}\n` to stdout, writes zero stderr bytes,
   emits no frozen sensitive token, and leaves the fixture and parent
   unchanged by every observation in AC1. Before invocation, the test asserts
   each pair strictly equals exactly one stored event's `id` and `receivedAt`.
3. All ten frozen zero-match cases succeed: `event-alpha` /
   `2026-08-25T12:00:01.000Z`, `Event-alpha` /
   `2026-08-25T12:00:00.000Z`, `event-alpha` /
   `2026-08-25T07:00:00-05:00`, ` event-alpha` /
   `2026-08-25T12:00:00.000Z`, `event-alpha` /
   ` 2026-08-25T12:00:00.000Z`, `7` / `7`, `event-café` /
   `instant-café`, `event-café` / `instant-café`, and `event-verbatim` /
   `verbatim-TIMESTAMP-MUST-NOT-BE-NORMALIZED` against Fixture A, plus
   `event-alpha` / `2026-08-25T12:00:00.000Z` against Frozen Fixture B. Each
   exits `0`, writes exactly `{"eventsWithIdAndTimestamp":0}\n` to stdout,
   writes zero stderr bytes, emits no frozen sensitive token, and leaves its
   fixture and parent unchanged by every observation in AC1. Before invocation
   the test proves each zero pair matches no single event and proves the exact
   byte distinctions frozen below; Fixture B retains its opaque siblings.
4. In separate fresh empty temporary parents, invocations with zero, one, two,
   or four positional arguments, and exactly-three-argument invocations with
   an empty event ID or empty timestamp, each exit `2`, write zero stdout
   bytes, and write exactly
   `usage: node app/tools/event-id-timestamp-count.js <state-file> <event-id> <timestamp>\n`
   to stderr. Every nonzero-arity case uses absent candidate
   `<parent>/arity-state-MUST-NOT-LEAK.json`; it stays absent and every parent
   keeps empty sorted entries, proving both validations precede file access.
5. Invoking with `event-alpha` / `2026-08-25T12:00:00.000Z` against absent
   `<parent>/PATH-MUST-NOT-LEAK.json` and, separately, empty directory
   `<parent>/DIRECTORY-MUST-NOT-LEAK` exits `1`, writes zero stdout bytes, and
   writes exactly `event-id-timestamp-count: cannot read state file\n` to
   stderr. The absent path stays absent. The directory stays a directory with
   identical BigInt numeric `mode`, `size`, `mtimeNs`, and sorted entries;
   both parents retain their sorted entries.
6. Invoking with `event-alpha` / `2026-08-25T12:00:00.000Z` against Frozen
   Fixtures C and D separately exits `1`, writes zero stdout bytes, and writes
   exactly `event-id-timestamp-count: state file is not valid JSON\n` to
   stderr. Neither stream contains `MALFORMED-PAYLOAD-MUST-NOT-LEAK`. Each
   fixture's exact bytes and BigInt numeric `mode`, `size`, and `mtimeNs`, and
   each parent's sorted entries, remain unchanged.
7. Each S1-S20 frozen structural-invalid fixture is invoked with `event-alpha`
   / `2026-08-25T12:00:00.000Z`; every invocation exits `1`, writes zero stdout
   bytes, and writes exactly
   `event-id-timestamp-count: invalid Relay state\n` to stderr. S19 has a
   matching event before its duplicate-ID sibling and S20 has a matching event
   before an invalid sibling, proving complete validation precedes counting.
   Before invocation, Fixtures A-D and S1-S20 are pairwise byte-distinct.
   Every fixture retains exact bytes and BigInt numeric `mode`, `size`, and
   `mtimeNs`, and every parent retains its sorted entries.
8. Every child invocation in AC1-AC7 uses a `5_000` ms timeout, returns with
   `error === undefined` and `signal === null`, and emits none of the complete
   sensitive-token set. Static inspection asserts that the Builder file's
   complete CommonJS module-specifier set is exactly `node:fs`, its complete
   `node:fs` operation set is exactly `readFileSync`, and its source contains
   none of `server.js`, `fetch(`, `WebSocket`, `node:http`, `node:https`,
   `node:net`, `node:child_process`, `setTimeout`, or `setInterval`. The
   Test-author file's complete module-specifier set is exactly `node:assert`,
   `node:child_process`, `node:fs`, `node:os`, `node:path`, and `node:test`.
9. Git history contains the complete Test-author commit before the Builder
   commit. Apart from role-owned ticket-log appends, the Test-author commit
   creates only `app/tests/event-id-timestamp-count.test.js` and the Builder
   commit creates only `app/tools/event-id-timestamp-count.js`. Existing app
   and test files, server, package, lockfiles, dependencies, persisted schema,
   `factory/KIT_PIN`, and Factory controls remain unchanged. After
   implementation, `node --test app/tests/event-id-timestamp-count.test.js`,
   `npm test --prefix app`, `.github/scripts/test-immutability-check.sh`,
   `scripts/repo-check`, and `scripts/secret-scan` each exit `0`.

## Planning ambiguity log

- **Filter validity and precedence:** The reconciled ticket requires both
  filters to be non-empty and invalid arguments to fail before file access.
  Wrong arity or either exact empty string is therefore a usage error with
  exit `2`; any non-empty string, including whitespace, leading zeros,
  non-ASCII text, or a non-date timestamp, proceeds unchanged.
- **Exact matching:** Neither `SPEC.md` nor the draft quantifies timezone
  equivalence, case, whitespace, or Unicode composition. JavaScript strict
  string equality is frozen for both fields. Direct literals cover equivalent
  instants, case, leading spaces, leading zeros, composed/decomposed Unicode,
  and a non-date string; no derived invalid fixture is used.
- **Conjunction and zero results:** Both predicates must be true on the same
  event, and no match succeeds with integer zero. This follows the count and
  conjunction convention in
  [`T-238`](../../factory/tickets/T-238.md#structurally-valid-state-exact-conjunction-and-successful-output).
- **State-validation depth:** The timestamp projection command treats `type`
  as opaque, but Relay's persisted model and general event-count validator
  require non-empty string `id`, `type`, and `receivedAt`. This contract uses
  that full shape and unique IDs, matching `app/server.js`, `event-count`, and
  T-238; payloads, extra fields, and sibling elements remain opaque.
- **Cross-ticket file boundaries:** A repository-wide ticket grep found only
  T-245 declaring either owned path. The concurrently leased
  `origin/ticket/T-243` and `origin/ticket/T-244` projections and branch tips
  own distinct outbox and job file pairs. No overlap is visible within the
  bounded-concurrency ceiling.
- **Protected source boundaries:** The two declared protected seams inspect
  only their corresponding existing tools. No protected test enumerates the
  tool directory, snapshots a T-245 file, or restricts its module specifier.
  `Protected-Test-Conflicts: none` is valid.
- **Global protected text:** The pinned readiness checker returned `GLOBAL
  TEXT PASS` separately for `event-id-timestamp-count`, its command path, the
  exact usage line, all three errors, `eventsWithIdAndTimestamp`, and both
  exact success JSON lines. No fixture-seam expansion is required.
- **Fixture determinism and lifecycle:** Every expectation is a direct
  literal. There is no generated ID, sequence, counter, clock, database,
  parent row, dependent table, foreign key, or cascade. Every case owns a
  fresh temporary parent removed recursively in `finally`; repetition needs
  no reset because the command is read-only.
- **Topology and sends:** This local file command has no HTTP, UI, auth,
  cookie, CORS, cross-origin, preview-deploy, server-start, or external-send
  path, so no deployment topology or `external` label is added.
- **Prior rulings:** [`factory/rulings.md`](../rulings.md) contains no ruling
  applicable to this local count; no ruling entry or escalation is needed.

## Frozen contract — version 1

- **Freeze result:** PASS. Contract version 1 is frozen.

This is the exact interface for the Test-author and Builder. If it proves
wrong, return T-245 to Ready and append a higher-numbered frozen contract;
version 1 must not be edited or removed.

### Command-line interface and failure precedence

- Repository-root invocation is exactly
  `node app/tools/event-id-timestamp-count.js <state-file> <event-id> <timestamp>`.
- Exactly three user-supplied positional arguments are required, and both
  filters must have JavaScript string length greater than zero. Wrong arity or
  an empty filter exits `2`, writes zero stdout bytes, writes exactly this
  single LF-terminated line to stderr, and does not read or create the path:

  ```text
  usage: node app/tools/event-id-timestamp-count.js <state-file> <event-id> <timestamp>
  ```

- Filter validation checks only exact string length. Arguments are already
  strings and are not trimmed, parsed, coerced, decoded, case-folded,
  Unicode-normalized, reformatted, or timezone-converted.
- The command then synchronously reads `<state-file>` as UTF-8. Any
  `readFileSync` failure exits `1`, writes zero stdout bytes, and writes:

  ```text
  event-id-timestamp-count: cannot read state file
  ```

- Bytes rejected by `JSON.parse` exit `1`, write zero stdout bytes, and write:

  ```text
  event-id-timestamp-count: state file is not valid JSON
  ```

- A parsed value failing the frozen structure exits `1`, writes zero stdout
  bytes, and writes:

  ```text
  event-id-timestamp-count: invalid Relay state
  ```

- Precedence is argument count, non-empty event ID, non-empty timestamp, file
  read, JSON parse, complete structural validation, then counting.
- Every error is exactly one LF-terminated line. It includes no path, filter,
  OS or parser detail, input byte, stack trace, stored field, payload, sibling
  value, or second line.
- Absolute temporary paths are used. Symlinks and portable permission-denial
  simulation are outside the fixture matrix; if `readFileSync` throws, the
  frozen read error applies. The directory case freezes the envelope, not an
  OS-specific errno or error class.

### Structurally valid state, conjunction, and successful output

A valid state is a parsed non-null, non-array JSON object with own `events`,
`jobs`, `approvals`, and `outbox` array properties. Additional top-level
properties are allowed and ignored.

Every event must be a non-null, non-array object with non-empty string own
properties `id`, `type`, and `receivedAt`. Event IDs are pairwise distinct
across the complete array. `payload` and extra event fields are opaque. Job,
approval, and outbox elements are wholly opaque; only their collections being
arrays is validated. `receivedAt` values need not be distinct or parseable.

The complete event array and ID uniqueness are validated before counting. An
event contributes one only when both `event.id === <event-id>` and
`event.receivedAt === <timestamp>` by JavaScript strict string equality on the
same event. No trimming, parsing, coercion, decoding, case folding, Unicode
normalization, reformatting, or timezone conversion occurs. No match succeeds
with count zero. Unique IDs bound every valid result to `0` or `1`.

Success exits `0`, writes zero stderr bytes, and writes one compact JSON object
plus exactly one LF, with no other key:

```json
{"eventsWithIdAndTimestamp":<integer 0 or 1>}
```

No filter, stored event field, payload, extra event field, sibling collection,
or top-level metadata value is emitted.

### Frozen Fixture A — exact conjunction variants and opaque content

`app/tests/event-id-timestamp-count.test.js` writes this exact parsed value to
an isolated temporary `state.json` using
`JSON.stringify(value, null, 2) + "\n"`:

```json
{
  "events": [
    {
      "id": "event-alpha",
      "type": "meeting",
      "payload": { "private": "SELECTED-PAYLOAD-MUST-NOT-LEAK" },
      "receivedAt": "2026-08-25T12:00:00.000Z"
    },
    {
      "id": "event-bravo",
      "type": "email",
      "payload": "OPAQUE-EVENT-PAYLOAD-MUST-NOT-LEAK",
      "extra": { "private": "OPAQUE-EVENT-EXTRA-MUST-NOT-LEAK" },
      "receivedAt": "2026-08-25T12:00:01.000Z"
    },
    {
      "id": "Event-alpha",
      "type": "meeting",
      "receivedAt": "2026-08-25T07:00:00-05:00"
    },
    {
      "id": " event-alpha",
      "type": "meeting",
      "receivedAt": " 2026-08-25T12:00:00.000Z"
    },
    {
      "id": "007",
      "type": "meeting",
      "receivedAt": "7"
    },
    {
      "id": "event-café",
      "type": "meeting",
      "receivedAt": "instant-café"
    },
    {
      "id": "event-verbatim",
      "type": "meeting",
      "payload": false,
      "receivedAt": "VERBATIM-TIMESTAMP-MUST-NOT-BE-NORMALIZED"
    }
  ],
  "jobs": [{ "lastError": "JOB-ERROR-MUST-NOT-LEAK" }],
  "approvals": [
    {
      "action": {
        "to": "APPROVAL-TO-MUST-NOT-LEAK",
        "subject": "APPROVAL-SUBJECT-MUST-NOT-LEAK",
        "body": "APPROVAL-BODY-MUST-NOT-LEAK"
      },
      "reason": "APPROVAL-REASON-MUST-NOT-LEAK"
    }
  ],
  "outbox": [
    {
      "to": "OUTBOX-TO-MUST-NOT-LEAK",
      "subject": "OUTBOX-SUBJECT-MUST-NOT-LEAK",
      "body": "OUTBOX-BODY-MUST-NOT-LEAK"
    }
  ],
  "metadata": { "note": "TOP-LEVEL-METADATA-MUST-NOT-LEAK" }
}
```

The exact query results are:

| Event-ID filter | Timestamp filter | Exact stdout |
|---|---|---|
| `event-alpha` | `2026-08-25T12:00:00.000Z` | `{"eventsWithIdAndTimestamp":1}\n` |
| `event-bravo` | `2026-08-25T12:00:01.000Z` | `{"eventsWithIdAndTimestamp":1}\n` |
| `Event-alpha` | `2026-08-25T07:00:00-05:00` | `{"eventsWithIdAndTimestamp":1}\n` |
| ` event-alpha` | ` 2026-08-25T12:00:00.000Z` | `{"eventsWithIdAndTimestamp":1}\n` |
| `007` | `7` | `{"eventsWithIdAndTimestamp":1}\n` |
| `event-café` | `instant-café` | `{"eventsWithIdAndTimestamp":1}\n` |
| `event-verbatim` | `VERBATIM-TIMESTAMP-MUST-NOT-BE-NORMALIZED` | `{"eventsWithIdAndTimestamp":1}\n` |
| `event-alpha` | `2026-08-25T12:00:01.000Z` | `{"eventsWithIdAndTimestamp":0}\n` |
| `Event-alpha` | `2026-08-25T12:00:00.000Z` | `{"eventsWithIdAndTimestamp":0}\n` |
| `event-alpha` | `2026-08-25T07:00:00-05:00` | `{"eventsWithIdAndTimestamp":0}\n` |
| ` event-alpha` | `2026-08-25T12:00:00.000Z` | `{"eventsWithIdAndTimestamp":0}\n` |
| `event-alpha` | ` 2026-08-25T12:00:00.000Z` | `{"eventsWithIdAndTimestamp":0}\n` |
| `7` | `7` | `{"eventsWithIdAndTimestamp":0}\n` |
| `event-café` | `instant-café` | `{"eventsWithIdAndTimestamp":0}\n` |
| `event-café` | `instant-café` | `{"eventsWithIdAndTimestamp":0}\n` |
| `event-verbatim` | `verbatim-TIMESTAMP-MUST-NOT-BE-NORMALIZED` | `{"eventsWithIdAndTimestamp":0}\n` |

The two leading-space filters begin with exactly one U+0020. The canonical
`2026-08-25T12:00:00.000Z` and offset `2026-08-25T07:00:00-05:00` strings
denote the same instant but are direct, byte-distinct literals. The composed
`café` uses U+00E9 and UTF-8 hex `636166c3a9`; decomposed `café` uses U+0065
then U+0301 and hex `63616665cc81`. The relevant IDs and timestamps prepend
the literal ASCII bytes for `event-` and `instant-`.

All IDs, timestamps, and queries are direct literals. Before invocation the
test proves every positive pair matches exactly one event and every zero pair
matches none. It also proves byte-distinctness for the case, leading-space,
leading-zero, timezone-representation, timestamp-case, and Unicode pairs. No
casing, normalization, parsing, timezone, or mutation operation derives an
invalid fixture.

### Frozen Fixtures B-D and filesystem paths

- Fixture B is exactly
  `{"events":[],"jobs":[null],"approvals":[7],"outbox":["OPAQUE-CONTENT-MUST-NOT-LEAK"],"metadata":false}\n`.
  It is valid, proves sibling elements are opaque, and the canonical query
  writes `{"eventsWithIdAndTimestamp":0}\n`.
- Fixture C is exactly
  `{"events":[],"payload":"MALFORMED-PAYLOAD-MUST-NOT-LEAK"\n`; its object
  is unclosed and `JSON.parse` rejects it.
- Fixture D is a zero-byte regular file and `JSON.parse` rejects it.
- The arity/filter candidate is absent absolute path
  `<temporary-parent>/arity-state-MUST-NOT-LEAK.json`.
- Read-failure paths are absent
  `<temporary-parent>/PATH-MUST-NOT-LEAK.json` and existing empty directory
  `<temporary-parent>/DIRECTORY-MUST-NOT-LEAK`.
- Wrong-arity vectors are `[]`, `[candidate]`,
  `[candidate, "event-alpha"]`, and
  `[candidate, "event-alpha", "2026-08-25T12:00:00.000Z", "extra"]`.
  Empty-filter vectors are
  `[candidate, "", "2026-08-25T12:00:00.000Z"]` and
  `[candidate, "event-alpha", ""]`.

### Frozen structural-invalid fixtures

Each value is written directly as the displayed compact UTF-8 JSON plus one
LF. No invalid fixture is derived from another fixture.

| ID | Exact JSON before final LF | Violated rule |
|---|---|---|
| S1 | `null` | root is null |
| S2 | `[]` | root is an array |
| S3 | `{"events":[],"jobs":[],"approvals":[]}` | missing own `outbox` array |
| S4 | `{"events":{},"jobs":[],"approvals":[],"outbox":[]}` | `events` is not an array |
| S5 | `{"events":[],"jobs":{},"approvals":[],"outbox":[]}` | `jobs` is not an array |
| S6 | `{"events":[],"jobs":[],"approvals":{},"outbox":[]}` | `approvals` is not an array |
| S7 | `{"events":[],"jobs":[],"approvals":[],"outbox":{}}` | `outbox` is not an array |
| S8 | `{"events":[null],"jobs":[],"approvals":[],"outbox":[]}` | event is null |
| S9 | `{"events":[[]],"jobs":[],"approvals":[],"outbox":[]}` | event is an array |
| S10 | `{"events":[{"type":"meeting","receivedAt":"2026-08-25T12:00:00.000Z"}],"jobs":[],"approvals":[],"outbox":[]}` | event lacks own `id` |
| S11 | `{"events":[{"id":"","type":"meeting","receivedAt":"2026-08-25T12:00:00.000Z"}],"jobs":[],"approvals":[],"outbox":[]}` | event `id` is empty |
| S12 | `{"events":[{"id":7,"type":"meeting","receivedAt":"2026-08-25T12:00:00.000Z"}],"jobs":[],"approvals":[],"outbox":[]}` | event `id` is not a string |
| S13 | `{"events":[{"id":"event-alpha","receivedAt":"2026-08-25T12:00:00.000Z"}],"jobs":[],"approvals":[],"outbox":[]}` | event lacks own `type` |
| S14 | `{"events":[{"id":"event-alpha","type":"","receivedAt":"2026-08-25T12:00:00.000Z"}],"jobs":[],"approvals":[],"outbox":[]}` | event `type` is empty |
| S15 | `{"events":[{"id":"event-alpha","type":7,"receivedAt":"2026-08-25T12:00:00.000Z"}],"jobs":[],"approvals":[],"outbox":[]}` | event `type` is not a string |
| S16 | `{"events":[{"id":"event-alpha","type":"meeting"}],"jobs":[],"approvals":[],"outbox":[]}` | event lacks own `receivedAt` |
| S17 | `{"events":[{"id":"event-alpha","type":"meeting","receivedAt":""}],"jobs":[],"approvals":[],"outbox":[]}` | event `receivedAt` is empty |
| S18 | `{"events":[{"id":"event-alpha","type":"meeting","receivedAt":null}],"jobs":[],"approvals":[],"outbox":[]}` | event `receivedAt` is not a string |
| S19 | `{"events":[{"id":"event-alpha","type":"meeting","receivedAt":"2026-08-25T12:00:00.000Z"},{"id":"event-alpha","type":"email","receivedAt":"2026-08-25T12:00:01.000Z"}],"jobs":[],"approvals":[],"outbox":[]}` | duplicate IDs after a matching event |
| S20 | `{"events":[{"id":"event-alpha","type":"meeting","receivedAt":"2026-08-25T12:00:00.000Z"},{"id":"event-invalid-second","receivedAt":"2026-08-25T12:00:01.000Z"}],"jobs":[],"approvals":[],"outbox":[]}` | matching event precedes sibling missing `type` |

Before invocation, the test compares the exact serialized bytes of Fixtures
A-D and S1-S20 pairwise; every pair must differ. Fixture D is empty bytes.

### Sensitive-token set, lifecycle, and read-only observations

The complete sensitive-token set checked against both streams after every
child invocation is:

1. `event-alpha`
2. `event-bravo`
3. `Event-alpha`
4. `007`
5. `7`
6. `event-café`
7. `event-café`
8. `event-verbatim`
9. `meeting`
10. `email`
11. `2026-08-25T12:00:00.000Z`
12. `2026-08-25T12:00:01.000Z`
13. `2026-08-25T07:00:00-05:00`
14. `instant-café`
15. `instant-café`
16. `VERBATIM-TIMESTAMP-MUST-NOT-BE-NORMALIZED`
17. `verbatim-TIMESTAMP-MUST-NOT-BE-NORMALIZED`
18. `SELECTED-PAYLOAD-MUST-NOT-LEAK`
19. `OPAQUE-EVENT-PAYLOAD-MUST-NOT-LEAK`
20. `OPAQUE-EVENT-EXTRA-MUST-NOT-LEAK`
21. `JOB-ERROR-MUST-NOT-LEAK`
22. `APPROVAL-TO-MUST-NOT-LEAK`
23. `APPROVAL-SUBJECT-MUST-NOT-LEAK`
24. `APPROVAL-BODY-MUST-NOT-LEAK`
25. `APPROVAL-REASON-MUST-NOT-LEAK`
26. `OUTBOX-TO-MUST-NOT-LEAK`
27. `OUTBOX-SUBJECT-MUST-NOT-LEAK`
28. `OUTBOX-BODY-MUST-NOT-LEAK`
29. `TOP-LEVEL-METADATA-MUST-NOT-LEAK`
30. `OPAQUE-CONTENT-MUST-NOT-LEAK`
31. `MALFORMED-PAYLOAD-MUST-NOT-LEAK`
32. `arity-state-MUST-NOT-LEAK`
33. `PATH-MUST-NOT-LEAK`
34. `DIRECTORY-MUST-NOT-LEAK`

Each runtime case owns a fresh temporary parent, creates only required inputs,
snapshots sorted parent entries and every regular file's exact bytes plus
`fs.statSync(path, { bigint: true }).mode`, `size`, and `mtimeNs`, waits for
each synchronous child, and recursively removes the parent in `finally`. The
directory case snapshots the same three fields and sorted entries. `atime` is
excluded because reading is the behavior under test. Repetition needs no reset.

Tests share no state path, process, port, server, database, table, generated
identifier, sequence, counter, clock, or timestamp. No parent row, sibling
dependent table, foreign key, cascade, or ordered cleanup exists. Every
expected identifier, type, timestamp, and result is a direct literal.

### File locations, protected boundaries, and topology

- Builder ownership is creation of
  `app/tools/event-id-timestamp-count.js` only. Its complete CommonJS
  module-specifier set is exactly `node:fs`; its only `node:fs` operation is
  `readFileSync`.
- Test-author ownership is creation of
  `app/tests/event-id-timestamp-count.test.js` only. Its complete specifier set
  is exactly `node:assert`, `node:child_process`, `node:fs`, `node:os`,
  `node:path`, and `node:test`.
- `app/tests/event-count.test.js` and `app/tests/event-timestamp.test.js` are
  protected reference-only seams and are not edited. Their source assertions
  read only their corresponding existing tools; no protected test targets a
  new file or enumerates either containing directory.
- `app/package.json` already discovers the new test via
  `node --test tests/*.test.js` and is not edited. No existing app, test,
  schema, manifest, lockfile, dependency, Factory, or documentation file is in
  implementation ownership.
- There are no endpoints, request/response shapes, UI selectors, accessible
  browser names, auth seams, cookies, CORS origins, cross-origin credentials,
  domains, preview deploys, external sends, or server processes. The complete
  topology is one local synchronous child reading one file and writing
  stdout/stderr under a `5_000` ms timeout.

### Criteria coverage audit

| Frozen contract element | Criterion coverage |
|---|---|
| Command, success shape, repetition, redaction, read-only observations | AC1 |
| All positive exact-match variants | AC2 |
| Same-event conjunction, zero matches, exact string semantics, empty events | AC3 |
| Arity, non-empty filters, usage, and pre-read precedence | AC4 |
| Read failures and filesystem preservation | AC5 |
| Malformed and zero-byte JSON | AC6 |
| Complete structure, uniqueness, distinctness, validation-before-counting | AC7 |
| Timeout, termination, module/filesystem boundaries, no network or timers | AC8 |
| Ownership, tests-first history, unchanged surfaces, implementation checks | AC9 |

Every path, output shape, fixture, token, lifecycle rule, file boundary, and
topology statement in version 1 is covered. No endpoint or UI surface exists.

## Coordination

T-245 owns only `app/tests/event-id-timestamp-count.test.js` and
`app/tools/event-id-timestamp-count.js`. T-243 and T-244 own distinct files and
can proceed independently; publication remains subject to bounded concurrency
and the protected-branch workflow.

## Spec-lint — round 1 (2026-08-25)

### Contract-element coverage

| Contract element | Positive | Failure/empty | Duplicate | Out of scope |
|---|---|---|---|---|
| Arity and non-empty-filter validation before file access | AC4 exit `2` + exact usage line | AC4 empty event ID and empty timestamp vectors | n/a | Trimming/coercion of filters excluded (contract) |
| Synchronous file read | AC1 read of Fixture A | AC5 absent path and directory, exact read error | n/a | Symlinks, permission-denial simulation (contract) |
| JSON parse | AC1-AC3 parsed fixtures | AC6 malformed Fixture C and zero-byte Fixture D | n/a | Parser detail in errors excluded (redacted envelope) |
| Structural validation (root + four collections + event shape) | AC1-AC3 valid Fixtures A and B | AC7 S1-S18 each violated rule | n/a | Job/approval/outbox element shape opaque (AC3, Fixture B) |
| Event-ID uniqueness before counting | AC1 bounded count `1` | AC7 S19 duplicate after a match | S19 exercises the duplicate itself | n/a |
| Validation-before-counting precedence | — | AC7 S19/S20 match precedes invalid sibling | n/a | n/a |
| Strict-string conjunction on one event | AC2 six pairs + AC1 (case, offset, space, zeros, Unicode, non-date) | AC3 nine single-predicate/byte-variant misses + empty-events Fixture B | Composed/decomposed pairs grouped as byte-variants | Timestamp parsing/normalization excluded (contract) |
| Success output shape (single key, compact, one LF) | AC1-AC3 exact stdout bytes | AC4-AC7 zero stdout bytes | n/a | No other key permitted (contract) |
| Redaction / sensitive-token set (34 tokens) | AC8 across every AC1-AC7 invocation | Error lines carry no path, filter, or stored value | Composed and decomposed token variants both listed | n/a |
| Read-only durable state | AC1 bytes + BigInt `mode`/`size`/`mtimeNs` + parent entries, all fixtures | AC4 candidate stays absent; AC5 directory preserved | AC1 repeated invocation | `atime` excluded (reading is the behavior) |
| Static source boundaries and timeout | AC8 exact module/`node:fs`-operation sets, forbidden-token absence, `5_000` ms, `error === undefined`, `signal === null` | n/a | n/a | Network, timers, server, child processes forbidden |
| Ownership, tests-first history, unchanged surfaces, gates | AC9 single-file commits in order, five checks exit `0` | n/a | n/a | Baseline CI controls stay outside the contract (per 2026-08-10 ruling's pattern) |

Every frozen path, error envelope, fixture, token, lifecycle rule, and file
boundary maps to at least one criterion; no material behavior is uncovered
and every criterion is implementable against the contract as written.

### Findings

1. The ambiguity-log link
   `T-238.md#structurally-valid-state-exact-conjunction-and-successful-output`
   does not resolve: the T-238 copy on this branch is a 39-line stub whose
   only headings are Description, Acceptance criteria, and Coordination; the
   cited section exists only in `origin/ticket/T-238`'s copy. The convention
   it cites is fully restated inside this ticket's own frozen contract, so no
   product decision is delegated. Non-blocking.
   SPEC-WARN: cite the T-238 contract by its branch-qualified location or drop the anchor; the local main-branch stub lacks that heading.
2. The composed and decomposed `event-café`/`instant-café` literals are
   visually identical in AC3, the query table, and the sensitive-token list
   (items 6/7 and 14/15) and are distinguishable only by raw bytes. The
   contract disambiguates with exact UTF-8 hex (`636166c3a9` vs `63616665cc81`)
   and a mandatory pre-invocation byte-distinctness proof, so the spec is
   decidable. Non-blocking.
   SPEC-WARN: Test-author should write the Unicode filters as escape sequences (`café` vs `café`) rather than raw literals, so the byte form survives editor normalization.

Verification notes: the structural rules and error envelopes byte-match the
shipped `app/tools/event-count.js` validator and convention; both declared
`Fixture-Seams` protected tests assert sources of their own tool and their
own `__filename` only, and no protected test enumerates `app/tools/` or
`app/tests/` or snapshots a T-245 path, so `Protected-Test-Conflicts: none`
holds; no global-shell UI literal is added, so no `GLOBAL TEXT PASS` gate is
triggered; `.github/scripts/test-immutability-check.sh` enforces exactly the
AC9 ordering with `factory/` appends exempt; T-243/T-244 branch projections
own distinct outbox and job file pairs; `factory/rulings.md` contains no
ruling this contract contradicts. No prior spec-lint verdict exists on
T-245, so this is round 1 and requires no operator authorization.

SPEC-LINT: PASS
