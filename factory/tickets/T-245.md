# T-245 — count Relay events by ID and timestamp offline

State: Backlog
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-245
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/event-count.test.js, app/tests/event-timestamp.test.js
Authentication-Seams: none
Builder ownership: app/tools/event-id-timestamp-count.js only
Test-author ownership: app/tests/event-id-timestamp-count.test.js only

## Description

Add a zero-dependency, read-only Node command that counts persisted Relay
events whose ID and receipt timestamp exactly match supplied strings. This
supports narrow offline event checks without starting Relay or exposing
payloads.

## Acceptance criteria

1. `node app/tools/event-id-timestamp-count.js <state-file> <event-id> <received-at>`
   emits compact JSON containing exactly one `eventsWithIdAndTimestamp` integer.
2. Event ID and receipt timestamp must be non-empty strings and match stored
   `id` and `receivedAt` values exactly, without trimming, timestamp parsing,
   normalization, or coercion.
3. Invalid arguments are rejected before file access. Unreadable, malformed,
   or structurally invalid state returns fixed redacted errors; complete event
   validation precedes counting.
4. Every invocation is offline, read-only, deterministic, and self-terminating.
5. Tests precede implementation. Targeted, full app, and immutability checks
   pass without dependency, server, schema, manifest, or Factory changes.

## Coordination

This ticket owns only its two new paths and is independent of T-243 and T-244.
