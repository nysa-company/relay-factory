# T-240 — count Relay approvals by status and recipient offline

State: Ready
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-240
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/approval-count.test.js, app/tests/pending-approvals.test.js
Authentication-Seams: none
Builder ownership: app/tools/approval-status-recipient-count.js only
Test-author ownership: app/tests/approval-status-recipient-count.test.js only

## Description

Add a zero-dependency, read-only Node command that counts persisted Relay
approvals whose status and action recipient exactly match supplied strings.
This supports narrow offline approval checks without starting Relay or exposing
approval contents.

## Acceptance criteria

1. `node app/tools/approval-status-recipient-count.js <state-file> <status> <recipient>`
   emits compact JSON containing exactly one
   `approvalsWithStatusAndRecipient` integer.
2. Status must be exactly `pending`, `sent`, `rejected`, or
   `blocked_recipient`; recipient must be a non-empty string. Both filters must
   exactly match the stored approval status and `action.to` value.
3. Invalid arguments are rejected before file access. Unreadable, malformed,
   or structurally invalid state returns fixed redacted errors; complete
   approval validation precedes counting.
4. Every invocation is offline, read-only, deterministic, and self-terminating.
5. Tests precede implementation. Targeted, full app, and immutability checks
   pass without dependency, server, schema, manifest, or Factory changes.

## Coordination

This ticket owns only its two new paths and is independent of T-241 and T-242.
