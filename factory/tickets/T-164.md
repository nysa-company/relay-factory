# T-164 — read one Relay approval

State: Planning
Initiative: I-002
Priority: normal
Risk class: low
External: no
Branch: ticket/T-164
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/server.js, app/tests/conformance.test.js
Authentication-Seams: none
Builder ownership: app/server.js only
Test-author ownership: app/tests/approval-detail.test.js only

## Description

Add `GET /api/approvals/:id` to the zero-dependency Relay API.

## Acceptance criteria

1. An existing approval returns HTTP 200 with exactly its stored approval and
   the related job whose `id` matches `jobId`.
2. An unknown approval returns HTTP 404 with the deterministic JSON error
   `{"error":"no such approval"}`.
3. The route is read-only and does not change durable state.
4. Tests are committed before implementation; no dependencies or Factory
   controls change.

Kit-SHA: 831fe510ad9ee623db8158760b1539f812b9b69f

## Planning log

- 2026-08-06: Contract freeze blocked by the current protected repository
  state. `app/server.js` already implements the ticket's exact
  `GET /api/approvals/:id` success and not-found behavior, and
  `app/tests/approval-detail.test.js` already protects the same endpoint,
  response envelope, deterministic fixture, and read-only guarantee as
  T-168's completed acceptance work. The existing test is explicitly labeled
  for T-168 frozen contract version 1, and both files entered the protected
  base before this T-164 planning branch. No import/export allowlist,
  exact-source assertion, source snapshot, global-shell literal, generated
  value, derived invalid fixture, database lifecycle, authentication, cookie,
  CORS, cross-origin, preview-deploy, or external-send issue applies; the
  blocker is that there is no unimplemented T-164 behavior against which a
  test-author can commit a failing acceptance test before a Builder change.
- Cross-ticket inspection found concurrent `app/server.js` ownership on leased
  `origin/ticket/T-163` and `origin/ticket/T-165`; that overlap is authorized
  by the existing detail-route ruling in
  [`factory/rulings.md`](../rulings.md). Leased T-170, T-171, and T-172 own
  disjoint application and test files. This ruling does not resolve the
  already-delivered T-164/T-168 duplicate.
- **Operator question:** Should T-164 be closed as a duplicate of completed
  T-168, or should T-164 return to Backlog with a new, non-overlapping product
  behavior and fresh Builder/Test-author file boundaries?

ROLE-ESCALATE: CONTRACT-BLOCKED
