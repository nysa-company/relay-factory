# T-186 — inspect one Relay approval state offline

Kit-SHA: b1a5563096b333fb5d537625a6c7cb9b2c61ef95
State: Review
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-186
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/approval-detail.test.js
Authentication-Seams: none
Builder ownership: app/tools/approval-state.js only
Test-author ownership: app/tests/approval-state.test.js only

## Spec'd description

Add one zero-dependency, read-only Node 18+ command,
`node app/tools/approval-state.js <state-file> <approval-id>`, that selects one
persisted Relay approval by an exact, case-sensitive ID while the service is
offline. It emits only the selected approval's `approvalId`, `jobId`, and
`status`. This gives an operator a minimal lifecycle lookup without exposing
the approval action, rejection reason, related event payload or job error,
outbox content, or unrelated persisted state.

Product-doc sources:

- [`SPEC.md` behavior 3](../../SPEC.md#behavior-spec) defines a proposed action,
  its approval record, and its initial `pending` status.
- [`SPEC.md` behaviors 4-5](../../SPEC.md#behavior-spec) define the `sent`,
  `blocked_recipient`, and `rejected` approval outcomes.
- [`SPEC.md` behavior 6](../../SPEC.md#behavior-spec) makes the atomically
  persisted state file Relay's durable source of truth.
- [`SPEC.md` behavior 7](../../SPEC.md#behavior-spec) establishes the persisted
  state collections; this command reads the same state representation without
  starting the service.
- [`SPEC.md` conformance cases 5-7](../../SPEC.md#test-cases-the-conformance-suite)
  require the approval gate, blocked-send record, and crash-recovered state to
  remain observable without duplicate effects.
- [`AGENTS.md` Boundaries](../../AGENTS.md#boundaries) requires `app/` to remain
  zero-dependency and keeps Factory controls outside application work.
  [`AGENTS.md` Verification](../../AGENTS.md#verification) defines the later
  full-app and immutability checks.

The command is a data-minimized projection over an existing regular file. It
does not start Relay, execute or reject an approval, write an outbox record,
change the persisted schema, or modify the server, worker, UI, package
manifest, dependencies, lockfiles, or Factory controls.

## Acceptance criteria

1. Given Frozen Fixture A, invoking
   `node app/tools/approval-state.js <fixture-path> <approval-id>` once for each
   of `appr-pending`, `appr-sent`, `appr-rejected`, and `appr-blocked` exits
   `0`, writes zero stderr bytes, and writes that ID's byte-exact frozen stdout
   line. Each parsed object has exactly the keys `approvalId`, `jobId`, and
   `status` in that order. No frozen sensitive token or unrelated approval ID
   appears in either stream.
2. Given Frozen Fixture A, each frozen unknown ID (`appr-missing`,
   `APPR-PENDING`, and `appr-pending `) exits `1`, writes zero stdout bytes,
   and writes exactly `approval-state: no such approval\n` to stderr. The three
   unknown literals are byte-distinct from every Fixture A approval ID.
3. Invoking the command with zero, one, or three positional arguments exits
   `2`, writes zero stdout bytes, and writes exactly
   `usage: node app/tools/approval-state.js <state-file> <approval-id>\n` to
   stderr. Each case uses a fresh empty temporary parent, and no supplied state
   path is created.
4. For each frozen read-failure path (an absent path and a directory path), the
   command exits `1`, writes zero stdout bytes, and writes exactly
   `approval-state: cannot read state file\n` to stderr. The absent path
   remains absent; the directory remains a directory with unchanged numeric
   mode, nanosecond modification time, and entries.
5. Given Frozen Fixture B, the command exits `1`, writes zero stdout bytes,
   writes exactly
   `approval-state: state file is not valid JSON\n` to stderr, and leaves the
   fixture's bytes, numeric mode, size, nanosecond modification time, and
   sibling directory entries unchanged. Its embedded sensitive token does not
   appear in either stream.
6. For every entry in the Frozen structural-invalid fixture table, the command
   exits `1`, writes zero stdout bytes, writes exactly
   `approval-state: invalid Relay state\n` to stderr, and leaves that fixture's
   bytes, numeric mode, size, nanosecond modification time, and sibling
   directory entries unchanged. Before invocation, Fixtures A, B, and S1-S16
   are pairwise byte-distinct.
7. Every invocation in criteria 1-6 terminates on its own within the test
   harness's `10_000` ms timeout and is not killed by a signal. Every existing
   input and temporary parent's entries are unchanged, and no absent path is
   created. Two invocations for `appr-pending` against unchanged Fixture A
   produce byte-identical stdout and stderr. Static inspection finds exactly
   `node:fs` among the Builder file's CommonJS module specifiers and finds no
   `server.js`, `node:http`, `node:https`, `node:net`,
   `node:child_process`, or `fetch` reference.
8. Git history contains the complete acceptance-test commit before the
   implementation commit. The test-author commit changes only
   `app/tests/approval-state.test.js`; the Builder commit changes only
   `app/tools/approval-state.js`. The targeted command
   `node --test app/tests/approval-state.test.js`, the full command
   `npm test --prefix app`, and
   `.github/scripts/test-immutability-check.sh` each exit `0`, while existing
   tests, `app/server.js`, `app/package.json`, lockfiles, the persisted schema,
   `factory/KIT_PIN`, and Factory controls remain unchanged by those stages.

## Frozen contract — version 1

- **Freeze result:** PASS. Contract version 1 is frozen.

This is the exact interface for the test-author and Builder. If it proves
wrong, return T-186 to Ready and append a higher-numbered frozen contract;
version 1 must not be edited or removed.

### Command-line interface

- Repository-root invocation:
  `node app/tools/approval-state.js <state-file> <approval-id>`.
- Exactly two positional arguments are accepted. The approval ID is used
  verbatim; it is not trimmed, case-folded, decoded, normalized, or prefixed.
- Zero, one, or three positional arguments produce exit code `2`, empty
  stdout, and exactly this single LF-terminated stderr line:

  ```text
  usage: node app/tools/approval-state.js <state-file> <approval-id>
  ```

- The command reads the named regular file directly as UTF-8. It does not
  import, require, spawn, or otherwise execute `app/server.js`; open an HTTP or
  other network listener; make an outbound request; or leave a timer or child
  process running.
- A read failure, including a nonexistent path, a directory in place of a
  regular file, or an OS read denial, produces exit code `1`, empty stdout, and
  exactly:

  ```text
  approval-state: cannot read state file
  ```

- Bytes rejected by `JSON.parse` produce exit code `1`, empty stdout, and
  exactly:

  ```text
  approval-state: state file is not valid JSON
  ```

- A parsed value that fails the frozen structural rules produces exit code
  `1`, empty stdout, and exactly:

  ```text
  approval-state: invalid Relay state
  ```

- A valid state with no approval whose `id` is strictly equal to the supplied
  ID produces exit code `1`, empty stdout, and exactly:

  ```text
  approval-state: no such approval
  ```

- No error includes the input path, approval ID, OS error, parser text, input
  content, stack trace, or a second line. Every displayed line above ends with
  one LF byte.
- Validation precedence is argument count, read, JSON parse, structure, then
  exact-ID lookup. No path is written, renamed, deleted, chmodded, or created.

The arity tests use fresh empty temporary parents. The one-argument case
supplies nonexistent `<tmp>/state-a.json`. The three-argument case supplies
nonexistent `<tmp>/state-a.json`, `appr-pending`, and `extra`. The usage result
rather than the read-failure result proves arity validation precedes file
access.

### Valid state and successful result

For this command, a structurally valid state is a non-null, non-array JSON
object with own `events`, `jobs`, `approvals`, and `outbox` properties whose
values are arrays.

Every `approvals` element must be a non-null, non-array object with:

- non-empty string `id`;
- non-empty string `jobId`; and
- string `status` equal to exactly one of `pending`, `sent`, `rejected`, or
  `blocked_recipient`.

Approval IDs must be pairwise distinct. Contents of `events`, `jobs`, and
`outbox`, additional top-level keys, and additional approval keys are opaque to
structural validation. In particular, `action`, `reason`, and `proposedAt` may
be present but are never copied to output. A referenced job need not exist;
the stored `jobId` is reported without joining another collection.

On an exact-ID match, serialize one new object with keys in this exact order:

```json
{"approvalId":<stored id>,"jobId":<stored jobId>,"status":<stored status>}
```

Use compact `JSON.stringify` formatting and append exactly one LF byte.
Success exits `0` with empty stderr. There are no endpoint paths, HTTP request
or response shapes, UI routes, or UI selectors in this contract.

### Frozen Fixture A — all approval states and sensitive fields

The test writes the following displayed JSON bytes plus one final LF to an
isolated temporary `state.json`; there is no repository fixture file:

```json
{
  "events": [
    {
      "id": "event-private",
      "type": "meeting",
      "payload": {
        "private": "EVENT-PAYLOAD-MUST-NOT-LEAK"
      },
      "receivedAt": "2026-08-23T06:00:00.000Z"
    }
  ],
  "jobs": [
    {
      "id": "job-pending",
      "eventId": "event-private",
      "status": "dead",
      "attempts": 3,
      "lastError": "JOB-ERROR-MUST-NOT-LEAK",
      "retries": 0,
      "attemptsSinceRetry": 3
    }
  ],
  "approvals": [
    {
      "id": "appr-pending",
      "jobId": "job-pending",
      "action": {
        "to": "PENDING-TO-MUST-NOT-LEAK",
        "subject": "PENDING-SUBJECT-MUST-NOT-LEAK",
        "body": "PENDING-BODY-MUST-NOT-LEAK"
      },
      "status": "pending",
      "proposedAt": "2026-08-23T06:00:01.000Z"
    },
    {
      "id": "appr-sent",
      "jobId": "job-sent",
      "action": {
        "to": "SENT-TO-MUST-NOT-LEAK",
        "subject": "SENT-SUBJECT-MUST-NOT-LEAK",
        "body": "SENT-BODY-MUST-NOT-LEAK"
      },
      "status": "sent",
      "proposedAt": "2026-08-23T06:00:02.000Z"
    },
    {
      "id": "appr-rejected",
      "jobId": "job-rejected",
      "action": {
        "to": "REJECTED-TO-MUST-NOT-LEAK",
        "subject": "REJECTED-SUBJECT-MUST-NOT-LEAK",
        "body": "REJECTED-BODY-MUST-NOT-LEAK"
      },
      "status": "rejected",
      "proposedAt": "2026-08-23T06:00:03.000Z",
      "reason": "REJECTION-REASON-MUST-NOT-LEAK"
    },
    {
      "id": "appr-blocked",
      "jobId": "job-blocked",
      "action": {
        "to": "BLOCKED-TO-MUST-NOT-LEAK",
        "subject": "BLOCKED-SUBJECT-MUST-NOT-LEAK",
        "body": "BLOCKED-BODY-MUST-NOT-LEAK"
      },
      "status": "blocked_recipient",
      "proposedAt": "2026-08-23T06:00:04.000Z"
    }
  ],
  "outbox": [
    {
      "approvalId": "appr-sent",
      "body": "OUTBOX-CONTENT-MUST-NOT-LEAK",
      "sentAt": "2026-08-23T06:00:05.000Z"
    }
  ],
  "metadata": {
    "note": "STATE-CONTENT-MUST-NOT-LEAK"
  }
}
```

The four exact `<approval-id>` to stdout mappings, each followed by one final
LF, are:

| Approval ID | Exact compact stdout JSON value |
|---|---|
| `appr-pending` | `{"approvalId":"appr-pending","jobId":"job-pending","status":"pending"}` |
| `appr-sent` | `{"approvalId":"appr-sent","jobId":"job-sent","status":"sent"}` |
| `appr-rejected` | `{"approvalId":"appr-rejected","jobId":"job-rejected","status":"rejected"}` |
| `appr-blocked` | `{"approvalId":"appr-blocked","jobId":"job-blocked","status":"blocked_recipient"}` |

The sensitive-token set is every Fixture A string ending
`-MUST-NOT-LEAK`. For each successful lookup, every other approval ID is an
unrelated ID and must be absent from both streams.

The frozen unknown-ID literals are `appr-missing`, `APPR-PENDING`, and
`appr-pending ` (with one trailing U+0020 space). They are literal child-process
arguments, not values derived at runtime, and are pairwise byte-distinct from
each other and from every Fixture A approval ID.

### Frozen Fixture B — malformed JSON

Fixture B's exact UTF-8 bytes are the following line with no closing quote or
brace:

```text
{"events":[],"secret":"MALFORMED-STATE-CONTENT-MUST-NOT-LEAK
```

followed by one LF.

### Frozen read-failure paths

Each read-failure case uses a fresh temporary parent:

- `<tmp>/missing-state.json` does not exist before invocation.
- `<tmp>/state-directory` exists as an empty directory, not a regular file.

### Frozen structural-invalid fixtures

Each table value is written directly as the displayed compact UTF-8 JSON plus
one LF. No invalid fixture is derived at runtime by casing, normalization, or
mutation.

| ID | Exact JSON value | Violated rule |
|---|---|---|
| S1 | `null` | root is null |
| S2 | `[]` | root is an array |
| S3 | `{"events":[],"jobs":[],"approvals":[]}` | missing `outbox` array |
| S4 | `{"events":{},"jobs":[],"approvals":[],"outbox":[]}` | `events` is not an array |
| S5 | `{"events":[],"jobs":{},"approvals":[],"outbox":[]}` | `jobs` is not an array |
| S6 | `{"events":[],"jobs":[],"approvals":{},"outbox":[]}` | `approvals` is not an array |
| S7 | `{"events":[],"jobs":[],"approvals":[],"outbox":{}}` | `outbox` is not an array |
| S8 | `{"events":[],"jobs":[],"approvals":[null],"outbox":[]}` | approval is null |
| S9 | `{"events":[],"jobs":[],"approvals":[[]],"outbox":[]}` | approval is an array |
| S10 | `{"events":[],"jobs":[],"approvals":[{"id":7,"jobId":"job-a","status":"pending"}],"outbox":[]}` | `id` is not a string |
| S11 | `{"events":[],"jobs":[],"approvals":[{"id":"","jobId":"job-a","status":"pending"}],"outbox":[]}` | `id` is empty |
| S12 | `{"events":[],"jobs":[],"approvals":[{"id":"appr-a","jobId":7,"status":"pending"}],"outbox":[]}` | `jobId` is not a string |
| S13 | `{"events":[],"jobs":[],"approvals":[{"id":"appr-a","jobId":"","status":"pending"}],"outbox":[]}` | `jobId` is empty |
| S14 | `{"events":[],"jobs":[],"approvals":[{"id":"appr-a","jobId":"job-a","status":null}],"outbox":[]}` | `status` is not a string |
| S15 | `{"events":[],"jobs":[],"approvals":[{"id":"appr-a","jobId":"job-a","status":"PENDING"}],"outbox":[]}` | status is outside the exact enum |
| S16 | `{"events":[],"jobs":[],"approvals":[{"id":"appr-a","jobId":"job-a","status":"pending"},{"id":"appr-a","jobId":"job-b","status":"sent"}],"outbox":[]}` | approval IDs are duplicated |

Before invoking the command, the acceptance test compares the exact bytes of
Fixtures A, B, and S1-S16 pairwise and requires every pair to differ.

### File locations, lifecycle, protected boundaries, and topology

- Builder implementation and ownership:
  `app/tools/approval-state.js` only. It is a new standalone CommonJS command;
  its only module specifier is `node:fs`.
- Test-author implementation and ownership:
  `app/tests/approval-state.test.js` only. Its allowed module specifiers are
  exactly `node:test`, `node:assert`, `node:child_process`, `node:fs`,
  `node:path`, and `node:os`.
- Existing protected seam:
  `app/tests/approval-detail.test.js` is reference-only and is not edited. It
  asserts the server's stored approval/job detail envelope and contains no
  import/export allowlist, exact-source assertion, source snapshot, or
  module-specifier restriction for the new Builder file.
- The tracked exact-source assertions in
  `app/tests/dead-jobs.test.js` and
  `app/tests/blocked-approvals.test.js` inspect only their own tool paths, not
  `approval-state.js`.
- Every runtime case owns a fresh temporary directory, writes only its frozen
  fixture, waits for the child command to terminate, and recursively removes
  that directory in teardown. Tests share no port, process, state file,
  database, table, counter, or generated value. There are no foreign keys or
  dependent-table cleanup obligations.
- Every child invocation uses `spawnSync` with `timeout: 10_000`; the test
  requires `result.error` to be `undefined` and `result.signal` to be `null`.
- For every existing fixture, the test fingerprint is its exact bytes,
  `fs.statSync(path, { bigint: true }).mode`, `.size`, and `.mtimeNs`; all must
  compare equal before and after invocation. Directory read-failure fixtures
  additionally retain their entry list.
- Fixture timestamps and IDs are literals. No generated identifier, sequence,
  counter, clock, initializer, or reset participates in an exact expectation.
- Targeted command:
  `node --test app/tests/approval-state.test.js`.
- Required repository commands after implementation:
  `npm test --prefix app` and
  `.github/scripts/test-immutability-check.sh`.
- The test-author stage precedes the Builder stage. Neither stage edits
  existing tests, `app/server.js`, `app/package.json`, lockfiles,
  `factory/KIT_PIN`, ticket state, the ledger, or Factory controls.
- This offline command has no cross-origin, authentication, cookie, CORS,
  domain, credential, preview-deploy, endpoint, UI, or selector topology. It
  cannot trigger an external send, so `External: no` remains correct.

## Criteria checklist

- **Pass/fail and unambiguous:** AC1-7 freeze process exit codes, exact
  stdout/stderr bytes, IDs, projection keys and order, fixture bytes and stat
  invariance, timeout/signal outcomes, module specifiers, and forbidden source
  references. AC8 is decided from Git history, per-stage diffs, and three exact
  command exit statuses.
- **Coverage:** AC1 covers all four approval statuses, the exact success shape,
  opaque sensitive fields, and Fixture A. AC2 covers exact case/space-sensitive
  lookup and the not-found envelope. AC3 covers argument arity and validation
  precedence. AC4 covers both read failures. AC5 covers malformed JSON and
  Fixture B. AC6 covers every structural rule through S1-S16 and pairwise
  fixture distinctness. AC7 covers every invocation's termination,
  determinism, read-only behavior, and offline source boundary. AC8 covers all
  file ownership and verification boundaries.
- **Derived-fixture distinctness:** every serialized fixture and unknown-ID
  argument is an exact literal, not a runtime mutation. AC2 requires the
  unknown IDs to differ from valid IDs, and AC6 requires A, B, and S1-S16 to be
  pairwise byte-distinct before use.
- **Generated-value determinism:** no generated value is asserted; all IDs and
  timestamps are frozen literals.
- **Fixture lifecycle closure:** each case has an isolated temporary parent,
  waits for its child, and performs recursive teardown. There is no database,
  parent/child row, non-cascading foreign key, or serialized shared resource.
- **Protected boundaries:** the Builder adds a new process-invoked file. The
  associated protected seam and all tracked tests contain no allowlist or
  snapshot that rejects it or its sole `node:fs` specifier. No protected-test
  conflict declaration or Test-author seam expansion is required.
- **Global protected text:** the pinned readiness check returned
  `GLOBAL TEXT PASS` separately for the usage line, all four error lines, and
  all four Fixture A expected output lines. There is no accessible UI name.

## Ambiguity log

1. **Exact error protocol.** The seed required fixed privacy-safe errors but
   did not freeze text, exit codes, or validation precedence. No entry in
   [`factory/rulings.md`](../rulings.md) addresses offline commands. Resolved
   as exact path-independent usage, read, parse, structure, and lookup
   outcomes, following the shipped offline-command convention.
2. **Meaning of structurally valid.** The product docs establish the four
   persisted collections and approval outcomes but do not define a general
   state-file validator. Resolved narrowly: validate every collection as an
   array and only the approval fields this command matches or emits; treat
   unrelated contents and additive fields as opaque.
3. **Duplicate approval IDs.** Exact-ID selection is ambiguous if two
   approvals share an ID. Relay derives approval IDs from idempotent event IDs
   in [`app/server.js`](../../app/server.js), so persisted duplicates are
   unreachable. The command rejects such state, and S16 makes that result
   observable instead of leaving array-order behavior to the Builder.
4. **Output naming and byte determinism.** The seed named `approvalId`,
   `jobId`, and `status` but did not specify key order, whitespace, or line
   ending. Resolved as the frozen key order, compact `JSON.stringify`, and one
   final LF, with byte-exact expectations for all four statuses.
5. **Exact lookup semantics.** “Exact ID” did not state whether case folding,
   trimming, or normalization was forbidden. Resolved as JavaScript strict
   string equality over the literal argument; the uppercase and trailing-space
   unknown IDs distinguish those alternatives.
6. **Sensitive-field proof.** The seed names private field categories but no
   sentinels. Fixture A gives each category a `-MUST-NOT-LEAK` literal, and
   Fixture B embeds malformed-state content; exact stream checks make leakage
   testable.
7. **Unreadable input portability.** Permission-denial fixtures are unreliable
   under privileged users. All OS read failures share one frozen result; an
   absent path and a directory path are the two portable representatives.
8. **Cross-ticket file ownership.** A grep of every Markdown ticket found no
   other declaration of `approval-state.js` or
   `approval-state.test.js`. T-180 and T-183 describe related approval
   inspectors but own distinct paths. The concurrently leased T-184 and T-185
   branches were inspected from `origin/ticket/T-184` and
   `origin/ticket/T-185`; they own distinct event and job-progress paths.
9. **Protected source and global text.** Inspection of the associated
   approval-detail seam and all tracked tests found no source boundary
   affecting the new file. The pinned readiness script accepted the ticket and
   every new shell-visible literal, so no `Protected-Test-Conflicts`
   declaration or fixture-seam edit is required.
10. **Topology, lifecycle, generated values, and external effects.** This is a
    local file-to-stdout command with isolated temporary fixtures and no
    database or network path. Auth, cookies, CORS, deployment domains,
    generated values, dependent rows, UI selectors, and external sends do not
    apply.

## Coordination

T-186 owns only `app/tools/approval-state.js` and
`app/tests/approval-state.test.js`. T-180's broader approval inspector,
T-183's approval/job-link inspector, and concurrently leased T-184/T-185 use
distinct paths, so there is no implementation-surface conflict.

## Planning log

- 2026-08-23: Planner froze contract version 1 with byte-exact results for all
  four approval states, exact case/space-sensitive lookup, fixed redacted
  errors, literal malformed and structural-invalid fixtures, duplicate-ID
  rejection, pairwise fixture distinctness, read-only lifecycle checks,
  protected source boundaries, and test-first file ownership. Product docs,
  rulings, the current state model, all protected tests, sibling ticket
  declarations, active leased branches, and every new shell-visible literal
  were checked; no operator ruling or escalation was required.
- 2026-08-23: spec-lint round 1 of frozen contract version 1 against `SPEC.md`
  (behavior spec 3-7, conformance cases 5-7), `AGENTS.md` (Boundaries,
  Verification), `app/server.js`, `app/package.json`, every tracked file in
  `app/tests/`, the shipped offline commands in `app/tools/`,
  `.github/scripts/test-immutability-check.sh`, `factory/rulings.md`,
  `factory/PROJECT.env`, sibling ticket declarations
  (T-177, T-179, T-180, T-183) and the concurrently leased `ticket/T-184` and
  `ticket/T-185` branches.

  Contract-element coverage:

  | Contract element (material behavior) | Positive | Failure / empty | Duplicate | Out of scope |
  |---|---|---|---|---|
  | Argument arity — exactly two positional | AC1, AC2, AC4, AC5, AC6 invoke with two arguments | AC3: zero, one, and three args → exit `2`, empty stdout, exact usage line, empty temporary parent afterwards | the three arity cases share one frozen usage literal | four-or-more args untested (finding 5) |
  | Verbatim ID use — no trim, case-fold, decode, normalize, or prefix | AC1 matches all four exact IDs | AC2: `APPR-PENDING` and `appr-pending ` → exit `1`, exact `no such approval` | the three unknown literals share one frozen error line | percent-decoding and Unicode-normalization variants grouped as equivalent non-matching permutations |
  | Read of the named regular file as UTF-8 | AC1, AC2, AC5, AC6 read frozen fixtures | AC4: absent path and directory path → exit `1`, exact `cannot read state file` | both read failures share one frozen literal | OS read-denial grouped as an equivalent read failure (ambiguity log 7); invalid UTF-8 bytes resolve to the parse failure by contract |
  | `JSON.parse` rejection | — | AC5: Fixture B unterminated string → exit `1`, exact `state file is not valid JSON` | — | parse-before-structure precedence is vacuous: unparsable bytes have no structure |
  | Structural rules on the four top-level collections | AC1 accepts Fixture A | AC6: S1, S2 (null and array root), S3 (missing `outbox`), S4-S7 (each collection non-array) | S4-S7 are one rule applied per collection | missing `events`, `jobs`, `approvals` and scalar-root permutations (finding 6) |
  | Structural rules on approval elements and the exact status enum | AC1 accepts all four enum values | AC6: S8, S9 (null and array element), S10-S13 (non-string and empty `id`, `jobId`), S14, S15 (non-string and out-of-enum `status`) | S10-S13 are the same rule over two fields | additive approval keys are opaque by contract |
  | Approval IDs pairwise distinct | AC1: Fixture A holds four distinct IDs | AC6: S16 duplicate `appr-a` → exit `1`, exact `invalid Relay state` | S16 makes array-order behavior unobservable rather than a builder choice | duplicates declared unreachable in real state (`appr-` + `eventId`, `server.js:69`) |
  | Exact-ID lookup miss | — | AC2: three unknown literals → exit `1`, empty stdout, exact `no such approval` | one frozen literal for all three | valid state with an empty `approvals` array (finding 8) |
  | Projection shape — exactly `approvalId`, `jobId`, `status`, in that order | AC1 byte-exact stdout plus a parsed key-order assertion | — | Fixture A `action`, `reason`, `proposedAt`, `outbox`, and extra top-level `metadata` must not be copied | — |
  | `jobId` reported without joining `jobs` | AC1: `appr-sent`, `appr-rejected`, `appr-blocked` reference jobs absent from Fixture A `jobs` | — | — | — |
  | Compact serialization plus exactly one trailing LF, exit `0`, empty stderr | AC1 byte-exact stdout and zero stderr bytes for all four IDs | — | — | — |
  | Error-stream discipline — one LF-terminated line, no path, ID, OS text, parser text, input content, stack, or second line | — | AC2-AC6 assert exact stderr bytes and zero stdout bytes on every error path | the four error literals plus the usage literal are pairwise distinguished by AC2-AC6 | — |
  | Validation precedence: arity, read, parse, structure, lookup | — | AC3 supplies nonexistent paths so exit `2` proves arity beats read; AC4 vs AC5 proves read beats parse; AC6's exact literal for S16 proves structure beats lookup for any supplied ID | — | the supplied ID in AC4-AC6 is not frozen (finding 7) |
  | Redaction of recipients, subjects, bodies, rejection reasons, job errors, event payload, outbox, metadata | AC1: no `-MUST-NOT-LEAK` token and no unrelated approval ID in either stream | AC5: Fixture B's embedded token absent from both streams | each sensitive category carries its own sentinel | — |
  | Read-only, offline execution — no listener, no Relay start, no outbound request, no path written or created | AC7 across every invocation in AC1-AC6 | AC3 keeps the temporary parent empty; AC4 keeps the absent path absent and the directory intact with unchanged mode, `mtimeNs`, and entries; AC5, AC6 pin bytes, mode, size, and `mtimeNs` | AC7 requires two `appr-pending` runs against unchanged Fixture A to be byte-identical | — |
  | Source boundary — sole `node:fs` specifier, no `server.js`, `node:http`, `node:https`, `node:net`, `node:child_process`, or `fetch` reference | AC7 static inspection of the Builder file | — | — | — |
  | Self-termination, no leftover timer or child process | AC7: every invocation terminates within `10_000` ms and is not killed by a signal | exit codes in AC2-AC6 are observed from terminated children | — | — |
  | Fixture lifecycle and isolation | fresh temporary parent per case, only its frozen fixture written, child awaited, recursive teardown; AC6 requires A, B, and S1-S16 pairwise byte-distinct before use | — | — | no database, dependent table, non-cascading foreign key, or shared serialized resource exists in this product |
  | Stage file ownership, commit order, verification commands | AC8: test-author commit touches only `app/tests/approval-state.test.js`, Builder commit only `app/tools/approval-state.js` | AC8 requires the targeted command, `npm test --prefix app`, and `.github/scripts/test-immutability-check.sh` to exit `0` with existing tests, `app/server.js`, `app/package.json`, lockfiles, the persisted schema, `factory/KIT_PIN`, and Factory controls unchanged | — | repository baseline checks stay protected-CI controls per the 2026-08-10 T-178 ruling |

  Findings:
  1. Criteria quality — AC1-AC7 fix process exit codes, byte-exact stdout and
     stderr, projection key set and order, fixture bytes with numeric mode,
     size and `mtimeNs`, sibling entries, termination and signal outcome,
     module specifiers, and forbidden source references; AC8 is decided from
     Git history, per-stage diffs, and three exact exit statuses. No criterion
     requires the builder to interpret a qualitative term. No blocking finding.
  2. Contract coverage — every material behavior in the table above has at
     least one covering criterion, and each criterion is implementable against
     the contract as written with `node:fs` alone plus `process.stdout`,
     `process.stderr`, and an exit code. I recomputed all four Fixture A
     projections from the frozen rules (exact-ID match, fixed key order,
     compact `JSON.stringify`, one trailing LF) and each is byte-identical to
     its frozen stdout row, including `appr-blocked` → `blocked_recipient`.
     Each of S1-S16 classifies structurally invalid for exactly the rule the
     table names, and Fixture B fails `JSON.parse` at its unterminated string.
     No blocking finding.
  3. Consistency — the contract's state shape matches `app/server.js`
     (`{events, jobs, approvals, outbox}` persisted by `persist()` at
     `server.js:28-33`), the approval record shape (`id`, `jobId`, `action`,
     `status`, `proposedAt`, optional `reason` at `server.js:68-78, 110`), and
     the four-value status enum at `server.js:76`, where `blocked_recipient` is
     set only by the allowlist refusal at `server.js:94` — matching `SPEC.md`
     behavior 4 and conformance case 6. Every linked `SPEC.md` and `AGENTS.md`
     anchor and relative path resolves, and the cited behaviors 3, 4-5, 6, 7
     and conformance cases 5-7 say what the description attributes to them.
     Neither recorded ruling in `factory/rulings.md` is contradicted: the
     detail-route envelope ruling concerns HTTP routes and `app/server.js`
     ownership, and the T-178 ruling is why AC8 lists only the targeted test,
     the full app suite, and immutability rather than the repository baseline
     scripts. The frozen error protocol matches the shipped offline-command
     convention (`app/tools/pending-approvals.js`,
     `app/tools/blocked-approvals.js`: exit `2` usage, exit `1`
     read/parse/structure, lowercase tool-prefixed single lines). Every invalid
     fixture and unknown-ID argument is an exact literal rather than a runtime
     transformation of a valid fixture, and I confirmed A, B, and S1-S16 are
     pairwise byte-distinct, so no invalid case is an identity transformation
     of an accepted valid input. No generated identifier, sequence, counter, or
     timestamp participates in an exact expectation, so there is no initializer
     or reset to trace. The single serialized command
     (`node --test app/tests/approval-state.test.js`) has per-case
     temporary-directory setup and recursive teardown, awaits every child, and
     shares no port, state file, database, table, or counter; this product has
     no parent/child rows or non-cascading foreign keys, so no child-first
     cleanup obligation and no helper outside Test-author ownership is
     implicated. Protected-boundary inspection was independent, not taken from
     the planner's summary: the only exact-source assertions in the tracked
     suite are `dead-jobs.test.js:344-370` and
     `blocked-approvals.test.js:440-466`, which read only their own tool paths
     and their own test source; the declared seam
     `app/tests/approval-detail.test.js` asserts only the stored
     approval/job detail envelope and holds no import/export allowlist, source
     snapshot, or module-specifier restriction; `backend-readiness.test.js`
     reads only `factory/backend-readiness.sh` and its own temporary logs;
     `factory-contract.test.js` reads only `factory/PROJECT.env` and
     `.gitignore`, and its `NONVISUAL_PATHS=app/tools/,app/tests/` assertion
     places both new files inside the declared nonvisual prefixes. No tracked
     test enumerates `app/tools/` or `app/tests/`, fetches `GET /`, or makes
     any page-wide text assertion against the HTML shell in `app/server.js`,
     and this ticket introduces no global-shell literal: the usage line, four
     error lines, and four expected stdout lines are child-process bytes the UI
     never renders, so `Protected-Test-Conflicts: none` is exact and no static
     check fails closed. `npm test --prefix app` runs
     `node --test tests/*.test.js`, so the new test file is collected without
     editing `app/package.json`, and `test-immutability-check.sh` exempts
     `factory/`, so the ticket's factory-only commits do not disturb test-first
     ordering. Recorded for traceability: the release tree selected by
     `factory/KIT_PIN` is not present in this worktree, so the planner's
     `ticket-readiness.py` `GLOBAL TEXT PASS` transcript could not be
     re-executed here; the conclusion above rests on the independent inspection
     just described. No contradiction found.
  4. Edge coverage — leakage of every named sensitive category (AC1 sentinels
     for event payload, job error, recipients, subjects, bodies, rejection
     reason, outbox content, and top-level metadata; AC5 the malformed-body
     token), isolation (fresh temporary parent per case), data loss (bytes,
     numeric mode, size, and `mtimeNs` equality plus unchanged sibling entries
     and no created path across AC3-AC7), external effect (AC7's offline source
     boundary), and irreversibility (the command never writes) are each covered
     or explicitly out of scope. There is no authorization decision,
     credential, cookie, CORS, deploy topology, or UI selector in an offline
     read-only command, and `External: no` is correct. No blocking finding.
  5. Four or more positional arguments have no covering criterion. The contract
     resolves the behavior — exactly two positional arguments are accepted and
     arity is checked first — so this is not a builder decision, and the case
     is an equivalent permutation of AC3's zero-, one-, and three-argument
     cases.
     SPEC-WARN: the test-author may add a four-argument case to AC3's arity loop asserting the same exit `2` and usage line, at no contract cost.
  6. The missing-key rule is exercised only for `outbox` (S3), and a scalar
     (string or number) root is not exercised alongside S1 and S2. Both are
     equivalent permutations of the same own-property plus `Array.isArray` and
     non-null-non-array-object rules that S3-S7 and S1-S2 already exercise.
     SPEC-WARN: the test-author may add missing-`events`/`jobs`/`approvals` and scalar-root rows to the S-table loop, keeping the frozen S1-S16 rows unchanged.
  7. AC4, AC5, and AC6 do not freeze which `<approval-id>` argument accompanies
     the failing state path. The contract makes every pre-lookup result
     path- and ID-independent, so no product behavior is left open, and AC6's
     exact `invalid Relay state` line still fails a lookup-first
     implementation for any argument; only run-to-run reproducibility is
     affected.
     SPEC-WARN: fix one literal second argument for AC4-AC6 (for example `appr-pending`, and `appr-a` for S16 so structure-before-lookup is visible in the diff).
  8. A structurally valid state whose `approvals` array is empty has no
     dedicated criterion. The contract resolves it to AC2's `no such approval`
     result, so it is an equivalent permutation of the lookup-miss class rather
     than an open behavior.
     SPEC-WARN: the test-author may add one valid empty-collections fixture asserting exit `1` and the exact `no such approval` line.
  9. AC7's `10_000` ms bound is a property of the harness, and `node:test`
     applies no default per-test timeout, so the bound only holds if the
     test-author sets it. The criterion remains pass/fail decidable and the
     contract fixes both the value and the `spawnSync` `timeout`,
     `result.error === undefined`, and `result.signal === null` assertions.
     SPEC-WARN: pass an explicit `{ timeout: 10_000 }` to each `node:test` case rather than relying on a runner default.
 10. `Fixture-Seams` lists `app/tests/approval-detail.test.js` while
     `Protected-Test-Conflicts` is `none` and Test-author ownership is
     `app/tests/approval-state.test.js` only. The seam authorizes no edit and
     the file stays reference-only, which matches the contract text.
     SPEC-WARN: no action required — the test-author must not modify the declared seam.
 11. Baseline verification in this worktree: `.github/scripts/test-immutability-check.sh`
     passes, and `npm test --prefix app` intermittently fails two pre-existing
     crash-recovery cases under full-suite load —
     `reject-reason.test.js` "4. Reason survives a server kill and restart" and
     `retry-dead.test.js` "4. Kill the server right after a retry is accepted"
     (66/68 and 65/68 passing on two consecutive runs). Both pass when their
     files run alone, so this is a pre-existing timing flake in tests neither
     stage may edit, not a defect in this ticket or an obstacle the contract
     created. AC8 remains satisfiable.
     SPEC-WARN: treat an AC8 full-suite failure limited to those two kill-and-restart cases as environmental and re-run the suite rather than changing T-186's scope.
 12. Fixture A's identifiers are internally consistent but not derivable by
     Relay's own initializer, which produces `job-<eventId>` and
     `appr-<eventId>` (`server.js:42, 69`) — Fixture A pairs event
     `event-private` with `job-pending` and `appr-pending`. No criterion or
     contract rule depends on derivability: the command reports stored `id`,
     `jobId`, and `status` verbatim and treats everything else as opaque, and
     the fixture is written as literal bytes by the test rather than generated
     by Relay, so no generated value participates in an expectation.
     SPEC-WARN: optional realism only — renaming Fixture A's event to `event-pending` (or the IDs to `appr-event-private`/`job-event-private`) would require a contract version 2 and buys no coverage.

SPEC-LINT: PASS

## Test-author log

- 2026-08-23: Wrote the failing acceptance tests in
  `app/tests/approval-state.test.js` (Test-author ownership only) against
  frozen contract version 1. Module specifiers are exactly `node:test`,
  `node:assert`, `node:child_process`, `node:fs`, `node:path`, and `node:os`.
  The declared seam `app/tests/approval-detail.test.js` was not modified.

  Tests written, one per acceptance criterion:

  | Test | Criterion covered |
  |---|---|
  | AC1: Frozen Fixture A returns each approval's byte-exact projection and leaks nothing else | AC1 — all four IDs, exit `0`, zero stderr bytes, byte-exact stdout, parsed key order `approvalId`, `jobId`, `status`, no sensitive token or unrelated ID |
  | AC2: each frozen unknown ID exits 1 with the exact no-such-approval line | AC2 — `appr-missing`, `APPR-PENDING`, `appr-pending ` as literal arguments, byte-distinctness asserted, exit `1`, empty stdout, exact stderr |
  | AC3: zero, one, or three positional arguments exit 2 with the exact usage line and create no path | AC3 — fresh empty temporary parent per case, exit `2`, empty stdout, exact usage line, supplied path not created |
  | AC4: each frozen read-failure path exits 1 with the exact cannot-read line and is left as it was | AC4 — absent path and directory path, exit `1`, empty stdout, exact stderr, directory mode, `mtimeNs`, and entries unchanged |
  | AC5: Frozen Fixture B exits 1 with the exact invalid-JSON line, unchanged bytes, and no leakage | AC5 — bytes, mode, size, `mtimeNs`, sibling entries unchanged; embedded token absent from both streams |
  | AC6: every frozen structural-invalid fixture exits 1 with the exact invalid-state line and unchanged bytes | AC6 — S1-S16 written as frozen literals, pairwise byte-distinctness of A, B, S1-S16 asserted before invocation, exit `1`, empty stdout, exact stderr, fingerprints unchanged |
  | AC7: every invocation self-terminates, repeats byte-identically, and stays read-only and offline | AC7 — explicit `{ timeout: 10_000 }` per case, `spawnSync` `timeout: 10_000` with `result.error === undefined` and `result.signal === null`, repeat determinism, read-only fingerprints, sole `node:fs` specifier and forbidden-reference inspection |

  Applied SPEC-WARN findings 7 and 9: each pre-lookup failure case fixes its
  second argument to the literal `appr-pending` (`appr-a` for S16, so
  structure-before-lookup is visible), and every case passes an explicit
  `{ timeout: 10_000 }` rather than relying on a runner default. Optional
  findings 5, 6, and 8 were declined; the frozen S1-S16 rows and arity cases
  are unchanged.

  Verification: ran only the narrowest scoped command,
  `node --test app/tests/approval-state.test.js`. All 7 tests fail for the
  right reason — `app/tools/approval-state.js` does not exist, so each child
  process exits `1` with Node's module-resolution error instead of the frozen
  bytes. Setup assertions that run before invocation (fixture pairwise
  byte-distinctness, unknown-ID distinctness, empty temporary parents) all
  passed, so no failure is a setup error. Broad verification
  (`npm test --prefix app`, `.github/scripts/test-immutability-check.sh`,
  `repo-check`, `secret-scan`) is deferred to the Builder stage, protected CI,
  and final certification per this role's scope.

  Criterion not expressible as a test — demo-check item for the Narrator's
  bundle:

  - **AC8** (Git history ordering, per-stage diff ownership, and the exit
    status of `npm test --prefix app` and
    `.github/scripts/test-immutability-check.sh`). It is decided from
    repository history and post-implementation commands, not from the test
    file; the Builder commit it compares against does not exist yet. Note
    SPEC-WARN finding 11: a full-suite failure limited to the two pre-existing
    kill-and-restart cases in `reject-reason.test.js` and `retry-dead.test.js`
    is environmental and should be re-run, not treated as an AC8 failure.

## Builder log

- 2026-08-23: Added the offline approval-state command in implementation
  commit `17b0406`. It validates the frozen state shape and unique approval
  IDs, performs a verbatim exact-ID lookup, and emits only the frozen
  `approvalId`, `jobId`, and `status` projection with privacy-safe errors.
  Targeted verification
  `node --test app/tests/approval-state.test.js` passed 7/7. No scoped lint or
  typecheck command exists. Broad verification is deferred to protected CI and
  final certification under the Builder v8 verification boundary.

  PR description: Adds an offline, read-only lookup for one persisted Relay
  approval. Touches `app/tools/approval-state.js` and this role-owned
  ticket-log append. No concerns.
Reviewer round 1 signed detail:

> Worktree is clean and HEAD is unchanged at `faf462b`, matching PR #178's head.
>
> ## Review: T-186 — offline approval-state lookup
>
> **Scope inspected:** frozen contract v1 (`factory/tickets/T-186.md`), test-author commit `1d2894b` (`app/tests/approval-state.test.js`, sole file, 589 lines), builder commit `17b0406` (`app/tools/approval-state.js`, sole file, 98 lines), sibling offline-tool conventions (`pending-approvals.js`, `blocked-approvals.js`), `app/server.js`'s approval-status enum, and the supplied CI evidence (PR [#178](https://github.com/nysa-company/relay-factory/pull/178) at `faf462b`, all required checks passed). No tests/builds/checks were rerun.
>
> ### 1. Do the tests test the acceptance criteria?
>
> Yes, all eight ACs have real, mutation-catching coverage, one test per criterion:
>
> - **AC1/AC7** assert byte-exact stdout via `Buffer` comparison for all four statuses, parsed key *order*, zero stderr bytes, and absence of every `-MUST-NOT-LEAK` sentinel and every unrelated approval ID — this would catch over-projection (e.g. leaking `action`/`reason`) or wrong key order.
> - **AC2** uses case- and space-variant literals (`APPR-PENDING`, `appr-pending `) that would only pass if the implementation avoided trim/case-fold — it does (strict `===`).
> - **AC3** proves argument-arity precedes file access by pointing at nonexistent paths and asserting exit `2`, not `1`.
> - **AC4/AC5/AC6** pin byte/mode/size/`mtimeNs`/sibling-entries fingerprints before and after every failure case, across all 16 structural fixtures plus both read-failure fixtures, with pairwise byte-distinctness asserted first (matches contract exactly).
> - **AC6/S16** (duplicate IDs) is the sharpest regression test: the second argument is fixed to `appr-a`, which *does* exist in S16's array — a "return first match" implementation would exit `0` with real output instead of the required `invalid Relay state`/exit `1`. I confirmed the implementation's `Set`-based duplicate check correctly rejects this rather than dodging via array order.
> - **AC7** asserts `node:fs` is the *only* module specifier (via regex over `require(...)` calls) and that none of `server.js`/`node:http`/`node:https`/`node:net`/`node:child_process`/`fetch` appear — this is a real static check, not a name-only string search that a comment could fool (it operates on actual `require()` calls). Every test case (including successes) also fingerprints the fixture before/after, so a stray write anywhere would fail.
> - **AC8** is a Git-history/ownership check outside the test file by design (correctly logged by the test-author as a non-test criterion); I independently verified it below.
>
> No criterion is untested, and none is unquantified/subjective as written — all eight are byte-exact and mechanically decidable. No planning-defect flag needed.
>
> ### 2. Does the change conform to spec and conventions?
>
> **Contract conformance** — verified line-by-line against the frozen contract:
> - Validation precedence (arity → read → parse → structure → lookup) matches the implementation's control flow exactly.
> - `isValidState` reproduces every rule in the S1–S16 table (own-property + array checks, non-null/non-array approval records, non-empty string `id`/`jobId`, exact-enum `status`, pairwise-distinct `id`) with no gaps or extra leniency.
> - Output is a fresh object literal in `{approvalId, jobId, status}` order via `JSON.stringify`, one trailing LF — byte-identical to the frozen table, and `jobId` is reported without joining `jobs` (per ambiguity-log item 3), matching Fixture A's `appr-sent → job-sent` (absent from `jobs`).
> - Error strings are literal constants (no path/OS-error/parser-text/stack interpolation) — no leakage vector.
> - Zero write/rename/delete/chmod calls exist in the file; `fs.statSync` + `fs.readFileSync` are the only calls used.
>
> **Convention conformance** — error-string shape (`<tool>: <message>
`, lowercase, single line) and exit-code scheme (`2`/`1`/`1`/`1`) match the shipped `pending-approvals.js` and `blocked-approvals.js` tools exactly.
>
> **File ownership / Git history (AC8)** — independently confirmed via `git show --stat`: test-author commit `1d2894b` (00:41:18) touches only `app/tests/approval-state.test.js`; builder commit `17b0406` (00:44:43) touches only `app/tools/approval-state.js`; test-author precedes builder. No commit in the 12-commit range touches `app/server.js`, `app/package.json`, lockfiles, or `factory/KIT_PIN`.
>
> **Structural pass:**
> - *SQL/data safety* — N/A, no persistence layer touched.
> - *Race conditions* — the `statSync` → `readFileSync` sequence is a narrow TOCTOU window (file could change type between calls), but `readFileSync` alone would already throw the same `EISDIR`/`ENOENT` for the two frozen representatives, so this doesn't change observable behavior or violate any criterion; this is a single-operator offline diagnostic tool with no privilege boundary, so it's a non-blocking observation, not a defect.
> - *LLM trust boundary* — N/A, no model output involved.
> - *Shell injection* — tests invoke the tool via `spawnSync` with an argument array (no `shell: true`, no string concatenation), so no injection surface.
> - *Enum completeness* — `status` enum (`pending`/`sent`/`rejected`/`blocked_recipient`) matches `app/server.js:76` exactly; I checked every sibling assignment site (`server.js:44,76,92,94,99,107,109,112,121,125,172`) and confirmed no fifth status value exists anywhere in the codebase that this validator would need to additionally accept.
>
> No scope creep, no invented behavior, no doc goes false (no README enumerates `app/tools/`, so nothing needs updating).
>
> **APPROVE.** Checked: all eight ACs against the frozen contract v1 line-by-line, the implementation's control flow and validation logic against the S1–S16/Fixture-A/B tables, Git history/ownership/commit-order for AC8, module-specifier and forbidden-reference static checks, sibling-tool convention consistency, and the enum's completeness against `app/server.js`. It passes because every criterion has a mutation-sensitive test, the implementation matches the contract byte-for-byte with no dodges (notably the S16 duplicate-ID and case/space-sensitive lookup cases, which a shortcut implementation would fail), and no structural-safety category applies adversely to this offline, read-only, single-file CLI.That background test run was killed intentionally — as reviewer I'm not supposed to rerun tests/builds, so I terminated it right after it started. No follow-up needed; my review above already relies solely on the supplied CI evidence and static inspection, not on any local run.

reviewer round 1: APPROVE
Reviewer round 2 signed detail:

> Worktree is clean and HEAD is unchanged at `3c3bf73`, matching PR [#178](https://github.com/nysa-company/relay-factory/pull/178)'s exact head `3c3bf736f086fa0b7c80c380b4d7355670b73393`. No tests/builds/checks were rerun.
>
> ## Review: T-186 — offline approval-state lookup (base-refresh round)
>
> **Scope inspected:** frozen contract v1 (`factory/tickets/T-186.md`), the T-186-owned diff against `origin/main` (`app/tools/approval-state.js` 98 lines, `app/tests/approval-state.test.js` 589 lines, plus Factory bookkeeping only), the round-1 reviewer verdict already recorded on the ticket (approved at prior head `faf462b`), the base-refresh attestation (`factory/attestations/T-186/refresh.json`) explaining why State reverted from `Approved` to `Review`, everything the merge of T-185 (`e3ef84a`) actually introduced, `app/server.js`'s approval-status enum, and the supplied CI evidence (every configured required check passed at this exact head). No tests/builds/checks were rerun.
>
> **Why a new round is warranted, not a rubber stamp:** the prior approval was recorded at `faf462b`. The base then advanced (T-185's `job-progress.js`/test merged into `main` and into this branch), and `factory/attestations/T-186/refresh.json` reset `State: Approved` → `State: Review`, dropping `Operator-Approval: Receipt`. I independently re-diffed rather than trusting that reset was harmless:
> - `git diff faf462b 3c3bf73 -- app/tools app/tests` → **only** `job-progress.js`/`job-progress.test.js` changed; `approval-state.js` and `approval-state.test.js` are byte-identical to the round-1-approved version.
> - `git diff --stat origin/main 3c3bf73` → T-186's branch-vs-main diff is exactly `app/tools/approval-state.js`, `app/tests/approval-state.test.js`, and Factory ticket/receipt/route-plan files. No scope creep, no touch to `job-progress.js`, `server.js`, `package.json`, or lockfiles.
> - Enum completeness re-checked against current `app/server.js` (unchanged by the merge): approval `status` is assigned only at lines 76/94/99/109 and enumerated only as `pending`/`sent`/`rejected`/`blocked_recipient` at line 172 — no fifth value exists anywhere the merge could have introduced. T-185's `job-progress.js` has its own, disjoint `STATUSES = {pending, done, dead}` for **job** status (a different field/enum), which `approval-state.js` never reads or validates — no interaction.
>
> ### 1. Do the tests test the acceptance criteria?
>
> Unchanged from round 1 and still holds against the current head: all eight ACs have real, mutation-catching coverage (byte-exact `Buffer` comparisons for all four statuses and error lines, parsed key *order* assertion, case/space-sensitive unknown-ID literals, arity-before-read-failure proof via nonexistent paths, byte/mode/size/`mtimeNs`/sibling-entry fingerprints pinned across all 16 structural fixtures + 2 read-failure fixtures + Fixture B, the S16 duplicate-ID case fixed to an argument that *would* match under a naive "first match" implementation, and a `require(...)`-call-based static check for the sole `node:fs` specifier and absence of `server.js`/`node:http`/`node:https`/`node:net`/`node:child_process`/`fetch`). No criterion is untested; none is unquantified/subjective as written. No new planning-defect flag.
>
> ### 2. Does the change conform to spec and conventions?
>
> Re-verified against the current `app/tools/approval-state.js` (unchanged since round 1): validation precedence (arity → read → parse → structure → lookup) matches the contract; `isValidState` reproduces every S1–S16 rule with no gaps; output is `{approvalId, jobId, status}` via a fresh object literal, compact `JSON.stringify`, one trailing LF; error strings are literal constants with no interpolation; only `fs.statSync`/`fs.readFileSync` are called, no write/rename/delete/chmod. AC8 file-ownership and commit-order still hold: `1d2894b` (tests only) precedes `17b0406` (implementation only), and neither T-185's merge nor the refresh commit touches `app/server.js`, `app/package.json`, lockfiles, or `factory/KIT_PIN`.
>
> **Structural pass** (re-run against the merge, not just the original diff):
> - *SQL/data safety* — N/A, no persistence layer.
> - *Race conditions* — same narrow `statSync`→`readFileSync` TOCTOU noted in round 1; non-blocking for this offline, single-operator CLI with no privilege boundary.
> - *LLM trust boundary* — N/A.
> - *Shell injection* — tests use `spawnSync` with an argument array, no `shell: true`.
> - *Enum completeness* — re-confirmed above against the post-merge `app/server.js`; the merge added an unrelated job-status enum in a different file/field, not a new approval-status value, so nothing new for `approval-state.js` to handle.
>
> No scope creep, no invented behavior introduced by the base refresh, no doc goes false.
>
> **APPROVE.** Checked: that the base-refresh (T-185 merge) touched zero files T-186 owns or depends on; that the round-1-approved implementation and test file are byte-identical at the new head; re-verification of contract conformance, AC coverage, and enum completeness against the current (post-merge) `app/server.js`; and that the branch-vs-main diff contains only the two owned files plus Factory bookkeeping. Every configured required GitHub check passed at this exact head. No local tests/builds were run.

reviewer round 2: APPROVE
