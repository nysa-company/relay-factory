# T-107 — add a backend-readiness operator check

State: Ready
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-107

## Goal

Recreate the useful outcome of superseded legacy ticket T-106 on the migrated
Relay product. Give operators one ticket-independent command that reports the
configured production/OpenAI and checking/Anthropic startup routes without
submitting task content.

The planner must preserve the behavioral intent and test-first evidence from
legacy branch `ticket/T-106-backend-readiness` at `cc80aa9`, while re-freezing
file locations for the external-product boundary. Relay may compose public
non-task interfaces from its pinned kit; it must not copy kit policy or modify
the sealed release.

## Acceptance boundary

- The command identifies all four configured adapters and both provider
  families.
- Disabled optional fallback is distinct from invalid or unavailable
  configuration, and no safe required route exits nonzero.
- Automated tests prove that the check never submits task content.
- Operator guidance states that fallback selection occurs only before task
  submission and is never a post-failure retry.

## Log

- 2026-07-15: operator created the first post-migration managed ticket from the
  preserved T-106 intent; State → Ready.
