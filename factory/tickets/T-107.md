# T-107 — add a backend-readiness operator check

State: Review
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-107

## Spec'd description

Add one product-owned, ticket-independent command that selects Relay's sealed
software-factory release from `factory/KIT_PIN` and composes that release's two
existing public non-task checks:

1. `scripts/adapters/contract-test.sh --adapters codex,cursor-openai,claude-code,cursor-anthropic`
2. `scripts/adapters/contract-test.sh --routes`

The command reports all four configured adapters, then the production/OpenAI
and checking/Anthropic startup routes. Its exit status is the route check's
status, not the all-adapter check's status: a disabled optional fallback remains
visible without making a ready primary route unsafe. Relay does not source
`scripts/lib/backend-policy.sh`, reproduce its adapter/family map or resolver,
change the sealed release, accept ticket/task text, or invoke a task-bearing
adapter.

This is the external-product version of the useful behavior and deterministic
non-task guarantees frozen on superseded T-106 at software-factory commit
`cc80aa9`. File ownership and tests move to Relay; kit policy stays behind the
pinned release's public commands.

Product-doc sources:

- [Relay README — factory engine selection](../../README.md): the product uses
  `factory/KIT_PIN` as its only supported engine selection.
- [Relay conformance spec — known modeling limitation](../../SPEC.md#known-modeling-limitation):
  product/engine concerns stay at their documented boundary.
- [Pinned engine architecture — engine model](https://github.com/nysa-company/software-factory/blob/3b63cc71609676fe5fde30a878032e999df05976/docs/architecture.md#engine-model):
  external products select an immutable exact-SHA kit; production uses the
  OpenAI family, checking uses the Anthropic family, and fallback is selected
  before task submission and never retried after failure.
- [Pinned engine architecture — operator route check](https://github.com/nysa-company/software-factory/blob/3b63cc71609676fe5fde30a878032e999df05976/docs/architecture.md#engine-model):
  `scripts/adapters/contract-test.sh --routes` is the documented non-task route
  diagnostic.
- [Pinned adapter contract entrypoint](https://github.com/nysa-company/software-factory/blob/3b63cc71609676fe5fde30a878032e999df05976/scripts/adapters/contract-test.sh):
  `--adapters` and `--routes`, their output, global environment loading, and
  exit statuses are release-owned public behavior composed by this ticket.

## Acceptance criteria

1. With `factory/KIT_PIN` containing Fixture SHA, both pinned check invocations
   returning `0`, and the exact stub output in Fixture A, running
   `factory/backend-readiness.sh` from the repository root exits `0`; stdout is
   exactly Fixture A's output and stderr is empty.
2. With the adapter invocation returning `1` and reporting both Cursor adapters
   as `UNAVAILABLE (fallback_disabled)`, while the route invocation returns `0`
   with ready Codex and Claude Code primary routes as in Fixture B, the command
   exits `0`; stdout and stderr are exactly Fixture B's output streams.
3. With the adapter invocation returning `1` and reporting both primaries as
   `UNAVAILABLE (test_primary_down)`, while the route invocation returns `0`
   and selects `cursor-openai` for `planner` and `cursor-anthropic` for
   `spec-linter` as in Fixture C, the command exits `0`; stdout is exactly
   Fixture C's stdout and stderr is exactly Fixture C's stderr.
4. With the adapter invocation returning `1` and the route invocation returning
   `1` because the production route has no ready adapter while the checking
   route is ready, the command exits `1`, prints the exact Fixture D stdout,
   and prints the exact Fixture D stderr.
5. Across Fixtures A-D, the stub call log contains exactly two lines in this
   order: `--adapters codex,cursor-openai,claude-code,cursor-anthropic` and
   `--routes`; the task trace remains an empty file, and no invocation contains
   a ticket ID, role task, task text, `run-agent.sh`, or an adapter wrapper path.
6. If `factory/KIT_PIN` is missing, has more than one line, is not one lowercase
   40-character hexadecimal SHA, or names a release without an executable
   `scripts/adapters/contract-test.sh`, the command exits `2`, makes zero stub
   calls, and emits exactly the matching Frozen contract error line on stderr
   with no stdout.
7. Supplying any argument, including `sentinel-task`, exits `2`, makes zero stub
   calls, leaves the task trace empty, emits exactly the Frozen contract usage
   line on stderr, and emits no stdout.
8. `README.md` documents the exact root-level command, identifies the four
   adapters and the production/OpenAI and checking/Anthropic families, defines
   exit `0`, `1`, and `2` exactly as frozen below, and states that fallback is
   selected only before task submission and is never a post-failure retry.

## Frozen contract — version 2

This version supersedes T-106 frozen contract version 1 for Relay's external
product boundary. If this contract proves wrong, return T-107 to Ready and
re-plan it as frozen contract version 3; do not edit version 2 in place.

### Entrypoint and arguments

Operator invocation from the Relay repository root:

```bash
factory/backend-readiness.sh
```

File location: `factory/backend-readiness.sh`. The script accepts no arguments,
does not read stdin, and never accepts a ticket, role, adapter list, or task.
Any argument is a usage error with exit `2`, no stdout, and exactly:

```text
usage: factory/backend-readiness.sh
```

### Pinned release selection

The script reads `factory/KIT_PIN` relative to its own repository location,
not the caller's working directory. The file must contain exactly one line
matching `^[0-9a-f]{40}$`. The selected check must be the executable regular
file at:

```text
$HOME/.factory/kits/releases/<KIT_PIN>/scripts/adapters/contract-test.sh
```

No symlink or alternate release path is accepted. Selection errors occur
before either check is invoked, exit `2`, emit no stdout, and emit exactly one
of these stderr lines:

```text
backend-readiness: factory/KIT_PIN must contain exactly one lowercase 40-character SHA
backend-readiness: pinned adapter contract check is unavailable
```

Tests set `HOME` to an isolated fixture directory; there is no product-specific
release-root override.

### Composed non-task calls

The selected entrypoint is invoked exactly twice, sequentially, with the
caller's environment and these exact arguments:

```text
--adapters codex,cursor-openai,claude-code,cursor-anthropic
--routes
```

The first call's status is reported but does not determine the wrapper exit;
this keeps disabled optional fallback distinct from an unsafe required route.
The second call's exit status is authoritative. Relay does not interpret,
rewrite, filter, or duplicate kit-owned probe and route policy.

### Output and exit status

The wrapper writes these headings/result lines to stdout and passes through
each check's stdout and stderr byte-for-byte in call order:

```text
[relay-readiness] adapters=openai:codex,cursor-openai;anthropic:claude-code,cursor-anthropic
<adapter check stdout>
[relay-readiness] adapter_check_exit=<adapter exit>
[relay-readiness] routes=production/openai;checking/anthropic
<route check stdout>
[relay-readiness] route_check_exit=<route exit>
[relay-readiness] result=<SAFE|UNSAFE>
```

`result=SAFE` and wrapper exit `0` occur only when the route check exits `0`.
For every nonzero route-check status, the wrapper emits `result=UNSAFE` and
exits `1`. Wrapper validation/usage failures exit `2` as specified above.

The provider labels in wrapper headings are operator presentation only. Route
selection, state/reason meanings, fallback eligibility, prerequisites, model
allowlisting, authentication probes, and adapter exit behavior remain entirely
owned by the pinned `contract-test.sh` and its release-private policy.

### Deterministic fixtures

Fixture SHA:

```text
1111111111111111111111111111111111111111
```

The executable fixture at the pinned path accepts only the two frozen calls.
It appends its complete argument string to `CALL_LOG`; any task-bearing branch
would append to `TASK_TRACE`. Each test starts with both files empty.

**Fixture A — all adapters and primary routes ready**

Adapter call exit `0`, stdout:

```text
[contract-test] codex: READY (local_contract_ready)
[contract-test] cursor-openai: READY (local_contract_ready)
[contract-test] claude-code: READY (local_contract_ready)
[contract-test] cursor-anthropic: READY (local_contract_ready)
[contract-test] requested adapter contracts hold
```

Route call exit `0`, stdout:

```text
[contract-test] planner route: codex (primary_ready)
[contract-test] spec-linter route: claude-code (primary_ready)
[contract-test] requested adapter contracts hold
```

**Fixture B — optional fallbacks disabled, primary routes ready**

Adapter call exit `1`, stdout:

```text
[contract-test] codex: READY (local_contract_ready)
[contract-test] claude-code: READY (local_contract_ready)
```

Adapter call stderr:

```text
[contract-test] FAIL: cursor-openai: UNAVAILABLE (fallback_disabled)
[contract-test] FAIL: cursor-anthropic: UNAVAILABLE (fallback_disabled)
[contract-test] One or more routes are unavailable or invalid. Fix config/auth/version before any factory run.
```

Route call exit `0` and stdout are Fixture A's route result; route stderr is
empty. Complete wrapper stdout is the Frozen Output template populated with
these two stdout bodies and exits `1` then `0`; complete wrapper stderr is the
adapter stderr above.

**Fixture C — primaries unavailable, startup fallbacks ready**

Adapter call exit `1`, stdout:

```text
[contract-test] cursor-openai: READY (local_contract_ready)
[contract-test] cursor-anthropic: READY (local_contract_ready)
```

Adapter call stderr:

```text
[contract-test] FAIL: codex: UNAVAILABLE (test_primary_down)
[contract-test] FAIL: claude-code: UNAVAILABLE (test_primary_down)
[contract-test] One or more routes are unavailable or invalid. Fix config/auth/version before any factory run.
```

Route call exit `0`, stdout:

```text
[contract-test] planner route: cursor-openai (primary_test_primary_down)
[contract-test] spec-linter route: cursor-anthropic (primary_test_primary_down)
[contract-test] requested adapter contracts hold
```

Route stderr is empty. Complete wrapper stdout is the Frozen Output template
populated with these two stdout bodies and exits `1` then `0`; complete wrapper
stderr is the adapter stderr above.

**Fixture D — no safe production route**

Adapter call exit `1`, stdout:

```text
[contract-test] claude-code: READY (local_contract_ready)
```

Adapter call stderr:

```text
[contract-test] FAIL: codex: UNAVAILABLE (test_primary_down)
[contract-test] FAIL: cursor-openai: UNAVAILABLE (fallback_disabled)
[contract-test] FAIL: cursor-anthropic: UNAVAILABLE (fallback_disabled)
[contract-test] One or more routes are unavailable or invalid. Fix config/auth/version before any factory run.
```

Route call exit `1`, stdout:

```text
[contract-test] spec-linter route: claude-code (primary_ready)
```

Route call stderr:

```text
[contract-test] FAIL: planner route: no_ready_route_primary_test_primary_down_fallback_fallback_disabled
[contract-test] One or more routes are unavailable or invalid. Fix config/auth/version before any factory run.
```

Complete wrapper stdout is the Frozen Output template populated with these two
stdout bodies and exits `1` then `1`, ending in `result=UNSAFE`; complete
wrapper stderr is the adapter stderr followed immediately by the route stderr.
For Fixtures A-D, no substring or unordered matching satisfies criteria 1-4.

### Exact file locations and verification

- Add `factory/backend-readiness.sh`.
- Add operator guidance to `README.md`.
- Add all automated acceptance coverage in
  `app/tests/backend-readiness.test.js`, using only Node's installed standard
  library and the isolated `HOME`, `CALL_LOG`, and `TASK_TRACE` fixtures above.
- Targeted test command:
  `node --test app/tests/backend-readiness.test.js`.
- Full product regression command: `npm test --prefix app`.
- Immutability regression command:
  `.github/scripts/test-immutability-check.sh`.
- Do not modify `factory/KIT_PIN`, the sealed release, kit scripts/policy,
  application server behavior, or existing tests.

## Criteria checklist

- **Pass/fail:** AC1-7 assert exact exit codes, stdout/stderr, invocations, and
  trace contents. AC8 names exact visible documentation statements.
- **Unambiguous:** every path, argument, order, fixture value, output wrapper,
  error, and exit meaning is frozen. “Ready” and “safe” appear only as literal
  kit output or the exact wrapper result.
- **Coverage:** AC1 covers the entrypoint, all adapters/families, primary routes,
  SAFE output, and exit `0`; AC2 covers disabled fallback and adapter-status
  independence; AC3 covers both startup fallback selections; AC4 covers UNSAFE
  output and exit `1`; AC5 covers both exact calls and the non-task boundary;
  AC6 covers pin/release validation; AC7 covers argument rejection; AC8 covers
  the sole documentation file and all operator guidance.
- **No dead contract elements:** every command, output line, fixture file,
  validation branch, exit status, and file location appears in at least one
  criterion.

## Ambiguity log

- **Where should the diagnostic live after migration?** Resolved from
  [Relay README](../../README.md): Relay owns the wrapper under `factory/`,
  while `factory/KIT_PIN` selects immutable engine behavior.
- **Should Relay port T-106's new kit mode?** No. Resolved from the external
  product boundary and the pinned entrypoint: compose its existing `--adapters`
  and `--routes` modes instead of changing/copying the sealed kit.
- **Which status controls the operator exit?** Resolved from T-106's preserved
  intent and the engine's optional-fallback rule: only the route check controls
  SAFE/UNSAFE; the all-adapter status remains visible.
- **How are provider families identified without copying resolver policy?**
  Resolved as fixed operator headings paired with the exact documented adapter
  names. All probe, state, reason, and selection logic remains kit-owned.
- **How is “never submits task content” mechanically proved?** Resolved from
  T-106's deterministic trace technique: the fixture separately logs calls and
  task-bearing execution; AC5 requires two exact non-task calls and an empty
  task trace.
- **May the wrapper use the active launcher record instead of `KIT_PIN`?** No.
  The operator explicitly requested composition from `factory/KIT_PIN`; the
  pinned architecture makes that full SHA the product's engine selection.
- **Does this command trigger an external send?** No. It accepts no task and
  invokes only non-task adapter/route probes, so `External: no` remains correct
  and no `external` label is required.

## Log

- 2026-07-15: operator created the first post-migration managed ticket from the
  preserved T-106 intent; State → Ready.
- 2026-07-15: planner froze contract version 2 for the Relay-owned
  `factory/backend-readiness.sh`: exact `KIT_PIN` release selection, composition
  of the pinned release's existing adapter and route checks, route-authoritative
  exit status, deterministic non-task trace proof, Relay test locations, and no
  sealed-kit or policy changes.
- 2026-07-15: spec-lint of frozen contract version 2 against the pinned kit
  (`contract-test.sh`, `backend-policy.sh`, `docs/architecture.md` at Kit-SHA).
  Fixtures A-D verified consistent with the real resolver and probes (adapter
  iteration order, `primary_test_primary_down`, and
  `no_ready_route_primary_test_primary_down_fallback_fallback_disabled` all
  reproduce exactly); criteria quality and provider/family naming are sound.
  Findings:
  1. Frozen contract "Pinned release selection" freezes "the executable
     **regular** file" and "No symlink or alternate release path is accepted,"
     but no criterion exercises it. AC6 tests only missing / multiline /
     non-SHA / missing-executable. A builder using `[ -x "$path" ]` (which
     follows symlinks) passes every criterion while accepting a symlinked
     `contract-test.sh`, violating the frozen contract. Fix: add a criterion
     where the pinned path is a symlink (or non-regular file) asserting exit
     `2`, zero stub calls, and the `pinned adapter contract check is
     unavailable` line — or declare symlink-rejection out of scope.
  2. Frozen contract "Pinned release selection" freezes that the script reads
     `factory/KIT_PIN` "relative to its own repository location, not the
     caller's working directory," but every criterion runs from the repository
     root (AC1: "from the repository root"), so cwd always equals the script's
     repo root. A builder resolving `KIT_PIN` relative to cwd passes all
     criteria while breaking the frozen location-independence. Fix: add a
     criterion invoking the script by absolute path from a different working
     directory and asserting identical behavior — or drop the clause.
  3. Frozen contract "Composed non-task calls" freezes invocation "with the
     caller's environment," but the deterministic fixtures replace
     `contract-test.sh` with a canned-output stub that ignores its
     environment, so no criterion exercises environment passthrough. A builder
     who scrubs the environment (e.g. `env -i`) passes all fixtures yet breaks
     the real kit, whose probes and route policy read `FACTORY_PROBE_*` and
     `~/.factory/global.env` from that environment. Fix: add a criterion that
     asserts a caller-set environment variable reaches the invoked check — or
     declare environment passthrough out of scope.

SPEC-LINT: FAIL — three frozen guarantees (regular-file/no-symlink, cwd-independent KIT_PIN read, caller-environment passthrough) have no covering criterion, so a builder can pass every AC while violating the contract.

## Re-plan — frozen contract version 3

### Spec'd description

Keep version 2's external-product design and no-dependency scope unchanged, but
close its three SPEC-LINT coverage gaps. The Relay-owned command still selects
the sealed release only through [`factory/KIT_PIN`](../../README.md), composes
only the pinned engine's documented public non-task checks, and leaves all
probe and route policy behind the external-product boundary described by the
[Relay conformance spec](../../SPEC.md#known-modeling-limitation) and the
[pinned engine architecture](https://github.com/nysa-company/software-factory/blob/3b63cc71609676fe5fde30a878032e999df05976/docs/architecture.md#engine-model).

Version 3 adds deterministic coverage proving that the pinned check itself
cannot be a symlink, `KIT_PIN` resolution does not depend on caller cwd, and one
caller-set environment sentinel reaches both composed calls. It adds no product
behavior, dependency, endpoint, external send, or file beyond version 2's
frozen scope. `External: no` remains correct.

### Acceptance criteria

1. Version 2 AC1-8 remain acceptance criteria without modification.
2. With Fixture E's pinned `contract-test.sh` replaced by a symlink to an
   executable regular stub, running `factory/backend-readiness.sh` exits `2`,
   emits no stdout, emits exactly
   `backend-readiness: pinned adapter contract check is unavailable` followed
   by a newline on stderr, makes zero stub calls, and leaves `TASK_TRACE` empty.
3. With Fixture F installed and the repository `factory/KIT_PIN` containing
   Fixture SHA, invoking the absolute path to `factory/backend-readiness.sh`
   while the process cwd is an isolated directory outside the repository exits
   `0`; stdout is exactly Fixture A's complete wrapper stdout, stderr is empty,
   and `CALL_LOG` contains the same two exact ordered lines required by version
   2 AC5.
4. With Fixture G installed and caller environment variable
   `RELAY_READINESS_SENTINEL=caller-environment`, running the command exits `0`;
   `ENV_LOG` contains exactly two lines in call order:
   `caller-environment|--adapters codex,cursor-openai,claude-code,cursor-anthropic`
   and `caller-environment|--routes`. Its stdout, stderr, `CALL_LOG`, and
   `TASK_TRACE` also satisfy version 2 AC1 and AC5.

### Frozen contract — version 3

Version 3 supersedes version 2. Version 2 and its failed SPEC-LINT record above
are preserved as history. The complete version 3 interface is version 2's
frozen contract incorporated without modification, plus the fixture and
verification requirements below. If version 3 proves wrong, return T-107 to
Ready and re-plan it as a new frozen contract version; do not edit version 3 in
place.

#### Additional deterministic fixtures

All additional fixtures use version 2's Fixture SHA, isolated `HOME`, empty
`CALL_LOG`, and empty `TASK_TRACE`. Fixture F's and Fixture G's regular
executable stubs return Fixture A's exact per-call stdout and statuses.

**Fixture E — symlinked pinned check rejected**

At the exact pinned check path, create a symbolic link to an executable regular
stub outside that path. The link target would append its arguments to
`CALL_LOG` and append `task` to `TASK_TRACE` if executed. The wrapper must reject
the symbolic link during pinned-release validation; the target is never run.

**Fixture F — caller cwd outside the repository**

Create an isolated empty directory outside the Relay repository and set it as
the child process cwd. Invoke the wrapper by its absolute filesystem path. Do
not place a `factory/KIT_PIN` or pinned release beneath that cwd. The wrapper
must read the repository's `factory/KIT_PIN` relative to its own location and
run the Fixture A stub under the isolated `HOME`.

**Fixture G — caller environment reaches both calls**

Set these caller environment values:

```text
RELAY_READINESS_SENTINEL=caller-environment
ENV_LOG=<absolute path to an empty fixture file>
```

For each accepted invocation, the regular executable stub appends exactly
`$RELAY_READINESS_SENTINEL|$*` plus a newline to `ENV_LOG` before producing its
Fixture A result. The expected complete file is:

```text
caller-environment|--adapters codex,cursor-openai,claude-code,cursor-anthropic
caller-environment|--routes
```

`RELAY_READINESS_SENTINEL` and `ENV_LOG` are test-only caller environment
values, not wrapper arguments or product configuration. The wrapper passes the
caller's environment through unchanged to each pinned call; it does not source,
copy, rename, or interpret these values.

#### File locations and verification

Version 2's exact file locations, commands, and modification exclusions remain
frozen. Fixtures E-G and their assertions belong in the same frozen
`app/tests/backend-readiness.test.js`; no second test file or dependency is
added.

### Version 3 criteria checklist

- **Pass/fail:** Version 2 AC1-8 retain their exact outputs, statuses, calls,
  trace, and documentation assertions. Version 3 AC2-4 add exact filesystem,
  cwd, environment, log, output, and exit observations.
- **Unambiguous:** AC2 names the rejected file type and exact failure; AC3 names
  both the absolute invocation and external cwd; AC4 freezes the environment
  key, value, destination, two exact lines, and call order.
- **Coverage:** Version 2's checklist remains valid for its incorporated
  contract. Version 3 AC2 covers executable-regular-file/no-symlink selection;
  AC3 covers script-relative `KIT_PIN` resolution; AC4 covers caller-environment
  passthrough to both exact composed calls.
- **No dead contract elements:** Fixture E is exercised by AC2, Fixture F by
  AC3, and Fixture G plus `ENV_LOG` and both sentinel observations by AC4.

### Version 3 ambiguity log

- **Should the lint gaps be removed from the contract or tested?** The operator
  explicitly required all three guarantees to be proved, so version 3 keeps
  them and adds Fixtures E-G.
- **Does rejecting a symlink mean checking only its target?** No. Resolved from
  version 2's exact phrase “No symlink”: Fixture E requires rejection even when
  the target is executable and regular.
- **Which non-root cwd proves script-relative selection?** Any isolated empty
  directory outside the repository is sufficient because Fixture F invokes the
  wrapper by absolute path and forbids a cwd-local pin.
- **How much environment behavior is in Relay's contract?** Only transparent
  passthrough is Relay-owned. Fixture G uses one inert sentinel observed by both
  calls; loading global environment and interpreting probe variables remain
  pinned-kit behavior.
- **Does the added fixture instrumentation expand product configuration?** No.
  `ENV_LOG` and `RELAY_READINESS_SENTINEL` exist only in the isolated test stub
  and are not read by the product wrapper.

### Version 3 planning log

- 2026-07-15: re-planned T-107 once after the recorded version 2 SPEC-LINT FAIL.
  Frozen contract version 3 preserves version 2 and closes only its three
  blocking coverage gaps with Fixtures E-G and AC2-4: symlink rejection,
  cwd-independent pin resolution, and caller-environment passthrough to both
  composed calls. No application code, tests, external behavior, or sealed-kit
  policy changed.

Kit-SHA: 3b63cc71609676fe5fde30a878032e999df05976

## Log

- 2026-07-15: spec-lint of frozen contract version 3 delta (AC2-4, Fixtures E-G)
  together with preserved version 2. Verified the three version 2 SPEC-LINT
  gaps are now mechanically closed — a builder cannot pass the new criteria
  while violating the frozen guarantee:
  1. Regular-file/no-symlink (v2 finding 1) — closed by v3 AC2 + Fixture E. The
     fixture symlinks the pinned check to a regular executable stub; a naive
     `[ -f "$path" ] && [ -x "$path" ]` builder accepts it (both tests follow
     the link to a regular executable), runs the stub, and exits `0`, failing
     AC2's required `pinned adapter contract check is unavailable`, exit `2`,
     and zero stub calls. AC2 therefore forces symlink detection.
  2. cwd-independent KIT_PIN read (v2 finding 2) — closed by v3 AC3 + Fixture F.
     cwd is an isolated directory outside the repo with no `factory/KIT_PIN`
     beneath it and the wrapper is invoked by absolute path; a cwd-relative
     resolver finds no pin and errors instead of exiting `0` with Fixture A
     stdout, so AC3 forces resolution relative to the script's own location.
  3. Caller-environment passthrough (v2 finding 3) — closed by v3 AC4 +
     Fixture G. The stub records `$RELAY_READINESS_SENTINEL|$*` to `ENV_LOG`
     for both calls; an environment-scrubbing builder (`env -i`) yields empty
     sentinels, failing the two exact `caller-environment|...` lines, so AC4
     forces unchanged passthrough to both composed calls.
  Criteria quality: v3 AC2-4 assert exact exit codes, stdout/stderr, filesystem
  type, cwd, `ENV_LOG`/`CALL_LOG`/`TASK_TRACE` contents, and call order — all
  pass/fail-decidable, no judgment terms. Contract coverage: Fixtures E, F, G
  and `ENV_LOG` are each exercised by AC2, AC3, AC4 respectively; version 2
  AC1-8 and its frozen contract are incorporated unchanged (v3 AC1). Consistency:
  the AC2 stderr line, wrapper output template, Fixture A bodies, Fixture SHA,
  and the two composed call arguments match version 2 byte-for-byte; no
  contradiction with the linked product docs. Edge coverage: symlink, external
  cwd, and environment-passthrough failure cases are now each covered by a
  criterion. No new findings.

SPEC-LINT: PASS

- 2026-07-15: first test-author run reached the five-minute wall-clock limit
  with exit 124, no completed provider result, no file changes, and a
  conservative USD 2.00 reservation; State → Blocked-Escalated. Operator
  adjudication raised only PER_RUN_TIMEOUT_MIN from 5 to 10 while preserving
  the USD 2.00 run and USD 10.00 ticket caps, authorized one fresh test-author
  run, and returned State → Building.
- 2026-07-15: test-author authored the failing v3 acceptance tests in
  `app/tests/backend-readiness.test.js` (Node stdlib only: `node:test`,
  `node:assert`, `node:child_process`, `node:fs`, `node:path`, `node:os`).
  Ten tests, one per testable criterion, run via
  `node --test app/tests/backend-readiness.test.js`; all ten fail before
  implementation with `factory/backend-readiness.sh not implemented yet`
  (missing feature, not setup error). Harness: each test builds an isolated
  repo layout with its own `factory/KIT_PIN` = Fixture SHA
  `1111…1111` (the sealed `factory/KIT_PIN` is not modified) and an isolated
  `HOME` holding a generated `contract-test.sh` stub at the pinned path that
  logs `$*`→`CALL_LOG`, `task`→`TASK_TRACE` on any non-frozen call,
  `$RELAY_READINESS_SENTINEL|$*`→`ENV_LOG`, and emits each fixture's exact
  stdout/stderr/exit; `spawnSync` captures exit, stdout, and stderr.
  - AC1 → Fixture A: exit 0, exact wrapper stdout, empty stderr.
  - AC2 → Fixture B: exit 0, exact stdout + adapter stderr (fallback disabled).
  - AC3 → Fixture C: exit 0, exact stdout + adapter stderr (startup fallbacks).
  - AC4 → Fixture D: exit 1, UNSAFE, exact stdout + adapter-then-route stderr.
  - AC5: across A-D, `CALL_LOG` is exactly the two ordered frozen calls,
    `TASK_TRACE` empty, and the log contains no ticket ID / `run-agent.sh` /
    `task`.
  - AC6: missing / multi-line / non-SHA `KIT_PIN` → exit 2 + the pin error;
    non-executable pinned check → exit 2 + the unavailable error; all with no
    stdout and zero stub calls.
  - AC7: any argument (`sentinel-task`, `--routes`, `T-107`) → exit 2, usage on
    stderr, no stdout, zero calls, empty task trace.
  - AC2 (v3) → Fixture E: symlinked pinned check → exit 2 + unavailable error,
    no stdout, zero calls, empty task trace (target logs a call+task if run).
  - AC3 (v3) → Fixture F: invoked by absolute path with cwd an isolated
    directory outside the repo → exit 0, Fixture A stdout, two call-log lines.
  - AC4 (v3) → Fixture G: `RELAY_READINESS_SENTINEL=caller-environment` and
    `ENV_LOG` set → exit 0, `ENV_LOG` exactly the two sentinel lines, plus AC1
    and AC5 assertions.
  - Not expressed as a test → **AC8 (README documentation)**: this run must not
    edit `README.md`, and the criterion is human-readable prose (documents the
    command, the four adapters and both families, exit 0/1/2 meanings, and the
    fallback-before-submission statement) not frozen byte-for-byte, so it
    becomes a demo-check item for the Narrator's bundle.

- 2026-07-15: the authorized test-author run committed the complete 341-line
  failing acceptance suite at 3668c24 before the provider exceeded the USD 2.00
  hard stop by USD 0.01762 and returned exit 7. Independent targeted execution
  exits 1 for the missing implementation. State → Blocked-Escalated. Because
  the sequencer requires a successful terminal role record, the operator kept
  the USD 2.00 per-run and USD 15.00 daily caps, raised only the aggregate
  ticket cap from USD 10.00 to USD 15.00, authorized one validation-only
  test-author closeout, and returned State → Building.
- 2026-07-15: validation-only test-author closeout. Inspected the committed
  test at 3668c24 (`app/tests/backend-readiness.test.js`, 341 lines) — not
  modified — and ran `node --test app/tests/backend-readiness.test.js` once:
  tests 10, pass 0, fail 10. Every failure is the same
  `factory/backend-readiness.sh not implemented yet at .../factory/backend-readiness.sh`
  thrown by `makeRepo` (test file line 181), which `scenario()` invokes before
  `makeHome`/`makeLogs` — so no HOME, `CALL_LOG`, or `TASK_TRACE` fixture is
  even built when the guard fires. Confirmed `factory/backend-readiness.sh`
  does not exist; the failures are the missing feature, not fixture setup. No
  implementation written, no test file changed, `factory/KIT_PIN` untouched.
  Test handoff verified: the ten failing acceptance tests are the first
  commits on the ticket branch and fail for the right reason before
  implementation. AC8 (README documentation) remains the sole demo-check item
  for the Narrator's bundle.
- 2026-07-15: reviewer checked frozen contract version 3 against commits
  3668c24 (tests) through 2c85575 (implementation).
  - Test adequacy: `app/tests/backend-readiness.test.js` covers every testable
    criterion with byte-for-byte stdout/stderr, exact exit codes, and
    `CALL_LOG`/`TASK_TRACE`/`ENV_LOG` proofs. v3 AC2 (symlink), AC3 (foreign
    cwd), and AC4 (env passthrough) each force the guarantee the v2 SPEC-LINT
    flagged; a naive `[ -x ]`/cwd-relative/`env -i` builder fails them. A
    contract-dodging implementation would be caught. AC8 (README prose) is
    correctly deferred to the Narrator demo-check; README present and conforms
    (command, four adapters, both families, exit 0/1/2, fallback-before-submit).
  - Spec conformance: `factory/backend-readiness.sh` composes only the two
    frozen non-task calls against the KIT_PIN-selected pinned check; it does not
    source `backend-policy.sh`, reproduce policy, accept task text, or touch the
    sealed kit. Route-check status is authoritative (SAFE/0, else UNSAFE/1);
    adapter status is reported only. Exit codes 0/1/2 all handled.
  - Structural pass: pin regex-validated `^[0-9a-f]{40}$` before path use;
    symlink rejected via `[ -L ]` ahead of `-f`/`-x`; path resolved
    script-relative with `CDPATH=` and `--` guards; all variables quoted;
    `printf` used throughout; caller environment passed through unscrubbed. No
    SQL/shell-injection surface; no new enum values.
  - Process: test commit 3668c24 precedes implementation 2c85575; the builder's
    feat commit touched only `README.md` and `factory/backend-readiness.sh`, not
    the test file; `factory/KIT_PIN` and the sealed release are untouched. The
    `factory/ENVELOPE.env` cap change lives in the operator chore commit
    1d48017, not the builder's diff.
  - Checks: `node --test app/tests/backend-readiness.test.js` → 10/10 pass;
    `npm test --prefix app` → 35/35 pass; `.github/scripts/test-immutability-check.sh`
    → exit 0.

  Reviewer verdict: APPROVE
