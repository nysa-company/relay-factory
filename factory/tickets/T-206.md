# T-206 — count all Relay outbox entries offline

Kit-SHA: 35dba5a318dd5d09a6ac820a03f24f01d57c7318
State: Planning
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-206
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/state-summary.test.js
Authentication-Seams: none
Builder ownership: app/tools/outbox-count.js only
Test-author ownership: app/tests/outbox-count.test.js only

## Description

Add a zero-dependency, read-only Node command that reports the total number of
outbox entries in a persisted Relay state file while the service is offline.

## Acceptance criteria

1. `node app/tools/outbox-count.js <state-file>` exits zero and emits
   deterministic compact JSON with exactly one `outboxEntries` nonnegative
   integer.
2. A valid state with no outbox entries returns `{"outboxEntries":0}` followed
   by one newline.
3. Missing, unreadable, malformed, or structurally invalid state is rejected
   with fixed redacted errors that reveal no path or stored content.
4. Every invocation is offline, read-only, and self-terminating; input bytes,
   mode, timestamps, and sibling directory entries remain unchanged.
5. Complete acceptance tests are committed before implementation. Targeted,
   full app, and immutability checks pass without dependency, server, schema,
   package-manifest, lockfile, or Factory-control changes.

## Coordination

This ticket owns only its two new `app/` paths and is independent of all prior
tickets.
