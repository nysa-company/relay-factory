# T-283 — Count Relay approvals by referenced job status offline

Kit-SHA: 12066ff5f92ac79191e1278b50142614f555e4fd
State: Blocked-Escalated
Resume-State: Planning
Initiative: I-002
Priority: normal
Risk class: low
External: no
Branch: ticket/T-283
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/approval-link.test.js, app/tests/job-status-count.test.js
Authentication-Seams: none
Builder ownership: app/tools/approval-job-status-count.js only
Test-author ownership: app/tests/approval-job-status-count.test.js only

## Description

Add a zero-dependency, read-only CommonJS command that counts persisted Relay
approvals whose referenced job has a supplied exact status while Relay is
offline.

## Acceptance criteria

1. `node app/tools/approval-job-status-count.js <state-file> <job-status>`
   emits compact JSON containing exactly one `approvalsForJobStatus` integer.
2. Status matching is exact and case-sensitive. Every approval must reference
   exactly one valid job; duplicate job or approval IDs and missing references
   are rejected before counting, and zero matches is valid.
3. Invalid arguments are rejected before file access. Unreadable, malformed,
   or structurally invalid state returns fixed redacted errors.
4. The command is offline, zero-dependency, deterministic, read-only, and
   self-terminating. Tests resolve fixtures from the test file, work from any
   current directory, use a five-second child timeout, and do not assume Git
   history depth.
5. Tests precede implementation. Targeted, full app, and immutability checks
   pass without dependency, server, schema, manifest, or Factory changes.

## Coordination

This ticket owns only its two new paths and is independent of T-282 and T-284.

## Ambiguity log

1. **Cross-ticket file-boundary conflict (blocking).** `factory/tickets/T-259.md`
   (State: Backlog) and `factory/tickets/T-252.md` (State: Backlog, contract
   version 1 already frozen on `origin/ticket/T-252` and marked
   `SPEC-LINT: FAIL`) both declare the identical exclusive ownership lines
   this ticket declares:
   `Builder ownership: app/tools/approval-job-status-count.js only` and
   `Test-author ownership: app/tests/approval-job-status-count.test.js only`.
   Their frozen/declared CLI contract is materially different from and
   incompatible with this ticket's: T-259/T-252 specify
   `node app/tools/approval-job-status-count.js <state-file> <job-id> <status>`
   emitting `{"approvalsWithJobAndStatus":<count>}` (exact match on both a
   job ID and a status), while this ticket (T-283) specifies
   `node app/tools/approval-job-status-count.js <state-file> <job-status>`
   emitting `{"approvalsForJobStatus":<count>}` (exact match on status only,
   via the approval's referenced job). The same two file paths cannot
   simultaneously satisfy both frozen contracts. `factory/rulings.md` was
   checked and contains no ruling covering this collision (only the
   detail-route and T-178 rulings), so it cannot be resolved by citation.
   This is a portfolio/ownership decision (which ticket keeps this file
   path, whether T-283 should be re-scoped to a distinct file name, or
   whether T-259/T-252 should be closed as superseded) that is outside
   product-doc authority. Escalated to the operator rather than resolved
   unilaterally.

## Planning log

- 2026-08-26: Planner run halted before drafting a spec'd description,
  acceptance criteria, or a frozen contract. Cause: the cross-ticket
  file-boundary check (grep of `factory/tickets/*.md` plus
  `origin/ticket/T-259` and `origin/ticket/T-252`) found T-259 and T-252
  already claim exclusive ownership of both paths this ticket's header
  claims, under an incompatible CLI/output contract. Operator question:
  which ticket should own `app/tools/approval-job-status-count.js` and
  `app/tests/approval-job-status-count.test.js` — should T-283 be re-scoped
  to different file names (e.g. an `approval-job-status-count` variant with
  a distinguishing suffix), or should T-259 and/or T-252 be closed/re-scoped
  as duplicates of T-283's job-status-only contract? No contract is frozen
  for T-283 pending that decision.

ROLE-ESCALATE: CONTRACT-BLOCKED
