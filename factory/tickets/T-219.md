# T-219 — count Relay events with one payload key offline

Kit-SHA: 7ae447baf7287e32a9df60c9453da6edec454e7b
State: Ready
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-219
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/event-metadata.test.js
Authentication-Seams: none
Builder ownership: app/tools/event-payload-key-count.js only
Test-author ownership: app/tests/event-payload-key-count.test.js only

## Description

Add a zero-dependency, read-only Node command that counts persisted Relay
events whose payload has one operator-supplied own-property key while the
service is offline.

## Acceptance criteria

1. `node app/tools/event-payload-key-count.js <state-file> <key>` exits zero
   and emits deterministic compact JSON with exactly one
   `eventsWithPayloadKey` nonnegative integer.
2. Matching uses an exact, case-sensitive own-property check without trimming,
   coercion, traversal, or prototype lookup. No match returns
   `{"eventsWithPayloadKey":0}` followed by one newline.
3. Missing arguments and missing, unreadable, malformed, or structurally
   invalid state are rejected with fixed redacted errors that reveal no path,
   key, event ID, type, timestamp, payload, or stored content; complete event
   validation precedes counting.
4. Every invocation is offline, read-only, and self-terminating; input bytes,
   mode, timestamps, and sibling directory entries remain unchanged.
5. Complete acceptance tests are committed before implementation. Targeted,
   full app, and immutability checks pass without dependency, server, schema,
   package-manifest, lockfile, or Factory-control changes.

## Coordination

This ticket owns only its two new `app/` paths and is independent of T-220 and
T-221. The prior q77 cohort T-216 through T-218 remains historical evidence.
