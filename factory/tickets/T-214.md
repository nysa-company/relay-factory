# T-214 — count Relay jobs that have been retried offline

Kit-SHA: 81b21b6d1abca7a7bde0425fbae941fcb9c112ed
State: Planning
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-214
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/job-attempts.test.js
Authentication-Seams: none
Builder ownership: app/tools/job-retry-count.js only
Test-author ownership: app/tests/job-retry-count.test.js only

## Spec'd description

Add `app/tools/job-retry-count.js`, a zero-dependency CommonJS command that
reads one persisted Relay state file while Relay is offline, validates the
complete stored job collection, and counts jobs whose stored nonnegative
integer `retries` is greater than zero. Each qualifying job contributes
exactly one even when its stored retry total is larger than one. The command
emits only `{"retriedJobs":<count>}`, allowing an operator to inspect how many
durable jobs have crossed the explicit retry boundary without starting Relay,
changing durable state, summing attempts or retries, or exposing job IDs,
event links, statuses, counters, errors, events, approvals, outbox entries, or
other stored content.

Product-doc and repository sources:

- [`SPEC.md` behavior 2](../../SPEC.md#behavior-spec) defines Relay's durable
  background-job attempt and dead-letter lifecycle.
- [`SPEC.md` behaviors 6 and 7](../../SPEC.md#behavior-spec) make the
  atomically persisted state file Relay's durable source and make jobs and
  attempts operator-visible.
- [`SPEC.md` conformance cases 3, 4, and 7](../../SPEC.md#test-cases-the-conformance-suite)
  require recorded attempts for transient failure, visible dead-letter
  outcomes, and duplicate-free crash recovery.
- The current [`app/server.js` state model](../../app/server.js) establishes
  the four stored collections and complete persisted job shape. Its
  `retryJob` transition increments `retries` once when a dead job is explicitly
  retried, independently of the cumulative `attempts` and reset
  `attemptsSinceRetry` counters.
- The shipped [`job-count` command](../../app/tools/job-count.js) and its
  [acceptance suite](../../app/tests/job-count.test.js) establish complete job
  validation, unique job IDs, compact count-only output, fixed redacted
  failures, and the read-only offline convention. The shipped
  [`job-error-count` command](../../app/tools/job-error-count.js) and its
  [acceptance suite](../../app/tests/job-error-count.test.js) establish a
  one-argument count predicate over that same validated job shape.
- The protected [`job-attempts` acceptance suite](../../app/tests/job-attempts.test.js)
  establishes the persisted `retries` and `attemptsSinceRetry` fields used by
  the declared reference-only fixture seam.
- [`AGENTS.md` Boundaries](../../AGENTS.md#boundaries) keeps `app/`
  zero-dependency, and [`AGENTS.md` Verification](../../AGENTS.md#verification)
  defines the later full-suite and immutability checks.

No endpoint, HTTP shape, UI selector, global-shell UI text, persisted schema,
server behavior, package, dependency, lockfile, Factory control,
authentication path, deployment topology, or external-send behavior changes.

## Acceptance criteria

1. Given Frozen Fixture A, each of two invocations of
   `node app/tools/job-retry-count.js <fixture-a-absolute-path>` exits `0`,
   writes exactly `{"retriedJobs":2}\n` to stdout, and writes zero stderr
   bytes. Before invocation, the test asserts that exactly two jobs have
   `retries > 0` and that those jobs' exact retry values `3` and `1` sum to
   `4`, proving the output counts jobs rather than retry events. Neither stream
   contains any member of the complete frozen sensitive-token set. After each
   invocation, Fixture A has identical bytes and BigInt numeric `mode`, `size`,
   and `mtimeNs`, and its parent has identical sorted entry names.
2. Frozen Fixture B, whose three jobs have `pending`, `done`, and `dead`
   statuses but all have `retries: 0`, and Frozen Fixture C, whose `jobs`
   array is empty while sibling collections contain opaque values, each
   produce the same result: one invocation exits `0`, writes exactly
   `{"retriedJobs":0}\n` to stdout, writes zero stderr bytes, emits no frozen
   sensitive token, and leaves its fixture and parent unchanged by every
   observation named in AC1.
3. In separate fresh empty temporary parents, the zero-argument invocation
   and the two-argument invocation with absent absolute path
   `<parent>/arity-state-MUST-NOT-LEAK.json` plus literal `extra` each exit
   `2`, write zero stdout bytes, and write exactly
   `usage: node app/tools/job-retry-count.js <state-file>\n` to stderr. The
   candidate path remains absent and each parent retains empty sorted entries.
4. Invoking the command against frozen absent path
   `<parent>/PATH-MUST-NOT-LEAK.json` and, separately, frozen empty directory
   `<parent>/DIRECTORY-MUST-NOT-LEAK` exits `1`, writes zero stdout bytes, and
   writes exactly `job-retry-count: cannot read state file\n` to stderr. The
   absent path remains absent. The directory remains a directory with
   identical BigInt numeric `mode`, `size`, `mtimeNs`, and sorted entries; both
   parents retain their sorted entries.
5. Invoking the command against Frozen Fixture D and, separately, Frozen
   Fixture E exits `1`, writes zero stdout bytes, and writes exactly
   `job-retry-count: state file is not valid JSON\n` to stderr. Neither stream
   contains `MALFORMED-PAYLOAD-MUST-NOT-LEAK`. Each fixture's exact bytes and
   BigInt numeric `mode`, `size`, and `mtimeNs`, and each parent's sorted
   entries, remain unchanged.
6. Each S1-S25 frozen structural-invalid fixture exits `1`, writes zero stdout
   bytes, and writes exactly `job-retry-count: invalid Relay state\n` to
   stderr. S25 contains one valid retried job before an invalid sibling,
   proving complete job-array validation precedes counting. Before invocation,
   Fixtures A-E and S1-S25 are pairwise byte-distinct, and S15's exact `DEAD`
   is asserted byte-distinct from valid `dead`. Every fixture retains its
   exact bytes and BigInt numeric `mode`, `size`, and `mtimeNs`, and every
   parent retains its sorted entries.
7. Every child invocation in AC1-AC6 uses a `5_000` ms timeout, returns with
   `error === undefined` and `signal === null`, and emits none of the complete
   frozen sensitive-token set. Static inspection asserts that the Builder
   file's complete CommonJS module-specifier set is exactly `node:fs`, its
   complete `node:fs` operation set is exactly `readFileSync`, and its source
   contains none of `server.js`, `fetch(`, `WebSocket`, `node:http`,
   `node:https`, `node:net`, `node:child_process`, `setTimeout`, or
   `setInterval`. Static inspection also asserts that the Test-author file's
   complete module-specifier set is exactly `node:assert`,
   `node:child_process`, `node:fs`, `node:os`, `node:path`, and `node:test`.
8. Git history contains the complete Test-author commit before the Builder
   commit. Apart from role-owned ticket-log appends, the Test-author commit
   creates only `app/tests/job-retry-count.test.js` and the Builder commit
   creates only `app/tools/job-retry-count.js`. Existing application and test
   files, `app/server.js`, `app/package.json`, lockfiles, dependencies,
   persisted schema, `factory/KIT_PIN`, and Factory controls remain unchanged.
   After implementation, `node --test app/tests/job-retry-count.test.js`,
   `npm test --prefix app`, and
   `.github/scripts/test-immutability-check.sh` each exit `0`.

## Frozen contract — version 1

- **Freeze result:** PASS. Contract version 1 is frozen.

This is the exact interface for the Test-author and Builder. If it proves
wrong, return T-214 to Ready and append a higher-numbered frozen contract;
version 1 must not be edited or removed.

### Command-line interface and failure precedence

- Repository-root invocation is exactly
  `node app/tools/job-retry-count.js <state-file>`.
- Exactly one user-supplied positional argument is accepted. Any other arity
  exits `2`, writes zero stdout bytes, writes exactly the following single
  LF-terminated line to stderr, and does not read or create a candidate state
  path:

  ```text
  usage: node app/tools/job-retry-count.js <state-file>
  ```

- After arity validation, the command synchronously reads `<state-file>`
  directly as UTF-8. Any `readFileSync` failure, including an absent path, a
  directory in place of a regular file, or an OS read denial, exits `1`,
  writes zero stdout bytes, and writes exactly:

  ```text
  job-retry-count: cannot read state file
  ```

- Readable bytes rejected by `JSON.parse` exit `1`, write zero stdout bytes,
  and write exactly:

  ```text
  job-retry-count: state file is not valid JSON
  ```

- A parsed value that fails the frozen structurally-valid-state rules exits
  `1`, writes zero stdout bytes, and writes exactly:

  ```text
  job-retry-count: invalid Relay state
  ```

- Failure precedence is argument count, file read, JSON parse, complete
  structural validation, then counting.
- Every error is one fixed line terminated by exactly one LF. No error includes
  the state path, OS error, parser text, input bytes, stack trace, job ID,
  event link, status, attempt or retry counter, `lastError`, event field,
  approval field, outbox field, or second line.
- Acceptance uses absolute temporary paths. Symlink inputs and portable
  simulation of OS permission denial are outside the required fixture matrix;
  if `readFileSync` throws for either, the frozen read-error result applies.
  The directory fixture freezes the exact command envelope, not any errno,
  error class, or operating-system text.

### Structurally valid state, retry predicate, and successful output

A structurally valid state for this command is a parsed non-null, non-array
JSON object with exactly four own properties: `events`, `jobs`, `approvals`,
and `outbox`, each an array.

Every `jobs` element must be a non-null, non-array object with:

- non-empty string own properties `id` and `eventId`;
- an own `status` property exactly equal to `pending`, `done`, or `dead`;
- own `attempts`, `retries`, and `attemptsSinceRetry` properties that are
  integers greater than or equal to zero; and
- an own `lastError` property equal to `null` or a string, including `""`.

Job IDs must be pairwise distinct across the complete array. Additional job
properties are allowed and ignored. Event, approval, and outbox elements are
wholly opaque; only the fact that their top-level collections are arrays is
validated. A job `eventId` need not resolve to a stored event. No relationship
is imposed among `status`, the three counters, and `lastError` beyond each
field's individual rule.

The command validates the complete job array and ID uniqueness before
counting. A job contributes exactly one when `job.retries > 0`; a job with
`retries === 0` contributes zero. The `retries` values are never summed, and
`attempts`, `attemptsSinceRetry`, `status`, and `lastError` do not affect the
count after structural validation.

Success exits `0`, writes zero stderr bytes, and writes one compact JSON object
followed by exactly one LF. Its only key is:

```json
{"retriedJobs":<nonnegative integer count>}
```

`retriedJobs` is the number of elements in the validated `jobs` array whose
stored `retries` is greater than zero. No stored job ID, `eventId`, status,
counter, `lastError`, additional job field, event, approval, or outbox value
is emitted.

### Frozen Fixture A — two retried jobs whose retry values sum to four

`app/tests/job-retry-count.test.js` writes this exact parsed value to an
isolated temporary `state.json` using
`JSON.stringify(value, null, 2) + "\n"`:

```json
{
  "events": [
    {
      "id": "OPAQUE-EVENT-ID-MUST-NOT-LEAK",
      "payload": {
        "private": "EVENT-PAYLOAD-MUST-NOT-LEAK"
      }
    }
  ],
  "jobs": [
    {
      "id": "JOB-ID-PENDING-NEW-MUST-NOT-LEAK",
      "eventId": "EVENT-ID-PENDING-NEW-MUST-NOT-LEAK",
      "status": "pending",
      "attempts": 0,
      "lastError": null,
      "retries": 0,
      "attemptsSinceRetry": 0
    },
    {
      "id": "JOB-ID-PENDING-RETRIED-MUST-NOT-LEAK",
      "eventId": "EVENT-ID-PENDING-RETRIED-MUST-NOT-LEAK",
      "status": "pending",
      "attempts": 9,
      "lastError": "PENDING-RETRY-ERROR-MUST-NOT-LEAK",
      "retries": 3,
      "attemptsSinceRetry": 0
    },
    {
      "id": "JOB-ID-PENDING-UNRETRIED-MUST-NOT-LEAK",
      "eventId": "EVENT-ID-PENDING-UNRETRIED-MUST-NOT-LEAK",
      "status": "pending",
      "attempts": 2,
      "lastError": "PENDING-ERROR-MUST-NOT-LEAK",
      "retries": 0,
      "attemptsSinceRetry": 2
    },
    {
      "id": "JOB-ID-DONE-RETRIED-MUST-NOT-LEAK",
      "eventId": "EVENT-ID-DONE-RETRIED-MUST-NOT-LEAK",
      "status": "done",
      "attempts": 4,
      "lastError": null,
      "retries": 1,
      "attemptsSinceRetry": 1,
      "ignoredDiagnostic": "IGNORED-JOB-FIELD-MUST-NOT-LEAK"
    },
    {
      "id": "JOB-ID-DEAD-UNRETRIED-MUST-NOT-LEAK",
      "eventId": "EVENT-ID-DEAD-UNRETRIED-MUST-NOT-LEAK",
      "status": "dead",
      "attempts": 3,
      "lastError": "DEAD-JOB-ERROR-MUST-NOT-LEAK",
      "retries": 0,
      "attemptsSinceRetry": 3
    }
  ],
  "approvals": [
    {
      "action": {
        "body": "APPROVAL-ACTION-MUST-NOT-LEAK"
      }
    }
  ],
  "outbox": [
    {
      "body": "OUTBOX-CONTENT-MUST-NOT-LEAK"
    }
  ]
}
```

Exactly the second and fourth jobs have `retries > 0`, so exact stdout is
`{"retriedJobs":2}\n`. Their retry values are `3` and `1`, whose sum is `4`;
neither `4` nor any attempts total is the result. The fixture also proves that
retry history is independent of current `pending` or `done` status, and that a
`dead` job with zero retries is not counted.

### Frozen Fixtures B-E and filesystem paths

- Fixture B contains exactly the UTF-8 bytes
  `{"events":[],"jobs":[{"id":"job-zero-pending","eventId":"event-zero-pending","status":"pending","attempts":0,"lastError":null,"retries":0,"attemptsSinceRetry":0},{"id":"job-zero-done","eventId":"event-zero-done","status":"done","attempts":1,"lastError":null,"retries":0,"attemptsSinceRetry":1},{"id":"job-zero-dead","eventId":"event-zero-dead","status":"dead","attempts":3,"lastError":"ZERO-RETRY-ERROR-MUST-NOT-LEAK","retries":0,"attemptsSinceRetry":3}],"approvals":[],"outbox":[]}\n`.
  It is structurally valid, contains every valid status, and writes exactly
  `{"retriedJobs":0}\n` because every retry value is zero.
- Fixture C contains exactly the UTF-8 bytes
  `{"events":[null],"jobs":[],"approvals":[7],"outbox":["OPAQUE-CONTENT-MUST-NOT-LEAK"]}\n`.
  It is structurally valid, proves empty jobs and unconsumed sibling
  collection elements are accepted, and writes exactly
  `{"retriedJobs":0}\n`.
- Fixture D contains exactly the UTF-8 bytes
  `{"jobs":[],"payload":"MALFORMED-PAYLOAD-MUST-NOT-LEAK"\n`. It is rejected
  by `JSON.parse` because the object is not closed.
- Fixture E is a zero-byte regular file. It is also rejected by `JSON.parse`.
- The arity path is absent absolute path
  `<temporary-parent>/arity-state-MUST-NOT-LEAK.json`.
- The read-failure paths are absent absolute path
  `<temporary-parent>/PATH-MUST-NOT-LEAK.json` and existing empty directory
  `<temporary-parent>/DIRECTORY-MUST-NOT-LEAK`.

### Frozen structural-invalid fixtures

Each value below is written directly as the displayed compact UTF-8 JSON plus
one LF.

| ID | Exact JSON before final LF | Violated rule |
|---|---|---|
| S1 | `null` | root is null |
| S2 | `[]` | root is an array |
| S3 | `{"events":{},"jobs":[],"approvals":[],"outbox":[]}` | `events` is not an array |
| S4 | `{"events":[],"jobs":{},"approvals":[],"outbox":[]}` | `jobs` is not an array |
| S5 | `{"events":[],"jobs":[],"approvals":{},"outbox":[]}` | `approvals` is not an array |
| S6 | `{"events":[],"jobs":[],"approvals":[],"outbox":{}}` | `outbox` is not an array |
| S7 | `{"events":[],"jobs":[],"approvals":[],"outbox":[],"metadata":[]}` | extra top-level property |
| S8 | `{"events":[],"jobs":[],"approvals":[]}` | missing own `outbox` property |
| S9 | `{"events":[],"jobs":[null],"approvals":[],"outbox":[]}` | job is null |
| S10 | `{"events":[],"jobs":[[]],"approvals":[],"outbox":[]}` | job is an array |
| S11 | `{"events":[],"jobs":[{"id":"","eventId":"event-invalid","status":"pending","attempts":0,"lastError":null,"retries":0,"attemptsSinceRetry":0}],"approvals":[],"outbox":[]}` | job `id` is empty |
| S12 | `{"events":[],"jobs":[{"id":7,"eventId":"event-invalid","status":"pending","attempts":0,"lastError":null,"retries":0,"attemptsSinceRetry":0}],"approvals":[],"outbox":[]}` | job `id` is not a string |
| S13 | `{"events":[],"jobs":[{"id":"job-invalid-event-empty","eventId":"","status":"pending","attempts":0,"lastError":null,"retries":0,"attemptsSinceRetry":0}],"approvals":[],"outbox":[]}` | job `eventId` is empty |
| S14 | `{"events":[],"jobs":[{"id":"job-invalid-event-type","eventId":7,"status":"pending","attempts":0,"lastError":null,"retries":0,"attemptsSinceRetry":0}],"approvals":[],"outbox":[]}` | job `eventId` is not a string |
| S15 | `{"events":[],"jobs":[{"id":"job-invalid-status-case","eventId":"event-invalid","status":"DEAD","attempts":3,"lastError":null,"retries":0,"attemptsSinceRetry":3}],"approvals":[],"outbox":[]}` | status is outside the exact enum |
| S16 | `{"events":[],"jobs":[{"id":"job-invalid-status-type","eventId":"event-invalid","status":null,"attempts":3,"lastError":null,"retries":0,"attemptsSinceRetry":3}],"approvals":[],"outbox":[]}` | status is not a string |
| S17 | `{"events":[],"jobs":[{"id":"job-invalid-attempts-negative","eventId":"event-invalid","status":"dead","attempts":-1,"lastError":null,"retries":0,"attemptsSinceRetry":3}],"approvals":[],"outbox":[]}` | `attempts` is negative |
| S18 | `{"events":[],"jobs":[{"id":"job-invalid-attempts-type","eventId":"event-invalid","status":"pending","attempts":0.5,"lastError":null,"retries":0,"attemptsSinceRetry":0}],"approvals":[],"outbox":[]}` | `attempts` is not an integer |
| S19 | `{"events":[],"jobs":[{"id":"job-invalid-retries-negative","eventId":"event-invalid","status":"dead","attempts":3,"lastError":null,"retries":-1,"attemptsSinceRetry":3}],"approvals":[],"outbox":[]}` | `retries` is negative |
| S20 | `{"events":[],"jobs":[{"id":"job-invalid-retries-type","eventId":"event-invalid","status":"pending","attempts":0,"lastError":null,"retries":"0","attemptsSinceRetry":0}],"approvals":[],"outbox":[]}` | `retries` is not an integer |
| S21 | `{"events":[],"jobs":[{"id":"job-invalid-window-negative","eventId":"event-invalid","status":"dead","attempts":3,"lastError":null,"retries":0,"attemptsSinceRetry":-1}],"approvals":[],"outbox":[]}` | `attemptsSinceRetry` is negative |
| S22 | `{"events":[],"jobs":[{"id":"job-invalid-window-type","eventId":"event-invalid","status":"pending","attempts":0,"lastError":null,"retries":0,"attemptsSinceRetry":null}],"approvals":[],"outbox":[]}` | `attemptsSinceRetry` is not an integer |
| S23 | `{"events":[],"jobs":[{"id":"job-invalid-error","eventId":"event-invalid","status":"dead","attempts":3,"lastError":7,"retries":0,"attemptsSinceRetry":3}],"approvals":[],"outbox":[]}` | `lastError` is neither null nor a string |
| S24 | `{"events":[],"jobs":[{"id":"job-duplicate","eventId":"event-a","status":"pending","attempts":0,"lastError":null,"retries":0,"attemptsSinceRetry":0},{"id":"job-duplicate","eventId":"event-b","status":"done","attempts":1,"lastError":null,"retries":0,"attemptsSinceRetry":1}],"approvals":[],"outbox":[]}` | duplicate job IDs |
| S25 | `{"events":[],"jobs":[{"id":"job-valid-retried-first","eventId":"event-valid","status":"pending","attempts":4,"lastError":"countable-first","retries":1,"attemptsSinceRetry":1},{"id":"job-invalid-missing-attempts","eventId":"event-invalid","status":"pending","lastError":null,"retries":0,"attemptsSinceRetry":0}],"approvals":[],"outbox":[]}` | valid retried first job followed by an invalid sibling missing own `attempts` |

Fixtures A-E and S1-S25 are pairwise byte-distinct before invocation. Fixtures
A-C satisfy every frozen structural rule. Fixtures D and E fail `JSON.parse`.
Every S fixture parses successfully and then fails a frozen structural rule.
S15 uses the frozen direct literal `DEAD`; it is not generated at runtime and
its UTF-8 bytes differ from valid `dead`. No other invalid fixture is produced
by casing, normalization, or mutation of valid fixture bytes.

### Sensitive-token set, lifecycle, and read-only observations

The complete frozen sensitive-token set is:

1. every string value in Frozen Fixture A that contains `MUST-NOT-LEAK`;
2. `ZERO-RETRY-ERROR-MUST-NOT-LEAK`,
   `OPAQUE-CONTENT-MUST-NOT-LEAK`, and
   `MALFORMED-PAYLOAD-MUST-NOT-LEAK`;
3. `arity-state-MUST-NOT-LEAK.json`,
   `arity-state-MUST-NOT-LEAK`, `PATH-MUST-NOT-LEAK.json`,
   `PATH-MUST-NOT-LEAK`, and `DIRECTORY-MUST-NOT-LEAK`.

The Test-author recursively derives item 1 from the frozen parsed Fixture A
and asserts that it equals an independently written direct expected list.
Every child invocation checks both output streams against every token. Exact
success and error stream assertions independently ensure that no status,
counter, ID, error, or other stored value is emitted.

Each runtime case owns a fresh temporary parent, creates only its required
input artifacts before invocation, snapshots sorted parent entries and every
existing regular file's exact bytes plus
`fs.statSync(path, { bigint: true }).mode`, `size`, and `mtimeNs`, waits
synchronously for each child process, and recursively removes the parent in
`finally`. The directory-path case snapshots the directory's same three
BigInt numeric stat fields and sorted entries. Repeated success needs no reset
because the command is read-only. Access time is excluded because reading the
fixture is the behavior under test.

Tests share no state path, process, port, server, database, table, generated
identifier, sequence, counter, clock, or timestamp. There is no parent row,
sibling dependent table, foreign key, cascade, or ordered cleanup obligation.
Every asserted ID, status, attempt counter, retry counter, and count is direct
literal fixture data; no generated value or initializer participates in an
expectation.

### File locations, protected boundaries, and topology

- Builder ownership is creation of `app/tools/job-retry-count.js` only. It is
  a standalone CommonJS command whose complete module-specifier set is exactly
  `node:fs` and whose only `node:fs` operation is `readFileSync`.
- Test-author ownership is creation of
  `app/tests/job-retry-count.test.js` only. Its complete module-specifier set
  is exactly `node:assert`, `node:child_process`, `node:fs`, `node:os`,
  `node:path`, and `node:test`.
- `app/tests/job-attempts.test.js` is the protected reference-only fixture
  seam. It is not edited and grants no edit right. Its source assertions and
  runtime behavior inspect only `app/tools/job-attempts.js`, not the new
  Builder path. Protected job-count, job-error-count, job-status-count, and
  other tool suites inspect only their corresponding named tools. No protected
  test enumerates `app/tools/` or `app/tests/`, restricts the new tool's
  import/export surface, or snapshots `app/tools/job-retry-count.js`.
  `Protected-Test-Conflicts: none` remains exact.
- `app/package.json` already includes the new test through
  `node --test tests/*.test.js`; neither role edits the manifest.
- Targeted acceptance is
  `node --test app/tests/job-retry-count.test.js`. Post-implementation
  repository verification is `npm test --prefix app` and
  `.github/scripts/test-immutability-check.sh`.
- No endpoint, HTTP shape, route, UI selector, global page-shell text,
  accessible UI name, authentication, cookie, CORS, cross-origin request,
  credential, domain, preview deployment, or external send is introduced.
  Topology is one local file-to-stdout process, and `External: no` remains
  exact.

## Criteria checklist

- **Pass/fail and unambiguous:** AC1-AC6 freeze exact arguments, exit codes,
  stream bytes, fixture bytes, stat fields, parent entries, validation
  precedence, retry arithmetic, and privacy observations. AC7 freezes timeout,
  process result, and static source boundaries. AC8 is decided from Git
  history, path diffs, and three exact command exit statuses.
- **Coverage:** AC1 covers two qualifying jobs with retry values `3` and `1`,
  count-not-sum behavior, repetition, privacy, and read-only behavior. AC2
  covers nonempty all-zero jobs across every valid status, empty jobs, and
  opaque sibling collections. AC3 proves arity handling precedes candidate-path
  use. AC4 covers two portable read failures. AC5 covers malformed and
  zero-byte JSON. AC6 covers every structural rule, job-ID uniqueness,
  complete validation before counting, exact casing distinction, and pairwise
  fixture distinctness. AC7 covers every invocation's termination, privacy,
  and complete source boundaries. AC8 covers role ownership, tests-first
  history, unchanged protected surfaces, and all post-implementation commands.
- **No dead contract elements:** every command path, output and error shape,
  fixture, retry predicate, sensitive token, validation class, lifecycle rule,
  file location, protected boundary, topology disposition, and verification
  command appears in AC1-AC8.
- **Derived-fixture distinctness:** S15's direct exact value is `DEAD`,
  byte-distinct from valid `dead`; no runtime casing or normalization
  transformation creates it. Fixtures A-E and S1-S25 are pairwise
  byte-compared before invocation.
- **Generated-value determinism:** every ID, status, counter, count-producing
  record, and expected count is literal fixture data. No generated identifier,
  sequence, counter, reset, initializer, clock, or timestamp supplies an
  expectation.
- **Fixture lifecycle closure:** every case owns an isolated temporary parent,
  waits for its child, and removes all artifacts recursively in `finally`.
  There is no database, parent row, sibling table, non-cascading foreign key,
  cascade, shared mutable fixture, or ordered cleanup dependency.
- **Cross-ticket file boundaries:** every local ticket ownership declaration
  was searched, and the visible leased `origin/ticket/T-213`,
  `origin/ticket/T-214`, and `origin/ticket/T-215` records were inspected.
  T-213 and T-215 own paths distinct from both T-214 paths; no other ticket
  declares either T-214 role-owned path.
- **Protected source boundaries:** the declared seam, associated job tools and
  suites, `app/tests/factory-contract.test.js`, and tracked source assertions
  were inspected. No allowlist, exact-source assertion, source snapshot, path
  digest, or module restriction applies to the new Builder path or its sole
  `node:fs` specifier. The pinned readiness checker returned `READINESS PASS`.
- **Global protected text:** the pinned readiness checker returned
  `GLOBAL TEXT PASS` separately for the usage line, all three error lines, the
  `retriedJobs` property name, and both exact success output lines. No
  accessible UI name is added.
- **Deploy/topology completeness:** the command is local file-to-stdout with
  no endpoint, UI, auth, cookie, CORS origin, credential, domain,
  preview-deploy, or external-send surface.

## Ambiguity log

1. **Does “retried” count jobs or retry events?** Resolved by the reconciled
   ticket: a job contributes one when its stored `retries` integer is greater
   than zero, regardless of that integer's magnitude. Fixture A freezes retry
   values `3` and `1`, an arithmetic sum of `4`, while the exact result is
   `2`.
2. **Does current job status affect the retry predicate?** No. The ticket names
   only stored `retries`, and [`app/server.js`](../../app/server.js) increments
   that field when transitioning a dead job back to `pending`; later worker
   processing can make the same job `done` or `dead`. Resolved as
   `job.retries > 0` after structural validation, independent of status,
   attempts, retry-window attempts, and error.
3. **What state structure is required before counting?** Resolved from the
   four collections and job fields in `app/server.js`, the shipped
   [`job-count` validator](../../app/tools/job-count.js), and the declared
   [`job-attempts` fixture seam](../../app/tests/job-attempts.test.js): require
   exactly four array collections, validate every job's complete persisted
   shape and unique ID, allow additional job fields, and leave sibling
   collection elements opaque.
4. **Must counters agree with status or with one another?** No shipped offline
   job validator imposes those relationships, and `attempts` is cumulative
   while `attemptsSinceRetry` resets in `retryJob`. Resolved by validating each
   counter independently as a nonnegative integer. Fixture A includes a
   pending job with nine cumulative attempts, three retries, and zero
   retry-window attempts.
5. **How are failures classified without leaking input?** Raw OS and parser
   errors can reveal paths or state bytes. Resolved from the shipped offline
   count convention: usage exit `2`, operational exits `1`, fixed single-line
   redacted errors, empty opposite streams, and precedence from arity through
   complete structure.
6. **How are offline, read-only, and self-terminating claims proved?** Resolved
   with synchronous child invocations under a `5_000` ms timeout, exact file
   and directory fingerprints, repeated success, isolated teardown, and a
   production source boundary admitting only `node:fs` and `readFileSync`.
7. **Are derived fixtures or generated expected values involved?** S15 uses
   direct literal `DEAD`, whose exact bytes are frozen and distinct from valid
   `dead`; it is not produced by a runtime transform. Every serialization is
   pairwise byte-compared, and all IDs, statuses, counters, and counts come
   from literal setup.
8. **Does fixture cleanup have hidden dependencies?** No service or database
   is started. Each case owns one temporary directory removed after its child
   exits, so there are no parent rows, sibling tables, foreign keys, cascades,
   resets, or teardown ordering.
9. **Does another ticket own these paths?** No. Local ownership declarations
   contain only T-214's two paths. The other concurrently leased records,
   [`T-213`](T-213.md) and [`T-215`](T-215.md), own distinct event-timestamp
   and approval-status count files.
10. **Do protected tests reject the new source boundary?** Direct inspection
    of `app/tests/job-attempts.test.js`, existing job-tool suites,
    `app/tests/factory-contract.test.js`, and tracked source assertions found
    only path-specific boundaries. The pinned readiness check returned
    `READINESS PASS`, so `Protected-Test-Conflicts: none` is exact.
11. **Does protected global text collide?** The pinned global-literal checker
    accepted every non-empty shell-visible literal named in the criteria
    checklist. `factory/PROJECT.env` declares both role-owned directories
    nonvisual, and no accessible UI name is introduced.
12. **Are deployment, authentication, or external-send details applicable?**
    No. The complete topology is one local process reading one local path and
    writing stdout/stderr; there is no network, UI, credential, preview, or
    send path, so no `external` label is required.
13. **Does an existing operator ruling change the contract?**
    [`factory/rulings.md`](../rulings.md) was checked before freeze. Its
    detail-route ruling does not alter this local command, and its T-178
    verification ruling confirms the targeted test, full app suite, and
    immutability as the post-implementation verification set. No new operator
    decision is required.

## Coordination

T-214 owns only `app/tools/job-retry-count.js` and
`app/tests/job-retry-count.test.js`. The protected
`app/tests/job-attempts.test.js` seam is reference-only and grants no edit
right. T-213 and T-215 own distinct paths and can proceed concurrently.

## Planning log

- 2026-08-24: Planner froze contract version 1 with exact greater-than-zero
  retry semantics, explicit count-not-sum arithmetic, byte-level count-only
  success and error envelopes, deterministic fixtures, complete job
  validation, privacy sentinels, read-only lifecycle, role ownership,
  protected-source and global-text results, and local-only topology.
  `SPEC.md`, `AGENTS.md`, `factory/rulings.md`, `factory/PROJECT.env`,
  `app/server.js`, `app/package.json`, `app/tools/job-count.js`,
  `app/tools/job-error-count.js`, `app/tools/job-attempts.js`,
  `app/tests/job-count.test.js`, `app/tests/job-error-count.test.js`,
  `app/tests/job-attempts.test.js`, `app/tests/factory-contract.test.js`,
  tracked protected source assertions,
  `.github/scripts/test-immutability-check.sh`, every local sibling ownership
  declaration, and the visible leased T-213/T-215 branch records were
  inspected. Fixtures A-E and S1-S25 were checked against the frozen parse and
  structure classes, pairwise distinctness requirements, and exact counts `2`
  and `0`. The pinned deterministic checker returned `READINESS PASS` for
  T-214 and `GLOBAL TEXT PASS` for every non-empty frozen shell-visible
  literal. No file-boundary conflict, protected-test conflict, topology
  requirement, external-send label, or operator decision remains.
