# T-275 — reject duplicate approval IDs in blocked reports

State: Ready
Initiative: I-002
Priority: normal
Risk class: low
External: no
Branch: ticket/T-275
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/blocked-approvals.test.js
Authentication-Seams: none
Builder ownership: app/tools/blocked-approvals.js only
Test-author ownership: app/tests/blocked-approvals.test.js only

## Description

The offline `blocked-approvals` report can accept duplicate approval IDs and
emit an ambiguous blocked-recipient list. Reject duplicate IDs before filtering
or reporting.

## Acceptance criteria

1. A state containing two otherwise valid approvals with the same non-empty
   `id` exits `1`, writes exactly the existing invalid-state stderr line, and
   writes zero stdout bytes.
2. Duplicate IDs are invalid whether both approvals are blocked or only one is
   blocked. Complete approval validation and uniqueness checking precede the
   blocked-recipient filter.
3. Valid unique approvals retain the existing compact array shape, field order,
   ID sort, exit statuses, and stderr messages byte-for-byte. Empty or
   no-blocked state still emits `[]`.
4. Existing collection, approval field, proposed timestamp, and four-value
   status validation remain unchanged. Job IDs are not made unique by this
   ticket.
5. Failures remain redacted and leak no recipient, action, reason, state bytes,
   input path, parser detail, or operating-system detail.
6. The command remains offline, zero-dependency, deterministic, read-only, and
   self-terminating. Tests precede implementation; the targeted test, full app
   suite, and immutability check pass without other product or Factory changes.

## Coordination

This ticket owns only `app/tools/blocked-approvals.js` and
`app/tests/blocked-approvals.test.js`. It is independent of T-273 and T-274.
