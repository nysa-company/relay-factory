# T-166 — read one Relay event

State: Review
Initiative: I-002
Priority: normal
Risk class: low
External: no
Branch: ticket/T-166
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/server.js, app/tests/conformance.test.js
Authentication-Seams: none
Builder ownership: app/server.js only
Test-author ownership: app/tests/event-detail.test.js only

## Description

Add a read-only `GET /api/events/:id` detail route to the zero-dependency Relay
API. It gives callers one stored event together with the one durable job created
for that event, without requiring them to retrieve the aggregate
`GET /api/state` document.

This is the focused read surface for the event and job relationship defined by
[`SPEC.md` behavior 1-2](../../SPEC.md#behavior-spec), over state persisted
under [behavior 6](../../SPEC.md#behavior-spec), and extends the API visibility
described by [behavior 7](../../SPEC.md#behavior-spec). The exact success
envelope and the concurrent `app/server.js` ownership decision come from the
dated [detail-route ruling](../rulings.md). The implementation remains inside
the zero-dependency app boundary in
[`AGENTS.md`](../../AGENTS.md#boundaries).

## Acceptance criteria

1. With the frozen `evt-detail-001` state fixture loaded, `GET
   /api/events/evt-detail-001` returns HTTP `200`, `Content-Type:
   application/json`, and the exact success body in Frozen contract version 1;
   the body has only the top-level keys `event` and `job`, and
   `job.eventId === event.id === "evt-detail-001"`.
2. With the same fixture loaded, `GET /api/events/evt-detail-missing` returns
   HTTP `404`, `Content-Type: application/json`, and the byte-for-byte body
   `{"error":"no such event"}`.
3. After one successful detail request and one unknown-event request, both the
   parsed `GET /api/state` response and the bytes of `DATA_DIR/state.json`
   equal their respective pre-request snapshots.
4. The acceptance-test commit adds only
   `app/tests/event-detail.test.js` and precedes every implementation commit;
   implementation commits modify only `app/server.js`. The branch adds no
   dependency metadata changes and no Factory-control changes beyond this
   Planner-owned ticket contract and the already-pinned route plan.

Kit-SHA: c13b59f03fc3e4d251aab75d509e186497a0b529

## Frozen contract — version 1

### Endpoint

- Method and path: `GET /api/events/:id`.
- Route match: pathname `^/api/events/([^/]+)$`; `:id` is matched exactly
  against `state.events[].id`. A request body is neither required nor read.
- Existing event: find the event whose `id` equals `:id`, then find the stored
  job whose `eventId` equals that event's `id`. Return HTTP `200` through the
  existing JSON response helper with exactly:

  ```json
  {"event":<stored event>,"job":<stored related job>}
  ```

  The top-level object has exactly the keys `event` then `job`, with no
  `ok`, `id`, or other envelope fields. Both nested records are returned
  verbatim from durable state; no nested field is omitted, renamed, derived,
  or added by this route.
- Unknown event: when no `state.events[]` record has `id === :id`, return HTTP
  `404` through the existing JSON response helper with exactly:

  ```json
  {"error":"no such event"}
  ```

- Both outcomes have `Content-Type: application/json`. Neither outcome
  mutates in-memory state, calls `persist()`, changes `DATA_DIR/state.json`, or
  triggers worker, approval, outbox, or external-send behavior.
- The product invariant in
  [`SPEC.md` behavior 1-2](../../SPEC.md#behavior-spec) supplies exactly one
  related job for every accepted event. Behavior for a manually corrupted
  state file containing an event without its job is outside this ticket.

### Acceptance fixture

`app/tests/event-detail.test.js` creates an isolated temporary `DATA_DIR` and
writes the following JSON value to `DATA_DIR/state.json` before starting the
real `app/server.js`. The dead job keeps the fixture stable without relying on
worker timing.

```json
{
  "events": [
    {
      "id": "evt-detail-001",
      "type": "meeting",
      "payload": {
        "to": "test@example.com",
        "subject": "Detail fixture"
      },
      "receivedAt": "2026-07-28T12:00:00.000Z"
    }
  ],
  "jobs": [
    {
      "id": "job-evt-detail-001",
      "eventId": "evt-detail-001",
      "status": "dead",
      "attempts": 3,
      "lastError": "simulated failure on attempt 3",
      "retries": 0,
      "attemptsSinceRetry": 3
    }
  ],
  "approvals": [],
  "outbox": []
}
```

For that fixture, the exact raw success body is:

```json
{"event":{"id":"evt-detail-001","type":"meeting","payload":{"to":"test@example.com","subject":"Detail fixture"},"receivedAt":"2026-07-28T12:00:00.000Z"},"job":{"id":"job-evt-detail-001","eventId":"evt-detail-001","status":"dead","attempts":3,"lastError":"simulated failure on attempt 3","retries":0,"attemptsSinceRetry":3}}
```

The acceptance suite uses localhost port `4761`, the existing default
allowlist `test@example.com`, and the unknown ID `evt-detail-missing`. It
captures both the parsed `/api/state` value and exact `state.json` bytes before
the detail requests, then compares both after the requests.

### UI, topology, and file boundaries

- UI selectors: none. This ticket adds no UI or `GET /` markup.
- Topology: same-origin HTTP on the existing Relay server. The route adds no
  authentication, cookies, CORS headers or origins, credentials mode, domains,
  or preview-deploy behavior.
- External effects: none. This `GET` route cannot approve an action or write an
  outbox item, so `External: no` remains correct and no `external` label is
  required.
- Builder file: `app/server.js` only.
- Test-author file: `app/tests/event-detail.test.js` only.
- `app/tests/conformance.test.js` is a read-only fixture/style seam and is not
  edited. `app/package.json`, dependency lockfiles, `factory/KIT_PIN`, route
  plans, ledger, ticket state, and other Factory controls are not changed by
  Test-author or Builder.
- Verification commands:
  `node --test app/tests/event-detail.test.js`,
  `npm test --prefix app`, and
  `.github/scripts/test-immutability-check.sh`.

If version 1 proves wrong during test-authoring or build, do not edit it in
place: return T-166 to Ready and append a newly planned contract version plus
the reason to this ticket.

## Contract coverage

- Criterion 1 covers the method/path, exact fixture, success status/header,
  success envelope, nested stored-record shapes, field order, and relation.
- Criterion 2 covers the unknown-ID fixture and exact error status/header/body.
- Criterion 3 covers every read-only and durable-state guarantee.
- Criterion 4 covers all Test-author and Builder file boundaries, commit
  ordering, dependency metadata, and Factory-control exclusions.
- There is no UI selector, auth, cookie, CORS, cross-origin, external-send, or
  preview topology element to exercise.

## Ambiguity log

- **Successful response envelope:** `SPEC.md` defines event intake, job
  creation, persistence, and aggregate state visibility, but not a detail
  response. The prior planning attempt correctly identified that
  “its stored event and related job” allowed incompatible envelopes. Resolved
  by the 2026-07-28
  [detail-route ruling](../rulings.md), which freezes exactly
  `{"event":<stored event>,"job":<stored related job>}`.
- **Related-job cardinality and malformed state:** the route needs one related
  job but the ticket did not define behavior for hand-edited state with no
  relation. Resolved from
  [`SPEC.md` behavior 1-2](../../SPEC.md#behavior-spec): every accepted event
  creates one job and duplicate delivery creates no second job. The contract
  covers valid product state; manually corrupted state is explicitly out of
  scope.
- **Meaning of read-only:** “does not change durable state” did not name an
  observable. Resolved from
  [`SPEC.md` behavior 6](../../SPEC.md#behavior-spec) and the current
  `DATA_DIR/state.json` persistence implementation by freezing both a parsed
  `/api/state` equality check and a byte-for-byte state-file equality check.
- **Cross-ticket file ownership:** the repository-wide sibling-ticket grep
  found repeated qualification tickets declaring `app/server.js`, and the
  currently leased T-167, T-168, and T-169 branches each declare that same
  Builder file. Their endpoint paths and Test-author files are distinct. The
  2026-07-28 [ruling](../rulings.md) explicitly permits these four additive
  detail routes to share ownership while protected publication remains
  serialized and each later branch refreshes from protected `main`.
- **Deploy and trust topology:** inspection of `app/server.js`,
  `Authentication-Seams: none`, and the requested read-only path found no
  cross-origin, authentication, cookie, CORS, credential, domain, or
  preview-deploy behavior to specify. The contract therefore freezes this as a
  same-origin, unauthenticated route with no topology changes.
- **External classification:** unlike approval execution, this route only
  reads event/job state. It cannot reach the outbox or perform a send, so the
  ticket does not receive the `external` label.

## Planning log

- 2026-07-28: Planner froze contract version 1 after applying the existing
  detail-route ruling, checking all sibling ticket records and every other
  leased ticket branch (T-167 through T-169), auditing deploy topology, and
  mapping every contract element to a mechanically checkable criterion.

## Spec lint — 2026-07-28 (spec-linter v6, round 1)

### Contract-element coverage

| Contract element | Positive | Failure/empty | Duplicate | Out of scope |
|---|---|---|---|---|
| Route match `GET /api/events/:id` (`^/api/events/([^/]+)$`) | C1 | C2 (unknown ID); non-matching paths keep the existing generic 404 | n/a — idempotent GET, re-request covered by C3's sequence | — |
| Success envelope: exactly `{"event":…,"job":…}`, keys/order, records verbatim | C1 (byte-exact raw body given in contract) | — | — | — |
| Related-job lookup (`job.eventId === event.id`) | C1 | Corrupted state with event but no job | — | Explicitly out of scope per SPEC behavior 1-2 invariant |
| Unknown-event outcome: 404, byte body `{"error":"no such event"}` | — | C2 | — | — |
| `Content-Type: application/json` on both outcomes | C1, C2 | — | — | — |
| Read-only guarantees (no state mutation, no `persist()`, no worker/approval/outbox/external trigger) | C3 (parsed `/api/state` + `state.json` byte snapshots) | — | C3 exercises both request kinds against one snapshot pair | — |
| Fixture (isolated `DATA_DIR`, dead job, port 4761, default allowlist) | C1–C3 setup | — | — | — |
| File/commit boundaries, no dependency or factory-control changes | C4 | — | — | — |
| UI, auth, cookies, CORS, credentials, preview topology, external effects | — | — | — | Contract freezes all as none/unchanged; nothing to exercise |

### Findings

1. SPEC-WARN: the ambiguity log and planning log cite the 2026-07-28
   detail-route ruling as authorizing concurrent `app/server.js` ownership for
   T-166–T-169, but the ruling's parenthetical labels the tickets "T-162
   through T-165"; the four route paths and the frozen envelope match the
   ruling exactly, so this is a citation-label drift — recommend the operator
   or planner update `factory/rulings.md` to name the current ticket IDs.
2. SPEC-WARN: the contract fixes behavior only for pathnames matching
   `^/api/events/([^/]+)$`; the Test-author may optionally add one
   representative check that a non-matching sibling path (e.g.
   `GET /api/events/`) still returns the existing generic
   `{"error":"not found"}` — a defensive test with no product-behavior change.

Verification against repository state: the frozen exact success body equals
`JSON.stringify` of the fixture records through the existing `json()` response
helper (insertion-order keys, no whitespace); the fixture's `dead` job means
the worker tick never calls `persist()`, so criterion 3's byte-for-byte
`state.json` comparison is implementable as written; the frozen envelope
matches the recorded ruling for `GET /api/events/:id`; SPEC.md behaviors 1-2,
6, and 7 and AGENTS.md boundaries are consistent with the description,
criteria, and contract. Every criterion is pass/fail decidable by a test or
git inspection.

SPEC-LINT: PASS

## Test-author comment — 2026-07-28

Failing acceptance tests committed as `app/tests/event-detail.test.js`
(commit 9cf08ff, before any implementation commit). One test per criterion:

1. `1. GET /api/events/evt-detail-001 returns 200 JSON with exactly the frozen {event,job} body`
   — asserts status, `Content-Type: application/json`, the byte-for-byte
   frozen success body, exact top-level keys `event` then `job`, and
   `job.eventId === event.id === "evt-detail-001"`.
2. `2. GET /api/events/evt-detail-missing returns 404 JSON with exact body {"error":"no such event"}`
   — asserts status, header, and the byte-for-byte frozen error body.
3. `3. one successful and one unknown-event detail request leave /api/state and state.json bytes unchanged`
   — snapshots parsed `/api/state` and exact `state.json` bytes before the
   requests, requires the success request to return 200, and compares both
   snapshots after.

Confirmed all three fail for the right reason against the current server:
the route is missing, so the success path returns 404 and the unknown-ID
path returns the generic `{"error":"not found"}` body. No setup errors; the
pre-existing 35 tests still pass and the test-immutability check holds.

Not expressible as a runtime test (demo-check items for the Narrator's
bundle): criterion 4 — the acceptance-test commit adds only
`app/tests/event-detail.test.js` and precedes every implementation commit;
implementation commits modify only `app/server.js`; no dependency-metadata
or Factory-control changes on the branch. Verify by git inspection at
bundle time.

Spec-lint SPEC-WARN 2's optional non-matching-path check (e.g.
`GET /api/events/`) was not added, keeping the suite to exactly one test
per criterion against the frozen contract.
Reviewer round 1 signed detail:

> Worktree clean, HEAD unchanged at `8cf4a11`.
>
> ## Verdict: APPROVE
>
> **What I checked and why it passes:**
>
> **Q1 — Test adequacy.** `app/tests/event-detail.test.js` has one test per criterion:
> - **C1**: asserts status 200, `Content-Type`, and a byte-for-byte match of the frozen success body — this catches wrong key order, added/renamed/omitted fields, or non-verbatim nested records, not just "some JSON came back." Also independently asserts `Object.keys` order and `job.eventId === event.id`.
> - **C2**: byte-for-byte 404 body — catches fallback to the generic `{"error":"not found"}` or a wrong status.
> - **C3**: snapshots both parsed `/api/state` and raw `state.json` bytes before/after one success + one unknown request — catches any stray mutation, `persist()` call, or worker/outbox side effect.
> - **C4** (commit ordering / file boundaries / no dependency or factory-control drift) is correctly identified as not runtime-testable and left as a git-inspection item rather than forced into a bogus test — not a planning defect, since it's a git-property, not a subjective one. I independently verified it: test-author commit `9cf08ff` touches only `app/tests/event-detail.test.js` and precedes builder commit `27d38ec`, which touches only `app/server.js`; no `package.json`/lockfile/`KIT_PIN` changes anywhere on the branch.
>
> Ran the suite: all 38 tests pass (3 new + 35 pre-existing), and `.github/scripts/test-immutability-check.sh` holds. Manually traced the implementation against each assertion — no dodge: the route does a real `state.events.find` / `state.jobs.find` lookup, not a hardcoded fixture echo.
>
> **Q2 — Spec/convention conformance.** `app/server.js` change is exactly the frozen contract: regex `^/api/events/([^/]+)$`, 404 with the exact body when unmatched, `{event, job}` via the existing `json()` helper (preserves key order, sets `Content-Type`), no `persist()` call, no new state field, no auth/CORS/topology addition — no scope creep. Route is inserted before `/health` and doesn't collide with any other path pattern.
>
> Structural pass: no SQL, no shared-state race (single-threaded, read-only handler, no check-then-act), no LLM-output handling, no shell invocation, no new enum/status value introduced — none of the five structural categories apply to this diff.
>
> One pre-existing item, not new: the spec-linter already flagged (SPEC-WARN 1) that `rulings.md` mislabels the concurrent-ownership ticket range as "T-162 through T-165" instead of T-166–T-169. That's a planning-doc drift already logged on the ticket for the operator/planner, not a reviewer blocker.That was the app test suite I ran as part of the T-166 review — already reported above: all 38 tests passed, including the 3 new event-detail tests. No follow-up needed; the review verdict stands as **APPROVE**.

reviewer round 1: APPROVE
