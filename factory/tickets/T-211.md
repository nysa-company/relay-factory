# T-211 — count Relay approvals for one recipient offline

Kit-SHA: 81b21b6d1abca7a7bde0425fbae941fcb9c112ed
State: Ready
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-211
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/approval-detail.test.js
Authentication-Seams: none
Builder ownership: app/tools/approval-recipient-count.js only
Test-author ownership: app/tests/approval-recipient-count.test.js only

## Description

Add a zero-dependency, read-only Node command that counts persisted Relay
approvals whose stored action `to` value exactly matches one operator-supplied
recipient while the service is offline.

## Acceptance criteria

1. `node app/tools/approval-recipient-count.js <state-file> <recipient>` exits
   zero and emits deterministic compact JSON with exactly one
   `approvalsForRecipient` nonnegative integer.
2. A valid state with no exact recipient match returns
   `{"approvalsForRecipient":0}` followed by one newline.
3. Missing arguments and missing, unreadable, malformed, or structurally
   invalid state are rejected with fixed redacted errors that reveal no path,
   recipient, approval ID, action, reason, or stored content; complete approval
   validation precedes counting.
4. Every invocation is offline, read-only, and self-terminating; input bytes,
   mode, timestamps, and sibling directory entries remain unchanged.
5. Complete acceptance tests are committed before implementation. Targeted,
   full app, and immutability checks pass without dependency, server, schema,
   package-manifest, lockfile, or Factory-control changes.

## Coordination

This ticket owns only its two new `app/` paths and is independent of T-210 and
T-212.
