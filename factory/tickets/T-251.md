# T-251 — count Relay outbox entries by approval and recipient offline

Kit-SHA: cd1a6798a5d0968511038f7461016cc77d4a8a32
State: Planning
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-251
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/outbox-detail.test.js, app/tests/outbox-recipient-count.test.js
Authentication-Seams: none
Builder ownership: app/tools/outbox-approval-recipient-count.js only
Test-author ownership: app/tests/outbox-approval-recipient-count.test.js only

## Description

Add a zero-dependency, read-only Node command that counts persisted Relay
outbox entries whose approval identifier and recipient exactly match supplied
strings. This supports narrow offline delivery checks without starting Relay
or exposing message bodies.

## Acceptance criteria

1. `node app/tools/outbox-approval-recipient-count.js <state-file> <approval-id> <recipient>`
   emits compact JSON containing exactly one `outboxWithApprovalAndRecipient`
   integer.
2. Approval identifier and recipient must be non-empty strings and match stored
   `approvalId` and `to` values exactly, without trimming or coercion.
3. Invalid arguments are rejected before file access. Unreadable, malformed,
   or structurally invalid state returns fixed redacted errors; complete outbox
   validation precedes counting.
4. Every invocation is offline, read-only, deterministic, and self-terminating.
5. Tests precede implementation. Targeted, full app, and immutability checks
   pass without dependency, server, schema, manifest, or Factory changes.

## Coordination

This ticket owns only its two new paths and is independent of T-249 and T-250.
