# T-251 — count Relay outbox entries by approval and recipient offline

Kit-SHA: cd1a6798a5d0968511038f7461016cc77d4a8a32
State: Blocked-Escalated
Resume-State: Planning
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-251
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/outbox-detail.test.js, app/tests/outbox-recipient-count.test.js
Authentication-Seams: none
Builder ownership: app/tools/outbox-approval-recipient-count.js only
Test-author ownership: app/tests/outbox-approval-recipient-count.test.js only

## Description

Add a zero-dependency, read-only Node command that counts persisted Relay
outbox entries whose approval identifier and recipient exactly match supplied
strings. This supports narrow offline delivery checks without starting Relay
or exposing message bodies.

## Acceptance criteria

1. `node app/tools/outbox-approval-recipient-count.js <state-file> <approval-id> <recipient>`
   emits compact JSON containing exactly one `outboxWithApprovalAndRecipient`
   integer.
2. Approval identifier and recipient must be non-empty strings and match stored
   `approvalId` and `to` values exactly, without trimming or coercion.
3. Invalid arguments are rejected before file access. Unreadable, malformed,
   or structurally invalid state returns fixed redacted errors; complete outbox
   validation precedes counting.
4. Every invocation is offline, read-only, deterministic, and self-terminating.
5. Tests precede implementation. Targeted, full app, and immutability checks
   pass without dependency, server, schema, manifest, or Factory changes.

## Coordination

This ticket owns only its two new paths and is independent of T-249 and T-250.

## Planning log

- 2026-08-25: Planner stopped before writing a spec'd description or frozen
  contract. A sibling-ticket grep of `factory/tickets/` for overlapping
  Builder/Test-author ownership (Criteria checklist item 5) found that
  Backlog ticket [T-243](T-243.md) — "count Relay outbox entries by approval
  ID and recipient offline" — declares the exact same Builder ownership
  (`app/tools/outbox-approval-recipient-count.js`) and Test-author ownership
  (`app/tests/outbox-approval-recipient-count.test.js`) as this ticket. T-243's
  description and acceptance criteria are near-identical to T-251's (compare
  "approval ID" vs. "approval identifier" and "exposing receipt contents" vs.
  "exposing message bodies"), and T-243's own Coordination note ("independent
  of T-244 and T-245") shows no awareness of T-251. `git branch -a` finds no
  `ticket/T-243` branch, local or remote — T-243 has never been readied or
  built. Neither the product docs nor
  [`factory/rulings.md`](../rulings.md) addresses duplicate tickets claiming
  the same implementation surface, and this mirrors the T-179/T-184 precedent
  recorded on [T-184](T-184.md#ambiguity-log). Exact operator question:
  **Should T-243 be canceled/closed as a duplicate of T-251 (which already
  holds an operator Ready receipt and is in Planning), or should T-243 be kept
  and reassigned distinct Builder and Test-author paths with a separately
  specified command interface?**

ROLE-ESCALATE: CONTRACT-BLOCKED

OPERATOR ANSWER: T-251 is the canonical ticket for this qualification; T-243 remains an unselected Backlog duplicate and must not proceed concurrently, so freeze T-251's contract on its existing declared paths without changing T-243.
OPERATOR ANSWER RECEIPT: d31bd57190368be823e4c96017b46e7a928da989d8994972ffc476fa0c53b690

OPERATOR RESUME: planner
OPERATOR RESUME RECEIPT: d31bd57190368be823e4c96017b46e7a928da989d8994972ffc476fa0c53b690
