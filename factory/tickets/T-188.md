# T-188 — inspect one Relay job link offline

Kit-SHA: e85890e4e8ae98038b1eaaee5b6bb239bc874052
State: Planning
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-188
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/job-detail.test.js
Authentication-Seams: none
Builder ownership: app/tools/job-link.js only
Test-author ownership: app/tests/job-link.test.js only

## Spec'd description

Add `app/tools/job-link.js`, a zero-dependency Node 18+ command that reads one
persisted Relay state file without starting the service, selects one job by
case-sensitive exact stored ID, and writes only that job's stored `id`,
`eventId`, and `status` under the keys `jobId`, `eventId`, and `status`. The
command validates the complete event/job relationship before lookup, so it
does not report a confident link from corrupt or ambiguous state. It never
prints event payloads, job errors, approval actions or reasons, outbox message
content, or unrelated state.

This gives an operator a deterministic offline job-to-event link check while
preserving Relay's state and process boundaries. It adds no endpoint, server
behavior, UI, schema, worker behavior, package, dependency, lockfile,
authentication path, deployment topology, external send, or Factory-control
change.

Product-doc sources:

- [`SPEC.md` behavior 1](../../SPEC.md#behavior-spec) defines exact event-ID
  intake and one enqueued job per accepted event.
- [`SPEC.md` behavior 2](../../SPEC.md#behavior-spec) defines the job lifecycle
  and the `pending`, `done`, and `dead` states.
- [`SPEC.md` behavior 6](../../SPEC.md#behavior-spec) makes the persisted state
  file Relay's durable source of truth.
- [`SPEC.md` behavior 7](../../SPEC.md#behavior-spec) makes events and jobs
  operator-visible collections, while
  [`SPEC.md` conformance cases 2-4 and 7](../../SPEC.md#test-cases-the-conformance-suite)
  establish one job per event, all lifecycle outcomes, and persisted recovery.
- The current [`app/server.js` model](../../app/server.js) stores the
  relationship in `job.eventId` and resolves job detail through that stored
  field.
- [`AGENTS.md` Boundaries](../../AGENTS.md#boundaries) keeps `app/`
  zero-dependency. [`AGENTS.md` Workflow and Verification](../../AGENTS.md)
  and [`docs/git-flow.md`](../../docs/git-flow.md) define test-first ordering
  and later repository verification.

## Acceptance criteria

1. Given Frozen Fixture A, each of two serialized invocations of
   `node app/tools/job-link.js <fixture-a-path> persisted-job-target` exits
   `0`, writes zero bytes to stderr, and writes exactly
   `{"jobId":"persisted-job-target","eventId":"event-link-target","status":"dead"}\n`
   to stdout. Neither stream contains any frozen sensitive sentinel. After
   each invocation, the fixture's exact bytes and BigInt `mode`, `size`,
   `mtimeNs`, and `ctimeNs`, and its parent directory's same four stat fields
   plus sorted entry names, are unchanged.
2. Separate Frozen Fixture A invocations for `job-event-link-pending` and
   `job-event-link-done` each exit `0`, write zero bytes to stderr, and write,
   respectively, exactly
   `{"jobId":"job-event-link-pending","eventId":"event-link-pending","status":"pending"}\n`
   and
   `{"jobId":"job-event-link-done","eventId":"event-link-done","status":"done"}\n`
   to stdout. Neither stream contains a frozen sensitive sentinel, and the
   file and parent observations from criterion 1 remain unchanged.
3. Separate Frozen Fixture A invocations for `job-link-missing`,
   `PERSISTED-JOB-TARGET`, and `persisted-job-target ` each exit `1`, write
   zero bytes to stdout, and write exactly `job-link: job not found\n` to
   stderr without echoing the supplied ID or a frozen sensitive sentinel.
   Each unknown ID is byte-distinct from every valid Fixture A job ID, and the
   file and parent observations from criterion 1 remain unchanged.
4. Separate zero-, one-, three-, and four-argument invocations after the
   script path exit `2`, write zero bytes to stdout, and write exactly
   `usage: node app/tools/job-link.js <state-file> <job-id>\n` to stderr. Each
   case owns a fresh empty temporary parent; the one-argument case supplies
   `<parent>/arity-state-does-not-exist.json`, and the three- and four-argument
   cases add `persisted-job-target` and one or two `extra` operands. Every
   absent path remains absent, and each parent's BigInt `mode`, `size`,
   `mtimeNs`, `ctimeNs`, and sorted entries remain unchanged.
5. Using `persisted-job-target` as the job-ID operand, a nonexistent
   `<parent>/PATH-SENTINEL-missing-state.json` and an existing empty
   `<parent>/DIRECTORY-PATH-SENTINEL` directory each exit `1`, write zero
   bytes to stdout, and write exactly
   `job-link: state file is unreadable\n` to stderr. The absent path stays
   absent; the directory stays empty; every existing directory and parent
   retains the observations from criterion 1; and neither stream contains a
   frozen path sentinel.
6. Using `persisted-job-target` as the job-ID operand, the frozen malformed
   fixture exits `1`, writes zero bytes to stdout, and writes exactly
   `job-link: state file is not valid JSON\n` to stderr. Neither stream
   contains `MALFORMED-STATE-SENTINEL`, and the fixture and parent observations
   from criterion 1 remain unchanged.
7. Using `persisted-job-target` as the job-ID operand, every frozen
   structural-invalid fixture S1-S28 exits `1`, writes zero bytes to stdout,
   and writes exactly
   `job-link: state file has invalid Relay state\n` to stderr. S28 contains
   the individually valid requested job plus an invalid sibling and produces
   the structural error rather than the frozen target success line, proving
   complete validation precedes lookup. Before any invocation, the test
   proves Frozen Fixture A, the malformed fixture, and S1-S28 are pairwise
   byte-distinct. Every fixture and parent retains the observations from
   criterion 1.
8. Every child invocation in criteria 1-7 uses a `10_000` ms timeout and
   completes with `error === undefined` and `signal === null`. Static
   inspection proves that the Builder file's complete CommonJS
   module-specifier set is exactly `node:fs`, its only `node:fs` operation is
   `readFileSync`, and its source contains none of `server.js`, `fetch(`,
   `WebSocket`, `node:http`, `node:https`, `node:net`, `node:child_process`,
   `setTimeout`, or `setInterval`. The test file's complete module-specifier
   set is exactly `node:test`, `node:assert`, `node:child_process`, `node:fs`,
   `node:path`, and `node:os`.
9. Git history places the complete test-author commit before the Builder
   commit. Apart from role-owned ticket appends, the test-author creates only
   `app/tests/job-link.test.js`, and the Builder creates only
   `app/tools/job-link.js`. Existing application files, protected tests,
   `app/server.js`, `app/package.json`, lockfiles, dependencies, persisted
   schema, worker behavior, `factory/KIT_PIN`, and Factory controls remain
   unchanged. After implementation,
   `node --test app/tests/job-link.test.js`, `npm test --prefix app`, and
   `.github/scripts/test-immutability-check.sh` each exit `0`.

## Frozen contract — version 1

- **Freeze result:** PASS. Contract version 1 is frozen.

This is the exact interface for the test-author and Builder. If it proves
wrong, return T-188 to Ready and append a higher-numbered frozen contract
without editing or removing version 1.

### Command interface and output

- Repository-root invocation is exactly
  `node app/tools/job-link.js <state-file> <job-id>`.
- Exactly two positional arguments after the script path are accepted. Any
  other arity exits `2`, writes zero bytes to stdout, writes exactly
  `usage: node app/tools/job-link.js <state-file> <job-id>\n` to stderr, and
  performs no state-file read.
- After arity validation, the command synchronously reads `<state-file>` as
  UTF-8, parses and validates it in memory, selects the job whose stored `id`
  is strictly equal to `<job-id>`, writes its result, and exits.
- Lookup is exact, case-sensitive JavaScript string equality. The command does
  not trim, normalize, decode, case-fold, prefix-match, or substring-match
  either value.
- Success exits `0`, writes zero stderr bytes, and writes one compact JSON
  object followed by exactly one LF. Its only keys, in order, are `jobId`,
  `eventId`, and `status`. Each value copies the selected job's stored `id`,
  `eventId`, or `status` without transformation.
- The exact success shape is
  `{"jobId":<stored nonempty string>,"eventId":<stored nonempty string>,"status":<"pending"|"done"|"dead">}\n`.
- A link-valid state with no exact job match exits `1`, writes zero bytes to
  stdout, and writes exactly `job-link: job not found\n` to stderr. The error
  never echoes the supplied ID.
- The command does not import or start `app/server.js`, open a socket, issue a
  request, spawn another process, schedule a timer, or write, append, create,
  rename, remove, chmod, truncate, or delete any path.
- No endpoint, HTTP response shape, UI route, selector, accessible name, or
  global page-shell text is introduced.

### Link-valid state and error precedence

A link-valid state is a parsed, non-null, non-array JSON object with own
`events`, `jobs`, `approvals`, and `outbox` properties whose values are arrays.
Additional top-level properties are allowed and ignored.

Every `events` element must be a non-null, non-array object with an own,
nonempty string `id`. Event IDs must be pairwise distinct. Other event
properties are opaque.

Every `jobs` element must be a non-null, non-array object with:

- an own, nonempty string `id`, pairwise distinct across jobs;
- an own, nonempty string `eventId` equal to exactly one stored event `id`,
  with no two jobs sharing one `eventId`; and
- an own string `status` equal to exactly `pending`, `done`, or `dead`.

Other job properties are opaque. Event records need not have a related job.
Entries in `approvals` and `outbox` are wholly opaque because this command
does not consume them. The command validates all events, jobs, ID uniqueness,
and links before searching for `<job-id>`.

After arity validation, failures have this exact precedence:

1. A nonexistent path, non-file path, or any filesystem read failure exits
   `1`, writes zero bytes to stdout, and writes exactly
   `job-link: state file is unreadable\n` to stderr.
2. Readable bytes rejected by `JSON.parse` exit `1`, write zero bytes to
   stdout, and write exactly
   `job-link: state file is not valid JSON\n` to stderr.
3. Parsed JSON that is not link-valid exits `1`, writes zero bytes to stdout,
   and writes exactly
   `job-link: state file has invalid Relay state\n` to stderr.
4. Only link-valid state reaches exact-ID lookup and the frozen not-found or
   success result.

Every error is one fixed LF-terminated line. No error includes the state path,
supplied ID, operating-system error, parser text, input bytes, stack trace,
event payload, job error, approval action or reason, outbox content, or other
state content.

### Frozen Fixture A — stored relationship, all statuses, and privacy

`app/tests/job-link.test.js` writes this exact parsed value to an isolated
temporary `state.json` using `JSON.stringify(value, null, 2) + "\n"`:

```json
{
  "events": [
    {
      "id": "event-link-pending",
      "type": "meeting",
      "payload": {
        "private": "PENDING-EVENT-PAYLOAD-SENTINEL"
      },
      "receivedAt": "2026-08-23T12:00:00.000Z"
    },
    {
      "id": "event-link-target",
      "type": "email",
      "payload": {
        "private": "TARGET-EVENT-PAYLOAD-SENTINEL"
      },
      "receivedAt": "2026-08-23T12:00:01.000Z"
    },
    {
      "id": "event-link-done",
      "type": "generic",
      "payload": {
        "private": "DONE-EVENT-PAYLOAD-SENTINEL"
      },
      "receivedAt": "2026-08-23T12:00:02.000Z"
    }
  ],
  "jobs": [
    {
      "id": "job-event-link-pending",
      "eventId": "event-link-pending",
      "status": "pending",
      "attempts": 0,
      "lastError": null,
      "retries": 0,
      "attemptsSinceRetry": 0
    },
    {
      "id": "persisted-job-target",
      "eventId": "event-link-target",
      "status": "dead",
      "attempts": 3,
      "lastError": "JOB-ERROR-SENTINEL",
      "retries": 0,
      "attemptsSinceRetry": 3
    },
    {
      "id": "job-event-link-done",
      "eventId": "event-link-done",
      "status": "done",
      "attempts": 1,
      "lastError": null,
      "retries": 0,
      "attemptsSinceRetry": 1,
      "diagnostic": "JOB-DIAGNOSTIC-SENTINEL"
    }
  ],
  "approvals": [
    {
      "id": "appr-event-link-target",
      "jobId": "persisted-job-target",
      "action": {
        "to": "APPROVAL-TO-SENTINEL",
        "subject": "APPROVAL-SUBJECT-SENTINEL",
        "body": "APPROVAL-BODY-SENTINEL"
      },
      "status": "rejected",
      "reason": "APPROVAL-REASON-SENTINEL",
      "proposedAt": "2026-08-23T12:00:03.000Z"
    },
    42
  ],
  "outbox": [
    {
      "to": "OUTBOX-TO-SENTINEL",
      "subject": "OUTBOX-SUBJECT-SENTINEL",
      "body": "OUTBOX-BODY-SENTINEL",
      "approvalId": "appr-event-link-target",
      "sentAt": "2026-08-23T12:00:04.000Z"
    },
    null
  ],
  "metadata": {
    "private": "METADATA-SENTINEL"
  }
}
```

The target job deliberately has stored ID `persisted-job-target`, not
`job-event-link-target`. Its output therefore distinguishes copying the stored
`eventId` relationship from deriving the event ID by stripping a `job-`
prefix. The three exact stdout byte strings are:

```text
{"jobId":"job-event-link-pending","eventId":"event-link-pending","status":"pending"}
{"jobId":"persisted-job-target","eventId":"event-link-target","status":"dead"}
{"jobId":"job-event-link-done","eventId":"event-link-done","status":"done"}
```

Each line independently ends with one LF. The frozen sensitive sentinel set is
exactly:

1. `PENDING-EVENT-PAYLOAD-SENTINEL`
2. `TARGET-EVENT-PAYLOAD-SENTINEL`
3. `DONE-EVENT-PAYLOAD-SENTINEL`
4. `JOB-ERROR-SENTINEL`
5. `JOB-DIAGNOSTIC-SENTINEL`
6. `APPROVAL-TO-SENTINEL`
7. `APPROVAL-SUBJECT-SENTINEL`
8. `APPROVAL-BODY-SENTINEL`
9. `APPROVAL-REASON-SENTINEL`
10. `OUTBOX-TO-SENTINEL`
11. `OUTBOX-SUBJECT-SENTINEL`
12. `OUTBOX-BODY-SENTINEL`
13. `METADATA-SENTINEL`

The scalar approval and null outbox item, the deliberately cross-lifecycle
privacy records, additive job field, and top-level `metadata` are opaque
probes, not claims that the server generates those combinations.

The unknown IDs are literal strings. `PERSISTED-JOB-TARGET` is the exact
uppercase form of the valid target ID, `persisted-job-target ` has one trailing
U+0020 SPACE, and `job-link-missing` is independently chosen. The test asserts
that all three are byte-distinct from every valid Fixture A job ID.

### Frozen failure fixtures

- The missing path is
  `<temporary-parent>/PATH-SENTINEL-missing-state.json` and does not exist.
- The non-file path is
  `<temporary-parent>/DIRECTORY-PATH-SENTINEL` and is an empty directory.
- The malformed fixture contains exactly
  `{"events":[],"private":"MALFORMED-STATE-SENTINEL"\n`.

Each structural-invalid fixture below is a direct UTF-8 literal ending in one
LF:

| ID | Violated rule | Exact bytes before final LF |
|---|---|---|
| S1 | root is null | `null` |
| S2 | root is an array | `[]` |
| S3 | missing own `outbox` | `{"events":[],"jobs":[],"approvals":[]}` |
| S4 | `events` is not an array | `{"events":{},"jobs":[],"approvals":[],"outbox":[]}` |
| S5 | `jobs` is not an array | `{"events":[],"jobs":{},"approvals":[],"outbox":[]}` |
| S6 | `approvals` is not an array | `{"events":[],"jobs":[],"approvals":{},"outbox":[]}` |
| S7 | `outbox` is not an array | `{"events":[],"jobs":[],"approvals":[],"outbox":{}}` |
| S8 | event is null | `{"events":[null],"jobs":[],"approvals":[],"outbox":[]}` |
| S9 | event is an array | `{"events":[[]],"jobs":[],"approvals":[],"outbox":[]}` |
| S10 | event lacks own `id` | `{"events":[{}],"jobs":[],"approvals":[],"outbox":[]}` |
| S11 | event `id` is empty | `{"events":[{"id":""}],"jobs":[],"approvals":[],"outbox":[]}` |
| S12 | event `id` is not a string | `{"events":[{"id":7}],"jobs":[],"approvals":[],"outbox":[]}` |
| S13 | event IDs are duplicated | `{"events":[{"id":"event-a"},{"id":"event-a"}],"jobs":[],"approvals":[],"outbox":[]}` |
| S14 | job is null | `{"events":[],"jobs":[null],"approvals":[],"outbox":[]}` |
| S15 | job is an array | `{"events":[],"jobs":[[]],"approvals":[],"outbox":[]}` |
| S16 | job lacks own `id` | `{"events":[{"id":"event-a"}],"jobs":[{"eventId":"event-a","status":"pending"}],"approvals":[],"outbox":[]}` |
| S17 | job `id` is empty | `{"events":[{"id":"event-a"}],"jobs":[{"id":"","eventId":"event-a","status":"pending"}],"approvals":[],"outbox":[]}` |
| S18 | job `id` is not a string | `{"events":[{"id":"event-a"}],"jobs":[{"id":7,"eventId":"event-a","status":"pending"}],"approvals":[],"outbox":[]}` |
| S19 | job lacks own `eventId` | `{"events":[{"id":"event-a"}],"jobs":[{"id":"job-a","status":"pending"}],"approvals":[],"outbox":[]}` |
| S20 | job `eventId` is empty | `{"events":[{"id":"event-a"}],"jobs":[{"id":"job-a","eventId":"","status":"pending"}],"approvals":[],"outbox":[]}` |
| S21 | job `eventId` is not a string | `{"events":[{"id":"event-a"}],"jobs":[{"id":"job-a","eventId":7,"status":"pending"}],"approvals":[],"outbox":[]}` |
| S22 | job lacks own `status` | `{"events":[{"id":"event-a"}],"jobs":[{"id":"job-a","eventId":"event-a"}],"approvals":[],"outbox":[]}` |
| S23 | job `status` is not a string | `{"events":[{"id":"event-a"}],"jobs":[{"id":"job-a","eventId":"event-a","status":7}],"approvals":[],"outbox":[]}` |
| S24 | job status casing is invalid | `{"events":[{"id":"event-a"}],"jobs":[{"id":"job-a","eventId":"event-a","status":"DEAD"}],"approvals":[],"outbox":[]}` |
| S25 | job IDs are duplicated | `{"events":[{"id":"event-a"},{"id":"event-b"}],"jobs":[{"id":"job-a","eventId":"event-a","status":"pending"},{"id":"job-a","eventId":"event-b","status":"done"}],"approvals":[],"outbox":[]}` |
| S26 | two jobs reference one event | `{"events":[{"id":"event-a"}],"jobs":[{"id":"job-a","eventId":"event-a","status":"pending"},{"id":"job-b","eventId":"event-a","status":"done"}],"approvals":[],"outbox":[]}` |
| S27 | job references no stored event | `{"events":[],"jobs":[{"id":"job-a","eventId":"event-a","status":"pending"}],"approvals":[],"outbox":[]}` |
| S28 | valid requested job has invalid sibling | `{"events":[{"id":"event-link-target"},{"id":"event-b"}],"jobs":[{"id":"persisted-job-target","eventId":"event-link-target","status":"dead"},{"id":"job-b","eventId":"event-b"}],"approvals":[],"outbox":[]}` |

Fixture A, the malformed fixture, and S1-S28 are stored as literal bytes, not
created by mutating another fixture. Before invocation, the test compares all
30 serialized values pairwise and requires every pair to be byte-distinct.
S24 freezes the exact result of the only casing-derived invalid value as
uppercase `DEAD`; it is byte-distinct from Fixture A and every other fixture.
No normalization mutation creates a serialized fixture.

### File locations, lifecycle, protected boundaries, and topology

- Builder ownership is creation of `app/tools/job-link.js` only. Its only
  module specifier is `node:fs`, and its only `node:fs` operation is
  `readFileSync`.
- Test-author ownership is creation of `app/tests/job-link.test.js` only. Its
  module specifiers are exactly `node:test`, `node:assert`,
  `node:child_process`, `node:fs`, `node:path`, and `node:os`.
- `app/tests/job-detail.test.js` is the protected relationship and fixture
  seam. It is inspected but not edited. It contains no import/export
  allowlist, exact-source assertion, source snapshot, hash, or module-specifier
  restriction affecting `app/tools/job-link.js` or `node:fs`.
- All tracked protected tests were inspected. Existing static source checks in
  `app/tests/dead-jobs.test.js` and
  `app/tests/blocked-approvals.test.js` read only their corresponding tool
  files. `app/tests/factory-contract.test.js` admits both owned paths through
  `NONVISUAL_PATHS=app/tools/,app/tests/`. `app/package.json` discovers the
  new test through `tests/*.test.js`. No protected conflict or protected-test
  edit is required; `Protected-Test-Conflicts: none` remains exact.
- Every runtime case creates a unique temporary parent, creates its required
  input before invoking the child, waits synchronously for each child to
  finish, and recursively removes the parent in `finally`. Repeated
  invocations are serialized. Tests share no path, process, port, database,
  table, parent row, dependent table, foreign key, generated sequence,
  counter, initializer, or clock. No reset or ordered relational cleanup is
  required.
- All asserted IDs, statuses, counters, and timestamps are literal fixture
  data. Generated temporary-directory names are not asserted.
- Regular-file and directory observations use
  `fs.statSync(path, { bigint: true })` for `mode`, `size`, `mtimeNs`, and
  `ctimeNs`, plus exact file bytes or sorted directory entries. Access time is
  not asserted because a read may update it under filesystem policy.
- The command is a local file-to-stdout process. There is no endpoint, UI
  selector, global shell, accessible name, auth, cookie, CORS, cross-origin
  request, preview deployment, domain, credential, or external-send behavior.
  `External: no` and `Authentication-Seams: none` remain exact.
- Targeted verification is `node --test app/tests/job-link.test.js`. Full app
  and immutability verification are `npm test --prefix app` and
  `.github/scripts/test-immutability-check.sh`.

### Protected global-text result

The pinned Factory readiness checker returned `GLOBAL TEXT PASS` separately
for the usage line, all four error lines, all three complete success output
objects, the three output property names, and the three status values. The
checker returned `READINESS PASS` for T-188. No protected global-text
collision, parser uncertainty, protected-test conflict declaration, or
Fixture-Seams expansion is required.

## Criteria checklist

- **Pass/fail and unambiguous:** criteria 1-7 freeze exact operands, process
  exits, stdout/stderr bytes, fixture bytes, sentinels, BigInt stat fields, and
  directory entries. Criterion 8 freezes timeout, signal, module, API, and
  source observations. Criterion 9 is decided from Git history, path diffs,
  and three exact command exits.
- **Coverage:** criteria 1-2 exercise every output key and allowed status,
  stored rather than derived linkage, repetition, privacy, and success-side
  read-only behavior. Criterion 3 exercises absent, case-distinct, and
  trim-distinct lookup. Criterion 4 exercises arity before read. Criteria 5-7
  exercise every error class, every structural rule, validation before lookup,
  fixture distinctness, and failure-side read-only behavior. Criteria 8-9
  cover offline termination, both role files, protected boundaries, commit
  order, prohibited edits, and every verification command.
- **No dead contract elements:** every command path, output/error shape,
  validation class, fixture, sentinel, file location, lifecycle rule, topology
  disposition, source restriction, and verification command appears in
  criteria 1-9.
- **Derived-fixture distinctness:** S24's exact uppercase `DEAD` value is
  frozen and all 30 serialized fixtures are pairwise byte-distinct. The two
  transformed-looking lookup IDs are frozen literals and checked against all
  valid IDs; they are not serialized invalid fixtures.
- **Generated-value determinism:** all expected IDs, counters, statuses, and
  timestamps are literals. No generated value, initializer, reset, or clock
  participates in an exact expectation.
- **Fixture lifecycle closure:** each case owns one temporary parent, waits for
  every child, and removes every artifact in `finally`. There is no database,
  sibling dependent table, non-cascading foreign key, or shared mutable
  resource.
- **Protected boundaries and global text:** direct inspection and the pinned
  deterministic checker admit the new paths, the sole production specifier,
  and every shell-visible literal. No check is unknown or unparsable.
- **Deploy/topology completeness:** this is a local child process over one
  file, with no HTTP, origin, authentication, cookie, CORS, deployment, UI, or
  external-send surface.

## Ambiguity log

1. **Which value defines the event relationship?** The seed names `eventId`
   but does not say whether it is derived from the job ID. Resolved from the
   stored relation used by [`app/server.js`](../../app/server.js): copy
   `job.eventId`. The target's non-derived job ID makes this observable.
2. **How much state must be structurally validated?** The product docs do not
   define a general offline validator. Resolved narrowly from
   [`SPEC.md` behaviors 1-2 and 6](../../SPEC.md#behavior-spec): validate the
   four collection arrays, all event IDs, all consumed job link/status fields,
   uniqueness, one job per event, and referential integrity. Leave
   unconsumed fields and approval/outbox entries opaque.
3. **Can lookup precede validation of unrelated records?** No. The persisted
   file is the durable source of truth under
   [`SPEC.md` behavior 6](../../SPEC.md#behavior-spec). S28 requires an invalid
   sibling to defeat an otherwise valid requested job before lookup emits a
   confident link.
4. **What does exact ID mean?** Resolved as JavaScript strict string equality
   with no trimming, normalization, decoding, prefix/substring match, or case
   folding. The case-only and trailing-space misses make the most likely
   alternatives observable.
5. **What are the exact privacy-safe failure classes?** The seed does not
   provide text, exits, or precedence. Resolved from the shipped offline CLI
   convention as usage exit `2`, operational exits `1`, fixed lines for read,
   parse, structure, and lookup, empty opposite streams, and no raw error or
   input echo.
6. **How is read-only, offline, and self-terminating behavior proved?**
   Resolved with byte/BigInt-stat/parent-entry snapshots, serialized child
   execution, a `10_000` ms timeout, and exact production source boundaries.
   Access time is excluded because reads may update it.
7. **Are derived or generated values involved?** S24 is the only
   casing-derived invalid serialized value; its exact uppercase bytes and
   pairwise distinctness are frozen. All identifiers, counters, statuses, and
   timestamps are literal; temporary-directory names are generated but never
   asserted.
8. **Does cleanup have hidden dependencies?** No database or service starts.
   Each child reads one isolated temporary artifact, exits before recursive
   `finally` teardown, and shares no parent row, dependent table, foreign key,
   port, reset, or clock.
9. **Do sibling or concurrently leased tickets overlap ownership?** A grep of
   every `factory/tickets/*.md` ownership declaration found no other owner for
   either T-188 path. The other active worktrees and both local and remote
   leased branches were inspected: T-187 owns the distinct `outbox-receipt`
   pair, and T-189 owns the distinct `approval-timestamp` pair.
10. **Do protected tests reject the planned production source or visible
    text?** No. The associated job-detail seam has no source allowlist,
    snapshot, or hash; existing source assertions are tool-specific. The
    pinned checker returned `READINESS PASS` and `GLOBAL TEXT PASS` for every
    frozen shell-visible literal, so `Protected-Test-Conflicts: none` remains
    exact.
11. **Are topology, auth, UI, or external-send details applicable?** No. This
    is a local file-to-stdout command with no network, deployment, credential,
    selector, or side effect. No `external` label is required.
12. **Does an existing operator ruling alter this contract?**
    [`factory/rulings.md`](../rulings.md) was checked before freeze. Its
    detail-route ruling does not change this offline command. Its 2026-08-10
    T-178 verification ruling confirms that targeted, full-app, and
    immutability checks are the exact ticket checks; repository baseline
    controls are not added to this frozen contract.

## Coordination

T-188 owns only `app/tools/job-link.js` and
`app/tests/job-link.test.js`. The concurrent T-187 and T-189 tickets own
distinct files, so there is no active implementation-surface conflict.

## Planning log

- 2026-08-23: Planner froze contract version 1 with exact command arity,
  stored-link semantics, all status projections, fixed privacy-safe failures,
  validation precedence, literal pairwise-distinct fixtures, read-only
  lifecycle observations, role ownership, protected-source and global-text
  results, topology disposition, and post-implementation checks. Product docs,
  current state code, rulings, every protected test, every sibling ownership
  declaration, and both other leased worktree and remote ticket branches were
  inspected. The pinned checker returned `READINESS PASS` and fourteen
  `GLOBAL TEXT PASS` results; no operator decision or conflict declaration was
  required.
- 2026-08-23: spec-lint round 1 of frozen contract version 1 against `SPEC.md`
  (behavior spec 1-2, 6-7, conformance cases 2-4 and 7), `AGENTS.md`
  (Boundaries, Workflow, Verification, repository baseline), `docs/git-flow.md`,
  `app/server.js`, `app/package.json`, every tracked file in `app/tests/`, the
  seven shipped offline commands in `app/tools/`,
  `.github/scripts/test-immutability-check.sh`, `factory/rulings.md`,
  `factory/PROJECT.env`, `factory/KIT_PIN`, every sibling ticket ownership
  declaration, and the concurrently leased `ticket/T-187` and `ticket/T-189`
  branches.

  Contract-element coverage:

  | Contract element (material behavior) | Positive | Failure / empty | Duplicate | Out of scope |
  |---|---|---|---|---|
  | Argument arity — exactly two positional operands | AC1, AC2, AC3, AC5, AC6, AC7 invoke with two operands | AC4: zero, one, three, and four operands → exit `2`, zero stdout bytes, exact usage line | the four arity cases share one frozen usage literal | five-or-more operands grouped as an equivalent permutation of the same arity rule |
  | Arity checked before any state-file read | — | AC4's one-, three-, and four-operand cases name `arity-state-does-not-exist.json` and still exit `2`, and every arity parent stays empty | the three path-bearing arity cases share one expectation | the zero-operand case has no path to read |
  | Synchronous UTF-8 read of the named path | AC1, AC2, AC3, AC6, AC7 read frozen fixtures | AC5: absent path and directory path → exit `1`, exact `job-link: state file is unreadable` | both read failures share one frozen literal | OS read-denial and symlink inputs (findings 7 and 8); invalid UTF-8 resolves to the parse class by contract |
  | `JSON.parse` rejection | — | AC6: frozen malformed fixture → exit `1`, exact `job-link: state file is not valid JSON`, no `MALFORMED-STATE-SENTINEL` in either stream | — | parse-before-structure precedence is vacuous: unparsable bytes have no structure |
  | Link-valid root — non-null, non-array object with own array `events`, `jobs`, `approvals`, `outbox`; extra top-level keys ignored | AC1, AC2, AC3 accept Fixture A, whose top-level `metadata` is ignored | AC7: S1, S2 (null and array root), S3 (missing own `outbox`), S4-S7 (each collection non-array) | S4-S7 are one rule applied per collection | missing own `events`, `jobs`, `approvals` and scalar-root rows grouped with S3 and S1-S2 (finding 9) |
  | Event element rules and pairwise-distinct event IDs | AC1-AC3: Fixture A's three distinct event IDs with opaque `type`, `payload`, `receivedAt` | AC7: S8, S9 (null and array element), S10-S12 (missing, empty, non-string `id`), S13 (duplicate `event-a`) | S10-S12 are the same rule over one field | duplicate event IDs are unreachable in real state (`server.js:38-40` idempotency) |
  | Job element rules — nonempty string `id`, `eventId`, and the exact `pending`/`done`/`dead` status enum | AC1, AC2 project all three enum values from Fixture A | AC7: S14, S15 (null and array element), S16-S18 (`id`), S19-S21 (`eventId`), S22-S24 (missing, non-string, and wrong-case `status`) | S16-S21 are the same two rules over two fields | additive job keys stay opaque (Fixture A's `diagnostic`, `attempts`, `lastError`, `retries`, `attemptsSinceRetry`) |
  | Job ID uniqueness, one job per `eventId`, and referential integrity to a stored event | AC1-AC3: Fixture A's three jobs each map 1:1 to a distinct stored event | AC7: S25 (duplicate `job-a`), S26 (two jobs on `event-a`), S27 (job with no stored event) | S25 makes array-order behavior unobservable rather than a builder choice | a stored event with no job is permitted by the contract but exercised by no accepted fixture (finding 6) |
  | Complete validation precedes exact-ID lookup | — | AC7: S28 holds the individually valid `persisted-job-target` beside an invalid sibling and must emit the structural line, not the frozen success line | S28 is the single decisive case for this rule | — |
  | Exact, case-sensitive lookup with no trim, normalize, decode, case-fold, prefix, or substring match | AC1, AC2 match all three stored IDs exactly | AC3: `job-link-missing`, `PERSISTED-JOB-TARGET`, `persisted-job-target ` → exit `1`, exact `job-link: job not found`, no ID echo | the three unknown literals share one frozen error line and are asserted byte-distinct from every valid job ID | percent-decoding and Unicode-normalization variants grouped as equivalent non-matching permutations; prefix and substring operands are not exercised (finding 5) |
  | Lookup miss on a link-valid state | — | AC3's three unknown IDs against Fixture A | one frozen literal for all three | a link-valid state with an empty `jobs` array (finding 9) |
  | Projection shape — exactly `jobId`, `eventId`, `status` in that order, each value copied from the stored job without transformation | AC1, AC2 assert three byte-exact stdout lines; `persisted-job-target` → `event-link-target` defeats any `job-`-prefix derivation | — | Fixture A's `attempts`, `lastError`, `diagnostic`, approvals, outbox, and `metadata` must not be copied | — |
  | Compact serialization with exactly one trailing LF, exit `0`, zero stderr bytes | AC1, AC2 byte-exact stdout plus zero stderr bytes | — | AC1 requires two serialized invocations to produce the same exact bytes | — |
  | Error-stream discipline — one fixed LF-terminated line, no path, ID, OS error, parser text, input bytes, stack, or state content | — | AC3-AC7 assert exact stderr bytes and zero stdout bytes on every failure path | the four error literals plus the usage literal are pairwise distinguished across AC3-AC7 | — |
  | Privacy — no event payload, job error, approval action or reason, outbox content, or unrelated state in either stream | AC1, AC2 exclude all thirteen frozen sensitive sentinels | AC3 excludes the sentinels and the supplied ID; AC5 excludes both path sentinels; AC6 excludes `MALFORMED-STATE-SENTINEL` | each sensitive category carries its own sentinel across all four collections plus top-level `metadata` | S1-S28 carry no sentinel, so AC7 asserts stream bytes rather than sentinel absence |
  | Read-only — no write, append, create, rename, remove, chmod, truncate, or delete on any path | AC1, AC2 pin Fixture A's bytes and BigInt `mode`, `size`, `mtimeNs`, `ctimeNs` plus its parent's four fields and sorted entries | AC3, AC5, AC6, AC7 repeat those observations; AC4 keeps every arity parent empty and every absent path absent | AC1's repeat invocation re-observes after each run | access time is excluded by contract because a read may update it |
  | Offline execution — no `app/server.js` import, socket, request, child process, or timer | AC8 static inspection: sole specifier `node:fs`, sole operation `readFileSync`, and no `server.js`, `fetch(`, `WebSocket`, `node:http`, `node:https`, `node:net`, `node:child_process`, `setTimeout`, or `setInterval` | AC8 applies to every invocation in AC1-AC7 | the same static pass also fixes the test file's six-specifier set | — |
  | Self-termination | AC8: every child uses a `10_000` ms timeout and returns `error === undefined`, `signal === null` | exit codes in AC3-AC7 are observed from terminated children | — | the harness must set the bound explicitly (finding 10) |
  | Fixture lifecycle and isolation | each case owns a unique temporary parent, writes only its own input, waits for the child, and removes the parent in `finally`; AC7 proves all 30 serialized fixtures pairwise byte-distinct before any invocation | — | invocations are serialized, so no shared path or port exists | no database, dependent table, non-cascading foreign key, generated sequence, counter, initializer, reset, or clock exists in this command |
  | No new endpoint, HTTP shape, UI route, selector, accessible name, or global page-shell text | — | — | — | `External: no`, `Authentication-Seams: none`, and `PREVIEW_PROVIDER=none` make deploy, auth, cookie, CORS, and preview surfaces inapplicable |
  | Stage ownership, commit order, and verification commands | AC9: test-author commit precedes the Builder commit and creates only `app/tests/job-link.test.js`; the Builder creates only `app/tools/job-link.js` | AC9 requires `node --test app/tests/job-link.test.js`, `npm test --prefix app`, and `.github/scripts/test-immutability-check.sh` to exit `0` with existing tests, `app/server.js`, `app/package.json`, lockfiles, the persisted schema, worker behavior, `factory/KIT_PIN`, and Factory controls unchanged | — | repository baseline checks remain protected-CI controls under the 2026-08-10 T-178 ruling |

  Findings:
  1. Criteria quality — AC1-AC7 freeze the exact operands, exit statuses,
     stdout and stderr bytes, fixture bytes, sentinel sets, BigInt `mode`,
     `size`, `mtimeNs`, `ctimeNs`, and sorted directory entries; AC8 freezes the
     timeout, signal outcome, module-specifier sets, the single permitted
     `node:fs` operation, and the forbidden source substrings; AC9 is decided
     from Git history, per-stage path diffs, and three exact command exits.
     No criterion contains an unquantified term or requires the builder to
     interpret product behavior. No blocking finding.
  2. Contract coverage — every material behavior in the table has at least one
     covering criterion, and every criterion is implementable against the
     contract with `node:fs.readFileSync` plus `process.stdout`,
     `process.stderr`, and an exit code. I recomputed the contract
     independently rather than trusting the planner's summary: a
     reference implementation of the frozen link-validity rules and precedence,
     run over the frozen fixtures, reproduces all three AC1/AC2 stdout lines
     byte-for-byte from `JSON.stringify(FixtureA, null, 2) + "\n"`, classifies
     each of S1-S28 as structurally invalid for exactly the rule its table row
     names (S28 as the structural line, not the success line), rejects the
     malformed fixture at `JSON.parse`, returns the not-found line for all
     three AC3 operands, and confirms all 30 serialized fixtures are pairwise
     byte-distinct. No blocking finding.
  3. Consistency — the four-collection state shape matches `app/server.js:16,
     28-33`; the job record and the `pending`/`done`/`dead` enum match
     `server.js:41-49, 44` and `SPEC.md` behavior 2; the stored `job.eventId`
     relation and its resolution match `server.js:199-205`; one job per
     accepted event and idempotency match `SPEC.md` behavior 1 with
     `server.js:38-40`; the durable state file matches behavior 6; and every
     linked `SPEC.md`, `AGENTS.md`, `docs/git-flow.md`, and relative source
     anchor resolves and says what the description attributes to it. The frozen
     error protocol matches the shipped offline-command convention (`exit 2`
     usage, `exit 1` read/parse/structure/lookup, one lowercase tool-prefixed
     LF-terminated line) in `app/tools/dead-jobs.js`,
     `app/tools/job-progress.js`, and `app/tools/approval-state.js`. Neither
     recorded ruling in `factory/rulings.md` is contradicted: the detail-route
     envelope ruling governs HTTP routes and `app/server.js` ownership, and the
     2026-08-10 T-178 ruling is why AC9 lists the targeted test, the full app
     suite, and immutability rather than the repository baseline scripts.
     Every invalid fixture is a stored literal, not a runtime transformation of
     an accepted valid fixture; S24's uppercase `DEAD` is byte-distinct from
     its lowercase counterpart and from all 29 other fixtures, so no invalid
     case is an identity transformation of a valid one. No generated
     identifier, sequence, counter, or timestamp participates in an exact
     expectation — every asserted value is fixture-literal and only
     temporary-directory names are generated and never asserted — so there is
     no initializer or reset to trace. The single serialized command has
     per-case temporary setup, an awaited child, and recursive `finally`
     teardown, and this product has no database, parent row, sibling dependent
     table, non-cascading foreign key, or helper outside Test-author ownership,
     so no child-first cleanup obligation exists. Protected-boundary inspection
     was independent: the declared seam `app/tests/job-detail.test.js` asserts
     only the stored `{job,event}` envelope and holds no import/export
     allowlist, exact-source assertion, source snapshot, hash, or
     module-specifier restriction; the tracked exact-source assertions are
     `dead-jobs.test.js:344-370`, `blocked-approvals.test.js:440-466`,
     `event-metadata.test.js:207-233, 489-493`,
     `job-progress.test.js:453-494`, and `approval-state.test.js:560-573`, each
     reading only its own tool path and its own test source;
     `backend-readiness.test.js` reads only `factory/backend-readiness.sh` and
     its own temporary logs; `factory-contract.test.js` reads only
     `factory/PROJECT.env` and `.gitignore`, and its
     `NONVISUAL_PATHS=app/tools/,app/tests/` assertion admits both owned paths.
     No tracked test enumerates `app/tools/` or `app/tests/`, fetches `GET /`,
     or makes a page-wide text assertion against the HTML shell in
     `app/server.js`, and this ticket introduces no global-shell literal: the
     usage line, four error lines, and three success lines are child-process
     bytes the UI never renders. `npm test --prefix app` runs
     `node --test tests/*.test.js`, so the new test file is collected without
     editing `app/package.json`, and `test-immutability-check.sh` exempts
     `factory/`, so this ticket's factory-only commits do not disturb
     test-first ordering. No blocking finding.
  4. Edge coverage — privacy and leakage (thirteen sentinels across events,
     jobs, approvals, outbox, and top-level `metadata`, plus the two path
     sentinels and the malformed-state sentinel), isolation (a unique temporary
     parent per case, serialized children), data loss (bytes plus BigInt
     `mode`, `size`, `mtimeNs`, `ctimeNs` and sorted parent entries across
     AC1-AC7, no path created in AC4-AC5, and a static proof that `readFileSync`
     is the only `node:fs` operation), external effect (AC8's offline source
     boundary), and irreversibility (the command never writes) are each covered
     or explicitly out of scope. There is no authorization decision, credential,
     cookie, CORS, deployment topology, or UI selector in an offline read-only
     command, so `External: no` and `Authentication-Seams: none` are correct.
     No blocking finding.
  5. No criterion supplies a proper prefix or substring of a stored job ID
     (for example `persisted-job` or `job-`), so an implementation using
     `startsWith` or `includes` instead of strict equality still satisfies
     AC1-AC3: the case-only and trailing-space misses are rejected by a
     prefix matcher too. The contract resolves the semantics explicitly
     ("does not trim, normalize, decode, case-fold, prefix-match, or
     substring-match"), so no product decision is left to the builder, and the
     Fixture A operand set makes this a coverage rather than a specification
     gap.
     SPEC-WARN: add `persisted-job` and `job-` to AC3's unknown-ID group, asserting the same exit `1` and `job-link: job not found` line.
  6. The contract permits a stored event with no related job, but every
     accepted fixture pairs each of Fixture A's three events with a job, so an
     implementation that also required one job per event would pass every
     criterion. Relay never persists an eventless event (`server.js:38-50`
     pushes the event and its job together), so no realistic state is
     misclassified and no product behavior is left open.
     SPEC-WARN: add one link-valid fixture with an extra job-less event, asserting the frozen success line for `persisted-job-target`.
  7. The read-failure class is exercised by an absent path and a directory, but
     not by an existing regular file the process cannot read. The contract
     collapses "any filesystem read failure" into one literal, so this is an
     equivalent permutation, and permission fixtures are environment-sensitive
     under some CI users.
     SPEC-WARN: add an existing unreadable regular file to AC5, or record permission failures as explicitly out of scope in the contract.
  8. Symlink inputs have no frozen disposition: a symlink to a valid state file
     reads through `readFileSync` and succeeds, while a broken symlink is a
     read failure. Both outcomes follow from the frozen read rule, so nothing is
     left to the builder, but neither is asserted.
     SPEC-WARN: record symlink inputs as explicitly out of scope alongside permission denial, or freeze the read-through outcome.
  9. Three equivalent permutations are unexercised: a missing own `events`,
     `jobs`, or `approvals` key (S3 covers only `outbox`), a scalar root
     alongside S1-S2, a zero-byte file in the parse class, and a link-valid
     state whose `jobs` array is empty. Each is the same rule the S-table and
     AC3 already exercise.
     SPEC-WARN: the test-author may extend the S-table loop with those rows and add one empty-`jobs` fixture asserting exit `1` and the exact `job-link: job not found` line, keeping frozen S1-S28 unchanged.
 10. AC8's `10_000` ms bound is a harness property, and `node:test` applies no
     default per-test timeout, so the bound holds only if the test-author sets
     it. The criterion stays pass/fail decidable because the contract fixes the
     value together with the `error === undefined` and `signal === null`
     assertions.
     SPEC-WARN: pass an explicit `{ timeout: 10_000 }` to each `node:test` case and the same `timeout` to each `spawnSync` call rather than relying on a runner default.
 11. AC4 names the one-argument operand
     (`<parent>/arity-state-does-not-exist.json`) but only implies that the
     three- and four-argument cases reuse it as their first operand. Every
     arity outcome is path-independent by contract, so only diff-level
     reproducibility is affected.
     SPEC-WARN: state in AC4 that the three- and four-argument cases prepend the same `arity-state-does-not-exist.json` path.
 12. Fixture A is internally consistent but not derivable by Relay's own
     initializer: `server.js:42` produces `job-<eventId>`, so
     `persisted-job-target` could not be generated for `event-link-target`, and
     `server.js:98` never writes an outbox receipt for a rejected approval.
     Both are deliberate and load-bearing — the non-derived job ID is what
     distinguishes a stored `eventId` copy from a `job-`-prefix derivation, and
     the contract already labels the cross-lifecycle records as opaque probes.
     No expectation depends on a generated value.
     SPEC-WARN: no action required — renaming the target job would remove the derivation probe and would require a contract version 2.
 13. `Fixture-Seams: app/tests/job-detail.test.js` is declared alongside
     `Protected-Test-Conflicts: none` and Test-author ownership of
     `app/tests/job-link.test.js` only. The seam authorizes no edit and the file
     stays reference-only, which matches the contract text and the readiness
     contract.
     SPEC-WARN: no action required — the test-author must not modify the declared seam.
 14. Static-check evidence, recorded for traceability: the release tree selected
     by `factory/KIT_PIN` (`e85890e4…`) is not present in this worktree, so the
     planner's transcript could not be re-executed from the pinned path. An
     available copy of the kit's `scripts/ticket-readiness.py` was run against
     this worktree and returned `READINESS PASS` for T-188 and `GLOBAL TEXT
     PASS` for the usage line, all four error lines, the three complete success
     objects, the three output property names, and the three status values —
     fourteen literals, matching the planner's count. The repository contains no
     `getByText`/`queryByText`/`findByText` assertion at all, so no global
     protected-text collision is possible, and the checker's protected-source
     scan reported no hash or assertion collision for `app/tools/job-link.js` or
     the declared seam. No static check was unknown or unparsable, so nothing
     fails closed. `.github/scripts/test-immutability-check.sh`,
     `scripts/repo-check`, and `scripts/secret-scan` each exit `0` on the
     current branch. The full app suite was not re-run in this cell to avoid
     port contention with the concurrently leased `ticket/T-187` and
     `ticket/T-189` worktrees; T-186 recorded two pre-existing crash-recovery
     timing flakes (`reject-reason.test.js` and `retry-dead.test.js` kill-and-
     restart cases) under full-suite load.
     SPEC-WARN: treat an AC9 full-suite failure limited to those two pre-existing kill-and-restart cases as environmental and re-run the suite rather than changing T-188's scope.
 15. Ownership and concurrency were re-checked independently: no other ticket
     declares either T-188 path, T-187 owns the `outbox-receipt` pair and T-189
     the `approval-timestamp` pair (both `Backlog`), and the backlogged T-182
     `event-job-link` and T-183 `approval-job-link` pairs are distinct file
     names that collide with nothing in this contract. `Coordination` is exact.
     No finding.

SPEC-LINT: PASS
