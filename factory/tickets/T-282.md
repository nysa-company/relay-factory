# T-282 — Count Relay jobs by referenced event type offline

State: Backlog
Initiative: I-002
Priority: normal
Risk class: low
External: no
Branch: ticket/T-282
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/job-count.test.js, app/tests/event-type-count.test.js
Authentication-Seams: none
Builder ownership: app/tools/job-event-type-count.js only
Test-author ownership: app/tests/job-event-type-count.test.js only

## Description

Add a zero-dependency, read-only CommonJS command that counts persisted Relay
jobs whose referenced event has a supplied exact type while Relay is offline.

## Acceptance criteria

1. `node app/tools/job-event-type-count.js <state-file> <event-type>` emits
   compact JSON containing exactly one `jobsForEventType` integer.
2. Type matching is exact and case-sensitive. Every job must reference exactly
   one valid event; duplicate event or job IDs and missing references are
   rejected before counting, and zero matches is valid.
3. Invalid arguments are rejected before file access. Unreadable, malformed,
   or structurally invalid state returns fixed redacted errors.
4. The command is offline, zero-dependency, deterministic, read-only, and
   self-terminating. Tests resolve fixtures from the test file, work from any
   current directory, use a five-second child timeout, and do not assume Git
   history depth.
5. Tests precede implementation. Targeted, full app, and immutability checks
   pass without dependency, server, schema, manifest, or Factory changes.

## Coordination

This ticket owns only its two new paths and is independent of T-283 and T-284.
