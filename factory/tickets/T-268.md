# T-268 — count string event types beside persisted numeric types

Kit-SHA: 6b79df558da1e7471950eb63a5e4e7e2b4d63d1f
State: Blocked-Escalated
Resume-State: Planning
Initiative: I-002
Priority: normal
Risk class: low
External: no
Branch: ticket/T-268
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/event-type-count.test.js
Authentication-Seams: none
Builder ownership: app/tools/event-type-count.js only
Test-author ownership: app/tests/event-type-count.test.js only

## Description

Relay can persist the numeric event type `7`, but the offline
`event-type-count` command rejects the whole state before it can count an
unrelated string type. One legitimate numeric sibling therefore prevents an
operator from querying ordinary string event types.

Accept persisted non-zero JSON-number types during structural validation.
Keep the command-line query a literal string and preserve strict,
non-coercing matching: query text `7` must not match stored number `7`.

## Acceptance criteria

1. Given a valid state containing events with stored types `7`, `-0.5`, and
   `meeting`, querying `meeting` exits `0`, writes the existing compact JSON
   result with count `1`, and writes zero stderr bytes.
2. Querying the same state with literal argument `7` or `-0.5` exits `0` and
   reports count `0`. The command does not parse JSON arguments, coerce values,
   or change case-sensitive string equality.
3. Existing valid all-string states and outputs remain byte-for-byte
   unchanged. Missing or empty types, zero, booleans, objects, arrays, invalid
   IDs or timestamps, and duplicate event IDs remain invalid.
4. Failures retain the existing exit codes, fixed redacted one-line messages,
   validation precedence, and zero stdout bytes.
5. The command remains deterministic, offline, zero-dependency, read-only,
   and self-terminating. It changes no input bytes, metadata, or sibling path.
6. Tests precede implementation. The targeted test, full app suite, and
   immutability check pass without server, schema, package, dependency,
   Factory, route, or configuration changes.

## Coordination

This ticket owns only `app/tools/event-type-count.js` and
`app/tests/event-type-count.test.js`. It is independent of T-267 and T-269. It
supersedes only the older commands' string-only structural validation rule;
historical frozen ticket contracts remain unchanged.

## Ambiguity log

1. **Cross-ticket file-ownership conflict with T-265.** The required sibling
   grep of `factory/tickets/` found `factory/tickets/T-265.md`
   ("count string event types beside persisted numeric types") declares the
   identical exclusive claim: Builder ownership of
   `app/tools/event-type-count.js` only and Test-author ownership of
   `app/tests/event-type-count.test.js` only. Its Description and Acceptance
   criteria are byte-for-byte identical to this ticket's, aside from the
   "independent of" sibling list in Coordination. Per this role's ceiling,
   T-265 is not visible in the local Backlog/Ready projection, so the required
   remote-branch check was run directly:
   `git show origin/ticket/T-265:factory/tickets/T-265.md`. It shows T-265 at
   `State: Planning`, pinned to the same `Kit-SHA`
   (`6b79df558da1e7471950eb63a5e4e7e2b4d63d1f`) as this ticket, with no
   `## Frozen contract` section yet — i.e. its Planner has not frozen an
   interface either. This is a symmetric conflict (unlike a prior precedent
   where one sibling ticket already held a frozen, implemented contract):
   neither T-265 nor T-268 has priority by virtue of being further along.
   `factory/rulings.md` records only the T-162–T-165 concurrent-`server.js`
   ruling and the T-178 verification-scope ruling; neither covers two tickets
   independently claiming exclusive ownership of the same non-`server.js`
   pair with identical contracts. This cannot be resolved from the product
   docs or an existing ruling, so it is escalated below instead of being
   decided unilaterally.

## Planning log

- 2026-08-26: Planner stopped before writing a spec'd description or freezing
  a contract because active, unmerged `origin/ticket/T-265` already declares
  the same exclusive Builder path (`app/tools/event-type-count.js`) and
  Test-author path (`app/tests/event-type-count.test.js`) with an identical
  acceptance-criteria set, and neither ticket has a frozen contract yet.
  Exact operator question: **Should T-268 be canceled as a duplicate of
  T-265 (or vice versa), or should T-268 be reassigned distinct Builder and
  Test-author paths with a separately specified command interface so both
  tickets can proceed without colliding on the same files?**

ROLE-ESCALATE: CONTRACT-BLOCKED

OPERATOR ANSWER: T-268 supersedes the cancelled q95 T-265 qualification attempt; treat origin/ticket/T-265 as failed-lane evidence and proceed with T-268's existing scope.
OPERATOR ANSWER RECEIPT: 6789c0716a2ef4afdfc0e9d0c3ee766a790ab529ff2ec08b269eeb4504a84288
