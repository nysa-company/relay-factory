# T-248 — count Relay outbox entries by subject and sent time offline

Kit-SHA: 9dfe16fc1b7b4f3bf7ae05e003fbfbdad0f25a96
State: Ready
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-248
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/outbox-detail.test.js, app/tests/approval-detail.test.js
Authentication-Seams: none
Builder ownership: app/tools/outbox-subject-sent-count.js only
Test-author ownership: app/tests/outbox-subject-sent-count.test.js only

## Description

Add a zero-dependency, read-only Node command that counts persisted Relay
outbox entries whose subject and sent timestamp exactly match supplied
strings. This supports narrow offline delivery checks without starting Relay
or exposing receipt bodies.

## Acceptance criteria

1. `node app/tools/outbox-subject-sent-count.js <state-file> <subject> <sent-at>`
   emits compact JSON containing exactly one `outboxWithSubjectAndSentAt`
   integer.
2. Subject and sent timestamp must be non-empty strings and match stored
   `subject` and `sentAt` values exactly, without trimming, timestamp parsing,
   normalization, or coercion.
3. Invalid arguments are rejected before file access. Unreadable, malformed,
   or structurally invalid state returns fixed redacted errors; complete
   outbox validation precedes counting.
4. Every invocation is offline, read-only, deterministic, and self-terminating.
5. Tests precede implementation. Targeted, full app, and immutability checks
   pass without dependency, server, schema, manifest, or Factory changes.

## Coordination

This ticket owns only its two new paths and is independent of T-246 and T-247.
