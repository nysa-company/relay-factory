# T-182 — inspect one Relay event-to-job link offline

Kit-SHA: a4db291522f19f36fe15a5411bbacf630cdd3e23
State: Planning
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-182
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/job-detail.test.js
Authentication-Seams: none
Builder ownership: app/tools/event-job-link.js only
Test-author ownership: app/tests/event-job-link.test.js only

## Description

Add `app/tools/event-job-link.js`, a zero-dependency Node 18+ command that reads
one persisted Relay state file without starting the service, selects one event
by case-sensitive exact ID, and reports only that event ID plus its stored
job's ID and status. An event without a related job is reported with two
explicit `null` values. The command never emits event payloads, job errors,
approval data, or outbox data and never changes the input filesystem.

This operational link follows the one-event/one-job and idempotency behavior in
[`SPEC.md` Behavior spec items 1–2](../../SPEC.md#behavior-spec), the persisted
and offline-recovery requirements in
[`SPEC.md` Behavior spec items 6–7](../../SPEC.md#behavior-spec), and the job
status outcomes in
[`SPEC.md` conformance cases 2–4](../../SPEC.md#test-cases-the-conformance-suite).
The stored relation and status names come from the current
[`app/server.js` durable-state and domain model](../../app/server.js). The
standalone, dependency-free boundary follows
[`AGENTS.md` Boundaries](../../AGENTS.md#boundaries), while test-first ordering
and verification follow [`AGENTS.md` Workflow and Verification](../../AGENTS.md#workflow)
and [`docs/git-flow.md`](../../docs/git-flow.md).

## Acceptance criteria

1. Given Frozen Fixture A and event ID `event-link-target`, each of two
   serialized invocations of
   `node app/tools/event-job-link.js <fixture-path> event-link-target` exits
   `0`, writes exactly
   `{"eventId":"event-link-target","jobId":"job-event-link-target","jobStatus":"dead"}\n`
   to stdout, and writes zero bytes to stderr. Neither stream contains any
   frozen sensitive sentinel. The fixture's bytes, `mode`, `size`, `mtimeNs`,
   and `ctimeNs`, and its parent directory's `mode`, `size`, `mtimeNs`,
   `ctimeNs`, and sorted entry names are unchanged after both invocations.
2. Given the same fixture and event ID `event-link-orphan`, the command exits
   `0`, writes exactly
   `{"eventId":"event-link-orphan","jobId":null,"jobStatus":null}\n` to stdout,
   writes zero bytes to stderr, and leaves the same file and directory
   observations unchanged.
3. Given Frozen Fixture A and `event-link-missing`, the command exits `1`,
   writes zero bytes to stdout, and writes exactly
   `event-job-link: event not found\n` to stderr. Separate zero-, one-, three-,
   and four-argument invocations after the script path instead exit `2`, write
   zero bytes to stdout, and write exactly
   `usage: node app/tools/event-job-link.js <state-file> <event-id>\n` to
   stderr; the supplied fixture paths are not read or changed.
4. A nonexistent path or an existing directory path exits `1` with empty
   stdout and exactly `event-job-link: state file is unreadable\n` on stderr.
   Frozen malformed JSON exits `1` with empty stdout and exactly
   `event-job-link: state file is not valid JSON\n` on stderr. The missing path
   remains absent; the directory remains empty; each existing path and its
   parent retain the frozen observations.
5. Every frozen structural-invalid byte string exits `1`, writes zero bytes to
   stdout, and writes exactly
   `event-job-link: state file has invalid Relay state\n` to stderr. The tests
   assert that every serialized valid, malformed, and structural-invalid
   fixture is pairwise byte-distinct and that each existing fixture and parent
   retains the frozen observations.
6. Every child invocation has a `10_000` ms test timeout and completes with no
   signal. Static inspection finds exactly `node:fs` as the production file's
   module specifier and no `server.js`, socket, HTTP, child-process, or
   filesystem-mutator reference; the test file's module specifiers are exactly
   the six frozen standard-library specifiers. Every test-owned temporary
   directory is removed recursively in `finally` only after its child
   invocations complete.
7. Git history places the commit creating
   `app/tests/event-job-link.test.js` before the commit creating
   `app/tools/event-job-link.js`. Apart from role-owned ticket appends, those
   are the only paths changed; `app/server.js`, existing tests,
   `app/package.json`, lockfiles, persisted schema, and Factory controls remain
   unchanged. The targeted command
   `node --test app/tests/event-job-link.test.js`, `npm test --prefix app`, and
   `.github/scripts/test-immutability-check.sh` each exit `0`.

## Frozen contract — version 1

- **Freeze result:** PASS. Contract version 1 is frozen.

If this contract proves wrong, return T-182 to Ready and append a higher
contract version to this ticket; the test-author and builder must not edit
version 1.

### Command interface and output

- Invocation is exactly
  `node app/tools/event-job-link.js <state-file> <event-id>`.
- Exactly two arguments after the script path are accepted. Any other arity
  exits `2`, writes no stdout, writes exactly
  `usage: node app/tools/event-job-link.js <state-file> <event-id>\n` to
  stderr, and does not inspect either supplied path.
- `<event-id>` is matched case-sensitively against stored event `id`. The
  command validates the complete link-relevant state before looking up the
  requested event.
- The command synchronously reads `<state-file>` as UTF-8, parses and validates
  it in memory, writes its result, and exits. It does not import or start
  `app/server.js`, open a network socket, spawn a process, or write, rename,
  chmod, truncate, or delete any path.
- A known event exits `0`, writes no stderr, and writes exactly one compact
  JSON object followed by one LF to stdout. Property order is `eventId`,
  `jobId`, `jobStatus`; no other property is present.
- If exactly one job has `eventId` equal to the selected event's `id`, `jobId`
  and `jobStatus` are that stored job's `id` and `status`. If no job has that
  `eventId`, both properties are JSON `null`.
- A structurally valid state without the requested event exits `1`, writes no
  stdout, and writes exactly `event-job-link: event not found\n` to stderr.
- Output never copies event `type`, `payload`, or `receivedAt`; job counters or
  `lastError`; approval records, actions, reasons, or statuses; outbox records;
  input paths; parser text; operating-system errors; stack traces; or any
  unconsumed field.

### Input validation and error precedence

A link-valid Relay state is a non-null, non-array JSON object with own
properties `events`, `jobs`, `approvals`, and `outbox`, each an array.
Additional top-level properties are allowed and ignored.

Every `events` element must be a non-null, non-array object whose `id` is a
nonempty string. Event IDs must be unique. Other event properties are allowed
and ignored.

Every `jobs` element must be a non-null, non-array object with:

- `id`: a nonempty string, unique across jobs;
- `eventId`: a nonempty string equal to exactly one stored event `id`, unique
  across jobs so one event cannot have two jobs; and
- `status`: exactly `pending`, `done`, or `dead`.

Other job properties are allowed and ignored. An event may have no job, which
is the frozen `null` result. Approval and outbox elements are not consumed or
validated beyond their collections being arrays.

After argument validation, failures are classified in this order:

1. A nonexistent path, non-regular path, or any filesystem read failure exits
   `1` with empty stdout and exactly
   `event-job-link: state file is unreadable\n` on stderr.
2. Readable bytes rejected by `JSON.parse` exit `1` with empty stdout and
   exactly `event-job-link: state file is not valid JSON\n` on stderr.
3. Parsed JSON that is not link-valid exits `1` with empty stdout and exactly
   `event-job-link: state file has invalid Relay state\n` on stderr.
4. Link-valid state without the requested event uses the frozen not-found
   result above.

### Frozen Fixture A

`app/tests/event-job-link.test.js` writes exactly the following UTF-8 bytes,
including one LF after the closing brace:

```json
{
  "events": [
    {
      "id": "event-link-other",
      "type": "meeting",
      "payload": {
        "private": "OTHER-EVENT-PAYLOAD-SENTINEL"
      },
      "receivedAt": "2026-08-22T12:00:00.000Z"
    },
    {
      "id": "event-link-target",
      "type": "email",
      "payload": {
        "private": "EVENT-PAYLOAD-SENTINEL"
      },
      "receivedAt": "2026-08-22T12:00:01.000Z"
    },
    {
      "id": "event-link-orphan",
      "type": "generic",
      "payload": {
        "private": "ORPHAN-EVENT-PAYLOAD-SENTINEL"
      },
      "receivedAt": "2026-08-22T12:00:02.000Z"
    },
    {
      "id": "event-link-done",
      "type": "generic",
      "payload": {},
      "receivedAt": "2026-08-22T12:00:03.000Z"
    }
  ],
  "jobs": [
    {
      "id": "job-event-link-other",
      "eventId": "event-link-other",
      "status": "pending",
      "attempts": 0,
      "lastError": null,
      "retries": 0,
      "attemptsSinceRetry": 0
    },
    {
      "id": "job-event-link-target",
      "eventId": "event-link-target",
      "status": "dead",
      "attempts": 3,
      "lastError": "JOB-ERROR-SENTINEL",
      "retries": 0,
      "attemptsSinceRetry": 3
    },
    {
      "id": "job-event-link-done",
      "eventId": "event-link-done",
      "status": "done",
      "attempts": 1,
      "lastError": null,
      "retries": 0,
      "attemptsSinceRetry": 1
    }
  ],
  "approvals": [
    {
      "id": "appr-event-link-other",
      "jobId": "job-event-link-other",
      "action": {
        "to": "APPROVAL-TO-SENTINEL",
        "subject": "APPROVAL-SUBJECT-SENTINEL",
        "body": "APPROVAL-BODY-SENTINEL"
      },
      "status": "rejected",
      "reason": "APPROVAL-REASON-SENTINEL",
      "proposedAt": "2026-08-22T12:00:04.000Z"
    },
    42
  ],
  "outbox": [
    {
      "to": "OUTBOX-TO-SENTINEL",
      "subject": "OUTBOX-SUBJECT-SENTINEL",
      "body": "OUTBOX-BODY-SENTINEL",
      "approvalId": "appr-event-link-other",
      "sentAt": "2026-08-22T12:00:05.000Z"
    },
    null
  ],
  "metadata": {
    "private": "METADATA-SENTINEL"
  }
}
```

The sensitive sentinel set is exactly
`OTHER-EVENT-PAYLOAD-SENTINEL`, `EVENT-PAYLOAD-SENTINEL`,
`ORPHAN-EVENT-PAYLOAD-SENTINEL`, `JOB-ERROR-SENTINEL`,
`APPROVAL-TO-SENTINEL`, `APPROVAL-SUBJECT-SENTINEL`,
`APPROVAL-BODY-SENTINEL`, `APPROVAL-REASON-SENTINEL`,
`OUTBOX-TO-SENTINEL`, `OUTBOX-SUBJECT-SENTINEL`, and
`OUTBOX-BODY-SENTINEL`, and `METADATA-SENTINEL`. Tests assert none occurs in
stdout or stderr for every Fixture A invocation. The `done` job exercises the
third valid status; scalar approval/outbox entries and `metadata` prove those
unconsumed values are allowed.

The exact target output is
`{"eventId":"event-link-target","jobId":"job-event-link-target","jobStatus":"dead"}\n`.
The exact orphan output is
`{"eventId":"event-link-orphan","jobId":null,"jobStatus":null}\n`.

### Frozen failure fixtures

The missing fixture is `<temporary-directory>/missing-state.json`, which does
not exist. The non-file fixture is
`<temporary-directory>/state-directory`, which exists as an empty directory.
The malformed fixture is a regular file containing exactly `{not-json}\n`.

Each structural-invalid fixture is a regular file containing exactly the
listed UTF-8 bytes:

| ID | Rule exercised | Exact bytes |
|---|---|---|
| S1 | non-null root | `null\n` |
| S2 | non-array root | `[]\n` |
| S3 | all four collections present | `{"events":[],"jobs":[],"approvals":[]}\n` |
| S4 | `events` is an array | `{"events":{},"jobs":[],"approvals":[],"outbox":[]}\n` |
| S5 | `jobs` is an array | `{"events":[],"jobs":{},"approvals":[],"outbox":[]}\n` |
| S6 | `approvals` is an array | `{"events":[],"jobs":[],"approvals":{},"outbox":[]}\n` |
| S7 | `outbox` is an array | `{"events":[],"jobs":[],"approvals":[],"outbox":{}}\n` |
| S8 | event is an object | `{"events":[null],"jobs":[],"approvals":[],"outbox":[]}\n` |
| S9 | event ID is nonempty | `{"events":[{"id":""}],"jobs":[],"approvals":[],"outbox":[]}\n` |
| S10 | event ID is a string | `{"events":[{"id":7}],"jobs":[],"approvals":[],"outbox":[]}\n` |
| S11 | event IDs are unique | `{"events":[{"id":"event-a"},{"id":"event-a"}],"jobs":[],"approvals":[],"outbox":[]}\n` |
| S12 | job is an object | `{"events":[],"jobs":[null],"approvals":[],"outbox":[]}\n` |
| S13 | job ID is nonempty | `{"events":[{"id":"event-a"}],"jobs":[{"id":"","eventId":"event-a","status":"pending"}],"approvals":[],"outbox":[]}\n` |
| S14 | job ID is a string | `{"events":[{"id":"event-a"}],"jobs":[{"id":7,"eventId":"event-a","status":"pending"}],"approvals":[],"outbox":[]}\n` |
| S15 | job event ID is nonempty | `{"events":[{"id":"event-a"}],"jobs":[{"id":"job-a","eventId":"","status":"pending"}],"approvals":[],"outbox":[]}\n` |
| S16 | job event ID is a string | `{"events":[{"id":"event-a"}],"jobs":[{"id":"job-a","eventId":7,"status":"pending"}],"approvals":[],"outbox":[]}\n` |
| S17 | job status enum is case-sensitive | `{"events":[{"id":"event-a"}],"jobs":[{"id":"job-a","eventId":"event-a","status":"DEAD"}],"approvals":[],"outbox":[]}\n` |
| S18 | job status is required | `{"events":[{"id":"event-a"}],"jobs":[{"id":"job-a","eventId":"event-a"}],"approvals":[],"outbox":[]}\n` |
| S19 | job IDs are unique | `{"events":[{"id":"event-a"},{"id":"event-b"}],"jobs":[{"id":"job-a","eventId":"event-a","status":"pending"},{"id":"job-a","eventId":"event-b","status":"done"}],"approvals":[],"outbox":[]}\n` |
| S20 | one job per event | `{"events":[{"id":"event-a"}],"jobs":[{"id":"job-a","eventId":"event-a","status":"pending"},{"id":"job-b","eventId":"event-a","status":"done"}],"approvals":[],"outbox":[]}\n` |
| S21 | every job references an event | `{"events":[],"jobs":[{"id":"job-a","eventId":"event-a","status":"pending"}],"approvals":[],"outbox":[]}\n` |

The test stores Fixture A, the malformed fixture, and S1–S21 as literal byte
strings and asserts pairwise byte inequality before invoking the command. None
is produced by casing, normalization, or mutation at runtime. IDs and
timestamps inside Fixture A are fixed literals, not generated values.

### File locations, protected boundaries, lifecycle, and topology

- Builder ownership is creation of `app/tools/event-job-link.js` only. Its only
  module specifier is `node:fs`; `process`, `JSON`, `Set`, and `Map` are runtime
  globals.
- Test-author ownership is creation of
  `app/tests/event-job-link.test.js` only. Its module specifiers are exactly
  `node:test`, `node:assert`, `node:child_process`, `node:fs`, `node:path`, and
  `node:os`.
- `app/tests/job-detail.test.js` is the protected relation and fixture seam. It
  is inspected but not edited. It has no import/export allowlist, exact-source
  assertion, source snapshot, module-specifier restriction, or digest over the
  new production path. Existing source checks in
  `app/tests/dead-jobs.test.js` and `app/tests/blocked-approvals.test.js` name
  only their own tools and do not constrain this new file.
- `app/package.json` already discovers `tests/*.test.js`; no package-manifest
  or script edit is authorized.
- Each acceptance case creates a unique temporary directory, writes its
  regular-file fixtures before any `spawnSync`, waits for each child to
  complete, and removes the directory recursively in `finally`. Repeated
  Fixture A invocations are serialized. There is no database, parent row,
  foreign key, dependent table, server, port, reset, generated sequence,
  counter initializer, or generated timestamp.
- A regular-file observation is its exact bytes plus `fs.statSync(path,
  {bigint:true})` values for `mode`, `size`, `mtimeNs`, and `ctimeNs`. A
  directory observation is its sorted entry names plus the same four stat
  values. The tests compare these observations before and after each applicable
  invocation; the missing path is checked with `fs.existsSync`.
- This local CLI introduces no endpoint, UI, selector, global page shell,
  authentication, cookie, CORS, cross-origin, preview-deploy, or external-send
  behavior. There are no deployment domains or UI selectors in this contract;
  `External: no` and `Authentication-Seams: none` remain exact.
- Targeted verification is
  `node --test app/tests/event-job-link.test.js`. Full app and immutability
  verification are `npm test --prefix app` and
  `.github/scripts/test-immutability-check.sh`.

### Criteria checklist

- **Pass/fail and unambiguous:** AC1–AC5 freeze every exit code, stdout/stderr
  byte string, fixture, sensitive exclusion, file stat field, and directory
  observation. AC6 freezes timeout, signal, source, and teardown checks. AC7
  freezes commit order, changed paths, prohibited edits, and command exits.
- **Coverage:** AC1 covers the linked event, exact relationship selection, all
  output properties and order, `dead`, privacy, repeatability, and success-side
  read-only behavior. AC2 covers the permitted no-job branch and both `null`
  values. AC3 covers exact-ID miss and every arity class. AC4 covers filesystem
  and parse failures. AC5 covers every structural rule and pairwise fixture
  distinctness. AC6 covers offline/self-terminating operation, module
  boundaries, and lifecycle. AC7 covers ownership and all required serialized
  verification commands.
- **Derived fixtures and generated values:** all bytes, IDs, statuses, and
  timestamps under assertion are frozen literals; every serialized fixture is
  pairwise byte-distinct. Temporary directory names are generated but never
  asserted.
- **Fixture lifecycle:** all setup precedes child execution, repeated commands
  are serialized, and teardown follows child completion in `finally`. No
  relational cleanup or reset is applicable.
- **Protected/global checks:** protected source inspection found no conflict.
  The pinned readiness checker returned `GLOBAL TEXT PASS` separately for the
  usage line, four error lines, three output property names, and both complete
  success output lines.

### Ambiguity log

1. **Which stored job represents the relationship?** Resolved from
   [`SPEC.md` Behavior spec items 1–2](../../SPEC.md#behavior-spec) and the
   current [`app/server.js` relation](../../app/server.js): match stored
   `job.eventId` to stored `event.id`; do not derive a job ID from the event ID.
2. **What does no related job mean?** The seed explicitly requires nullable
   job fields. Frozen as an event that exists with no matching job, producing
   JSON `null` for both `jobId` and `jobStatus`; a missing event remains a
   distinct fixed error.
3. **How much state is validated?** Resolved from the command's safe-link scope
   and [`SPEC.md` durable collections](../../SPEC.md#behavior-spec): validate
   the four collections plus every event ID and consumed job link/status field;
   allow unrelated fields and do not create a second schema for approval or
   outbox entries.
4. **How are corrupt ambiguous links handled?** The product model permits one
   event ID and one job per accepted event. Duplicate event IDs, duplicate job
   IDs, two jobs for one event, or a dangling job would make the report
   ambiguous or contradict that model, so each is frozen as invalid state.
5. **What are deterministic privacy-safe failures?** Raw filesystem and parser
   errors can disclose paths or input. The ticket's redaction requirement is
   frozen as separate path-independent usage, read, parse, structure, and
   not-found lines, with empty stdout and no stack trace.
6. **How is read-only/offline behavior proved?** Resolved with exact file bytes
   and non-access stat fields, parent entry names, serialized child execution,
   a self-termination timeout, and static production-source checks. Access time
   is deliberately not asserted because a read may update it under filesystem
   policy.
7. **Do sibling or leased tickets overlap ownership?** Repository-wide ticket
   search finds the two T-182 paths only on T-182. Bounded concurrency is three;
   the other active worktrees and `origin/ticket/T-181` and
   `origin/ticket/T-183` were inspected. They own
   `outbox-inspect.js`/`outbox-inspect.test.js` and
   `approval-job-link.js`/`approval-job-link.test.js`, so there is no file
   overlap.
8. **Do protected tests block the planned source?** Direct inspection of all
   tracked `app/tests/*.test.js`, `app/package.json`, and the immutability check
   found no static guard over the new paths or `node:fs`.
   `app/tests/job-detail.test.js` is a read-only shape reference, and the
   package glob admits the new test. No protected-test conflict declaration or
   seam edit is required.
9. **Does global shell text collide with protected page-wide assertions?** The
   pinned readiness checker returned `GLOBAL TEXT PASS` for every new frozen
   shell literal and output name. No protected test or `Fixture-Seams` addition
   is required.
10. **Does deploy topology or an external action need a contract?** No. The
    command reads one local file and has no network or service path, so auth,
    cookies, CORS, domains, preview deployment, UI, and external send are
    absent. `factory/rulings.md` contains no ruling that changes this local CLI
    scope.

## Coordination

T-182 owns only its two new `app/` paths. T-181 and T-183 own distinct CLI and
test files, so the three active tickets can proceed concurrently.
