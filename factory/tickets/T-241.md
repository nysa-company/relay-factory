# T-241 — count Relay jobs by event ID and status offline

Kit-SHA: f65b6ad17daed977025e97d3ce5a582f1ff3e728
State: Ready
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-241
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/job-count.test.js, app/tests/job-status-count.test.js
Authentication-Seams: none
Builder ownership: app/tools/job-event-status-count.js only
Test-author ownership: app/tests/job-event-status-count.test.js only

## Description

Add a zero-dependency, read-only Node command that counts persisted Relay jobs
whose event ID and status exactly match supplied strings. This supports narrow
offline queue checks without starting Relay or exposing job contents.

## Acceptance criteria

1. `node app/tools/job-event-status-count.js <state-file> <event-id> <status>`
   emits compact JSON containing exactly one `jobsWithEventAndStatus` integer.
2. Event ID must be a non-empty string; status must be exactly `pending`,
   `done`, or `dead`. Both filters must exactly match the stored job `eventId`
   and status values.
3. Invalid arguments are rejected before file access. Unreadable, malformed,
   or structurally invalid state returns fixed redacted errors; complete job
   validation precedes counting.
4. Every invocation is offline, read-only, deterministic, and self-terminating.
5. Tests precede implementation. Targeted, full app, and immutability checks
   pass without dependency, server, schema, manifest, or Factory changes.

## Coordination

This ticket owns only its two new paths and is independent of T-240 and T-242.
