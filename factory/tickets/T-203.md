# T-203 — count all Relay events offline

Kit-SHA: f9f5be2729a0e741978d3fe9fe5c23bb40ff38fd
State: Ready
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-203
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/state-summary.test.js
Authentication-Seams: none
Builder ownership: app/tools/event-count.js only
Test-author ownership: app/tests/event-count.test.js only

## Description

Add a zero-dependency, read-only Node command that reports the total number of
events in a persisted Relay state file while the service is offline.

## Acceptance criteria

1. `node app/tools/event-count.js <state-file>` exits zero and emits
   deterministic compact JSON with exactly one `events` integer.
2. A valid state with no events returns `{"events":0}` followed by one newline.
3. Missing, unreadable, malformed, or structurally invalid state is rejected
   with fixed redacted errors that reveal no path or stored content.
4. Every invocation is offline, read-only, and self-terminating; input bytes,
   mode, timestamps, and sibling directory entries remain unchanged.
5. Complete acceptance tests are committed before implementation. Targeted,
   full app, and immutability checks pass without dependency, server, schema,
   package-manifest, lockfile, or Factory-control changes.

## Coordination

This ticket owns only its two new `app/` paths and is independent of the
T-200–T-202 cohort.
