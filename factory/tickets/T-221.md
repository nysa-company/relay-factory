# T-221 — count Relay outbox entries with one subject offline

Kit-SHA: 7ae447baf7287e32a9df60c9453da6edec454e7b
State: Planning
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-221
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/outbox-detail.test.js
Authentication-Seams: none
Builder ownership: app/tools/outbox-subject-count.js only
Test-author ownership: app/tests/outbox-subject-count.test.js only

## Description

Add a zero-dependency, read-only Node command that counts persisted Relay
sandbox outbox entries whose subject exactly matches one operator-supplied
string while the service is offline.

## Acceptance criteria

1. `node app/tools/outbox-subject-count.js <state-file> <subject>` exits zero
   and emits deterministic compact JSON with exactly one
   `outboxEntriesWithSubject` nonnegative integer.
2. Matching compares stored and supplied strings exactly, without trimming,
   parsing, normalization, or coercion. No exact match returns
   `{"outboxEntriesWithSubject":0}` followed by one newline.
3. Missing arguments and missing, unreadable, malformed, or structurally
   invalid state are rejected with fixed redacted errors that reveal no path,
   subject, recipient, body, approval ID, timestamp, or stored content;
   complete outbox validation precedes counting.
4. Every invocation is offline, read-only, and self-terminating; input bytes,
   mode, timestamps, and sibling directory entries remain unchanged.
5. Complete acceptance tests are committed before implementation. Targeted,
   full app, and immutability checks pass without dependency, server, schema,
   package-manifest, lockfile, or Factory-control changes.

## Coordination

This ticket owns only its two new `app/` paths and is independent of T-219 and
T-220. The prior q77 cohort T-216 through T-218 remains historical evidence.
