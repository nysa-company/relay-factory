# T-179 — inspect one Relay event offline

State: Building
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-179
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/health.test.js
Authentication-Seams: none
Builder ownership: app/tools/event-inspect.js only
Test-author ownership: app/tests/event-inspect.test.js only

## Spec'd description

Add `app/tools/event-inspect.js`, a zero-dependency Node 18+ command that reads
one persisted Relay state file while the service is offline and selects one
event by exact ID. It emits only the stored event's `id`, `type`, and
`receivedAt`, plus a Boolean `hasJob` derived from the persisted jobs
collection. This gives an operator a deterministic event lookup without
starting Relay, changing state, or exposing event payloads, job details,
approval actions, or outbox contents.

Product-doc sources:

- [`SPEC.md` behaviors 1-2](../../SPEC.md#behavior-spec) define exact event-ID
  intake, idempotency, and the related background job.
- [`SPEC.md` behaviors 6-7](../../SPEC.md#behavior-spec) define persisted
  on-disk state and the event/job collections this command reads offline.
- [`SPEC.md` conformance cases 1-2 and 7](../../SPEC.md#test-cases-the-conformance-suite)
  require accepted events to remain visible, duplicate IDs to create only one
  job, and persisted work to survive restart without duplication.
- The current [`app/server.js` durable-state model](../../app/server.js)
  establishes the stored event fields and the job-to-event `eventId` link.
- [`AGENTS.md` Boundaries](../../AGENTS.md#boundaries) keeps the app
  zero-dependency, and [`AGENTS.md` Verification](../../AGENTS.md#verification)
  defines the later full-suite and immutability checks.

Product issue: https://github.com/nysa-company/relay-factory/issues/136

## Acceptance criteria

1. Given Frozen Fixture A and event ID `event-alpha`, each of two invocations
   of `node app/tools/event-inspect.js <fixture-a-path> event-alpha` completes
   within 5 seconds, exits `0`, writes exactly
   `{"id":"event-alpha","type":"meeting","receivedAt":"2026-08-07T12:00:00.000Z","hasJob":true}\n`
   to stdout, and writes zero stderr bytes. Every frozen sensitive token is
   absent from both streams, and Fixture A's bytes, numeric mode, size,
   `mtimeMs`, and sorted parent-directory entries are unchanged after each
   invocation.
2. Given the same Frozen Fixture A and event ID `event-zulu`, the command
   completes within 5 seconds, exits `0`, writes exactly
   `{"id":"event-zulu","type":"email","receivedAt":"2026-08-07T12:00:01.000Z","hasJob":false}\n`
   to stdout, writes zero stderr bytes, and leaves the fixture and its parent
   directory unchanged by the observations named in criterion 1.
3. Each frozen lookup miss—`event-missing` and case-distinct `Event-alpha`
   against Fixture A, and `event-missing` against Frozen Fixture B—completes
   within 5 seconds, exits `1`, writes zero stdout bytes, and writes exactly
   `event-inspect: event not found\n` to stderr. Both existing fixtures and
   their parent directories remain unchanged by the observations named in
   criterion 1.
4. Each frozen arity case—zero, one, and three positional arguments—completes
   within 5 seconds, exits `2`, writes zero stdout bytes, and writes exactly
   `usage: node app/tools/event-inspect.js <state-file> <event-id>\n` to
   stderr. The one-argument case passes an absent candidate state path, which
   remains absent, and its temporary parent remains empty.
5. For both frozen read failures, an absent path and a directory path, the
   command completes within 5 seconds, exits `1`, writes zero stdout bytes,
   and writes exactly `event-inspect: cannot read state file\n` to stderr.
   The absent path remains absent; the directory remains a directory with
   identical entries; neither parent gains or loses an entry.
6. Given Frozen Fixture C, the command completes within 5 seconds, exits `1`,
   writes zero stdout bytes, writes exactly
   `event-inspect: state file is not valid JSON\n` to stderr, does not emit
   `MALFORMED-PAYLOAD-MUST-NOT-LEAK`, and leaves the fixture and parent
   directory unchanged by the observations named in criterion 1.
7. For every S1-S19 frozen structural-invalid fixture, the command completes
   within 5 seconds, exits `1`, writes zero stdout bytes, writes exactly
   `event-inspect: invalid Relay state\n` to stderr, and leaves that fixture
   and its parent directory unchanged by the observations named in criterion
   1. Before invocation, the acceptance test proves Fixtures A-C and S1-S19
   are pairwise byte-distinct.
8. The acceptance test statically proves that the Builder file's complete
   CommonJS module-specifier set is exactly `node:fs`, its only `node:fs`
   operation is `readFileSync`, and its source contains no `server.js`,
   `fetch(`, `WebSocket`, `node:http`, `node:https`, `node:net`,
   `node:child_process`, `setTimeout`, or `setInterval` reference. Together
   with criteria 1-7's process and filesystem assertions, every command path
   is offline, self-terminating, and read-only.
9. Git history contains the complete test-author commit before the Builder
   commit. Apart from role-owned ticket-log appends, the test-author commit
   creates only `app/tests/event-inspect.test.js` and the Builder commit
   creates only `app/tools/event-inspect.js`. The targeted command
   `node --test app/tests/event-inspect.test.js`, `npm test --prefix app`, and
   `.github/scripts/test-immutability-check.sh` each exit `0` after
   implementation; existing application files, `app/server.js`,
   `app/package.json`, lockfiles, dependencies, persisted schema,
   `factory/KIT_PIN`, and Factory controls are unchanged by both stages.

## Frozen contract — version 1

- **Freeze result:** PASS. Contract version 1 is frozen.

This is the exact interface for the test-author and Builder. If it proves
wrong, return T-179 to Ready and append a higher-numbered frozen contract;
version 1 must not be edited or removed.

### Command-line interface and errors

- Repository-root invocation is exactly
  `node app/tools/event-inspect.js <state-file> <event-id>`.
- Exactly two positional arguments are accepted. Relative state paths resolve
  from the command process's working directory; acceptance tests use absolute
  temporary paths.
- Zero, one, or three positional arguments produce exit code `2`, empty
  stdout, and exactly this single LF-terminated stderr line:

  ```text
  usage: node app/tools/event-inspect.js <state-file> <event-id>
  ```

- The command reads `<state-file>` directly as UTF-8. Any read failure,
  including an absent path, a directory in place of a file, or an OS read
  denial, produces exit code `1`, empty stdout, and exactly:

  ```text
  event-inspect: cannot read state file
  ```

- Bytes rejected by `JSON.parse` produce exit code `1`, empty stdout, and
  exactly:

  ```text
  event-inspect: state file is not valid JSON
  ```

- A parsed value that fails the frozen structural rules produces exit code
  `1`, empty stdout, and exactly:

  ```text
  event-inspect: invalid Relay state
  ```

- A valid state with no event whose stored `id` is strictly equal to the
  supplied `<event-id>` produces exit code `1`, empty stdout, and exactly:

  ```text
  event-inspect: event not found
  ```

- Validation precedence is argument count, read, JSON parse, complete
  structural validation, then event lookup.
- Errors never contain the state path, event ID, OS error, parser text, input
  bytes, stack trace, payload, job field, approval field, outbox field, or a
  second line. Every displayed error is terminated by exactly one LF.

### Structurally valid state and successful output

For this command, structurally valid state is a non-null, non-array JSON object
with own `events`, `jobs`, `approvals`, and `outbox` properties whose values
are arrays.

Every `events` element must be a non-null, non-array object with non-empty
string own properties `id`, `type`, and `receivedAt`. Event IDs must be
pairwise distinct. `payload` and all other event fields are opaque and are
never copied to output.

Every `jobs` element must be a non-null, non-array object with a non-empty
string own property `eventId`. All other job fields are opaque. Approval and
outbox elements are wholly opaque. Additional top-level properties are
allowed and ignored.

The event lookup uses exact, case-sensitive JavaScript string equality and
does not trim, normalize, decode, or case-fold either ID. For the selected
event, `hasJob` is `true` iff at least one job has `job.eventId` strictly equal
to the selected stored event ID; otherwise it is `false`.

Success exits `0`, writes zero stderr bytes, and writes one compact JSON object
plus exactly one LF. Its only keys, in order, are:

```json
{"id":<stored id>,"type":<stored type>,"receivedAt":<stored receivedAt>,"hasJob":<boolean>}
```

No endpoint, HTTP shape, route, UI selector, or global page-shell text is
introduced.

### Frozen Fixture A — two events and mixed related content

The test writes this exact parsed value to an isolated temporary `state.json`
using `JSON.stringify(value, null, 2) + "\n"`:

```json
{
  "events": [
    {
      "id": "event-zulu",
      "type": "email",
      "payload": {
        "private": "UNSELECTED-PAYLOAD-MUST-NOT-LEAK"
      },
      "receivedAt": "2026-08-07T12:00:01.000Z"
    },
    {
      "id": "event-alpha",
      "type": "meeting",
      "payload": {
        "private": "SELECTED-PAYLOAD-MUST-NOT-LEAK"
      },
      "receivedAt": "2026-08-07T12:00:00.000Z"
    }
  ],
  "jobs": [
    {
      "id": "job-event-alpha",
      "eventId": "event-alpha",
      "status": "pending",
      "lastError": "RELATED-JOB-DETAIL-MUST-NOT-LEAK"
    },
    {
      "id": "job-other",
      "eventId": "event-other",
      "status": "dead",
      "lastError": "UNRELATED-JOB-DETAIL-MUST-NOT-LEAK"
    }
  ],
  "approvals": [
    {
      "id": "appr-event-alpha",
      "jobId": "job-event-alpha",
      "action": {
        "to": "APPROVAL-TO-MUST-NOT-LEAK",
        "subject": "APPROVAL-SUBJECT-MUST-NOT-LEAK",
        "body": "APPROVAL-BODY-MUST-NOT-LEAK"
      },
      "status": "rejected",
      "reason": "APPROVAL-REASON-MUST-NOT-LEAK"
    }
  ],
  "outbox": [
    {
      "to": "OUTBOX-TO-MUST-NOT-LEAK",
      "subject": "OUTBOX-SUBJECT-MUST-NOT-LEAK",
      "body": "OUTBOX-BODY-MUST-NOT-LEAK",
      "approvalId": "appr-event-alpha"
    }
  ],
  "metadata": {
    "note": "TOP-LEVEL-METADATA-MUST-NOT-LEAK"
  }
}
```

For `event-alpha`, expected stdout is exactly:

```text
{"id":"event-alpha","type":"meeting","receivedAt":"2026-08-07T12:00:00.000Z","hasJob":true}
```

followed by one LF. For `event-zulu`, expected stdout is exactly:

```text
{"id":"event-zulu","type":"email","receivedAt":"2026-08-07T12:00:01.000Z","hasJob":false}
```

followed by one LF. Every uppercase value containing `MUST-NOT-LEAK` is a
frozen sensitive token that must be absent from stdout and stderr.

### Frozen Fixtures B-C and read-failure paths

- Fixture B is the exact UTF-8 byte sequence
  `{"events":[],"jobs":[],"approvals":[],"outbox":[]}\n`. It is structurally
  valid and every event lookup returns the frozen not-found error.
- Fixture C is the exact UTF-8 byte sequence
  `{"events":[],"payload":"MALFORMED-PAYLOAD-MUST-NOT-LEAK"\n`. It is
  malformed because its object is not closed.
- The missing-path case uses `<tmp>/missing-state.json`, which does not exist.
- The directory-path case uses `<tmp>/state-directory`, an empty directory.
- Frozen lookup IDs are the exact strings `event-alpha`, `event-zulu`,
  `event-missing`, and `Event-alpha`. `Event-alpha` is byte-distinct from
  every valid fixture ID and proves that lookup does not case-fold.

### Frozen structural-invalid fixtures

Each table value is written directly as the displayed compact UTF-8 JSON plus
one LF. No invalid fixture is created by casing, normalization, or mutation.

| ID | Exact JSON value | Violated rule |
|---|---|---|
| S1 | `[]` | root is not an object |
| S2 | `{"events":[],"jobs":[],"approvals":[]}` | missing own `outbox` array |
| S3 | `{"events":{},"jobs":[],"approvals":[],"outbox":[]}` | `events` is not an array |
| S4 | `{"events":[],"jobs":{},"approvals":[],"outbox":[]}` | `jobs` is not an array |
| S5 | `{"events":[],"jobs":[],"approvals":{},"outbox":[]}` | `approvals` is not an array |
| S6 | `{"events":[],"jobs":[],"approvals":[],"outbox":{}}` | `outbox` is not an array |
| S7 | `{"events":[null],"jobs":[],"approvals":[],"outbox":[]}` | event is not an object |
| S8 | `{"events":[[]],"jobs":[],"approvals":[],"outbox":[]}` | event is an array |
| S9 | `{"events":[{"id":"","type":"meeting","receivedAt":"2026-08-07T12:00:00.000Z"}],"jobs":[],"approvals":[],"outbox":[]}` | event `id` is empty |
| S10 | `{"events":[{"id":7,"type":"meeting","receivedAt":"2026-08-07T12:00:00.000Z"}],"jobs":[],"approvals":[],"outbox":[]}` | event `id` is not a string |
| S11 | `{"events":[{"id":"event-a","type":7,"receivedAt":"2026-08-07T12:00:00.000Z"}],"jobs":[],"approvals":[],"outbox":[]}` | event `type` is not a string |
| S12 | `{"events":[{"id":"event-a","type":"","receivedAt":"2026-08-07T12:00:00.000Z"}],"jobs":[],"approvals":[],"outbox":[]}` | event `type` is empty |
| S13 | `{"events":[{"id":"event-a","type":"meeting","receivedAt":null}],"jobs":[],"approvals":[],"outbox":[]}` | event `receivedAt` is not a string |
| S14 | `{"events":[{"id":"event-a","type":"meeting","receivedAt":""}],"jobs":[],"approvals":[],"outbox":[]}` | event `receivedAt` is empty |
| S15 | `{"events":[{"id":"same","type":"a","receivedAt":"1"},{"id":"same","type":"b","receivedAt":"2"}],"jobs":[],"approvals":[],"outbox":[]}` | event IDs are duplicated |
| S16 | `{"events":[],"jobs":[null],"approvals":[],"outbox":[]}` | job is not an object |
| S17 | `{"events":[],"jobs":[[]],"approvals":[],"outbox":[]}` | job is an array |
| S18 | `{"events":[],"jobs":[{"eventId":7}],"approvals":[],"outbox":[]}` | job `eventId` is not a string |
| S19 | `{"events":[],"jobs":[{"eventId":""}],"approvals":[],"outbox":[]}` | job `eventId` is empty |

Before invoking the command, the acceptance test asserts that the serialized
bytes of Fixtures A-C and S1-S19 are pairwise distinct. The case-distinct
lookup ID is frozen directly and is not produced by a runtime transformation.

### File locations, protected boundaries, lifecycle, and verification

- Builder ownership is creation of `app/tools/event-inspect.js` only. It is a
  standalone CommonJS command whose complete module-specifier set is
  `node:fs`; its only filesystem operation is `readFileSync`.
- Test-author ownership is creation of
  `app/tests/event-inspect.test.js` only, using `node:test`, isolated temporary
  directories, and child Node processes with a 5-second timeout.
- `app/tests/health.test.js` is the existing protected fixture seam and is
  reference-only. It contains no import/export allowlist, exact-source
  assertion, source snapshot, or module-specifier restriction for the new
  Builder file. No existing protected test imports or snapshots
  `app/tools/event-inspect.js`. Existing source-boundary assertions in
  `app/tests/dead-jobs.test.js` and `app/tests/blocked-approvals.test.js`
  inspect only their corresponding tool files.
- Every test case creates its own temporary parent, writes only its frozen
  runtime fixture, snapshots bytes and numeric `mode`, `size`, and `mtimeMs`,
  snapshots sorted parent entries, waits for the child process, and removes
  the parent recursively in `finally`. Tests share no file, process, port,
  database, table, generated identifier, sequence, counter, clock, or
  timestamp. There are no foreign keys or dependent-table cleanup
  obligations.
- Targeted verification is
  `node --test app/tests/event-inspect.test.js`. Required repository
  verification after implementation is `npm test --prefix app` and
  `.github/scripts/test-immutability-check.sh`.
- The test-author stage precedes the Builder stage. Neither stage changes an
  existing test, `app/server.js`, `app/package.json`, a lockfile, dependency,
  persisted schema, `factory/KIT_PIN`, ticket state, ledger, or Factory
  control.
- This local CLI has no endpoint, UI, cross-origin request, authentication,
  cookie, CORS, domain, credential, preview deployment, or external-send
  behavior. `External: no` remains exact.

## Criteria checklist

- **Pass/fail and unambiguous:** AC1-7 freeze exact arguments, fixture bytes,
  outputs, errors, exit codes, 5-second process bounds, and filesystem
  observations. AC8 freezes statically checkable source boundaries. AC9 is
  decided from Git history, per-stage diffs, and three exact command exits.
- **Coverage:** AC1 covers exact selection, the output shape, `hasJob: true`,
  repeat determinism, redaction, and Fixture A. AC2 covers `hasJob: false`.
  AC3 covers empty valid state, case-sensitive and absent lookup misses, and
  Fixture B. AC4-6 cover arity, read, and parse precedence. AC7 covers every
  structural rule and fixture distinctness. AC8 covers offline/read-only
  source boundaries; AC9 covers files, stage ordering, and verification.
- **Derived-fixture distinctness:** A-C and S1-S19 are exact literals and are
  pairwise checked before use. No invalid serialized fixture is derived by a
  casing, normalization, or mutation operation. The case-distinct lookup ID
  is itself frozen as exact bytes.
- **Generated-value determinism:** all IDs and timestamps are literal. No
  generated identifier, sequence, counter, initializer, reset, or clock
  participates in an expectation.
- **Fixture lifecycle closure:** every case has isolated setup, waits for its
  child process, and performs recursive `finally` teardown. No serialized
  invocation shares a resource; there is no database, parent row, child
  table, foreign key, or cascading cleanup.
- **Protected boundaries:** all tracked protected tests were inspected. The
  associated seam has no allowlist, source assertion, snapshot, or parser
  restricting the new file or its sole `node:fs` specifier, so
  `Protected-Test-Conflicts: none` is exact.
- **Global protected text:** the pinned readiness checker returned
  `GLOBAL TEXT PASS` separately for the usage line, all four error lines, and
  both exact successful output lines. The command adds no UI or accessible
  name.
- **Deploy/topology completeness:** the command is a local file-to-stdout
  process with no origin, authentication, cookie, CORS, credential, domain,
  preview-deploy, endpoint, UI, or external-send surface.

## Ambiguity log

1. **Output field for job existence.** The seed requires “whether a related
   job exists” without naming the JSON property. Resolved as the Boolean
   `hasJob`, the minimal explicit field paired with the three named stored
   event fields; its exact key order and true/false derivation are frozen.
2. **Meaning of related job.** [`SPEC.md` behavior 2](../../SPEC.md#behavior-spec)
   and the [`app/server.js` state model](../../app/server.js) link jobs through
   `job.eventId`. Resolved as exact equality with the selected stored event ID;
   no job ID, status, attempts, or error is exposed.
3. **Structural-validation depth.** The product has no general state-file
   validator. Resolved narrowly: validate all four collections, every event
   field needed for safe deterministic output, event-ID uniqueness required
   by idempotent intake, and every job `eventId` needed for `hasJob`; approval,
   outbox, payload, and unrelated record fields remain opaque.
4. **Exact lookup and errors.** The seed does not specify normalization, error
   text, exit codes, or precedence. No entry in
   [`factory/rulings.md`](../rulings.md) addresses offline event inspection.
   Resolved as strict case-sensitive equality, usage exit `2`, operational
   exits `1`, four path-independent lines, and the frozen precedence above.
5. **Read-only and self-termination proof.** Resolved with 5-second child
   timeouts, repeated invocation, byte/stat/directory-entry comparisons, and
   the exact production module/API boundary. No service or network module is
   admitted.
6. **Sensitive-data boundary.** “Safe operational metadata” did not supply
   fixture values. Fixture A gives every excluded payload, job, approval,
   outbox, and additive top-level field a unique sentinel; exact output and
   sentinel exclusions make leakage mechanically observable.
7. **Cross-ticket file ownership.** Repository-wide ticket grep found only
   canceled T-177 declaring the same paths; its local record says
   `Superseded-By: T-179`, so T-179 is the replacement owner. The stale remote
   T-177 branch has no live worktree. The other listed ticket worktrees and
   remote ticket branches own distinct paths; T-178 owns
   `job-inspect.js`/`job-inspect.test.js`. No concurrent file-boundary conflict
   remains.
8. **Protected source and global text.** The declared
   `app/tests/health.test.js` seam and all tracked tests contain no static
   boundary for the new file. The pinned readiness check accepted every fixed
   visible literal, so no protected-test conflict declaration or seam
   expansion is required.
9. **Topology and external effects.** This is an offline local file read with
   no HTTP route, auth, cookie, CORS, origin, credential, preview deployment,
   UI, approval execution, or send. No deploy topology or `external` label is
   required.
10. **Superseded contract evidence.** The stale remote T-177 branch contains
    a frozen version 1 for the canceled ticket identity. T-179 is a new ticket,
    not a new epoch appended to T-177; this contract freezes T-179 version 1
    while preserving the canceled T-177 record unchanged.

## Coordination

T-179 supersedes canceled T-177 and owns only its two new `app/` paths. It has
no file overlap with the completed offline commands T-173 through T-176 or
the separately owned T-178 job inspector. Its semantic change remains
entirely inside Relay's certified `NONVISUAL_PATHS` policy.

## Planning log

- 2026-08-09: Planner froze contract version 1 for the exact offline command,
  event projection, `hasJob` derivation, deterministic success and error
  streams, valid and invalid fixtures, privacy checks, lifecycle closure,
  protected-source boundaries, and stage ownership. The canceled predecessor,
  sibling tickets, active and parked worktrees, remote ticket branches,
  `factory/rulings.md`, the current state model, all protected tests, and
  global visible literals were checked; no operator ruling, protected-test
  conflict, live file conflict, deployment topology, or external-send label
  was required.
- 2026-08-09: spec-lint of frozen contract version 1 against `SPEC.md`
  (behavior spec 1, 2, 6, 7 and conformance cases 1, 2, 7), `AGENTS.md`
  (boundaries, verification), `app/server.js`, `app/package.json`, the whole
  tracked `app/tests/` suite (including the declared seam
  `app/tests/health.test.js` and the exact-source assertions in
  `app/tests/dead-jobs.test.js` and `app/tests/blocked-approvals.test.js`),
  `.github/scripts/test-immutability-check.sh`, `factory/rulings.md`, sibling
  and superseded ticket declarations, the worktree and branch list, and the
  pinned `scripts/ticket-readiness.py` at `factory/KIT_PIN`.

  Contract-element coverage:

  | Contract element (material behavior) | Positive | Failure / empty | Duplicate | Out of scope |
  |---|---|---|---|---|
  | Argument arity — exactly two positional | AC1-AC3, AC5-AC7 invoke with two arguments | AC4: zero, one, and three args → exit `2`, empty stdout, exact usage line, absent candidate path still absent, empty parent | the three arity cases share one frozen usage literal | four-or-more args untested (finding 5) |
  | Read of `<state-file>` as UTF-8 | AC1-AC3, AC6, AC7 read frozen fixtures | AC5: absent path and directory path → exit `1`, exact `cannot read state file`, absent path still absent, directory entries unchanged | both read failures share one frozen literal | OS read-denial grouped as an equivalent read failure (finding 7) |
  | `JSON.parse` failure | — | AC6: Fixture C unclosed object → exit `1`, exact `state file is not valid JSON` | — | — |
  | Structural rules on the four top-level collections | AC1-AC3 accept Fixtures A and B | AC7: S1 (non-object root), S2 (missing own `outbox`), S3-S6 (each collection non-array) | S3-S6 are one rule applied per collection | missing `events`, `jobs`, `approvals` and `null`/scalar roots (finding 6) |
  | Structural rules on event elements | AC1, AC2 accept Fixture A's two full events | AC7: S7, S8 (non-object, array element), S9-S14 (empty and non-string `id`, `type`, `receivedAt`) | S9-S14 are the same non-empty-string rule over three fields | wholly absent event keys grouped with the non-string cases (finding 6) |
  | Event-ID pairwise uniqueness | AC1, AC2 use Fixture A's two distinct IDs | AC7: S15 duplicate `same` IDs → exit `1`, exact `invalid Relay state` | — | — |
  | Structural rules on job elements (`eventId`) | AC1 accepts Fixture A's related and unrelated jobs | AC7: S16, S17 (non-object, array element), S18, S19 (non-string, empty `eventId`) | S18, S19 share one rule | all other job fields are opaque by contract |
  | Exact case-sensitive lookup, no trim/normalize/case-fold | AC1 selects `event-alpha`, AC2 selects `event-zulu` from an unsorted `events` array | AC3: `event-missing` and `Event-alpha` against Fixture A and `event-missing` against empty valid Fixture B → exit `1`, exact `event not found` | the three misses share one frozen literal | Unicode-normalization inputs excluded by the contract's strict-equality rule |
  | Success projection — exactly `id`, `type`, `receivedAt`, `hasJob`, in that order | AC1, AC2 byte-exact stdout | AC3 proves a non-selecting run emits zero stdout bytes | AC1 and AC2 pin two distinct projections from one fixture | — |
  | `hasJob` derivation by strict `job.eventId` equality | AC1: `true` for `event-alpha` | AC2: `false` for `event-zulu` despite Fixture A holding a job whose `eventId` is `event-other` | Fixture A carries both a matching and a non-matching job | multi-job matches are an equivalent permutation of the same `some` rule |
  | Compact serialization plus exactly one trailing LF, exit `0`, empty stderr | AC1, AC2 byte-exact stdout and zero stderr bytes | AC3-AC7 require zero stdout bytes on every error path | AC1's two invocations pin repeat determinism | — |
  | Error-stream discipline — one LF-terminated line, no path, ID, OS text, parser text, input bytes, stack, payload, job, approval, or outbox field, no second line | — | AC3-AC7 assert exact stderr bytes for all four error literals | the four error literals plus the usage literal are pairwise distinguished by AC3-AC7 | — |
  | Validation precedence: arity, read, parse, structure, lookup | — | AC4's one-argument case names an absent path and still requires exit `2`, so arity beats read; AC5 vs AC6 separates read from parse; AC7's S-fixtures parse cleanly yet must yield `invalid Relay state`, and S2-S6, S16-S19 hold empty or absent `events`, so structure beats lookup | — | no criterion combines two failures beyond AC4's absent path (finding 5) |
  | Redaction of payloads, job details, approval actions and reasons, outbox rows, additive top-level metadata | AC1: every frozen `MUST-NOT-LEAK` sentinel absent from both streams | AC6: Fixture C's embedded sentinel absent from both streams | every excluded field in Fixture A carries a unique sentinel, including the selected event's own payload | — |
  | Read-only, offline execution: no write, create, delete, listener, or outbound request | AC1, AC2 pin bytes, numeric mode, size, `mtimeMs`, and sorted parent entries across two invocations | AC3-AC7 repeat the same invariance on every error path; AC4 keeps the candidate path absent and its parent empty; AC5 keeps the directory a directory | AC1's repeated invocation re-checks invariance after the second run | — |
  | Source boundary — complete specifier set `node:fs`, sole `readFileSync`, no `server.js`, `fetch(`, `WebSocket`, `node:http`, `node:https`, `node:net`, `node:child_process`, `setTimeout`, `setInterval` | AC8 static inspection of the Builder file | — | — | — |
  | Self-termination within 5 seconds | AC1, AC2 | AC3-AC7 apply the same bound to every error invocation | — | signal-kill disposition unasserted (finding 8) |
  | Fixture lifecycle and isolation | per-case temporary parent, only its frozen fixture written, child awaited, recursive `finally` teardown | AC7 requires A-C and S1-S19 pairwise byte-distinct before any invocation | — | no database, parent row, dependent table, non-cascading foreign key, or shared serialized resource exists in this product |
  | Stage file ownership, commit order, verification commands | AC9: test-author commit creates only `app/tests/event-inspect.test.js`, Builder commit only `app/tools/event-inspect.js` | AC9 requires the targeted command, `npm test --prefix app`, and `.github/scripts/test-immutability-check.sh` to exit `0` with existing tests, `app/server.js`, `app/package.json`, lockfiles, dependencies, persisted schema, `factory/KIT_PIN`, and Factory controls unchanged | — | — |
  | No endpoint, HTTP shape, route, UI selector, or global page-shell text introduced | contract states it; the seven new fixed literals are stdout/stderr only | — | — | — |

  Findings:
  1. Criteria quality — AC1-AC7 freeze exact arguments, fixture bytes, exit
     codes, byte-exact stdout and stderr, a 5-second process bound, and the
     filesystem observations (bytes, numeric mode, size, `mtimeMs`, sorted
     parent entries). AC8 freezes a statically checkable source boundary and
     AC9 is decided from Git history, per-stage diffs, and three exact exit
     statuses. No criterion contains a qualitative term the builder must
     interpret to choose product behavior. No blocking finding.
  2. Contract coverage — every material behavior in the table above has at
     least one covering criterion and every criterion is implementable against
     the contract as written. I recomputed both Fixture A projections from the
     frozen rules (exact-equality selection, fixed key order, `some` over
     `job.eventId`, compact `JSON.stringify`) and both are byte-identical to
     the frozen expected stdout lines; `Event-alpha` selects nothing. Fixture B
     classifies valid with no events, Fixture C fails `JSON.parse`, and each of
     S1-S19 classifies structurally invalid for exactly the rule its table row
     names, under a validator built only from the frozen structural text. No
     blocking finding.
  3. Consistency — the contract's state shape matches the persisted model in
     `app/server.js` (`{events, jobs, approvals, outbox}` at `server.js:16`),
     the stored event fields `id`, `type`, `payload`, `receivedAt` at
     `server.js:40`, and the job-to-event `eventId` link at `server.js:43` and
     `server.js:55`; those match `SPEC.md` behaviors 1, 2, 6, 7 and conformance
     cases 1, 2, 7. Every linked `SPEC.md` and `AGENTS.md` anchor and relative
     path resolves. The two recorded rulings in `factory/rulings.md` concern
     detail-route envelopes with concurrent `app/server.js` ownership and
     T-178's verification set; neither is contradicted by this offline command,
     and the frozen verification set here is the ticket's own targeted test plus
     the two `AGENTS.md` checks, matching the T-178 ruling's shape rather than
     adding repository baseline checks. Fixture ownership is exact: only
     canceled `T-177` (`Superseded-By: T-179`) and this ticket name
     `event-inspect`, `T-178` owns `job-inspect`, and no live worktree holds
     `ticket/T-177`. Derived-fixture check: I serialized A, B, C and S1-S19
     exactly as frozen and confirmed all 22 byte sequences are pairwise
     distinct, that C alone fails `JSON.parse`, and that no invalid fixture is a
     casing, normalization, or mutation transform of an accepted valid fixture —
     the case-distinct lookup ID `Event-alpha` is a frozen literal, not a
     runtime transformation. Generated-value check: every ID and timestamp in
     every expectation is a frozen literal; no initializer, reset, sequence,
     counter, or clock participates, so there is no unproducible value and no
     setup correction to scope. Lifecycle closure: the single serialized command
     `node --test app/tests/event-inspect.test.js` gives each case its own
     temporary parent and recursive `finally` teardown and shares no file,
     process, port, database, table, or timestamp; this product has no parent
     row, dependent table, or non-cascading foreign key, so no child-first
     cleanup obligation and no helper outside Test-author ownership is
     implicated. Protected-boundary check, made independently of the ticket's
     claim: the Builder file is new and untracked; the only exact-source
     assertions in the tracked suite are `dead-jobs.test.js:344-368` and
     `blocked-approvals.test.js:440-453`, each of which reads only its own
     `TOOL` constant (`app/tools/dead-jobs.js`, `app/tools/blocked-approvals.js`)
     and, in the first case, its own test source; the declared seam
     `app/tests/health.test.js` contains no import/export allowlist, source
     snapshot, or module-specifier restriction; and every `readdirSync` in the
     suite reads a per-case temporary directory, not `app/tools` or `app/tests`,
     so adding two files collides with nothing. `Protected-Test-Conflicts: none`
     is therefore exact. Global protected text, re-run in this worktree with the
     pinned `scripts/ticket-readiness.py` at `863e38a6`: `--ticket T-179`
     returned `READINESS PASS`, and `--global-literal` returned
     `GLOBAL TEXT PASS` separately for the usage line, all four error lines, and
     both success lines; the checker's static `*ByText` scan is the only
     supported form and the tracked suite contains no `*ByText` assertion at
     all, so no static check was unknown or unparsable. `npm test --prefix app`
     is `node --test tests/*.test.js`, so the new test file is collected without
     editing `app/package.json`, and `.github/scripts/test-immutability-check.sh`
     exempts `factory/`, so role-owned ticket-log appends cannot break AC9's
     commit-ordering requirement. No blocking finding.
  4. Edge coverage — the security-relevant surface is disclosure, and it is
     covered: Fixture A gives the selected event's payload, the unselected
     event's payload, the related and unrelated job details, the approval
     action and reason, the outbox row, and an additive top-level metadata
     field each a unique sentinel that AC1 requires absent from both streams,
     and the four error literals are path-, ID-, and parser-independent by
     contract and byte-exact by AC3-AC7. Data loss and external effect are
     covered twice over: AC1-AC7 assert byte, mode, size, `mtimeMs`, and
     directory-entry invariance on every path, and AC8 statically confines the
     Builder file to `node:fs.readFileSync` with no network, server, child
     process, or timer reference. There is no authorization, isolation,
     irreversible, or send surface to cover — `External: no` is exact for a
     local file-to-stdout read. No blocking finding.
  5. AC4 fixes zero, one, and three arguments but not four or more, and apart
     from AC4's absent one-argument path no criterion combines two failure
     conditions. Both are equivalent permutations of the frozen precedence
     order, which AC4-AC7 already pin at each adjacent boundary, so neither
     leaves a product decision to the builder.
     SPEC-WARN: the test-author may add one four-argument case to the arity loop and one combined case such as three arguments against the directory path.
  6. The S-table omits missing `events`, `jobs`, and `approvals`, a `null` or
     scalar root, and event or job objects lacking the key entirely rather than
     holding a wrong-typed value. Each is the same `hasOwnProperty` plus type
     rule that S2-S6, S1, and S9-S19 already exercise per collection and per
     field, so these are equivalent permutations, not gaps.
     SPEC-WARN: the test-author may add these rows to the S-table loop, keeping the frozen S1-S19 rows unchanged.
  7. The contract names an OS read denial as a third read failure, but only the
     absent path and the directory path are frozen. A permission-denied fixture
     is environment-dependent and the contract already groups all three under
     one literal, so this is not a coverage gap.
     SPEC-WARN: no action required — do not add a `chmod`-based case that would behave differently under a root CI user.
  8. AC1-AC7 require completion within 5 seconds but no criterion states how a
     breach is observed, and `node:test` applies no default per-case timeout.
     The bound is still pass/fail decidable and the contract fixes both the
     value and the child-process mechanism.
     SPEC-WARN: pass an explicit 5000 ms `timeout` to each child invocation and assert the terminating signal is null, so a timeout kill fails as a timeout rather than as a mismatched exit code.
  9. The explicit sentinel-absence sweep appears only in AC1 and AC6. AC2-AC5
     and AC7 assert byte-exact stdout and stderr, which already excludes every
     sentinel, so this is a defensive redundancy rather than a hole.
     SPEC-WARN: the test-author may reuse one shared sentinel-absence helper across all invocations at negligible cost.
  10. The contract fixes relative-path resolution against the process working
      directory, but every criterion uses absolute temporary paths, so that
      sentence is unexercised. It describes no distinct product decision — the
      command passes the argument straight to `readFileSync`.
      SPEC-WARN: no action required.
  11. `Fixture-Seams` lists `app/tests/health.test.js` while
      `Protected-Test-Conflicts` is `none` and Test-author ownership is
      `app/tests/event-inspect.test.js` only. Readiness accepts this shape and
      the seam authorizes no edit; the file stays reference-only.
      SPEC-WARN: no action required — the test-author must not modify the declared seam.

- 2026-08-09: Test author wrote the failing acceptance tests in
  `app/tests/event-inspect.test.js` (sole Test-author-owned path) as the first
  commit on `ticket/T-179`. No existing test, seam, tool, application file, or
  Factory control was edited; the declared fixture seam
  `app/tests/health.test.js` stays reference-only.

  Tests written, one per acceptance criterion and named after it:

  | Test | Criterion | Asserts |
  |---|---|---|
  | AC1 | 1 | two `event-alpha` invocations against Fixture A: exit `0`, zero stderr bytes, byte-exact `hasJob:true` projection in the frozen key order, and unchanged fixture bytes, numeric mode, size, `mtimeMs`, and sorted parent entries after each run |
  | AC2 | 2 | `event-zulu` against Fixture A: exit `0`, zero stderr bytes, byte-exact `hasJob:false` projection, fixture and parent unchanged |
  | AC3 | 3 | `event-missing` and `Event-alpha` against Fixture A and `event-missing` against Fixture B: exit `1`, zero stdout bytes, exact `event-inspect: event not found` line, fixtures and parents unchanged |
  | AC4 | 4 | zero, one, and three positional arguments: exit `2`, zero stdout bytes, exact usage line; the one-argument candidate path stays absent and its parent stays empty |
  | AC5 | 5 | absent path and directory path: exit `1`, zero stdout bytes, exact `event-inspect: cannot read state file` line; absent path still absent, directory still a directory with identical entries and stat, parents unchanged |
  | AC6 | 6 | Fixture C: exit `1`, zero stdout bytes, exact `event-inspect: state file is not valid JSON` line, `MALFORMED-PAYLOAD-MUST-NOT-LEAK` absent from both streams, fixture and parent unchanged |
  | AC7 | 7 | pairwise byte-distinctness of Fixtures A-C and S1-S19 before any invocation, then each of S1-S19: exit `1`, zero stdout bytes, exact `event-inspect: invalid Relay state` line, fixture and parent unchanged |
  | AC8 | 8 | static inspection of the Builder file: complete CommonJS specifier set exactly `node:fs`, the only `node:fs` operation used (destructured, namespaced, or inline) exactly `readFileSync`, and no `server.js`, `fetch(`, `WebSocket`, `node:http`, `node:https`, `node:net`, `node:child_process`, `setTimeout`, or `setInterval` reference |

  Applied SPEC-WARN 8: every child invocation passes an explicit 5000 ms
  `timeout` and asserts the terminating signal is null, so a breach of the
  frozen 5-second bound fails as a timeout rather than as a mismatched exit
  code. Applied SPEC-WARN 9: one shared sentinel-absence helper runs on every
  invocation's stdout and stderr, covering all thirteen frozen
  `MUST-NOT-LEAK` tokens. The optional additions in SPEC-WARN 5 and 6
  (four-or-more arguments, combined failure cases, extra S-table rows) were
  not taken; the frozen arity set and S-table are encoded exactly as written.
  SPEC-WARN 7, 10, and 11 required no action, and the declared seam was not
  modified.

  Not expressible as a test in this file — demo-check item for the Narrator's
  bundle:

  - **AC9 in full.** Commit ordering, per-stage diff scope
    (`app/tests/event-inspect.test.js` for this stage,
    `app/tools/event-inspect.js` for the Builder), the unchanged state of
    existing application files, `app/server.js`, `app/package.json`,
    lockfiles, dependencies, persisted schema, `factory/KIT_PIN`, and Factory
    controls, and the exit statuses of
    `node --test app/tests/event-inspect.test.js`, `npm test --prefix app`,
    and `.github/scripts/test-immutability-check.sh` are properties of Git
    history and the verification run, not of a `node:test` case. A test
    asserting them from inside the suite it verifies would be circular and
    would fail for setup reasons at this stage.

  Failing-run evidence before implementation: `node --test
  app/tests/event-inspect.test.js` reports `tests 8`, `pass 0`, `fail 8`;
  AC1-AC7 fail because the child process exits with `MODULE_NOT_FOUND` for
  `app/tools/event-inspect.js` instead of the frozen streams, and AC8 fails on
  the file's absence. Every failure is the missing feature, not a setup error.

  No contract blocker was found. Fixtures A-C and S1-S19 were serialized
  exactly as frozen and confirmed pairwise byte-distinct (22 of 22), Fixture C
  alone fails `JSON.parse`, and both frozen success lines were recomputed
  byte-identically from the frozen selection, key order, and `hasJob` rules.
  No invalid fixture is derived by casing, normalization, or mutation — the
  case-distinct lookup ID `Event-alpha` is a frozen literal. No generated
  identifier, sequence, counter, initializer, reset, clock, or timestamp
  participates in any assertion. There is no database, parent row, dependent
  table, or non-cascading foreign key, so the single serialized command needs
  no child-first cleanup beyond each case's recursive `finally` teardown of
  its own temporary parent. `Protected-Test-Conflicts: none` is exact: no
  tracked protected test imports, snapshots, or allowlists
  `app/tools/event-inspect.js`.

SPEC-LINT: PASS

Kit-SHA: 863e38a68235dd682c56ca44ee079797f626413a

- 2026-08-09: Builder added the offline event-inspect command with the frozen
  argument, read, parse, structural-validation, exact-selection, output, and
  deterministic-error behavior. Targeted verification
  `node --test app/tests/event-inspect.test.js` passed 8/8. No scoped lint or
  typecheck command exists; `npm test --prefix app` and
  `.github/scripts/test-immutability-check.sh` are deferred to protected CI and
  final certification under the Builder v6 verification boundary.
  PR description: Adds deterministic, data-minimized lookup of one Relay event
  from a persisted state file. Touches `app/tools/event-inspect.js` and this
  role-owned ticket-log append. No concerns.
