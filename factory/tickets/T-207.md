# T-207 — count Relay jobs with recorded errors offline

State: Backlog
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-207
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/job-detail.test.js
Authentication-Seams: none
Builder ownership: app/tools/job-error-count.js only
Test-author ownership: app/tests/job-error-count.test.js only

## Description

Add a zero-dependency, read-only Node command that counts persisted Relay jobs
whose `lastError` is a non-empty string while the service is offline.

## Acceptance criteria

1. `node app/tools/job-error-count.js <state-file>` exits zero and emits
   deterministic compact JSON with exactly one `jobsWithErrors` nonnegative
   integer.
2. A valid state with no jobs carrying a non-empty string `lastError` returns
   `{"jobsWithErrors":0}` followed by one newline.
3. Missing, unreadable, malformed, or structurally invalid state is rejected
   with fixed redacted errors that reveal no path, job error, or stored
   content; complete job validation precedes counting.
4. Every invocation is offline, read-only, and self-terminating; input bytes,
   mode, timestamps, and sibling directory entries remain unchanged.
5. Complete acceptance tests are committed before implementation. Targeted,
   full app, and immutability checks pass without dependency, server, schema,
   package-manifest, lockfile, or Factory-control changes.

## Coordination

This ticket owns only its two new `app/` paths and is independent of T-208 and
T-209.
