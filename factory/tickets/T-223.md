# T-223 — count Relay approvals with one body offline

State: Backlog
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-223
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/approval-detail.test.js
Authentication-Seams: none
Builder ownership: app/tools/approval-body-count.js only
Test-author ownership: app/tests/approval-body-count.test.js only

## Description

Add a zero-dependency, read-only Node command that counts persisted Relay
approvals whose action body exactly matches one operator-supplied string while
the service is offline.

## Acceptance criteria

1. `node app/tools/approval-body-count.js <state-file> <body>` exits zero and
   emits deterministic compact JSON with exactly one `approvalsWithBody`
   nonnegative integer.
2. Matching compares stored and supplied strings exactly, without trimming,
   parsing, normalization, or coercion. No exact match returns
   `{"approvalsWithBody":0}` followed by one newline.
3. Missing arguments and missing, unreadable, malformed, or structurally
   invalid state are rejected with fixed redacted errors that reveal no path,
   body, approval ID, job link, action, reason, timestamp, or stored content;
   complete approval validation precedes counting.
4. Every invocation is offline, read-only, and self-terminating; input bytes,
   mode, timestamps, and sibling directory entries remain unchanged.
5. Complete acceptance tests are committed before implementation. Targeted,
   full app, and immutability checks pass without dependency, server, schema,
   package-manifest, lockfile, or Factory-control changes.

## Coordination

This ticket owns only its two new `app/` paths and is independent of T-222 and
T-224. The q78 cohort T-219 through T-221 remains historical evidence.
