# T-223 — count Relay approvals with one body offline

Kit-SHA: 7ae447baf7287e32a9df60c9453da6edec454e7b
State: Planning
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-223
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/approval-detail.test.js
Authentication-Seams: none
Builder ownership: app/tools/approval-body-count.js only
Test-author ownership: app/tests/approval-body-count.test.js only

## Spec'd description

Add `app/tools/approval-body-count.js`, a zero-dependency CommonJS command
that reads one persisted Relay state file while Relay is offline, validates the
complete stored approval collection, and counts approvals whose stored
`action.body` value is exactly equal to one operator-supplied `<body>`. It
reports only `{"approvalsWithBody":<count>}`, allowing an operator to
inspect body cardinality without starting Relay, changing durable state,
approving or rejecting an action, creating an outbox receipt, performing a real
send, or exposing the supplied body, stored actions, approval identities,
job links, reasons, statuses, proposal timestamps, events, jobs, outbox
records, or unrelated state.

Product-doc and repository sources:

- [`SPEC.md` behavior 3](../../SPEC.md#behavior-spec) defines each approval's
  proposed send action with `to`, `subject`, and `body` and its initial
  `pending` status.
- [`SPEC.md` behaviors 4 and 5](../../SPEC.md#behavior-spec) define the
  `sent`, `blocked_recipient`, and `rejected` approval outcomes.
- [`SPEC.md` behaviors 6 and 7](../../SPEC.md#behavior-spec) make the
  atomically persisted state file Relay's durable source and make approvals
  operator-visible, while
  [`SPEC.md` conformance cases 5-7](../../SPEC.md#test-cases-the-conformance-suite)
  require the approval gate, allowlist outcome, and duplicate-free recovery.
- [`SPEC.md`'s known modeling limitation](../../SPEC.md#known-modeling-limitation)
  establishes that Relay creates only sandbox outbox records and performs no
  external delivery.
- The current [`app/server.js` state model](../../app/server.js) establishes
  the four stored collections, approval core shape, exact approval statuses,
  proposed action fields, optional rejection reason, and atomic persistence.
- The shipped [`approval-count` command](../../app/tools/approval-count.js)
  and its [acceptance suite](../../app/tests/approval-count.test.js) establish
  the complete approval validation, unique approval identities, count-only
  envelope, redacted failures, and read-only offline convention. The shipped
  [`approval-reason-count` command](../../app/tools/approval-reason-count.js)
  and its
  [acceptance suite](../../app/tests/approval-reason-count.test.js) establish
  validation of the persisted optional `reason` domain. The shipped
  [`event-type-count` command](../../app/tools/event-type-count.js) and its
  [acceptance suite](../../app/tests/event-type-count.test.js) establish
  case-sensitive strict argument-to-field matching.
- The protected
  [approval-detail acceptance suite](../../app/tests/approval-detail.test.js)
  establishes the persisted approval shape used by the declared fixture seam.
- [`AGENTS.md` Boundaries](../../AGENTS.md#boundaries) keeps `app/`
  zero-dependency, and [`AGENTS.md` Verification](../../AGENTS.md#verification)
  defines the later full-suite and immutability checks.

No endpoint, HTTP shape, UI selector, global-shell UI text, persisted schema,
server behavior, package, dependency, lockfile, Factory control,
authentication path, deployment topology, or external-send behavior changes.

## Acceptance criteria

1. Given Frozen Fixture A and body
   `TARGET-BODY-MUST-NOT-LEAK`, each of two invocations of
   `node app/tools/approval-body-count.js <fixture-a-absolute-path>
   TARGET-BODY-MUST-NOT-LEAK` exits `0`, writes exactly
   `{"approvalsWithBody":2}\n` to stdout, and writes zero stderr bytes.
   Neither stream contains any member of the complete frozen sensitive-token
   set. After each invocation, Fixture A has identical bytes and BigInt numeric
   `mode`, `size`, and `mtimeNs`, and its parent has identical sorted entry
   names.
2. Given Frozen Fixture A, the three exact bodies
   `OTHER-BODY-MUST-NOT-LEAK`,
   ` TARGET-BODY-MUST-NOT-LEAK`, and
   `café` are queried in that order. Each invocation exits `0`,
   writes exactly `{"approvalsWithBody":1}\n` to stdout, writes zero stderr
   bytes, emits no frozen sensitive token, and leaves the fixture and parent
   unchanged by every observation named in AC1. Before invocation, the test
   asserts that each query is strictly equal to exactly one stored
   `action.body`.
3. All five frozen zero-match cases succeed: the case-distinct body
   `Target-BODY-MUST-NOT-LEAK`, string body `7`, the
   exact zero-byte body `""`, and decomposed body
   `café` against Frozen Fixture A, plus body
   `TARGET-BODY-MUST-NOT-LEAK` against Frozen Fixture B.
   Each exits `0`, writes exactly `{"approvalsWithBody":0}\n` to stdout,
   writes zero stderr bytes, emits no frozen sensitive token, and leaves its
   fixture and parent unchanged by every observation named in AC1. Before
   invocation, the test asserts that Fixture A's numeric `action.body` is the
   number `7` while the query is string `"7"`; that no stored `action.body` is
   `""`; that the case-distinct query is byte-distinct from every stored string
   `action.body`; and that composed `café` and decomposed
   `café` have the frozen distinct UTF-8 byte sequences
   `636166c3a9` and
   `63616665cc81`. Fixture B's empty `approvals`
   array coexists with the frozen opaque event, job, outbox, and metadata
   values.
4. In separate fresh empty temporary parents, the zero-argument invocation,
   the one-argument invocation with only absent absolute path
   `<parent>/arity-state-MUST-NOT-LEAK.json`, and the three-argument invocation
   with that path,
   `TARGET-BODY-MUST-NOT-LEAK`, and literal `extra` each
   exit `2`, write zero stdout bytes, and write exactly
   `usage: node app/tools/approval-body-count.js <state-file> <body>\n`
   to stderr. The candidate path remains absent and each parent retains empty
   sorted entries.
5. Invoking the command with body
   `TARGET-BODY-MUST-NOT-LEAK` against frozen absent path
   `<parent>/PATH-MUST-NOT-LEAK.json` and, separately, frozen empty directory
   `<parent>/DIRECTORY-MUST-NOT-LEAK` exits `1`, writes zero stdout bytes, and
   writes exactly `approval-body-count: cannot read state file\n` to
   stderr. The absent path remains absent. The directory remains a directory
   with identical BigInt numeric `mode`, `size`, `mtimeNs`, and sorted entries;
   both parents retain their sorted entries.
6. Invoking the command with body
   `TARGET-BODY-MUST-NOT-LEAK` against Frozen Fixture C and,
   separately, Frozen Fixture D exits `1`, writes zero stdout bytes, and writes
   exactly `approval-body-count: state file is not valid JSON\n` to
   stderr. Neither stream contains `MALFORMED-PAYLOAD-MUST-NOT-LEAK`. Each
   fixture's exact bytes and BigInt numeric `mode`, `size`, and `mtimeNs`, and
   each parent's sorted entries, remain unchanged.
7. Each S1-S30 frozen structural-invalid fixture is invoked with body
   `TARGET-BODY-MUST-NOT-LEAK`; every invocation exits `1`,
   writes zero stdout bytes, and writes exactly
   `approval-body-count: invalid Relay state\n` to stderr. S29 and S30 each
   contain one valid matching approval before, respectively, a duplicate ID
   and an invalid sibling, proving complete approval-array validation and ID
   uniqueness precede counting. Before invocation, Fixtures
   A-D and S1-S30 are pairwise byte-distinct; S26's exact `PENDING` bytes are
   asserted distinct from valid `pending`. Every fixture retains its exact
   bytes and BigInt numeric `mode`, `size`, and `mtimeNs`, and every parent
   retains its sorted entries.
8. Every child invocation in AC1-AC7 uses a `5_000` ms timeout, returns with
   `error === undefined` and `signal === null`, and emits none of the complete
   frozen sensitive-token set. Static inspection asserts that the Builder
   file's complete CommonJS module-specifier set is exactly `node:fs`, its
   complete `node:fs` operation set is exactly `readFileSync`, and its source
   contains none of `server.js`, `fetch(`, `WebSocket`, `node:http`,
   `node:https`, `node:net`, `node:child_process`, `setTimeout`, or
   `setInterval`. The Test-author file resolves the Builder path with exactly
   `path.join(__dirname, "..", "tools", "approval-body-count.js")`, and
   static inspection also asserts that the Test-author file's complete
   module-specifier set is exactly `node:assert`,
   `node:child_process`, `node:fs`, `node:os`, `node:path`, and `node:test`.
9. Git history contains the complete Test-author commit before the Builder
   commit. Apart from role-owned ticket-log appends, the Test-author commit
   creates only `app/tests/approval-body-count.test.js` and the Builder
   commit creates only `app/tools/approval-body-count.js`. Existing
   application and test files, `app/server.js`, `app/package.json`, lockfiles,
   dependencies, persisted schema, `factory/KIT_PIN`, and Factory controls
   remain unchanged. After implementation,
   `node --test app/tests/approval-body-count.test.js`,
   `npm test --prefix app`, and
   `.github/scripts/test-immutability-check.sh` each exit `0`.

## Frozen contract — version 1

- **Freeze result:** PASS. Contract version 1 is frozen.

This is the exact interface for the Test-author and Builder. If it proves
wrong, return T-223 to Ready and append a higher-numbered frozen contract;
version 1 must not be edited or removed.

### Command-line interface and failure precedence

- Repository-root invocation is exactly
  `node app/tools/approval-body-count.js <state-file> <body>`.
- Exactly two user-supplied positional arguments are accepted. Any other arity
  exits `2`, writes zero stdout bytes, writes exactly the following single
  LF-terminated line to stderr, and does not read or create a candidate state
  path:

  ```text
  usage: node app/tools/approval-body-count.js <state-file> <body>
  ```

- After arity validation, the command synchronously reads `<state-file>`
  directly as UTF-8. Any `readFileSync` failure, including an absent path, a
  directory in place of a regular file, or an OS read denial, exits `1`,
  writes zero stdout bytes, and writes exactly:

  ```text
  approval-body-count: cannot read state file
  ```

- Readable bytes rejected by `JSON.parse` exit `1`, write zero stdout bytes,
  and write exactly:

  ```text
  approval-body-count: state file is not valid JSON
  ```

- A parsed value that fails the frozen structurally-valid-state rules exits
  `1`, writes zero stdout bytes, and writes exactly:

  ```text
  approval-body-count: invalid Relay state
  ```

- Failure precedence is argument count, file read, JSON parse, complete
  structural validation, then counting.
- Every error is one fixed line terminated by exactly one LF. No error includes
  the state path, supplied body, OS error, parser text, input bytes, stack
  trace, approval field, action value, reason, event field, job field, outbox
  field, or second line.
- Acceptance uses absolute temporary paths. Symlink inputs and portable
  simulation of OS permission denial are outside the required fixture matrix;
  if `readFileSync` throws for either, the frozen read-error result applies.
  The directory fixture freezes the exact command envelope, not any errno,
  error class, or operating-system text.

### Structurally valid state, exact match, and successful output

A structurally valid state for this command is a parsed non-null, non-array
JSON object with own `events`, `jobs`, `approvals`, and `outbox` properties
whose values are arrays. Additional top-level properties are allowed and
ignored.

Every `approvals` element must be a non-null, non-array object with:

- non-empty string own properties `id`, `jobId`, and `proposedAt`;
- an own `status` property exactly equal to `pending`, `sent`, `rejected`, or
  `blocked_recipient`;
- an own `action` property that is a non-null, non-array object with own `to`,
  `subject`, and `body` properties; and
- either no own `reason` property, or an own `reason` whose value is `null` or
  a string, including the empty string.

Approval IDs must be pairwise distinct across the complete array. The values
of `action.to`, `action.subject`, and `action.body` are opaque. Additional
action and approval properties are allowed and ignored. Event, job, and outbox
elements are wholly opaque; only the fact that their top-level collections are
arrays is validated. A `jobId` need not resolve to a stored job. No
relationship is imposed among `status`, `action`, `proposedAt`, and `reason`.

The command validates the complete approval array and ID uniqueness before
counting. An approval contributes one to the count only when
`approval.action.body === <body>` using case-sensitive JavaScript strict
equality. The command does not coerce, trim, case-fold, decode, or
Unicode-normalize either value. Because command-line arguments are strings, an
opaque non-string stored `action.body` never matches. An empty body is a
valid second positional argument. A supplied body with no matches is a
successful query with count zero; there is no not-found error.

Success exits `0`, writes zero stderr bytes, and writes one compact JSON object
followed by exactly one LF. Its only key is:

```json
{"approvalsWithBody":<nonnegative integer count>}
```

`approvalsWithBody` is the number of elements in the validated `approvals`
array whose stored `action.body` matches the supplied body exactly. The
supplied body is not echoed. No stored action value, approval ID,
`jobId`, reason, status, proposal timestamp, additional approval field, event,
job, outbox, or top-level metadata value is emitted.

### Frozen Fixture A — repeated and exact body variants

`app/tests/approval-body-count.test.js` writes this exact parsed value to
an isolated temporary `state.json` using
`JSON.stringify(value, null, 2) + "\n"`:

```json
{
  "events": [
    {
      "payload": "EVENT-PAYLOAD-MUST-NOT-LEAK"
    }
  ],
  "jobs": [
    {
      "lastError": "JOB-ERROR-MUST-NOT-LEAK"
    }
  ],
  "approvals": [
    {
      "id": "APPROVAL-ID-TARGET-ONE-MUST-NOT-LEAK",
      "jobId": "JOB-ID-TARGET-ONE-MUST-NOT-LEAK",
      "action": {
        "to": "TO-TARGET-ONE-MUST-NOT-LEAK",
        "subject": "SUBJECT-TARGET-ONE-MUST-NOT-LEAK",
        "body": "TARGET-BODY-MUST-NOT-LEAK"
      },
      "status": "pending",
      "proposedAt": "2026-08-24T12:00:00.000Z"
    },
    {
      "id": "APPROVAL-ID-OTHER-MUST-NOT-LEAK",
      "jobId": "JOB-ID-OTHER-MUST-NOT-LEAK",
      "action": {
        "to": "TO-OTHER-MUST-NOT-LEAK",
        "subject": "SUBJECT-OTHER-MUST-NOT-LEAK",
        "body": "OTHER-BODY-MUST-NOT-LEAK"
      },
      "status": "sent",
      "proposedAt": "2026-08-24T12:00:01.000Z",
      "reason": ""
    },
    {
      "id": "APPROVAL-ID-NUMERIC-MUST-NOT-LEAK",
      "jobId": "JOB-ID-NUMERIC-MUST-NOT-LEAK",
      "action": {
        "to": false,
        "subject": null,
        "body": 7,
        "ignored": "IGNORED-ACTION-FIELD-MUST-NOT-LEAK"
      },
      "status": "blocked_recipient",
      "proposedAt": "not-an-iso-instant",
      "reason": null,
      "ignoredDiagnostic": "IGNORED-APPROVAL-FIELD-MUST-NOT-LEAK"
    },
    {
      "id": "APPROVAL-ID-TARGET-TWO-MUST-NOT-LEAK",
      "jobId": "JOB-ID-TARGET-TWO-MUST-NOT-LEAK",
      "action": {
        "to": "TO-TARGET-TWO-MUST-NOT-LEAK",
        "subject": "SUBJECT-TARGET-TWO-MUST-NOT-LEAK",
        "body": "TARGET-BODY-MUST-NOT-LEAK"
      },
      "status": "rejected",
      "proposedAt": "2026-08-24T12:00:03.000Z",
      "reason": "REJECTION-REASON-MUST-NOT-LEAK"
    },
    {
      "id": "APPROVAL-ID-LEADING-MUST-NOT-LEAK",
      "jobId": "JOB-ID-LEADING-MUST-NOT-LEAK",
      "action": {
        "to": "TO-LEADING-MUST-NOT-LEAK",
        "subject": "SUBJECT-LEADING-MUST-NOT-LEAK",
        "body": " TARGET-BODY-MUST-NOT-LEAK"
      },
      "status": "sent",
      "proposedAt": "2026-08-24T12:00:04.000Z"
    },
    {
      "id": "APPROVAL-ID-UNICODE-MUST-NOT-LEAK",
      "jobId": "JOB-ID-UNICODE-MUST-NOT-LEAK",
      "action": {
        "to": "TO-UNICODE-MUST-NOT-LEAK",
        "subject": "SUBJECT-UNICODE-MUST-NOT-LEAK",
        "body": "café"
      },
      "status": "pending",
      "proposedAt": "2026-08-24T12:00:05.000Z",
      "reason": " "
    }
  ],
  "outbox": [
    {
      "body": "OUTBOX-CONTENT-MUST-NOT-LEAK"
    }
  ],
  "metadata": {
    "note": "TOP-LEVEL-METADATA-MUST-NOT-LEAK"
  }
}
```

The exact body results are:

| Query argument | Exact stdout |
|---|---|
| `TARGET-BODY-MUST-NOT-LEAK` | `{"approvalsWithBody":2}\n` |
| `OTHER-BODY-MUST-NOT-LEAK` | `{"approvalsWithBody":1}\n` |
| ` TARGET-BODY-MUST-NOT-LEAK` | `{"approvalsWithBody":1}\n` |
| `café` | `{"approvalsWithBody":1}\n` |
| `Target-BODY-MUST-NOT-LEAK` | `{"approvalsWithBody":0}\n` |
| string `7` | `{"approvalsWithBody":0}\n` |
| zero-byte string `""` | `{"approvalsWithBody":0}\n` |
| decomposed `café` | `{"approvalsWithBody":0}\n` |

The leading-space query begins with exactly one U+0020. The composed query
contains U+00E9. The decomposed query contains U+0065 followed by U+0301.
Their frozen UTF-8 hex sequences are the values asserted in AC3.

### Frozen Fixtures B-D and filesystem paths

- Fixture B contains exactly the UTF-8 bytes
  `{"events":[null],"jobs":[7],"approvals":[],"outbox":["OPAQUE-OUTBOX-MUST-NOT-LEAK"],"metadata":false}\n`.
  It is structurally valid, proves that an empty approval collection,
  unconsumed sibling collection elements, and an additional top-level property
  are accepted, and querying
  `TARGET-BODY-MUST-NOT-LEAK` writes exactly
  `{"approvalsWithBody":0}\n`.
- Fixture C contains exactly the UTF-8 bytes
  `{"approvals":[],"payload":"MALFORMED-PAYLOAD-MUST-NOT-LEAK"\n`. It is
  rejected by `JSON.parse` because the object is not closed.
- Fixture D is a zero-byte regular file. It is also rejected by `JSON.parse`.
- Fixtures B-D and S1-S30 each use basename `state.json` in a distinct
  isolated temporary parent; that basename is not a sensitive token.
- The arity path is absent absolute path
  `<temporary-parent>/arity-state-MUST-NOT-LEAK.json`.
- The read-failure paths are absent absolute path
  `<temporary-parent>/PATH-MUST-NOT-LEAK.json` and existing empty directory
  `<temporary-parent>/DIRECTORY-MUST-NOT-LEAK`.
- Every query body is passed directly in the child-process argument
  array. No query is produced by a runtime casing, trimming, normalization, or
  mutation transform.

### Frozen structural-invalid fixtures

Each value below is written directly as the displayed compact UTF-8 JSON plus
one LF. No structural-invalid fixture is produced by casing, normalization, or
mutation of another fixture.

| ID | Exact JSON before final LF | Violated rule |
|---|---|---|
| S1 | `null` | root is null |
| S2 | `[]` | root is an array |
| S3 | `{"events":[],"jobs":[],"approvals":[]}` | missing own `outbox` array |
| S4 | `{"events":{},"jobs":[],"approvals":[],"outbox":[]}` | `events` is not an array |
| S5 | `{"events":[],"jobs":{},"approvals":[],"outbox":[]}` | `jobs` is not an array |
| S6 | `{"events":[],"jobs":[],"approvals":{},"outbox":[]}` | `approvals` is not an array |
| S7 | `{"events":[],"jobs":[],"approvals":[],"outbox":{}}` | `outbox` is not an array |
| S8 | `{"events":[],"jobs":[],"approvals":[null],"outbox":[]}` | approval is null |
| S9 | `{"events":[],"jobs":[],"approvals":[[]],"outbox":[]}` | approval is an array |
| S10 | `{"events":[],"jobs":[],"approvals":[{"jobId":"job-a","action":{"to":"a","subject":"b","body":"c"},"status":"pending","proposedAt":"time-a"}],"outbox":[]}` | approval lacks own `id` |
| S11 | `{"events":[],"jobs":[],"approvals":[{"id":"","jobId":"job-a","action":{"to":"a","subject":"b","body":"c"},"status":"pending","proposedAt":"time-a"}],"outbox":[]}` | approval `id` is empty |
| S12 | `{"events":[],"jobs":[],"approvals":[{"id":7,"jobId":"job-a","action":{"to":"a","subject":"b","body":"c"},"status":"pending","proposedAt":"time-a"}],"outbox":[]}` | approval `id` is not a string |
| S13 | `{"events":[],"jobs":[],"approvals":[{"id":"appr-a","action":{"to":"a","subject":"b","body":"c"},"status":"pending","proposedAt":"time-a"}],"outbox":[]}` | approval lacks own `jobId` |
| S14 | `{"events":[],"jobs":[],"approvals":[{"id":"appr-a","jobId":"","action":{"to":"a","subject":"b","body":"c"},"status":"pending","proposedAt":"time-a"}],"outbox":[]}` | approval `jobId` is empty |
| S15 | `{"events":[],"jobs":[],"approvals":[{"id":"appr-a","jobId":7,"action":{"to":"a","subject":"b","body":"c"},"status":"pending","proposedAt":"time-a"}],"outbox":[]}` | approval `jobId` is not a string |
| S16 | `{"events":[],"jobs":[],"approvals":[{"id":"appr-a","jobId":"job-a","status":"pending","proposedAt":"time-a"}],"outbox":[]}` | approval lacks own `action` |
| S17 | `{"events":[],"jobs":[],"approvals":[{"id":"appr-a","jobId":"job-a","action":null,"status":"pending","proposedAt":"time-a"}],"outbox":[]}` | approval `action` is null |
| S18 | `{"events":[],"jobs":[],"approvals":[{"id":"appr-a","jobId":"job-a","action":[],"status":"pending","proposedAt":"time-a"}],"outbox":[]}` | approval `action` is an array |
| S19 | `{"events":[],"jobs":[],"approvals":[{"id":"appr-a","jobId":"job-a","action":{"subject":"b","body":"c"},"status":"pending","proposedAt":"time-a"}],"outbox":[]}` | action lacks own `to` |
| S20 | `{"events":[],"jobs":[],"approvals":[{"id":"appr-a","jobId":"job-a","action":{"to":"a","body":"c"},"status":"pending","proposedAt":"time-a"}],"outbox":[]}` | action lacks own `subject` |
| S21 | `{"events":[],"jobs":[],"approvals":[{"id":"appr-a","jobId":"job-a","action":{"to":"a","subject":"b"},"status":"pending","proposedAt":"time-a"}],"outbox":[]}` | action lacks own `body` |
| S22 | `{"events":[],"jobs":[],"approvals":[{"id":"appr-a","jobId":"job-a","action":{"to":"a","subject":"b","body":"c"},"status":"pending"}],"outbox":[]}` | approval lacks own `proposedAt` |
| S23 | `{"events":[],"jobs":[],"approvals":[{"id":"appr-a","jobId":"job-a","action":{"to":"a","subject":"b","body":"c"},"status":"pending","proposedAt":""}],"outbox":[]}` | approval `proposedAt` is empty |
| S24 | `{"events":[],"jobs":[],"approvals":[{"id":"appr-a","jobId":"job-a","action":{"to":"a","subject":"b","body":"c"},"status":"pending","proposedAt":7}],"outbox":[]}` | approval `proposedAt` is not a string |
| S25 | `{"events":[],"jobs":[],"approvals":[{"id":"appr-a","jobId":"job-a","action":{"to":"a","subject":"b","body":"c"},"proposedAt":"time-a"}],"outbox":[]}` | approval lacks own `status` |
| S26 | `{"events":[],"jobs":[],"approvals":[{"id":"appr-a","jobId":"job-a","action":{"to":"a","subject":"b","body":"c"},"status":"PENDING","proposedAt":"time-a"}],"outbox":[]}` | status is outside the exact enum |
| S27 | `{"events":[],"jobs":[],"approvals":[{"id":"appr-a","jobId":"job-a","action":{"to":"a","subject":"b","body":"c"},"status":null,"proposedAt":"time-a"}],"outbox":[]}` | status is not a string |
| S28 | `{"events":[],"jobs":[],"approvals":[{"id":"appr-a","jobId":"job-a","action":{"to":"a","subject":"b","body":"c"},"status":"rejected","proposedAt":"time-a","reason":7}],"outbox":[]}` | own `reason` is neither null nor a string |
| S29 | `{"events":[],"jobs":[],"approvals":[{"id":"appr-duplicate","jobId":"job-a","action":{"to":"a","subject":"b","body":"TARGET-BODY-MUST-NOT-LEAK"},"status":"pending","proposedAt":"time-a"},{"id":"appr-duplicate","jobId":"job-b","action":{"to":"d","subject":"e","body":"f"},"status":"sent","proposedAt":"time-b"}],"outbox":[]}` | valid matching first approval followed by a duplicate approval ID |
| S30 | `{"events":[],"jobs":[],"approvals":[{"id":"appr-valid-first","jobId":"job-valid","action":{"to":"a","subject":"b","body":"TARGET-BODY-MUST-NOT-LEAK"},"status":"pending","proposedAt":"time-a"},{"id":"appr-invalid-second","jobId":"job-invalid","action":{"to":"d","subject":"e","body":"f"},"status":"rejected","proposedAt":"time-b","reason":false}],"outbox":[]}` | valid matching first approval followed by an invalid sibling reason |

Before any command invocation, the acceptance test compares the exact
serialized bytes of Fixtures A-D and S1-S30 pairwise and requires every pair
to differ. Fixture D's exact serialization is the empty byte sequence. Fixture
A and Fixture B satisfy every frozen structural rule. Fixture C and Fixture D
fail `JSON.parse`. Every S fixture parses successfully and fails the stated
structural rule. S26 freezes direct literal `PENDING`; its UTF-8 bytes differ
from valid `pending`.

### Sensitive-token set, lifecycle, and read-only observations

The complete frozen sensitive-token set is:

1. every string value in Fixtures A-C or the frozen path basenames that
   contains `MUST-NOT-LEAK`;
2. both the complete `.json` basename and its stem for each frozen `.json`
   path whose stem ends in `MUST-NOT-LEAK`;
3. composed `café` and decomposed `café`; and
4. `2026-08-24T12:00:00.000Z`, `2026-08-24T12:00:01.000Z`,
   `not-an-iso-instant`, `2026-08-24T12:00:03.000Z`,
   `2026-08-24T12:00:04.000Z`, and `2026-08-24T12:00:05.000Z`.

The Test-author derives item 1 by recursively walking the frozen parsed
Fixture A value and parsing the exact Fixture B where applicable, then unions
the direct Fixture C and path literals, and asserts that the resulting sorted
set equals the sorted direct expected-token list in the test. The complete set
is checked against stdout and stderr after every child invocation. Exact stream
assertions remain independently authoritative.

Each runtime case owns a fresh temporary parent, creates only its required
input artifacts before invocation, snapshots sorted parent entries and every
existing regular file's exact bytes plus
`fs.statSync(path, { bigint: true }).mode`, `size`, and `mtimeNs`, waits
synchronously for each child process, and recursively removes the parent in
`finally`. The directory-path case snapshots the directory's same three
BigInt numeric stat fields and sorted entries. Multi-invocation cases run in
the order frozen by their criterion; each result is independent because the
command is read-only. Access time is excluded because reading the fixture is
the behavior under test.

Tests share no state path, process, port, server, database, table, generated
identifier, sequence, counter, clock, or timestamp. There is no parent row,
sibling dependent table, foreign key, cascade, or ordered cleanup obligation.
Every asserted ID, body, count, status, and timestamp is direct literal
fixture data; no generated value or initializer participates in an
expectation.

### File locations, protected boundaries, and topology

- Builder ownership is creation of
  `app/tools/approval-body-count.js` only. It is a standalone CommonJS
  command whose complete module-specifier set is exactly `node:fs` and whose
  only `node:fs` operation is `readFileSync`.
- Test-author ownership is creation of
  `app/tests/approval-body-count.test.js` only. Its complete
  module-specifier set is exactly `node:assert`, `node:child_process`,
  `node:fs`, `node:os`, `node:path`, and `node:test`. It resolves the Builder
  path with exactly
  `path.join(__dirname, "..", "tools", "approval-body-count.js")`; source
  inspection reads that resolved path and does not use a bare repository-root
  Builder-path string.
- `app/tests/approval-detail.test.js` is the protected reference-only fixture
  seam. It is not edited and grants no edit right. Its source exercises only
  `app/server.js`, not the new Builder path. The protected approval-count,
  approval-reason-count, and other tool suites inspect only their
  corresponding named tools. No protected test enumerates `app/tools/` or
  `app/tests/`, restricts the new tool's import/export surface, or snapshots
  `app/tools/approval-body-count.js`.
  `Protected-Test-Conflicts: none` remains exact.
- `app/package.json` already includes the new test through
  `node --test tests/*.test.js`; neither role edits the manifest.
- Targeted acceptance is
  `node --test app/tests/approval-body-count.test.js`.
  Post-implementation verification is `npm test --prefix app` and
  `.github/scripts/test-immutability-check.sh`.
- No endpoint, HTTP shape, route, UI selector, global page-shell text,
  accessible UI name, authentication, cookie, CORS, cross-origin request,
  credential, domain, preview deployment, or external send is introduced.
  Topology is one local file-to-stdout process, and `External: no` remains
  exact.

## Criteria checklist

- **Pass/fail and unambiguous:** AC1-AC7 freeze exact arguments, argument
  types, invocation order, exit codes, stream bytes, fixture bytes, stat
  fields, parent entries, privacy observations, and validation classes. AC8
  freezes timeout, process result, and complete static source boundaries. AC9
  is decided from Git history, path diffs, and three exact command exit
  statuses.
- **Coverage:** AC1 covers duplicate exact matches, deterministic repetition,
  count-only output, privacy, and read-only behavior. AC2 covers three distinct
  positive exact matches, including leading whitespace and composed Unicode.
  AC3 covers case sensitivity, numeric/string non-coercion, empty-body
  acceptance, Unicode non-normalization, an empty approval collection, and
  opaque sibling values. AC4 proves arity handling precedes candidate-path
  use. AC5 covers two read failures. AC6 covers malformed and zero-byte JSON.
  AC7 covers every structural rule, complete validation before counting,
  identity uniqueness, casing-derived distinctness, and pairwise fixture
  distinctness. AC8 covers every invocation's termination, privacy, and source
  boundary. AC9 covers role ownership, tests-first history, unchanged
  protected surfaces, and all post-implementation commands.
- **No dead contract elements:** every command path, output and error shape,
  fixture, query body, sensitive-token class, validation rule, lifecycle
  rule, file location, protected boundary, topology disposition, and
  verification command appears in AC1-AC9.
- **Derived-fixture distinctness:** every invalid serialization is a direct
  literal. S26's exact direct literal `PENDING` is frozen and compared
  bytewise with valid `pending`. The case-distinct, leading-space, composed,
  and decomposed bodies are direct literals; their exact differences are
  asserted before invocation. Fixtures A-D and S1-S30 are pairwise
  byte-compared.
- **Generated-value determinism:** every ID, body, count-producing
  approval, status, reason, and timestamp is literal fixture data. No generated
  identifier, sequence, counter, reset, initializer, transformation, or clock
  supplies an expectation.
- **Fixture lifecycle closure:** every case owns an isolated temporary parent,
  waits for each child, and removes all artifacts recursively in `finally`.
  There is no database, parent row, sibling table, non-cascading foreign key,
  cascade, shared mutable fixture, or ordered dependent cleanup.
- **Cross-ticket file boundaries:** every local ticket ownership declaration
  and every visible `origin/ticket/T-*` ticket record was inspected. T-222 and
  T-224 own distinct command/test pairs; no sibling or leased ticket owns
  either T-223 path.
- **Protected boundaries:** the associated seam and tracked protected tests
  were inspected. No allowlist, exact-source assertion, source snapshot, path
  digest, or module restriction applies to the new Builder path or its sole
  `node:fs` specifier. The pinned readiness checker returned
  `READINESS PASS`.
- **Global protected text:** the pinned readiness checker returned
  `GLOBAL TEXT PASS` separately for the usage line, all three error lines, the
  output property name, all three exact success output lines, every non-empty
  query-body literal, and string query `7`. The empty body
  contributes no text bytes and no tracked protected test contains a global
  page-shell text assertion.
- **Deploy/topology completeness:** the command is local file-to-stdout with
  no endpoint, UI, auth, cookie, CORS origin, credential, domain,
  preview-deploy, or external-send surface.

## Ambiguity log

1. **What counts as an exact body match?** Neither the product spec nor
   [`factory/rulings.md`](../rulings.md) defines body normalization.
   Resolved from the ticket's exact-match requirement and the shipped strict
   field-match convention as case-sensitive JavaScript strict equality with no
   coercion, trimming, case folding, decoding, or Unicode normalization. AC2
   and AC3 make each material distinction observable with direct literals.
2. **Is an empty body or a body with no matching approval an
   error?** Resolved from the ticket's exactly-two-argument interface and
   zero-match requirement: an empty string is still a supplied second
   argument, and every no-match query against structurally valid state exits
   `0` with `{"approvalsWithBody":0}\n`.
3. **Is the supplied body echoed?** The original ticket requires exactly
   one `approvalsWithBody` integer. Resolved by freezing a count-only
   object whose single key is `approvalsWithBody`; all supplied and stored
   bodies are forbidden from both output streams.
4. **What approval structure is validated before counting?** Resolved from
   [`app/server.js`](../../app/server.js), the shipped
   [`approval-count` command](../../app/tools/approval-count.js), the shipped
   [`approval-reason-count` command](../../app/tools/approval-reason-count.js),
   and the declared
   [`approval-detail` fixture seam](../../app/tests/approval-detail.test.js):
   require all four arrays; validate every approval's identity, job link,
   action object and own message keys, exact status, proposal timestamp,
   optional reason domain, and unique ID; leave sibling collection elements
   opaque.
5. **Must action `to`, `subject`, and `body` values be strings; timestamps
   parse as ISO-8601; or job links resolve?** Persisted action values originate in
   webhook payloads, and shipped offline validators treat them as opaque.
   Resolved by requiring own action keys, non-empty string `proposedAt` and
   `jobId`, and no action-value typing, ISO parse, or relationship lookup.
   `false` action `to`, `null` subject, numeric `action.body`, an unresolvable
   job link, and `not-an-iso-instant` make these choices observable.
6. **Which approval statuses and reason forms are valid?** Resolved from the
   server's four stored status outcomes and rejection path, plus the shipped
   approval-reason validator: status is exactly one of `pending`, `sent`,
   `rejected`, or `blocked_recipient`; reason may be absent, `null`, or any
   string, including `""` and `" "`. No cross-field relationship is imposed.
7. **Why reject duplicate approval IDs?** The stored ID is the durable approval
   identity used by approval and rejection operations, and
   [`SPEC.md` conformance case 7](../../SPEC.md#test-cases-the-conformance-suite)
   requires recovery without duplication. Resolved consistently with
   `approval-count`: reject duplicate IDs before counting.
8. **How are failures classified without leaking input?** Raw OS and parser
   errors can expose paths or state. Resolved using the shipped offline
   count-command convention: usage exit `2`, operational exits `1`, fixed
   single-line redacted errors, empty opposite streams, and precedence from
   arity through complete structure.
9. **How are offline, read-only, and self-terminating claims proved?** Resolved
   with synchronous child invocations under a `5_000` ms timeout, exact file
   and directory fingerprints, repeated success, isolated teardown, and a
   production source boundary admitting only `node:fs` and `readFileSync`.
10. **Are derived fixtures or generated expected values involved?** Only S26
    represents a casing-derived invalid class; its exact direct literal
    `PENDING` and byte difference from valid `pending` are frozen. All query
    variants are direct literals, all serializations are pairwise
    byte-compared, and every expected identifier, body, timestamp, and
    count comes from literal setup.
11. **Does fixture cleanup have hidden dependencies?** No service or database
    is started. Each case owns one temporary directory removed after its child
    exits, so there are no parent rows, sibling tables, foreign keys, cascades,
    resets, or teardown ordering.
12. **Do sibling or concurrently leased tickets overlap ownership?** A grep of
    every local `factory/tickets/*.md` ownership declaration and inspection of
    every visible remote `origin/ticket/T-*` ticket record found no other owner
    of either T-223 path. T-222 and T-224 own distinct paths.
13. **Do protected tests reject the new source boundary?** Direct inspection
    of `app/tests/approval-detail.test.js`,
    `app/tests/approval-count.test.js`,
    `app/tests/approval-reason-count.test.js`,
    `app/tests/factory-contract.test.js`, and tracked source assertions found
    only path-specific boundaries. The pinned readiness check returned
    `READINESS PASS`, so `Protected-Test-Conflicts: none` is exact.
14. **Does protected global text collide?** The pinned global-literal checker
    accepted every non-empty shell-visible literal named in the criteria
    checklist. The empty argument adds no literal text, no accessible UI name
    is added, and no tracked protected test contains a global page-shell text
    assertion.
15. **Are deployment, authentication, or external-send details applicable?**
    No. The complete topology is a local process reading one local path and
    writing stdout/stderr; there is no network, UI, credential, preview, or
    send path, so no `external` label is required.
16. **Does an existing operator ruling change the contract?**
    [`factory/rulings.md`](../rulings.md) was checked before freeze. Its
    detail-route ruling is consistent with the cited persisted approval shape,
    and its T-178 verification ruling confirms the targeted test, full app
    suite, and immutability as the post-implementation verification set. No
    new operator decision is required.

## Coordination

T-223 owns only `app/tools/approval-body-count.js` and
`app/tests/approval-body-count.test.js`. The protected
`app/tests/approval-detail.test.js` seam is reference-only and grants no edit
right. Every local sibling ticket and visible remote leased-ticket branch was
checked; no implementation path overlaps.

## Planning log

- 2026-08-24: Planner froze contract version 1 with exact strict body
  matching, byte-level count-only success and error envelopes, complete
  approval validation, deterministic and pairwise-distinct fixtures, privacy
  sentinels, read-only lifecycle, role ownership, protected-source and
  global-text results, and local-only topology. `SPEC.md`, `AGENTS.md`,
  `app/server.js`, `app/package.json`, `app/tools/approval-count.js`,
  `app/tools/approval-reason-count.js`, `app/tools/event-type-count.js`,
  `app/tests/approval-count.test.js`,
  `app/tests/approval-reason-count.test.js`,
  `app/tests/approval-detail.test.js`,
  `app/tests/factory-contract.test.js`, tracked protected source assertions,
  `factory/rulings.md`, every local sibling ownership declaration, and every
  visible remote `origin/ticket/T-*` ticket record were inspected. The pinned
  deterministic checker returned `READINESS PASS` for T-223 and
  `GLOBAL TEXT PASS` for every non-empty frozen shell-visible literal. No
  file-boundary conflict, protected-test conflict, topology requirement,
  external-send label, or operator decision remains.

- 2026-08-24: Spec-linter round 1 (v11) audited contract version 1 against
  `SPEC.md`, `AGENTS.md`, `app/server.js`, `app/package.json`,
  `app/tools/approval-count.js`, `app/tools/approval-reason-count.js`,
  `app/tests/approval-count.test.js`, `app/tests/approval-reason-count.test.js`,
  `app/tests/approval-detail.test.js`, `factory/rulings.md`,
  `factory/KIT_PIN`, all local ticket ownership declarations (T-222, T-224),
  and every visible `origin/ticket/T-*` tree. Attempted independent
  re-execution of the pinned `ticket-readiness.py` checker the ticket cites for
  its `READINESS PASS` and `GLOBAL TEXT PASS` claims: no such script exists
  anywhere in this worktree, at `factory/KIT_PIN`'s resolved release directory
  (`~/.factory/kits/releases/<pin>` is absent entirely), in any sibling
  worktree, or anywhere else on this machine (full-filesystem search returned
  no match). The checker's output cannot be reproduced or verified from this
  environment.

### Spec-lint contract-element coverage

| Material contract behavior | Positive | Failure/empty | Duplicate | Out of scope |
|---|---|---|---|---|
| Arity gate: exactly two positional arguments | AC1/AC2/AC3 (2 args, exit `0`) | AC4 zero-, one-, three-arg exit `2` + exact usage line, candidate path never read/created | n/a | arity >3 (equivalent permutation) |
| `readFileSync` of `<state-file>` as UTF-8 | AC1-AC3 read frozen fixtures | AC5 absent path and directory-in-place exit `1` + `cannot read state file` | n/a | symlink inputs and OS permission denial declared out of matrix by contract |
| `JSON.parse` of readable bytes | AC1-AC3 parse successfully | AC6 Fixture C (unclosed) and Fixture D (zero-byte) exit `1` + `state file is not valid JSON` | n/a | BOM/invalid-UTF-8 (equivalent parse permutation) |
| Root is non-null, non-array object with four own array collections | AC1-AC3 | AC7 S1 (null), S2 (array), S3 (missing `outbox`), S4-S6 (`events`/`jobs`/`approvals` non-array), S7 (`outbox` non-array) | n/a | primitive root and missing `events`/`jobs`/`approvals` keys (SPEC-WARN below) |
| Approval element: non-null/non-array object, own `id`/`jobId`/`proposedAt` non-empty strings, own `status` in exact enum, own `action` object with own `to`/`subject`/`body` | AC1-AC3 Fixture A carries string, boolean, null, and numeric action values | AC7 S8-S25 (null/array approval, each field missing/empty/wrong-type) | n/a | job link need not resolve; `proposedAt` need not be ISO-parseable (stated) |
| Optional `reason`: absent, `null`, or string incl. `""`/`" "` | AC1/AC3 exercise absent, `""`, `null`, `" "` | AC7 S28 (`reason` is `7`) | n/a | no cross-field relationship imposed (stated) |
| Approval ID uniqueness | AC1-AC3 three distinct target/other/leading/unicode IDs | AC7 S29 duplicate ID after one valid approval | AC7 S30 invalid sibling after one valid approval, proving full-array validation before counting | none |
| Exact body match: case-sensitive strict equality, no coercion/trim/case-fold/Unicode-normalize | AC1 exact repeat match (count 2); AC2 leading-space and composed-Unicode matches (count 1 each) | AC3 case-distinct, numeric-vs-string `7`, empty-body, decomposed-Unicode all count 0 | AC1 two identical invocations reproduce byte-identical output | none |
| Success envelope: exit `0`, zero stderr, one compact `{"approvalsWithBody":n}` object, one LF | AC1 `{"approvalsWithBody":2}\n` | AC2/AC3 `{"approvalsWithBody":1}\n` / `{"approvalsWithBody":0}\n` | AC1 repeats the same invocation twice with identical bytes | none |
| Failure precedence arity → read → parse → structure → count | AC4 precedes AC5 candidate-path use | AC5/AC6/AC7 produce three mutually distinct fixed lines | n/a | none |
| Data minimization: no supplied/stored body, action field, approval ID, `jobId`, reason, status, timestamp, event/job/outbox/metadata value emitted | AC1-AC3 assert no member of the frozen token set on either stream | AC4-AC7 assert fixed redacted single lines | AC8 re-asserts the complete token set across every AC1-AC7 child | none |
| Read-only, offline, self-terminating execution | AC1-AC3 byte + BigInt `mode`/`size`/`mtimeNs` + parent-entry snapshots | AC4 candidate absence, AC5 directory stat/entries, AC6/AC7 fixture bytes and stats unchanged | AC1 second invocation needs no reset | access time excluded with stated reason |
| Frozen source boundary of both role files | AC8 Builder specifier set exactly `node:fs`, operation set exactly `readFileSync`, forbids `server.js`/`fetch(`/`WebSocket`/`node:http`/`node:https`/`node:net`/`node:child_process`/`setTimeout`/`setInterval` | AC8 fixes the Test-author specifier set exactly and its `path.join` resolution form | n/a | none |
| Fixture lifecycle isolation and teardown | contract fixes one temporary parent per case, synchronous child wait, recursive removal in `finally` | AC1-AC7 assert parent sorted entries before and after | AC7 asserts pairwise byte-distinctness of Fixtures A-D and S1-S30 before invocation | no database, parent row, sibling dependent table, foreign key, cascade, or ordered cleanup exists |
| Role ownership, tests-first history, verification set | AC9 Test-author commit creates only the test, Builder commit only the tool | AC9 protected surfaces, manifest, lockfiles, schema, `KIT_PIN`, Factory controls unchanged | AC9 three exact post-implementation commands each exit `0` | repository baseline checks stay CI-owned per the 2026-08-10 T-178 ruling |
| Topology: local file-to-stdout, `External: no` | contract and AC8 admit no network, UI, auth, or send surface | AC8 static forbidden-token list enforces it | n/a | endpoints, UI text, auth, CORS, preview deploys declared not introduced |

### Spec-lint findings

1. **Consistency (blocking):** the frozen contract's "Protected boundaries" and
   "Global protected text" checklist items, ambiguity-log entries 13 and 14,
   and the Planning log each rest the claim that no protected test or
   global-shell literal conflict exists on "the pinned readiness checker" /
   `ticket-readiness.py` having returned `READINESS PASS` and
   `GLOBAL TEXT PASS`. Independent re-execution was attempted: no
   `ticket-readiness.py` (or any similarly named script) exists anywhere in
   this worktree, at the release directory `factory/KIT_PIN` resolves to
   (`~/.factory/kits/releases/7ae447baf7287e32a9df60c9453da6edec454e7b` is
   absent), in any sibling worktree, or anywhere else on this machine. This is
   an unknown, unreproducible static check standing in for real verification
   of the new Builder file's protected-test and global-literal safety. Per the
   spec-linter's own standing rule, an unknown or unparsable static check FAILs
   closed rather than being accepted on the ticket's word — a builder could
   ship a source-boundary or global-text collision that the cited checker was
   supposed to catch, and nothing in this ticket or repository substitutes for
   it.
2. SPEC-WARN: S3 is the only fixture testing a missing top-level array
   property (`outbox`), while S4-S7 test each collection being present but
   non-array; no fixture tests `events`, `jobs`, or `approvals` being entirely
   absent. This is very likely equivalent given a validator that loops
   identically over all four collection names, but the Test-author may add one
   more missing-key permutation alongside S3 for full symmetry.
3. SPEC-WARN: consider recording the exact `factory/KIT_PIN` value alongside
   any future re-run of a readiness checker (as several prior tickets, e.g.
   T-190/T-199/T-203, do) so a later auditor can confirm which pinned release
   was actually exercised, once such a checker is present and runnable in this
   environment.

SPEC-LINT: FAIL — the ticket's protected-boundary and global-text safety claims depend entirely on a `ticket-readiness.py` checker that does not exist anywhere in this environment, so those claims are unverified and must fail closed.
