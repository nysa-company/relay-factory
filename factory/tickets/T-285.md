# T-285 — Count Relay approvals by referenced event type offline

Kit-SHA: 12066ff5f92ac79191e1278b50142614f555e4fd
State: Planning
Initiative: I-002
Priority: normal
Risk class: low
External: no
Branch: ticket/T-285
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/approval-link.test.js, app/tests/event-type-count.test.js
Authentication-Seams: none
Builder ownership: app/tools/approval-event-type-count.js only
Test-author ownership: app/tests/approval-event-type-count.test.js only

## Description

Add a zero-dependency, read-only CommonJS command that counts persisted Relay
approvals whose job references an event with a supplied exact type while Relay
is offline.

## Acceptance criteria

1. `node app/tools/approval-event-type-count.js <state-file> <event-type>` emits
   compact JSON containing exactly one `approvalsForEventType` integer.
2. Type matching is exact and case-sensitive. Every approval must reference
   exactly one valid job and every referenced job exactly one valid event;
   duplicate event, job, or approval IDs and missing references are rejected
   before counting, and zero matches is valid.
3. Invalid arguments are rejected before file access. Unreadable, malformed,
   or structurally invalid state returns fixed redacted errors.
4. The command is offline, zero-dependency, deterministic, read-only, and
   self-terminating. Tests resolve fixtures from the test file, work from any
   current directory, use a five-second child timeout, and do not assume Git
   history depth.
5. Tests precede implementation. Targeted, full app, and immutability checks
   pass without dependency, server, schema, manifest, or Factory changes.

## Coordination

This ticket owns only its two new paths and is independent of T-286 and T-287.
