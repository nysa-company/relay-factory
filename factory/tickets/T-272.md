# T-272 — list sent Relay approvals offline

Kit-SHA: bcffd8f0d3d0596752136d7ee2fd95e4f912806c
State: Planning
Initiative: I-002
Priority: normal
Risk class: low
External: no
Branch: ticket/T-272
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: none
Authentication-Seams: none
Builder ownership: app/tools/sent-approvals.js only
Test-author ownership: app/tests/sent-approvals.test.js only

## Description

Relay provides offline lists for pending and blocked approvals, but not for the
approvals already sent. Add the symmetric sent-approval diagnostic while
keeping message content and recipients private.

## Acceptance criteria

1. `node app/tools/sent-approvals.js <state-file>` exits `0`, writes one compact
   JSON array line, and writes zero stderr bytes for valid state.
2. The array contains only approvals with status `sent`, sorted by approval ID
   in code-point order. Each entry has exactly `id`, `jobId`, and `proposedAt`
   in that order. Empty or all-non-sent approvals emit `[]`.
3. Approval records require a non-empty approval ID unique across the
   `approvals` array, a non-empty job ID (job IDs need not be unique across
   records), a non-empty proposed timestamp, and one of the four supported
   statuses. Invalid records fail before filtering.
4. Usage, unreadable-file, invalid-JSON, and invalid-state failures use fixed
   redacted one-line stderr messages, the established exit codes, and zero
   stdout bytes. No recipients, actions, reasons, state contents, or paths
   leak.
5. Repeated invocation is byte-for-byte deterministic. The command is offline,
   zero-dependency, read-only, and self-terminating, and changes no input bytes,
   metadata, or sibling paths.
6. Tests precede implementation; the targeted test, full app suite, and
   immutability check pass without other product or Factory changes.

## Coordination

This ticket owns only `app/tools/sent-approvals.js` and
`app/tests/sent-approvals.test.js`. It is independent of T-270 and T-271.

## Ambiguity log

1. **"unique non-empty approval and job IDs" (AC3) — does uniqueness apply to
   `jobId` too, or only to `id`?** Resolved from established codebase
   precedent, not escalated: every approval-scoped tool that validates
   approvals (`app/tools/approval-count.js:39-57`,
   `app/tools/approval-reason-count.js:39-60`,
   `app/tools/approval-recipient-count.js:35-56`,
   `app/tools/approval-state.js:33-47`) enforces uniqueness only on
   `approval.id` via a `Set`, while `jobId` is required to be a non-empty
   string with no cross-record uniqueness check (a job may legitimately have
   more than one approval, e.g. after rejection and resubmission). The frozen
   contract below applies that same asymmetric rule.
2. **Does a `sent` approval record require an `action` object (`to`/
   `subject`/`body`), matching `approval-count.js`'s validator?** Resolved
   from precedent: the ticket's direct symmetric siblings —
   `app/tools/pending-approvals.js:14-41` and
   `app/tools/blocked-approvals.js:14-41`, which the ticket description
   names explicitly ("Relay provides offline lists for pending and blocked
   approvals") — validate and project only `id`/`jobId`/`proposedAt`/`status`
   and do not require an `action` object. `sent-approvals.js` follows that
   same shape, not the count tools' stricter validator, since its output
   contract (AC2) is field-for-field identical to the two named siblings.
3. **Cross-ticket file-boundary check**: grepped `factory/tickets/T-270.md`
   and `factory/tickets/T-271.md` — both declare disjoint Builder/Test-author
   ownership (`event-type-summary.*`, `pending-jobs.*`) with no overlap on
   `app/tools/sent-approvals.js` or `app/tests/sent-approvals.test.js`. No
   conflict found.
4. **Protected-test / global-literal check**: no existing file references
   `sent-approvals`; `app/server.js` carries no listing of tool names or
   other global-shell surface for this command. No protected-test
   source-boundary conflict and no global-literal collision. Both readiness
   scripts named in the Planner instructions require
   `$FACTORY_RELEASE_PATH`, which is unset in this worktree; the manual greps
   above are the substitute evidence and found nothing to declare.

## Frozen contract — version 1

- **Freeze result:** PASS. Contract version 1 is frozen.

### Interface

`node app/tools/sent-approvals.js <state-file>`

Byte-identical structure, messages, and exit codes to the sibling tools
`app/tools/pending-approvals.js` and `app/tools/blocked-approvals.js`, with
the filter status changed to `sent` and one added cross-record validation
rule (approval-ID uniqueness). Tool name in messages is `sent-approvals`.

**Exit codes**

| Condition | stdout | stderr | exit |
|---|---|---|---|
| valid state | one compact JSON array line + `\n` | (none) | `0` |
| arg count `!== 1` | (none) | `usage: node app/tools/sent-approvals.js <state-file>\n` | `2` |
| file unreadable (missing, directory, permission) | (none) | `sent-approvals: cannot read state file\n` | `1` |
| file content not valid JSON | (none) | `sent-approvals: state file is not valid JSON\n` | `1` |
| JSON parses but fails state validation | (none) | `sent-approvals: invalid Relay state\n` | `1` |

These four stderr strings are exact and fixed (no interpolation of paths,
IDs, or state contents).

### State validation (`isValidState`)

Top-level `state` must be a non-null, non-array object with own array
properties `events`, `jobs`, `approvals`, `outbox` (presence and
`Array.isArray`, extra top-level keys allowed — matching
`pending-approvals.js`/`blocked-approvals.js`, not the stricter
`Object.keys(state).length === COLLECTIONS.length` check used by
`dead-jobs.js`/`job-status-count.js`).

Every entry of `state.approvals` must satisfy, in this order, else the whole
state is invalid:

1. non-null, non-array object;
2. `id`: non-empty string, and not equal to any earlier approval's `id`
   (first-seen-wins `Set` check — duplicate approval IDs invalidate the
   state);
3. `jobId`: non-empty string (no cross-record uniqueness check);
4. `proposedAt`: non-empty string;
5. `status`: one of exactly `"pending"`, `"sent"`, `"rejected"`,
   `"blocked_recipient"`.

Validation runs over every record before any filtering; one invalid record
invalidates the entire state (AC3).

### Output projection

```
state.approvals
  .filter(approval => approval.status === "sent")
  .map(({ id, jobId, proposedAt }) => ({ id, jobId, proposedAt }))
  .sort((a, b) => a.id < b.id ? -1 : a.id > b.id ? 1 : 0)
```

Serialized with `JSON.stringify` (compact, no whitespace) plus a trailing
`\n`. Key order per entry is exactly `id`, `jobId`, `proposedAt`. Empty or
all-non-`sent` input emits `[]\n`.

### Fixtures (frozen, byte-exact; Test-author owns `app/tests/sent-approvals.test.js` only)

- **Fixture A** — reuse the same five-approval shape as
  `app/tests/pending-approvals.test.js`'s `FIXTURE_A` (`appr-zulu` pending,
  `appr-sent` sent, `appr-alpha` pending, `appr-rejected` rejected,
  `appr-blocked` blocked_recipient — each with a distinct `action.to` /
  `subject` / `body` token suffixed `-MUST-NOT-LEAK`, an `outbox` entry, and a
  `metadata` block), so it stays byte-distinct from that file's fixture only
  in whichever byte range Test-author chooses (they are separate files, byte
  distinctness across files is not required — only within this file's own
  fixture set, per the checklist). Expected output: the single `sent` record,
  `[{"id":"appr-sent","jobId":"job-sent","proposedAt":"2026-08-06T04:00:02.000Z"}]\n`.
  Assert `appr-zulu`, `appr-alpha`, `appr-rejected`, `appr-blocked` and every
  `-MUST-NOT-LEAK` token are absent from stdout and stderr.
- **Fixture B** — `{"events":[],"jobs":[],"approvals":[],"outbox":[]}\n` →
  `[]\n`.
- **Fixture C** — malformed JSON, e.g.
  `{"events":[],"secret":"MALFORMED-BODY-MUST-NOT-LEAK"\n` → invalid-JSON
  path; assert the secret token never appears in stdout/stderr.
- **Duplicate-ID fixture (new for this ticket, AC3)** — two approval records
  sharing `id: "appr-dup"` with different `jobId`s, one of them `status:
  "sent"` — must be byte-distinct from every other fixture in this file and
  must invalidate the whole state (exit `1`, `INVALID_STATE` line, `[]`
  never emitted despite one qualifying `sent` record existing).
- **Structural-invalid set** — mirror
  `app/tests/pending-approvals.test.js`'s `STRUCTURAL_INVALID` (S1–S11:
  non-object top level, missing/wrong-typed collections, null approval
  entry, empty `id`, non-string `jobId`, null `proposedAt`, uppercase
  `status`) plus the new duplicate-ID case above. Test-author must verify
  all fixtures in the file, including the duplicate-ID one, are pairwise
  byte-distinct (checklist item 7) before use.

Every fixture pair in the test file must be asserted pairwise byte-distinct
(mirroring `pending-approvals.test.js`'s AC6 loop) before use in assertions.

### Non-goals / explicit exclusions

- No `action` object required or read.
- No uniqueness constraint on `jobId` across records.
- No changes to any file outside `app/tools/sent-approvals.js` and
  `app/tests/sent-approvals.test.js`.
