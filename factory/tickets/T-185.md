# T-185 — inspect one Relay job's progress offline

Kit-SHA: b1a5563096b333fb5d537625a6c7cb9b2c61ef95
State: Ready
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-185
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/retry-dead.test.js
Authentication-Seams: none
Builder ownership: app/tools/job-progress.js only
Test-author ownership: app/tests/job-progress.test.js only

## Description

Add a zero-dependency, read-only Node command that selects one job by exact ID
from a persisted Relay state file while the service is offline. It reports only
safe progress counters and never prints the job error or event payload.

## Acceptance criteria

1. `node app/tools/job-progress.js <state-file> <job-id>` exits zero and emits
   deterministic compact JSON with exactly `jobId`, `status`, `attempts`, and
   `retries`.
2. An unknown job ID exits nonzero with one fixed, privacy-safe error.
3. Missing, unreadable, malformed, or structurally invalid state is rejected
   with fixed redacted errors that reveal no path, last error, event payload,
   approval action, outbox content, or state content.
4. Every invocation is offline, read-only, and self-terminating; input bytes,
   mode, timestamps, and sibling directory entries remain unchanged.
5. Complete acceptance tests are committed before implementation. Targeted,
   full app, and immutability checks pass without dependency, server, schema,
   package-manifest, lockfile, or Factory-control changes.

## Coordination

This ticket owns only its two new `app/` paths and is independent of the other
qualification tickets.
