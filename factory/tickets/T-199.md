# T-199 — count Relay events by type offline

State: Backlog
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-199
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/event-metadata.test.js
Authentication-Seams: none
Builder ownership: app/tools/event-type-count.js only
Test-author ownership: app/tests/event-type-count.test.js only

## Description

Add a zero-dependency, read-only Node command that counts persisted Relay
events whose type exactly matches one operator-supplied value.

## Acceptance criteria

1. `node app/tools/event-type-count.js <state-file> <event-type>` exits zero
   and emits deterministic compact JSON with exactly `eventType` and `events`.
2. A valid type with no matching events returns an `events` count of zero.
3. Missing, unreadable, malformed, or structurally invalid state is rejected
   with fixed redacted errors that reveal no path or stored content.
4. Every invocation is offline, read-only, and self-terminating; input bytes,
   mode, timestamps, and sibling directory entries remain unchanged.
5. Complete acceptance tests are committed before implementation. Targeted,
   full app, and immutability checks pass without dependency, server, schema,
   package-manifest, lockfile, or Factory-control changes.

## Coordination

This ticket owns only its two new `app/` paths.
