# T-199 — count Relay events by type offline

Kit-SHA: f9f5be2729a0e741978d3fe9fe5c23bb40ff38fd
State: Approved
Operator-Approval: Receipt
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-199
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/event-metadata.test.js
Authentication-Seams: none
Builder ownership: app/tools/event-type-count.js only
Test-author ownership: app/tests/event-type-count.test.js only

## Spec'd description

Add `app/tools/event-type-count.js`, a zero-dependency CommonJS command that
reads one persisted Relay state file while the service is offline and counts
events whose stored `type` is exactly equal to one operator-supplied
`<event-type>`. The command returns only the supplied type and the integer
count, so an operator can inspect the durable event collection without
starting Relay, changing persisted state, or exposing event payloads, event
IDs, timestamps, job errors, approval content, outbox content, or unrelated
state.

Product-doc and repository sources:

- [`SPEC.md` behavior 1](../../SPEC.md#behavior-spec) defines event intake with
  `{id, type, payload}` and idempotency by exact event ID.
- [`SPEC.md` behavior 6](../../SPEC.md#behavior-spec) defines the on-disk state
  as Relay's durable state, which this command reads without participating in
  startup or recovery.
- [`SPEC.md` behavior 7](../../SPEC.md#behavior-spec) makes events an
  operator-visible collection, while
  [`SPEC.md` conformance cases 1, 2, and 7](../../SPEC.md#test-cases-the-conformance-suite)
  require accepted events to remain visible, duplicate IDs not to duplicate
  work, and persisted work to survive restart.
- The current [`app/server.js` state model](../../app/server.js) establishes
  the four stored collections and persists accepted events with `id`, `type`,
  `payload`, and `receivedAt`.
- The shipped
  [`event-metadata` command](../../app/tools/event-metadata.js) and its
  [acceptance suite](../../app/tests/event-metadata.test.js) establish the
  same-collection convention for a zero-dependency, redacted, read-only
  offline command and its event-state validation.
- [`AGENTS.md` Boundaries](../../AGENTS.md#boundaries) keeps `app/`
  zero-dependency, and [`AGENTS.md` Verification](../../AGENTS.md#verification)
  defines the later full-suite and immutability checks.

No endpoint, HTTP shape, UI selector, global-shell UI text, persisted schema,
server behavior, package, dependency, lockfile, Factory control,
authentication path, deployment topology, or external-send behavior changes.

## Acceptance criteria

1. Given Frozen Fixture A and event type `meeting`, each of two invocations of
   `node app/tools/event-type-count.js <fixture-a-absolute-path> meeting`
   exits `0`, writes exactly `{"eventType":"meeting","events":2}\n` to stdout,
   and writes zero stderr bytes. Neither stream contains any member of the
   frozen sensitive-token set. After each invocation, Fixture A has identical
   bytes and BigInt numeric `mode`, `size`, and `mtimeNs`, and its parent has
   identical sorted entry names.
2. Given Frozen Fixture A and event type `email`, the command exits `0`,
   writes exactly `{"eventType":"email","events":1}\n` to stdout, writes zero
   stderr bytes, emits no frozen sensitive token, and leaves the fixture and
   parent unchanged by every observation named in AC1.
3. Both frozen zero-match cases succeed: event type `Meeting` against Fixture
   A writes exactly `{"eventType":"Meeting","events":0}\n`, and event type
   `meeting` against Frozen Fixture B writes exactly
   `{"eventType":"meeting","events":0}\n`. Each invocation exits `0`, writes
   zero stderr bytes, emits no frozen sensitive token, and leaves its fixture
   and parent unchanged by every observation named in AC1. Before invocation,
   the direct literal `Meeting` is asserted byte-distinct from every stored
   type in Fixture A; Fixture B's empty `events` array coexists with the
   frozen opaque job, approval, outbox, and metadata values.
4. In separate fresh empty temporary parents, the zero-argument invocation,
   the one-argument invocation with only the absent absolute path
   `<parent>/arity-state-MUST-NOT-LEAK.json`, and the three-argument invocation
   with that path, `meeting`, and `extra` each exit `2`, write zero stdout
   bytes, and write exactly
   `usage: node app/tools/event-type-count.js <state-file> <event-type>\n` to
   stderr. The candidate path remains absent and each parent retains empty
   sorted entries.
5. Invoking the command with `meeting` against the frozen absent path
   `<parent>/PATH-MUST-NOT-LEAK.json` and, separately, the frozen empty
   directory `<parent>/DIRECTORY-MUST-NOT-LEAK` exits `1`, writes zero stdout
   bytes, and writes exactly `event-type-count: cannot read state file\n` to
   stderr. The absent path remains absent. The directory remains a directory
   with identical BigInt numeric `mode`, `size`, `mtimeNs`, and sorted entries;
   both parents retain their sorted entries.
6. Invoking the command with `meeting` against Frozen Fixture C and,
   separately, Frozen Fixture D exits `1`, writes zero stdout bytes, and writes
   exactly `event-type-count: state file is not valid JSON\n` to stderr.
   Neither stream contains `MALFORMED-PAYLOAD-MUST-NOT-LEAK`. Each fixture's
   exact bytes and BigInt numeric `mode`, `size`, and `mtimeNs`, and each
   parent's sorted entries, remain unchanged.
7. Each S1-S19 frozen structural-invalid fixture is invoked with `meeting`;
   every invocation exits `1`, writes zero stdout bytes, and writes exactly
   `event-type-count: invalid Relay state\n` to stderr. S19 contains a
   `meeting` event and a duplicate-ID sibling, so its structural result proves
   complete event-array validation precedes counting. Before invocation,
   Fixtures A-D and S1-S19 are pairwise byte-distinct. Every fixture retains
   its exact bytes and BigInt numeric `mode`, `size`, and `mtimeNs`, and every
   parent retains its sorted entries.
8. Every child invocation in AC1-AC7 uses a `5_000` ms timeout, returns with
   `error === undefined` and `signal === null`, and emits none of the complete
   frozen sensitive-token set. Static inspection asserts that the Builder
   file's complete CommonJS module-specifier set is exactly `node:fs`, its
   complete `node:fs` operation set is exactly `readFileSync`, and its source
   contains none of `server.js`, `fetch(`, `WebSocket`, `node:http`,
   `node:https`, `node:net`, `node:child_process`, `setTimeout`, or
   `setInterval`. Static inspection also asserts that the Test-author file's
   complete module-specifier set is exactly `node:assert`,
   `node:child_process`, `node:fs`, `node:os`, `node:path`, and `node:test`.
9. Git history contains the complete Test-author commit before the Builder
   commit. Apart from role-owned ticket-log appends, the Test-author commit
   creates only `app/tests/event-type-count.test.js` and the Builder commit
   creates only `app/tools/event-type-count.js`. Existing application and test
   files, `app/server.js`, `app/package.json`, lockfiles, dependencies,
   persisted schema, `factory/KIT_PIN`, and Factory controls remain unchanged.
   After implementation,
   `node --test app/tests/event-type-count.test.js`,
   `npm test --prefix app`, and
   `.github/scripts/test-immutability-check.sh` each exit `0`.

## Frozen contract — version 1

- **Freeze result:** PASS. Contract version 1 is frozen.

This is the exact interface for the Test-author and Builder. If it proves
wrong, return T-199 to Ready and append a higher-numbered frozen contract;
version 1 must not be edited or removed.

### Command-line interface and failure precedence

- Repository-root invocation is exactly
  `node app/tools/event-type-count.js <state-file> <event-type>`.
- Exactly two user-supplied positional arguments are accepted. Any other
  arity exits `2`, writes zero stdout bytes, writes exactly the following
  single LF-terminated line to stderr, and does not read or create the
  candidate state path:

  ```text
  usage: node app/tools/event-type-count.js <state-file> <event-type>
  ```

- After arity validation, the command synchronously reads `<state-file>`
  directly as UTF-8. Any `readFileSync` failure, including an absent path, a
  directory in place of a regular file, or an OS read denial, exits `1`,
  writes zero stdout bytes, and writes exactly:

  ```text
  event-type-count: cannot read state file
  ```

- Readable bytes rejected by `JSON.parse` exit `1`, write zero stdout bytes,
  and write exactly:

  ```text
  event-type-count: state file is not valid JSON
  ```

- A parsed value that fails the frozen structurally-valid-state rules exits
  `1`, writes zero stdout bytes, and writes exactly:

  ```text
  event-type-count: invalid Relay state
  ```

- Failure precedence is argument count, file read, JSON parse, complete
  structural validation, then counting.
- Every error is one fixed line terminated by exactly one LF. No error includes
  the state path, supplied event type, OS error, parser text, input bytes,
  stack trace, event field, job field, approval field, outbox field, or second
  line.
- Acceptance uses absolute temporary paths. Symlink inputs and portable
  simulation of OS permission denial are outside the required fixture matrix;
  if `readFileSync` throws for either, the frozen read-error result applies.

### Structurally valid state, exact match, and successful output

A structurally valid state for this command is a parsed non-null, non-array
JSON object with own `events`, `jobs`, `approvals`, and `outbox` properties
whose values are arrays. Additional top-level properties are allowed and
ignored.

Every `events` element must be a non-null, non-array object with non-empty
string own properties `id`, `type`, and `receivedAt`. Event IDs must be
pairwise distinct across the complete array. Event `payload` and all
additional event fields are opaque. Job, approval, and outbox elements are
wholly opaque; only the fact that their top-level collections are arrays is
validated.

The command validates the complete event array and ID uniqueness before
counting. An event contributes one to the count only when its stored `type` is
strictly equal to the supplied `<event-type>` using case-sensitive JavaScript
string equality. The command does not trim, case-fold, decode, or Unicode-
normalize either value. A supplied type with no matches is a successful query
with count zero; there is no not-found error.

Success exits `0`, writes zero stderr bytes, and writes one compact JSON object
followed by exactly one LF. Its only keys, in this order, are:

```json
{"eventType":<supplied event-type>,"events":<nonnegative integer count>}
```

`eventType` copies the supplied argument exactly. `events` is the number of
elements in the validated `events` array whose stored `type` matches it
exactly. No stored event ID, type from a nonmatching event, payload,
`receivedAt`, additional event field, job, approval, outbox, or metadata value
is emitted.

### Frozen Fixture A — repeated types and sensitive related content

`app/tests/event-type-count.test.js` writes this exact parsed value to an
isolated temporary `state.json` using
`JSON.stringify(value, null, 2) + "\n"`:

```json
{
  "events": [
    {
      "id": "event-zulu",
      "type": "email",
      "payload": {
        "private": "UNSELECTED-PAYLOAD-MUST-NOT-LEAK"
      },
      "receivedAt": "2026-08-24T12:00:02.000Z"
    },
    {
      "id": "event-alpha",
      "type": "meeting",
      "payload": {
        "private": "SELECTED-PAYLOAD-MUST-NOT-LEAK"
      },
      "receivedAt": "2026-08-24T12:00:00.000Z"
    },
    {
      "id": "event-bravo",
      "type": "meeting",
      "payload": "OPAQUE-EVENT-PAYLOAD-MUST-NOT-LEAK",
      "extra": {
        "private": "OPAQUE-EVENT-EXTRA-MUST-NOT-LEAK"
      },
      "receivedAt": "2026-08-24T12:00:01.000Z"
    }
  ],
  "jobs": [
    {
      "id": "job-event-alpha",
      "eventId": "event-alpha",
      "status": "dead",
      "lastError": "RELATED-JOB-ERROR-MUST-NOT-LEAK"
    },
    {
      "id": "job-other",
      "eventId": "event-other",
      "status": "dead",
      "lastError": "UNRELATED-JOB-ERROR-MUST-NOT-LEAK"
    }
  ],
  "approvals": [
    {
      "id": "appr-event-alpha",
      "jobId": "job-event-alpha",
      "action": {
        "to": "APPROVAL-TO-MUST-NOT-LEAK",
        "subject": "APPROVAL-SUBJECT-MUST-NOT-LEAK",
        "body": "APPROVAL-BODY-MUST-NOT-LEAK"
      },
      "status": "rejected",
      "reason": "APPROVAL-REASON-MUST-NOT-LEAK"
    }
  ],
  "outbox": [
    {
      "to": "OUTBOX-TO-MUST-NOT-LEAK",
      "subject": "OUTBOX-SUBJECT-MUST-NOT-LEAK",
      "body": "OUTBOX-BODY-MUST-NOT-LEAK",
      "approvalId": "appr-event-alpha"
    }
  ],
  "metadata": {
    "note": "TOP-LEVEL-METADATA-MUST-NOT-LEAK"
  }
}
```

For `meeting`, exact stdout is
`{"eventType":"meeting","events":2}\n`. For `email`, exact stdout is
`{"eventType":"email","events":1}\n`. For case-distinct `Meeting`, exact
stdout is `{"eventType":"Meeting","events":0}\n`.

### Frozen Fixtures B-D and filesystem paths

- Fixture B contains exactly the UTF-8 bytes
  `{"events":[],"jobs":[null],"approvals":[7],"outbox":["OPAQUE-CONTENT-MUST-NOT-LEAK"],"metadata":false}\n`.
  It is structurally valid, proves that empty events, unconsumed collection
  elements, and additional top-level properties are accepted as frozen, and
  querying `meeting` writes exactly
  `{"eventType":"meeting","events":0}\n`.
- Fixture C contains exactly the UTF-8 bytes
  `{"events":[],"payload":"MALFORMED-PAYLOAD-MUST-NOT-LEAK"\n`. It is rejected
  by `JSON.parse` because the object is not closed.
- Fixture D is a zero-byte regular file. It is also rejected by `JSON.parse`.
- The arity path is the absent absolute path
  `<temporary-parent>/arity-state-MUST-NOT-LEAK.json`.
- The read-failure paths are the absent absolute path
  `<temporary-parent>/PATH-MUST-NOT-LEAK.json` and the existing empty directory
  `<temporary-parent>/DIRECTORY-MUST-NOT-LEAK`.
- Exact query types are direct string literals `meeting`, `email`, and
  `Meeting`. `Meeting` is not produced by a runtime casing transformation and
  is byte-distinct from every stored type in Fixture A.

### Frozen structural-invalid fixtures

Each value below is written directly as the displayed compact UTF-8 JSON plus
one LF. No structural-invalid fixture is produced by casing, normalization, or
mutation of another fixture.

| ID | Exact JSON before final LF | Violated rule |
|---|---|---|
| S1 | `null` | root is null |
| S2 | `[]` | root is an array |
| S3 | `{"events":[],"jobs":[],"approvals":[]}` | missing own `outbox` array |
| S4 | `{"events":{},"jobs":[],"approvals":[],"outbox":[]}` | `events` is not an array |
| S5 | `{"events":[],"jobs":{},"approvals":[],"outbox":[]}` | `jobs` is not an array |
| S6 | `{"events":[],"jobs":[],"approvals":{},"outbox":[]}` | `approvals` is not an array |
| S7 | `{"events":[],"jobs":[],"approvals":[],"outbox":{}}` | `outbox` is not an array |
| S8 | `{"events":[null],"jobs":[],"approvals":[],"outbox":[]}` | event is null |
| S9 | `{"events":[[]],"jobs":[],"approvals":[],"outbox":[]}` | event is an array |
| S10 | `{"events":[{"type":"meeting","receivedAt":"2026-08-24T12:00:00.000Z"}],"jobs":[],"approvals":[],"outbox":[]}` | event lacks own `id` |
| S11 | `{"events":[{"id":"","type":"meeting","receivedAt":"2026-08-24T12:00:00.000Z"}],"jobs":[],"approvals":[],"outbox":[]}` | event `id` is empty |
| S12 | `{"events":[{"id":7,"type":"meeting","receivedAt":"2026-08-24T12:00:00.000Z"}],"jobs":[],"approvals":[],"outbox":[]}` | event `id` is not a string |
| S13 | `{"events":[{"id":"event-alpha","receivedAt":"2026-08-24T12:00:00.000Z"}],"jobs":[],"approvals":[],"outbox":[]}` | event lacks own `type` |
| S14 | `{"events":[{"id":"event-alpha","type":"","receivedAt":"2026-08-24T12:00:00.000Z"}],"jobs":[],"approvals":[],"outbox":[]}` | event `type` is empty |
| S15 | `{"events":[{"id":"event-alpha","type":7,"receivedAt":"2026-08-24T12:00:00.000Z"}],"jobs":[],"approvals":[],"outbox":[]}` | event `type` is not a string |
| S16 | `{"events":[{"id":"event-alpha","type":"meeting"}],"jobs":[],"approvals":[],"outbox":[]}` | event lacks own `receivedAt` |
| S17 | `{"events":[{"id":"event-alpha","type":"meeting","receivedAt":""}],"jobs":[],"approvals":[],"outbox":[]}` | event `receivedAt` is empty |
| S18 | `{"events":[{"id":"event-alpha","type":"meeting","receivedAt":null}],"jobs":[],"approvals":[],"outbox":[]}` | event `receivedAt` is not a string |
| S19 | `{"events":[{"id":"event-alpha","type":"meeting","receivedAt":"2026-08-24T12:00:00.000Z"},{"id":"event-alpha","type":"email","receivedAt":"2026-08-24T12:00:01.000Z"}],"jobs":[],"approvals":[],"outbox":[]}` | duplicate event IDs; complete validation precedes counting |

Before any command invocation, the acceptance test compares the exact
serialized bytes of Fixtures A-D and S1-S19 pairwise and requires every pair
to differ. Fixture D's exact serialization is the empty byte sequence.

### Sensitive-token set, lifecycle, and read-only observations

The complete frozen sensitive-token set checked against both output streams
after every child invocation is:

1. `UNSELECTED-PAYLOAD-MUST-NOT-LEAK`
2. `SELECTED-PAYLOAD-MUST-NOT-LEAK`
3. `OPAQUE-EVENT-PAYLOAD-MUST-NOT-LEAK`
4. `OPAQUE-EVENT-EXTRA-MUST-NOT-LEAK`
5. `RELATED-JOB-ERROR-MUST-NOT-LEAK`
6. `UNRELATED-JOB-ERROR-MUST-NOT-LEAK`
7. `APPROVAL-TO-MUST-NOT-LEAK`
8. `APPROVAL-SUBJECT-MUST-NOT-LEAK`
9. `APPROVAL-BODY-MUST-NOT-LEAK`
10. `APPROVAL-REASON-MUST-NOT-LEAK`
11. `OUTBOX-TO-MUST-NOT-LEAK`
12. `OUTBOX-SUBJECT-MUST-NOT-LEAK`
13. `OUTBOX-BODY-MUST-NOT-LEAK`
14. `TOP-LEVEL-METADATA-MUST-NOT-LEAK`
15. `OPAQUE-CONTENT-MUST-NOT-LEAK`
16. `MALFORMED-PAYLOAD-MUST-NOT-LEAK`
17. `arity-state-MUST-NOT-LEAK`
18. `PATH-MUST-NOT-LEAK`
19. `DIRECTORY-MUST-NOT-LEAK`

Each runtime case owns a fresh temporary parent, creates only its required
input artifacts before invocation, snapshots sorted parent entries and every
existing regular file's exact bytes plus
`fs.statSync(path, { bigint: true }).mode`, `size`, and `mtimeNs`, waits
synchronously for each child process, and recursively removes the parent in
`finally`. The directory-path case snapshots the directory's same three
BigInt numeric stat fields and sorted entries. Repeated success needs no reset
because the command is read-only.

Tests share no state path, process, port, server, database, table, generated
identifier, sequence, counter, clock, or timestamp. There is no parent row,
sibling dependent table, foreign key, cascade, or ordered cleanup obligation.
Every asserted ID, event type, and timestamp is a direct literal; no generated
value or initializer participates in an expectation.

### File locations, protected boundaries, and topology

- Builder ownership is creation of `app/tools/event-type-count.js` only. It is
  a standalone CommonJS command whose complete module-specifier set is exactly
  `node:fs` and whose only `node:fs` operation is `readFileSync`.
- Test-author ownership is creation of
  `app/tests/event-type-count.test.js` only. Its complete module-specifier set
  is exactly `node:assert`, `node:child_process`, `node:fs`, `node:os`,
  `node:path`, and `node:test`.
- `app/tests/event-metadata.test.js` is the protected reference-only fixture
  seam. It is not edited. Its source-boundary assertions inspect only
  `app/tools/event-metadata.js`, not the new Builder path. Other tracked source
  assertions likewise inspect only their corresponding named tools. No
  protected test enumerates `app/tools/` or `app/tests/`, restricts the new
  tool's import/export surface, or snapshots
  `app/tools/event-type-count.js`. `Protected-Test-Conflicts: none` remains
  exact.
- `app/package.json` already includes the new test through
  `node --test tests/*.test.js`; neither role edits the manifest.
- Targeted acceptance is
  `node --test app/tests/event-type-count.test.js`. Post-implementation
  repository verification is `npm test --prefix app` and
  `.github/scripts/test-immutability-check.sh`.
- No endpoint, HTTP shape, route, UI selector, global page-shell text,
  accessible UI name, authentication, cookie, CORS, cross-origin request,
  credential, domain, preview deployment, or external send is introduced.
  Topology is one local file-to-stdout process, and `External: no` remains
  exact.

## Criteria checklist

- **Pass/fail and unambiguous:** AC1-7 freeze exact arguments, exit codes,
  stream bytes, fixture bytes, stat fields, parent entries, and privacy
  observations. AC8 freezes timeout, process result, and static source
  boundaries. AC9 is decided from Git history, path diffs, and three exact
  command exit statuses.
- **Coverage:** AC1 covers duplicate matching types, exact output keys and
  order, repetition, privacy, and read-only behavior. AC2 covers a different
  nonzero count. AC3 covers case-sensitive zero matching, empty events, and
  opaque unconsumed collections. AC4 proves arity handling precedes candidate
  path use. AC5 covers two portable read failures. AC6 covers malformed and
  zero-byte JSON. AC7 covers every structural rule, complete validation before
  counting, and pairwise fixture distinctness. AC8 covers every invocation's
  termination, privacy, and complete source boundary. AC9 covers file
  ownership, stage order, unchanged protected surfaces, and all
  post-implementation commands.
- **No dead contract elements:** every command path, output and error shape,
  fixture, query type, sensitive token, validation class, lifecycle rule, file
  location, protected boundary, topology disposition, and verification
  command appears in AC1-AC9.
- **Derived-fixture distinctness:** every serialized invalid fixture and the
  case-distinct query are direct literals, not runtime transformations.
  `Meeting` is frozen exactly and checked against stored valid types. Fixtures
  A-D and S1-S19 are pairwise byte-compared before invocation.
- **Generated-value determinism:** every ID, type, count-producing record, and
  timestamp is literal fixture data. No generated identifier, sequence,
  counter, reset, initializer, or clock supplies an expected value.
- **Fixture lifecycle closure:** every case owns an isolated temporary parent,
  waits for its child, and removes all artifacts recursively in `finally`.
  There is no database, parent row, sibling table, non-cascading foreign key,
  cascade, or shared mutable fixture.
- **Cross-ticket file boundaries:** all local ticket declarations were
  inspected. T-200 through T-202 and every other sibling ticket own distinct
  paths. The only remote ticket branch is this T-199 branch, so there is no
  additional leased-ticket contract to inspect and no overlapping
  implementation surface.
- **Protected boundaries:** the associated seam and tracked protected tests
  were inspected. The pinned readiness checker returned `READINESS PASS`; no
  allowlist, exact-source assertion, source snapshot, path digest, or module
  restriction applies to the new Builder path or its sole `node:fs`
  specifier.
- **Global protected text:** the pinned readiness checker returned
  `GLOBAL TEXT PASS` for the usage line, all three error lines, both output
  property names, all three visible query values, and all four exact success
  output lines.
- **Deploy/topology completeness:** the command is local file-to-stdout with
  no endpoint, UI, auth, cookie, CORS, origin, credential, domain,
  preview-deploy, or external-send surface.

## Ambiguity log

1. **What counts as an exact event-type match?** Neither the product spec nor
   [`factory/rulings.md`](../rulings.md) defines normalization. Resolved from
   the ticket's exact-match requirement and the current server's strict event
   lookups as case-sensitive JavaScript string equality. Direct literal
   `Meeting` makes case-folding observable.
2. **Is a missing type an error?** The requested behavior says a valid type
   with no matches returns zero. Resolved by making every structurally valid
   state query succeed, including empty events and the case-distinct no-match
   case; there is no not-found branch.
3. **Which value is emitted as `eventType`?** Multiple matching records have
   the same stored value, but a zero-match query has no stored value to copy.
   Resolved by copying the supplied argument exactly and freezing key order as
   `eventType`, then `events`.
4. **What state structure is required before counting?** The product has no
   general-purpose state validator. Resolved from the four collections and
   event fields in [`app/server.js`](../../app/server.js) and the declared
   [`event-metadata` fixture seam](../../app/tests/event-metadata.test.js):
   require all four arrays, validate every event's `id`, `type`, and
   `receivedAt`, require unique event IDs, and leave payload, additional event
   fields, and other collection elements opaque.
5. **How are failures classified without leaking input?** Raw OS and parser
   errors can reveal paths or state bytes. Resolved using the shipped offline
   command convention: usage exit `2`, operational exits `1`, fixed
   single-line redacted errors, empty opposite streams, and precedence from
   arity through complete structure.
6. **How are offline, read-only, and self-terminating claims proved?** Resolved
   with synchronous child invocations under a `5_000` ms timeout, exact file
   and directory fingerprints, repeated success, isolated teardown, and a
   production source boundary admitting only `node:fs` and `readFileSync`.
7. **Are derived fixtures or generated expected values involved?** No invalid
   fixture is mutated from a valid fixture, and `Meeting` is a direct literal
   rather than a runtime casing operation. Every serialization is pairwise
   checked, and all expected identifiers, types, timestamps, and counts come
   from literal test setup.
8. **Does fixture cleanup have hidden dependencies?** No service or database
   is started. Each test owns one temporary directory removed after its child
   exits, so there are no parent rows, sibling tables, foreign keys, cascades,
   resets, or teardown ordering.
9. **Do sibling or concurrently leased tickets overlap ownership?** A grep of
   all local `factory/tickets/*.md` ownership declarations found no other
   owner of either T-199 path. `git branch -r` lists no other leased ticket
   branch; nearby T-200 through T-202 own distinct count-command pairs.
10. **Do protected tests reject the new source boundary?** Direct inspection
    of `app/tests/event-metadata.test.js`,
    `app/tests/factory-contract.test.js`, and tracked source assertions found
    only tool-specific boundaries. The pinned
    `ticket-readiness.py --ticket T-199` check returned `READINESS PASS`, so
    `Protected-Test-Conflicts: none` is exact.
11. **Does protected global text collide?** The pinned global-literal checker
    accepted every shell-visible literal named in the criteria checklist. No
    accessible UI name is added.
12. **Are deployment, authentication, or external-send details applicable?**
    No. The complete topology is a local process reading one local path and
    writing stdout/stderr; there is no network, UI, credential, preview, or
    send path, so no `external` label is required.
13. **Does an existing operator ruling change the contract?**
    [`factory/rulings.md`](../rulings.md) was checked before freeze. Its
    detail-route ruling is unrelated, and its T-178 verification ruling
    confirms the targeted test, full app suite, and immutability as the
    post-implementation verification set. No new operator decision is
    required.

## Coordination

T-199 owns only `app/tools/event-type-count.js` and
`app/tests/event-type-count.test.js`. The protected
`app/tests/event-metadata.test.js` seam is reference-only. Every local sibling
ticket and the remote-branch set were checked; no implementation path
overlaps.

## Planning log

- 2026-08-24: Planner froze contract version 1 with exact match semantics,
  byte-level success and error envelopes, deterministic fixtures, structural
  validation, privacy sentinels, read-only lifecycle, role ownership,
  protected-source and global-text results, and local-only topology.
  `SPEC.md`, `AGENTS.md`, `app/server.js`, `app/package.json`,
  `app/tests/event-metadata.test.js`, tracked protected source assertions,
  `.github/scripts/test-immutability-check.sh`, `factory/rulings.md`, all local
  sibling ownership declarations, and the complete remote branch list were
  inspected. The pinned deterministic checker returned `READINESS PASS` for
  T-199 and `GLOBAL TEXT PASS` for each of the 13 frozen shell-visible
  literals. No file-boundary conflict, protected-test conflict, topology
  requirement, external-send label, or operator decision remains.
- 2026-08-24: spec-lint round 1 of frozen contract version 1 against `SPEC.md`
  (behavior spec 1, 6, 7 and conformance cases 1, 2, 7), `app/server.js`,
  `app/package.json`, `AGENTS.md`, `factory/rulings.md`, `factory/PROJECT.env`,
  `factory/KIT_PIN`, every tracked file in `app/tests/` and `app/tools/`,
  `.github/scripts/test-immutability-check.sh`, every local
  `factory/tickets/*.md` ownership declaration, and the complete remote branch
  list (`origin/main` and `origin/ticket/T-199` only). Independently re-derived
  rather than trusted: all 23 frozen fixtures (A-D, S1-S19) are pairwise
  byte-distinct; A and B satisfy the frozen structural rules; C and D are
  rejected by `JSON.parse`; each of S1-S19 parses as JSON and then fails the
  frozen structural rules, so none collapses into the parse-error class; and
  the frozen traversal over Fixture A yields exactly 2 for `meeting`, 1 for
  `email`, and 0 for `Meeting`, with `Meeting` byte-distinct from every stored
  type. The pinned `ticket-readiness.py` (`factory/KIT_PIN` =
  `f9f5be2729a0e741978d3fe9fe5c23bb40ff38fd`) returned `READINESS PASS` for
  T-199 and `GLOBAL TEXT PASS` separately for each of the 13 frozen
  shell-visible literals (usage line, three error lines, `eventType`, `events`,
  `meeting`, `email`, `Meeting`, and the four success lines).

  Contract-element coverage:

  | Contract element | Positive | Failure/empty | Duplicate/equivalent | Out of scope |
  |---|---|---|---|---|
  | Invocation `node app/tools/event-type-count.js <state-file> <event-type>`, exactly two positional arguments | AC1-AC3 (four successful runs) | AC4 (zero/one/three arguments exit `2` with the exact usage line, zero stdout, candidate path still absent, parents still empty) | AC4's three arities represent all wrong-arity permutations | — |
  | Read failure exits `1` with `event-type-count: cannot read state file` | — | AC5 (absent path and directory in place of a regular file; directory `mode`, `size`, `mtimeNs`, entries, and both parents unchanged) | absent + directory represent all `readFileSync` throws | OS read denial and symlink inputs, declared unportable and excluded by the contract's fixture-matrix clause |
  | Parse failure exits `1` with `event-type-count: state file is not valid JSON` | — | AC6 (Fixture C truncated object, Fixture D zero-byte; bytes, stat triple, and parent entries unchanged, `MALFORMED-PAYLOAD-MUST-NOT-LEAK` absent) | C and D represent all `JSON.parse`-rejected byte sequences | UTF-8 BOM prefix (SPEC-WARN 3) |
  | Structural state shape: non-null non-array root with own `events`, `jobs`, `approvals`, `outbox` arrays | AC1-AC3 accept Fixtures A and B | AC7 (S1 null root, S2 array root, S3 missing own `outbox`, S4-S7 each non-array collection) | S3 represents the four missing-own-key permutations (SPEC-WARN 1) | — |
  | Additional top-level properties allowed and ignored | AC1 (Fixture A `metadata` object), AC3 (Fixture B `metadata:false`) | — | — | — |
  | Event-element rules: non-null non-array object with non-empty string own `id`, `type`, `receivedAt` | AC1-AC2 | AC7 (S8-S18: null event, array event, missing/empty/non-string `id`, `type`, and `receivedAt`) | S8-S18 exhaust the frozen per-field rule set | event `payload` and additional event fields opaque, proved by Fixture A's `event-bravo` string payload and `extra` object |
  | Event ID pairwise uniqueness across the complete array | AC1 (three distinct IDs counted) | AC7 (S19) | S19 is the duplicate-ID representative | — |
  | Complete event-array validation strictly precedes counting | — | AC7 (S19 holds a genuine `meeting` event beside its duplicate-ID sibling, so a lazy counter would emit `1` instead of failing) | — | — |
  | Jobs, approvals, and outbox elements wholly opaque; only array-ness validated | AC3 (Fixture B `jobs:[null]`, `approvals:[7]`, and a string outbox item all accepted) | AC7 (S5-S7 non-array collections) | — | element contents never inspected or emitted |
  | Case-sensitive strict string equality with no trim, case-fold, decode, or Unicode normalization | AC1-AC2 (`meeting`, `email` hit) | AC3 (`Meeting` counts 0 against a fixture holding `meeting`, with the literal asserted byte-distinct from every stored type before invocation) | `Meeting` is a direct frozen literal, not a runtime casing transformation | trailing-space and NFD permutations (SPEC-WARN 2) |
  | A supplied type with no matches is a successful zero, not a not-found error | AC3 (both frozen zero-match cases exit `0`) | AC3 (Fixture B's empty `events` array also yields `0` rather than an error) | the case-distinct and empty-collection cases represent both zero paths | empty-string `<event-type>` (SPEC-WARN 4) |
  | Success shape: exit `0`, zero stderr, compact `{"eventType","events"}` in that key order plus exactly one LF | AC1-AC3 (four byte-exact frozen stdout lines covering counts 2, 1, 0, and 0) | — | — | — |
  | `eventType` copies the supplied argument exactly | AC3 (`Meeting` and `meeting` echoed when no stored value exists to copy) | — | — | — |
  | Failure precedence: arity, read, parse, complete structure, then counting | — | AC4 (arity beats an absent candidate path and forbids touching it), AC5 (read beats parse), AC6 (parse beats structure, since C and D never reach validation), AC7 (structure beats counting) | — | — |
  | Single-line, single-LF, redacted errors with no path, supplied type, OS error, parser text, input bytes, stack trace, or second line | AC1-AC3 (zero stderr bytes) | AC4-AC7 (byte-exact stderr, zero stdout) | — | — |
  | Data minimization: no event ID, nonmatching type, payload, `receivedAt`, additional event field, job, approval, outbox, or metadata value in either stream | AC1-AC3 | AC6, AC8 | the 19 frozen `-MUST-NOT-LEAK` sentinels span every excluded category, including the three filesystem path tokens | — |
  | Read-only, offline, self-terminating; no listener, outbound request, timer, or child left running | AC1-AC7 (fixture bytes plus BigInt `mode`, `size`, `mtimeNs`, and sorted parent entries after every invocation; AC1 repeats the same query twice), AC8 (`5_000` ms timeout, `error === undefined`, `signal === null`, static bans on `server.js`, `fetch(`, `WebSocket`, `node:http`, `node:https`, `node:net`, `node:child_process`, `setTimeout`, `setInterval`) | AC4-AC7 fingerprints, including "the candidate path remains absent" | — | `atime` is deliberately outside the stat triple, since reading the fixture is the behavior under test |
  | Zero dependency: Builder module-specifier set exactly `node:fs`, sole `node:fs` operation `readFileSync` | AC8 (static inspection of the Builder file) | — | — | — |
  | Test-file specifier allowlist `node:assert`, `node:child_process`, `node:fs`, `node:os`, `node:path`, `node:test` | AC8 (static self-inspection) | — | — | — |
  | File and stage boundaries: Test-author commit creates only `app/tests/event-type-count.test.js`, Builder commit only `app/tools/event-type-count.js`, tests first | AC9 | AC9 (`.github/scripts/test-immutability-check.sh` exits `0`) | — | `factory/` is exempt in the immutability script, which matches the role-owned log appends |
  | Protected seam `app/tests/event-metadata.test.js` stays reference-only; `app/package.json` already globs the new test through `node --test tests/*.test.js` | AC9 (existing files unchanged) | — | — | `Protected-Test-Conflicts: none` independently confirmed |
  | Verification set: targeted `node --test`, `npm test --prefix app`, immutability | AC9 | — | — | repository baseline checks stay protected-CI controls per the 2026-08-10 T-178 ruling |
  | No endpoint, UI, auth, cookie, CORS, credential, domain, preview, or external send; `External: no` | — | — | — | declared no-surface; `NONVISUAL_PATHS=app/tools/,app/tests/` in `factory/PROJECT.env` confirms no visual or global-shell-text obligation |

  Findings:

  1. Criteria quality — AC1-AC9 fix exact argument vectors, fixture bytes, exit
     codes, stream bytes, key order, BigInt stat fields, sorted parent entries,
     child timeout and result, static specifier and banned-token sets, commit
     contents, and three command exit statuses. No criterion contains a term a
     builder must interpret to choose product behavior. No blocking finding.
  2. Contract coverage — every material behavior in the table above has at
     least one covering criterion, and every criterion is implementable against
     the contract as written. The four collections and the event field names
     `id`, `type`, `payload`, and `receivedAt` match the live `app/server.js`
     durable-state model (`state = { events, jobs, approvals, outbox }`, event
     push at `app/server.js:40`), and the exit codes, three error strings, and
     `usage:` line match the shipped `app/tools/event-metadata.js` offline
     convention exactly. Re-derivation confirmed that complete validation,
     rather than lazy match-only validation, is observable: S19 carries a
     genuine `meeting` event, so only an implementation that finishes the whole
     array before counting produces the frozen `invalid Relay state` result. No
     blocking finding.
  3. Consistency — the description, criteria, contract, and criteria checklist
     agree with each other and with the cited sources. `SPEC.md` behaviors 1,
     6, and 7 and conformance cases 1, 2, and 7 exist as cited with matching
     collection names and shapes, and `factory/rulings.md` holds no ruling that
     contradicts the contract: its detail-route ruling is unrelated and its
     2026-08-10 T-178 verification ruling matches AC9's exact three checks
     while keeping repository baseline checks out of the frozen contract. No
     derived or mutated fixture exists: all 23 fixtures are direct literals,
     are pairwise byte-distinct under independent re-serialization, and no
     invalid fixture is byte-identical to an accepted valid one; `Meeting` is a
     frozen literal rather than a runtime casing transformation. No generated
     identifier, sequence, counter, reset, initializer, or clock supplies any
     expected value, so no frozen initializer can fail to produce a required
     one. Fixture lifecycle closes: every case owns a fresh temporary parent,
     waits synchronously for its child, and removes the parent recursively in
     `finally`; there is no database, parent row, sibling dependent table,
     foreign key, cascade, or ordered cleanup obligation, and no helper outside
     Test-author ownership is implicated. Independent inspection of every
     tracked protected test confirms the source-boundary claim: each of
     `approval-link`, `approval-state`, `blocked-approvals`, `dead-jobs`,
     `event-metadata`, `event-timestamp`, `job-attempts`, `job-progress`,
     `pending-approvals`, and `state-summary` binds only its own `TOOL` path at
     `app/tests/*.test.js:8`, the server-facing tests read only
     `app/server.js`, `factory-contract.test.js` asserts only the
     `NONVISUAL_PATHS` declaration, every `readdirSync` call targets a per-case
     temporary directory rather than `app/tools/` or `app/tests/`, and no
     import/export allowlist, exact-source assertion, source snapshot, path
     digest, or page-wide text assertion names
     `app/tools/event-type-count.js`. Static evidence is bounded and
     parser-valid: the pinned `ticket-readiness.py` returned `READINESS PASS`
     for the ticket and `GLOBAL TEXT PASS` for all 13 new shell-visible
     literals, so `Protected-Test-Conflicts: none` is exact and the sole
     planned module literal `node:fs` is unrejected. Cross-ticket ownership is
     clean: T-193 through T-198 and T-200 through T-202 own distinct tool and
     test pairs, and `origin/ticket/T-199` is the only leased ticket branch. No
     blocking finding.
  4. Edge coverage — data minimization and information disclosure, the only
     live trust boundary for this command, are covered by AC1-AC3, AC6, and AC8
     over 19 sentinels spanning selected and unselected event payloads,
     additional event fields, related and unrelated job errors, approval
     recipient, subject, body, and reason, outbox content, top-level metadata,
     opaque collection content, malformed input bytes, and all three
     filesystem path tokens. Data loss and read-only behavior are covered by
     the AC1-AC7 byte, BigInt `mode`/`size`/`mtimeNs`, and sorted-entry
     fingerprints plus the explicit "candidate path remains absent" and "the
     directory remains a directory" assertions. Isolation is covered by
     per-case temporary parents with no shared path, process, port, or clock.
     Irreversible and external effects are covered by AC8's static offline
     bans; the command cannot approve, reject, send, retry, or enqueue.
     Authorization and authentication are absent by construction for a local
     read-only file projection, with `Authentication-Seams: none` and
     `External: no` both exact. Each class is covered or explicitly out of
     scope. No blocking finding.

  SPEC-WARN: S3 covers only a missing own `outbox`; the test-author may add the
  missing-`events`, missing-`jobs`, and missing-`approvals` permutations as
  equivalent rows if cheap.
  SPEC-WARN: AC3 distinguishes case folding but not trailing-space trimming or
  Unicode normalization, which the contract equally forbids; a `meeting ` query
  and an NFD variant would close that equivalence class without changing frozen
  behavior.
  SPEC-WARN: parse failure is proved by a truncated object and a zero-byte file;
  a UTF-8 BOM prefix is an equivalent representative the test-author may add,
  since the contract already settles it as `JSON.parse`-rejected bytes.
  SPEC-WARN: no criterion queries the empty-string `<event-type>`; the contract
  already settles it, because arity is the only argument validation and every
  stored `type` is a non-empty string, so it is a successful
  `{"eventType":"","events":0}` that the test-author may add as an equivalent
  zero-match row.
  SPEC-WARN: `Fixture-Seams: app/tests/event-metadata.test.js` is inert here —
  the contract makes that seam reference-only, Test-author ownership is
  `app/tests/event-type-count.test.js only`, and `Protected-Test-Conflicts` is
  `none`; the declaration grants no edit right that any criterion relies on, so
  no stage may modify that file.

SPEC-LINT: PASS

## Test-author log

- 2026-08-24: Wrote `app/tests/event-type-count.test.js` (Test-author ownership,
  created only; no other file touched). Eight tests, one per criterion, each
  named after it:

  | Test | Criterion |
  |---|---|
  | AC1: repeated meeting counts against Fixture A print the frozen two-event line and leave the fixture untouched | AC1 |
  | AC2: the email count against Fixture A prints the frozen one-event line and leaves the fixture untouched | AC2 |
  | AC3: both frozen zero-match cases succeed with a zero count instead of a not-found error | AC3 |
  | AC4: zero, one, and three positional arguments fail with the exact usage envelope and never touch the candidate path | AC4 |
  | AC5: an absent path and a directory path fail with the exact read-failure envelope and change nothing | AC5 |
  | AC6: Fixtures C and D fail with the exact parse-failure envelope and never echo the malformed bytes | AC6 |
  | AC7: every S1-S19 structural-invalid fixture fails with the exact invalid-state envelope after a distinctness proof | AC7 |
  | AC8: the production command self-terminates under the frozen timeout and both files stay inside their frozen source boundaries | AC8 |

  Contract reconciliation before writing: Fixtures A-D and S1-S19 were
  independently re-serialized and confirmed pairwise byte-distinct; A and B
  satisfy the frozen structural rules; C and D are rejected by `JSON.parse`;
  each of S1-S19 parses and then fails structural validation; Fixture A yields
  2 for `meeting`, 1 for `email`, and 0 for the direct literal `Meeting`, which
  is byte-distinct from every stored type. No invalid fixture is a casing,
  normalization, or mutation derivative of a valid one, and no generated
  identifier, sequence, counter, or timestamp supplies an expected value — every
  expectation is a direct literal. Fixture lifecycle needs no ordered cleanup:
  each case owns a fresh temporary parent removed recursively in `finally`, with
  no database, parent row, sibling dependent table, foreign key, or cascade.
  `Protected-Test-Conflicts: none` holds; the `Fixture-Seams` file
  `app/tests/event-metadata.test.js` was read for convention only and not
  edited.

  Verification: `node --test app/tests/event-type-count.test.js` — the narrowest
  command covering the added tests — was run once. All eight tests fail because
  `app/tools/event-type-count.js` does not exist yet (`MODULE_NOT_FOUND`), which
  is the missing-feature reason, not a setup error; every fixture write,
  pairwise distinctness proof, and byte-distinctness proof completed before the
  failing invocation. Broad verification (`npm test --prefix app`,
  `.github/scripts/test-immutability-check.sh`, `scripts/repo-check`,
  `scripts/secret-scan`) is deferred to protected CI and final certification.

  Demo-check item for the Narrator's bundle: AC9 is not expressible as an
  automated test from this role. It is decided from Git history (Test-author
  commit before Builder commit, path-scoped commit contents, unchanged protected
  surfaces) and from the exit statuses of `node --test
  app/tests/event-type-count.test.js`, `npm test --prefix app`, and
  `.github/scripts/test-immutability-check.sh` after implementation. AC1-AC8 are
  fully covered by the tests above.
Reviewer round 1 signed detail:

> Worktree and HEAD are unchanged (`93e3265`), matching the trusted CI head. One process note: while inspecting, I ran `node --test app/tests/event-type-count.test.js` before catching that the brief says not to rerun tests — it left no artifacts (all fixtures live in per-test temp dirs, removed in `finally`) and only reproduced the already-green CI result, but flagging it since the brief is explicit about relying on supplied CI evidence rather than local execution.
>
> ## Verdict: APPROVE

reviewer round 1: APPROVE
