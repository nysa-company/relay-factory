# T-189 — inspect one Relay approval timestamp offline

Kit-SHA: e85890e4e8ae98038b1eaaee5b6bb239bc874052
State: Planning
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-189
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/approval-detail.test.js
Authentication-Seams: none
Builder ownership: app/tools/approval-timestamp.js only
Test-author ownership: app/tests/approval-timestamp.test.js only

## Spec'd description

Add one zero-dependency, read-only Node 18+ command,
`node app/tools/approval-timestamp.js <state-file> <approval-id>`, that reads a
persisted Relay state file without starting the service and selects one
approval by verbatim, case-sensitive ID. It emits only that approval's stored
identifier, lifecycle status, and proposal timestamp under the exact keys
`approvalId`, `status`, and `proposedAt`. This gives an operator a minimal
offline timing lookup without exposing the approval action, rejection reason,
related event payload or job error, outbox content, or unrelated state.

Product-doc sources:

- [`SPEC.md` behavior 3](../../SPEC.md#behavior-spec) defines the proposed
  action, its initial `pending` status, and the approval-before-execution gate.
- [`SPEC.md` behaviors 4-5](../../SPEC.md#behavior-spec) define the `sent`,
  `blocked_recipient`, and `rejected` approval outcomes.
- [`SPEC.md` behavior 6](../../SPEC.md#behavior-spec) makes the atomically
  persisted state file Relay's durable source of truth.
- [`SPEC.md` behavior 7](../../SPEC.md#behavior-spec) establishes approvals as
  an operator-visible persisted collection.
- [`SPEC.md` conformance cases 5-7](../../SPEC.md#test-cases-the-conformance-suite)
  require approval gating, safety-blocked outcomes, and crash-recovered state
  to remain observable without duplicate effects.
- The current [`app/server.js` approval model](../../app/server.js) stores
  `id`, `jobId`, `action`, `status`, and `proposedAt`, with optional `reason`
  after rejection.
- [`AGENTS.md` Boundaries](../../AGENTS.md#boundaries) keeps `app/`
  zero-dependency and Factory controls outside application work.
  [`AGENTS.md` Verification](../../AGENTS.md#verification) defines the later
  full-app and immutability checks.

The command is a data-minimized projection over one existing regular file. It
does not start Relay, approve or reject a proposal, write an outbox record,
change the persisted schema, or modify the server, worker, UI, package
manifest, dependencies, lockfiles, or Factory controls.

## Acceptance criteria

1. Given Frozen Fixture A, invoking
   `node app/tools/approval-timestamp.js <fixture-path> <approval-id>` once for
   each of `appr-timestamp-pending`, `appr-timestamp-sent`,
   `appr-timestamp-rejected`, and `appr-timestamp-blocked` exits `0`, writes
   zero stderr bytes, and writes that ID's byte-exact frozen stdout line. Each
   parsed object has exactly the keys `approvalId`, `status`, and `proposedAt`
   in that order. No frozen sensitive token or unrelated approval ID appears
   in either stream.
2. Given Frozen Fixture A, each frozen unknown ID (`appr-timestamp-missing`,
   `APPR-TIMESTAMP-PENDING`, and `appr-timestamp-pending ` with one trailing
   U+0020 space) exits `1`, writes zero stdout bytes, and writes exactly
   `approval-timestamp: no such approval\n` to stderr. The three unknown
   literals are pairwise byte-distinct and byte-distinct from every Fixture A
   approval ID.
3. Invoking the command with zero, one, or three positional arguments exits
   `2`, writes zero stdout bytes, and writes exactly
   `usage: node app/tools/approval-timestamp.js <state-file> <approval-id>\n`
   to stderr. Each case uses a fresh empty temporary parent; the one-argument
   case supplies absent `<tmp>/state-a.json`, and the three-argument case
   supplies that absent path, `appr-timestamp-pending`, and `extra`. No
   supplied path is created and each parent remains empty.
4. For each frozen read-failure path—an absent path and a directory path—the
   command exits `1`, writes zero stdout bytes, and writes exactly
   `approval-timestamp: cannot read state file\n` to stderr. The absent path
   remains absent; the directory remains a directory with unchanged BigInt
   numeric mode, size, `mtimeNs`, and entries; each parent retains its sorted
   entry names.
5. Given Frozen Fixture B and approval ID `appr-timestamp-pending`, the command
   exits `1`, writes zero stdout bytes, writes exactly
   `approval-timestamp: state file is not valid JSON\n` to stderr, and leaves
   the fixture's exact bytes, BigInt numeric mode, size, `mtimeNs`, and parent
   entries unchanged. `MALFORMED-STATE-MUST-NOT-LEAK` appears in neither
   stream.
6. For every S1-S17 frozen structural-invalid fixture and the frozen lookup ID
   listed for it, the command exits `1`, writes zero stdout bytes, writes
   exactly `approval-timestamp: invalid Relay state\n` to stderr, and leaves
   that fixture's exact bytes, BigInt numeric mode, size, `mtimeNs`, and parent
   entries unchanged. Before invocation, the test proves Fixtures A-B and
   S1-S17 are pairwise byte-distinct.
7. Every child invocation in criteria 1-6 uses a `10_000` ms process timeout,
   terminates with `error === undefined` and `signal === null`, and leaves every
   existing regular file's exact bytes, BigInt numeric mode, size, and
   `mtimeNs` unchanged. Each directory fixture retains those same stat fields
   and its entries; each temporary parent retains its sorted entries; each
   absent path remains absent. Two invocations for `appr-timestamp-pending`
   against unchanged Fixture A produce byte-identical stdout and stderr.
   Static inspection finds exactly `node:fs` as the Builder file's complete
   CommonJS module-specifier set, exactly `readFileSync` and `statSync` as its
   `node:fs` operations, and no `server.js`, `node:http`, `node:https`,
   `node:net`, `node:child_process`, `fetch`, `WebSocket`, `setTimeout`, or
   `setInterval` reference.
8. Git history contains the complete acceptance-test commit before the
   implementation commit. Apart from role-owned ticket-log appends, the
   test-author commit creates only
   `app/tests/approval-timestamp.test.js`, and the Builder commit creates only
   `app/tools/approval-timestamp.js`. The targeted command
   `node --test app/tests/approval-timestamp.test.js`, the full command
   `npm test --prefix app`, and
   `.github/scripts/test-immutability-check.sh` each exit `0` after
   implementation. Existing tests, `app/server.js`, `app/package.json`,
   lockfiles, dependencies, the persisted schema, `factory/KIT_PIN`, and
   Factory controls remain unchanged by those stages.

## Frozen contract — version 1

- **Freeze result:** PASS. Contract version 1 is frozen.

This is the exact interface for the test-author and Builder. If it proves
wrong, return T-189 to Ready and append a higher-numbered frozen contract;
version 1 must not be edited or removed.

### Command-line interface and errors

- Repository-root invocation is exactly
  `node app/tools/approval-timestamp.js <state-file> <approval-id>`.
- Exactly two positional arguments are accepted. The approval ID is used
  verbatim; it is not trimmed, case-folded, decoded, normalized, or prefixed.
- Zero, one, or three positional arguments produce exit code `2`, empty
  stdout, and exactly this single LF-terminated stderr line:

  ```text
  usage: node app/tools/approval-timestamp.js <state-file> <approval-id>
  ```

- After arity validation, the command uses `statSync` to require the named path
  to be a regular file and `readFileSync` to read it directly as UTF-8. It
  does not import, require, spawn, or otherwise execute `app/server.js`; open a
  listener or other socket; make an outbound request; or leave a timer or
  child process running.
- A read failure, including a nonexistent path, a directory in place of a
  regular file, or an OS read denial, produces exit code `1`, empty stdout, and
  exactly:

  ```text
  approval-timestamp: cannot read state file
  ```

- Bytes rejected by `JSON.parse` produce exit code `1`, empty stdout, and
  exactly:

  ```text
  approval-timestamp: state file is not valid JSON
  ```

- A parsed value that fails the frozen structural rules produces exit code
  `1`, empty stdout, and exactly:

  ```text
  approval-timestamp: invalid Relay state
  ```

- A valid state with no approval whose `id` is strictly equal to the supplied
  ID produces exit code `1`, empty stdout, and exactly:

  ```text
  approval-timestamp: no such approval
  ```

- No error includes the input path, approval ID, OS error, parser text, input
  content, stack trace, or a second line. Every displayed line above ends with
  exactly one LF byte.
- Validation precedence is argument count, regular-file/read access, JSON
  parse, complete structural validation, then exact-ID lookup. No path is
  written, renamed, deleted, chmodded, or created.

The arity tests use fresh empty temporary parents. The one-argument case
supplies nonexistent `<tmp>/state-a.json`. The three-argument case supplies
nonexistent `<tmp>/state-a.json`, `appr-timestamp-pending`, and `extra`. The
usage result rather than a read-failure result proves arity validation
precedes file access.

### Valid state and successful result

For this command, a structurally valid state is a non-null, non-array JSON
object with own `events`, `jobs`, `approvals`, and `outbox` properties whose
values are arrays.

Every `approvals` element must be a non-null, non-array object with:

- a non-empty string `id`;
- a string `status` equal to exactly one of `pending`, `sent`, `rejected`, or
  `blocked_recipient`; and
- a non-empty string `proposedAt`.

Approval IDs must be pairwise distinct. Contents of `events`, `jobs`, and
`outbox`, additional top-level keys, and additional approval keys are opaque
to structural validation. In particular, `jobId`, `action`, and `reason` may
be present but are never copied to output. A referenced job need not exist.
`proposedAt` is copied as a stored non-empty string; the command does not parse,
validate as a date, reformat, or timezone-convert it.

On an exact-ID match, serialize one new object with keys in this exact order:

```json
{"approvalId":<stored id>,"status":<stored status>,"proposedAt":<stored proposedAt>}
```

Use compact `JSON.stringify` formatting and append exactly one LF byte.
Success exits `0` with empty stderr. There are no endpoint paths, HTTP request
or response shapes, UI routes, UI selectors, accessible names, or global page
shell changes in this contract.

### Frozen Fixture A — all approval states, timestamps, and sensitive fields

The test writes the following displayed JSON bytes plus one final LF to an
isolated temporary `state.json`; there is no repository fixture file:

```json
{
  "events": [
    {
      "id": "event-timestamp-private",
      "type": "meeting",
      "payload": {
        "private": "EVENT-PAYLOAD-MUST-NOT-LEAK"
      },
      "receivedAt": "2026-08-23T12:00:00.000Z"
    }
  ],
  "jobs": [
    {
      "id": "job-timestamp-private",
      "eventId": "event-timestamp-private",
      "status": "dead",
      "attempts": 3,
      "lastError": "JOB-ERROR-MUST-NOT-LEAK",
      "retries": 0,
      "attemptsSinceRetry": 3
    }
  ],
  "approvals": [
    {
      "id": "appr-timestamp-pending",
      "jobId": "job-timestamp-pending",
      "action": {
        "to": "PENDING-TO-MUST-NOT-LEAK",
        "subject": "PENDING-SUBJECT-MUST-NOT-LEAK",
        "body": "PENDING-BODY-MUST-NOT-LEAK"
      },
      "status": "pending",
      "proposedAt": "2026-08-23T12:00:01.000Z"
    },
    {
      "id": "appr-timestamp-sent",
      "jobId": "job-timestamp-sent",
      "action": {
        "to": "SENT-TO-MUST-NOT-LEAK",
        "subject": "SENT-SUBJECT-MUST-NOT-LEAK",
        "body": "SENT-BODY-MUST-NOT-LEAK"
      },
      "status": "sent",
      "proposedAt": "2026-08-23T12:00:02.000Z"
    },
    {
      "id": "appr-timestamp-rejected",
      "jobId": "job-timestamp-rejected",
      "action": {
        "to": "REJECTED-TO-MUST-NOT-LEAK",
        "subject": "REJECTED-SUBJECT-MUST-NOT-LEAK",
        "body": "REJECTED-BODY-MUST-NOT-LEAK"
      },
      "status": "rejected",
      "proposedAt": "2026-08-23T12:00:03.000Z",
      "reason": "REJECTION-REASON-MUST-NOT-LEAK"
    },
    {
      "id": "appr-timestamp-blocked",
      "jobId": "job-timestamp-blocked",
      "action": {
        "to": "BLOCKED-TO-MUST-NOT-LEAK",
        "subject": "BLOCKED-SUBJECT-MUST-NOT-LEAK",
        "body": "BLOCKED-BODY-MUST-NOT-LEAK"
      },
      "status": "blocked_recipient",
      "proposedAt": "2026-08-23T12:00:04.000Z"
    }
  ],
  "outbox": [
    {
      "approvalId": "appr-timestamp-sent",
      "body": "OUTBOX-CONTENT-MUST-NOT-LEAK",
      "sentAt": "2026-08-23T12:00:05.000Z"
    }
  ],
  "metadata": {
    "note": "STATE-CONTENT-MUST-NOT-LEAK"
  }
}
```

The four exact `<approval-id>` to stdout mappings, each followed by one final
LF, are:

| Approval ID | Exact compact stdout JSON value |
|---|---|
| `appr-timestamp-pending` | `{"approvalId":"appr-timestamp-pending","status":"pending","proposedAt":"2026-08-23T12:00:01.000Z"}` |
| `appr-timestamp-sent` | `{"approvalId":"appr-timestamp-sent","status":"sent","proposedAt":"2026-08-23T12:00:02.000Z"}` |
| `appr-timestamp-rejected` | `{"approvalId":"appr-timestamp-rejected","status":"rejected","proposedAt":"2026-08-23T12:00:03.000Z"}` |
| `appr-timestamp-blocked` | `{"approvalId":"appr-timestamp-blocked","status":"blocked_recipient","proposedAt":"2026-08-23T12:00:04.000Z"}` |

The sensitive-token set is every Fixture A string ending
`-MUST-NOT-LEAK`. For each successful lookup, every other approval ID is an
unrelated ID and must be absent from both streams.

The frozen unknown-ID literals are `appr-timestamp-missing`,
`APPR-TIMESTAMP-PENDING`, and `appr-timestamp-pending ` (with one trailing
U+0020 space). They are literal child-process arguments, not values produced by
a runtime casing or whitespace mutation, and are pairwise byte-distinct from
each other and every Fixture A approval ID.

### Frozen Fixture B, read-failure paths, and structural-invalid fixtures

Fixture B's exact UTF-8 bytes are the following line with no closing quote or
brace:

```text
{"events":[],"secret":"MALFORMED-STATE-MUST-NOT-LEAK
```

followed by one LF.

Each read-failure case uses a fresh temporary parent:

- `<tmp>/missing-state.json` does not exist before invocation.
- `<tmp>/state-directory` exists as an empty directory, not a regular file.

Each structural-invalid table value is written directly as the displayed
compact UTF-8 JSON plus one LF. No invalid fixture is created at runtime by
casing, normalization, or mutation.

| ID | Exact JSON value | Lookup ID | Violated rule |
|---|---|---|---|
| S1 | `null` | `appr-timestamp-pending` | root is null |
| S2 | `[]` | `appr-timestamp-pending` | root is an array |
| S3 | `{"events":[],"jobs":[],"approvals":[]}` | `appr-timestamp-pending` | missing own `outbox` array |
| S4 | `{"events":{},"jobs":[],"approvals":[],"outbox":[]}` | `appr-timestamp-pending` | `events` is not an array |
| S5 | `{"events":[],"jobs":{},"approvals":[],"outbox":[]}` | `appr-timestamp-pending` | `jobs` is not an array |
| S6 | `{"events":[],"jobs":[],"approvals":{},"outbox":[]}` | `appr-timestamp-pending` | `approvals` is not an array |
| S7 | `{"events":[],"jobs":[],"approvals":[],"outbox":{}}` | `appr-timestamp-pending` | `outbox` is not an array |
| S8 | `{"events":[],"jobs":[],"approvals":[null],"outbox":[]}` | `appr-timestamp-pending` | approval is null |
| S9 | `{"events":[],"jobs":[],"approvals":[[]],"outbox":[]}` | `appr-timestamp-pending` | approval is an array |
| S10 | `{"events":[],"jobs":[],"approvals":[{"id":7,"status":"pending","proposedAt":"2026-08-23T12:00:01.000Z"}],"outbox":[]}` | `appr-timestamp-pending` | `id` is not a string |
| S11 | `{"events":[],"jobs":[],"approvals":[{"id":"","status":"pending","proposedAt":"2026-08-23T12:00:01.000Z"}],"outbox":[]}` | `appr-timestamp-pending` | `id` is empty |
| S12 | `{"events":[],"jobs":[],"approvals":[{"id":"appr-invalid","status":null,"proposedAt":"2026-08-23T12:00:01.000Z"}],"outbox":[]}` | `appr-invalid` | `status` is not a string |
| S13 | `{"events":[],"jobs":[],"approvals":[{"id":"appr-invalid","status":"PENDING","proposedAt":"2026-08-23T12:00:01.000Z"}],"outbox":[]}` | `appr-invalid` | status is outside the exact enum |
| S14 | `{"events":[],"jobs":[],"approvals":[{"id":"appr-invalid","status":"pending"}],"outbox":[]}` | `appr-invalid` | `proposedAt` is missing |
| S15 | `{"events":[],"jobs":[],"approvals":[{"id":"appr-invalid","status":"pending","proposedAt":""}],"outbox":[]}` | `appr-invalid` | `proposedAt` is empty |
| S16 | `{"events":[],"jobs":[],"approvals":[{"id":"appr-invalid","status":"pending","proposedAt":null}],"outbox":[]}` | `appr-invalid` | `proposedAt` is not a string |
| S17 | `{"events":[],"jobs":[],"approvals":[{"id":"appr-duplicate","status":"pending","proposedAt":"2026-08-23T12:00:01.000Z"},{"id":"appr-duplicate","status":"sent","proposedAt":"2026-08-23T12:00:02.000Z"}],"outbox":[]}` | `appr-duplicate` | approval IDs are duplicated; validation precedes lookup |

Before invoking the command, the acceptance test compares the exact bytes of
Fixtures A-B and S1-S17 pairwise and requires every pair to differ. S13 freezes
uppercase `PENDING` directly and verifies its serialized bytes differ from
every valid and invalid fixture. The unknown IDs are also direct literals, not
derived values.

### File locations, lifecycle, protected boundaries, and topology

- Builder ownership is creation of
  `app/tools/approval-timestamp.js` only. It is a standalone CommonJS command;
  its complete module-specifier set is `node:fs`, and its complete
  `node:fs` operation set is `readFileSync` and `statSync`.
- Test-author ownership is creation of
  `app/tests/approval-timestamp.test.js` only. Its allowed module specifiers
  are exactly `node:test`, `node:assert`, `node:child_process`, `node:fs`,
  `node:path`, and `node:os`.
- Existing protected seam `app/tests/approval-detail.test.js` is
  reference-only and is not edited. It asserts the server's stored
  approval/job detail envelope and contains no import/export allowlist,
  exact-source assertion, source snapshot, hash, or module-specifier
  restriction for the new Builder file.
- Existing exact-source and module-specifier assertions in
  `app/tests/dead-jobs.test.js`,
  `app/tests/blocked-approvals.test.js`,
  `app/tests/event-metadata.test.js`,
  `app/tests/job-progress.test.js`, and
  `app/tests/approval-state.test.js` inspect only their corresponding tool
  paths, not `approval-timestamp.js`. No tracked test enumerates the allowed
  contents of `app/tools/` or `app/tests/`.
- Every runtime case owns a fresh temporary directory, creates only its frozen
  fixture or directory, waits synchronously for each child command to
  terminate, and recursively removes the temporary parent in `finally`.
  Repeated invocations require no reset because the command is read-only.
  Tests share no process, port, state file, database, table, generated
  identifier, sequence, counter, clock, or timestamp. There are no foreign
  keys or dependent-table cleanup obligations.
- Every existing regular-file fixture is fingerprinted by exact bytes and
  `fs.statSync(path, { bigint: true }).mode`, `.size`, and `.mtimeNs`, with
  sorted parent entries captured before and after invocation. The directory
  read-failure fixture receives the same stat and entry checks.
- Fixture IDs, statuses, and timestamps are exact literals. No generated
  identifier, sequence, counter, timestamp, initializer, or reset participates
  in an exact expectation.
- Targeted verification is
  `node --test app/tests/approval-timestamp.test.js`. Required repository
  verification after implementation is `npm test --prefix app` and
  `.github/scripts/test-immutability-check.sh`.
- The test-author stage precedes the Builder stage. Neither stage edits an
  existing test, `app/server.js`, `app/package.json`, a lockfile, dependency,
  persisted schema, `factory/KIT_PIN`, ticket state, ledger, or Factory
  control.
- This local file-to-stdout command has no endpoint, UI, cross-origin request,
  authentication, cookie, CORS, domain, credential, preview deployment, or
  external-send behavior. `External: no` remains exact.

## Criteria checklist

- **Pass/fail and unambiguous:** AC1-6 freeze exact arguments, fixture bytes,
  process exit codes, stdout/stderr bytes, lookup semantics, and filesystem
  observations. AC7 freezes timeout, signal, repeatability, module specifiers,
  filesystem operations, and forbidden references. AC8 is decided from Git
  history, per-stage diffs, and three exact command exits.
- **Coverage:** AC1 covers all four approval statuses, every success key,
  timestamp projection, Fixture A, and sensitive fields. AC2 covers verbatim
  case/space-sensitive misses. AC3 covers arity and precedence. AC4-5 cover
  read and parse failures. AC6 covers every structural rule, validation before
  lookup, and fixture distinctness. AC7 covers every invocation's
  self-termination, deterministic read-only behavior, and offline source
  boundary. AC8 covers all file, stage-order, and verification boundaries.
- **No dead contract elements:** every command path, success/error shape,
  fixture, lookup ID, sentinel, lifecycle rule, file location, protected
  boundary, topology disposition, and verification command appears in at
  least one acceptance criterion.
- **Derived-fixture distinctness:** Fixtures A-B and S1-S17 are exact serialized
  literals and are pairwise checked before use. S13's uppercase `PENDING` is
  frozen directly and required to differ from every fixture. Case- and
  space-distinct unknown IDs are exact arguments and are compared with every
  valid ID.
- **Generated-value determinism:** all expected IDs, statuses, and timestamps
  are frozen literals. No generated identifier, sequence, counter, clock,
  initializer, or reset participates in an expectation.
- **Fixture lifecycle closure:** each case has isolated setup, waits for its
  child process, and performs recursive `finally` teardown. The serialized
  targeted command shares no resource; there is no database, parent row,
  sibling dependent table, foreign key, or cascading-cleanup question.
- **Protected boundaries:** the associated seam and all tracked tests were
  inspected. No allowlist, exact-source assertion, source snapshot, hash, or
  module-specifier restriction rejects the new file or its sole `node:fs`
  specifier, so `Protected-Test-Conflicts: none` is exact.
- **Global protected text:** the pinned readiness checker returned
  `GLOBAL TEXT PASS` separately for the usage line, all four error lines, all
  four exact successful output lines, the three property names, and the four
  status values. The command adds no accessible UI name.
- **Deploy/topology completeness:** the command is a local file-to-stdout
  process with no origin, authentication, cookie, CORS, credential, domain,
  preview-deploy, endpoint, UI, or external-send surface.

## Ambiguity log

1. **Exact error protocol.** The seed required fixed privacy-safe errors but
   did not freeze text, exit codes, or validation precedence. No entry in
   [`factory/rulings.md`](../rulings.md) addresses offline commands. Resolved
   as exact path-independent usage, read, parse, structure, and lookup
   outcomes following the shipped offline-tool convention.
2. **Meaning of structurally valid.** The product docs establish the four
   persisted collections and approval lifecycle but do not define a general
   state-file validator. Resolved narrowly from
   [`app/server.js`](../../app/server.js): require all collection arrays and
   validate only approval `id`, `status`, and `proposedAt`, which this command
   matches or emits; unrelated collections and additive fields remain opaque.
3. **Timestamp validation.** Relay creates `proposedAt` with
   `new Date().toISOString()`, but the docs do not define an offline date
   parser, canonicalizer, or timezone conversion. Resolved by requiring a
   non-empty stored string and returning it verbatim. Fixture A freezes exact
   ISO-formatted values generated by the current model without making
   reformatting part of this command.
4. **Duplicate approval IDs.** Exact-ID selection is ambiguous if two
   approvals share an ID. Relay derives approval IDs from idempotent event IDs
   in [`app/server.js`](../../app/server.js), making persisted duplicates
   unreachable in normal operation. S17 freezes duplicate IDs as invalid and
   proves complete validation precedes array lookup.
5. **Output naming and byte determinism.** Relay stores the identifier as
   `id`, while the seed requires `approvalId`, and the seed did not specify key
   order, whitespace, or line ending. Resolved by copying stored `id` to
   `approvalId`, freezing key order `approvalId`, `status`, `proposedAt`,
   compact `JSON.stringify`, and one final LF.
6. **Exact lookup semantics.** “Exact ID” did not state whether case folding,
   trimming, decoding, or normalization was forbidden. Resolved as JavaScript
   strict string equality over the literal argument; uppercase and
   trailing-space unknown IDs distinguish those alternatives.
7. **Sensitive-field proof.** The seed names private field categories but no
   sentinels. Fixture A gives every category a `-MUST-NOT-LEAK` literal, and
   Fixture B embeds malformed-state content; byte-exact streams make leakage
   mechanically detectable.
8. **Unreadable input portability and timestamps.** Permission-denial fixtures
   are unreliable under privileged users, and ordinary reads may update
   filesystem access time. All OS read failures share the frozen result; an
   absent path and directory are portable representatives. Read-only
   invariance is measured with bytes, mode, size, modification time in
   nanoseconds, and directory entries, not access time.
9. **Cross-ticket file ownership.** A grep of every Markdown ticket found no
   other declaration of `approval-timestamp.js` or
   `approval-timestamp.test.js`. Related T-180, T-183, and T-186 approval
   inspectors own distinct paths. Because bounded concurrency is active, the
   other leased branches were also inspected with
   `git show origin/ticket/T-187:factory/tickets/T-187.md` and
   `git show origin/ticket/T-188:factory/tickets/T-188.md`; they own distinct
   outbox-receipt and job-link paths.
10. **Protected source and global text.** Inspection of the approval-detail
    seam and all tracked tests found no static source boundary affecting the
    new file. The pinned readiness checker accepted the ticket and every new
    shell-visible literal, so no `Protected-Test-Conflicts` declaration or
    fixture-seam expansion is required.
11. **Topology, lifecycle, generated values, and external effects.** This is a
    local file-to-stdout command with isolated temporary fixtures and no
    database or network path. Auth, cookies, CORS, deploy domains, generated
    values, dependent rows, UI selectors, and external sends do not apply.

## Coordination

T-189 owns only `app/tools/approval-timestamp.js` and
`app/tests/approval-timestamp.test.js`. Related approval inspectors T-180,
T-183, and T-186 and the active leased T-187/T-188 tickets own distinct paths,
so there is no implementation-surface conflict. The change remains within
Relay's configured `NONVISUAL_PATHS`.

## Planning log

- 2026-08-23: Planner froze contract version 1 with byte-exact results for all
  four approval statuses and proposal timestamps, verbatim
  case/space-sensitive lookup, fixed redacted errors, literal malformed and
  structural-invalid fixtures, duplicate-ID rejection, pairwise fixture
  distinctness, read-only lifecycle checks, protected source boundaries, and
  test-first file ownership. Product docs, rulings, current code, all tracked
  protected tests, sibling ticket declarations, both other active leased
  branches, fixture lifecycle, generated-value rules, topology, and every new
  shell-visible literal were checked; no operator ruling or escalation was
  required.
- 2026-08-23: spec-lint round 1 of frozen contract version 1 against `SPEC.md`
  (behavior spec 3-7, conformance cases 5-7), `AGENTS.md` (Boundaries,
  Verification), `app/server.js`, `app/package.json`, every tracked file in
  `app/tests/`, the shipped offline commands in `app/tools/`,
  `.github/scripts/test-immutability-check.sh`, `factory/rulings.md`,
  `factory/PROJECT.env`, sibling ticket declarations (T-180, T-183, T-186) and
  the concurrently leased `ticket/T-187` and `ticket/T-188` branches.

  Contract-element coverage:

  | Contract element (material behavior) | Positive | Failure / empty | Duplicate | Out of scope |
  |---|---|---|---|---|
  | Argument arity — exactly two positional | AC1, AC2, AC4, AC5, AC6 invoke with two arguments | AC3: zero, one, and three args → exit `2`, empty stdout, exact usage line, temporary parent still empty | the three arity cases share one frozen usage literal | four-or-more args untested (finding 5) |
  | Verbatim ID use — no trim, case-fold, decode, normalize, or prefix | AC1 matches all four exact IDs | AC2: `APPR-TIMESTAMP-PENDING` and `appr-timestamp-pending ` → exit `1`, exact `no such approval` | the three unknown literals share one frozen error line | percent-decoding and Unicode-normalization variants grouped as equivalent non-matching permutations |
  | Regular-file check plus UTF-8 read of the named path | AC1, AC2, AC5, AC6 read frozen fixtures | AC4: absent path and directory path → exit `1`, exact `cannot read state file` | both read failures share one frozen literal | OS read-denial grouped as an equivalent read failure (ambiguity log 8); invalid UTF-8 bytes resolve to the parse failure by contract |
  | `JSON.parse` rejection | — | AC5: Fixture B's unterminated string → exit `1`, exact `state file is not valid JSON` | — | parse-before-structure precedence is vacuous: unparsable bytes have no structure |
  | Structural rules on the four top-level collections | AC1 accepts Fixture A | AC6: S1, S2 (null and array root), S3 (missing own `outbox`), S4-S7 (each collection non-array) | S4-S7 are one rule applied per collection | missing `events`, `jobs`, `approvals` and scalar-root permutations (finding 6) |
  | Structural rules on approval elements and the exact status enum | AC1 accepts all four enum values | AC6: S8, S9 (null and array element), S10, S11 (non-string and empty `id`), S12, S13 (non-string and out-of-enum `status`) | S10-S11 and S12-S13 are the same rule per field | additive approval keys (`jobId`, `action`, `reason`) are opaque by contract |
  | `proposedAt` required as a non-empty stored string, copied verbatim | AC1 returns the four frozen ISO values byte-exactly | AC6: S14 missing, S15 empty, S16 non-string → exit `1`, exact `invalid Relay state` | S14-S16 are one rule over one field | no date parsing, canonicalization, reformatting, or timezone conversion is in scope (ambiguity log 3) |
  | Approval IDs pairwise distinct | AC1: Fixture A holds four distinct IDs | AC6: S17 duplicate `appr-duplicate` → exit `1`, exact `invalid Relay state` | S17 makes array-order behavior unobservable rather than a builder choice | duplicates declared unreachable in real state (`appr-` + `eventId`, `server.js:69`) |
  | Exact-ID lookup miss | — | AC2: three unknown literals → exit `1`, empty stdout, exact `no such approval` | one frozen literal for all three | valid state with an empty `approvals` array (finding 8) |
  | Projection shape — exactly `approvalId`, `status`, `proposedAt`, in that order | AC1 byte-exact stdout plus a parsed key-set-and-order assertion | — | Fixture A `jobId`, `action`, `reason`, `events`, `jobs`, `outbox`, and extra top-level `metadata` must not be copied | — |
  | Approval reported without joining `jobs` | AC1: all four approvals reference `jobId`s absent from Fixture A `jobs` | — | — | — |
  | Compact serialization plus exactly one trailing LF, exit `0`, empty stderr | AC1 byte-exact stdout and zero stderr bytes for all four IDs | — | — | — |
  | Error-stream discipline — one LF-terminated line, no path, ID, OS text, parser text, input content, stack, or second line | — | AC2-AC6 assert exact stderr bytes and zero stdout bytes on every error path | the four error literals plus the usage literal are pairwise distinguished by AC2-AC6 | — |
  | Validation precedence: arity, read, parse, structure, lookup | — | AC3 supplies nonexistent paths so exit `2` proves arity beats read; AC4 vs AC5 proves read beats parse; AC6's exact literal for S12-S17 (whose lookup IDs are present) proves structure beats lookup | — | the supplied second argument in AC4 is not frozen (finding 7) |
  | Redaction of recipients, subjects, bodies, rejection reason, job error, event payload, outbox content, metadata | AC1: no `-MUST-NOT-LEAK` token and no unrelated approval ID in either stream | AC5: Fixture B's embedded token absent from both streams | each sensitive category carries its own sentinel | — |
  | Read-only, offline execution — no listener, no Relay start, no outbound request, no path written or created | AC7 across every invocation in AC1-AC6 | AC3 keeps each temporary parent empty; AC4 keeps the absent path absent and the directory intact with unchanged mode, size, `mtimeNs`, and entries; AC5, AC6 pin bytes, mode, size, and `mtimeNs` | AC7 requires two `appr-timestamp-pending` runs against unchanged Fixture A to be byte-identical | — |
  | Source boundary — sole `node:fs` specifier, `readFileSync` and `statSync` only, no `server.js`, `node:http`, `node:https`, `node:net`, `node:child_process`, `fetch`, `WebSocket`, `setTimeout`, or `setInterval` reference | AC7 static inspection of the Builder file | — | — | — |
  | Self-termination, no leftover timer or child process | AC7: every invocation terminates within `10_000` ms with `error === undefined` and `signal === null` | exit codes in AC2-AC6 are observed from terminated children | — | — |
  | Fixture lifecycle and isolation | fresh temporary parent per case, only its frozen fixture or directory written, child awaited, recursive `finally` teardown; AC6 requires A, B, and S1-S17 pairwise byte-distinct before use | — | — | no database, parent row, sibling dependent table, foreign key, or shared serialized resource exists in this product |
  | Stage file ownership, commit order, verification commands | AC8: test-author commit creates only `app/tests/approval-timestamp.test.js`, Builder commit only `app/tools/approval-timestamp.js` | AC8 requires the targeted command, `npm test --prefix app`, and `.github/scripts/test-immutability-check.sh` to exit `0` with existing tests, `app/server.js`, `app/package.json`, lockfiles, the persisted schema, `factory/KIT_PIN`, and Factory controls unchanged | — | repository baseline checks stay protected-CI controls per the 2026-08-10 T-178 ruling |
  | No endpoint, UI, cross-origin, auth, cookie, CORS, domain, credential, preview-deploy, or external-send surface | — | — | — | contract states none exists; `External: no` matches, and `app/tools/` is inside `NONVISUAL_PATHS=app/tools/,app/tests/` (`app/tests/factory-contract.test.js:12`) |

  Findings:
  1. Criteria quality — AC1-AC7 fix process exit codes, byte-exact stdout and
     stderr, the projection key set and order, fixture bytes with BigInt
     numeric mode, size and `mtimeNs`, parent entries, termination and signal
     outcome, module specifiers, `node:fs` operations, and forbidden source
     references; AC8 is decided from Git history, per-stage diffs, and three
     exact exit statuses. No criterion requires the builder to interpret a
     qualitative term. No blocking finding.
  2. Contract coverage — every material behavior in the table above has at
     least one covering criterion, and each criterion is implementable against
     the contract as written with `node:fs` alone plus `process.stdout`,
     `process.stderr`, and an exit code. I recomputed all four Fixture A
     projections from the frozen rules (exact-ID match, key order
     `approvalId`, `status`, `proposedAt`, compact `JSON.stringify`, one
     trailing LF) and each is byte-identical to its frozen stdout row,
     including `appr-timestamp-blocked` → `blocked_recipient` and
     `2026-08-23T12:00:04.000Z`. I executed the frozen structural rules over
     S1-S17: each classifies invalid for exactly the rule the table names, and
     Fixture A classifies valid. Fixture B fails `JSON.parse` at its
     unterminated string. No blocking finding.
  3. Consistency — the contract's state shape matches `app/server.js`
     (`{events, jobs, approvals, outbox}` at `server.js:16`), the approval
     record shape (`id`, `jobId`, `action`, `status`, `proposedAt`, optional
     `reason` at `server.js:68-78, 109-110`), and the four-value status enum at
     `server.js:76`, where `blocked_recipient` is set only by the allowlist
     refusal at `server.js:94` — matching `SPEC.md` behavior 4 and conformance
     case 6. `proposedAt` is produced by `new Date().toISOString()`
     (`server.js:77`), so the frozen ISO-shaped literals are consistent with
     the model even though the command never parses them. Every linked
     `SPEC.md` and `AGENTS.md` anchor and relative path resolves, and the cited
     behaviors 3, 4-5, 6, 7 and conformance cases 5-7 say what the description
     attributes to them. Neither recorded ruling in `factory/rulings.md` is
     contradicted: the detail-route envelope ruling governs HTTP responses and
     `app/server.js` ownership, not an offline projection, and the T-178 ruling
     is why AC8 lists only the targeted test, the full app suite, and
     immutability rather than the repository baseline scripts. The frozen error
     protocol matches the shipped offline-command convention
     (`app/tools/approval-state.js:3-7, 53-88`: exit `2` usage, exit `1`
     read/parse/structure/lookup, lowercase tool-prefixed single lines, arity
     then `statSync` regular-file check then `readFileSync` then `JSON.parse`
     then validation then `find`). Derived-fixture rule: every invalid fixture
     and unknown-ID argument is an exact literal, not a runtime transformation
     of a valid fixture; I compared the exact serialized bytes of Fixtures A-B
     and S1-S17 pairwise and found no collision, and S13's uppercase `PENDING`
     and the `APPR-TIMESTAMP-PENDING` and trailing-space unknown IDs are
     byte-distinct from every valid fixture and approval ID. Generated-value
     rule: no identifier, sequence, counter, clock, initializer, or reset
     participates in an expectation — every fixture byte is a frozen literal
     the test's own setup writes (see finding 10 for the one realism gap).
     Lifecycle rule: the only serialized test command is
     `node --test app/tests/approval-timestamp.test.js`; each case creates a
     fresh temporary parent, awaits its child synchronously, and removes the
     parent recursively in `finally`, and there is no database, parent row,
     sibling dependent table, non-cascading foreign key, or cross-test shared
     resource requiring child-first cleanup. No blocking finding.
  4. Edge coverage — leakage of every named sensitive category (AC1 sentinels
     for event payload, job error, recipients, subjects, bodies, rejection
     reason, outbox content, and top-level metadata; AC5 the malformed-body
     token), isolation (fresh temporary parent per case), data loss (bytes,
     BigInt numeric mode, size, and `mtimeNs` equality plus unchanged parent
     entries and no created path across AC3-AC7), external effect (AC7's
     offline source boundary), and irreversibility (the command never writes)
     are each covered or explicitly out of scope. There is no authorization
     decision, credential, cookie, CORS, deploy topology, or UI selector in an
     offline read-only command, and `External: no` is correct. No blocking
     finding.
  5. Four or more positional arguments have no covering criterion. The contract
     resolves the behavior — exactly two positional arguments are accepted and
     arity is checked first — so this is not a builder decision, and the case
     is an equivalent permutation of AC3's zero-, one-, and three-argument
     cases.
     SPEC-WARN: the test-author may add a four-argument case to AC3's arity loop asserting the same exit `2` and usage line, at no contract cost.
  6. The missing-own-key rule is exercised only for `outbox` (S3), and a scalar
     root is not exercised alongside S1 and S2. Both are equivalent
     permutations of the same own-property plus `Array.isArray` and
     non-null-non-array-object rules that S3-S7 and S1-S2 already exercise.
     SPEC-WARN: the test-author may add missing-`events`/`jobs`/`approvals` and scalar-root rows to the S-table loop, keeping the frozen S1-S17 rows unchanged.
  7. AC4 does not freeze which `<approval-id>` argument accompanies the two
     read-failure paths, while AC5 and AC6 do freeze theirs. The contract makes
     every pre-lookup result path- and ID-independent, so no product behavior
     is left open and the exact `cannot read state file` line still fails a
     lookup-first implementation for any argument; only run-to-run
     reproducibility of the diff is affected.
     SPEC-WARN: fix one literal second argument for AC4 (for example `appr-timestamp-pending`) so the read-before-lookup order is visible in the test source.
  8. A structurally valid state whose `approvals` array is empty has no
     dedicated criterion. The contract resolves it to AC2's `no such approval`
     result, so it is an equivalent permutation of the lookup-miss class rather
     than an open behavior.
     SPEC-WARN: the test-author may add one valid empty-collections fixture asserting exit `1` and the exact `no such approval` line.
  9. AC7's `10_000` ms bound is a property of the harness, and `node:test`
     applies no default per-test timeout, so the bound only holds if the
     test-author sets it. The criterion stays pass/fail decidable because the
     contract fixes the value and the `spawnSync` `timeout`,
     `result.error === undefined`, and `result.signal === null` assertions.
     SPEC-WARN: pass an explicit `{ timeout: 10_000 }` to each `node:test` case rather than relying on a runner default.
 10. Fixture A's identifiers are internally consistent but not derivable by
     Relay's own initializer, which produces `job-<eventId>` and
     `appr-<eventId>` (`server.js:42, 69`) — Fixture A pairs event
     `event-timestamp-private` with `job-timestamp-private` and approvals
     `appr-timestamp-pending`/`-sent`/`-rejected`/`-blocked`. No criterion or
     contract rule depends on derivability: the command reports stored `id`,
     `status`, and `proposedAt` verbatim and treats everything else as opaque,
     and the fixture is written as literal bytes by the test rather than
     generated by Relay, so no generated value participates in an expectation
     and no setup correction is owed.
     SPEC-WARN: optional realism only — renaming Fixture A's IDs to match `appr-<eventId>` would require a contract version 2 and buys no coverage.
 11. Protected source and global text, verified independently of the planner.
     Every tracked test under `app/tests/` was inspected: the exact-source and
     module-specifier assertions in `dead-jobs.test.js:366-367`,
     `blocked-approvals.test.js:451-452`, `event-metadata.test.js:489-493`,
     `job-progress.test.js:492-493`, and `approval-state.test.js:571-587` each
     resolve a single `TOOL` constant built from their own tool filename, so
     none reads, hashes, snapshots, or allowlists `app/tools/approval-timestamp.js`
     or restricts its `node:fs` specifier; no tracked test enumerates the
     allowed contents of `app/tools/` or `app/tests/`; and the declared seam
     `app/tests/approval-detail.test.js` asserts only the server's stored
     approval/job detail envelope. The pinned kit's
     `scripts/ticket-readiness.py` (KIT_PIN `e85890e4`) returns `READINESS PASS`
     for T-189 in this worktree, and returns `GLOBAL TEXT PASS` separately for
     each of the sixteen new shell-visible literals: the usage line, the four
     error lines, the four exact stdout lines, `approvalId`, `status`,
     `proposedAt`, `pending`, `sent`, `rejected`, and `blocked_recipient`. No
     static check was unknown or unparsable. `Protected-Test-Conflicts: none`
     is exact and no `Fixture-Seams` expansion is owed. No blocking finding.
 12. `Fixture-Seams` lists `app/tests/approval-detail.test.js` while
     `Protected-Test-Conflicts` is `none` and Test-author ownership is
     `app/tests/approval-timestamp.test.js` only. The seam authorizes no edit
     and the file stays reference-only, which matches the contract text and the
     sibling tickets' convention.
     SPEC-WARN: no action required — the test-author must not modify the declared seam.
 13. Cross-ticket ownership re-checked directly: no other Markdown ticket
     declares `approval-timestamp.js` or `approval-timestamp.test.js`, and the
     two concurrently leased branches own distinct paths
     (`ticket/T-187` → `app/tools/outbox-receipt.js`,
     `app/tests/outbox-receipt.test.js`; `ticket/T-188` →
     `app/tools/job-link.js`, `app/tests/job-link.test.js`). No blocking
     finding.
 14. Inter-tool validator divergence: the shipped `app/tools/approval-state.js`
     additionally requires a non-empty string `jobId` on every approval
     (`approval-state.js:39-40`), so a state this command accepts can be
     rejected by that one. Ambiguity log 2 resolves this deliberately — each
     command validates only the fields it matches or emits — and neither tool
     is a dependency of the other, so no product behavior is left to the
     builder.
     SPEC-WARN: the Builder must not copy `approval-state.js`'s `jobId` requirement into the new validator; `jobId` is opaque here and S14-S16 pin `proposedAt` in its place.
 15. Baseline verification in this worktree:
     `.github/scripts/test-immutability-check.sh` passes, and
     `npm test --prefix app` already fails on unmodified `HEAD` under
     full-suite load: 80 of 90 top-level tests passed on the first run, and a
     second run failed a partly different set. The failures land only in
     pre-existing tests neither stage may edit — the offline-tool
     structural-invalid and read-only cases ("AC6: every frozen
     structural-invalid fixture …", "AC7: every invocation self-terminates …",
     "AC7: every S1-S19 structural-invalid fixture …", "AC2: the event-zulu
     lookup …") and the kill-and-restart cases ("7. crash recovery: SIGKILL
     mid-flight loses nothing, duplicates nothing", "4. Reason survives a
     server kill and restart", "2. A dead job, retried, still failing → dead
     again after exactly 3 new attempts …"). Every one of
     `approval-state.test.js`, `blocked-approvals.test.js`,
     `event-metadata.test.js`, `job-progress.test.js`, `dead-jobs.test.js`,
     `state-summary.test.js`, and `pending-approvals.test.js` passes when run
     alone, so this is a pre-existing child-process timing flake under
     concurrent load, not a defect this contract creates. AC8 remains
     satisfiable, but T-189 adds another spawn-heavy file to the same suite.
     SPEC-WARN: treat an AC8 full-suite failure confined to those pre-existing files as environmental and re-run (or run the suite with reduced concurrency) rather than changing T-189's scope or editing an existing test.

SPEC-LINT: PASS
