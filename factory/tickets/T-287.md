# T-287 — Count Relay outbox entries by referenced event type offline

Kit-SHA: 238dd3d0399a58f326bc1be4da5636bce7106cb8
State: Building
Resume-State: Building
Initiative: I-002
Priority: normal
Risk class: low
External: no
Branch: ticket/T-287
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/outbox-approval-count.test.js, app/tests/approval-link.test.js, app/tests/event-type-count.test.js
Authentication-Seams: none
Builder ownership: app/tools/outbox-event-type-count.js only
Test-author ownership: app/tests/outbox-event-type-count.test.js only

## Description

Add a zero-dependency, read-only CommonJS command that counts persisted Relay
outbox entries whose approval and job reference an event with a supplied exact
type while Relay is offline.

## Acceptance criteria

1. `node app/tools/outbox-event-type-count.js <state-file> <event-type>` emits
   compact JSON containing exactly one `outboxEntriesForEventType` integer.
2. Type matching is exact and case-sensitive. Every outbox entry must
   reference exactly one valid approval, every referenced approval exactly one
   valid job, and every referenced job exactly one valid event; duplicate IDs,
   duplicate outbox references, and missing references are rejected before
   counting, and zero matches is valid.
3. Invalid arguments are rejected before file access. Unreadable, malformed,
   or structurally invalid state returns fixed redacted errors.
4. The command is offline, zero-dependency, deterministic, read-only, and
   self-terminating. Tests resolve fixtures from the test file, work from any
   current directory, use a five-second child timeout, and do not assume Git
   history depth.
5. Tests precede implementation. Targeted, full app, and immutability checks
   pass without dependency, server, schema, manifest, or Factory changes.

## Coordination

This ticket owns only its two new paths and is independent of T-285 and T-286.

## Ambiguity log

- **Full-record schema per collection**: AC2 only names the reference chain
  (`outbox.approvalId -> approval`, `approval.jobId -> job`,
  `job.eventId -> event`) and does not restate every field each record must
  carry. Resolved by reusing the exact per-collection schema already frozen by
  this ticket's three Fixture-Seam tests rather than inventing a new one:
  outbox record shape from `app/tests/outbox-approval-count.test.js`
  (`to`/`subject`/`body` present, `approvalId` and `sentAt` non-empty
  strings), approval and job record shape from `app/tests/approval-link.test.js`
  (`approval.id`/`jobId`/`status`, `job.id`/`eventId`), event record shape
  from `app/tests/event-type-count.test.js` (`event.id`/`type`/`receivedAt`).
  This keeps a state file that is valid/invalid for one sibling tool
  valid/invalid the same way for this one, instead of three subtly divergent
  validators over the same collections.
- **`job.eventId` and `approval.jobId` must resolve to an existing record**:
  `approval-link.js` validates `approval.jobId` against a real job but never
  checks `job.eventId` against a real event (it only echoes the string).
  AC2 explicitly requires "every referenced job exactly one valid event", so
  this ticket's job validation is strictly stronger than `approval-link.js`'s
  on that one point; frozen below.
- **Read-failure envelope for missing vs. directory paths**: not
  distinguished by any sibling tool (both collapse to one "cannot read state
  file" line to avoid leaking OS/path detail). Reused verbatim below.
- **Duplicate outbox reference**: "duplicate outbox references...rejected"
  in AC2 is resolved identically to `outbox-approval-count.js`'s frozen S19
  fixture: two outbox entries sharing one `approvalId` is structurally
  invalid, i.e. at most one outbox entry per approval.
- **Cross-ticket file-boundary check**: `MAX_CONCURRENT_TICKETS=3` in
  `factory/PROJECT.env`, so T-285 and T-286 (both `Backlog`, both leased on
  `origin/ticket/T-285` / `origin/ticket/T-286`) were inspected directly.
  T-285 owns `app/tools/approval-event-type-count.js` (+ its own test file)
  and T-286 owns `app/tools/outbox-job-status-count.js` (+ its own test
  file); neither overlaps this ticket's `app/tools/outbox-event-type-count.js`
  / `app/tests/outbox-event-type-count.test.js`. No conflict.
- **Protected-test / global-text preflight**: ran
  `python3 "$FACTORY_RELEASE_PATH/scripts/ticket-readiness.py" --ticket T-287 --workdir "$PWD"`
  (READINESS PASS, confirming no protected-source hash/assertion collision
  against the declared Builder + Fixture-Seams paths) and
  `--global-literal` for the new JSON key `outboxEntriesForEventType`, the
  error prefix `outbox-event-type-count`, and the usage line (all `GLOBAL
  TEXT PASS`). `Protected-Test-Conflicts` stays `none`.

## Frozen contract — version 1

- **Freeze result:** PASS. Contract version 1 is frozen.

### File layout

- `app/tools/outbox-event-type-count.js` (Builder ownership, CommonJS,
  zero-dependency, offline, read-only).
- `app/tests/outbox-event-type-count.test.js` (Test-author ownership).

### CLI

```
node app/tools/outbox-event-type-count.js <state-file> <event-type>
```

- Exactly 2 positional arguments required. 0, 1, 3, or more arguments exit
  `2` with stderr exactly
  `usage: node app/tools/outbox-event-type-count.js <state-file> <event-type>\n`
  and stdout empty, **before any filesystem access**.
- Success: stdout is exactly
  `${JSON.stringify({ outboxEntriesForEventType: <integer> })}\n` (compact,
  single line, e.g. `{"outboxEntriesForEventType":0}\n`), stderr empty, exit
  `0`.
- Unreadable state file (absent path, directory path, permission denied, or
  any other read failure): stderr exactly
  `outbox-event-type-count: cannot read state file\n`, stdout empty, exit `1`.
- State file readable but not valid JSON: stderr exactly
  `outbox-event-type-count: state file is not valid JSON\n`, stdout empty,
  exit `1`.
- State file valid JSON but structurally invalid per the schema below:
  stderr exactly `outbox-event-type-count: invalid Relay state\n`, stdout
  empty, exit `1`.
- No other stdout/stderr content, no second line, no OS error text
  (`ENOENT`, `EISDIR`, `SyntaxError`, stack frames, the input path, or the
  supplied event-type) ever appears in stdout or stderr.
- Read-only: the state file's bytes and stat (`mode`, `size`, `mtimeNs`) and
  its parent directory's entry list are byte-identical before and after every
  invocation, across both successful and failing runs.
- Deterministic: repeated invocations with the same arguments produce
  byte-identical stdout/stderr/exit code.

### State validation (exact algorithm, evaluated top to bottom; first
### violation makes the whole state invalid)

The state file must parse to a JSON object (not `null`, not an array) with
own properties `events`, `jobs`, `approvals`, `outbox`, each an array.

1. **Events** — walk `state.events` in order. Each element must be a plain
   object with own properties `id` (non-empty string), `type` (non-empty
   string), `receivedAt` (non-empty string). `id` must be unique across all
   events. Build a lookup `eventTypeById: id -> type`. Any other own
   properties on an event record are ignored/opaque.
2. **Jobs** — walk `state.jobs` in order. Each element must be a plain
   object with own properties `id` (non-empty string), `eventId` (non-empty
   string). `id` must be unique across all jobs. `eventId` must be a key of
   `eventTypeById` (i.e. reference a valid event from step 1). Build a
   lookup `jobEventTypeById: id -> eventTypeById[eventId]`. Any other own
   properties on a job record (e.g. `status`, `lastError`) are
   ignored/opaque.
3. **Approvals** — walk `state.approvals` in order. Each element must be a
   plain object with own properties `id` (non-empty string), `jobId`
   (non-empty string), `status` (string, one of exactly `"pending"`,
   `"sent"`, `"rejected"`, `"blocked_recipient"`). `id` must be unique
   across all approvals. `jobId` must be a key of `jobEventTypeById` (i.e.
   reference a valid job from step 2, which already resolves to a valid
   event). Build a lookup `approvalEventTypeById: id ->
   jobEventTypeById[jobId]`. Any other own properties (e.g. `action`,
   `proposedAt`, `reason`) are ignored/opaque.
4. **Outbox** — walk `state.outbox` in order. Each element must be a plain
   object with own properties `to`, `subject`, `body` (any value, presence
   only), `approvalId` (non-empty string), `sentAt` (non-empty string).
   `approvalId` must be a key of `approvalEventTypeById` (i.e. reference a
   valid approval from step 3, which already resolves to a valid job and
   event). No two outbox elements may share the same `approvalId` (at most
   one outbox entry per approval). Any other own properties are
   ignored/opaque.

If every step passes, the state is valid. `outboxEntriesForEventType` is the
count of outbox elements whose resolved `approvalEventTypeById[approvalId]`
is exactly (case-sensitive, `===`) equal to the supplied `<event-type>`
argument. Zero matches is a valid, successful `0` result, not an error.

### Module boundary (mirrors the three Fixture-Seam tools' AC8/AC9 checks)

`app/tools/outbox-event-type-count.js` must:
- `require` exactly one module specifier: `"node:fs"`.
- Call exactly one `node:fs` operation: `readFileSync`.
- Contain none of: `server.js`, `fetch(`, `WebSocket`, `node:http`,
  `node:https`, `node:net`, `node:child_process`, `setTimeout`,
  `setInterval`.

### Test conventions (Test-author ownership:
### `app/tests/outbox-event-type-count.test.js`)

- Resolve the tool path via `path.join(__dirname, "..", "tools",
  "outbox-event-type-count.js")`; do not depend on `process.cwd()`.
- Write fixtures to a per-test `fs.mkdtempSync(path.join(os.tmpdir(), ...))`
  directory; no fixture files committed elsewhere in the repo.
- Invoke the tool with `child_process.spawnSync(process.execPath, [TOOL,
  ...args], { timeout: 5_000 })`; assert `result.signal === null` (the
  process must self-terminate inside the 5-second timeout, never rely on the
  parent killing it).
- Do not assume any Git history depth (no `git log`, no commit-count
  assertions).
- Cover at minimum: one exact-match count > 0, one case-distinct or
  leading/trailing-space no-match returning `0`, the three read-failure
  modes (arity, absent path, directory path), malformed JSON, and at least
  one structurally-invalid fixture for each of: duplicate event id,
  duplicate job id, duplicate approval id, duplicate outbox `approvalId`
  (S19-style), job referencing a missing event, approval referencing a
  missing job, outbox entry referencing a missing approval, and a missing
  required field in each of the four collections.

## Spec-lint log

| Contract element | Positive | Failure/empty | Duplicate | Out of scope |
| --- | --- | --- | --- | --- |
| CLI arity and usage envelope | 2 arguments invoke command | 0, 1, 3+ reject before file access | n/a | n/a |
| Count output envelope | compact one-key JSON, exit 0 | zero match is successful `0` | repeated invocation is byte-identical | n/a |
| Exact event-type matching | exact case-sensitive resolved match | case/space-distinct match is `0` | n/a | n/a |
| Read and parse failures | n/a | absent/directory/read failure; malformed JSON use fixed redacted envelopes | n/a | n/a |
| State root and collections | valid object with four arrays | non-object, missing, or non-array collection rejects | n/a | opaque top-level properties ignored |
| Event validation | valid id/type/receivedAt records resolve type | malformed fields reject | duplicate event id rejects | extra event fields ignored |
| Job validation and event linkage | valid id/eventId resolves existing event | malformed or missing event reference rejects | duplicate job id rejects | extra job fields ignored |
| Approval validation and job linkage | valid id/jobId/status resolves existing job | malformed status or missing job reference rejects | duplicate approval id rejects | extra approval fields ignored |
| Outbox validation and approval linkage | valid receipt resolves existing approval | malformed receipt or missing approval reference rejects | repeated approvalId rejects | extra receipt fields ignored |
| Error redaction and read-only behavior | successful and failing runs preserve file and directory state | no path, input, OS, parse, or stack detail leaks | deterministic repeated results | no network, server, child process, timers, dependencies, or writes |
| Test execution conventions | path-independent per-test fixtures and self-terminating child | timeout/signal assertion catches non-termination | n/a | no Git-history assumption or committed fixtures |

1. No blocking findings. The frozen CLI, validation order, reference-chain semantics, output/error envelopes, and read-only/offline boundary are pass/fail decidable and covered by acceptance criteria and the required test cases. The referenced Fixture-Seam schemas match the stated reuse, and no product documentation or recorded ruling conflicts with the contract. `ticket-readiness.py --ticket T-287` and all three declared global-literal checks return PASS; the existing seam tests' source-boundary assertions do not reject the planned module or its literals.

SPEC-LINT: PASS

## Test-author log (round 3)

- Contract-blocked before adding AC9--AC13 tests: AC14 requires reordering
  existing branch history so test-file commits precede builder commit
  `9c83e60`, but the Test-author role is append-only and explicitly forbids
  rebase, reset, amend, or other authenticated-history rewrites. The role
  assigns authorized tests-first history repair to the separate trusted
  operator reorder command. No tests were changed, because any new
  Test-author test commit would also remain after the builder commit and
  cannot satisfy the frozen branch-order requirement.

ROLE-ESCALATE: CONTRACT-BLOCKED

## Test-author log

- Added `app/tests/outbox-event-type-count.test.js` with contract tests for
  AC1 output, AC2 exact matching and state/reference validation, AC3 argument
  and redacted-error handling, and AC4 deterministic/read-only/offline module
  behavior.
- AC5's tests-first ordering and its targeted/full-app/immutability pass
  requirement cannot be expressed as an independent product acceptance test;
  it is a Narrator demo-check / certification item. The focused test command
  was run before implementation and fails because the owned tool is absent.

## Builder log

- 2026-08-27: Created `app/tools/outbox-event-type-count.js` in implementation
  commit `9c83e60` as the sole implementation path for frozen contract
  version 1. The zero-dependency CommonJS command validates the state root
  and its four collections, then validates events, jobs (against
  `eventTypeById`), approvals (against `jobEventTypeById`), and outbox
  entries (against `approvalEventTypeById`) top to bottom exactly per the
  frozen algorithm, before counting outbox entries whose resolved approval
  event type is `===` the supplied argument.
- Module boundary note: the file references `node:fs` via a single
  `const fs = require("node:fs")` and calls `fs.readFileSync` once, matching
  the AC4/module-boundary test's `source.match(/readFileSync/g).length === 1`
  assertion. The nearest sibling tools (`outbox-approval-count.js`,
  `event-type-count.js`) destructure `{ readFileSync }` from the require,
  which produces two source occurrences of the token and would fail this
  ticket's stricter single-occurrence check; the module-object form was used
  instead to satisfy the frozen contract exactly.
- Verification: `node --test app/tests/outbox-event-type-count.test.js`
  passed all 4 ticket-scoped acceptance tests (AC1-AC4). One run hit an
  environmental `spawnSync ETIMEDOUT` on the node binary itself (not a child
  timeout/signal), which cleared on immediate re-run and is unrelated to the
  implementation. `node --check app/tools/outbox-event-type-count.js` passed.
  `.github/scripts/test-immutability-check.sh` passed. No scoped lint or
  typecheck command is provided for `app/tools/`; the full
  `npm test --prefix app` suite, `scripts/repo-check`, and
  `scripts/secret-scan` are repository-wide/full-suite commands out of scope
  for this role and are deferred to protected CI and final certification.
- PR description: Adds an offline, read-only outbox-entries-by-event-type
  count for persisted Relay state. Touches
  `app/tools/outbox-event-type-count.js` and this Builder ticket-log entry.
  No concerns.
Reviewer round 1 signed detail:

> REQUEST CHANGES
>
> 1. AC1/AC2 counting is not discriminating: the only successful fixture has a one-to-one event/job/approval/outbox chain, so a tool that counts matching events, jobs, or approvals instead of referenced outbox entries still returns `2`. Add a valid asymmetric chain fixture where matching upstream records and matching outbox entries differ.
>
> 2. AC2’s frozen validation schema is only partially tested. Cover every constrained required field/type/empty-value rule, each missing/non-array top-level collection, all required outbox-presence fields, and successful handling of opaque extra fields. Also include a valid `blocked_recipient` approval; the current fixture exercises only the other three allowed statuses.
>
> 3. AC3/AC4 coverage does not verify permission-denied read failures or the exact-one-`node:fs`-operation constraint. Add a permission-denied fixture where supported, and make the static boundary assertion reject any additional `fs` operation rather than merely counting `readFileSync` text.
>
> FIX-OWNER: test-author

reviewer round 1: REQUEST CHANGES
reviewer round 1 FIX-OWNER: test-author

## Ambiguity log (round 2 additions)

- **Discriminating-count fixture exact shape**: Reviewer finding 1 requires a
  valid fixture where counting matching events, jobs, or approvals differs
  from the correct referenced-outbox-entry count. Not specified by any
  product doc; resolved by freezing one exact fixture (below) whose four
  candidate counts — events of the target type, jobs resolving to it,
  approvals resolving to it, outbox entries resolving to it — are 2, 3, 4,
  and 1 respectively, so every plausible wrong implementation (count events,
  count jobs, count approvals) disagrees with the correct answer and with
  each other.
- **Permission-denied fixture portability**: `fs.chmodSync(file, 0o000)` does
  not deny reads to a process running as root (common in CI containers).
  Resolved by freezing the exact skip condition
  `process.getuid && process.getuid() === 0` scoped to only the
  permission-denied assertion, not the whole test, so the rest of AC3
  coverage still runs unconditionally on root CI.
- **"Exact-one-`fs`-operation" static check**: the frozen module boundary
  already required exactly one `node:fs` call, but the existing test only
  regex-counted the literal token `readFileSync`, which a bracket-access
  (`fs["readFileSync"]`) or an added second real operation disguised behind
  string concatenation could evade. Resolved by freezing an exact
  call-site-enumeration regex (below) instead of a token count.

## Frozen contract — version 2

- **Freeze result:** PASS. Contract version 2 is frozen.

This epoch is a test-only repair of Reviewer round 1's three findings
(FIX-OWNER: test-author). It does not change `app/tools/outbox-event-type-count.js`,
its CLI, output/error envelopes, or the state-validation algorithm frozen in
version 1, all of which remain in force unchanged. It adds three repaired
acceptance criteria (AC6-AC8, additive to AC1-AC5) and freezes the exact
fixtures/assertions `app/tests/outbox-event-type-count.test.js` must add to
satisfy them.

### Repaired acceptance criteria

6. **(Reviewer finding 1 — discrimination)** At least one passing test
   invocation uses a valid state fixture in which the count of events whose
   own `type` equals the target event type, the count of jobs whose resolved
   event type equals it, the count of approvals whose resolved event type
   equals it, and the correct `outboxEntriesForEventType` result are four
   mutually distinct integers, and the test asserts the tool's stdout matches
   only the correct (outbox-entry) count.
7. **(Reviewer finding 2 — validation coverage)** The test suite contains, at
   minimum, one invalid-state fixture for each cell of the field/collection
   matrix in "Frozen validation-coverage matrix" below, one valid fixture per
   collection demonstrating an ignored opaque extra own property, and one
   valid fixture containing an approval with `status: "blocked_recipient"`
   that is counted like any other valid approval.
8. **(Reviewer finding 3 — permission and fs-boundary strictness)** One test
   asserts the `CANNOT_READ` envelope for a file made unreadable via
   `fs.chmodSync(file, 0o000)` (skipping only that specific assertion when
   `process.getuid && process.getuid() === 0`), and the module-boundary test
   asserts the tool's source contains exactly one `fs.<identifier>(` call
   site and that identifier is exactly `readFileSync`, per the frozen regex
   below, rather than a substring count of the token `readFileSync`.

### Frozen fixture: discrimination fixture (for AC6)

Add a new fixture-building function (name and internal shape are the
Test-author's choice; the following IDs, references, and cardinalities are
frozen and must be reproduced exactly):

```
events:   e1(type "meeting"), e2(type "meeting"), e3(type "email")
jobs:     j1->e1, j2->e1, j3->e2, j4->e3
approvals:a1->j1, a2->j1, a3->j2, a4->j3, a5->j4
outbox:   one entry for a1 only (approvalId "a1")
```

For target event type `"meeting"`:
- events with `type === "meeting"`: 2 (e1, e2)
- jobs resolving to `"meeting"`: 3 (j1, j2, j3)
- approvals resolving to `"meeting"`: 4 (a1, a2, a3, a4)
- correct `outboxEntriesForEventType`: 1 (only a1 has an outbox entry)

The test must assert the tool's stdout is exactly
`{"outboxEntriesForEventType":1}\n` for this fixture and `"meeting"`, and must
not pass if the implementation returned 2, 3, or 4.

### Frozen validation-coverage matrix (for AC7)

Top-level collections — for each of `events`, `jobs`, `approvals`, `outbox`,
freeze two invalid fixtures: the collection key entirely absent from the
state object, and the collection present but not an array (e.g. set to an
object). (8 fixtures total; the existing "non-array collection" case for
`events` counts toward this and does not need duplication.)

Per-record required-field violations — for each field below, freeze a fixture
that (a) deletes the field, and (b) sets the field to an empty string `""`
where the field is a non-empty-string field, or to a value outside the
allowed set where noted:

- `events[]`: `id`, `type`, `receivedAt` (each non-empty string: missing +
  empty-string fixtures for each, 6 total; existing "event required field"
  case for `type` counts toward the missing set).
- `jobs[]`: `id`, `eventId` (each non-empty string: missing + empty-string
  for each, 4 total; existing "job required field" case for `eventId` counts
  toward the missing set; the existing "job missing event" case is the
  reference-resolution check and is separate from these field-presence
  checks).
- `approvals[]`: `id`, `jobId` (each non-empty string: missing + empty-string,
  4 total; existing "approval required field" case for `status` counts
  toward AC2's existing status coverage, not this list), `status` (missing,
  and non-string value, 2 total; the existing "invalid approval status" case
  already covers an out-of-enum string value).
- `outbox[]`: `to`, `subject`, `body` (presence-only fields: one missing-field
  fixture per field, 3 total; existing "outbox required field" case for
  `subject` counts toward this), `approvalId`, `sentAt` (each non-empty
  string: missing + empty-string for each, 4 total).

Opaque extra-field acceptance — for each of `events`, `jobs`, `approvals`,
`outbox`, freeze one valid fixture where one record carries an additional
own property not named in the schema (e.g. `events[0].note = "x"`), asserting
the command still exits `0` with the count unaffected.

Status enum completeness — freeze one valid fixture whose approval `status`
is exactly `"blocked_recipient"` and is counted identically to the other
three statuses (extending, not replacing, the existing `state()` fixture's
pending/sent/rejected coverage).

### Frozen assertions (for AC8)

Permission-denied fixture:

```js
const denied = fixture(dir, "denied.json", state());
fs.chmodSync(denied, 0o000);
const isRoot = typeof process.getuid === "function" && process.getuid() === 0;
if (!isRoot) {
  assertResult(run([denied, "meeting"]), 1, "", CANNOT_READ);
}
fs.chmodSync(denied, 0o644);
```

The `chmodSync(denied, 0o644)` restore is required in all cases (root or not)
so the temp file remains cleanable.

Module-boundary fs-operation check (replaces the existing
`source.match(/readFileSync/g).length === 1` line only; the existing
`require(...)` specifier assertion and forbidden-token loop are unchanged):

```js
const fsCallSites = [...source.matchAll(/\bfs\s*[.\[]\s*["']?(\w+)["']?\s*\(/g)].map(m => m[1]);
assert.deepStrictEqual(fsCallSites, ["readFileSync"]);
```

This enumerates every `fs.foo(...)` or `fs["foo"](...)` call site in source
order and requires the list to contain exactly one entry, `readFileSync`,
closing both the bracket-access evasion and the "second real operation"
evasion the token-count check missed.

### Coverage checklist re-verification (version 2)

- Pass/fail: AC6-AC8 each name exact fixtures, exact IDs/cardinalities, or an
  exact regex/assertion; none require judgment.
- Unambiguous: every new fixture's IDs, reference edges, and expected counts
  are enumerated; the permission-denied skip condition is a single boolean
  expression.
- Coverage: AC6 exercises the version-1 counting semantics under adversarial
  cardinalities; AC7 exercises every remaining untested cell of the version-1
  validation algorithm (all four collections' required fields, opaque-field
  tolerance, and full status enum); AC8 exercises the version-1 read-failure
  envelope under permission denial and hardens the existing module-boundary
  element. No version-1 contract element is left without a version-2 or
  version-1 criterion touching it.
- Cross-ticket file-boundary: re-checked; `factory/tickets/T-285.md` and
  `T-286.md` still declare only `app/tools/approval-event-type-count.js` and
  `app/tools/outbox-job-status-count.js` (plus their own test files); no
  other ticket file references `outbox-event-type-count`. No conflict.
- Deploy/topology: not applicable (offline CLI, no network/auth/cookie
  surface).
- Derived-fixture distinctness: the new discrimination fixture (`e1/e2/e3`,
  `j1..j4`, `a1..a5`, one outbox entry) is byte-distinct from every existing
  fixture in the test file (different IDs and cardinalities).
- Generated-value determinism: not applicable; no generated IDs, counters, or
  timestamps are asserted, all fixture values are frozen string literals.
- Fixture lifecycle closure: each new fixture is written under the existing
  per-test `mkdtempSync` directory; the permission-denied fixture restores
  its mode to `0o644` before the test ends so no unreadable file is left
  behind for OS temp cleanup, matching this ticket's existing convention of
  not deleting temp directories.
- Protected source boundaries / global protected text: this epoch adds no
  new production file, export, module specifier, or global-shell literal
  (only test-file assertions against `app/tools/outbox-event-type-count.js`,
  unchanged from version 1); the version-1
  `ticket-readiness.py --ticket T-287` PASS and global-literal PASS results
  still apply unchanged.

## Spec-lint log (round 2)

| Contract element | Positive | Failure/empty | Duplicate | Out of scope |
| --- | --- | --- | --- | --- |
| CLI and result envelope | exact two-argument compact one-key JSON result | arity, read, JSON, and structural failures have fixed envelopes; zero is successful | deterministic repeated result | n/a |
| Event-type traversal/counting | AC6 fixture resolves 2 events, 3 jobs, 4 approvals, and exactly 1 outbox entry for `meeting` | case/space-distinct input returns 0 | n/a | n/a |
| Root and collection validation | all four required arrays accepted in a valid state | each absent/non-array collection rejects | n/a | opaque top-level fields |
| Record fields and links | all event/job/approval/outbox required fields and links validate in order | missing, empty, invalid-status, and missing-reference cases reject | event, job, approval, and outbox-reference duplicates reject | opaque record fields |
| Approval status set | all four frozen statuses, including `blocked_recipient`, are valid and countable | non-string/out-of-set status rejects | n/a | n/a |
| Offline/read-only boundary | single `node:fs` require and sole `fs.readFileSync` call site | absent, directory, malformed, and non-root permission-denied reads redact; timeout/signal assert termination | n/a | network, server, timers, dependencies, writes, Git-history assumptions |
| Fixture lifecycle | per-test temporary directories contain all fixtures | denied fixture restores `0o644` on both root and non-root paths | n/a | no committed fixtures or unrelated-test cleanup |

1. No blocking findings. Version 2 keeps every material version-1 behavior covered by a decidable criterion, closes the reviewer-identified discrimination and validation-matrix gaps with exact fixtures, and has no contradiction with the linked Fixture-Seam schemas or `factory/rulings.md`. The declared production boundary is confined to the owned tool; the associated test's frozen module-specifier allowlist and source assertions are compatible with the new call-site assertion. The repository does not contain the pinned `ticket-readiness.py` wrapper, so its recorded version-1 readiness/global-text PASS evidence was inspected rather than re-executed; version 2 introduces no Builder literal to recheck.

SPEC-LINT: PASS

## Test-author log (round 2)

- Added AC6's frozen asymmetric `e1`–`e3` / `j1`–`j4` / `a1`–`a5` fixture,
  which distinguishes the required single outbox entry from the 2 matching
  events, 3 matching jobs, and 4 matching approvals.
- Added AC7's complete collection and required-field matrix, opaque own-field
  acceptance for each collection, and countable `blocked_recipient` status.
- Added AC8's root-safe permission-denied assertion and replaced the source
  token count with the frozen exact `fs` call-site assertion.
- Ran `node --test app/tests/outbox-event-type-count.test.js`: 7 passing
  tests. This focused v2 repair runs against the existing v1 implementation;
  no new implementation was authored. AC5's ordering/full-app/immutability
  requirement remains a Narrator demo-check and certification item.

## Builder log (round 2)

- 2026-08-27: No implementation change made. Frozen contract version 2 is
  explicitly a test-only repair ("It does not change
  `app/tools/outbox-event-type-count.js`, its CLI, output/error envelopes, or
  the state-validation algorithm frozen in version 1"), and the existing
  `app/tools/outbox-event-type-count.js` from commit `9c83e60` already
  satisfies it unchanged.
- Verification: `node --test app/tests/outbox-event-type-count.test.js`
  passed all 7 ticket-scoped acceptance tests (AC1-AC4, AC6-AC8).
  `node --check app/tools/outbox-event-type-count.js` passed. No scoped lint
  or typecheck command is provided for `app/tools/`.
- `.github/scripts/test-immutability-check.sh` **fails**:
  `FAIL: test commit 2cda4b330a835ae9c9e55c260e4c599a331cdcb4 ('test:
  strengthen T-287 outbox event type coverage') follows implementation`.
  Root cause: reviewer round 1 assigned `FIX-OWNER: test-author`, so the
  version-2 repair's test-strengthening commit (`2cda4b3`) was necessarily
  authored and committed after the version-1 implementation commit
  (`9c83e60`) already existed in this branch's history — the check's
  test-before-implementation invariant is evaluated per commit across the
  whole branch, not per contract epoch, so a same-file, tests-only, later
  epoch (owned entirely by test-author) trips it even though no
  implementation code changed. Builder ownership is
  `app/tools/outbox-event-type-count.js` only (no test-file edits) and prior
  commits are append-only (no rebase/reset/amend), so this ordering cannot be
  remedied from this role; flagging for reviewer/operator attention rather
  than claiming a pass. This is a pre-existing condition in branch history
  established before this Builder invocation began, not a regression
  introduced here.
  `npm test --prefix app`, `scripts/repo-check`, and `scripts/secret-scan`
  remain repository-wide/full-suite commands out of scope for this role and
  are deferred to protected CI and final certification.
- PR description: No implementation change; confirms the existing
  `app/tools/outbox-event-type-count.js` (frozen version 1) already satisfies
  the version-2 test-only contract repair, all 7 ticket-scoped acceptance
  tests pass. Flags that `test-immutability-check.sh` fails for a pre-existing,
  structural reason (round-2 test-author commit necessarily follows the
  round-1 implementation commit) that is outside Builder's remit to fix.
Reviewer round 2 signed detail:

> REQUEST CHANGES
>
> 1. AC1/AC2/AC7 tests still leave required schema behavior untested. Add cases for four-or-more arguments; non-string values for every non-empty-string field; array/null record elements and an array root; and a valid fixture proving `outbox.to`, `subject`, and `body` accept arbitrary present JSON values. Also snapshot a structurally-invalid fixture before/after invocation to cover AC4’s read-only guarantee on failing validation.
>
> 2. AC5 and the repository convention require test commits before implementation, but Trusted CI fails `test-immutability`: test commit `2cda4b3` follows implementation commit `9c83e60`. Rewrite/reorder the branch so all test-file changes precede implementation, then obtain passing required CI at the resulting HEAD.
>
> Implementation otherwise matches the frozen reference-chain contract. The approval-status enum is pre-existing and complete in its other consumers; no SQL, concurrency, shell, network, or LLM trust-boundary issue is present.
>
> FIX-OWNER: test-author

reviewer round 2: REQUEST CHANGES
reviewer round 2 FIX-OWNER: test-author

## Ambiguity log (round 3 additions)

- **"Four-or-more arguments" as a distinct case**: the frozen CLI envelope
  already covers arity `0, 1, 3, or more` under one error path, and version 1
  already tests 0/1/3. Reviewer finding 1 asks for an explicit 4-argument
  case rather than a new envelope. Resolved by freezing one additional arity
  fixture (4 positional arguments) asserting the identical, already-frozen
  usage envelope — no contract change, coverage-only.
- **"Non-string value" scope**: AC7's version-2 matrix tested only
  missing/empty-string mutations for non-empty-string fields (plus one
  existing non-string case for `approvals[].status`). Reviewer finding 1
  extends this to every non-empty-string field. Resolved by freezing the same
  sentinel non-string value (JSON number `42`) across all nine remaining
  fields for consistency and minimal new surface, matching the existing
  `status` non-string fixture's use of a bare number.
- **"Array/null record elements and an array root"**: not previously
  distinguished from `"non-object root"` (`null`) or `"non-array collection"`
  (object-in-place-of-array). Resolved by freezing: one root-is-array fixture
  (root value `[]`), one null-element and one array-element fixture for
  `events` (proving both non-plain-object element shapes are rejected), and
  one null-element fixture each for `jobs`, `approvals`, `outbox` (the
  element-shape check is identical per collection, so one collection gets
  both shapes and the rest get the cheaper null-element case — this is a
  coverage minimum, not a claim that array-element is untested elsewhere).
- **"Arbitrary present JSON values" for `to`/`subject`/`body`**: the frozen
  version-1 schema already states these three fields are "any value,
  presence only," but no existing fixture exercises a non-string value.
  Resolved by freezing one valid fixture using an array, a number, and an
  object respectively (below), proving presence-only acceptance without
  redefining the schema.
- **AC4 read-only coverage on the `INVALID` path**: the existing AC4 test
  snapshots only the malformed-JSON (`NOT_JSON`) failure path. Resolved by
  freezing one additional snapshot around a structurally-invalid-but-valid-
  JSON fixture (reusing the already-frozen "duplicate event id" mutation) to
  close the read-only proof for the third failure envelope.
- **Commit-ordering repair mechanics (reviewer finding 2)**: this is not a
  product-behavior ambiguity — Reviewer round 2 already named the fix
  ("rewrite/reorder the branch...") and its owner (`FIX-OWNER: test-author`),
  so no operator escalation is needed; `factory/rulings.md` has no entry on
  branch-history reordering and none is added, since this is a mechanical
  repair of this ticket's own branch, not a new precedent. Resolved by
  freezing the observable end-state only (`test-immutability-check.sh` exits
  `0` at the branch's final HEAD, with every commit touching the
  Test-author-owned test file ordered before the sole commit touching the
  Builder-owned tool file) rather than prescribing a specific rebase
  sequence, since the reordering mechanics belong to whichever role executes
  the repair, not to this contract.
- **Cross-ticket file-boundary re-check**: re-inspected `factory/tickets/T-285.md`
  and `T-286.md` directly (both still `State: Backlog`, `origin/ticket/T-285`
  and `origin/ticket/T-286` still exist as separate leased branches under
  `MAX_CONCURRENT_TICKETS=3`). Both declare only their own
  `app/tools/*.js` + own test file; neither references
  `outbox-event-type-count`. No conflict.
- **Protected source boundaries / global protected text**: version 3 adds no
  new production file, export, module specifier, or global-shell literal —
  only additional fixtures/assertions inside the existing
  Test-author-owned `app/tests/outbox-event-type-count.test.js` against the
  unchanged `app/tools/outbox-event-type-count.js`. The pinned
  `ticket-readiness.py` wrapper is still absent from this repository (as
  version 2 recorded), so there is nothing new to run it against; version 1's
  recorded READINESS PASS and global-literal PASS results still apply
  unchanged.

## Frozen contract — version 3

- **Freeze result:** PASS. Contract version 3 is frozen.

This epoch is a test-only repair of Reviewer round 2's two findings
(FIX-OWNER: test-author). It does not change
`app/tools/outbox-event-type-count.js`, its CLI, output/error envelopes, or
the state-validation algorithm frozen in version 1, all of which remain in
force unchanged. It adds six repaired acceptance criteria (AC9-AC14,
additive to AC1-AC8) and freezes the exact fixtures/assertions
`app/tests/outbox-event-type-count.test.js` must add, plus one branch-history
end-state requirement, to satisfy them.

### Repaired acceptance criteria

9. **(Reviewer finding 1 — four-or-more arguments)** One test invocation
   supplies exactly 4 positional arguments (e.g. `[stateFile, "meeting",
   "extra1", "extra2"]`) and asserts the same frozen arity envelope as the
   existing 3-argument case: exit `2`, stderr exactly the usage line, stdout
   empty.
10. **(Reviewer finding 1 — non-string field values)** For each of the nine
    fields `events[].id`, `events[].type`, `events[].receivedAt`,
    `jobs[].id`, `jobs[].eventId`, `approvals[].id`, `approvals[].jobId`,
    `outbox[].approvalId`, `outbox[].sentAt`, one invalid-state fixture sets
    that field to the JSON number `42` (in place of a non-empty string) on
    an otherwise-valid `state()` fixture, and asserts the frozen `INVALID`
    envelope (exit `1`, stdout empty, stderr exactly
    `outbox-event-type-count: invalid Relay state\n`).
11. **(Reviewer finding 1 — array/null record elements and array root)** Six
    invalid-state fixtures, each asserting the frozen `INVALID` envelope:
    (a) the state root is the JSON array `[]`; (b) `events[0]` is replaced
    with `null`; (c) `events[0]` is replaced with `[]`; (d) `jobs[0]` is
    replaced with `null`; (e) `approvals[0]` is replaced with `null`;
    (f) `outbox[0]` is replaced with `null`.
12. **(Reviewer finding 1 — arbitrary presence-only JSON values)** One valid
    fixture sets one outbox entry's `to` to `["a@example.test"]` (array),
    `subject` to `7` (number), and `body` to `{"note":"structured"}` (object),
    leaving every other field valid, and asserts the command still exits `0`
    with the count unaffected by these three fields' types.
13. **(Reviewer finding 1 — read-only on structural-invalidity failure)** One
    additional read-only snapshot pair (mirroring the existing
    malformed-JSON snapshot in the AC4 test) taken immediately before and
    after invoking the tool against a structurally-invalid-but-valid-JSON
    fixture (reusing the frozen "duplicate event id" mutation), asserting
    the file's bytes/mode/size/mtimeNs and the parent directory's entry list
    are byte-identical across the `INVALID`-returning invocation.
14. **(Reviewer finding 2 — commit ordering)** At the branch's final HEAD,
    `.github/scripts/test-immutability-check.sh` exits `0`, and every commit
    that touches the Test-author-owned `app/tests/outbox-event-type-count.test.js`
    precedes, in branch order, the sole commit that touches the
    Builder-owned `app/tools/outbox-event-type-count.js`. Achieving this may
    require reordering (not altering the net diff or message of) this
    branch's existing commits; it must not change Builder or Test-author file
    ownership, and it must not use `--no-verify` or amend/rebase away any
    commit's authorial diff content.

### Frozen fixture values (for AC10-AC13)

Non-string sentinel (AC10): the JSON number literal `42`, applied one field
at a time to an otherwise-valid `state()` clone, for each of the nine fields
listed in AC9 above.

Structural fixtures (AC11), each applied to an otherwise-valid `state()`
clone unless the whole root is replaced:

```
root array:        the fixture file's entire JSON content is []
events[0] = null:  value.events[0] = null
events[0] = []:    value.events[0] = []
jobs[0] = null:    value.jobs[0] = null
approvals[0] = null: value.approvals[0] = null
outbox[0] = null:  value.outbox[0] = null
```

Arbitrary presence-only fixture (AC12), applied to an otherwise-valid
`state()` clone, first outbox entry only:

```
value.outbox[0].to = ["a@example.test"];
value.outbox[0].subject = 7;
value.outbox[0].body = { note: "structured" };
```

Expected result for `"meeting"` against this fixture: exit `0`, stdout
`{"outboxEntriesForEventType":2}\n` (unchanged from the base `state()`
fixture's AC1 result, since only field types on an already-matching outbox
entry changed).

Read-only structural-invalidity snapshot (AC13): reuse the exact "duplicate
event id" mutation already frozen in version 1's AC2 test
(`value.events[1].id = value.events[0].id`) as the fixture; snapshot the
file (bytes/mode/size/mtimeNs) and the containing temp directory's sorted
entry list before invocation, invoke with `["meeting"]` as the second
argument, assert the frozen `INVALID` envelope, then assert the snapshot
taken after invocation is deep-equal to the snapshot taken before.

### Coverage checklist re-verification (version 3)

- Pass/fail: AC9-AC13 each name an exact fixture value, mutation, or snapshot
  pair and an exact expected envelope; none require judgment. AC14 names an
  exact exit code and an exact commit-order relationship, checkable by
  running the named script against the branch's git history.
- Unambiguous: the non-string sentinel, the six structural fixtures, the
  three presence-only field values, and the reused read-only mutation are
  each given a literal value or an exact reference to an already-frozen
  mutation; AC14's "precedes in branch order" is a total order over commits
  touching two named, disjoint file paths.
- Coverage: AC9 closes the arity boundary the existing 0/1/3 cases left one
  case short of "or more"; AC10 closes the non-string-type gap across all
  nine non-empty-string fields the version-2 matrix left partially tested
  (only `status` had a non-string case); AC11 closes the array-root and
  null/array-element gap the existing `null`-root and object-collection
  cases did not cover; AC12 closes the untested "presence only, any value"
  half of the `to`/`subject`/`body` schema clause; AC13 closes the
  `INVALID`-path half of AC4's read-only guarantee (only the `NOT_JSON` path
  was previously snapshotted); AC14 closes the branch-history requirement
  already stated in AC5 but not yet satisfied. No version-1/2 contract
  element is left without a version-1, -2, or -3 criterion touching it.
- Ambiguity log: six new entries above (four-or-more-arguments scope,
  non-string-value scope, array/null-element/array-root scope, arbitrary-
  JSON-value scope, AC4 read-only INVALID-path scope, and commit-ordering
  mechanics), plus re-run cross-ticket, protected-source, and global-text
  checks.
- Cross-ticket file-boundary: re-checked this round; see ambiguity log above.
  No conflict.
- Deploy/topology: not applicable (offline CLI, no network/auth/cookie
  surface; unchanged from versions 1-2).
- Derived-fixture distinctness: every new AC10-AC13 fixture differs from
  every existing fixture and from every other new fixture in at least one of
  mutated field, mutated value, or JSON structure (a `42`-valued field vs. a
  deleted/empty field vs. a `null`/array element vs. an array root are four
  byte-distinct transformations); none is a no-op relative to a valid
  fixture.
- Generated-value determinism: not applicable; no generated IDs, counters,
  or timestamps are asserted; all new fixture values (`42`, `[]`, `null`,
  `["a@example.test"]`, `7`, `{"note":"structured"}`) are frozen literals.
- Fixture lifecycle closure: all new fixtures are written under the existing
  per-test `mkdtempSync` directories; none opens a file handle, chmods a
  file, or otherwise requires teardown beyond the existing convention of not
  deleting temp directories. AC14's commit reordering does not touch any
  fixture or test-runtime state.
- Protected source boundaries / global protected text: no new production
  file, export, module specifier, or global-shell literal is introduced;
  see ambiguity log above. `Protected-Test-Conflicts` stays `none`.

## Spec-lint log (round 3)

| Contract element | Positive | Failure/empty | Duplicate | Out of scope |
| --- | --- | --- | --- | --- |
| CLI and result envelope | exactly two arguments return compact one-key JSON | 0, 1, 3, and 4 arguments return the fixed usage envelope; zero matches succeeds | repeated invocation is byte-identical | n/a |
| Resolved outbox count | AC6's frozen fixture distinguishes 2 events, 3 jobs, 4 approvals, and 1 outbox entry | case- or space-distinct type returns `0` | n/a | direct event/job/approval counts |
| State root and collections | object with four arrays validates | null/array root, missing collection, and non-array collection reject | n/a | opaque top-level fields |
| Event, job, and approval records | required non-empty strings and enum status resolve the full chain | missing, empty, `42`, null/array elements, invalid status, and broken references reject | duplicate event/job/approval IDs reject | opaque record fields |
| Outbox records | present `to`/`subject`/`body` accept the frozen array/number/object values; valid link counts | missing required fields, invalid `approvalId`/`sentAt`, null element, and missing approval reject | repeated `approvalId` rejects | extra outbox fields |
| Fixed error and safety boundary | success and malformed/structural failures preserve file bytes/stat and directory entries | arity, absent, directory, malformed, structural, and non-root permission failures use redacted envelopes | deterministic output is required | network, server, timers, dependencies, writes, and Git-history assumptions |
| Test execution and source boundary | path-independent temporary fixtures, five-second self-terminating child, one `node:fs`/`readFileSync` call site | `signal !== null` and disallowed source tokens fail | n/a | committed fixtures |
| Version-3 test repairs | AC9--AC13 freeze exact added fixtures, envelopes, and snapshots | all enumerated invalid variants reject | n/a | additional equivalent permutations |
| Branch-history repair | AC14 requires passing immutability check with all test-file commits before the sole tool commit | a later test-file commit fails | n/a | reordering mechanics that preserve authorial content |

1. No blocking findings. The version-3 additions make every added test obligation pass/fail decidable with exact fixtures and expected envelopes, while preserving coverage of the version-1/2 CLI, traversal, validation, redaction, and offline/read-only contract. The linked Fixture-Seam schemas and `factory/rulings.md` do not conflict. The current protected source checks associated with the declared Builder file use compatible `node:fs`/`readFileSync` allowlists and forbidden-token assertions; version 3 introduces no Builder literal or global-shell literal. The repository has no runnable `ticket-readiness.py`, so no unknown or unparsable static check was substituted for its recorded prior PASS evidence.

SPEC-LINT: PASS

OPERATOR RESUME: planner
OPERATOR RESUME RECEIPT: 2bd116ea0fa82a23d36a459a340c32d4123917c543317585a81feff42c3a678f

## Ambiguity log (round 4 additions)

- **AC14 execution ownership (Test-author round 3 contract block)**: Reviewer
  round 2 tagged `FIX-OWNER: test-author` for its commit-ordering finding
  (AC14), but achieving AC14's end-state requires rewriting existing branch
  history (reordering commits without altering their diff/message), which
  exceeds both Test-author's and Builder's authorized action set — both
  roles are append-only and explicitly forbidden from rebase, reset, amend,
  or other authenticated-history rewrites. Test-author correctly refused to
  attempt this and escalated (`ROLE-ESCALATE: CONTRACT-BLOCKED`) rather than
  violating its own role boundary. No entry in `factory/rulings.md` covered
  reorder ownership at the time of that escalation, but this repository has
  a directly on-point operational precedent: T-104 hit the identical
  test-immutability ordering violation from a Reviewer round-2 finding, went
  `Blocked-Escalated`, and was resolved when "operator adjudication accepted
  reviewer round 2's recommendation and reordered history with unchanged
  tree content" (`factory/tickets/T-104.md`). Applying that precedent here:
  AC14's history-reordering mechanics are an operator action, not a
  Test-author or Builder action. A corresponding ruling is added to
  `factory/rulings.md` so future tickets do not re-escalate the same
  question. AC14 is restated below as AC15 with execution ownership made
  explicit; its observable end-state (immutability check passes; test-file
  commits precede the tool commit) is unchanged from version 3.
- **Cross-ticket file-boundary re-check**: re-inspected `factory/tickets/T-285.md`
  and `T-286.md` (both still `State: Backlog`); neither declares
  `outbox-event-type-count.js` or its test file. No conflict.
- **Protected source boundaries / global protected text**: this epoch adds no
  new production file, export, module specifier, or global-shell literal —
  it only corrects execution-ownership wording for a branch-history
  requirement already frozen in version 3. Version 1's recorded readiness
  and global-literal PASS results still apply unchanged.

## Frozen contract — version 4

- **Freeze result:** PASS. Contract version 4 is frozen.

This epoch is a contract-only repair of the AC14 execution-ownership gap
that blocked Test-author round 3 (`ROLE-ESCALATE: CONTRACT-BLOCKED`). It
does not change `app/tools/outbox-event-type-count.js`, its CLI,
output/error envelopes, the state-validation algorithm frozen in version 1,
or any AC1-13 fixture/assertion frozen in versions 1-3, all of which remain
in force unchanged. It adds one repaired acceptance criterion (AC15,
additive, superseding AC14's unresolved execution-ownership question) and
requires no new test fixture.

### Repaired acceptance criteria

15. **(Reviewer finding 2, round 2 — commit ordering; execution ownership
    corrected, supersedes AC14)** At the branch's final HEAD,
    `.github/scripts/test-immutability-check.sh` exits `0`, and every commit
    that touches the Test-author-owned
    `app/tests/outbox-event-type-count.test.js` precedes, in branch order,
    the sole commit that touches the Builder-owned
    `app/tools/outbox-event-type-count.js`. This end-state is reached only
    through an operator-authorized reorder of this branch's existing
    commits — reordering commit sequence while preserving each commit's
    authored diff and message unchanged — per the `factory/rulings.md` entry
    recorded for this ticket. Neither Test-author nor Builder may rebase,
    reset, amend, or otherwise rewrite branch history to satisfy this
    criterion; both roles' file ownership stays exactly as declared above.
    Test-author and Builder are neither blocked by nor responsible for
    executing this reorder; they may continue append-only work against their
    owned files, and this criterion is verified independently at the
    branch's final HEAD before merge.

### Coverage checklist re-verification (version 4)

- Pass/fail: AC15 names an exact command (`test-immutability-check.sh`) and
  exact exit code, and an exact commit-order relationship over two disjoint,
  already-declared file paths — no judgment call.
- Unambiguous: "operator-authorized reorder... preserving each commit's
  authored diff and message" and "neither role may rebase/reset/amend" leave
  no room for a second reader to assign this action to Test-author or
  Builder.
- Coverage: AC15 fully supersedes AC14's observable requirement (unchanged)
  while closing the only gap in it (unstated execution ownership); no
  version 1-3 contract element loses coverage.
- Ambiguity log: this round's entry above records the finding and its
  resolution via T-104 precedent and a new `factory/rulings.md` entry.
- Cross-ticket file-boundary: re-checked this round; see ambiguity log above.
  No conflict.
- Deploy/topology: not applicable (offline CLI, no network/auth/cookie
  surface; unchanged from versions 1-3).
- Derived-fixture distinctness: not applicable; this epoch adds no fixture.
- Generated-value determinism: not applicable; this epoch adds no fixture.
- Fixture lifecycle closure: not applicable; this epoch adds no test
  fixture, setup, or teardown.
- Protected source boundaries / global protected text: no new production
  file, export, module specifier, or global-shell literal introduced; see
  ambiguity log above. `Protected-Test-Conflicts` stays `none`.

## Spec-lint log (round 4)

| Contract element | Positive | Failure/empty | Duplicate | Out of scope |
| --- | --- | --- | --- | --- |
| CLI and result envelope | exactly two arguments emit the compact one-key integer JSON result | 0, 1, 3, and 4+ arguments use the fixed usage envelope; zero matches succeeds | repeated calls are byte-identical | n/a |
| Resolved outbox count | the frozen asymmetric fixture yields one referenced outbox entry for `meeting` | case- or space-distinct event type yields `0` | n/a | direct event, job, or approval counts |
| State root, records, and links | four arrays, valid records, enum statuses, and the full event/job/approval/outbox chain validate | malformed root/collection/record, missing/empty/non-string constrained fields, invalid status, and missing reference reject | event/job/approval IDs and outbox approval references reject | opaque extra fields; arbitrary present JSON values for `to`/`subject`/`body` |
| Error and safety boundary | successful, malformed-JSON, and structural-invalid calls preserve frozen file and directory snapshots | arity, absent, directory, permission, parse, and structural failures use redacted envelopes | deterministic output required | network, server, child process, timers, dependencies, writes, and Git-history assumptions |
| Test/runtime and source boundary | path-independent temporary fixtures, five-second self-terminating child, one `node:fs`/`readFileSync` call site | non-null child signal and disallowed source tokens fail | n/a | committed fixtures |
| Version-3 fixture repairs | exact `42`, root/record-shape, presence-only, and structural-invalid snapshot fixtures are specified | every enumerated invalid mutation returns the fixed invalid-state envelope | n/a | additional equivalent permutations |
| Branch-history repair | final HEAD passes immutability with every test-file commit before the sole tool-file commit | a later test-file commit fails the criterion | n/a | execution by Test-author or Builder; reorder is operator-owned |

1. No blocking findings. AC15 is pass/fail decidable, is consistent with the 2026-08-27 ruling that history reordering is operator-owned, and supersedes only AC14's execution-ownership gap without changing its observable end state. The linked fixture-seam schemas remain consistent with the frozen validation algorithm. The protected test for the Builder-owned tool has an exact `node:fs` allowlist, an exact `fs` call-site assertion, and forbidden-token checks; no source snapshot or page-wide text assertion applies to this standalone CLI. Version 4 introduces no planned module or global-shell literal, so the recorded version-1 readiness/global-text PASS evidence remains applicable.

SPEC-LINT: PASS

## Test-author log (round 4)

- Added named acceptance tests AC9--AC13 in
  `app/tests/outbox-event-type-count.test.js`: four-argument usage handling;
  the frozen `42` non-string matrix; array-root and null/array record shapes;
  arbitrary present JSON values for outbox `to`/`subject`/`body`; and an
  `INVALID`-path read-only snapshot.
- Focused local verification passed:
  `node --test --test-name-pattern='AC9 rejects exactly four positional arguments with the frozen usage envelope' app/tests/outbox-event-type-count.test.js`
  and
  `node --test --test-name-pattern='AC10|AC11|AC12|AC13' app/tests/outbox-event-type-count.test.js`.
- AC15's final immutability/commit-order check is an operator-owned reorder
  and independent certification/demo-check item under frozen contract v4; it
  is not expressible as an additional product test by this append-only role.

## Builder log (round 3)

- 2026-08-27: No implementation change made. Frozen contract versions 3 and 4
  are explicitly test-only/contract-only repairs ("It does not change
  `app/tools/outbox-event-type-count.js`, its CLI, output/error envelopes, or
  the state-validation algorithm frozen in version 1"), and the existing
  `app/tools/outbox-event-type-count.js` from commit `9c83e60` already
  satisfies AC1-AC13 unchanged.
- Verification: `node --test app/tests/outbox-event-type-count.test.js`
  passed all 12 ticket-scoped acceptance tests (AC1-AC4, AC6-AC13; AC5 and
  AC15 are ordering/process criteria, not independent product tests).
  `node --check app/tools/outbox-event-type-count.js` passed. No scoped lint
  or typecheck command is provided for `app/tools/`.
- `.github/scripts/test-immutability-check.sh` still fails: test commits
  `2cda4b3` and `6735720` follow implementation commit `9c83e60`. Root cause:
  this is the same pre-existing branch-order condition recorded in the round-2
  Builder log, now explicitly dispositioned by frozen contract version 4's
  AC15 as an operator-authorized reorder — "Neither Test-author nor Builder
  may rebase, reset, amend, or otherwise rewrite branch history to satisfy
  this criterion... Test-author and Builder are neither blocked by nor
  responsible for executing this reorder." Builder ownership remains
  `app/tools/outbox-event-type-count.js` only and prior commits stay
  append-only, so no fix is attempted from this role; AC15 is verified
  independently at the branch's final HEAD before merge, per contract.
  `npm test --prefix app`, `scripts/repo-check`, and `scripts/secret-scan`
  remain repository-wide/full-suite commands out of scope for this role and
  are deferred to protected CI and final certification.
- PR description: No implementation change; confirms the existing
  `app/tools/outbox-event-type-count.js` (frozen version 1) already satisfies
  the version-3/version-4 contract repairs, all 12 ticket-scoped acceptance
  tests pass. Flags that `test-immutability-check.sh` fails for the
  pre-existing, structural reason already dispositioned by AC15 as
  operator-owned, outside Builder's remit to fix.
Reviewer round 3 signed detail:

> REQUEST CHANGES
>
> 1. AC1/AC2 never prove a non-`meeting` type can produce a positive count. Add `"email"` → `1`; a tool hard-coded to count only `"meeting"` passes today.
>
> 2. AC2 permits empty arrays, but no valid empty-state fixture exists. Add `{ events: [], jobs: [], approvals: [], outbox: [] }` expecting `{"outboxEntriesForEventType":0}\n`.
>
> 3. AC5/AC15 is not met at this HEAD: test commits `2cda4b3` and `6735720` follow tool commit `9c83e60`. Passed CI does not prove the literal ordering criterion because its script resets ordering by contract epoch. This requires the contract-designated operator reorder before approval.
>
> FIX-OWNER: test-author

reviewer round 3: REQUEST CHANGES
reviewer round 3 FIX-OWNER: test-author
