# T-184 — inspect one Relay event offline

Kit-SHA: b1a5563096b333fb5d537625a6c7cb9b2c61ef95
State: Planning
Resume-State: Planning
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-184
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/job-detail.test.js
Authentication-Seams: none
Builder ownership: app/tools/event-metadata.js only
Test-author ownership: app/tests/event-metadata.test.js only

## Spec'd description

Add `app/tools/event-metadata.js`, a zero-dependency Node 18+ command that reads
one persisted Relay state file while the service is offline and selects one
event by exact ID. It emits only the selected event's stored ID, type, and
receipt timestamp under the exact keys `eventId`, `type`, and `receivedAt`.
This gives an operator a deterministic event lookup without starting Relay,
changing state, or exposing event payloads, job errors, approval fields,
outbox fields, or unrelated state content.

Product-doc sources:

- [`SPEC.md` behavior 1](../../SPEC.md#behavior-spec) defines exact event-ID
  intake and the stored event's `type` and payload.
- [`SPEC.md` behaviors 2 and 6](../../SPEC.md#behavior-spec) define the related
  job collection and persisted on-disk state that must remain unchanged.
- [`SPEC.md` behavior 7](../../SPEC.md#behavior-spec) defines events as one of
  Relay's durable observable collections.
- [`SPEC.md` conformance cases 1, 2, and 7](../../SPEC.md#test-cases-the-conformance-suite)
  require accepted events to remain visible, duplicate IDs not to duplicate
  work, and persisted work to survive restart.
- The current [`app/server.js` durable-state model](../../app/server.js)
  establishes the four stored collections and event fields `id`, `type`,
  `payload`, and `receivedAt`.
- [`AGENTS.md` Boundaries](../../AGENTS.md#boundaries) keeps `app/`
  zero-dependency, and [`AGENTS.md` Verification](../../AGENTS.md#verification)
  defines the later full-suite and immutability checks.

## Acceptance criteria

1. Given Frozen Fixture A and event ID `event-alpha`, each of two invocations
   of `node app/tools/event-metadata.js <fixture-a-path> event-alpha` exits
   `0`, writes exactly
   `{"eventId":"event-alpha","type":"meeting","receivedAt":"2026-08-07T12:00:00.000Z"}\n`
   to stdout, and writes zero stderr bytes. Every frozen sensitive token is
   absent from both streams, and Fixture A's bytes, BigInt numeric mode, size,
   `mtimeNs`, and sorted parent-directory entries are unchanged after each
   invocation.
2. Given the same Frozen Fixture A and event ID `event-zulu`, the command exits
   `0`, writes exactly
   `{"eventId":"event-zulu","type":"email","receivedAt":"2026-08-07T12:00:01.000Z"}\n`
   to stdout, writes zero stderr bytes, and leaves the fixture and its parent
   unchanged by every observation named in criterion 1.
3. Each frozen lookup miss—`event-missing` and case-distinct `Event-alpha`
   against Fixture A, and `event-missing` against Frozen Fixture B—exits `1`,
   writes zero stdout bytes, and writes exactly
   `event-metadata: event not found\n` to stderr. Both existing fixtures and
   their parent directories remain unchanged by every observation named in
   criterion 1.
4. Each frozen arity case—zero, one, and three positional arguments—exits `2`,
   writes zero stdout bytes, and writes exactly
   `usage: node app/tools/event-metadata.js <state-file> <event-id>\n` to
   stderr. Each case uses a fresh empty temporary parent; the one- and
   three-argument cases pass an absent candidate state path, which remains
   absent, and the parent remains empty.
5. For both frozen read failures, an absent path and a directory path, the
   command exits `1`, writes zero stdout bytes, and writes exactly
   `event-metadata: cannot read state file\n` to stderr. The absent path
   remains absent; the directory remains a directory with identical BigInt
   numeric mode, size, `mtimeNs`, and entries; neither parent gains or loses
   an entry.
6. Given Frozen Fixture C and event ID `event-alpha`, the command exits `1`,
   writes zero stdout bytes, writes exactly
   `event-metadata: state file is not valid JSON\n` to stderr, does not emit
   `MALFORMED-PAYLOAD-MUST-NOT-LEAK`, and leaves the fixture and parent
   directory unchanged by every observation named in criterion 1.
7. For every S1-S19 frozen structural-invalid fixture and event ID
   `event-alpha`, the command exits `1`, writes zero stdout bytes, writes
   exactly `event-metadata: invalid Relay state\n` to stderr, and leaves that
   fixture and its parent directory unchanged by every observation named in
   criterion 1. Before invocation, the acceptance test proves Fixtures A-C
   and S1-S19 are pairwise byte-distinct.
8. Every child invocation in criteria 1-7 uses a `5_000` ms process timeout,
   returns with `error === undefined` and `signal === null`, and emits none of
   the frozen sensitive tokens in either stream.
   Static inspection proves that the Builder file's complete CommonJS
   module-specifier set is exactly `node:fs`, its only `node:fs` operation is
   `readFileSync`, and its source contains no `server.js`, `fetch(`,
   `WebSocket`, `node:http`, `node:https`, `node:net`, `node:child_process`,
   `setTimeout`, or `setInterval` reference.
9. Git history contains the complete test-author commit before the Builder
   commit. Apart from role-owned ticket-log appends, the test-author commit
   creates only `app/tests/event-metadata.test.js` and the Builder commit
   creates only `app/tools/event-metadata.js`. The targeted command
   `node --test app/tests/event-metadata.test.js`, `npm test --prefix app`, and
   `.github/scripts/test-immutability-check.sh` each exit `0` after
   implementation; existing application files, `app/server.js`,
   `app/package.json`, lockfiles, dependencies, persisted schema,
   `factory/KIT_PIN`, and Factory controls are unchanged by both stages.

## Frozen contract — version 1

- **Freeze result:** PASS. Contract version 1 is frozen.

This is the exact interface for the test-author and Builder. If it proves
wrong, return T-184 to Ready and append a higher-numbered frozen contract;
version 1 must not be edited or removed.

### Command-line interface and errors

- Repository-root invocation is exactly
  `node app/tools/event-metadata.js <state-file> <event-id>`.
- Exactly two positional arguments are accepted. Relative state paths resolve
  from the command process's working directory; acceptance tests use absolute
  temporary paths.
- Zero, one, or three positional arguments produce exit code `2`, empty
  stdout, and exactly this single LF-terminated stderr line:

  ```text
  usage: node app/tools/event-metadata.js <state-file> <event-id>
  ```

- The command reads `<state-file>` directly as UTF-8. Any read failure,
  including an absent path, a directory in place of a file, or an OS read
  denial, produces exit code `1`, empty stdout, and exactly:

  ```text
  event-metadata: cannot read state file
  ```

- Bytes rejected by `JSON.parse` produce exit code `1`, empty stdout, and
  exactly:

  ```text
  event-metadata: state file is not valid JSON
  ```

- A parsed value that fails the frozen structural rules produces exit code
  `1`, empty stdout, and exactly:

  ```text
  event-metadata: invalid Relay state
  ```

- A valid state with no event whose stored `id` is strictly equal to the
  supplied `<event-id>` produces exit code `1`, empty stdout, and exactly:

  ```text
  event-metadata: event not found
  ```

- Validation precedence is argument count, read, JSON parse, complete
  structural validation, then event lookup.
- Errors never contain the state path, event ID, OS error, parser text, input
  bytes, stack trace, payload, job field, approval field, outbox field, or a
  second line. Every displayed error is terminated by exactly one LF.

### Structurally valid state and successful output

For this command, structurally valid state is a non-null, non-array JSON object
with own `events`, `jobs`, `approvals`, and `outbox` properties whose values
are arrays.

Every `events` element must be a non-null, non-array object with non-empty
string own properties `id`, `type`, and `receivedAt`. Event IDs must be
pairwise distinct. `payload` and all other event fields are opaque and are
never copied to output. Job, approval, and outbox elements are wholly opaque;
the command does not validate or emit any field from those collections.
Additional top-level properties are allowed and ignored.

The event lookup uses exact, case-sensitive JavaScript string equality and
does not trim, normalize, decode, or case-fold either ID.

Success exits `0`, writes zero stderr bytes, and writes one compact JSON object
plus exactly one LF. Its only keys, in order, are:

```json
{"eventId":<stored id>,"type":<stored type>,"receivedAt":<stored receivedAt>}
```

The output value of `eventId` is copied from the selected event's stored `id`.
No endpoint, HTTP shape, route, UI selector, or global page-shell text is
introduced.

### Frozen Fixture A — two events and sensitive related content

The test writes this exact parsed value to an isolated temporary `state.json`
using `JSON.stringify(value, null, 2) + "\n"`:

```json
{
  "events": [
    {
      "id": "event-zulu",
      "type": "email",
      "payload": {
        "private": "UNSELECTED-PAYLOAD-MUST-NOT-LEAK"
      },
      "receivedAt": "2026-08-07T12:00:01.000Z"
    },
    {
      "id": "event-alpha",
      "type": "meeting",
      "payload": {
        "private": "SELECTED-PAYLOAD-MUST-NOT-LEAK"
      },
      "receivedAt": "2026-08-07T12:00:00.000Z"
    }
  ],
  "jobs": [
    {
      "id": "job-event-alpha",
      "eventId": "event-alpha",
      "status": "dead",
      "lastError": "RELATED-JOB-ERROR-MUST-NOT-LEAK"
    },
    {
      "id": "job-other",
      "eventId": "event-other",
      "status": "dead",
      "lastError": "UNRELATED-JOB-ERROR-MUST-NOT-LEAK"
    }
  ],
  "approvals": [
    {
      "id": "appr-event-alpha",
      "jobId": "job-event-alpha",
      "action": {
        "to": "APPROVAL-TO-MUST-NOT-LEAK",
        "subject": "APPROVAL-SUBJECT-MUST-NOT-LEAK",
        "body": "APPROVAL-BODY-MUST-NOT-LEAK"
      },
      "status": "rejected",
      "reason": "APPROVAL-REASON-MUST-NOT-LEAK"
    }
  ],
  "outbox": [
    {
      "to": "OUTBOX-TO-MUST-NOT-LEAK",
      "subject": "OUTBOX-SUBJECT-MUST-NOT-LEAK",
      "body": "OUTBOX-BODY-MUST-NOT-LEAK",
      "approvalId": "appr-event-alpha"
    }
  ],
  "metadata": {
    "note": "TOP-LEVEL-METADATA-MUST-NOT-LEAK"
  }
}
```

For `event-alpha`, expected stdout is exactly:

```text
{"eventId":"event-alpha","type":"meeting","receivedAt":"2026-08-07T12:00:00.000Z"}
```

followed by one LF. For `event-zulu`, expected stdout is exactly:

```text
{"eventId":"event-zulu","type":"email","receivedAt":"2026-08-07T12:00:01.000Z"}
```

followed by one LF. Every uppercase value containing `MUST-NOT-LEAK` is a
frozen sensitive token that must be absent from stdout and stderr.

### Frozen Fixtures B-C and read-failure paths

- Fixture B is the exact UTF-8 byte sequence
  `{"events":[],"jobs":[],"approvals":[],"outbox":[]}\n`. It is structurally
  valid and every event lookup returns the frozen not-found error.
- Fixture C is the exact UTF-8 byte sequence
  `{"events":[],"payload":"MALFORMED-PAYLOAD-MUST-NOT-LEAK"\n`. It is
  malformed because its object is not closed.
- The missing-path case uses `<tmp>/missing-state.json`, which does not exist.
- The directory-path case uses `<tmp>/state-directory`, an empty directory.
- Frozen lookup IDs are the exact strings `event-alpha`, `event-zulu`,
  `event-missing`, and `Event-alpha`. `Event-alpha` is byte-distinct from
  every valid fixture ID and proves that lookup does not case-fold.

### Frozen structural-invalid fixtures

Each table value is written directly as the displayed compact UTF-8 JSON plus
one LF. No invalid fixture is created by casing, normalization, or mutation.

| ID | Exact JSON value | Violated rule |
|---|---|---|
| S1 | `null` | root is null |
| S2 | `[]` | root is an array |
| S3 | `{"events":[],"jobs":[],"approvals":[]}` | missing own `outbox` array |
| S4 | `{"events":{},"jobs":[],"approvals":[],"outbox":[]}` | `events` is not an array |
| S5 | `{"events":[],"jobs":{},"approvals":[],"outbox":[]}` | `jobs` is not an array |
| S6 | `{"events":[],"jobs":[],"approvals":{},"outbox":[]}` | `approvals` is not an array |
| S7 | `{"events":[],"jobs":[],"approvals":[],"outbox":{}}` | `outbox` is not an array |
| S8 | `{"events":[null],"jobs":[],"approvals":[],"outbox":[]}` | event is null |
| S9 | `{"events":[[]],"jobs":[],"approvals":[],"outbox":[]}` | event is an array |
| S10 | `{"events":[{"type":"meeting","receivedAt":"2026-08-07T12:00:00.000Z"}],"jobs":[],"approvals":[],"outbox":[]}` | event lacks own `id` |
| S11 | `{"events":[{"id":"","type":"meeting","receivedAt":"2026-08-07T12:00:00.000Z"}],"jobs":[],"approvals":[],"outbox":[]}` | event `id` is empty |
| S12 | `{"events":[{"id":7,"type":"meeting","receivedAt":"2026-08-07T12:00:00.000Z"}],"jobs":[],"approvals":[],"outbox":[]}` | event `id` is not a string |
| S13 | `{"events":[{"id":"event-alpha","receivedAt":"2026-08-07T12:00:00.000Z"}],"jobs":[],"approvals":[],"outbox":[]}` | event lacks own `type` |
| S14 | `{"events":[{"id":"event-alpha","type":"","receivedAt":"2026-08-07T12:00:00.000Z"}],"jobs":[],"approvals":[],"outbox":[]}` | event `type` is empty |
| S15 | `{"events":[{"id":"event-alpha","type":7,"receivedAt":"2026-08-07T12:00:00.000Z"}],"jobs":[],"approvals":[],"outbox":[]}` | event `type` is not a string |
| S16 | `{"events":[{"id":"event-alpha","type":"meeting"}],"jobs":[],"approvals":[],"outbox":[]}` | event lacks own `receivedAt` |
| S17 | `{"events":[{"id":"event-alpha","type":"meeting","receivedAt":""}],"jobs":[],"approvals":[],"outbox":[]}` | event `receivedAt` is empty |
| S18 | `{"events":[{"id":"event-alpha","type":"meeting","receivedAt":null}],"jobs":[],"approvals":[],"outbox":[]}` | event `receivedAt` is not a string |
| S19 | `{"events":[{"id":"event-alpha","type":"meeting","receivedAt":"2026-08-07T12:00:00.000Z"},{"id":"event-alpha","type":"email","receivedAt":"2026-08-07T12:00:01.000Z"}],"jobs":[],"approvals":[],"outbox":[]}` | event IDs are duplicated; complete validation precedes lookup |

Before invoking the command, the acceptance test asserts that the serialized
bytes of Fixtures A-C and S1-S19 are pairwise distinct. The case-distinct
lookup ID is frozen directly and is not produced by a runtime transformation.

### File locations, protected boundaries, lifecycle, and verification

- Builder ownership is creation of `app/tools/event-metadata.js` only. It is a
  standalone CommonJS command whose complete module-specifier set is
  `node:fs`; its only filesystem operation is `readFileSync`.
- Test-author ownership is creation of
  `app/tests/event-metadata.test.js` only, using `node:test`, isolated
  temporary directories, and child Node processes with a `5_000` ms timeout.
- `app/tests/job-detail.test.js` is the existing protected fixture seam and is
  reference-only. It contains no import/export allowlist, exact-source
  assertion, source snapshot, or module-specifier restriction for the new
  Builder file. No existing protected test imports or snapshots
  `app/tools/event-metadata.js`. Existing source-boundary assertions in
  `app/tests/dead-jobs.test.js` and
  `app/tests/blocked-approvals.test.js` inspect only their corresponding tool
  files.
- Every runtime case creates its own temporary parent, creates only the
  required frozen file or directory, fingerprints each existing file's exact
  bytes plus
  `fs.statSync(path, { bigint: true }).mode`, `size`, and `mtimeNs`, snapshots
  the directory fixture's same three stat fields and entries, snapshots sorted
  parent entries, waits for the child process, and removes the parent
  recursively in `finally`. Tests share no file, process, port, database,
  table, generated identifier, sequence, counter, clock, or timestamp. There
  are no foreign keys or dependent-table cleanup obligations.
- Targeted verification is
  `node --test app/tests/event-metadata.test.js`. Required repository
  verification after implementation is `npm test --prefix app` and
  `.github/scripts/test-immutability-check.sh`.
- The test-author stage precedes the Builder stage. Neither stage changes an
  existing test, `app/server.js`, `app/package.json`, a lockfile, dependency,
  persisted schema, `factory/KIT_PIN`, ticket state, ledger, or Factory
  control.
- This local CLI has no endpoint, UI, cross-origin request, authentication,
  cookie, CORS, domain, credential, preview deployment, or external-send
  behavior. `External: no` remains exact.

## Criteria checklist

- **Pass/fail and unambiguous:** AC1-7 freeze exact arguments, fixture bytes,
  output and error bytes, exit codes, and filesystem observations. AC8 freezes
  timeout, signal, privacy, and source-boundary assertions. AC9 is decided
  from Git history, per-stage diffs, and three exact command exits.
- **Coverage:** AC1-2 cover both exact successful projections and every output
  key. AC3 covers exact and case-sensitive misses plus Fixture B. AC4-6 cover
  arity, read, and parse failures. AC7 covers every structural rule,
  validation-before-lookup, and fixture distinctness. AC8 covers every
  invocation's privacy, self-termination, offline behavior, and production
  source boundary. AC9 covers files, stage ordering, and verification.
- **Derived-fixture distinctness:** A-C and S1-S19 are exact literals and are
  pairwise checked before use. No invalid serialized fixture is derived by a
  casing, normalization, or mutation operation. The case-distinct lookup ID
  is itself frozen as exact bytes.
- **Generated-value determinism:** all IDs and timestamps are literals. No
  generated identifier, sequence, counter, initializer, reset, or clock
  participates in an expectation.
- **Fixture lifecycle closure:** every case has isolated setup, waits for its
  child process, and performs recursive `finally` teardown. No serialized
  invocation shares a resource; there is no database, parent row, child table,
  foreign key, or cascading cleanup.
- **Protected boundaries:** all tracked protected tests were inspected. The
  associated seam has no allowlist, source assertion, snapshot, or parser
  restricting the new file or its sole `node:fs` specifier, so
  `Protected-Test-Conflicts: none` is exact.
- **Global protected text:** the pinned readiness checker returned
  `GLOBAL TEXT PASS` separately for the usage line, all four error lines, and
  both exact successful output lines. The command adds no UI or accessible
  name.
- **Deploy/topology completeness:** the command is a local file-to-stdout
  process with no origin, authentication, cookie, CORS, credential, domain,
  preview-deploy, endpoint, UI, or external-send surface.

## Coordination

T-184 owns only `app/tools/event-metadata.js` and
`app/tests/event-metadata.test.js`. The operator reassignment makes both paths
distinct from T-179's `event-inspect` pair and from every other active,
parked, or remotely leased qualification ticket. The complete semantic change
remains inside Relay's certified `NONVISUAL_PATHS` policy.

## Ambiguity log

1. **Original cross-ticket file ownership blocker.** A sibling-ticket grep found
   that canceled T-177, active T-179, and T-184 all declare Builder ownership
   of `app/tools/event-inspect.js` and Test-author ownership of
   `app/tests/event-inspect.test.js`. T-177 is explicitly superseded by T-179.
   Although the local T-179 projection says Backlog, the required remote-branch
   check shows `origin/ticket/T-179` in Review with frozen contract version 1,
   a test-first commit, and an implementation commit that already created both
   paths. T-179 also freezes a different success shape (`id`, `type`,
   `receivedAt`, and `hasJob`) from T-184's requested shape (`eventId`, `type`,
   and `receivedAt`). Neither the product docs nor
   [`factory/rulings.md`](../rulings.md) assigns these paths to T-184 or
   authorizes replacing T-179's frozen interface. The operator resolved this
   on 2026-08-23 by assigning T-184 the distinct
   `app/tools/event-metadata.js` and `app/tests/event-metadata.test.js` paths
   while preserving the requested `eventId`/`type`/`receivedAt` interface.
2. **Output alias and key order.** Relay stores the event identifier as `id`,
   while the operator-preserved ticket interface names `eventId`. Resolved by
   copying the selected stored `id` into `eventId` and freezing the exact key
   order `eventId`, `type`, `receivedAt`.
3. **Structural-validation depth.** The product has no general state-file
   validator. Resolved narrowly from the
   [`app/server.js` model](../../app/server.js): validate all four collection
   arrays, every event field needed for deterministic output, and event-ID
   uniqueness required by idempotent intake. Job, approval, outbox, payload,
   and unrelated fields remain opaque.
4. **Exact lookup and errors.** The seed did not specify normalization, error
   text, exit codes, or precedence. No entry in
   [`factory/rulings.md`](../rulings.md) addresses offline event metadata.
   Resolved as strict case-sensitive equality, usage exit `2`, operational
   exits `1`, four path-independent lines, and the frozen precedence above,
   following the shipped offline-command convention.
5. **Sensitive-data proof.** The seed names excluded categories but supplies
   no fixture values. Fixture A gives selected and unselected payloads,
   related and unrelated job errors, approval action/reason fields, outbox
   fields, and additive metadata unique `MUST-NOT-LEAK` sentinels. Fixture C
   adds a malformed-input sentinel; exact stream checks make leakage
   mechanically observable.
6. **Read-only and self-termination proof.** Resolved with a `5_000` ms child
   timeout, null-signal and no-error assertions, repeated invocation,
   byte/BigInt-stat/directory-entry comparisons, and the exact production
   module/API boundary. No service, network, child-process, or timer module is
   admitted in production.
7. **Current cross-ticket ownership.** A grep of all local ticket Markdown
   records found no other declaration of either reassigned path. The active
   remote T-179 branch owns the distinct `event-inspect` pair; leased T-185
   owns `job-progress`; parked leased T-186 owns `approval-state`; and remote
   T-181 through T-183 own distinct outbox, event-job-link, and
   approval-job-link pairs. No implementation-surface conflict remains.
8. **Protected source and global text.** The declared
   `app/tests/job-detail.test.js` seam and all tracked tests contain no static
   boundary for the new file. Existing exact-source checks are tool-specific.
   The pinned readiness checker accepted every fixed shell-visible literal,
   so no protected-test conflict declaration or seam expansion is required.
9. **Fixtures, generated values, and cleanup.** All fixture IDs and timestamps
   are literals, all serialized fixtures are pairwise checked, and every test
   owns one temporary parent removed in `finally`. There is no database,
   generated sequence, parent row, dependent table, or foreign key.
10. **Topology and external effects.** This is an offline local file read with
    no HTTP route, auth, cookie, CORS, origin, credential, preview deployment,
    UI, approval execution, or send. No deployment topology or `external`
    label is required.
11. **Verification scope.** The 2026-08-10 T-178 entry in
    [`factory/rulings.md`](../rulings.md) confirms that an offline inspector's
    required checks are its targeted test, full app suite, and immutability.
    The frozen verification set follows that ruling and adds no product suite
    to this Planner run.

## Planning log

- 2026-08-23: Planner stopped before writing a spec'd description or frozen
  contract because active T-179 already owns and implements both application
  paths assigned to T-184. Exact operator question: **Should T-184 be canceled
  as a duplicate of T-179, or should T-184 be assigned distinct Builder and
  Test-author paths with a separately specified command interface?**

ROLE-ESCALATE: CONTRACT-BLOCKED

OPERATOR ANSWER: Reassign T-184 to Builder path app/tools/event-metadata.js and Test-author path app/tests/event-metadata.test.js; preserve the specified eventId/type/receivedAt interface and every other acceptance criterion.
OPERATOR ANSWER RECEIPT: a40a3d0b665be95a6b70a4c33d875ccd17e8bc11694e5be1fdf2b7a3c889d82a

OPERATOR RESUME: planner
OPERATOR RESUME RECEIPT: a40a3d0b665be95a6b70a4c33d875ccd17e8bc11694e5be1fdf2b7a3c889d82a

- 2026-08-23: Planner applied the operator's reassignment and froze contract
  version 1 for the exact `event-metadata` command, output projection,
  deterministic success and error streams, valid and invalid fixtures,
  privacy checks, lifecycle closure, protected-source boundaries, and
  test-first stage ownership. `SPEC.md`, `AGENTS.md`, `app/server.js`,
  `app/package.json`, the full tracked protected-test suite,
  `.github/scripts/test-immutability-check.sh`, `factory/rulings.md`, every
  local sibling ownership declaration, active and parked leased branches, and
  nearby remote ticket branches were inspected. The pinned readiness checker
  returned `GLOBAL TEXT PASS` for all seven new shell-visible literals. No
  remaining operator decision, protected-test conflict, file-boundary
  conflict, deployment topology, or external-send label is required.
