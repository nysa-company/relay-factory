# T-175 — report pending Relay approvals offline

State: Awaiting Approval
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-175
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/health-approvals.test.js
Authentication-Seams: none
Builder ownership: app/tools/pending-approvals.js only
Test-author ownership: app/tests/pending-approvals.test.js only

## Spec'd description

Add one zero-dependency, read-only Node command,
`node app/tools/pending-approvals.js <state-file>`, that reports pending Relay
approvals from persisted state while the Relay service is offline. The command
projects only `id`, `jobId`, and `proposedAt`, sorts the projection by approval
ID, and never prints approval actions or rejection reasons. This gives an
operator a deterministic queue view without opening an HTTP listener,
executing an approval, or changing persisted state.

Product-doc sources:

- [`SPEC.md` behavior 3](../../SPEC.md#behavior-spec) defines the proposed
  action, its `pending` state, and the approval gate.
- [`SPEC.md` behaviors 4-6](../../SPEC.md#behavior-spec) distinguish later
  approval outcomes from pending work and require state to persist on disk.
- [`SPEC.md` behavior 7](../../SPEC.md#behavior-spec) establishes the persisted
  state collections exposed by Relay; this ticket reads the same state
  representation directly rather than starting the service.
- [`SPEC.md` conformance cases 5-7](../../SPEC.md#test-cases-the-conformance-suite)
  require pending approvals to remain gated, blocked/rejected work not to send,
  and crash-recovered state not to duplicate effects. The new command is a
  read-only projection over those records.
- [`AGENTS.md` Boundaries](../../AGENTS.md#boundaries) requires the app to stay
  zero-dependency and keeps Factory controls outside application work.
  [`AGENTS.md` Verification](../../AGENTS.md#verification) defines the later
  full-suite and immutability checks.

## Acceptance criteria

1. Given Frozen Fixture A, one invocation of
   `node app/tools/pending-approvals.js <fixture-path>` exits `0`, writes
   exactly the Frozen Fixture A expected stdout bytes, and writes zero stderr
   bytes. The two output entries are ordered `appr-alpha`, then `appr-zulu`;
   each has exactly the keys `id`, `jobId`, and `proposedAt`. No non-pending
   approval or frozen sensitive token appears in either stream.
2. Given Frozen Fixture B, the same command exits `0`, writes exactly `[]\n`
   to stdout, and writes zero stderr bytes.
3. Invoking the command with zero positional arguments or with two positional
   arguments exits `2`, writes zero stdout bytes, and writes exactly
   `usage: node app/tools/pending-approvals.js <state-file>\n` to stderr,
   without creating or changing a state path.
4. For each frozen read-failure case (the absent path and the directory path),
   the command exits `1`, writes zero stdout bytes, and writes exactly
   `pending-approvals: cannot read state file\n` to stderr. The absent path
   remains absent; the directory remains a directory with the same entries.
5. Given Frozen Fixture C, the command exits `1`, writes zero stdout bytes,
   writes exactly
   `pending-approvals: state file is not valid JSON\n` to stderr, and leaves
   Fixture C's bytes, mode, and modification time unchanged. Its embedded
   sensitive token does not appear in either output stream.
6. For every entry in the Frozen structural-invalid fixture table, the command
   exits `1`, writes zero stdout bytes, writes exactly
   `pending-approvals: invalid Relay state\n` to stderr, and leaves that
   fixture's bytes, mode, and modification time unchanged.
7. Across criteria 1, 2, 5, and 6, the command process terminates on its own
   after the single report or error, and every existing fixture has the same
   bytes, mode, and modification time before and after invocation. A second
   invocation against unchanged Fixture A produces byte-identical stdout and
   stderr. No invocation starts Relay, opens a network listener, writes a
   sibling file, executes an approval action, or changes any collection.
8. Git history contains the complete acceptance-test commit before the
   implementation commit. The test-author commit changes only
   `app/tests/pending-approvals.test.js`; the Builder commit changes only
   `app/tools/pending-approvals.js`. The three commands frozen under File
   locations and verification each exit `0`, and `app/server.js`,
   `app/package.json`, dependency lockfiles, existing tests,
   `factory/KIT_PIN`, and Factory controls are unchanged by those stages.

## Frozen contract — version 1

- **Freeze result:** PASS. Contract version 1 is frozen.

This is the exact interface for the test-author and Builder. If it proves
wrong, return T-175 to Ready and append a higher-numbered frozen contract;
version 1 must not be edited or removed.

### Command-line interface

- Repository-root invocation:
  `node app/tools/pending-approvals.js <state-file>`.
- The command accepts exactly one positional argument. Relative paths are
  interpreted from the command process's working directory; acceptance tests
  pass an absolute temporary path.
- Zero or two positional arguments produce exit code `2`, empty stdout, and
  exactly this single LF-terminated stderr line:

  ```text
  usage: node app/tools/pending-approvals.js <state-file>
  ```

- The command reads the named file directly as UTF-8. It does not import,
  require, spawn, or otherwise execute `app/server.js`; it opens no HTTP or
  other network listener and does not leave a timer or child process running.
- Read failure, including a nonexistent path, a directory in place of a file,
  or an OS read denial, produces exit code `1`, empty stdout, and exactly:

  ```text
  pending-approvals: cannot read state file
  ```

- Bytes that cannot be parsed by `JSON.parse` produce exit code `1`, empty
  stdout, and exactly:

  ```text
  pending-approvals: state file is not valid JSON
  ```

- A parsed value that fails the frozen structural rules produces exit code
  `1`, empty stdout, and exactly:

  ```text
  pending-approvals: invalid Relay state
  ```

- The command does not include the input path, an OS error, parser text, input
  content, a stack trace, or a second line in any error. Every displayed line
  above is terminated by one LF byte.
- Validation precedence is argument count, read, JSON parse, then structure.
  No error path writes the input or creates a replacement state file.

### Valid state and report shape

For this command, a structurally valid state is a non-null, non-array JSON
object with own `events`, `jobs`, `approvals`, and `outbox` properties whose
values are arrays.

Every `approvals` element must be a non-null, non-array object with:

- non-empty string `id`;
- non-empty string `jobId`;
- non-empty string `proposedAt`; and
- string `status` equal to exactly one of `pending`, `sent`, `rejected`, or
  `blocked_recipient`.

The command does not reinterpret timestamps. Contents of `events`, `jobs`, and
`outbox`, additional top-level keys, and additional approval keys are opaque
to structural validation. In particular, `action` and `reason` may be present
but are never copied to output.

On valid input, select only approvals whose `status` is exactly `pending`,
project each to a newly serialized object with keys in this exact order:

```json
{"id":<stored id>,"jobId":<stored jobId>,"proposedAt":<stored proposedAt>}
```

Sort projected entries by ascending approval `id` using JavaScript string
less-than/greater-than comparison, not locale collation. Serialize the array
with compact `JSON.stringify` formatting and append exactly one LF byte.
Successful stderr is empty and the exit code is `0`. An empty selection is
exactly `[]\n`.

There are no endpoint paths, HTTP response shapes, UI routes, or UI selectors
in this contract.

### Frozen Fixture A — mixed approval states

The test writes this complete JSON value plus the shown final LF to an isolated
temporary `state.json`; there is no repository fixture file:

```json
{
  "events": [],
  "jobs": [],
  "approvals": [
    {
      "id": "appr-zulu",
      "jobId": "job-zulu",
      "action": {
        "to": "ZULU-TO-MUST-NOT-LEAK",
        "subject": "ZULU-SUBJECT-MUST-NOT-LEAK",
        "body": "ZULU-BODY-MUST-NOT-LEAK"
      },
      "status": "pending",
      "proposedAt": "2026-08-06T04:00:05.000Z"
    },
    {
      "id": "appr-sent",
      "jobId": "job-sent",
      "action": {
        "to": "SENT-TO-MUST-NOT-LEAK",
        "subject": "SENT-SUBJECT-MUST-NOT-LEAK",
        "body": "SENT-BODY-MUST-NOT-LEAK"
      },
      "status": "sent",
      "proposedAt": "2026-08-06T04:00:02.000Z"
    },
    {
      "id": "appr-alpha",
      "jobId": "job-alpha",
      "action": {
        "to": "ALPHA-TO-MUST-NOT-LEAK",
        "subject": "ALPHA-SUBJECT-MUST-NOT-LEAK",
        "body": "ALPHA-BODY-MUST-NOT-LEAK"
      },
      "status": "pending",
      "proposedAt": "2026-08-06T04:00:01.000Z"
    },
    {
      "id": "appr-rejected",
      "jobId": "job-rejected",
      "action": {
        "to": "REJECTED-TO-MUST-NOT-LEAK",
        "subject": "REJECTED-SUBJECT-MUST-NOT-LEAK",
        "body": "REJECTED-BODY-MUST-NOT-LEAK"
      },
      "status": "rejected",
      "proposedAt": "2026-08-06T04:00:03.000Z",
      "reason": "REJECTION-REASON-MUST-NOT-LEAK"
    },
    {
      "id": "appr-blocked",
      "jobId": "job-blocked",
      "action": {
        "to": "BLOCKED-TO-MUST-NOT-LEAK",
        "subject": "BLOCKED-SUBJECT-MUST-NOT-LEAK",
        "body": "BLOCKED-BODY-MUST-NOT-LEAK"
      },
      "status": "blocked_recipient",
      "proposedAt": "2026-08-06T04:00:04.000Z"
    }
  ],
  "outbox": [
    {
      "to": "OUTBOX-TO-MUST-NOT-LEAK",
      "subject": "OUTBOX-SUBJECT-MUST-NOT-LEAK",
      "body": "OUTBOX-BODY-MUST-NOT-LEAK",
      "approvalId": "appr-sent",
      "sentAt": "2026-08-06T04:00:06.000Z"
    }
  ],
  "metadata": {
    "note": "METADATA-MUST-NOT-LEAK"
  }
}
```

Expected stdout is exactly this compact JSON line plus one final LF:

```text
[{"id":"appr-alpha","jobId":"job-alpha","proposedAt":"2026-08-06T04:00:01.000Z"},{"id":"appr-zulu","jobId":"job-zulu","proposedAt":"2026-08-06T04:00:05.000Z"}]
```

The fixture covers both pending entries, every accepted non-pending status,
opaque approval fields, opaque outbox data, and an additional top-level key.

### Frozen Fixture B — empty pending selection

Fixture B's exact UTF-8 bytes are:

```text
{"events":[],"jobs":[],"approvals":[],"outbox":[]}
```

followed by one LF. Its expected stdout is exactly `[]` followed by one LF.

### Frozen Fixture C — malformed JSON

Fixture C's exact UTF-8 bytes are:

```text
{"events":[],"secret":"MALFORMED-BODY-MUST-NOT-LEAK"
```

followed by one LF. It is malformed because its object is not closed.

### Frozen read-failure paths

Each read-failure test uses a fresh temporary parent:

- `<tmp>/missing-state.json` does not exist before invocation.
- `<tmp>/state-directory` exists as an empty directory, not a regular file.

### Frozen structural-invalid fixtures

Each table value is written directly as the displayed compact UTF-8 JSON plus
one LF. No invalid fixture is derived at runtime by casing, normalization, or
mutation.

| ID | Exact JSON value | Violated rule |
|---|---|---|
| S1 | `[]` | root is not an object |
| S2 | `{"events":[],"jobs":[],"approvals":[]}` | missing `outbox` array |
| S3 | `{"events":{},"jobs":[],"approvals":[],"outbox":[]}` | `events` is not an array |
| S4 | `{"events":[],"jobs":{},"approvals":[],"outbox":[]}` | `jobs` is not an array |
| S5 | `{"events":[],"jobs":[],"approvals":{},"outbox":[]}` | `approvals` is not an array |
| S6 | `{"events":[],"jobs":[],"approvals":[],"outbox":{}}` | `outbox` is not an array |
| S7 | `{"events":[],"jobs":[],"approvals":[null],"outbox":[]}` | approval is not an object |
| S8 | `{"events":[],"jobs":[],"approvals":[{"id":"","jobId":"job-a","proposedAt":"2026-08-06T04:00:00.000Z","status":"pending"}],"outbox":[]}` | `id` is empty |
| S9 | `{"events":[],"jobs":[],"approvals":[{"id":"appr-a","jobId":7,"proposedAt":"2026-08-06T04:00:00.000Z","status":"pending"}],"outbox":[]}` | `jobId` is not a string |
| S10 | `{"events":[],"jobs":[],"approvals":[{"id":"appr-a","jobId":"job-a","proposedAt":null,"status":"pending"}],"outbox":[]}` | `proposedAt` is not a string |
| S11 | `{"events":[],"jobs":[],"approvals":[{"id":"appr-a","jobId":"job-a","proposedAt":"2026-08-06T04:00:00.000Z","status":"PENDING"}],"outbox":[]}` | status is outside the exact enum |

Before invoking the command, the acceptance test asserts that Fixtures A, B,
C, and S1-S11 are pairwise byte-distinct. Thus no invalid case is an identity
transformation of a valid fixture.

### File locations, lifecycle, and verification

- Builder implementation and ownership:
  `app/tools/pending-approvals.js` only. It is a new standalone CommonJS
  command using only installed Node standard-library modules.
- Test-author implementation and ownership:
  `app/tests/pending-approvals.test.js` only. It uses `node:test`, isolated
  temporary directories, and child Node processes; no service is started.
- Existing protected seam:
  `app/tests/health-approvals.test.js` is reference-only and is not edited.
- Every test case owns a fresh temporary directory, writes only its frozen
  runtime fixture, and removes that directory in teardown. Tests share no
  port, process, state file, database, table, counter, or generated value.
  There are no foreign keys or dependent-table cleanup obligations.
- Fixture timestamps and IDs are literals. No generated identifier, sequence,
  counter, clock, initializer, or reset participates in an exact expectation.
- Targeted command:
  `node --test app/tests/pending-approvals.test.js`.
- Required repository commands after implementation:
  `npm test --prefix app` and
  `.github/scripts/test-immutability-check.sh`.
- The test-author stage precedes the Builder stage. Neither stage edits
  existing tests, `app/server.js`, `app/package.json`, lockfiles,
  `factory/KIT_PIN`, ticket state, the ledger, or Factory controls.
- This offline command has no cross-origin, authentication, cookie, CORS,
  domain, credential, or preview-deploy topology. It cannot trigger an
  external send, so `External: no` remains correct.

## Criteria checklist

- **Pass/fail and unambiguous:** AC1-7 freeze process exit codes and exact
  stdout/stderr bytes, fixture bytes, projection keys, ordering, validation
  outcomes, and read-only observations. AC8 is decided from Git history,
  per-stage diffs, and three exact command exit statuses. No subjective quality
  phrase remains.
- **Coverage:** AC1 covers every valid-state rule, all four approval statuses,
  ordering, projection shape, extra fields, redaction, and Fixture A. AC2
  covers the empty selection and Fixture B. AC3 covers argument arity and
  usage output. AC4 covers both read failures. AC5 covers malformed JSON,
  parser-message suppression, and Fixture C. AC6 covers every structural rule
  through S1-S11. AC7 covers repeat determinism, lifecycle, offline execution,
  and no mutation. AC8 covers every file boundary and verification command.
- **Derived-fixture distinctness:** all fixtures are exact literals, not
  runtime mutations; the test additionally proves A, B, C, and S1-S11 are
  pairwise byte-distinct before use.
- **Generated-value determinism:** no generated value is asserted. All IDs and
  timestamps are frozen literals.
- **Fixture lifecycle closure:** each case has an isolated temporary parent and
  recursive teardown. No serialized command shares resources, and there is no
  database or parent/child row cleanup.
- **Protected boundaries:** the Builder adds a new file. The associated
  protected seam contains no import/export allowlist, exact-source assertion,
  or source snapshot and does not import the new module. No protected-test
  conflict declaration or Test-author seam expansion is required.
- **Global protected text:** the readiness check returned `GLOBAL TEXT PASS`
  for the usage line and all three new deterministic error literals. The
  command adds no UI or accessible name.

## Ambiguity log

1. **Exact error protocol.** The seed ticket required concise deterministic
   errors but did not freeze their text or exit codes. No ruling in
   [`factory/rulings.md`](../rulings.md) addresses offline commands. Resolved
   as the four exact, path-independent outcomes above, following the existing
   repository convention that usage errors exit `2` and operational failures
   exit nonzero without platform-specific details.
2. **Meaning of structurally valid.** `SPEC.md` and `app/server.js` establish
   the four persisted collections and the approval status/fields but do not
   define a general state-file validator. Resolved narrowly for this read-only
   report: validate all top-level collections and only the approval fields the
   command must classify or return; treat unrelated collection contents and
   additive fields as opaque. S1-S11 make that boundary testable.
3. **Sorting and JSON determinism.** “Approval-id-sorted JSON” did not specify
   locale, whitespace, property order, or line termination. Resolved as
   JavaScript string comparison, compact JSON, the exact projected key order,
   and one final LF, with byte-exact expectations in AC1-2.
4. **Sensitive-field boundary.** The seed named recipients, subjects, bodies,
   and rejection reasons but did not provide sentinel data. Fixture A includes
   unique values in every named field, a nonempty outbox, all approval
   statuses, and additive metadata; exact stdout/stderr assertions prove none
   leaks.
5. **Missing versus unreadable input.** OS permission failures are not portable
   under privileged test users. The contract groups all read failures under
   one deterministic error and freezes an absent path plus a directory path as
   portable representatives.
6. **Cross-ticket file ownership.** A grep of `factory/tickets/` found only
   stranded T-172 declaring the same Builder and test-author paths. T-175's
   reconciled coordination record explicitly replaces T-172, and T-172 has no
   active worktree. The other active leased branches, T-173 and T-174, were
   inspected both at `origin/ticket/T-XXX` and in their worktrees; they own
   `state-summary.js`/`state-summary.test.js` and
   `dead-jobs.js`/`dead-jobs.test.js`, respectively, with no T-175 overlap.
7. **Protected source and text boundaries.** The new Builder file has no
   existing importer or source snapshot. The declared
   `app/tests/health-approvals.test.js` seam was inspected and asserts existing
   server HTTP behavior only, so it remains unchanged. Readiness checks for
   every new fixed visible literal returned `GLOBAL TEXT PASS`; no
   `Protected-Test-Conflicts` entry is needed.
8. **Deploy topology and external effects.** The command is an offline local
   file read with no server, origin, auth, cookie, credential, CORS, or preview
   behavior. Approval execution and persisted schema changes are explicitly
   excluded, so no external label or topology contract is required.

## Coordination

This ticket replaces the stranded pre-provider T-172 identity. It has no
dependency on T-173 or T-174 and owns no file they own, so all three may run
concurrently.

## Planning log

- 2026-08-06: Planner froze contract version 1. The contract fixes byte-exact
  success and error streams, a mixed-status redaction fixture, empty and
  failure fixtures, structural validation, read-only/offline behavior,
  lifecycle closure, and stage file ownership. Sibling ticket declarations,
  both active leased branches, the protected fixture seam, and global visible
  literals were checked; no operator decision or new ruling was required.
- 2026-08-06: spec-lint of frozen contract version 1 against `SPEC.md`
  (behavior spec 3-7, conformance cases 5-7), `AGENTS.md` (boundaries,
  verification), `app/server.js`, `app/package.json`, the existing
  `app/tests/` suite, `.github/scripts/test-immutability-check.sh`,
  `factory/rulings.md`, the sibling ticket declarations T-170 through T-174,
  and the pinned `scripts/ticket-readiness.py` at `factory/KIT_PIN`.

  Contract-element coverage:

  | Contract element (material behavior) | Positive | Failure / empty | Duplicate | Out of scope |
  |---|---|---|---|---|
  | Argument arity — exactly one positional | AC1, AC2, AC4, AC5, AC6 invoke with one path | AC3: zero and two args → exit `2`, empty stdout, exact usage line | — | three-or-more args untested (see finding 5) |
  | Read of the named file as UTF-8, no server import/spawn | AC1, AC2 read frozen fixtures | AC4: absent path and directory path → exit `1`, exact `cannot read state file` | — | OS read-denial grouped as an equivalent read failure (ambiguity log 5) |
  | `JSON.parse` failure | — | AC5: Fixture C unclosed object → exit `1`, exact `state file is not valid JSON` | — | — |
  | Structural rules on the four top-level collections | AC1, AC2 accept Fixtures A and B | AC6: S1-S6 (non-object root, missing `outbox`, each non-array collection) | — | root `null` (see finding 8) |
  | Structural rules on approval elements and the status enum | AC1 accepts all four enum values in Fixture A | AC6: S7-S11 (non-object element, empty `id`, non-string `jobId`, non-string `proposedAt`, out-of-enum status) | — | numeric `id`, absent `status`, array-valued element (see finding 8) |
  | Pending-only selection | AC1: only `appr-alpha`, `appr-zulu` emitted | AC2: no pending approvals → `[]\n` | Fixture A holds `sent`, `rejected`, `blocked_recipient` siblings that must not appear | — |
  | Projection shape — exactly `id`, `jobId`, `proposedAt`, in that order | AC1 byte-exact stdout | AC2 empty array | Fixture A `action`, `reason`, and extra top-level `metadata` must not be copied | — |
  | Ascending `id` sort by JavaScript string comparison | AC1 fixes the order `appr-alpha`, then `appr-zulu` against reversed input order | AC2 empty | equal-`id` tie order unspecified (see finding 7) | locale collation excluded by the contract |
  | Compact serialization plus exactly one trailing LF, exit `0`, empty stderr | AC1, AC2 byte-exact stdout and zero stderr bytes | AC2 fixes the empty selection as exactly `[]\n` | — | — |
  | Error-stream discipline — one LF-terminated line, no path, OS text, parser text, input content, stack, or second line | — | AC3-AC6 assert exact stderr bytes and zero stdout bytes on every error path | four distinct error literals are pairwise distinguished by AC3-AC6 | — |
  | Validation precedence: arity, read, parse, structure | — | AC3 (arity beats read), AC4 vs AC5 (read beats parse), AC5 vs AC6 (parse beats structure) | — | — |
  | Redaction of recipients, subjects, bodies, rejection reasons | AC1 sentinel tokens absent from both streams | AC5 Fixture C embedded secret absent from both streams | every named sensitive field carries a unique sentinel | — |
  | Read-only, offline execution: no listener, no Relay start, no approval executed, no sibling write, no collection change | AC7 across criteria 1, 2, 5, 6 | AC3 asserts no state path created or changed; AC4 asserts the absent path stays absent and the directory keeps its entries | AC7 second invocation is byte-identical | AC7 sweep does not extend to AC3/AC4 (see finding 6) |
  | Self-termination, no leftover timer or child process | AC7 | exit codes in AC3-AC6 are observed from a terminated child | — | — |
  | Fixture lifecycle and isolation | contract fixes a fresh temporary parent per case with recursive teardown; no port, state file, database, table, counter, or generated value is shared | — | — | no foreign keys or dependent-row cleanup exist in this product |
  | Stage file ownership, commit order, verification commands | AC8: test-author commit touches only `app/tests/pending-approvals.test.js`, Builder commit only `app/tools/pending-approvals.js` | AC8 requires the three frozen commands to exit `0` and `app/server.js`, `app/package.json`, lockfiles, existing tests, `factory/KIT_PIN`, and Factory controls unchanged | — | — |

  Findings:
  1. Criteria quality — AC1-AC7 fix process exit codes, byte-exact stdout and
     stderr, fixture bytes, mode, and mtime, projection key set and order,
     sort order, and validation outcome; AC8 is decided from Git history,
     per-stage diffs, and three exact exit statuses. No criterion requires the
     builder to interpret a qualitative term. No blocking finding.
  2. Contract coverage — every material behavior in the table above has at
     least one covering criterion, and each criterion is implementable against
     the contract as written. I recomputed the Fixture A projection from the
     frozen rules (pending filter, string-comparison sort, compact
     `JSON.stringify`, fixed key order) and it is byte-identical to the frozen
     expected stdout line; Fixture B classifies valid with an empty selection,
     Fixture C fails `JSON.parse`, and S1-S11 each classify structurally
     invalid for exactly the rule the table names. No blocking finding.
  3. Consistency — the contract's state shape matches `app/server.js`
     (`{events, jobs, approvals, outbox}` persisted by `persist()`), the
     approval record shape (`id`, `jobId`, `action`, `status`, `proposedAt`,
     optional `reason`), the four-value status enum, and the outbox record
     `{...action, approvalId, sentAt}`; every linked `SPEC.md` and `AGENTS.md`
     anchor and relative path resolves. The only recorded ruling in
     `factory/rulings.md` concerns detail-route envelopes and `app/server.js`
     concurrent ownership and is not contradicted by this offline command.
     Every invalid fixture is an exact literal, not a runtime transformation,
     and I confirmed A, B, C, and S1-S11 are pairwise byte-distinct, so no
     invalid case is an identity transformation of an accepted valid fixture.
     No generated identifier, sequence, counter, or timestamp participates in
     an exact expectation; all IDs and timestamps are frozen literals with no
     initializer or reset to trace. The single serialized command
     (`node --test app/tests/pending-approvals.test.js`) has per-case
     temporary-directory setup and recursive teardown, shares no port, state
     file, or database, and this product has no parent/child rows or
     non-cascading foreign keys, so there is no cleanup-ordering obligation.
     Protected-boundary inspection: the Builder file is new, and no tracked
     test imports, snapshots, or asserts the source of `app/tools/*`; the
     declared seam `app/tests/health-approvals.test.js` asserts only `/health`
     HTTP responses and contains no import/export allowlist, exact-source
     assertion, or source snapshot. I independently re-ran the pinned
     `ticket-readiness.py` in this worktree: `--ticket T-175` returned
     `READINESS PASS`, and `--global-literal` returned `GLOBAL TEXT PASS` for
     the usage line and all three error literals, with no unknown or
     unparsable static check left outstanding. No contradiction found.
  4. Edge coverage — leakage of every named sensitive field (AC1, AC5),
     isolation (per-case temporary parent), data loss (byte, mode, and mtime
     equality in AC4-AC7 plus no sibling write), external effect (AC7: no
     Relay start, no listener, no approval executed), and irreversibility (the
     command is read-only) are each covered or explicitly out of scope.
     Authorization, cookies, CORS, and deploy topology are absent from this
     offline command and explicitly excluded. No blocking finding.
  5. Three or more positional arguments have no covering criterion. The
     contract does resolve the behavior — it accepts exactly one positional
     argument and checks arity first — so this is not a builder decision, but
     the frozen exit code and stderr line are only tested for zero and two.
     SPEC-WARN: extend AC3 with a three-argument case asserting the same exit `2` and usage line.
  6. AC7's lifecycle and offline sweep covers criteria 1, 2, 5, and 6 only.
     AC3 and AC4 independently assert that no state path is created or
     changed, and their exit-code assertions require a terminated child, so
     nothing material is unpinned.
     SPEC-WARN: name criteria 3 and 4 in AC7's no-listener and no-sibling-write sweep so every invocation path shares one offline assertion.
  7. Two pending approvals with an equal `id` would leave the emitted order
     unpinned. Relay generates approval IDs as `appr-<eventId>` and event
     intake is idempotent, so this state is unreachable from the product.
     SPEC-WARN: state the equal-`id` tie as out of scope, or add one tie fixture fixing the emitted order.
  8. The structural table omits root `null`, a numeric non-empty `id`, an
     absent `status`, and an array-valued approval element. Each is an
     equivalent permutation of a rule S1-S11 already exercises.
     SPEC-WARN: the test-author may add these as extra rows to the S-table loop at no contract cost.
  9. The contract fixes that relative paths resolve from the process working
     directory, but every acceptance case passes an absolute temporary path,
     which is the deliberate isolation choice.
     SPEC-WARN: optionally add one relative-path invocation run from the repository root against a fixture inside a temporary directory.
  10. `Fixture-Seams` lists `app/tests/health-approvals.test.js` while
     `Protected-Test-Conflicts` is `none` and Test-author ownership is
     `app/tests/pending-approvals.test.js` only. Readiness accepts this and
     the seam authorizes no edit; the file stays reference-only.
     SPEC-WARN: no action required — the test-author must not modify the declared seam.

SPEC-LINT: PASS

Kit-SHA: c1e258b04f5031cb00783ac016bd7343ceedd3b9

## Test-author log

- 2026-08-07: Authored failing acceptance tests in
  `app/tests/pending-approvals.test.js` (the only Test-author-owned path)
  against frozen contract version 1. The declared `Fixture-Seams` file
  `app/tests/health-approvals.test.js` was read for reference only and is
  unchanged; `Protected-Test-Conflicts` is `none`, so no protected-test repair
  was needed.

  Tests written, one per acceptance criterion:

  | Test | Criterion | Asserts |
  |---|---|---|
  | `AC1: Frozen Fixture A reports exactly the two pending approvals, id-sorted and projected` | AC1 | exit `0`, byte-exact stdout, zero stderr bytes, order `appr-alpha` then `appr-zulu`, exactly the keys `id`/`jobId`/`proposedAt` in that order, and absence of every sentinel token and non-pending approval ID from both streams |
  | `AC2: Frozen Fixture B reports an empty pending selection as exactly []` | AC2 | exit `0`, stdout exactly `[]\n`, zero stderr bytes |
  | `AC3: zero or two positional arguments exit 2 with the exact usage line and no state path change` | AC3 | exit `2`, zero stdout bytes, byte-exact usage line, and an unchanged temporary directory with neither named state path created |
  | `AC4: each frozen read-failure path exits 1 with the exact cannot-read line and leaves the path as it was` | AC4 | for the absent path and the directory path: exit `1`, zero stdout bytes, byte-exact `cannot read state file` line; the absent path stays absent and the directory stays a directory with the same entries |
  | `AC5: Frozen Fixture C exits 1 with the exact invalid-JSON line, unchanged bytes, and no parser or secret leakage` | AC5 | exit `1`, zero stdout bytes, byte-exact `state file is not valid JSON` line, unchanged bytes/mode/mtime, and no embedded secret in either stream |
  | `AC6: every frozen structural-invalid fixture exits 1 with the exact invalid-state line and unchanged bytes` | AC6 | pairwise byte-distinctness of A, B, C, and S1-S11 before invocation, then per fixture: exit `1`, zero stdout bytes, byte-exact `invalid Relay state` line, unchanged bytes/mode/mtime |
  | `AC7: every invocation self-terminates, repeats byte-identically, and changes no fixture or sibling path` | AC7 | across the criteria 1, 2, 5, and 6 fixtures: self-termination without a kill signal, unchanged bytes/mode/mtime, no sibling path written; plus a second Fixture A invocation producing byte-identical stdout and stderr |

  All fixtures are written as the frozen literals (Fixture A as its displayed
  pretty-printed bytes plus one LF; B, C, and S1-S11 as their displayed compact
  bytes plus one LF). No fixture is derived by casing, normalization, or
  mutation, and no generated identifier, counter, clock, or sequence
  participates in any expectation. Each case owns a fresh temporary directory
  removed in teardown; there are no dependent rows or cleanup ordering
  obligations.

  Verification: `node --test app/tests/pending-approvals.test.js` was run once
  before implementation. All seven tests fail because
  `app/tools/pending-approvals.js` does not exist yet (the child process emits
  a module-resolution error instead of the frozen streams), not from setup
  errors. The tests were additionally confirmed non-trivial by running them
  outside the repository against a throwaway conforming implementation, where
  all seven pass; that scratch copy was deleted and no implementation code was
  added here.

  Criteria not expressible as an automated test (demo-check items for the
  Narrator's bundle):

  1. **AC8** — commit ordering, per-stage diff scope, and the three frozen
     repository commands. This is decided from Git history and the Builder's
     later commit, both of which are outside a runtime test at test-author
     time. Check at bundle time that the test-author commit
     (`129c7f1`) precedes the implementation commit, that it changes only
     `app/tests/pending-approvals.test.js` and the Builder commit only
     `app/tools/pending-approvals.js`, that
     `node --test app/tests/pending-approvals.test.js`,
     `npm test --prefix app`, and `.github/scripts/test-immutability-check.sh`
     each exit `0`, and that `app/server.js`, `app/package.json`, lockfiles,
     existing tests, `factory/KIT_PIN`, and Factory controls are unchanged.
  2. **AC7 partial — "no invocation starts Relay or opens a network listener."**
     The test asserts the observable proxy: the child terminates on its own
     within a bounded timeout and is never killed by signal, which a live
     listener or leaked timer would prevent, and no sibling file is written.
     A direct assertion that no socket was bound is not observable from
     outside the process without a platform-specific probe, so confirm by
     inspection of `app/tools/pending-approvals.js` at bundle time that it
     neither imports `app/server.js` nor uses `node:http`/`node:net`.
Reviewer round 1 signed detail:

> ## Review: T-175 — offline pending-approvals report
>
> **Verdict: APPROVE**

reviewer round 1: APPROVE
