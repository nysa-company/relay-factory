# T-230 — count Relay outbox entries with one recipient and body offline

Kit-SHA: 7ae447baf7287e32a9df60c9453da6edec454e7b
State: Ready
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-230
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/outbox-detail.test.js
Authentication-Seams: none
Builder ownership: app/tools/outbox-recipient-body-count.js only
Test-author ownership: app/tests/outbox-recipient-body-count.test.js only

## Description

Add a zero-dependency, read-only Node command that counts sandbox outbox entries
whose recipient and body exactly match two supplied strings while offline.

## Acceptance criteria

1. `node app/tools/outbox-recipient-body-count.js <state-file> <recipient> <body>`
   emits compact JSON with exactly one `outboxWithRecipientAndBody` integer.
2. Both strings match exactly without trimming, normalization, or coercion; no
   match emits zero plus one newline.
3. Missing, unreadable, malformed, or structurally invalid state returns fixed
   redacted errors; complete outbox validation precedes counting.
4. Every invocation is offline, read-only, deterministic, and self-terminating.
5. Tests precede implementation; targeted, full app, and immutability checks
   pass without dependency, server, schema, manifest, or Factory changes.

## Coordination

This ticket owns only its two new paths and is independent of T-228 and T-229.
