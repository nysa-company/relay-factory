# T-250 — count Relay approvals by recipient and subject offline

Kit-SHA: cd1a6798a5d0968511038f7461016cc77d4a8a32
State: Blocked-Escalated
Resume-State: Planning
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-250
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/approval-recipient-count.test.js, app/tests/approval-detail.test.js
Authentication-Seams: none
Builder ownership: app/tools/approval-recipient-subject-count.js only
Test-author ownership: app/tests/approval-recipient-subject-count.test.js only

## Description

Add a zero-dependency, read-only Node command that counts persisted Relay
approvals whose action recipient and subject exactly match supplied strings.
This supports narrow offline approval checks without starting Relay or exposing
approval bodies.

## Acceptance criteria

1. `node app/tools/approval-recipient-subject-count.js <state-file> <recipient> <subject>`
   emits compact JSON containing exactly one `approvalsWithRecipientAndSubject`
   integer.
2. Recipient and subject must be non-empty strings and match stored
   `action.to` and `action.subject` values exactly, without trimming or
   coercion.
3. Invalid arguments are rejected before file access. Unreadable, malformed,
   or structurally invalid state returns fixed redacted errors; complete
   approval validation precedes counting.
4. Every invocation is offline, read-only, deterministic, and self-terminating.
5. Tests precede implementation. Targeted, full app, and immutability checks
   pass without dependency, server, schema, manifest, or Factory changes.

## Coordination

This ticket owns only its two new paths and is independent of T-249 and T-251.

## Planning log

- 2026-08-25: Planner stopped before writing a spec'd description or frozen
  contract. Local `factory/tickets/T-226.md` sits in `State: Backlog` with no
  `Kit-SHA`, but its leased remote branch `origin/ticket/T-226`
  (`5c2cf078041bda5e9fcc6af618b069ace8c19f81`) carries `State: Planning`,
  `Kit-SHA: 7ae447baf7287e32a9df60c9453da6edec454e7b`, and an already-frozen
  "Frozen contract — version 1" (`Freeze result: PASS`) for the identical
  Builder ownership `app/tools/approval-recipient-subject-count.js` and
  identical Test-author ownership
  `app/tests/approval-recipient-subject-count.test.js` declared on this
  ticket's own header — the same conjunction-count command interface
  (`node app/tools/approval-recipient-subject-count.js <state-file>
  <recipient> <subject>` emitting `approvalsWithRecipientAndSubject`) that
  T-250 was dispatched to plan. Freezing T-250's contract for these two paths
  risks a direct collision with T-226's leased branch at merge time, or
  duplicated Builder work if both proceed under `Merge-Policy: auto`; the
  product docs and `factory/rulings.md` do not resolve which ticket is
  canonical, and closing or reassigning either ticket is an operator-owned
  state transition this role does not hold. Exact operator question: **Is
  T-226's leased branch a stale artifact from a prior kit generation that
  should be discarded so T-250 proceeds to freeze its own contract for
  `app/tools/approval-recipient-subject-count.js` and
  `app/tests/approval-recipient-subject-count.test.js`, or should T-250 be
  canceled as a duplicate and T-226 resumed from its existing frozen
  contract, or should T-250 be assigned distinct Builder and Test-author
  paths with a separately specified command interface?**

ROLE-ESCALATE: CONTRACT-BLOCKED

OPERATOR ANSWER: T-250 is the canonical ticket for this qualification; T-226 remains an older parked ticket and must not proceed concurrently, so freeze T-250's contract on its existing declared paths without changing T-226.
OPERATOR ANSWER RECEIPT: 0315ae08732aa7e04ca435838d6b8228e40c972d6248a185347e0044a0b2fe71
