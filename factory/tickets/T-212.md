# T-212 — count Relay outbox entries for one approval offline

State: Backlog
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-212
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/outbox-detail.test.js
Authentication-Seams: none
Builder ownership: app/tools/outbox-approval-count.js only
Test-author ownership: app/tests/outbox-approval-count.test.js only

## Description

Add a zero-dependency, read-only Node command that counts persisted Relay
outbox entries whose stored `approvalId` exactly matches one operator-supplied
approval ID while the service is offline.

## Acceptance criteria

1. `node app/tools/outbox-approval-count.js <state-file> <approval-id>` exits
   zero and emits deterministic compact JSON with exactly one `outboxEntries`
   nonnegative integer.
2. A valid state with no exact approval ID match returns
   `{"outboxEntries":0}` followed by one newline.
3. Missing arguments and missing, unreadable, malformed, or structurally
   invalid state are rejected with fixed redacted errors that reveal no path,
   approval ID, recipient, subject, body, or stored content; complete outbox
   validation precedes counting.
4. Every invocation is offline, read-only, and self-terminating; input bytes,
   mode, timestamps, and sibling directory entries remain unchanged.
5. Complete acceptance tests are committed before implementation. Targeted,
   full app, and immutability checks pass without dependency, server, schema,
   package-manifest, lockfile, or Factory-control changes.

## Coordination

This ticket owns only its two new `app/` paths and is independent of T-210 and
T-211.
