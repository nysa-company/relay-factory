# T-174 — report dead Relay jobs offline

State: Planning
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-174
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/retry-dead.test.js
Authentication-Seams: none
Builder ownership: app/tools/dead-jobs.js only
Test-author ownership: app/tests/dead-jobs.test.js only

## Spec'd description

Add a zero-dependency Node command, `app/tools/dead-jobs.js`, that reads one
persisted Relay state file without starting the service and writes a stable,
minimal JSON report of dead jobs. This gives an operator an offline
dead-letter inventory while withholding `lastError`, retry-window state,
events, approvals, and outbox contents. The command is read-only and does not
change retry behavior, the persisted schema, the server, or the UI.

Product-doc sources:

- [`SPEC.md` behavior 2](../../SPEC.md#behavior-spec) defines a permanently
  failing job as dead after three attempts and requires dead-letter work to be
  visible rather than silently dropped.
- [`SPEC.md` behavior 6](../../SPEC.md#behavior-spec) defines the on-disk state
  as Relay's durable source of truth; this command reads that artifact without
  participating in service startup or recovery.
- [`SPEC.md` conformance cases 3-4](../../SPEC.md#test-cases-the-conformance-suite)
  establish recorded attempts and the dead status used by the report.
- [`AGENTS.md` Boundaries](../../AGENTS.md#boundaries) requires the Relay
  product to remain zero-dependency and keeps Factory controls outside
  application work. [`AGENTS.md` Verification](../../AGENTS.md#verification)
  defines the repository verification commands used after implementation.

## Acceptance criteria

1. Given Frozen Fixture A, one invocation of
   `node app/tools/dead-jobs.js <fixture-a-path>` exits `0`, writes nothing to
   stderr, and writes exactly
   `[{"id":"job-alpha","eventId":"alpha","attempts":3,"retries":0},{"id":"job-zeta","eventId":"zeta","attempts":6,"retries":1}]\n`
   to stdout. The bytes of Fixture A are identical before and after the
   invocation.
2. Given Frozen Fixture B, one invocation of
   `node app/tools/dead-jobs.js <fixture-b-path>` exits `0`, writes nothing to
   stderr, writes exactly `[]\n` to stdout, and leaves Fixture B byte-identical.
3. Given each frozen invalid-input case in turn (nonexistent path, directory
   path, malformed JSON bytes, and structurally invalid JSON bytes), the
   command exits exactly `1`, writes nothing to stdout, and writes exactly
   `dead-jobs: invalid Relay state\n` to stderr. Existing input artifacts and
   their parent-directory entries are identical before and after each
   invocation.
4. Invoking the command with zero state-file arguments or with two state-file
   arguments exits exactly `1`, writes nothing to stdout, and writes exactly
   `Usage: node app/tools/dead-jobs.js <state-file>\n` to stderr.
5. Git history contains the complete acceptance-test commit before the
   implementation commit. The test-author commit changes only
   `app/tests/dead-jobs.test.js`; the Builder commit changes only
   `app/tools/dead-jobs.js`; neither stage changes existing tests,
   `app/server.js`, `app/package.json`, a dependency/lock file, the persisted
   state schema, retry behavior, `factory/KIT_PIN`, or Factory controls.
6. After implementation, each command under File locations and verification
   exits `0`.

## Frozen contract — version 1

- **Freeze result:** PASS. Contract version 1 is frozen.

This is the exact interface for the test-author and Builder. If it proves
wrong, return T-174 to Ready and append frozen contract version 2 without
editing or removing this version.

### Command interface

- Invocation: `node app/tools/dead-jobs.js <state-file>`.
- The invocation accepts exactly one user-supplied argument. Zero or more than
  one user-supplied argument is a usage failure: exit `1`, empty stdout, and
  exactly `Usage: node app/tools/dead-jobs.js <state-file>\n` on stderr.
- The command reads `<state-file>` directly and does not import, require, or
  start `app/server.js`. It performs no writes, renames, deletes, permission
  changes, or directory creation.
- A valid invocation exits `0`, has empty stderr, and writes exactly one
  compact JSON array followed by one LF (`\n`) to stdout.
- Include only jobs whose `status` is exactly the lowercase string `dead`.
  Each output element has exactly four keys in this order: `id`, `eventId`,
  `attempts`, `retries`. Values are copied unchanged from the stored job.
- Output elements are ascending by `id` using JavaScript's default string
  ordering, which compares UTF-16 code units. Input array order is not
  preserved.
- A valid state containing no dead jobs writes exactly `[]\n`.
- JSON whitespace and object-key order in the input file are not significant.
  Output bytes, including output object-key order and the final LF, are
  significant.

For this command, a structurally valid state is a parsed, non-null, non-array
JSON object with exactly four top-level properties: `events`, `jobs`,
`approvals`, and `outbox`, each an array. The entries in `events`, `approvals`,
and `outbox` are not validated because this report does not consume them.
Every element of `jobs` must be a non-null, non-array object with:

- non-empty string `id` and `eventId`;
- `status` exactly one of `pending`, `done`, or `dead`;
- integer `attempts`, `retries`, and `attemptsSinceRetry`, each greater than
  or equal to zero; and
- `lastError` equal to `null` or a string.

Any read failure (including a nonexistent path or a path that is a directory),
JSON parse failure, or failure of the structure above is an invalid-state
failure: exit `1`, empty stdout, and exactly
`dead-jobs: invalid Relay state\n` on stderr. Error details from the operating
system or JSON parser are not exposed.

### Frozen Fixture A — sorted projection

The acceptance test writes this exact complete JSON value, including its final
LF, to a regular file in a fresh temporary directory:

```json
{
  "events": [
    {
      "id": "zeta",
      "type": "meeting",
      "payload": {
        "failTimes": 99
      },
      "receivedAt": "2026-08-06T20:00:00.000Z"
    },
    {
      "id": "middle",
      "type": "generic",
      "payload": {},
      "receivedAt": "2026-08-06T20:00:01.000Z"
    },
    {
      "id": "alpha",
      "type": "email",
      "payload": {
        "failTimes": 3
      },
      "receivedAt": "2026-08-06T20:00:02.000Z"
    }
  ],
  "jobs": [
    {
      "id": "job-zeta",
      "eventId": "zeta",
      "status": "dead",
      "attempts": 6,
      "lastError": "simulated failure on attempt 6",
      "retries": 1,
      "attemptsSinceRetry": 3
    },
    {
      "id": "job-middle",
      "eventId": "middle",
      "status": "done",
      "attempts": 1,
      "lastError": null,
      "retries": 0,
      "attemptsSinceRetry": 1
    },
    {
      "id": "job-alpha",
      "eventId": "alpha",
      "status": "dead",
      "attempts": 3,
      "lastError": "simulated failure on attempt 3",
      "retries": 0,
      "attemptsSinceRetry": 3
    }
  ],
  "approvals": [
    {
      "id": "appr-middle",
      "jobId": "job-middle",
      "action": {
        "to": "test@example.com",
        "subject": "Middle action",
        "body": "Not part of the dead-job report."
      },
      "status": "sent",
      "proposedAt": "2026-08-06T20:00:03.000Z"
    }
  ],
  "outbox": [
    {
      "to": "test@example.com",
      "subject": "Middle action",
      "body": "Not part of the dead-job report.",
      "approvalId": "appr-middle",
      "sentAt": "2026-08-06T20:00:04.000Z"
    }
  ]
}
```

The exact stdout is:

```text
[{"id":"job-alpha","eventId":"alpha","attempts":3,"retries":0},{"id":"job-zeta","eventId":"zeta","attempts":6,"retries":1}]
```

The final LF shown by the command contract follows the closing bracket.
`job-middle` proves non-dead jobs are excluded; the reversed dead-job input
order proves sorting; `lastError`, `status`, `attemptsSinceRetry`, the events,
the approval, and the outbox receipt prove the four-field projection.

### Frozen Fixture B — no dead jobs

The acceptance test writes this exact complete JSON value, including its final
LF, to another regular file:

```json
{
  "events": [],
  "jobs": [],
  "approvals": [],
  "outbox": []
}
```

Its exact stdout is `[]\n`.

### Frozen invalid-input cases

Each case uses a fresh temporary parent directory:

1. Nonexistent path: `<temporary-parent>/missing-state.json` does not exist.
2. Directory path: `<temporary-parent>/state-directory` exists as an empty
   directory and is supplied where the state-file path is expected.
3. Malformed JSON: a regular file contains exactly the bytes
   `{"events":[]` with no final LF.
4. Structurally invalid JSON: a regular file contains exactly
   `{"events":[],"jobs":{},"approvals":[],"outbox":[]}\n`.

The malformed and structurally invalid byte strings are frozen independently,
are byte-distinct from one another and from both valid fixtures, and are not
created by a casing, normalization, or mutation transform. No generated
identifier, sequence, counter, or timestamp is asserted; all asserted record
values are literal fixture data.

### File locations and verification

- Builder implementation and ownership: `app/tools/dead-jobs.js` only, using
  Node's installed standard library. Its only module specifier may be
  `node:fs`; `process` and `JSON` are runtime globals.
- Test-author implementation and ownership:
  `app/tests/dead-jobs.test.js` only, using `node:test`, `node:assert`,
  `node:child_process`, `node:fs`, `node:path`, and `node:os`.
- `app/tests/retry-dead.test.js` is the protected reference for the persisted
  dead-job shape. It is not edited.
- UI selectors: none. This ticket has no HTTP endpoint, UI, cross-origin,
  authentication, cookie, CORS, preview-deploy, or external-send behavior.
- Targeted acceptance command:
  `node --test app/tests/dead-jobs.test.js`.
- Full regression command: `npm test --prefix app`.
- Test-immutability command:
  `.github/scripts/test-immutability-check.sh`.
- Every acceptance test creates a unique temporary directory, completes its
  child command before assertions and cleanup, and removes that directory in
  teardown. Tests do not use a server, port, database, parent row, or dependent
  table. The same lifecycle is closed when the targeted command runs alone and
  when the full regression command runs test files concurrently.

### Protected-source and protected-text checks

The production file is new and is executed as a child command rather than
imported by an existing test. Inspection of
`app/tests/retry-dead.test.js`, the full tracked `app/tests/` set,
`app/package.json`, and `.github/scripts/test-immutability-check.sh` found no
import/export allowlist, exact-source assertion, source snapshot, or module
specifier restriction that rejects `app/tools/dead-jobs.js` or `node:fs`.
The package's existing `tests/*.test.js` command admits the new acceptance
test without a package-manifest edit. Therefore `Protected-Test-Conflicts:
none` remains exact and no protected test enters Test-author ownership.

The pinned Factory readiness check returned `GLOBAL TEXT PASS` separately for
all four frozen shell-visible literals: the Fixture A compact JSON line, `[]`,
`dead-jobs: invalid Relay state`, and
`Usage: node app/tools/dead-jobs.js <state-file>`. There are no accessible
names or global UI literals.

## Criteria checklist

- **Pass/fail:** AC1-4 assert exact exit codes and stdout/stderr bytes, and
  AC1-3 assert exact filesystem preservation. AC5 is decided from commit
  order and path diffs. AC6 is decided from three process exit statuses.
- **Unambiguous:** invocation arity, valid-state structure, status filter,
  UTF-16 sort order, projection and key order, final LF, exact error outputs,
  fixture bytes, file paths, and allowed stage diffs are frozen.
- **Coverage:** AC1 exercises every projected field, filtering, sorting,
  deterministic serialization, and valid-input read-only behavior. AC2
  exercises the empty result. AC3 exercises all four invalid fixtures and
  invalid-input immutability. AC4 exercises both arity failures. AC5 exercises
  all ownership and no-change boundaries. AC6 exercises every verification
  command.
- **No dead contract elements:** every command path, output/error shape,
  fixture, lifecycle rule, file location, and verification boundary appears
  in AC1-6. UI and deploy/auth topology are explicitly absent.
- **Derived fixtures and generated values:** no invalid fixture uses a derived
  transformation, all serialized fixtures are byte-distinct, and no generated
  value is asserted.
- **Fixture lifecycle:** all temporary artifacts are unique per test and are
  removed only after the child exits; there are no relational dependents or
  shared resources requiring ordered cleanup.

## Ambiguity log

1. **What is a dead job and which counters are reportable?** Resolved from
   [`SPEC.md` behavior 2](../../SPEC.md#behavior-spec), conformance cases 3-4,
   and the current persisted job shape in `app/server.js`: filter exact
   `status: "dead"` and project cumulative `attempts` plus `retries`.
   `attemptsSinceRetry` and `lastError` remain deliberately excluded by the
   reconciled ticket's exact four-field report.
2. **What makes output deterministic?** The seed ticket required job-ID
   sorting but did not define comparator, whitespace, key order, or line
   ending. Resolved as default JavaScript UTF-16 string order and one exact
   compact JSON line with ordered keys and final LF; Frozen Fixture A makes a
   missing sort or extra field observable.
3. **What is “structurally invalid”?** The product spec defines persisted
   collections but not an offline validator. Resolved narrowly from the
   current `app/server.js` state and job records: require exactly the four
   collection arrays and validate every field of every job, while leaving
   unconsumed collection entries alone. This does not change or claim a new
   persisted schema.
4. **How are failures exposed?** The reconciled ticket requires concise,
   deterministic errors but does not require OS or parser details. Resolved as
   one exact invalid-state error for all read/parse/structure failures and a
   separate exact usage error for argument-count failures, with empty stdout
   and exit `1`.
5. **Can offline inspection mutate or start Relay?** Resolved from
   [`SPEC.md` behavior 6](../../SPEC.md#behavior-spec) and the ticket's
   “without starting the service” requirement: direct read-only file access,
   byte-preservation checks, no server import, and no process, port, or
   `DATA_DIR` topology.
6. **Do sibling or leased tickets overlap file ownership?** A grep of all
   `factory/tickets/*.md` found the stranded pre-provider T-171 declaration
   for the same two paths. T-174 explicitly replaces T-171. The other active
   leased branches `origin/ticket/T-173` and `origin/ticket/T-175` were
   inspected with `git show`; they own
   `app/tools/state-summary.js`/`app/tests/state-summary.test.js` and
   `app/tools/pending-approvals.js`/`app/tests/pending-approvals.test.js`,
   respectively. No concurrently active ownership conflict exists.
7. **Do protected tests constrain the new source or visible text?** Inspection
   found no static source boundary affecting this process-invoked new file.
   All four shell-visible literals passed the pinned Factory global-text
   collision check. No conflict declaration or protected-test edit is needed.
8. **Are deployment, authentication, external-send labeling, or fixture
   dependency cleanup applicable?** No. This is a local file-to-stdout
   command, has no network or database topology, and cannot trigger a send.
   `External: no`, `Authentication-Seams: none`, and no child-table cleanup
   remain correct.

## Coordination

This ticket replaces the stranded pre-provider T-171 identity. It has no
dependency on T-173 or T-175 and owns no file they own, so all three may run
concurrently.

## Planning log

- 2026-08-06: Planner froze contract version 1 with exact command arity,
  valid-state structure, byte-level success and failure outputs, literal
  fixtures, read-only checks, lifecycle closure, role ownership, and
  verification commands. Sibling and active leased branches were inspected;
  protected source boundaries and global text were checked; no operator ruling
  or escalation was required.
- 2026-08-06: Spec lint (v11) — contract-element coverage:

  | Material behavior | Positive | Failure/empty | Duplicate | Out-of-scope disposition |
  |---|---|---|---|---|
  | Arity: exactly one user-supplied argument | AC1, AC2, AC3 (single path) | AC4 (zero and two arguments; three-or-more is an equivalent permutation grouped under AC4) | — | — |
  | Success envelope: exit `0`, empty stderr, one compact JSON array plus one LF | AC1, AC2 | — | — | — |
  | Dead filter: include only exact lowercase `status: "dead"` | AC1 (`job-middle` is `done` and excluded) | AC2 (no dead jobs) | — | Non-enum `status` values are rejected by structural validation, not filtered — see finding 1 |
  | Four-key projection `id`, `eventId`, `attempts`, `retries` in order, values copied unchanged | AC1 (`lastError`, `status`, `attemptsSinceRetry` withheld) | AC2 (empty array) | — | — |
  | Ascending `id` ordering by UTF-16 code units; input order not preserved | AC1 (reversed dead-job input order) | AC2 | Unreachable: `SPEC.md` behavior 1 makes event IDs, and therefore `job-<id>`, unique | Comparator divergence unobservable in Fixture A — finding 3 |
  | Empty result writes exactly `[]\n` | — | AC2 | — | — |
  | Input JSON whitespace and key order insignificant | AC1, AC2 (pretty-printed multi-line fixtures) | AC3 case 4 (single-line input) | — | — |
  | Read-only: no writes, renames, deletes, permission changes, directory creation; no `app/server.js` import or service start | AC1, AC2 (fixture bytes identical after invocation) | AC3 (existing artifacts and parent-directory entries identical) | — | AC4's arity-failure paths not byte-asserted — finding 4 |
  | Structural validity: top-level is a non-null, non-array object with exactly four properties, each an array | AC1, AC2 | AC3 case 4 covers only wrong element type for `jobs`; exact-arity of the property set is unexercised — finding 2 | — | — |
  | Structural validity: every `jobs` element's `id`, `eventId`, `status`, `attempts`, `retries`, `attemptsSinceRetry`, `lastError` | AC1 (valid elements) | **Uncovered** — finding 1 | — | — |
  | `events`, `approvals`, `outbox` entries deliberately unvalidated | AC1 (populated, unconsumed entries pass) | AC2 (empty) | — | Recorded in contract and ambiguity log 3 |
  | Invalid-state envelope: exit `1`, empty stdout, exactly `dead-jobs: invalid Relay state\n`, no OS or parser detail leaked | — | AC3 (all four frozen cases) | — | — |
  | Read failure: nonexistent path, directory path | — | AC3 cases 1-2 (permission-denied is an equivalent read-failure permutation) | — | — |
  | JSON parse failure | — | AC3 case 3 | — | — |
  | Usage envelope: exit `1`, empty stdout, exactly `Usage: node app/tools/dead-jobs.js <state-file>\n` | — | AC4 | — | — |
  | Role ownership, test-first commit order, no change to existing tests, `app/server.js`, `app/package.json`, dependencies, persisted schema, retry behavior, or Factory controls | AC5 | AC5 | — | — |
  | Verification commands (targeted, full regression, test-immutability) | AC6 | AC6 | — | — |
  | HTTP, UI, cross-origin, authentication, cookies, CORS, preview deploy, external send | — | — | — | Explicitly absent in the frozen contract; `External: no`, `Authentication-Seams: none` |
  | Fixture lifecycle: unique temporary directory per test, child completes before assertions and cleanup, teardown removal, closed under concurrent file execution | AC1-AC4 | AC3 | — | No server, port, database, parent row, or dependent table — no ordered child-first cleanup applies |

  Findings this pass:

  1. Per-job structural validation is a whole frozen contract clause with no
     criterion. The contract requires every `jobs` element to be a non-null,
     non-array object with non-empty string `id` and `eventId`, `status`
     exactly one of `pending`/`done`/`dead`, integer `attempts`, `retries`,
     and `attemptsSinceRetry` each `>= 0`, and `lastError` `null` or a string.
     The four frozen invalid cases are a nonexistent path, a directory, a
     parse failure, and `jobs` supplied as `{}` — none contains a `jobs`
     element at all, so no criterion ever reaches element validation. A
     builder that parses the file, confirms the four top-level values are
     arrays, and filters `status === "dead"` satisfies AC1-AC6 completely
     while implementing none of this clause, and would then emit a report
     from damaged state rather than the frozen error — precisely the failure
     the validation exists to prevent for an offline recovery tool. This is
     not an equivalent permutation of case 4: that case exercises top-level
     value typing, a different branch. Add one frozen invalid fixture whose
     `jobs` array holds a single element violating one field (for example
     `status` as `"DEAD"`), asserted under AC3's existing envelope; an
     exhaustive per-field mutation list is not required.
  2. The contract's "exactly four top-level properties" is unexercised, so
     exact-arity versus presence-only checking is unobservable. Fixtures A
     and B both carry exactly the four properties and case 4 varies a value
     type, not the property set. Two builders — one testing
     `Object.keys(state).length === 4`, one testing only that the four named
     properties are arrays — both pass every criterion yet disagree on a
     state file carrying a fifth property, which is an operator-visible
     difference for a tool whose input is Relay's evolving durable artifact.
     Add one frozen invalid fixture with an extra top-level property, or an
     added property and a missing one, under AC3's existing envelope.
  3. SPEC-WARN: Fixture A's `job-alpha`/`job-zeta` pair cannot distinguish the
     frozen UTF-16 code-unit comparator from `localeCompare` or a default
     `Array.prototype.sort` on already-lowercase ASCII, so AC1 proves only
     that sorting happens, not that it is the frozen ordering. The comparator
     itself is frozen, so this is not a builder decision. The test-author may
     add one dead job whose `id` mixes case (for example `job-Zulu` against
     `job-alpha`, where code-unit order places the uppercase ID first and
     `localeCompare` does not) within AC1's existing grouping.
  4. SPEC-WARN: AC4 asserts exit code, stdout, and stderr for the arity
     failures but, unlike AC3, does not assert that supplied paths and their
     parent-directory entries are unchanged. The two-argument case does pass
     real paths, and the contract's no-write guarantee is absolute. The
     test-author may extend AC4's two-argument case with the same
     byte-preservation assertion AC3 already uses.
  5. Criteria quality holds otherwise: AC1-AC4 assert exact exit codes and
     exact stdout/stderr byte strings, AC1-AC3 assert exact filesystem
     preservation, AC5 is decided from commit order and path diffs, and AC6
     from three process exit statuses. No subjective or unquantified term
     ("nicely", "correctly", "appropriate", "fast") appears in any criterion.
  6. Consistency holds. Fixture A's job records match the shape
     `app/server.js` persists (`id`, `eventId`, `status`, `attempts`,
     `lastError`, `retries`, `attemptsSinceRetry`) and reproduce values the
     protected `app/tests/retry-dead.test.js` already asserts exactly —
     `attempts: 3`/`retries: 0`/`attemptsSinceRetry: 3` with
     `"simulated failure on attempt 3"` for the first death, and
     `attempts: 6`/`retries: 1`/`attemptsSinceRetry: 3` with
     `"simulated failure on attempt 6"` after one retry — consistent with
     `SPEC.md` behavior 2 and conformance cases 3-4. Paths, module
     specifiers, and the three verification commands match the repository:
     `app/package.json` runs `node --test tests/*.test.js`, which admits the
     new acceptance file with no manifest edit. The only entry in
     `factory/rulings.md` concerns detail-route envelopes and concurrent
     `app/server.js` ownership; it does not touch this ticket.
  7. Derived-fixture and generated-value checks pass. The malformed bytes
     `{"events":[]` and the structurally invalid single line
     `{"events":[],"jobs":{},"approvals":[],"outbox":[]}` are each
     byte-distinct from one another and from the multi-line Fixtures A and B,
     and neither is produced by a casing, normalization, or mutation
     transform of an accepted valid fixture. No generated identifier,
     sequence, counter, or timestamp is asserted; every asserted value is
     literal fixture data written by the test, so no frozen initializer or
     reset scope is implicated.
  8. Protected source and text boundaries independently verified, not taken
     from the planner's summary. Direct inspection of all nine tracked files
     in `app/tests/`, `app/package.json`, and
     `.github/scripts/test-immutability-check.sh` found no import/export
     allowlist, exact-source assertion, source snapshot, or module-specifier
     restriction that could reject `app/tools/dead-jobs.js` or `node:fs`;
     `app/tests/backend-readiness.test.js` reads only
     `factory/backend-readiness.sh` and its own temporary logs. No test makes
     a page-wide assertion against the `GET /` global shell, and this ticket
     introduces no global-shell literal — all four frozen literals are child
     stdout/stderr never rendered by the UI. `Protected-Test-Conflicts: none`
     is therefore exact, no protected test enters Test-author ownership, and
     nothing here fails closed. Recorded for traceability: the pinned kit
     release tree is not present in this worktree, so the planner's
     `ticket-readiness.py` `GLOBAL TEXT PASS` transcript could not be
     re-executed; the finding above rests on independent inspection instead.
  9. Edge coverage is otherwise complete. Data loss is bounded by the
     read-only guarantee asserted in AC1-AC3; there is no external effect,
     network call, credential, trust boundary, or irreversible action; and
     isolation is closed by unique temporary directories per test under both
     the targeted and the concurrent full-regression command. Baseline
     verification is green in this worktree (44 tests pass, immutability
     holds), so AC6 is satisfiable.

  SPEC-LINT: FAIL — the frozen structural-validation clause has no criterion, so a builder can skip all per-job and property-arity validation and still pass every acceptance criterion.

Kit-SHA: 831fe510ad9ee623db8158760b1539f812b9b69f
