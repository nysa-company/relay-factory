# T-213 — count Relay events at one timestamp offline

State: Backlog
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-213
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/event-timestamp.test.js
Authentication-Seams: none
Builder ownership: app/tools/event-timestamp-count.js only
Test-author ownership: app/tests/event-timestamp-count.test.js only

## Description

Add a zero-dependency, read-only Node command that counts persisted Relay
events whose stored `receivedAt` value exactly matches one operator-supplied
timestamp while the service is offline.

## Acceptance criteria

1. `node app/tools/event-timestamp-count.js <state-file> <timestamp>` exits
   zero and emits deterministic compact JSON with exactly one
   `eventsAtTimestamp` nonnegative integer.
2. Matching compares stored and supplied strings exactly, without parsing,
   normalization, coercion, or timezone conversion. No exact match returns
   `{"eventsAtTimestamp":0}` followed by one newline.
3. Missing arguments and missing, unreadable, malformed, or structurally
   invalid state are rejected with fixed redacted errors that reveal no path,
   timestamp, event ID, payload, job error, approval, or stored content;
   complete event validation precedes counting.
4. Every invocation is offline, read-only, and self-terminating; input bytes,
   mode, timestamps, and sibling directory entries remain unchanged.
5. Complete acceptance tests are committed before implementation. Targeted,
   full app, and immutability checks pass without dependency, server, schema,
   package-manifest, lockfile, or Factory-control changes.

## Coordination

This ticket owns only its two new `app/` paths and is independent of T-214 and
T-215.
