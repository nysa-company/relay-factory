# T-187 — inspect one Relay outbox receipt offline

Kit-SHA: e85890e4e8ae98038b1eaaee5b6bb239bc874052
State: Planning
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-187
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/outbox-detail.test.js
Authentication-Seams: none
Builder ownership: app/tools/outbox-receipt.js only
Test-author ownership: app/tests/outbox-receipt.test.js only

## Spec'd description

Add `app/tools/outbox-receipt.js`, a zero-dependency Node 18+ command that reads
one persisted Relay state file while the service is offline and selects one
sandbox outbox receipt by exact approval ID. It emits only the stored
`approvalId` and `sentAt`. This gives an operator a deterministic receipt
lookup without starting Relay, changing state, or exposing the recipient,
subject, body, event payloads, job errors, approval content, or unrelated
state.

Product-doc sources:

- [`SPEC.md` behavior 4](../../SPEC.md#behavior-spec) defines an approved
  allowlisted send as a sandbox outbox record.
- [`SPEC.md` behaviors 6-7](../../SPEC.md#behavior-spec) define persisted
  on-disk state and the outbox collection this command reads offline.
- [`SPEC.md` conformance cases 5-7](../../SPEC.md#test-cases-the-conformance-suite)
  require sends to appear only after approval, blocked sends not to appear,
  and persisted work not to duplicate across restart.
- [`SPEC.md` known modeling limitation](../../SPEC.md#known-modeling-limitation)
  establishes that Relay's receipt is the persisted in-process outbox record,
  not evidence of real delivery.
- The current [`app/server.js` durable-state model](../../app/server.js)
  establishes the stored receipt fields and approval-ID link.
- [`AGENTS.md` Boundaries](../../AGENTS.md#boundaries) keeps the app
  zero-dependency, and [`AGENTS.md` Verification](../../AGENTS.md#verification)
  defines the later full-suite and immutability checks.

The command is a read-only projection over the existing state file. It does
not start the service, approve or reject an action, send a message, write an
outbox record, or change the server, persisted schema, worker, package
manifest, dependencies, UI, or Factory controls.

## Acceptance criteria

1. Given Frozen Fixture A and approval ID `appr-receipt-alpha`, each of two
   invocations of
   `node app/tools/outbox-receipt.js <fixture-a-path> appr-receipt-alpha`
   completes within `5_000` ms, has `error === undefined` and
   `signal === null`, exits `0`, writes exactly
   `{"approvalId":"appr-receipt-alpha","sentAt":"2026-08-23T12:00:01.000Z"}\n`
   to stdout, and writes zero stderr bytes. Every frozen sensitive token and
   the unselected receipt ID `appr-receipt-zulu` is absent from both streams.
   Fixture A's exact bytes, BigInt numeric `mode`, `size`, `mtimeNs`, and
   sorted parent-directory entries are unchanged after each invocation.
2. Each frozen lookup miss—`appr-missing` and case-distinct
   `Appr-receipt-alpha` against Fixture A, and `appr-missing` against Frozen
   Fixture B—completes within `5_000` ms, has `error === undefined` and
   `signal === null`, exits `1`, writes zero stdout bytes, and writes exactly
   `outbox-receipt: outbox receipt not found\n` to stderr. Each existing
   fixture and its parent directory remain unchanged by every observation
   named in criterion 1.
3. Each frozen arity case—zero, one, and three positional arguments—completes
   within `5_000` ms, has `error === undefined` and `signal === null`, exits
   `2`, writes zero stdout bytes, and writes exactly
   `usage: node app/tools/outbox-receipt.js <state-file> <approval-id>\n` to
   stderr. The one-argument case supplies absent
   `<tmp>/candidate-state.json`; the three-argument case supplies absent
   `<tmp>/state-a.json`, `appr-a`, and `extra-argument`. Every candidate path
   remains absent and every case leaves its fresh temporary parent empty.
4. For both frozen read failures, absent `<tmp>/missing-state.json` and empty
   directory `<tmp>/state-directory`, invocation with approval ID
   `appr-receipt-alpha` completes within `5_000` ms, has
   `error === undefined` and `signal === null`, exits `1`, writes zero stdout
   bytes, and writes exactly `outbox-receipt: cannot read state file\n` to
   stderr. The absent path remains absent; the directory's type, BigInt
   numeric `mode`, `size`, `mtimeNs`, and sorted entries remain identical;
   neither parent gains or loses an entry.
5. Given Frozen Fixture C and approval ID `appr-receipt-alpha`, the command
   completes within `5_000` ms, has `error === undefined` and
   `signal === null`, exits `1`, writes zero stdout bytes, writes exactly
   `outbox-receipt: state file is not valid JSON\n` to stderr, does not emit
   `MALFORMED-STATE-MUST-NOT-LEAK`, and leaves the fixture and parent
   directory unchanged by every observation named in criterion 1.
6. For every S1-S19 frozen structural-invalid fixture, the command completes
   within `5_000` ms, has `error === undefined` and `signal === null`, exits
   `1`, writes zero stdout bytes, writes exactly
   `outbox-receipt: invalid Relay state\n` to stderr, emits no frozen sensitive
   token, and leaves that fixture and its parent directory unchanged by every
   observation named in criterion 1. Each invocation uses
   `appr-receipt-alpha` except S19, which uses `appr-duplicate` to prove
   complete structural validation precedes lookup. Before invocation, the
   acceptance test proves Fixtures A-C and S1-S19 are pairwise byte-distinct.
7. Static inspection proves the Builder file's complete CommonJS
   module-specifier set is exactly `node:fs`, its only `node:fs` operation is
   `readFileSync`, and its source contains no `server.js`, `fetch(`,
   `WebSocket`, `node:http`, `node:https`, `node:net`,
   `node:child_process`, `setTimeout`, or `setInterval` reference. Static
   inspection also proves the Test-author file's complete module-specifier set
   is exactly `node:test`, `node:assert`, `node:child_process`, `node:fs`,
   `node:path`, and `node:os`. Together with criteria 1-6, these assertions
   prove every command path is offline, self-terminating, deterministic, and
   read-only.
8. Git history contains the complete acceptance-test commit before the
   implementation commit. Apart from role-owned ticket-log appends, the
   Test-author commit creates only `app/tests/outbox-receipt.test.js` and the
   Builder commit creates only `app/tools/outbox-receipt.js`. The targeted
   command `node --test app/tests/outbox-receipt.test.js`, the full command
   `npm test --prefix app`, and
   `.github/scripts/test-immutability-check.sh` each exit `0` after
   implementation; existing application files, protected tests,
   `app/server.js`, `app/package.json`, lockfiles, dependencies, persisted
   schema, `factory/KIT_PIN`, and Factory controls are unchanged by both
   stages.

## Frozen contract — version 1

- **Freeze result:** PASS. Contract version 1 is frozen.

This is the exact interface for the Test-author and Builder. If it proves
wrong, return T-187 to Ready and append a higher-numbered frozen contract;
version 1 must not be edited or removed.

### Command-line interface and errors

- Repository-root invocation is exactly
  `node app/tools/outbox-receipt.js <state-file> <approval-id>`.
- Exactly two positional arguments are accepted. The state path is read
  directly; acceptance tests use absolute temporary paths.
- Zero, one, or three positional arguments produce exit code `2`, empty
  stdout, and exactly this single LF-terminated stderr line:

  ```text
  usage: node app/tools/outbox-receipt.js <state-file> <approval-id>
  ```

- The command reads `<state-file>` directly as UTF-8. Any read failure,
  including an absent path, a directory in place of a regular file, or an OS
  read denial, produces exit code `1`, empty stdout, and exactly:

  ```text
  outbox-receipt: cannot read state file
  ```

- Bytes rejected by `JSON.parse` produce exit code `1`, empty stdout, and
  exactly:

  ```text
  outbox-receipt: state file is not valid JSON
  ```

- A parsed value that fails the frozen structural rules produces exit code
  `1`, empty stdout, and exactly:

  ```text
  outbox-receipt: invalid Relay state
  ```

- A valid state with no outbox item whose stored `approvalId` is strictly
  equal to the supplied `<approval-id>` produces exit code `1`, empty stdout,
  and exactly:

  ```text
  outbox-receipt: outbox receipt not found
  ```

- Validation precedence is argument count, read, JSON parse, complete
  structural validation, then receipt lookup.
- Errors never contain the state path, approval ID, OS error, parser text,
  input bytes, stack trace, event content, job content, approval content,
  recipient, subject, body, or a second line. Every displayed error is
  terminated by exactly one LF.
- No path is written, renamed, deleted, chmodded, or created.

### Structurally valid state and successful output

For this command, structurally valid state is a non-null, non-array JSON object
with own `events`, `jobs`, `approvals`, and `outbox` properties whose values
are arrays.

Every `outbox` element must be a non-null, non-array object with these own
properties:

- non-empty string `approvalId`; and
- non-empty string `sentAt`.

Outbox approval IDs must be pairwise distinct. Contents of `events`, `jobs`,
and `approvals`, additional top-level properties, and additional outbox
properties are opaque to structural validation. In particular, `to`,
`subject`, and `body` may be present but are never copied to output. `sentAt`
is copied as stored without date parsing or normalization.

The lookup uses exact, case-sensitive JavaScript string equality and does not
trim, normalize, decode, or case-fold either ID. Success exits `0`, writes
zero stderr bytes, and writes one compact JSON object plus exactly one LF. Its
only keys, in order, are:

```json
{"approvalId":<stored approvalId>,"sentAt":<stored sentAt>}
```

No endpoint, HTTP request or response shape, route, UI selector, or global
page-shell text is introduced.

### Frozen Fixture A — two receipts and sensitive related content

The test writes this exact parsed value to an isolated temporary `state.json`
using `JSON.stringify(value, null, 2) + "\n"`:

```json
{
  "events": [
    {
      "id": "event-private",
      "payload": {
        "secret": "EVENT-PAYLOAD-MUST-NOT-LEAK"
      }
    }
  ],
  "jobs": [
    {
      "id": "job-private",
      "lastError": "JOB-ERROR-MUST-NOT-LEAK"
    }
  ],
  "approvals": [
    {
      "id": "appr-private",
      "action": {
        "to": "APPROVAL-TO-MUST-NOT-LEAK",
        "subject": "APPROVAL-SUBJECT-MUST-NOT-LEAK",
        "body": "APPROVAL-BODY-MUST-NOT-LEAK"
      },
      "reason": "APPROVAL-REASON-MUST-NOT-LEAK"
    }
  ],
  "outbox": [
    {
      "to": "OTHER-TO-MUST-NOT-LEAK",
      "subject": "OTHER-SUBJECT-MUST-NOT-LEAK",
      "body": "OTHER-BODY-MUST-NOT-LEAK",
      "approvalId": "appr-receipt-zulu",
      "sentAt": "2026-08-23T12:00:00.000Z"
    },
    {
      "to": "SELECTED-TO-MUST-NOT-LEAK",
      "subject": "SELECTED-SUBJECT-MUST-NOT-LEAK",
      "body": "SELECTED-BODY-MUST-NOT-LEAK",
      "approvalId": "appr-receipt-alpha",
      "sentAt": "2026-08-23T12:00:01.000Z"
    }
  ],
  "metadata": {
    "secret": "TOPLEVEL-MUST-NOT-LEAK"
  }
}
```

Expected stdout is exactly:

```text
{"approvalId":"appr-receipt-alpha","sentAt":"2026-08-23T12:00:01.000Z"}
```

followed by one LF. The frozen sensitive-token set is every Fixture A string
ending `-MUST-NOT-LEAK`.

### Frozen Fixture B — empty valid state

Fixture B's exact UTF-8 bytes are:

```text
{"events":[],"jobs":[],"approvals":[],"outbox":[]}
```

followed by one LF. It contains no matching receipt, so lookup produces the
frozen not-found result.

### Frozen Fixture C — malformed JSON

Fixture C's exact UTF-8 bytes are:

```text
{"events":[],"secret":"MALFORMED-STATE-MUST-NOT-LEAK"
```

followed by one LF. It is malformed because its object is not closed.

### Frozen arity and read-failure paths

Each arity case uses a fresh empty temporary parent:

- the zero-argument case supplies no path;
- the one-argument case supplies nonexistent
  `<tmp>/candidate-state.json`; and
- the three-argument case supplies nonexistent `<tmp>/state-a.json`,
  `appr-a`, and `extra-argument`.

The temporary parent's entry list remains empty after invocation. Each
read-failure case also uses a fresh temporary parent:

- `<tmp>/missing-state.json` does not exist before invocation; and
- `<tmp>/state-directory` exists as an empty directory, not a regular file.

Both read-failure invocations use approval ID `appr-receipt-alpha`.

### Frozen structural-invalid fixtures

Each table value is written directly as the displayed compact UTF-8 JSON plus
one LF. No invalid fixture is created by casing, normalization, or mutation.
Every invocation uses approval ID `appr-receipt-alpha` except S19, which uses
`appr-duplicate`.

| ID | Exact JSON value | Violated rule |
|---|---|---|
| S1 | `null` | root is null |
| S2 | `[]` | root is an array |
| S3 | `{"jobs":[],"approvals":[],"outbox":[]}` | missing own `events` array |
| S4 | `{"events":[],"approvals":[],"outbox":[]}` | missing own `jobs` array |
| S5 | `{"events":[],"jobs":[],"outbox":[]}` | missing own `approvals` array |
| S6 | `{"events":[],"jobs":[],"approvals":[]}` | missing own `outbox` array |
| S7 | `{"events":{},"jobs":[],"approvals":[],"outbox":[]}` | `events` is not an array |
| S8 | `{"events":[],"jobs":{},"approvals":[],"outbox":[]}` | `jobs` is not an array |
| S9 | `{"events":[],"jobs":[],"approvals":{},"outbox":[]}` | `approvals` is not an array |
| S10 | `{"events":[],"jobs":[],"approvals":[],"outbox":{}}` | `outbox` is not an array |
| S11 | `{"events":[],"jobs":[],"approvals":[],"outbox":[null]}` | receipt is null |
| S12 | `{"events":[],"jobs":[],"approvals":[],"outbox":[[]]}` | receipt is an array |
| S13 | `{"events":[],"jobs":[],"approvals":[],"outbox":[{"sentAt":"2026-08-23T12:00:00.000Z"}]}` | missing own `approvalId` |
| S14 | `{"events":[],"jobs":[],"approvals":[],"outbox":[{"approvalId":7,"sentAt":"2026-08-23T12:00:00.000Z"}]}` | `approvalId` is not a string |
| S15 | `{"events":[],"jobs":[],"approvals":[],"outbox":[{"approvalId":"","sentAt":"2026-08-23T12:00:00.000Z"}]}` | `approvalId` is empty |
| S16 | `{"events":[],"jobs":[],"approvals":[],"outbox":[{"approvalId":"appr-a"}]}` | missing own `sentAt` |
| S17 | `{"events":[],"jobs":[],"approvals":[],"outbox":[{"approvalId":"appr-a","sentAt":7}]}` | `sentAt` is not a string |
| S18 | `{"events":[],"jobs":[],"approvals":[],"outbox":[{"approvalId":"appr-a","sentAt":""}]}` | `sentAt` is empty |
| S19 | `{"events":[],"jobs":[],"approvals":[],"outbox":[{"approvalId":"appr-duplicate","sentAt":"1"},{"approvalId":"appr-duplicate","sentAt":"2"}]}` | receipt approval IDs are duplicated |

Before invoking the command, the acceptance test asserts that the serialized
bytes of Fixtures A-C and S1-S19 are pairwise distinct. The case-distinct
lookup ID `Appr-receipt-alpha` is frozen directly and is not produced by a
runtime transformation.

### File locations, protected boundaries, lifecycle, and verification

- Builder ownership is creation of `app/tools/outbox-receipt.js` only. It is a
  standalone CommonJS command whose complete module-specifier set is
  `node:fs`; its only filesystem operation is `readFileSync`.
- Test-author ownership is creation of
  `app/tests/outbox-receipt.test.js` only. Its complete module-specifier set is
  `node:test`, `node:assert`, `node:child_process`, `node:fs`, `node:path`, and
  `node:os`.
- `app/tests/outbox-detail.test.js` is the existing protected fixture seam and
  is reference-only. It contains no import/export allowlist, exact-source
  assertion, source snapshot, or module-specifier restriction for the new
  Builder file. Existing exact-source assertions inspect only their respective
  tool files. No existing protected test imports, snapshots, enumerates, or
  allowlists `app/tools/outbox-receipt.js`.
- Every runtime case creates its own temporary parent, writes only its frozen
  fixture when one is required, invokes the command with `spawnSync` and
  `timeout: 5_000`, waits for the child to terminate, and removes the parent
  recursively in `finally`. Repeated invocations require no reset because the
  command is read-only. Tests share no file, process, port, database, table,
  generated identifier, sequence, counter, clock, or timestamp. There are no
  foreign keys or dependent-table cleanup obligations.
- Existing file fingerprints consist of exact bytes and
  `fs.statSync(path, { bigint: true })` values `mode`, `size`, and `mtimeNs`,
  plus sorted parent entries; every named observation compares equal before
  and after invocation.
- All fixture IDs and timestamps are literals. No generated identifier,
  sequence, counter, initializer, reset, or clock participates in an exact
  expectation.
- Targeted verification is
  `node --test app/tests/outbox-receipt.test.js`. Required repository
  verification after implementation is `npm test --prefix app` and
  `.github/scripts/test-immutability-check.sh`.
- The Test-author stage precedes the Builder stage. Neither stage changes an
  existing test, `app/server.js`, `app/package.json`, a lockfile, dependency,
  persisted schema, `factory/KIT_PIN`, ticket state, ledger, or Factory
  control.
- This local CLI has no endpoint, UI, cross-origin request, authentication,
  cookie, CORS, domain, credential, preview deployment, or external-send
  behavior. It cannot trigger a send, so `External: no` remains exact.

## Criteria checklist

- **Pass/fail and unambiguous:** AC1-6 freeze exact arguments, fixture bytes,
  outputs, errors, exit codes, `5_000` ms process bounds, error and signal
  outcomes, and filesystem observations. AC7 freezes statically checkable
  source boundaries. AC8 is decided from Git history, per-stage diffs, and
  three exact command exits.
- **Coverage:** AC1 covers exact selection, output shape and key order, repeat
  determinism, redaction, and Fixture A. AC2 covers absent and case-distinct
  lookup misses plus Fixture B. AC3-5 cover argument, read, and parse
  precedence. AC6 covers every structural rule, S1-S19, fixture distinctness,
  and structure-before-lookup precedence. AC7 covers both files' module
  boundaries and offline/read-only behavior. AC8 covers file ownership, stage
  ordering, and verification. Every material contract element is exercised.
- **Derived-fixture distinctness:** A-C and S1-S19 are exact literals and are
  pairwise checked before use. No invalid serialized fixture is derived by a
  casing, normalization, or mutation operation. The case-distinct lookup ID
  is itself frozen as exact bytes and is byte-distinct from every valid ID.
- **Generated-value determinism:** all IDs and timestamps are literals. No
  generated identifier, sequence, counter, initializer, reset, or clock
  participates in an expectation.
- **Fixture lifecycle closure:** every case has isolated setup, waits
  synchronously for its child process, needs no reset because execution is
  read-only, and performs recursive `finally` teardown. No serialized
  invocation shares a resource; there is no database, parent row, child table,
  foreign key, or cascading cleanup.
- **Protected boundaries:** the associated seam and all tracked protected tests
  were inspected. No allowlist, source assertion, snapshot, hash, parser, or
  module restriction rejects the new file or its sole `node:fs` specifier, so
  `Protected-Test-Conflicts: none` is exact.
- **Global protected text:** the pinned readiness checker returned
  `GLOBAL TEXT PASS` separately for the usage line, all four error lines, the
  exact successful output line, and both output property names. The command
  adds no UI or accessible name.
- **Deploy/topology completeness:** the command is a local file-to-stdout
  process with no origin, authentication, cookie, CORS, credential, domain,
  preview-deploy, endpoint, UI, or external-send surface.

## Ambiguity log

1. **Receipt identity and safe output.** The seed names exact approval-ID
   lookup and exactly `approvalId` and `sentAt` but does not freeze
   serialization. [`SPEC.md` behavior 4](../../SPEC.md#behavior-spec) and the
   current [`app/server.js` outbox write](../../app/server.js) identify those
   as the non-message receipt fields. Resolved as the exact two-key compact
   object above, with stored values copied unchanged.
2. **Meaning of structurally valid.** The product has no general state-file
   validator. Resolved narrowly: validate all four collections, then every
   outbox field needed for exact lookup and output; treat unrelated
   collections, message fields, and additive fields as opaque.
3. **Duplicate receipt IDs.** The docs do not state a generic duplicate-row
   policy. The current server derives one approval ID per event and refuses a
   second approval execution after status leaves `pending`, while conformance
   case 7 requires no duplicate send after restart. Duplicate outbox
   `approvalId` values are therefore invalid rather than selected by array
   order.
4. **Exact lookup and errors.** The seed does not specify normalization, error
   text, exit codes, or precedence. No entry in
   [`factory/rulings.md`](../rulings.md) addresses offline receipt
   inspection. Resolved by the shipped offline-command convention: strict
   case-sensitive equality, usage exit `2`, operational exits `1`, fixed
   path-independent lines, and the frozen precedence above.
5. **Sensitive-data boundary.** “Without printing message contents” does not
   provide test values for every prohibited field. Fixture A gives selected
   and unselected receipt message fields and every related collection a
   distinct `-MUST-NOT-LEAK` token; exact stream checks make leakage
   mechanically observable.
6. **Read-only and self-termination proof.** Resolved with explicit `5_000` ms
   child timeouts, error and signal checks, repeated invocation,
   byte/stat/directory-entry comparisons, and the exact production module/API
   boundary. No service or network module is admitted.
7. **Cross-ticket file ownership.** Repository-wide ticket grep found no other
   declaration of either T-187-owned path. The other concurrently leased
   branches T-188 and T-189 were inspected with `git show`; they own distinct
   job-link and approval-timestamp paths. Related in-flight T-181 has an
   equivalent receipt-inspection goal but owns the distinct
   `outbox-inspect.js` pair. There is no file-boundary conflict.
8. **Protected source and global text.** The declared
   `app/tests/outbox-detail.test.js` seam and all tracked tests contain no
   static boundary for the new file. The pinned readiness check accepted
   every fixed shell-visible literal and output property name, so no
   protected-test conflict declaration or fixture-seam expansion is required.
9. **Topology, lifecycle, generated values, and external effects.** This is an
   offline local file read with isolated literal temporary fixtures and no
   database or network path. Auth, cookies, CORS, origins, credentials,
   preview deployment, generated values, dependent rows, UI selectors, and
   external sends do not apply. `External: no` remains correct.

## Coordination

T-187 owns only `app/tools/outbox-receipt.js` and
`app/tests/outbox-receipt.test.js`. Concurrently leased T-188 and T-189 own
distinct paths. T-181's related receipt inspector also owns distinct
`outbox-inspect.js` paths, so there is no implementation-surface conflict.

## Planning log

- 2026-08-23: Planner froze contract version 1 for the exact offline command,
  receipt projection, deterministic success and error streams, valid and
  invalid fixtures, privacy checks, fixture lifecycle, protected-source
  boundaries, and test-first stage ownership. Checked `SPEC.md`, `AGENTS.md`,
  `factory/rulings.md`, the current state model, the declared protected seam,
  all tracked protected tests, sibling ticket declarations, both other leased
  ticket branches, related in-flight T-181, and every new shell-visible
  literal. No operator ruling, protected-test conflict, file-boundary
  conflict, deployment topology, or external-send label was required.
