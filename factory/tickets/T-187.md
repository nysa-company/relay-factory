# T-187 — inspect one Relay outbox receipt offline

Kit-SHA: e85890e4e8ae98038b1eaaee5b6bb239bc874052
State: Planning
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-187
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/outbox-detail.test.js
Authentication-Seams: none
Builder ownership: app/tools/outbox-receipt.js only
Test-author ownership: app/tests/outbox-receipt.test.js only

## Description

Add a zero-dependency, read-only Node command that selects one outbox receipt
by exact approval ID from a persisted Relay state file while the service is
offline. It reports lifecycle metadata without printing message contents.

## Acceptance criteria

1. `node app/tools/outbox-receipt.js <state-file> <approval-id>` exits zero and
   emits deterministic compact JSON with exactly `approvalId` and `sentAt`.
2. An unknown approval ID exits nonzero with one fixed, privacy-safe error.
3. Missing, unreadable, malformed, or structurally invalid state is rejected
   with fixed redacted errors that reveal no path, recipient, subject, body,
   event payload, job error, approval action, or state content.
4. Every invocation is offline, read-only, and self-terminating; input bytes,
   mode, timestamps, and sibling directory entries remain unchanged.
5. Complete acceptance tests are committed before implementation. Targeted,
   full app, and immutability checks pass without dependency, server, schema,
   package-manifest, lockfile, or Factory-control changes.

## Coordination

This ticket owns only its two new `app/` paths and is independent of the other
qualification tickets.
