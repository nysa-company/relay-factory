# T-215 — count Relay approvals with one status offline

Kit-SHA: 81b21b6d1abca7a7bde0425fbae941fcb9c112ed
State: Ready
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-215
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/approval-state.test.js
Authentication-Seams: none
Builder ownership: app/tools/approval-status-count.js only
Test-author ownership: app/tests/approval-status-count.test.js only

## Description

Add a zero-dependency, read-only Node command that counts persisted Relay
approvals whose stored `status` exactly matches one operator-supplied value
while the service is offline.

## Acceptance criteria

1. `node app/tools/approval-status-count.js <state-file> <status>` exits zero
   and emits deterministic compact JSON with exactly one
   `approvalsWithStatus` nonnegative integer.
2. Matching compares stored and supplied strings exactly, without trimming,
   case folding, normalization, or coercion. No exact match returns
   `{"approvalsWithStatus":0}` followed by one newline.
3. Missing arguments and missing, unreadable, malformed, or structurally
   invalid state are rejected with fixed redacted errors that reveal no path,
   status, approval ID, action, reason, job link, or stored content; complete
   approval validation precedes counting.
4. Every invocation is offline, read-only, and self-terminating; input bytes,
   mode, timestamps, and sibling directory entries remain unchanged.
5. Complete acceptance tests are committed before implementation. Targeted,
   full app, and immutability checks pass without dependency, server, schema,
   package-manifest, lockfile, or Factory-control changes.

## Coordination

This ticket owns only its two new `app/` paths and is independent of T-213 and
T-214.
