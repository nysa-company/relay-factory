# T-246 — count Relay approvals by status and recipient offline

Kit-SHA: 9dfe16fc1b7b4f3bf7ae05e003fbfbdad0f25a96
State: Planning
Initiative: I-002
Priority: low
Risk class: low
External: no
Branch: ticket/T-246
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/tests/pending-approvals.test.js, app/tests/blocked-approvals.test.js
Authentication-Seams: none
Builder ownership: app/tools/approval-status-recipient-count.js only
Test-author ownership: app/tests/approval-status-recipient-count.test.js only

## Description

Add a zero-dependency, read-only Node command that counts persisted Relay
approvals whose status and recipient exactly match supplied strings. This
supports narrow offline approval checks without starting Relay or exposing
approval contents.

## Acceptance criteria

1. `node app/tools/approval-status-recipient-count.js <state-file> <status> <recipient>`
   emits compact JSON containing exactly one `approvalsWithStatusAndRecipient`
   integer.
2. Status must be one of Relay's four stored approval states. Recipient must be
   a non-empty string. Both filters match stored `status` and `action.to`
   values exactly, without trimming or coercion.
3. Invalid arguments are rejected before file access. Unreadable, malformed,
   or structurally invalid state returns fixed redacted errors; complete
   approval validation precedes counting.
4. Every invocation is offline, read-only, deterministic, and self-terminating.
5. Tests precede implementation. Targeted, full app, and immutability checks
   pass without dependency, server, schema, manifest, or Factory changes.

## Coordination

This ticket owns only its two new paths and is independent of T-247 and T-248.
