# T-165 — read one Relay sandbox outbox receipt

State: Blocked-Escalated
Resume-State: Planning
Initiative: I-002
Priority: normal
Risk class: low
External: no
Branch: ticket/T-165
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/server.js, app/tests/conformance.test.js
Authentication-Seams: none
Builder ownership: app/server.js only
Test-author ownership: app/tests/outbox-detail.test.js only

## Description

Add `GET /api/outbox/:approvalId` to the zero-dependency Relay API.

## Acceptance criteria

1. An existing outbox item returns HTTP 200 as
   `{"sandbox":true,"receipt":<stored outbox item>}`.
2. An unknown approval ID returns HTTP 404 with the deterministic JSON error
   `{"error":"no such outbox receipt"}`.
3. The route is read-only, explicitly identifies the outbox as sandboxed, and
   never performs an external send.
4. Tests are committed before implementation; no dependencies or Factory
   controls change.

Kit-SHA: 831fe510ad9ee623db8158760b1539f812b9b69f

## Ambiguity log

1. **The requested behavior is already present in the planning baseline.**
   Current `app/server.js` already implements `GET /api/outbox/:approvalId`
   with the ticket's exact 200 and 404 response shapes, and
   `app/tests/outbox-detail.test.js` already asserts the same two-receipt,
   exact-selection, exact-error, and read-only behavior. The completed
   [T-169 contract](T-169.md#frozen-contract--version-1) and
   [detail-route ruling](../rulings.md) show that this behavior was previously
   planned, tested, implemented, and approved. Neither
   [`SPEC.md`](../../SPEC.md#behavior-spec) nor `factory/rulings.md` defines
   additional outbox-detail behavior for T-165.
2. **The declared stage boundaries cannot produce a test-first unit from this
   baseline.** The only Test-author-owned file,
   `app/tests/outbox-detail.test.js`, is an existing protected test containing
   the requested assertions, while the only Builder-owned file,
   `app/server.js`, already satisfies them. A new test-author commit could not
   establish a failing contract test without changing already-approved
   behavior, and a Builder commit has no missing specified behavior to add.
   No prior ruling authorizes treating an already-completed duplicate as fresh
   implementation work.
3. **Operator decision required.** Should T-165 be closed as a duplicate of
   completed T-169, or should the operator replace it with a distinct product
   requirement and fresh Test-author/Builder ownership surfaces? No contract
   version is frozen until that choice is made.

## Planning log

- 2026-08-06: Planner inspected the product spec, current application and
  protected acceptance tests, sibling ticket ownership declarations, active
  worktrees, completed T-169, and `factory/rulings.md`. The prior ruling
  resolves the response envelope and concurrent `app/server.js` ownership but
  does not resolve the duplicate-completed-work condition. Planning stopped
  before freeze because the current ticket cannot yield a test-first Builder
  change without an operator decision.

ROLE-ESCALATE: CONTRACT-BLOCKED
