# T-110 — read one Relay event

State: Review
Initiative: I-002
Priority: normal
Risk class: low
External: no
Branch: ticket/T-110
Depends-On: none
Merge-Policy: auto
Product-Decisions: frozen
Protected-Test-Conflicts: none
Fixture-Seams: app/server.js, app/tests/conformance.test.js
Authentication-Seams: none
Builder ownership: app/server.js only
Test-author ownership: app/tests/event-detail.test.js only

## Spec'd description

Add one additive, read-only `GET /api/events/:id` detail route to the
zero-dependency Relay API. The route lets an operator or API client retrieve
one durable event together with the job created for it, without downloading
and filtering the complete `GET /api/state` document. It returns stored
records unchanged, has an exact missing-event response, and does not mutate
memory or persisted state.

Product-doc sources:

- [`SPEC.md` behavior 1](../../SPEC.md#behavior-spec) defines accepted events,
  event-ID idempotency, and the one-job relationship.
- [`SPEC.md` behaviors 2 and 6-7](../../SPEC.md#behavior-spec) define the
  related durable job, atomic persisted state, and the aggregate JSON state
  view over which this route is a read-only projection.
- [`SPEC.md` conformance cases 1-4 and 7](../../SPEC.md#test-cases-the-conformance-suite)
  make stored events, related jobs, job status and attempts, and restart
  durability observable.
- The exact success envelope and serialized concurrent ownership of
  `app/server.js` come from the operator's
  [`detail-route ruling`](../rulings.md).
- [`AGENTS.md` Boundaries](../../AGENTS.md#boundaries) requires the
  zero-dependency product boundary, and
  [`AGENTS.md` Verification](../../AGENTS.md#verification) defines the
  downstream repository checks.

## Acceptance criteria

1. With Frozen Fixture A loaded, `GET /api/events/event-detail` returns HTTP
   `200`, header `Content-Type: application/json`, and parsed JSON with
   exactly the two top-level keys `event` and `job`; `event` deep-equals
   Fixture A's complete event object, `job` deep-equals Fixture A's complete
   job object, and `job.eventId === event.id`.
2. With Frozen Fixture A loaded, `GET /api/events/event-missing` returns HTTP
   `404`, header `Content-Type: application/json`, and parsed JSON deep-equal
   to `{"error":"no such event"}` with no additional key.
3. Starting from Frozen Fixture A, the complete parsed `GET /api/state` value
   and the exact bytes of `<DATA_DIR>/state.json` captured immediately before
   one request from criterion 1 and one request from criterion 2 are,
   respectively, deep-equal and byte-equal to the values captured after both
   responses; `approvals` and `outbox` remain empty.
4. Git history places the test-author commit before the Builder commit. The
   test-author's application diff creates only
   `app/tests/event-detail.test.js`; the Builder's application diff changes
   only `app/server.js` and leaves the `ui()` function unchanged. Neither
   stage changes `app/package.json`, creates a lockfile, modifies an existing
   test, or changes a `factory/` path other than appending its required role
   note to `factory/tickets/T-110.md`.
5. From the repository root,
   `node --test app/tests/event-detail.test.js`,
   `npm test --prefix app`, and
   `.github/scripts/test-immutability-check.sh` each exit `0`.

## Frozen contract — version 1

- **Freeze result:** PASS. Contract version 1 is frozen.

This is the exact interface for the test-author and Builder. If it proves
wrong, return T-110 to Ready, preserve version 1 unchanged, and append a
higher-numbered frozen contract that records the superseding decision.

### HTTP interface

- Method and path: `GET /api/events/:id`, where `:id` is exactly one
  non-empty segment captured from the parsed URL pathname.
- Lookup: select the first stored `state.events` entry whose string `id`
  exactly equals `:id`, then select the first stored `state.jobs` entry whose
  string `eventId` exactly equals the selected event's `id`.
- Existing event: return HTTP `200`, header
  `Content-Type: application/json`, and parsed JSON exactly equal to
  `{"event":<complete stored event>,"job":<complete stored related job>}`.
  Neither stored object is projected, renamed, normalized, defaulted, or
  given an additional field.
- Unknown event: return HTTP `404`, header
  `Content-Type: application/json`, and parsed JSON exactly equal to
  `{"error":"no such event"}`.
- Both outcomes are read-only: they do not mutate an in-memory collection,
  invoke persistence, or change `<DATA_DIR>/state.json`.
- Manually corrupted state containing an event without its related job, or
  multiple jobs for one event, is outside this contract. Relay-created state
  persists an accepted event and exactly one job together, and duplicate
  event intake does not create another job.
- UI selectors and accessible names: none. The route does not change the
  existing `GET /` markup or `ui()` output.
- Authentication, cookies, CORS, cross-origin behavior, credentials, domains,
  and preview-deploy topology are unchanged. Requests use unauthenticated
  same-origin HTTP on the existing Relay server; the acceptance topology is
  loopback HTTP with no credentials.

### Frozen Fixture A

`app/tests/event-detail.test.js` creates a fresh temporary `DATA_DIR` and
writes the following complete parsed JSON value to
`<DATA_DIR>/state.json` before starting the real server:

```json
{
  "events": [
    {
      "id": "event-detail",
      "type": "meeting",
      "payload": {
        "failTimes": 99
      },
      "receivedAt": "2026-08-07T12:00:00.000Z"
    }
  ],
  "jobs": [
    {
      "id": "job-event-detail",
      "eventId": "event-detail",
      "status": "dead",
      "attempts": 3,
      "lastError": "simulated failure on attempt 3",
      "retries": 0,
      "attemptsSinceRetry": 3
    }
  ],
  "approvals": [],
  "outbox": []
}
```

The existing-resource ID is exactly `event-detail`. The unknown ID is the
literal `event-missing`, which is byte-distinct from `event-detail`; it is not
created by casing, normalization, or mutation. Tests compare parsed JSON
values, so object-property order and insignificant serialized whitespace are
not response-interface requirements.

Every asserted event ID, job ID, counter, status, error string, and timestamp
is written literally. Fixture setup generates only an unasserted temporary
directory name; it generates no asserted identifier, sequence, counter, or
timestamp. The dead job is not selected by the pending-only worker, and
`WORKER_MS=60000` further prevents fixture activity during an acceptance
case.

### Fixture lifecycle

Each of the three product acceptance cases owns a distinct loopback port
(`4781`, `4782`, or `4783`) and performs this closed lifecycle:

1. create a fresh temporary directory;
2. write Frozen Fixture A to its `state.json`;
3. start `app/server.js` with that directory as `DATA_DIR`,
   `WORKER_MS=60000`, and `ALLOWLIST=test@example.com`;
4. wait until `GET /health` succeeds before issuing acceptance requests;
5. consume every response body;
6. in `finally`, send `SIGKILL` if the child is running, await its exit, and
   recursively remove the temporary directory.

There is no database and no parent/child table cleanup. No setup state is
shared between cases, so no reset helper, foreign-key cleanup, or cascading
delete edit is required. Ports `4781-4783` are distinct from every fixed port
in the current protected suite.

### File locations and verification

- Test-author application ownership: create
  `app/tests/event-detail.test.js` only, following the existing
  `node:test`, real-child-server, and temporary-`DATA_DIR` pattern in
  `app/tests/conformance.test.js`. No existing test is edited.
- Builder application ownership: modify `app/server.js` only, adding the
  event-detail route without changing `ui()`.
- The two downstream roles may append their durable role notes to
  `factory/tickets/T-110.md`; they do not modify any other `factory/` path.
- No fixture file, UI file, dependency, or lockfile is added.
- Targeted verification:
  `node --test app/tests/event-detail.test.js`.
- Repository verification:
  `npm test --prefix app`.
- Protected-test ordering verification:
  `.github/scripts/test-immutability-check.sh`.
- Planner validation is contract-only; the three product commands above are
  frozen for downstream verification and are not run by the Planner.

## Criteria checklist

- **Pass/fail:** AC1-3 use exact status, header, key-set, deep-equality, and
  byte-equality assertions. AC4 is decided by the commit graph and exact
  per-stage diffs. AC5 is decided by three exact process exit statuses.
- **Unambiguous:** the route, IDs, complete fixture, complete response shapes,
  relationship, comparison semantics, lifecycle, file boundaries, ports, and
  commands are named exactly.
- **Coverage:** AC1 covers the success path, lookup relationship, and every
  non-empty fixture record field; AC2 covers the unknown ID and exact error;
  AC3 covers both branches' read-only behavior, the persisted file, and all
  four state collections; AC4 covers every file, dependency, and no-UI
  boundary; AC5 covers every verification command.
- **No dead contract elements:** every endpoint, response shape, fixture
  collection, port block, file boundary, and verification command appears in
  AC1-5 or is required by their deterministic test lifecycle.
- **Derived-fixture distinctness:** `event-missing` is a literal and is
  byte-distinct from the only valid event ID, `event-detail`; no derived
  casing, normalization, or mutation fixture exists.
- **Generated-value determinism:** every asserted value is literal; the
  temporary directory name is generated but is not asserted or exposed by
  the interface.
- **Fixture lifecycle closure:** each acceptance case has isolated setup and
  awaited child-process/filesystem teardown. There are no relational tables,
  dependent rows, shared reset helpers, or serialized cleanup dependencies.
- **Protected source boundaries:** inspection of existing protected tests
  found no import/export allowlist, exact-source assertion, or source snapshot
  restricting an additive route in `app/server.js`. Existing static
  module-specifier checks target offline tool files, not `app/server.js`.
  The planned test spawns the existing production file and adds no production
  module specifier, so no protected-test conflict declaration is required.
- **Global protected text:** the contract introduces no global-shell visible
  literal or accessible name. `"no such event"` is an API-only JSON value and
  is not rendered by the global UI, so no global-literal collision check
  applies.

## Ambiguity log

1. **What is the exact successful response envelope?** `SPEC.md` defines
   durable aggregate state but not an event-detail envelope. Resolved by the
   prior [`factory/rulings.md`](../rulings.md) decision as exactly
   `{"event":<stored event>,"job":<stored related job>}`.
2. **How is the related job selected?** Resolved from
   [`SPEC.md` behaviors 1-2](../../SPEC.md#behavior-spec), the ruling, and the
   durable model: the related job's `eventId` must exactly equal the selected
   event's `id`.
3. **How is read-only behavior observable?** Resolved from
   [`SPEC.md` behaviors 6-7](../../SPEC.md#behavior-spec): compare both the
   complete public aggregate state value and the exact persisted file bytes
   before and after the success and missing-resource reads.
4. **How are orphaned events or duplicate related jobs handled?** Product docs
   define Relay-created state, in which event acceptance persists one event
   and one job together and duplicate intake creates no second job. They do
   not define manually corrupted state recovery, so those malformed-state
   cases are explicitly excluded rather than assigned invented behavior.
5. **How is the fixture kept deterministic while the worker runs?** Resolved
   from [`SPEC.md` conformance case 4](../../SPEC.md#test-cases-the-conformance-suite):
   the fixture uses a dead job at exactly three attempts, which the
   pending-only worker does not select; all asserted values are literal.
6. **Do sibling or leased tickets overlap the file boundary?** A scan of
   `factory/tickets/` found repeated Ready-generation event-detail tickets,
   including T-162 and T-166, with the same `app/server.js` and
   `app/tests/event-detail.test.js` declarations; neither has a leased branch
   or active worktree, so T-110 owns the only active event-detail test
   surface. Every other available leased remote-tracking branch was inspected:
   `origin/ticket/T-163`, T-164, and T-165 declare `app/server.js` with
   distinct test files, while T-170, T-171, and T-172 own disjoint
   `app/tools/` and test paths. The existing
   [`concurrent-ownership ruling`](../rulings.md) permits the distinct
   additive detail routes to share `app/server.js` with serialized protected
   publication. The current worktree list contains no other active ticket
   worktree.
7. **Do fixture lifecycle or ports overlap protected tests?** The new
   `4781-4783` block is absent from the repository and distinct from the
   current fixed test ports. Per-case temporary directories and awaited
   teardown prevent shared fixture state.
8. **Does this require deploy, authentication, or external-send topology?**
   No. It is an unauthenticated same-origin read over the existing server,
   changes no domain, credential, cookie, SameSite, CORS, cross-origin, or
   preview behavior, and cannot enter an approval/send path. `External: no`
   remains correct and no `external` label is required.

## Planning log

- 2026-08-07: Planner froze contract version 1 for the exact event/job detail
  response, deterministic durable fixture, missing-event error, read-only
  proof, fixture lifecycle, and test-first file boundaries. The Planner
  inspected the product docs, current production and protected test sources,
  sibling ownership declarations, every available leased ticket branch, and
  active worktrees. The existing detail-route ruling resolved the response
  envelope and shared-server ownership; no new operator ruling, protected-test
  conflict, global-literal check, or external label was required.

Kit-SHA: c27d49fdec2e579928e720b187bf87846ced6b53

## Log

- 2026-08-07: Spec-lint round 1 against frozen contract version 1.

### Contract-element coverage

| Contract element | Positive | Failure/empty | Duplicate/repeat | Out of scope |
|---|---|---|---|---|
| Route `GET /api/events/:id`, one non-empty path segment | AC1 `GET /api/events/event-detail` | AC2 `GET /api/events/event-missing` | — | Trailing-slash, empty-segment, and percent-encoded ids unexercised (finding 3) |
| Event lookup by exact `id` equality | AC1 returns Fixture A's event | AC2 returns 404 for a byte-distinct id, so the route cannot return an unconditional record | — | Case-variant and whitespace-variant ids equivalent to AC2 |
| Related-job lookup by `job.eventId === event.id` | AC1 asserts the equality, but Fixture A holds exactly one job, so the assertion is vacuous | AC2 never reaches job selection | Contract's "first stored entry" rule unexercised | Multiple jobs per event excluded by contract; **wrong-record selection is uncovered (finding 1)** |
| Success envelope: HTTP `200`, exactly the keys `event` and `job` | AC1 exact status and sorted key-set | AC2 shows the envelope is not emitted for an unknown id | — | Property order and whitespace explicitly not interface |
| Unprojected complete stored objects (no rename, default, or added field) | AC1 deep-equals both complete fixture records, every field non-empty | — | — | — |
| Unknown event: HTTP `404`, exactly `{"error":"no such event"}` | — | AC2 exact status and deep-equal body with no extra key | — | Other absent ids equivalent |
| `Content-Type: application/json` on both branches | AC1 header | AC2 header | — | Charset parameter absent from the frozen literal |
| Read-only in memory and over `GET /api/state` | AC3 deep-equal complete state before/after both requests | AC3 asserts `approvals` and `outbox` stay empty | AC3 covers one AC1 request and one AC2 request in the same case | — |
| Read-only on `<DATA_DIR>/state.json` | AC3 byte-equality before/after | AC3 same case | — | Sidecar/temp-file creation in `DATA_DIR` unasserted (finding 4) |
| Malformed state (orphan event, multiple jobs for one event) | — | — | — | Explicitly out of contract; Relay-created state persists one event and one job together |
| `ui()` and `GET /` markup unchanged; no UI selector or accessible name | AC4 Builder diff leaves `ui()` unchanged | — | — | No global-shell literal is introduced, so no readiness global-text evidence is owed |
| Authentication, cookies, CORS, cross-origin, deploy topology unchanged | AC1-AC3 run unauthenticated loopback HTTP on the existing server | — | — | `External: no`; no approval or send path is reachable |
| Fixture A lifecycle: fresh `DATA_DIR`, `WORKER_MS=60000`, `/health` gate, ports `4781-4783`, awaited SIGKILL and recursive removal | AC1-AC3 each own one port and one directory | AC3 observes the persisted file across teardown-free execution | Three independent cases share no setup state | No database, parent row, foreign key, or reset helper exists |
| File ownership and test-first commit order | AC4 exact per-stage diffs and commit graph | AC4 forbids `app/package.json`, lockfile, existing-test, and other `factory/` changes | — | — |
| Verification commands | AC5 three exact exit statuses | — | — | — |

### Findings

1. Related-job selection is untestable as frozen. The contract's material
   lookup rule is "the first stored `state.jobs` entry whose string `eventId`
   exactly equals the selected event's `id`", but Frozen Fixture A contains
   exactly one event and exactly one job, so AC1's `job.eventId === event.id`
   holds for any implementation, including `job: state.jobs[0]` or
   `job: state.jobs.find(j => j.status === "dead")`, which ignores the
   relationship entirely and returns the wrong job on any real multi-job state.
   AC2 constrains only event lookup. Fixture A is frozen and its complete
   parsed value is fixed by the contract, so the test-author cannot close this
   gap; the sibling receipt route already uses a two-record fixture
   (`app/tests/outbox-detail.test.js`, `appr-other` plus the target) for exactly
   this reason. This is a Planner fix: append a contract version 2 whose
   Fixture A carries at least one additional non-matching event and one
   additional job belonging to that other event, and extend AC1 to assert the
   returned pair is the matching one.
2. SPEC-WARN: AC3 names "one request from criterion 1 and one request from
   criterion 2" without requiring their statuses inside that case, so the
   read-only assertions can pass vacuously against a server where the route
   does not exist and both requests fall through to the generic
   `{"error":"not found"}` 404. Assert `200` for the existing id and `404` with
   the exact `no such event` body inside the AC3 case, as
   `app/tests/job-detail.test.js` does at its AC3.
3. SPEC-WARN: no criterion exercises non-matching path shapes for the frozen
   "exactly one non-empty segment" rule (`/api/events`, `/api/events/`,
   `/api/events/a/b`, and a percent-encoded id). These are equivalent
   permutations of the existing 404 disposition against the server's current
   generic fallback; the test-author may add them without a contract change.
4. SPEC-WARN: AC3 compares only the bytes of `<DATA_DIR>/state.json`; add an
   assertion that the temporary directory's entry list is unchanged so a
   stray persist temp file (`state.json.<hex>`, which `persist()` creates
   before rename) or any other sidecar write is caught.
5. SPEC-WARN: fourteen tickets (`T-114`, `T-118`, `T-122`, `T-126`, `T-130`,
   `T-134`, `T-138`, `T-142`, `T-146`, `T-150`, `T-154`, `T-158`, `T-162`,
   `T-166`) sit in `State: Ready` declaring the identical
   `app/server.js` / `app/tests/event-detail.test.js` ownership T-110 is
   executing; ambiguity log item 6 names only two of them. None is leased, so
   this is not a live conflict, but ask the operator to cancel or supersede the
   stranded identities so one cannot be scheduled after T-110 merges.

Verified independently this pass: every asserted Fixture A value is producible
by the frozen initializers in `app/server.js` — `job-event-detail` matches
`"job-" + evt.id`, and `attempts: 3` / `attemptsSinceRetry: 3` /
`status: "dead"` / `lastError: "simulated failure on attempt 3"` are exactly
what `processJob` writes at `MAX_ATTEMPTS` with `payload.failTimes = 99`;
`retries: 0` matches the untouched retry counter. `event-missing` is a literal
byte-distinct from `event-detail` and from no accepted valid fixture. The
pending-only `workerTick` cannot select the dead job, so `WORKER_MS=60000` is
belt-and-braces rather than load-bearing. Ports `4781-4783` appear nowhere in
the repository or in any leased ticket; `npm test` runs
`node --test tests/*.test.js` in parallel and the existing fixed ports are
`4719`, `4721-4722`, `4731-4733`, `4741-4745`, `4751-4756`, `4761-4763`,
`4771-4773`, and `4779`, with `origin/ticket/T-163` holding `4761-4763`. There
is no database, foreign key, generated identifier, sequence, counter, or
generated timestamp, so no lifecycle dependency closure is owed; each case owns
one temporary directory removed recursively in `finally`. Every static check in
the protected suite was enumerated and parsed: `app/tests/dead-jobs.test.js`
AC5 and `app/tests/blocked-approvals.test.js` restrict module specifiers of
`app/tools/dead-jobs.js`, `app/tools/blocked-approvals.js`, and their own test
sources only, and `app/tests/backend-readiness.test.js` asserts against
`factory/backend-readiness.sh` fixtures; none carries an import/export
allowlist, exact-source assertion, or source snapshot for `app/server.js`, whose
only reference across the suite is `path.join(__dirname, "..", "server.js")`
for spawning. No protected test makes a page-wide or `GET /` text assertion, the
contract adds no global-shell visible literal or accessible name, and
`"no such event"` is an API-only JSON value, so no `GLOBAL TEXT PASS` evidence
is owed and no `Protected-Test-Conflicts` declaration is required. The envelope
`{"event":<stored event>,"job":<stored related job>}` and the shared
`app/server.js` ownership match the recorded detail-route ruling in
`factory/rulings.md` exactly; no other ruling touches this contract. Product-doc
names, paths, and shapes match `SPEC.md` and the current `app/server.js` state
model. Baseline `npm test --prefix app` passes 66/66 at `fd57cf8`.

SPEC-LINT: FAIL — Frozen Fixture A's single event and single job make the
contract's related-job lookup rule unverifiable, so a builder can pass every
criterion while returning an unrelated job.

## Frozen contract — version 2

- **Freeze result:** PASS. Contract version 2 is frozen.

Version 2 supersedes version 1 for Test-author and Builder execution. Version
1 and its spec-lint output remain immutable history. This repair epoch makes
relationship-based job selection observable, requires both route outcomes
inside the read-only case, and detects sidecar filesystem writes. If version 2
proves wrong, return T-110 to Ready and append a higher numbered contract
without editing either frozen version.

### Version 2 spec'd description

Add one additive, unauthenticated, read-only `GET /api/events/:id` route to the
zero-dependency Relay API. The route returns one complete stored event and the
complete stored job whose `eventId` matches that event's `id`, avoiding
client-side filtering of `GET /api/state`. Version 2 does not add product
behavior beyond version 1; it repairs the acceptance fixture so an
implementation that returns an unrelated job cannot pass.

Product-doc sources:

- [`SPEC.md` behaviors 1-2](../../SPEC.md#behavior-spec) define accepted event
  IDs, idempotency, and the exactly-one related job created for an accepted
  event.
- [`SPEC.md` behaviors 6-7](../../SPEC.md#behavior-spec) define atomic durable
  state and the aggregate JSON state view over which this route is a read-only
  projection.
- [`SPEC.md` conformance cases 1-4 and 7](../../SPEC.md#test-cases-the-conformance-suite)
  define stored events, related jobs, attempts, terminal dead status, and
  restart durability used by the fixture.
- The operator's [`detail-route ruling`](../rulings.md) freezes the exact
  success envelope and permits serialized ownership of `app/server.js` by the
  additive detail-route tickets.
- [`AGENTS.md` Boundaries](../../AGENTS.md#boundaries) keeps the application
  zero-dependency, and [`AGENTS.md` Verification](../../AGENTS.md#verification)
  defines the downstream repository checks.

### Version 2 acceptance criteria

1. With Version 2 Frozen Fixture A loaded,
   `GET /api/events/event-detail` returns HTTP `200`, header
   `Content-Type: application/json`, and parsed JSON with exactly the two
   top-level keys `event` and `job`; `event` deep-equals Fixture A's first
   event object, `job` deep-equals Fixture A's second job object,
   `job.eventId === event.id`, and neither returned object deep-equals the
   non-matching record of its collection.
2. With Version 2 Frozen Fixture A loaded,
   `GET /api/events/event-missing` returns HTTP `404`, header
   `Content-Type: application/json`, and parsed JSON deep-equal to
   `{"error":"no such event"}` with no additional key.
3. Starting from Version 2 Frozen Fixture A, capture the complete parsed
   `GET /api/state` value, the exact bytes of `<DATA_DIR>/state.json`, and the
   sorted entry names of `<DATA_DIR>` immediately before both detail
   requests. In the same case, the existing-event request returns HTTP `200`
   with parsed JSON exactly equal to
   `{"event":<Fixture A first event>,"job":<Fixture A second job>}`, and the
   missing-event request returns HTTP `404` with parsed JSON exactly equal to
   `{"error":"no such event"}`. After both response bodies are consumed, the
   complete state is deep-equal, the state-file bytes are byte-equal, and the
   directory entry names are exactly equal to their before values;
   `approvals` and `outbox` remain empty.
4. Git history places the complete Test-author commit before the Builder
   commit. The Test-author's application diff creates only
   `app/tests/event-detail.test.js`; the Builder's application diff changes
   only `app/server.js` and leaves the complete `ui()` function byte-for-byte
   unchanged. Neither stage changes `app/package.json`, creates a lockfile or
   dependency, modifies an existing test, or changes a `factory/` path other
   than appending its required role note to
   `factory/tickets/T-110.md`.
5. After implementation,
   `node --test app/tests/event-detail.test.js`,
   `npm test --prefix app`, and
   `.github/scripts/test-immutability-check.sh` each exit exactly `0` from the
   repository root.

### Version 2 HTTP interface

- Method and path: `GET /api/events/:id`, where `:id` is exactly one non-empty
  segment captured from the parsed URL pathname.
- Lookup: select the first stored `state.events` entry whose string `id`
  exactly equals `:id`, then select the first stored `state.jobs` entry whose
  string `eventId` exactly equals the selected event's `id`.
- Existing event: return HTTP `200`, header
  `Content-Type: application/json`, and parsed JSON exactly equal to
  `{"event":<complete stored event>,"job":<complete stored related job>}`.
  Neither stored object is projected, renamed, normalized, defaulted, or
  given an additional field.
- Unknown event: return HTTP `404`, header
  `Content-Type: application/json`, and parsed JSON exactly equal to
  `{"error":"no such event"}`.
- Both outcomes are read-only: they do not mutate an in-memory collection,
  invoke persistence, alter `<DATA_DIR>/state.json`, or create, rename, or
  remove any entry in `<DATA_DIR>`.
- Manually corrupted state containing an event without its related job, or
  multiple jobs for one event, is outside this contract. Relay-created state
  persists an accepted event and exactly one job together, and duplicate
  event intake does not create another job.
- UI selectors and accessible names: none. The route does not change the
  existing `GET /` markup or any byte of the `ui()` function.
- Authentication, cookies, CORS, cross-origin behavior, credentials, domains,
  and preview-deploy topology are unchanged. Acceptance requests use
  unauthenticated loopback HTTP against the existing Relay server origin,
  with no cookie or credential.

### Version 2 Frozen Fixture A

`app/tests/event-detail.test.js` creates a fresh temporary `DATA_DIR` and
writes the following complete parsed JSON value to
`<DATA_DIR>/state.json` before starting the real server:

```json
{
  "events": [
    {
      "id": "event-detail",
      "type": "meeting",
      "payload": {
        "failTimes": 99
      },
      "receivedAt": "2026-08-07T12:00:00.000Z"
    },
    {
      "id": "event-other",
      "type": "email",
      "payload": {
        "failTimes": 99
      },
      "receivedAt": "2026-08-07T12:00:01.000Z"
    }
  ],
  "jobs": [
    {
      "id": "job-event-other",
      "eventId": "event-other",
      "status": "dead",
      "attempts": 6,
      "lastError": "simulated failure on attempt 6",
      "retries": 1,
      "attemptsSinceRetry": 3
    },
    {
      "id": "job-event-detail",
      "eventId": "event-detail",
      "status": "dead",
      "attempts": 3,
      "lastError": "simulated failure on attempt 3",
      "retries": 0,
      "attemptsSinceRetry": 3
    }
  ],
  "approvals": [],
  "outbox": []
}
```

The requested event is the first event, while its related job is the second
job. The first job belongs to the second event. Therefore returning
`state.jobs[0]`, selecting by status, or pairing records by array position
fails AC1 and AC3. The existing-resource ID is exactly `event-detail`; the
non-matching stored ID is exactly `event-other`; and the unknown ID is exactly
`event-missing`. These three literal UTF-8 strings are pairwise byte-distinct
and none is created by casing, normalization, or mutation. Tests compare
parsed response JSON, so object-property order and insignificant response
whitespace are not interface requirements.

Every asserted event ID, job ID, counter, status, error string, and timestamp
is written literally. Fixture setup generates only an unasserted temporary
directory name; it generates no asserted identifier, sequence, counter, or
timestamp. The two dead jobs are not selected by the pending-only worker. The
target job's `attempts: 3`, `lastError: "simulated failure on attempt 3"`,
`retries: 0`, and `attemptsSinceRetry: 3` are the first dead-state values
produced by `MAX_ATTEMPTS=3`. The non-matching job's `attempts: 6`,
`lastError: "simulated failure on attempt 6"`, `retries: 1`, and
`attemptsSinceRetry: 3` are the values produced after one retry reaches the
same three-attempt window. `WORKER_MS=60000` provides an additional bound
during each acceptance case.

### Version 2 fixture lifecycle

Each of the three product acceptance cases owns a distinct loopback port
(`4781`, `4782`, or `4783`) and performs this closed lifecycle:

1. create a fresh temporary directory;
2. write Version 2 Frozen Fixture A to its `state.json`;
3. start `app/server.js` with that directory as `DATA_DIR`,
   `WORKER_MS=60000`, and `ALLOWLIST=test@example.com`;
4. wait until `GET /health` succeeds before issuing acceptance requests;
5. consume every response body;
6. in `finally`, send `SIGKILL` if the child is running, await its exit, and
   recursively remove the temporary directory.

AC3 records the sorted `DATA_DIR` entry names before and after both route
branches; both values are exactly `["state.json"]`. There is no database and
no parent/child table cleanup. No setup state is shared between cases, so no
reset helper, foreign-key cleanup, or cascading delete edit is required.
Ports `4781-4783` are absent from the current protected suite and distinct
from its fixed ports.

### Version 2 file locations and verification

- Test-author application ownership: create
  `app/tests/event-detail.test.js` only, using the installed Node standard
  library and following the real-child-server and temporary-`DATA_DIR`
  lifecycle pattern in `app/tests/conformance.test.js`. No existing test is
  edited.
- Builder application ownership: modify `app/server.js` only, adding the
  event-detail route without changing `ui()` or adding a module specifier.
- The downstream roles may append durable role notes to
  `factory/tickets/T-110.md`; they do not modify another `factory/` path.
- No fixture file, UI file, dependency, package-manifest edit, or lockfile is
  added.
- Targeted verification:
  `node --test app/tests/event-detail.test.js`.
- Repository verification:
  `npm test --prefix app`.
- Protected-test ordering verification:
  `.github/scripts/test-immutability-check.sh`.
- Planner validation is contract-only; the three product commands above are
  frozen for downstream verification and are not run by the Planner.

### Version 2 protected-boundary result

Inspection of every current protected `app/tests/*.test.js`,
`app/package.json`, and `.github/scripts/test-immutability-check.sh` found no
import/export allowlist, exact-source assertion, source snapshot, or
module-specifier restriction governing additive code in `app/server.js`.
The existing static source checks read only their own Builder-owned
`app/tools/` files. Existing server tests spawn `app/server.js` by path, so
the planned route adds no production module specifier. The package's existing
`tests/*.test.js` pattern admits the new acceptance file without a manifest
edit. `Protected-Test-Conflicts: none` therefore remains exact.

The only new production literal, `"no such event"`, is an API-only JSON value.
No global-shell visible literal, page-wide text, UI selector, or accessible
name is introduced, so the global-literal readiness check is not applicable.

### Version 2 criteria checklist

- **Pass/fail and unambiguous:** AC1-3 freeze exact statuses, headers, object
  keys, complete deep-equality targets, relationship equality, state and byte
  comparisons, and directory entries. AC4 is decided by commit order and
  exact per-stage diffs. AC5 is decided by three exact exit statuses.
- **Coverage:** AC1 covers the success path, exact event lookup, complete
  stored records, response envelope, and a related job that is not the first
  job or the same-position record. AC2 covers the unknown ID and exact error.
  AC3 covers both outcomes again so read-only assertions cannot pass against
  an unrouted generic `404`; it also covers all four state collections, the
  persisted file, and every directory entry. AC4 covers every ownership,
  dependency, package, protected-test, and no-UI boundary. AC5 covers every
  verification command.
- **No dead contract elements:** every endpoint outcome, response shape,
  fixture record, relationship, collection, port block, file boundary,
  topology statement, and verification command appears in AC1-5 or is
  necessary for their deterministic lifecycle.
- **Derived-fixture distinctness:** `event-detail`, `event-other`, and
  `event-missing` are independent pairwise byte-distinct literals; no derived
  casing, normalization, or mutation fixture exists.
- **Generated-value determinism:** every asserted record value is literal.
  The two job counter/error groups agree with the frozen three-attempt
  initializer and one-retry progression; the generated temporary directory
  name is neither asserted nor exposed.
- **Fixture lifecycle closure:** each case has isolated setup and awaited
  child-process/filesystem teardown. Both jobs are terminal, and there are no
  relational tables, dependent rows, shared reset helpers, or serialized
  cleanup dependencies.
- **Protected source and global text:** no static protected boundary governs
  the additive route, no new module specifier is planned, and no global-shell
  literal or accessible name is introduced. No conflict declaration,
  Fixture-Seam edit, or global-text readiness invocation is required.
- **Deploy/topology and external effects:** acceptance uses one unauthenticated
  loopback origin with no cookies or credentials. No CORS, SameSite,
  cross-origin, domain, preview-deploy, approval, outbox, or external-send
  behavior changes; `External: no` remains exact.

### Version 2 ambiguity-log addendum

9. **How is related-job selection made observable?** Version 1's sole job made
   `job.eventId === event.id` vacuous. Version 2 places a non-matching job
   first and the target job second while the target event remains first.
   AC1 and AC3 require the second job, so array-first, status-based, and
   same-position pairing implementations fail.
10. **How can the read-only case avoid passing before the route exists?**
    Version 2 AC3 asserts the exact `200` success envelope and exact `404`
    missing-event envelope before comparing state. The server's generic
    `{"error":"not found"}` fallback cannot satisfy the case.
11. **How are writes outside `state.json` detected?** AC3 snapshots sorted
    `DATA_DIR` entry names as exactly `["state.json"]` before and after both
    requests, so a persistence temporary file, log, or other sidecar is
    observable in addition to byte changes in the state file.
12. **Do the added fixture values describe producible Relay records?** Yes.
    [`SPEC.md` behavior 2](../../SPEC.md#behavior-spec) and the current
    `MAX_ATTEMPTS=3` model produce the target's first dead window and the
    non-matching job's second dead window after one retry. Both fixture events
    use `failTimes: 99`, so neither job creates an approval.
13. **Do sibling or leased tickets conflict with the repair boundary?** A
    fresh repository-wide ownership scan found that Ready tickets T-111
    through T-166 all repeat `app/server.js` ownership in four generated
    detail-route families. The same fourteen stranded event-detail identities
    listed by the version 1 lint
    (`T-114`, `T-118`, `T-122`, `T-126`, `T-130`, `T-134`, `T-138`,
    `T-142`, `T-146`, `T-150`, `T-154`, `T-158`, `T-162`, and `T-166`)
    are the only tickets that also repeat
    `app/tests/event-detail.test.js`. None of the Ready identities has an
    active worktree, and only T-163 through T-165 have remote leased branches.
    `origin/ticket/T-163` is a no-application-diff preservation contract for
    the already delivered job route. Leased T-164 and T-165 are
    contract-blocked duplicates with no frozen implementation authority.
    Leased T-170 through T-172 own disjoint offline-tool paths. The
    [`detail-route ruling`](../rulings.md) permits the four detail families to
    share `app/server.js` with serialized publication. No active worktree
    besides T-110 owns an application path.
14. **Do protected source boundaries or global text change in this repair?**
    No. Reinspection found static source assertions only for distinct
    `app/tools/` files, and server references are child-process paths rather
    than import allowlists. The repair adds no UI or global-shell text, so
    `Protected-Test-Conflicts: none` remains exact and no protected test enters
    Test-author ownership.
15. **Does a prior ruling answer the product or topology questions?** Yes.
    The existing [`detail-route ruling`](../rulings.md) supplies the exact
    event/job envelope and serialized server ownership. No new operator
    ruling is needed. The route remains unauthenticated same-origin loopback
    HTTP and cannot trigger an external send.

### Version 2 planning log

- 2026-08-07: Planner opened the test-first repair epoch after version 1
  spec-lint failed. Contract version 2 preserves all prior history, adds a
  byte-distinct non-matching event/job pair, makes correct relationship
  selection observable, asserts both route envelopes inside the read-only
  case, and closes sidecar-write observation with directory-entry snapshots.
  The Planner rechecked all sibling ownership declarations, every leased
  remote ticket branch, active worktrees, protected source assertions,
  fixture determinism and lifecycle, topology, `factory/rulings.md`, and
  global-text applicability. No operator ruling, protected-test conflict,
  global-literal check, or external label is required.

## Log

- 2026-08-07: Spec-lint round 2 against frozen contract version 2.

### Contract-element coverage (version 2)

| Contract element | Positive | Failure/empty | Duplicate/repeat | Out of scope |
|---|---|---|---|---|
| Route `GET /api/events/:id`, exactly one non-empty segment | AC1 `GET /api/events/event-detail` | AC2 `GET /api/events/event-missing` | AC3 re-issues both branches in one case | Trailing-slash, empty-segment, multi-segment, and percent-encoded ids unexercised (finding 2) |
| Event lookup by exact `id` equality | AC1 returns the first fixture event and asserts it is not `event-other` | AC2 returns `404` for a byte-distinct unknown id | AC3 repeats both | Case- and whitespace-variant ids equivalent to AC2 |
| Related-job lookup by `job.eventId === event.id` | AC1 requires the second job while the target event is first, and forbids deep-equality with `job-event-other`; `state.jobs[0]`, status-based, and index-paired selection all fail | AC2 never reaches job selection | AC3 asserts the exact success envelope again | Tie-break among multiple jobs for one event excluded by contract |
| Success envelope: HTTP `200`, exactly the keys `event` and `job` | AC1 exact status and key-set; AC3 exact whole-body equality | AC2 shows the envelope is not emitted for an unknown id | AC3 | Property order and insignificant whitespace explicitly not interface |
| Unprojected complete stored objects (no rename, default, or added field) | AC1 and AC3 deep-equal both complete fixture records; every asserted field is non-empty | — | — | — |
| Unknown event: HTTP `404`, exactly `{"error":"no such event"}` | — | AC2 exact status and deep-equal body with no extra key; AC3 repeats it | AC3 | Other absent ids equivalent |
| `Content-Type: application/json` on both branches | AC1 header | AC2 header | — | Charset parameter absent from the frozen literal, matching `json()` |
| Read-only in memory and over `GET /api/state` | AC3 deep-equal complete state before/after both requests | AC3 asserts `approvals` and `outbox` stay empty | AC3 covers one AC1 request and one AC2 request in one case | — |
| Read-only on `<DATA_DIR>/state.json` | AC3 byte-equality before/after | AC3 same case | — | — |
| Read-only on `<DATA_DIR>` entries (no create, rename, remove) | AC3 sorted entry names exactly `["state.json"]` before and after | AC3 same case | — | A `persist()` temp file `state.json.<hex>` is now observable |
| Malformed state (orphan event, multiple jobs for one event) | — | — | — | Explicitly out of contract; Relay-created state persists one event with exactly one job |
| `ui()` and `GET /` markup unchanged; no UI selector or accessible name | AC4 Builder diff leaves `ui()` byte-for-byte unchanged | — | — | No global-shell literal is introduced, so no readiness global-text evidence is owed |
| Authentication, cookies, CORS, cross-origin, deploy topology unchanged | AC1-AC3 run unauthenticated loopback HTTP against the existing server | — | — | `External: no`; no approval, outbox, or send path is reachable; the route exposes no record `GET /api/state` does not already expose unauthenticated |
| Fixture A lifecycle: fresh `DATA_DIR`, `WORKER_MS=60000`, `/health` gate, ports `4781-4783`, awaited SIGKILL and recursive removal | AC1-AC3 each own one port and one directory | AC3 observes state, bytes, and entries across the case | Three independent cases share no setup state | No database, parent row, foreign key, or reset helper exists |
| File ownership and test-first commit order | AC4 exact per-stage diffs and commit graph | AC4 forbids `app/package.json`, lockfile, existing-test, and other `factory/` changes | — | — |
| Verification commands | AC5 three exact exit statuses | — | — | — |

### Findings

1. SPEC-WARN: version 2 Fixture A's job order (`job-event-other` first while its
   event `event-other` is second) is not reachable through Relay intake, since
   `acceptEvent` pushes each event and its job together and nothing reorders
   `state.jobs`; the `receivedAt` values `12:00:00.000Z` / `12:00:01.000Z` imply
   the opposite order. This is not blocking — every asserted record value is
   exactly what the frozen initializers write, and any intake-producible
   two-record state has aligned indices, so index-paired implementations could
   not be detected without the shuffle. Have the Test-author state the deliberate
   shuffle in a fixture comment so a reviewer does not read it as a fixture
   defect.
2. SPEC-WARN: no criterion still exercises the frozen "exactly one non-empty
   segment" path rule (`/api/events`, `/api/events/`, `/api/events/a/b`, and a
   percent-encoded id). These are equivalent permutations of the existing `404`
   disposition against the server's generic `{"error":"not found"}` fallback; the
   Test-author may add them without a contract change.
3. SPEC-WARN: the header field `Fixture-Seams: app/server.js,
   app/tests/conformance.test.js` is broader than contract version 2, which
   states no existing test is edited and gives the Test-author only
   `app/tests/event-detail.test.js`. The seam is inert because no protected-test
   conflict exists, but the surplus `app/tests/conformance.test.js` entry could
   read as edit authority the contract does not grant.
4. SPEC-WARN: fourteen tickets (`T-114`, `T-118`, `T-122`, `T-126`, `T-130`,
   `T-134`, `T-138`, `T-142`, `T-146`, `T-150`, `T-154`, `T-158`, `T-162`,
   `T-166`) remain in `State: Ready` declaring the identical
   `app/server.js` / `app/tests/event-detail.test.js` ownership T-110 is
   executing; version 2's ambiguity item 13 records them but none is cancelled.
   None is leased, so this is not a live conflict, but ask the operator to cancel
   or supersede the stranded identities so one cannot be scheduled after T-110
   merges.

Verified independently this pass. Round 1's blocking finding is closed: the
target event is `events[0]` while its job is `jobs[1]`, and AC1 plus AC3 require
that pair and forbid the non-matching record, so `state.jobs[0]`, status
selection, and index pairing all fail. Both new job records are producible by the
frozen initializers in `app/server.js`: `job-event-other` matches
`"job-" + evt.id`, and `attempts: 6` with `lastError: "simulated failure on
attempt 6"`, `retries: 1`, and `attemptsSinceRetry: 3` are exactly what
`retryJob` (which zeroes `attemptsSinceRetry` and increments `retries` without
resetting `attempts`) followed by three more `processJob` calls at
`MAX_ATTEMPTS=3` writes with `payload.failTimes = 99`; the same trace is already
asserted by `app/tests/retry-dead.test.js` AC2. The target job's version 1 values
remain exact. `event-detail`, `event-other`, and `event-missing` are pairwise
byte-distinct literals and no invalid fixture is a transformation of a valid one.
Both fixture jobs are terminal, so the pending-only `workerTick` selects neither
and no `persist()` runs during a case; `WORKER_MS=60000` is an additional bound,
and AC3's expected entry list `["state.json"]` is therefore exact. Ports
`4781-4783` appear nowhere in the repository or in any leased branch; the current
fixed ports are `4719`, `4721-4722`, `4731-4733`, `4741-4745`, `4751-4756`,
`4761-4763`, `4771-4773`, and `4779` under parallel `node --test tests/*.test.js`.
There is no database, foreign key, generated identifier, sequence, counter, or
generated timestamp, so no lifecycle dependency closure is owed; each case owns
one temporary directory removed recursively in `finally`. Every static check in
the protected suite was enumerated and parsed: `app/tests/dead-jobs.test.js` AC5
and `app/tests/blocked-approvals.test.js` restrict module specifiers and
forbidden substrings of `app/tools/dead-jobs.js`, `app/tools/blocked-approvals.js`,
and `dead-jobs.test.js`'s own source only; `app/tests/state-summary.test.js` and
`app/tests/pending-approvals.test.js` snapshot only temporary fixture files; and
`app/tests/backend-readiness.test.js` asserts against `factory/backend-readiness.sh`
fixtures. None carries an import/export allowlist, exact-source assertion, or
source snapshot for `app/server.js`, whose only suite-wide reference is
`path.join(__dirname, "..", "server.js")` for spawning, and the package's
`tests/*.test.js` pattern admits the new file without a manifest edit. No
protected test makes a page-wide or `GET /` text assertion, no test references
`/api/events`, the contract adds no global-shell visible literal or accessible
name, and `"no such event"` is an API-only JSON value, so no `GLOBAL TEXT PASS`
evidence is owed and `Protected-Test-Conflicts: none` is exact. The envelope
`{"event":<stored event>,"job":<stored related job>}` and shared `app/server.js`
ownership match the recorded detail-route ruling in `factory/rulings.md` exactly;
no other ruling touches this contract. Product-doc names, paths, shapes, and
counts match `SPEC.md` behaviors 1-2 and 6-7, conformance cases 1-4 and 7, and the
current `app/server.js` state model. Baseline `npm test --prefix app` passes 66/66
at `72580e9`.

SPEC-LINT: PASS

## Log

- 2026-08-07: Test-author wrote the failing acceptance tests for frozen contract
  version 2 in `app/tests/event-detail.test.js` (the only Test-author-owned
  application path). No existing test, `app/server.js`, `app/package.json`,
  lockfile, or other `factory/` path was touched.

### Tests written

| Criterion | Test name | Asserted contract surface |
|---|---|---|
| AC1 | `AC1: GET /api/events/event-detail returns 200 JSON with exactly {event,job} deep-equal to the fixture's first event and second job, job.eventId === event.id, and neither non-matching record` | `200`, `Content-Type: application/json`, sorted key-set exactly `["event","job"]`, deep-equality with Fixture A's first event and second job, `job.eventId === event.id`, and non-equality with `event-other` / `job-event-other` |
| AC2 | `AC2: GET /api/events/event-missing returns 404 JSON deep-equal to {"error":"no such event"} with no additional keys` | `404`, `Content-Type: application/json`, body deep-equal to `{"error":"no such event"}` |
| AC3 | `AC3: the AC1 and AC2 requests leave /api/state, the state.json bytes, and the DATA_DIR entry names unchanged; approvals and outbox stay empty` | exact `200` success envelope and exact `404` missing envelope inside the case, then deep-equal `GET /api/state`, byte-equal `<DATA_DIR>/state.json`, sorted `DATA_DIR` entries exactly `["state.json"]` before and after, and empty `approvals` / `outbox` |

Fixture A version 2 is written verbatim to a fresh temporary `DATA_DIR`; each
case owns one port (`4781`, `4782`, `4783`), gates on `GET /health`, consumes
every response body, and in `finally` awaits `SIGKILL` exit and recursive
directory removal. Per round-2 finding 1, the deliberate event/job index shuffle
is documented in a fixture comment so it does not read as a fixture defect.

### Confirmed failing for the right reason

`node --test app/tests/event-detail.test.js` exits `1` with 3 failed / 0 passed.
AC1 and AC3 fail `404 !== 200` and AC2 fails with actual `{"error":"not found"}`
— the server's generic fallback, because `GET /api/events/:id` does not exist.
No failure came from setup: every child server started and passed the `/health`
gate before the acceptance requests.

### Criteria that are not expressible as tests (demo-check items)

- **AC4** — commit ordering and per-stage diff boundaries are properties of the
  Git history, not of a runtime assertion. Demo-check the commit graph and each
  stage's `git diff --name-only`, including that `ui()` is byte-for-byte
  unchanged and that no lockfile or `app/package.json` edit exists.
- **AC5** — the three exit statuses are process-level results. Demo-check
  `node --test app/tests/event-detail.test.js`, `npm test --prefix app`, and
  `.github/scripts/test-immutability-check.sh` each exiting `0` after
  implementation.
- Round-2 SPEC-WARN 2 (non-matching path shapes `/api/events`, `/api/events/`,
  `/api/events/a/b`, percent-encoded id) was deliberately not encoded. Those
  paths resolve to the server's existing generic `{"error":"not found"}` `404`
  both before and after implementation, so such tests would pass against an
  empty implementation and are excluded as trivially-passing.

- 2026-08-07: Builder implemented frozen contract version 2 in
  `app/server.js` without touching tests: `GET /api/events/:id` now returns
  the complete stored event and its related job, or the exact unknown-event
  response, without mutation or persistence. Targeted verification
  `node --test app/tests/event-detail.test.js` passed 3/3, and
  `node --check app/server.js` passed. No scoped lint or typecheck command
  exists; `npm test --prefix app` and
  `.github/scripts/test-immutability-check.sh` are deferred to protected CI
  and final certification under the Builder v6 verification boundary.

  PR description: Adds the read-only event detail route and exact 404
  response. Touches `app/server.js` and this role-owned ticket-log append. No
  concerns.
